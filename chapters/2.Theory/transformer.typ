#import "/template/template.typ": *

=== 从相机里的目标说起：为什么需要一串坐标系

上一章的 PnP 走到终点，交出的答案是"目标在*相机坐标系*下的位姿"：比如目标在相机前方 3 m、偏右 0.4 m。看起来万事俱备——瞄过去打就是了？

慢着。相机装在云台上，而云台在转。"目标在相机正前方 3 m"这句话带着一个隐藏的时效：它只在快门按下的那一瞬间成立。云台往左甩 30°，同一个纹丝不动的目标，在相机系里的坐标就完全变了。你要预测目标半秒后的位置，却把它的坐标记录在一个自己都在打转的坐标系里——这就像坐在旋转木马上给场外的朋友画运动轨迹，画出来的全是圆。

所以一台自瞄机器人至少要同时维护这几个坐标系，每一个都有不可替代的存在理由：

- *相机系（camera）*——测量的原生家园。PnP 的输出天然表达在这里，别无选择：相机只知道自己看到了什么。
- *云台系（gimbal）*——执行的家园。最终发给电控的指令是云台的 yaw 和 pitch 角，"往哪转"必须相对云台自己的轴来说。
- *惯性系（odom）*——预测与弹道的家园。它固定在场地上（严格说：固定在开机时刻的位置），云台怎么转它都不动。目标的运动规律、重力的方向，只有在这里才是简洁的。上一章结尾说要把位姿搬进的"世界系"，在自瞄链路里就由它担任。

底盘导航还会引入自己的坐标系（哨兵的路径规划就活在里面），但自瞄链路的主角是上面三个。本章的任务，就是把"同一个点在不同坐标系下的坐标如何换算"这件事讲透：先攻克旋转——它是变换里唯一有难度的部分，一口气看完它的四种数学语言；再加上平移得到完整的刚体变换；最后把 camera → gimbal → odom 这条链串起来，回答一个贯穿后半本书的问题：为什么预测必须在惯性系里做。

#definition[
#set par(first-line-indent: 0pt)
本章的记号约定（全章严格遵守）：

- $bold(p)_A$ 表示同一个空间点 $p$ 在 $A$ 系下的坐标；
- $bold(R)_(A B)$ 表示把 $B$ 系坐标换算成 $A$ 系坐标的旋转矩阵：$bold(p)_A = bold(R)_(A B) thin bold(p)_B$；
- $bold(T)_(A B)$ 同理，是含平移的完整变换：$bold(p)_A = bold(T)_(A B) thin bold(p)_B$；
- 链式复合时相邻脚标"相消"：$bold(T)_(A C) = bold(T)_(A B) thin bold(T)_(B C)$。

读法口诀：脚标从右往左念，"从 $B$ 到 $A$"。
]

=== 旋转矩阵与 SO(3)

==== 三根基向量去了哪里

旋转到底在做什么？一个坐标系由三根互相垂直的单位基向量 $hat(bold(x)), hat(bold(y)), hat(bold(z))$ 撑起来。旋转之后，每根基向量都指向了新的方向。把三根新基向量（在原坐标系下的坐标）按列排成一个 $3 times 3$ 矩阵，这个矩阵*就是*旋转矩阵：

$
bold(R) = mat(bold(r)_1, bold(r)_2, bold(r)_3)
$

其中 $bold(r)_1$ 是 $hat(bold(x))$ 去的地方，$bold(r)_2$ 是 $hat(bold(y))$ 去的地方，$bold(r)_3$ 是 $hat(bold(z))$ 去的地方。旋转不改变长度和夹角，所以这三列必须仍然是互相垂直的单位向量。把这个要求写成矩阵语言，就是旋转矩阵的定义：

$
bold(R)^top bold(R) = bold(I), quad op("det")(bold(R)) = +1
$

第一个条件（正交性）说的正是"列是互相垂直的单位向量"；第二个条件把镜像反射排除在外——把右手系照成左手系的变换行列式是 $-1$，它保长度保夹角，却不是任何真实转动能实现的。满足这两个条件的矩阵全体记作 $"SO"(3)$（特殊正交群），这就是"所有三维旋转"的数学名字。

现在可以回答一个经典问题：*旋转矩阵有 9 个数，为什么只有 3 个自由度？*数一数约束就行：三列各是单位向量，3 个约束；三列两两正交，又是 3 个约束。$9 - 6 = 3$，与直觉一致——你只需要 yaw、pitch、roll 三个角就能摆出任何姿态。9 个数里有 6 个是"冗余"的，代价是必须时刻维持 6 个约束（浮点误差积累后矩阵会慢慢"不正交"，修复它要做正交化，远不如后文四元数归一化来得干脆）；回报是一条极好用的性质：

$
bold(R)^(-1) = bold(R)^top
$

求逆只需转置，一次乘法都不用。这条性质直接来自 $bold(R)^top bold(R) = bold(I)$。

==== 一个矩阵，两种读法

$bold(R) bold(v)$ 这个乘法有两种解读，几乎每个人第一次都会搞混：

- *主动读法*：坐标系不动，把向量 $bold(v)$ 抓起来转到新方向；
- *被动读法*：向量根本没动，$bold(R)$ 只是把它的坐标从一个坐标系换算到另一个坐标系。

两种读法都对，用哪种取决于场景。本章做坐标变换，用的全是被动读法，也就是记号约定里的 $bold(p)_A = bold(R)_(A B) thin bold(p)_B$。此时 $bold(R)_(A B)$ 的列有一个可以救命的具体含义：第 $i$ 列就是 $B$ 系第 $i$ 根基向量在 $A$ 系下的坐标。调试时算一列出来对一对，立刻知道矩阵是不是你想要的那个、方向有没有搞反。

绕三根坐标轴的基本旋转矩阵是一切旋转表示的原材料（角度正方向按右手定则：拇指指向转轴正向，四指弯曲的方向为正）：

$
bold(R)_x (theta) = mat(1, 0, 0; 0, cos theta, -sin theta; 0, sin theta, cos theta), quad
bold(R)_y (theta) = mat(cos theta, 0, sin theta; 0, 1, 0; -sin theta, 0, cos theta), quad
bold(R)_z (theta) = mat(cos theta, -sin theta, 0; sin theta, cos theta, 0; 0, 0, 1)
$

注意 $bold(R)_y$ 里两个 $sin theta$ 的正负位置和另外两个相反——不是笔误，是为了让三个矩阵的正方向都符合同一个右手定则。

==== 复合与不可交换

两次旋转怎么合成？矩阵乘法：

$
bold(R)_"总" = bold(R)_2 bold(R)_1
$

*从右往左读*：先做 $bold(R)_1$，再做 $bold(R)_2$。这个顺序和脚标相消的链式记号是自洽的：$bold(R)_(A C) = bold(R)_(A B) bold(R)_(B C)$，作用在 $bold(p)_C$ 上时最靠右的先干活。

矩阵乘法不可交换，旋转也不可交换——这不是数学的洁癖，拿起手机就能验证：屏幕朝上，先绕竖直轴转 90° 再绕手机长轴翻 90°，和反过来做，最终姿态完全不同。用基本旋转矩阵算一遍：$bold(R)_z (90°) bold(R)_x (90°)$ 把 $x$ 轴单位向量送到 $(0, 1, 0)$，而 $bold(R)_x (90°) bold(R)_z (90°)$ 把它送到 $(0, 0, 1)$——一个指向侧方，一个指向天上。写坐标变换代码时乘反顺序，是这个领域出镜率最高的 bug，而且编译器毫无怨言，程序照常运行，只是打不中。

=== 欧拉角：最直观的表示，和它的代价

==== 云台本身就是一台欧拉角机器

用三个角度描述姿态，是最符合人类直觉的方案：先绕 $z$ 轴偏航（yaw，$psi$），再绕新的 $y$ 轴俯仰（pitch，$theta$），再绕更新的 $x$ 轴横滚（roll，$phi.alt$）。RM 机器人的云台就是这套描述的机械实体：yaw 电机的轴竖直、pitch 电机的轴水平，枪管姿态天然就是"yaw 角 + pitch 角"（roll 恒为零）。电控和视觉之间来回传的云台姿态，最常见的形式正是这两个角。按上面的顺序把基本旋转矩阵串起来：

$
bold(R) = bold(R)_z (psi) thin bold(R)_y (theta) thin bold(R)_x (phi.alt)
$

从旋转矩阵反解欧拉角也有现成公式（设 $bold(R) = [r_(i j)]$）：

$
theta = -arcsin(r_(3 1)), quad phi.alt = op("atan2")(r_(3 2), r_(3 3)), quad psi = op("atan2")(r_(2 1), r_(1 1))
$

这里必须用 $op("atan2")$ 而不是 $arctan$：$arctan$ 只吃一个比值，分不清 $(+, +)$ 和 $(-, -)$ 两个象限；$op("atan2")(y, x)$ 同时看两个参数的符号，在完整的 $(-pi, pi]$ 范围内给出正确角度。凡是从坐标反解角度的场合——包括后文自瞄链末端解算云台指令——一律用它。

但欧拉角有一个绕不开的坑：*约定太多*。三次旋转的轴序有 12 种选择（ZYX、ZXY、XYZ……），每种又分"绕转动后的新轴转"（内旋）和"绕固定的原轴转"（外旋），共 24 种约定。上面写的是 ZYX 内旋，也是机器人与航空领域的主流约定，它恰好等价于 XYZ 外旋——这就是为什么公式里矩阵从右往左是 $x$、$y$、$z$ 的顺序。报欧拉角不报约定，等于报数字不报单位：同样一组 $(30°, 20°, 10°)$，按不同约定摆出来的姿态天差地别。跨设备、跨库对接时（电控的姿态解算、Eigen 的 `eulerAngles`、可视化工具），第一件事就是把约定对齐。

==== 万向锁：三个参数的宿命

欧拉角最著名的失效模式是万向锁（gimbal lock）。把 $theta = 90°$ 代入 ZYX 的复合公式，$cos theta = 0$、$sin theta = 1$，展开整理（用到和差角公式）后旋转矩阵塌缩成：

$
bold(R) = mat(0, -sin(psi - phi.alt), cos(psi - phi.alt); 0, cos(psi - phi.alt), sin(psi - phi.alt); -1, 0, 0)
$

盯着看一眼：$psi$ 和 $phi.alt$ 只以差值 $psi - phi.alt$ 的形式出现。你能确定它们的差，却无法分别确定各自的值——三个参数里只剩两个在起作用，姿态空间在这一点丢了一个自由度。物理图景同样直白：俯仰 90° 后，原来竖直的偏航轴被"抬"得和横滚轴指向了同一根物理轴，转 yaw 和转 roll 变成了同一件事（$theta = -90°$ 时同理，塌缩成只剩 $psi + phi.alt$）：

#figure(
  image("images/theory-transform-gimbal-lock.png", width: 100%),
  caption: [万向锁的直接演示（图中彩色箭头为机体系基向量，灰色细箭头为世界系参考，图由 `examples/theory/transform_gimbal_lock.py` 生成）。左：俯仰 $-90°$（机头竖直朝上，即正文里塌缩成 $psi + phi.alt$ 的那种情形）的基准姿态。中：在此基础上多转 25° 的 yaw。右：改为多转 25° 的 roll——最终姿态与中图*逐元素完全相同*（数值验证差值在 $10^(-16)$ 量级）。yaw 和 roll 此刻绕的是同一根物理轴，两个参数只能合力提供一个自由度。],
)

值得强调的是：万向锁不是欧拉角设计得笨，而是"只用三个数"的宿命。可以证明，任何三参数的旋转表示都无法做到既覆盖所有姿态、又处处光滑无奇异——$"SO"(3)$ 的整体形状是一个"绕回自身"的紧致弯曲空间，和 $RR^3$ 的形状本质不同，硬要用三个数铺满它，接缝处必然出问题。欧拉角把问题埋在俯仰 ±90°，下一节的旋转向量把问题埋在转角 $pi$ 处——换表示只能挪动奇异点，消灭它得靠第四个数（四元数一节揭晓）。

==== 万向锁什么时候真的咬人

教材讲到这里通常就收工了，但工程上更要紧的问题是：它*什么时候*咬人？先说结论：对 RM 地面机器人，直接读云台的 yaw/pitch 角来用，基本咬不到你——云台俯仰被机械限位约束在 ±35° 上下的量级，离 90° 远得很。阿波罗飞船当年是真的会锁（它的三框架惯性平台一旦框架对齐就丢失姿态基准，宇航员的操作规程就是绕开那个姿态），但你的步兵不会抬头看天。

真正会咬人的，是下面这些把欧拉角*当数学对象用*的场合：

- *拿欧拉角做积分或滤波状态*。把机体角速度换算成欧拉角变化率的公式里含有 $tan theta$ 和 $1 \/ cos theta$ 项，俯仰远未到 90° 时放大效应就已开始（60° 时系数已经翻倍），姿态要求任意的场合（空中机器人的大机动）会直接数值爆炸。这就是为什么 IMU 姿态解算和 SLAM 的内部状态不用欧拉角。
- *拿欧拉角做插值*。对两组欧拉角逐分量线性插值，中间姿态会走不匀速的弯路，平滑云台轨迹时表现为莫名的抖动。
- *角度回绕*。yaw 从 179° 转到 −179°，数值上跳了 358°，实际只转了 2°——这不是万向锁，但同属"三个数塞进弯曲空间"的接缝问题。把目标朝向角放进滤波器状态时（下一章整车观测正要这么做），不处理回绕，一次跨越 ±180° 就能把滤波器打飞。

一句话总结：欧拉角是很好的人机接口，是很差的计算载体。显示、调参、发云台指令用它；内部运算，请往下读。

=== 轴角与旋转向量：OpenCV 的 rvec

欧拉角把一次旋转拆成三步，但欧拉旋转定理（Euler's rotation theorem）给出一个更漂亮的事实：*任何*三维旋转，不管多复杂，都等价于绕某一根固定轴 $hat(bold(n))$ 转一个角 $theta$——三步坍缩成一步。于是姿态可以用"一根单位轴 + 一个角"描述，这就是轴角表示。

绕轴转动的显式公式靠几何分解就能推出来。把待旋转的向量 $bold(v)$ 拆成平行于轴与垂直于轴的两部分：平行分量 $hat(bold(n)) (hat(bold(n)) dot bold(v))$ 在旋转中纹丝不动；垂直分量在垂直于轴的平面里转过 $theta$，落在"原方向"与"叉积方向 $hat(bold(n)) times bold(v)$"张成的圆上。三部分拼起来就是罗德里格斯旋转公式（Rodrigues' rotation formula）：

$
bold(v)' = bold(v) cos theta + (hat(bold(n)) times bold(v)) sin theta + hat(bold(n)) (hat(bold(n)) dot bold(v)) (1 - cos theta)
$

把它整理成矩阵形式（利用反对称矩阵 $[hat(bold(n))]_times$ 实现叉积：$[hat(bold(n))]_times bold(v) = hat(bold(n)) times bold(v)$）：

$
bold(R) = bold(I) cos theta + (1 - cos theta) hat(bold(n)) hat(bold(n))^top + sin theta [hat(bold(n))]_times, quad
[hat(bold(n))]_times = mat(0, -n_z, n_y; n_z, 0, -n_x; -n_y, n_x, 0)
$

工程上更常用它的紧凑打包：把轴和角乘在一起，得到*旋转向量* $bold(r) = theta hat(bold(n))$——方向是转轴，模长是转角，三个数装下全部信息。你其实早就见过它：OpenCV 的 `solvePnP` 返回的 `rvec` 就是旋转向量，而 `cv::Rodrigues` 这个函数名下干的就是上面这条公式（以及它的逆运算）。上一章 PnP 解出的装甲板姿态，交到你手里时就是这个形式。

旋转向量的奇异点在哪？$theta = 0$ 时轴的方向无定义，但这无害——公式在 $theta arrow 0$ 时平滑地退化为 $bold(I)$，谁当轴都一样。真正的接缝在 $theta = pi$：绕 $hat(bold(n))$ 转 $pi$ 和绕 $-hat(bold(n))$ 转 $pi$ 是同一个旋转，两个相反的旋转向量对应同一姿态，靠近这里时表示会突变。好在描述单次旋转只需 $theta in [0, pi]$，作为"计算的中转格式"它非常称职；不称职的是当滤波状态或做长链复合——两个旋转向量之间没有简洁的复合公式，得转回矩阵绕一圈。

顺带给有兴趣的读者留一句话：旋转向量正是李群理论里 $"SO"(3)$ 的对数坐标，矩阵指数 $bold(R) = exp([theta hat(bold(n))]_times)$ 就是 Rodrigues 公式的另一个名字。视觉里程计与 SLAM 的优化全靠这套语言，顺着"李群、李代数"四个字深挖即可，本书不展开。

=== 四元数：把旋转打包成四个数

==== 半角、三明治与双覆盖

轴角还剩最后一个痛点没解决：复合。两个轴角对 $(hat(bold(n))_1, theta_1)$、$(hat(bold(n))_2, theta_2)$ 连着转，合成的轴和角是多少？没有简洁公式。四元数就是为此而生的打包格式——它把轴角信息装进四个数：

$
bold(q) = vec(q_w, q_x, q_y, q_z) = vec(cos(theta \/ 2), sin(theta \/ 2) thin hat(bold(n)))
$

$q_w$ 叫标量部分，$(q_x, q_y, q_z)$ 叫向量部分，整体满足单位模长约束 $q_w^2 + q_x^2 + q_y^2 + q_z^2 = 1$。四个数、一个约束，自由度恰好是 3。

为什么多一个数就没有奇异了？想想地球仪：用经纬度两个数描述球面，两极必然出毛病（北极点的经度无定义）；改用三维单位向量描述同一个球面——三个数加一个模长约束——处处光滑，没有任何特殊点。四元数做的是同一件事，高一维：三参数表示 $"SO"(3)$ 必有接缝（上文的宿命），而单位四元数活在四维空间的单位球面上，把整个旋转空间无褶皱地铺开。

四元数的乘法（Hamilton 积）定义为（记 $bold(p) = (p_w, bold(p)_v)$，$bold(q) = (q_w, bold(q)_v)$）：

$
bold(p) times.o bold(q) = vec(p_w q_w - bold(p)_v dot bold(q)_v, p_w bold(q)_v + q_w bold(p)_v + bold(p)_v times bold(q)_v)
$

注意向量部分里那个叉积——它让四元数乘法不可交换。理应如此：旋转本身不可交换，忠实表示它的代数也不能交换。先转 $bold(q)_1$ 再转 $bold(q)_2$，复合就是一次乘法：

$
bold(q)_"总" = bold(q)_2 times.o bold(q)_1
$

和旋转矩阵一样从右往左读——轴角复合的痛点就此消失。单位四元数的逆就是共轭（向量部分取反）：$bold(q)^(-1) = bold(q)^* = (q_w, -bold(q)_v)$，含义是反向旋转。用四元数旋转一个向量，则要把 $bold(v)$ 写成标量部分为零的"纯四元数"，从两侧夹击：

$
vec(0, bold(v)') = bold(q) times.o vec(0, bold(v)) times.o bold(q)^*
$

这个"三明治"结构也回答了定义里最扎眼的问题：*为什么是 $theta \/ 2$ 而不是 $theta$？一种直观的理解：三明治让 $bold(q)$ 从向量的两侧各作用一次，每侧贡献一半转角，两半合出完整的 $theta$。半角带来一个著名的副作用：把四个分量全部取反，相当于给 $theta$ 加上 $2 pi$——转一整圈回到原地，所以 $bold(q)$ 和 $-bold(q)$ 表示同一个旋转*，这叫双覆盖（double cover）。它不是缺陷，但要求你在比较、插值、滤波两个四元数之前先对符号：若 $bold(q)_1 dot bold(q)_2 < 0$，先把其中一个整体取反，否则算法会"绕远路"走大圆的另一半。插值本身留一句话即可：球面线性插值（slerp）能在两个姿态之间以恒定角速度平滑过渡，是四元数独有的福利。

==== 工程细节：归一化、效率与存储顺序

四元数在实际系统里几乎是姿态的默认载体（飞控、SLAM、游戏引擎清一色），除了无奇异，还有几笔实惠账：

- *约束好维护*。浮点误差积累后模长会漂离 1，修复只需除以模长一次；旋转矩阵漂了要重新正交化 6 个约束，麻烦得多。
- *复合便宜*。一次 Hamilton 积 16 次乘法 12 次加法，一次 $3 times 3$ 矩阵乘法 27 次乘法 18 次加法。姿态的存储（4 个数对 9 个数）与长链复合，四元数全面占优。
- 但*变换大量点时矩阵更快*：三明治积要做两次 Hamilton 积，而 $bold(R) bold(v)$ 只要 9 次乘法。所以成熟代码的习惯是姿态用四元数存着，需要批量变换点时先转成矩阵再乘——两种表示各干各的强项。

"先转成矩阵"用的公式顺手写在这里（把三明治积逐项展开、按 $bold(v)$ 的系数归并就能得到，全程只有代数）：

$
bold(R)(bold(q)) = mat(
1 - 2(q_y^2 + q_z^2), 2(q_x q_y - q_w q_z), 2(q_x q_z + q_w q_y);
2(q_x q_y + q_w q_z), 1 - 2(q_x^2 + q_z^2), 2(q_y q_z - q_w q_x);
2(q_x q_z - q_w q_y), 2(q_y q_z + q_w q_x), 1 - 2(q_x^2 + q_y^2)
)
$

不含任何三角函数——四元数分量里已经"预存"了半角的正余弦，这也是它在嵌入式端受欢迎的原因之一。至此四种表示的互转路径全部打通：欧拉角按定义拼基本旋转、轴角经 Rodrigues 公式即得矩阵；矩阵反解欧拉角的公式前文已给，反解旋转向量则由 `cv::Rodrigues` 代劳；轴角进四元数是定义本身。

最后一个必须点名的坑：*存储顺序*。数学上惯写 $(w, x, y, z)$（标量在前），但 Eigen 的 `Quaterniond` 构造函数按 $(w, x, y, z)$ 收参数、内部与 `coeffs()` 却按 $(x, y, z, w)$ 存放——两种顺序在同一个库里并存。视觉和电控之间用串口协议传 IMU 四元数时，协议文档里不写清顺序，装出来的姿态会错得离谱且不报任何错。姿态相关的接口边界上，四个数的顺序永远值得用一行注释写死。Eigen 的四元数、轴角、矩阵互转 API 在基础篇 Eigen 一节都演示过，此处不再重复。

=== 选型：四种表示各用在哪

四种语言各自登场完毕，把账摊开算一次总账：

#figure(
  table(
    columns: (auto, auto, auto, auto, 1fr),
    align: (left, center, center, center, left),
    stroke: (x, y) => (
      top: if y == 1 or y == 0 { 1pt } else { 0pt },
      bottom: if y == 4 { 1pt } else { 0pt },
    ),
    inset: 7pt,
    [*表示*], [*参数量*], [*约束数*], [*奇异性*], [*复合与典型场合*],
    [旋转矩阵], [9], [6], [无], [矩阵乘法；批量变换点、推导雅可比],
    [欧拉角], [3], [0], [俯仰 ±90° 万向锁], [无简洁公式；人机界面、云台指令、调参显示],
    [旋转向量], [3], [0], [$theta = pi$ 处不连续], [无简洁公式；OpenCV 接口（rvec）、优化中的局部增量],
    [四元数], [4], [1], [无（但 $plus.minus bold(q)$ 双覆盖）], [Hamilton 积；姿态存储、滤波、插值],
  ),
  caption: [旋转表示选型决策表。参数量减约束数恒等于 3——自由度不会变，变的只是把冗余和奇异安排在哪里。读表规则：顺着"你要拿它干什么"选行，而不是挑一个"最好的"——成熟系统四种全用，在边界处互转。],
)

工程原则一句话：存储与滤波用四元数，批量算点用矩阵，人机交互用欧拉角，OpenCV 边界用 rvec；四者互转全有闭式公式和现成库函数，真正要小心的是每次转换时把约定（轴序、内旋外旋、四元数存储顺序、角度单位）写明白。姿态 bug 十有八九不在数学，在约定。

=== SE(3)：把平移带上

==== 平移为什么进不了 3×3 矩阵

两个坐标系之间除了转，还有挪：完整的刚体变换是"旋转 + 平移"，$bold(p)' = bold(R) bold(p) + bold(t)$。能不能像旋转一样，把整个变换写成一个矩阵乘法？直接用 $3 times 3$ 矩阵不行，原因很本质：矩阵乘法是线性变换，线性变换必须把零向量映到零向量——原点不许动。而平移干的恰恰就是搬原点。

解决办法是上一章投影链里已经用过的那个便宜戏法——齐次坐标：给坐标补一个恒为 1 的第四维，让平移发生在新增的维度上：

$
vec(bold(p)', 1) = mat(bold(R), bold(t); bold(0)^top, 1) vec(bold(p), 1) = bold(T) vec(bold(p), 1)
$

把右边按分块乘法展开，第一行正是 $bold(R) bold(p) + bold(t)$——一次矩阵乘法同时完成旋转和平移。这样的 $4 times 4$ 矩阵 $bold(T)$ 叫齐次变换矩阵，全体记作 $"SE"(3)$（特殊欧氏群），自由度 $3 + 3 = 6$：三个转、三个挪，正是"位姿"二字的完整内涵。上一章相机外参的 $[bold(R) | bold(t)]$ 写法，就是它去掉底行的紧凑形式。

==== 变换链：脚标相消

把变换写成矩阵的全部动机，是让*链式复合变成连乘*。本章开头的记号约定在这里兑现威力：

$
bold(p)_A = bold(T)_(A B) thin bold(T)_(B C) thin bold(p)_C
$

相邻的 $B$ 像多米诺一样相消，链条随便加长，脚标对得上就不会错。这个记号纪律值得严格执行到代码里——把变量命名为 `T_gimbal_camera`（对应 $bold(T)_(G C)$），乘错顺序在代码评审时一眼就能看出来。基础篇 Eigen 一节的 `CoordinateTransformer` 类用 `Isometry3d` 实现的正是这套连乘。

求逆同样有闭式解，但要小心一个高频错误。对 $bold(p)_A = bold(R) bold(p)_B + bold(t)$ 反解出 $bold(p)_B = bold(R)^top bold(p)_A - bold(R)^top bold(t)$，所以：

$
bold(T)^(-1) = mat(bold(R)^top, -bold(R)^top bold(t); bold(0)^top, 1)
$

不是把 $bold(t)$ 取个负号完事——平移部分是 $-bold(R)^top bold(t)$，旋转和平移在求逆时是耦合的。手写逆变换时把这一项写成 $-bold(t)$，得到的变换在旋转为零时碰巧正确，一旦云台转起来就开始漂，是又一个"编译通过、静静打偏"的经典。

=== 自瞄坐标系链：camera → gimbal → odom

==== 链上的三环

现在把全章组装起来。自瞄的核心链路只有一行公式：

$
bold(p)_O = bold(T)_(O G) (t) thin bold(T)_(G C) thin bold(p)_C
$

从右往左读，每一环各有来历：

- $bold(p)_C$：目标在相机系的位置，上一章 PnP 的输出，每帧图像刷新一次。
- $bold(T)_(G C)$：相机系到云台系，*固定不变*——相机焊在云台上，跟着一起转。它包含两部分：一是安装位置的小平移（相机不在枪管轴线上，差着几厘米）；二是一次纯粹的*轴重排*——OpenCV 的相机光学系约定 $z$ 朝前、$x$ 朝右、$y$ 朝下，而机器人惯用的机体系约定 $x$ 朝前、$y$ 朝左、$z$ 朝上（ROS 的 REP-103 标准），两套约定之间隔着一个换轴旋转，忘了它是新手把目标解算到天上去的头号原因。$bold(T)_(G C)$ 的数值靠外参/手眼标定测出，实操流程在实战技术篇「相机标定与手眼标定」一章。它标歪的症状很有辨识度：静止目标在惯性系的坐标会随云台转动画圈——因为固定环节的误差被时变的 $bold(T)_(O G) (t)$ 转着圈搬运。
- $bold(T)_(O G) (t)$：云台系到惯性系，*随时间变化*，旋转部分来自 IMU 的姿态四元数（电控以 1 kHz 量级解算并下发，这正是上一节存储顺序警告的主战场）。odom 系原点定在开机时刻的位置，yaw 会随 IMU 积分缓慢漂移，但一发弹丸从出膛到命中不过零点几秒，这个时间尺度内它是足够好的惯性系。

把两个旋转环节写具体，全章的知识点会在这里会师。轴重排那一步不用标定就能写出来：光学系的 $z$（前）要变成机体系的 $x$，光学系的 $x$（右）是机体系的 $-y$，光学系的 $y$（下）是机体系的 $-z$——按"第 $i$ 列是 $C$ 系第 $i$ 根基向量在 $G$ 系下的坐标"的读法逐列填出：

$
bold(R)_(G C)^"轴重排" = mat(0, 0, 1; -1, 0, 0; 0, -1, 0)
$

它正交、行列式为 $+1$，是一个如假包换的旋转（安装偏差再在它上面叠一个小旋转）。而云台若用编码器角度而非四元数上报姿态，$bold(R)_(O G) = bold(R)_z (psi_"yaw") thin bold(R)_y (theta_"pitch")$——正是欧拉角一节的 ZYX 复合去掉 roll，基础篇 `updateGimbalPose` 那两行 `AngleAxisd` 连乘写的就是它。

==== 为什么预测必须在惯性系

链条既然通了，预测放在哪个坐标系做？这是本章真正要你带走的结论，值得从数学上看清楚。设目标在惯性系静止：$bold(p)_O$ 是常向量。它在相机系里的坐标是：

$
bold(p)_C (t) = bold(T)_(C G) thin bold(T)_(G O) (t) thin bold(p)_O
$

$bold(T)_(G O) (t)$ 随云台转动而变，所以 *$bold(p)_C (t)$ 是时变的——目标明明没动，它在相机系的坐标却在跑*。跑多快？坐标的变化率由云台角速度乘以距离给出，量级是 $omega r$：云台以 2 rad/s 的角速度扫过（自瞄跟随中很平常的速度），3.1 m 外的静止目标在相机系里的"速度"高达约 6.2 m/s——比步兵底盘全速狂奔的 3～4 m/s 还快。假运动比真运动更大，这就是致命之处：

#figure(
  image("images/theory-transform-inertial-frame.png", width: 100%),
  caption: [静止目标在两个坐标系里的对比。左：俯视图，机器人原点处云台在 0.5 s 内匀速扫过约 57°，目标（星号）在 odom 系纹丝不动。右：同一个目标的坐标随时间的变化——odom 系下是两条水平虚线（蓝），相机系下（红实线）却以最高约 6.2 m/s 的表观速度大幅滑动。读图规则：红线的斜率全部来自云台自转，与目标运动无关；若把滤波和预测建在相机系上，这份斜率会被当成目标速度喂进滤波器。生成脚本 `examples/theory/transform_inertial_frame.py`。],
)

如果把下一章的卡尔曼滤波直接架在相机系上，滤波器会把这 6.2 m/s 的假速度当成目标真实速度来外推，预测点被甩向切向；更糟的是它构成正反馈——你根据错误预测转动云台，云台转动又制造新的假速度。而在惯性系里，云台怎么转，静止目标的坐标就是不动，滤波器看到的每一点速度都是目标真实的速度，"目标近似匀速运动"这类运动模型假设也只有在这里才成立。所以正确的数据流是：每帧把 PnP 结果立刻变换到 odom 系，滤波、预测全部在 odom 系完成，最后把预测出的击打点变换回云台系解算指令。回程很短：击打点 $bold(p)_G = bold(T)_(G O) bold(p)_O$，然后 yaw $= op("atan2")(y, x)$、pitch 由几何加弹道给出（基础篇 `calculateAimAngles` 是它的裸几何版本）。

弹道这一步同样离不开惯性系：弹丸在飞行中被重力拉出抛物线，补偿计算需要"重力方向"这个输入——它在 odom 系里是恒定的 $-z$ 方向，在相机系里却随云台姿态任意打转。重力方向恒定，是弹道解算（实战技术篇专章展开）必须在惯性系进行的全部理由。

==== 时间戳：链上最隐蔽的一环

链式公式里那个不起眼的 $(t)$ 藏着最后一个陷阱：$bold(T)_(O G) (t)$ 必须取*图像曝光那一瞬间*的云台姿态。相机曝光、图像传输、识别计算，一路走下来几十毫秒过去了；IMU 姿态以 1 kHz 刷新，"到货最新"的那个四元数对应的是*现在*的云台，不是快门按下时的云台。图省事直接取最新姿态，误差有多大？云台以 3 rad/s 转动时，10 ms 的时刻错位就是 0.03 rad ≈ 1.7° 的姿态误差，投到 4 m 外就是约 12 cm 的位置偏差——小装甲板两灯条中心相距约 135 mm，半宽不到 7 cm，这一下就是整板脱靶。云台转得越快、目标越远，这笔账越狠，而它在静止调试时完全隐身——静止时任何时刻的姿态都一样，时间戳 bug 只在运动中现形。

解法的思想一句话就能说完：给每帧图像打上曝光时刻的时间戳，拿它去 IMU 姿态队列里插值出当时的姿态再上链；工程实现（硬件触发、时钟同步、队列插值）留给实战技术篇「时间戳对齐」一章。

==== 本章小结

- 多坐标系不是繁文缛节：相机系是测量的家、云台系是执行的家、惯性系是预测和弹道的家，各司其职。
- 旋转矩阵 9 个数 6 个约束 3 个自由度，$bold(R)^top bold(R) = bold(I)$ 且 $op("det")(bold(R)) = +1$；求逆即转置；复合从右往左，不可交换。
- 欧拉角最直观但约定繁多；俯仰 ±90° 处两个转轴对齐、丢一个自由度（万向锁），且任何三参数表示必有奇异——它是好界面、坏载体。
- 旋转向量 $theta hat(bold(n))$ 经 Rodrigues 公式与矩阵互转，OpenCV 的 rvec 就是它。
- 四元数用"四个数一个约束"换来处处无奇异，Hamilton 积复合，共轭即求逆；$bold(q)$ 与 $-bold(q)$ 同旋转（双覆盖），插值用 slerp，接口处认准存储顺序。
- $"SE"(3)$ 齐次矩阵把旋转平移合成一次乘法，链式复合脚标相消，求逆的平移部分是 $-bold(R)^top bold(t)$。
- 自瞄链 $bold(p)_O = bold(T)_(O G) (t) thin bold(T)_(G C) thin bold(p)_C$：固定环节靠标定，时变环节靠 IMU 并以曝光时刻取值；滤波与预测必须在惯性系做——云台一转，相机系里的静止目标就有了比真车还快的假速度。

目标的位置序列已经安放进不随你转动的坐标系，接下来的问题自然浮现：这串带噪声的位置，怎么变成对下一时刻的最优预测？这正是下一章卡尔曼滤波的开场白。
