#import "/template/template.typ": *


#definition()[
#set list(indent: 2em)
#{
  set par(first-line-indent: 0pt)
  [本章使用的符号规定：]
}

- *粗体小写字母* 表示向量，例如 $bold(x)$
- *粗体大写字母* 表示矩阵，例如 $bold(A)$
- *普通小写字母* 标量或向量的分量
- *普通大写字母* 表示矩阵元素
]

=== 什么是卡尔曼滤波

自瞄程序每一帧都能从图像里解算出目标的位置，但这份数据有两个先天缺陷。一是有噪声：识别与 PnP 解算的误差叠加在一起，哪怕装甲板纹丝不动，解出的坐标也在毫米到厘米量级上抖动。二是会过时：从开火到命中隔着一整条延迟链，按 5 m 距离、25 m/s 弹速算，仅弹丸飞行一项就要 200 ms——你瞄准的永远是目标“过去”的位置。所以自瞄必须回答两个问题：目标现在到底在哪（滤除噪声），以及它接下来会去哪（预测运动）。

卡尔曼滤波（Kalman Filter, KF）就是同时回答这两个问题的算法。它随时间维护一份对系统状态（例如位置和速度）的估计，连同这份估计的不确定性：每一步先用运动模型*预测*状态如何演化，再用新到的测量*修正*预测，两者按各自的不确定性加权融合——模型可靠就多信模型，测量精确就多信测量。可以证明，对“线性模型 + 高斯噪声”的系统，这种融合在估计误差方差最小的意义下是最优的。它由 Rudolf Kálmán 于 1960 年提出，第一批著名用户是阿波罗飞船的导航计算机，今天则运行在几乎每一台 RM 机器人上。

比起把最终公式背下来，沿着问题的演进把它亲手“发明”一遍要有用得多，这正是本章的路线：先从一台体重秤开始，引出不存历史数据的递推估计；体重不会动而赛车会动，于是需要“预测 + 修正”结构的 g-h 滤波器；g-h 的增益靠拍脑袋设定，于是以误差方差最小为准则推导最优增益，得到一维卡尔曼滤波；真实目标不止一个状态量，于是引入协方差矩阵，推广出多维形式；观测往往是非线性的，于是有局部线性化的 EKF 和采样传播的 UKF；最后回到自瞄，看这套理论如何落在装甲板跟踪与整车建模上。


=== 如何准确地测量体重

从一个和赛场毫无关系的问题起步：一台读数带随机误差的体重秤，怎么称准体重？单次读数靠不住，但你早就知道答案——多称几次取平均。

对同一对象测 $n$ 次，取平均值作为估计：

$ hat(x)_n = frac(1, n) sum_(i=1)^n z_i $

但按定义直接算平均，工程上有个麻烦：要存下全部历史数据 $z_1, z_2, ..., z_n$，每来一个新读数就从头加一遍，时间复杂度 $O(n)$。放到你的自瞄里想一想：跟踪一开就是几分钟，帧号没有上限，总不能让第 10000 帧的估计去遍历前 9999 帧的数据——嵌入式的内存和 10 ms 的帧预算都不答应。

出路是递推（Recursive）形式：只用上一次的估计 $hat(x)_(n-1)$ 和当前测量 $z_n$ 完成更新，历史数据一概不存。

对平均值公式做一点代数变形，它恰好能改写成这种形式：

$
hat(x)_n = hat(x)_(n-1) + 1/n (z_n - hat(x)_(n-1))
$

看清楚这个式子说了什么：第 $n$ 次估计只依赖 $hat(x)_(n-1)$ 和 $z_n$，时间和空间复杂度都是 $O(1)$——滤波器跑多久，每一步的开销都不变。后面出场的每一种滤波器都保持这个性质。

#note("详细推导")[
$
hat(x)_n &= 1/n sum_(i=1)^n z_i 
= 1/n (sum_(i=1)^(n-1) z_i + z_n) \
&= 1/n sum_(i=1)^(n-1) z_i + 1/n z_n 
= (n-1)/(n(n-1)) sum_(i=1)^(n-1) z_i + 1/n z_n \
&= (n-1)/n dot 1/(n-1) sum_(i=1)^(n-1) z_i + 1/n z_n 
= (n-1)/n hat(x)_(n-1) + 1/n z_n \
&= hat(x)_(n-1) - 1/n hat(x)_(n-1) + 1/n z_n \
&= hat(x)_(n-1) + 1/n (z_n - hat(x)_(n-1))
$

#table(
  columns: (auto, 1fr),
  align: (center, left),
  stroke: (x, y) => (
    top: if y == 1 or y == 0 { 1pt } else { 0pt },
    bottom: if y == 5 { 1pt } else { 0pt },
  ),
  inset: 8pt,
  
  [*符号*], [*含义*],
  [$x$], [体重真值],
  [$z_n$], [第 $n$ 次测量值],
  [$hat(x)_n$], [基于前 $n$ 次测量的估计值],
  [$hat(x)_(n-1)$], [基于前 $n-1$ 次测量的估计值],
  [$z_n - hat(x)_(n-1)$], [测量残差],
)
]

别小看这个改写，它已经具备状态估计滤波器的基本形态。增益系数 $K_n = 1/n$ 决定新测量和历史估计各占多少话语权：$n$ 越大 $K_n$ 越小——手里的数据越多，单次新读数就越难撼动估计。

不信的话，对照卡尔曼滤波的状态更新公式，结构一模一样：

$
hat(x)_k = hat(x)_(k|k-1) + K_k (z_k - h(hat(x)_(k|k-1)))
$

其中：
- $hat(x)_(k|k-1)$：基于前 $k-1$ 次观测的先验估计
- $z_k$：第 $k$ 次测量值
- $h(dot)$：观测函数
- $K_k$：卡尔曼增益

体重秤场景里，状态不随时间变化（$hat(x)_(k|k-1) = hat(x)_(k-1)$），观测函数是恒等映射（$h(x) = x$）——递推平均就是卡尔曼滤波在静态系统下的特例。也就是说，你在学会“卡尔曼滤波”这个词之前就已经用过它了。

把递推公式的三个部分各自标出来：

$
hat(x)_n = underbrace(hat(x)_(n-1), "先验估计") + underbrace(1/n, "增益") underbrace((z_n - hat(x)_(n-1)), "残差")
$

*先验估计* $hat(x)_(n-1)$ 是新测量到来前你的全部认知；*测量残差* $r_n = z_n - hat(x)_(n-1)$ 是观测和先验的分歧；*增益系数* $K_n$ 决定这份分歧能把估计拉动多远。“先验 + 增益 × 残差”——这个骨架从体重秤一路用到 UKF，值得现在就刻进脑子。


=== 从体重秤到赛车：静态估计的局限性

递推平均在体重秤上好使，靠的是一个隐含前提：称量期间体重不变，读数的一切波动都是传感器噪声。可你真正要对付的目标不是体重——装甲板在动。前提一破，会发生什么？

拿一个数字干净的例子做实验：一辆赛车从 0 米处出发，以 20 m/s 匀速行驶；GPS 每秒报一次位置，误差 ±5 米。GPS 噪声之于赛车，正如 PnP 抖动之于装甲板——例子换了皮，问题还是同一个。

真实轨迹是 $p_t = 20t$，GPS 给你的是 $z_t = p_t + "噪声"$。把递推平均直接怼上去：

#figure(
table(
columns: (auto, auto, auto, auto, auto),
align: (center, center, center, center, center),
stroke: (x, y) => (
  top: if y == 1 or y == 0 { 1pt } else { 0pt },
  bottom: if y == 6 { 1pt } else { 0pt },
),
inset: 8pt,
[*时刻 (s)*], [*真实位置 (m)*], [*GPS 测量 (m)*], [*递推平均 (m)*], [*误差 (m)*],
[0], [0], [1.2], [1.2], [+1.2],
[1], [20], [18.3], [9.8], [−10.2],
[2], [40], [43.1], [20.9], [−19.1],
[3], [60], [56.8], [29.9], [−30.1],
[4], [80], [82.4], [40.4], [−39.6],
[5], [100], [97.6], [49.9], [−50.1],
),
caption: [递推平均追踪动态目标的失效]
)

误差一路单调涨到 50 米，而且还会继续涨下去。滤波器没坏，是它的世界观错了：递推平均假定被估计量恒定，于是把测量值的一切变化都当噪声去平均——它分不清“秤在抖”和“车在跑”。

一句话点破：*递推平均只有修正，没有预测*。目标既然在动，就该在新测量到来之前，先按运动规律预测它现在该在哪，再用测量修正预测。这正是 g-h 滤波器的核心思想。


==== g-h 滤波器

给运动目标做估计，就得把缺的那半补上：

#align(center)[
  *估计 = 预测（运动模型）+ 修正（测量）*
]

设第 $k-1$ 时刻你手里有位置估计 $hat(p)_(k-1)$ 和速度估计 $hat(v)_(k-1)$。按匀速运动模型，第 $k$ 时刻的预测是：

$ p_("pred",k) = hat(p)_(k-1) + hat(v)_(k-1) dot Delta t $
$ v_("pred",k) = hat(v)_(k-1) $

其中 $Delta t$ 为采样间隔。

代个数：若 $hat(p)_0 = 0$ m，$hat(v)_0 = 15$ m/s，$Delta t = 1$ s，则 $p_("pred",1) = 15$ m。

当测量值 $z_k$ 到来时，计算残差：

$ r_k = z_k - p_("pred",k) $


*位置修正*

$ hat(p)_k = p_("pred",k) + g dot r_k $

其中 $g in (0, 1)$ 是位置增益：$g = 0$ 完全信预测，$g = 1$ 完全信测量。

*速度修正*

$ hat(v)_k = v_("pred",k) + frac(h, Delta t) dot r_k $

其中 $h in (0, 1)$ 为速度增益。除以 $Delta t$ 是为了将位置残差（单位：m）转换为速度修正量（单位：m/s）。


亲手代一遍（$g = 0.3$，$h = 0.1$，$z_1 = 18.3$ m）：$r_1 = 3.3$ m，$hat(p)_1 = 15.99$ m，$hat(v)_1 = 15.33$ m/s。


#note("g-h 滤波器算法")[
  *输入*：$hat(p)_(k-1)$，$hat(v)_(k-1)$，$z_k$，$Delta t$，$g$，$h$

  *预测*：
  $ p_("pred",k) = hat(p)_(k-1) + hat(v)_(k-1) Delta t, quad v_("pred",k) = hat(v)_(k-1) $

  *修正*：
  $ r_k = z_k - p_("pred",k) $
  $ hat(p)_k = p_("pred",k) + g r_k, quad hat(v)_k = v_("pred",k) + frac(h, Delta t) r_k $

  *输出*：$hat(p)_k$，$hat(v)_k$
]


*数值示例*

还是那辆赛车。参数：真实速度 20 m/s，$Delta t = 1$ s，$g = 0.3$，$h = 0.1$，初始估计 $hat(p)_0 = 0$，$hat(v)_0 = 15$（故意设错，看它能不能自己纠回来）。

#figure(
table(
  columns: (auto, auto, auto, auto, auto, auto),
  align: center,
  stroke: (x, y) => (
    top: if y == 1 or y == 0 { 1pt } else { 0pt },
    bottom: if y == 6 { 1pt } else { 0pt },
  ),
  inset: 8pt,
  [*t (s)*], [*真实位置*], [*GPS*], [*g-h 估计*], [*真实速度*], [*速度估计*],
  [0], [0], [—], [0.0], [20], [15.00],
  [1], [20], [18.3], [16.0], [20], [15.33],
  [2], [40], [43.1], [34.9], [20], [16.51],
  [3], [60], [56.8], [53.0], [20], [17.05],
  [4], [80], [82.4], [73.8], [20], [18.29],
  [5], [100], [97.6], [93.7], [20], [18.84],
),
caption: [g-h 滤波器追踪结果]
)

重点看速度那一列：没有任何传感器测过速度，估计却自己从 15.00 爬到了 18.84 m/s——故意设错的初值正被残差一步步纠正。位置误差也不再像递推平均那样发散，而是稳定在几米量级。

*递推平均与 g-h 滤波器对比*

#figure(
table(
  columns: (auto, auto, auto, auto),
  align: center,
  stroke: (x, y) => (
    top: if y == 1 or y == 0 { 1pt } else { 0pt },
    bottom: if y == 6 { 1pt } else { 0pt },
  ),
  inset: 8pt,
  [*t (s)*], [*真实位置 (m)*], [*递推平均 (m)*], [*g-h (m)*],
  [0], [0], [1.2], [0.0],
  [1], [20], [9.8], [16.0],
  [2], [40], [20.9], [34.9],
  [3], [60], [29.9], [53.0],
  [4], [80], [40.4], [73.8],
  [5], [100], [49.9], [93.7],
),
caption: [同一数据下两种方法的对比：第 5 秒误差 50.1 m 对 6.3 m——预测项的有无是分水岭。]
)

#table(
  columns: (auto, 1fr, 1fr),
  align: (center, left, left),
  stroke: (x, y) => (
    top: if y == 1 or y == 0 { 1pt } else { 0pt },
    bottom: if y == 5 { 1pt } else { 0pt },
  ),
  inset: 8pt,
  [], [*递推平均*], [*g-h 滤波器*],
  [适用场景], [静态量], [动态系统],
  [预测机制], [无], [有（运动模型）],
  [状态数], [1], [2（位置+速度）],
  [增益], [$1/n$（递减）], [$g, h$（固定）],
  [第 5 秒误差], [50.1 m], [6.3 m],
)


==== 从标量到矩阵：为高维系统做准备

标量公式在一维赛道上还算清爽，但数一数状态量的涨法：

- 一维运动：$(p, v)$ — 2 个状态
- 二维平面：$(x, y, v_x, v_y)$ — 4 个状态
- 三维空间 + 加速度：9 个状态

你的自瞄一上来就是三维位置加三维速度——6 个状态。为每个状态抄一遍结构雷同的方程？这种重复正是矩阵的用武之地：把状态打包成向量，无论几维，公式都长同一副面孔。

*状态向量*

定义状态向量 $bold(x)_k = vec(p_k, v_k)$ 与增益向量 $bold(K) = vec(g, h \/ Delta t)$，两个标量修正公式立刻并成一行：

$ hat(bold(x))_k = bold(x)_("pred") + bold(K) dot (z_k - p_("pred")) $

增益向量的每一行对应一个状态的修正规则。

*观测矩阵*

还差一块拼图：GPS 只测位置，测不到速度——状态是二维的，测量只有一维，得有个东西负责从状态向量里“取出”能和测量对上的那部分。这就是观测矩阵 $bold(H)$：

$ bold(H) = mat(1, 0) $

它的作用一乘便知：

$ bold(H) bold(x)_("pred") = mat(1, 0) vec(p_("pred"), v_("pred")) = p_("pred") $

换传感器，只需换 $bold(H)$：
- 仅测位置：$bold(H) = mat(1, 0)$
- 仅测加速度：$bold(H) = mat(0, 0, 1)$
- 同时测位置和速度：$bold(H) = mat(1, 0; 0, 1)$

*完整的矩阵形式*

$ hat(bold(x))_k = bold(x)_("pred") + bold(K) (z_k - bold(H) bold(x)_("pred")) $

逐项读一遍：$bold(x)_("pred")$ 是模型预测；$bold(H) bold(x)_("pred")$ 是“预测出的状态该对应什么测量”；$z_k - bold(H) bold(x)_("pred")$ 是测量残差；$bold(K)$ 决定残差能把估计拉多远——还是“先验 + 增益 × 残差”那副骨架。

*验证*

不放心就展开验算：

$ vec(hat(p)_k, hat(v)_k) &= vec(p_("pred"), v_("pred")) + vec(g, h \/ Delta t) (z_k - mat(1, 0) vec(p_("pred"), v_("pred"))) \
&= vec(p_("pred") + g(z_k - p_("pred")), v_("pred") + (h \/ Delta t)(z_k - p_("pred"))) $

与标量形式一字不差。

*预测步骤的矩阵形式*

预测那一半也照样打包。定义状态转移矩阵：

$ bold(F) = mat(1, Delta t; 0, 1) $

预测步骤为 $bold(x)_("pred") = bold(F) hat(bold(x))_(k-1)$，展开后：

$ vec(p_("pred"), v_("pred")) = mat(1, Delta t; 0, 1) vec(hat(p)_(k-1), hat(v)_(k-1)) = vec(hat(p)_(k-1) + Delta t hat(v)_(k-1), hat(v)_(k-1)) $


==== 完整算法（矩阵形式）

把预测和修正合在一起，就是 g-h 滤波器的矩阵版本——记住它的形状，后面每一种滤波器都会沿用：

#note("g-h 滤波器（矩阵形式）")[
  *定义*：
  $ bold(x)_k = vec(p_k, v_k), quad bold(F) = mat(1, Delta t; 0, 1), quad bold(H) = mat(1, 0), quad bold(K) = vec(g, h \/ Delta t) $

  *预测*：$bold(x)_("pred") = bold(F) hat(bold(x))_(k-1)$

  *修正*：$hat(bold(x))_k = bold(x)_("pred") + bold(K) (z_k - bold(H) bold(x)_("pred"))$
]


==== g-h 滤波器的局限性

g-h 滤波器有了“预测 + 修正”的骨架，可 $g$ 和 $h$ 是拍脑袋定的：拍大了估计跟着噪声抖，拍小了目标一变速估计就拖在后面。凭什么是 0.3 和 0.1？说不出道理。卡尔曼滤波补上的正是这一块：

+ 引入误差协方差矩阵 $bold(P)_k$ 量化估计的不确定性
+ 根据最小化估计误差的准则，动态计算最优增益 $bold(K)_k$

下一节就来把这套机制推出来。


=== 最优增益的推导

上一节搭好了 g-h 滤波器的骨架，核心公式是：

$ hat(x)_k = x_("pred") + K (z_k - x_("pred")) $

结构没什么可挑的：预测与测量加权融合，$K$ 分配话语权。可 $K$ 到底该取多少？g-h 里它来自工程师的手感，而手感不是数学。本节换上概率的眼镜重新审视状态估计，把“最优的增益”亲手推出来——这正是卡尔曼滤波的核心贡献。

==== 不确定性的量化

先说清“最优”指什么。回到赛车：预测位置 35 米，GPS 报 38 米，你该报多少？

g-h 滤波器的回答是“取决于你设的 $g$”——若 $g = 0.3$，估计为 $35 + 0.3 times (38 - 35) = 35.9$ 米。这个答案绕开了真正的问题：凭什么选 $g = 0.3$？

更讲理的思路是掂量预测和测量各自有多可信。运动模型很准（赛车在笔直赛道上匀速行驶），就该多信预测；GPS 很精而模型粗糙，就该多信测量。而且“可信程度”本来就不是常数——你的 PnP 在 2 m 处稳、7 m 外抖，可信度必须是一个可计算的量，不能是一个手感。方差正是刻画不确定性的自然工具。

设预测值 $x_("pred")$ 的误差服从均值为零、方差为 $sigma_("pred")^2$ 的分布，测量值 $z$ 的误差服从均值为零、方差为 $sigma_z^2$ 的分布。方差越大，对应的值越不可信。于是优化目标可以说得毫不含糊：选一个增益 $K$，让融合后的估计 $hat(x)$ 误差方差最小。

==== 两个估计的最优融合

把问题剥到最简：手里有对同一个量 $x$ 的两个独立估计 $x_1$ 和 $x_2$，误差方差分别为 $sigma_1^2$ 和 $sigma_2^2$，怎么把它们捏成一个更准的估计？

最自然的想法是加权平均：

$ hat(x) = w_1 x_1 + w_2 x_2, quad w_1 + w_2 = 1 $

设 $w_1 = 1 - K$，$w_2 = K$，则：

$ hat(x) = (1 - K) x_1 + K x_2 = x_1 + K(x_2 - x_1) $

看着眼熟吗？这就是滤波器的修正公式。接下来推导让 $hat(x)$ 方差最小的那个 $K$。

设 $x_1 = x + epsilon_1$，$x_2 = x + epsilon_2$，其中 $epsilon_1$、$epsilon_2$ 为零均值、相互独立的误差项，方差分别为 $sigma_1^2$、$sigma_2^2$。融合估计的误差为：

$ hat(x) - x = (1-K)(x + epsilon_1) + K(x + epsilon_2) - x = (1-K)epsilon_1 + K epsilon_2 $

由于 $epsilon_1$ 与 $epsilon_2$ 独立，误差方差为：

$ sigma_(hat(x))^2 = (1-K)^2 sigma_1^2 + K^2 sigma_2^2 $

对 $K$ 求导并令其为零：

$ frac(d sigma_(hat(x))^2, d K) = -2(1-K)sigma_1^2 + 2K sigma_2^2 = 0 $

解得：

$ K^* = frac(sigma_1^2, sigma_1^2 + sigma_2^2) $

读一下这个结果：最优增益与 $x_1$ 的方差成正比，与 $x_2$ 的方差成反比——谁越不可信，谁的话语权越低。当 $sigma_1^2 >> sigma_2^2$ 时，$K^* approx 1$，融合结果几乎照抄 $x_2$；当 $sigma_1^2 << sigma_2^2$ 时，$K^* approx 0$，融合结果几乎不理 $x_2$。

把最优增益代回方差公式，得到融合后的方差：

$ sigma_(hat(x))^2 = (1 - K^*)sigma_1^2 = frac(sigma_1^2 sigma_2^2, sigma_1^2 + sigma_2^2) $

盯住一个事实：$sigma_(hat(x))^2 < min(sigma_1^2, sigma_2^2)$——融合后的估计比任何一个单独的估计都准，哪怕其中一个很糟。两份带噪的信息合在一起，只会更好，不会更坏，这就是信息融合的全部底气。

==== 卡尔曼增益的形式

回到滤波器的语境：$x_1$ 就是预测值 $x_("pred")$，$x_2$ 就是测量值 $z$。换上符号，最优增益为：

$ K = frac(sigma_("pred")^2, sigma_("pred")^2 + sigma_z^2) $

这就是一维卡尔曼增益。拿直觉核对一遍：

- 预测不确定性大（$sigma_("pred")^2$ 大），$K$ 接近 1，多信测量
- 测量噪声大（$sigma_z^2$ 大），$K$ 接近 0，多信预测
- 两者相当时，$K approx 0.5$，各取一半

和 g-h 的固定增益不同，卡尔曼增益跟着不确定性走：滤波器刚起步时估计还不可靠，$K$ 大，测量说了算；数据攒多了，估计收敛，$sigma_("pred")^2$ 变小，$K$ 随之回落，新测量的话语权变轻。这种自适应正是卡尔曼滤波的核心优势——后面讲自瞄丢目标再重捕时，你会看到它救场的样子。

==== 不确定性的传播

增益要用 $sigma_("pred")^2$ 来算，那 $sigma_("pred")^2$ 又从哪来？答案是：不确定性自己也要被“滤波”——跟着预测和修正两个阶段一步步演变。

*预测阶段的不确定性增长*

看匀速运动模型 $x_k = x_(k-1) + v Delta t$。就算上一时刻的位置估计分毫不差，模型本身也在撒谎——赛车可能轻微加减速。这种模型不确定性称为过程噪声，记其方差为 $sigma_w^2$。

设第 $k-1$ 时刻的估计方差为 $sigma_(k-1)^2$。位置预测直接继承了上一时刻的估计（$x_("pred") = hat(x)_(k-1) + hat(v) Delta t$），所以预测误差由两部分叠加：上一时刻的估计误差，加上过程噪声。假设两者独立，预测方差为：

$ sigma_("pred")^2 = sigma_(k-1)^2 + sigma_w^2 $

它告诉你一件事：预测这一步，不确定性只增不减——估计得再准，模型误差也会一步步把不确定性垫高。

*修正阶段的不确定性减小*

新测量一到，融合把不确定性压回去。由前面的推导，修正后的方差为：

$ sigma_k^2 = (1 - K) sigma_("pred")^2 = frac(sigma_("pred")^2 sigma_z^2, sigma_("pred")^2 + sigma_z^2) $

由于 $0 < K < 1$，修正后的方差总是小于预测方差——每次测量都带来新信息，估计随之变得更确定。一涨一消，滤波器就在这两股力量之间来回呼吸。

把增益公式代入，修正后的方差也可写为：

$ sigma_k^2 = (1 - K) sigma_("pred")^2 $

这个形式算起来最省事，后面的多维推广会直接用它。

==== 一维卡尔曼滤波算法

零件齐了，拼起来。设系统状态为位置 $x$，状态转移方程为 $x_k = x_(k-1) + u_k + w_k$，其中 $u_k$ 为已知控制量（如速度乘以时间），$w_k$ 为过程噪声。测量方程为 $z_k = x_k + v_k$，其中 $v_k$ 为测量噪声。

*初始化*

设定初始估计 $hat(x)_0$ 和初始方差 $sigma_0^2$。对初始状态一无所知？就把 $sigma_0^2$ 设大些——这等于告诉滤波器“别信我给的初值”，它会自己用头几次测量把估计拉到位。

*预测*

$ x_("pred") = hat(x)_(k-1) + u_k $
$ sigma_("pred")^2 = sigma_(k-1)^2 + sigma_w^2 $

*修正*

$ K_k = frac(sigma_("pred")^2, sigma_("pred")^2 + sigma_z^2) $
$ hat(x)_k = x_("pred") + K_k (z_k - x_("pred")) $
$ sigma_k^2 = (1 - K_k) sigma_("pred")^2 $

算法在预测与修正之间交替，每一步都输出状态估计 $hat(x)_k$ 及其不确定性 $sigma_k^2$。与 g-h 滤波器唯一的本质区别：增益 $K_k$ 不再是预设常数，而是按当前不确定性现算的。

==== 数值示例：重新追踪赛车

还是那辆赛车、那串 GPS 读数，这回换卡尔曼滤波上场。参数设定如下：

- 过程噪声标准差：$sigma_w = 1$ m（模型不完全准确）
- 测量噪声标准差：$sigma_z = 5$ m（GPS 精度有限）
- 初始估计：$hat(x)_0 = 0$ m
- 初始标准差：$sigma_0 = 10$ m（对初始位置不确定）
- 控制量：$u_k = 20$ m（真实速度 20 m/s，$Delta t = 1$ s）

#figure(
table(
  columns: (auto, auto, auto, auto, auto, auto, auto),
  align: center,
  stroke: (x, y) => (
    top: if y == 1 or y == 0 { 1pt } else { 0pt },
    bottom: if y == 6 { 1pt } else { 0pt },
  ),
  inset: 8pt,
  [*t (s)*], [*真实*], [*预测*], [*$sigma_("pred")$*], [*测量*], [*$K$*], [*估计*],
  [0], [0], [—], [—], [—], [—], [0.0],
  [1], [20], [20.0], [10.0], [18.3], [0.80], [18.6],
  [2], [40], [38.6], [4.6], [43.1], [0.46], [40.7],
  [3], [60], [60.7], [3.5], [56.8], [0.33], [59.4],
  [4], [80], [79.4], [3.0], [82.4], [0.27], [80.2],
  [5], [100], [100.2], [2.8], [97.6], [0.24], [99.6],
),
caption: [一维卡尔曼滤波追踪赛车]
)

#note("第 1 步计算过程")[
预测：$x_("pred") = 0 + 20 = 20$ m

预测方差：$sigma_("pred")^2 = 10^2 + 1^2 = 101$，$sigma_("pred") approx 10.0$ m

卡尔曼增益：$K = 101 / (101 + 25) = 0.80$

修正：$hat(x)_1 = 20 + 0.80 times (18.3 - 20) = 18.6$ m

修正方差：$sigma_1^2 = (1 - 0.80) times 101 = 20.2$，$sigma_1 approx 4.5$ m
]

这张表最值得盯的是 $K$ 那一列：从初始的 0.80 逐步下降到 0.24——观测越攒越多，估计的不确定性 $sigma_("pred")$ 不断缩小，滤波器对新测量的依赖随之变轻，没有人去调它，它自己就这么走了。再看 $sigma_("pred")$ 一列：从 10 m 迅速下降到 2.8 m，几步之内滤波器就把状态摸清了。最后看战果：第 5 秒的估计误差仅为 0.4 m，远优于 g-h 滤波器的 6.3 m 和递推平均的 50.1 m。

改进的根源就一条：增益跟着实际的不确定性走，而不是定死的权重。估计还不可靠时（初始阶段），放手采纳测量；估计已经可靠时，对异常测量保持适度怀疑。

==== 从 g-h 滤波器到卡尔曼滤波

回头看，g-h 滤波器与卡尔曼滤波的关系一目了然。两者共享同一条修正公式：

$ hat(x)_k = x_("pred") + K (z_k - x_("pred")) $

差异全在增益从哪来：

#table(
  columns: (auto, 1fr, 1fr),
  align: (center, left, left),
  stroke: (x, y) => (
    top: if y == 1 or y == 0 { 1pt } else { 0pt },
    bottom: if y == 4 { 1pt } else { 0pt },
  ),
  inset: 8pt,
  [], [*g-h 滤波器*], [*卡尔曼滤波*],
  [增益], [手动设定常数 $g$], [动态计算 $K_k = sigma_("pred")^2 / (sigma_("pred")^2 + sigma_z^2)$],
  [依据], [工程经验], [最小化估计误差方差],
  [自适应], [无], [有（增益随不确定性变化）],
  [额外输出], [无], [估计的不确定性 $sigma_k^2$],
)

你可以把卡尔曼滤波理解成 g-h 的最优化版本：同一副“预测-修正”框架，增益却由概率推理自动给出。还有一份容易被低估的附赠品：它连同估计一起输出不确定性 $sigma_k^2$——“我算出来的位置有多可信”这句话本身就是数据。你的自瞄要不要开火、火控敢不敢给提前量，问的正是这个数。

==== 小结

本节把状态估计变成了一个优化问题，落下三颗钉子：

状态估计的目标是最小化估计误差的方差。预测和测量各自带着不确定性时，最优融合就是按方差加权：方差越大，权重越低。由此导出的卡尔曼增益 $K = sigma_("pred")^2 / (sigma_("pred")^2 + sigma_z^2)$ 物理意义清晰——它在模型预测与传感器测量之间分配信任。

不确定性在预测阶段增长（模型不完美），在修正阶段减小（测量带来新信息）。卡尔曼滤波的递推结构正是在这两个过程之间交替，持续追踪状态估计及其不确定性。

目前的推导限于一维。下一节把它推广到多维：方差换成协方差矩阵，增益变成矩阵，但核心思想——基于不确定性的最优融合——一个字都不变。


=== 多维卡尔曼滤波

一维的故事讲完了：最优增益由预测方差与测量方差的比值决定，融合后的估计误差方差最小。可你真正要滤的从来不止一个数——赛车至少要位置、速度两个变量，二维平面追踪要 $(x, y, v_x, v_y)$ 四个，你的自瞄是三维位置加三维速度六个。本节把一维结论推广到多维，建立完整的卡尔曼滤波方程组——这也是实车代码里真正跑着的那组公式。

==== 为什么需要协方差矩阵

一维时，不确定性就是一个方差 $sigma^2$。推广到多维，最省事的想法是每个状态各记各的：对状态向量 $bold(x) = vec(p, v)$，记一个位置方差 $sigma_p^2$、一个速度方差 $sigma_v^2$，完事。

且慢——这样记，漏掉了一个关键现象：状态分量的误差往往是勾连的。想一想：若当前位置被高估了，按 $p = p_0 + v t$ 的关系，多半是因为速度也被高估了。位置误差和速度误差不是各错各的，而是倾向于同向偏离。

协方差正是刻画这种勾连的工具。对于两个随机变量 $X$ 和 $Y$，协方差定义为：

$ "Cov"(X, Y) = E[(X - mu_X)(Y - mu_Y)] $

$X$ 和 $Y$ 倾向于同向偏离各自的均值，协方差为正；倾向于反向偏离，为负；两者独立，为零。方差不过是协方差的特例：$"Var"(X) = "Cov"(X, X)$。

对 $n$ 维状态向量 $bold(x) = vec(x_1, x_2, dots.v, x_n)$，把所有方差和协方差码进一个 $n times n$ 的对称矩阵：

$ bold(P) = mat(
  "Var"(x_1), "Cov"(x_1, x_2), dots, "Cov"(x_1, x_n);
  "Cov"(x_2, x_1), "Var"(x_2), dots, "Cov"(x_2, x_n);
  dots.v, dots.v, dots.down, dots.v;
  "Cov"(x_n, x_1), "Cov"(x_n, x_2), dots, "Var"(x_n)
) $

这就是协方差矩阵。对角线元素是各状态的方差，非对角线元素是状态之间的协方差。以位置-速度系统为例：

$ bold(P) = mat(
  sigma_p^2, sigma_(p v);
  sigma_(p v), sigma_v^2
) $

其中 $sigma_(p v) = "Cov"(p, v)$ 描述位置误差与速度误差的相关程度。$sigma_(p v) > 0$，就是“位置被高估时速度也倾向于被高估”。

别把协方差矩阵当成记账簿——非对角线上那些数是滤波器的秘密武器：测量修正了某个状态，与它勾连的状态就该跟着动。你的相机测不到目标速度，速度估计却能一帧帧变准，靠的正是 $sigma_(p v)$ 这根线把位置残差牵到速度上。这件事马上在示例里能看见。

==== 多维系统的数学描述

推公式之前，先把多维系统的数学模型立起来。

*状态转移方程*

系统状态从第 $k-1$ 时刻怎么走到第 $k$ 时刻，由状态转移方程说了算：

$ bold(x)_k = bold(F) bold(x)_(k-1) + bold(B) bold(u)_k + bold(w)_k $

其中 $bold(F)$ 是状态转移矩阵，描述状态的自然演化；$bold(B)$ 是控制矩阵，$bold(u)_k$ 是控制输入；$bold(w)_k$ 是过程噪声，代表模型的不确定性。

以匀速运动模型为例，状态为 $bold(x) = vec(p, v)$，状态转移方程为：

$ vec(p_k, v_k) = mat(1, Delta t; 0, 1) vec(p_(k-1), v_(k-1)) + vec(w_p, w_v) $

状态转移矩阵 $bold(F) = mat(1, Delta t; 0, 1)$ 读出来就是两句话：新位置 = 旧位置 + 速度 $times$ 时间，新速度 = 旧速度（匀速假设）。

过程噪声 $bold(w)_k$ 通常假设服从零均值高斯分布，其协方差矩阵记为 $bold(Q)$：

$ bold(w)_k tilde cal(N)(bold(0), bold(Q)) $

$bold(Q)$ 的大小是你替模型交的“诚实税”：系统严格照模型走（真匀速），$bold(Q)$ 就小；模型只是粗略近似，$bold(Q)$ 就得大。

*观测方程*

传感器测到的东西和状态是什么关系，由观测方程交代：

$ bold(z)_k = bold(H) bold(x)_k + bold(v)_k $

其中 $bold(H)$ 是观测矩阵，将状态空间映射到测量空间；$bold(v)_k$ 是测量噪声，协方差矩阵记为 $bold(R)$。

继续位置-速度的例子。传感器只测得到位置，观测方程为：

$ z_k = mat(1, 0) vec(p_k, v_k) + v_k $

观测矩阵 $bold(H) = mat(1, 0)$ 负责从状态向量里取出位置分量。测量噪声方差 $bold(R) = sigma_z^2$ 反映传感器精度。

==== 预测阶段

和一维时一样，每一步分预测和修正两个阶段。预测阶段拿系统模型推算下一时刻的状态及其不确定性。

*状态预测*

状态预测直接套状态转移方程（噪声项均值为零，预测时不用管它）：

$ bold(x)_(k|k-1) = bold(F) bold(x)_(k-1) + bold(B) bold(u)_k $

下标 $k|k-1$ 表示“基于前 $k-1$ 时刻信息对第 $k$ 时刻的预测”。

*协方差预测*

不确定性怎么跟着传播？设第 $k-1$ 时刻的估计协方差为 $bold(P)_(k-1)$，来推预测协方差 $bold(P)_(k|k-1)$。

状态预测误差为：

$ bold(x)_k - bold(x)_(k|k-1) = bold(F)(bold(x)_(k-1) - hat(bold(x))_(k-1)) + bold(w)_k $

等号右边第一项是上一时刻估计误差经 $bold(F)$ 变换的结果，第二项是过程噪声。两者独立，于是预测协方差为：

$ bold(P)_(k|k-1) = bold(F) bold(P)_(k-1) bold(F)^top + bold(Q) $

对照一维的 $sigma_("pred")^2 = sigma_(k-1)^2 + sigma_w^2$ 读它：还是那两笔账——上一时刻的不确定性（经状态转移放大或缩小）加上模型自己的不确定性。$bold(P)_(k-1)$ 再小，$bold(Q)$ 也会把 $bold(P)_(k|k-1)$ 垫高：预测这一步，不确定性照旧只增不减。

#note("协方差变换公式")[
若随机向量 $bold(x)$ 的协方差矩阵为 $bold(P)$，则线性变换 $bold(y) = bold(A) bold(x)$ 的协方差矩阵为 $bold(A) bold(P) bold(A)^top$。

证明：设 $bold(x)$ 的均值为 $bold(mu)$，则

$ "Cov"(bold(y)) = E[(bold(A) bold(x) - bold(A) bold(mu))(bold(A) bold(x) - bold(A) bold(mu))^top] = bold(A) E[(bold(x) - bold(mu))(bold(x) - bold(mu))^top] bold(A)^top = bold(A) bold(P) bold(A)^top $
]

==== 修正阶段

新测量 $bold(z)_k$ 一到，修正阶段把预测与测量融合成更准的估计。

*卡尔曼增益*

多维增益的推导原则和一维一模一样：最小化修正后的估计误差协方差。过程要用到矩阵求导，这里直接给结果：

$ bold(K)_k = bold(P)_(k|k-1) bold(H)^top (bold(H) bold(P)_(k|k-1) bold(H)^top + bold(R))^(-1) $

别被矩阵吓到，拿一维的 $K = sigma_("pred")^2 \/ (sigma_("pred")^2 + sigma_z^2)$ 对照着读：“分子” $bold(P)_(k|k-1) bold(H)^top$ 是预测不确定性投到测量空间的样子；“分母” $bold(H) bold(P)_(k|k-1) bold(H)^top + bold(R)$ 是预测测量的不确定性（来自状态预测）与测量本身的不确定性（来自传感器）之和。预测不确定时 $bold(K)$ 大、多信测量；测量不可靠时 $bold(K)$ 小、多信预测——直觉原封不动。

*状态修正*

修正公式与 g-h 滤波器形式相同：

$ hat(bold(x))_k = bold(x)_(k|k-1) + bold(K)_k (bold(z)_k - bold(H) bold(x)_(k|k-1)) $

括号里的 $bold(z)_k - bold(H) bold(x)_(k|k-1)$ 称为测量残差或新息（Innovation），是实际测量与预测测量的分歧。卡尔曼增益决定这份分歧怎么分摊到各个状态分量上。

*协方差修正*

修正后的协方差为：

$ bold(P)_k = (bold(I) - bold(K)_k bold(H)) bold(P)_(k|k-1) $

其中 $bold(I)$ 为单位矩阵。测量为什么总能减小不确定性？把增益公式代进去，上式可以等价地写成 $bold(P)_k = bold(P)_(k|k-1) - bold(K)_k bold(S)_k bold(K)_k^top$，其中 $bold(S)_k = bold(H) bold(P)_(k|k-1) bold(H)^top + bold(R)$ 是新息的协方差，正定；被减去的 $bold(K)_k bold(S)_k bold(K)_k^top$ 因而是半正定项——所以任何方向上的方差都不会增大。

==== 完整算法

预测两式加修正三式，这就是教科书封面上那组著名的卡尔曼滤波方程——你已经亲手把它们全部推了一遍：

#note("多维卡尔曼滤波算法")[
*系统模型*

状态转移：$bold(x)_k = bold(F) bold(x)_(k-1) + bold(B) bold(u)_k + bold(w)_k$，$bold(w)_k tilde cal(N)(bold(0), bold(Q))$

观测方程：$bold(z)_k = bold(H) bold(x)_k + bold(v)_k$，$bold(v)_k tilde cal(N)(bold(0), bold(R))$

*初始化*

初始状态估计 $hat(bold(x))_0$，初始协方差 $bold(P)_0$

*预测*

$ bold(x)_(k|k-1) = bold(F) hat(bold(x))_(k-1) + bold(B) bold(u)_k $
$ bold(P)_(k|k-1) = bold(F) bold(P)_(k-1) bold(F)^top + bold(Q) $

*修正*

$ bold(K)_k = bold(P)_(k|k-1) bold(H)^top (bold(H) bold(P)_(k|k-1) bold(H)^top + bold(R))^(-1) $
$ hat(bold(x))_k = bold(x)_(k|k-1) + bold(K)_k (bold(z)_k - bold(H) bold(x)_(k|k-1)) $
$ bold(P)_k = (bold(I) - bold(K)_k bold(H)) bold(P)_(k|k-1) $
]

算法在预测与修正之间交替，每一步输出状态估计 $hat(bold(x))_k$ 及其协方差 $bold(P)_k$。协方差矩阵不只喂给下一步的增益计算，它本身就是估计的置信区间。

==== 示例：一维位置-速度系统

公式落到数字上才算数。还是赛车，但这回不再把速度当已知控制量喂进去——速度也进状态，看滤波器能不能只靠位置测量把它“变”出来。你的自瞄每帧干的就是这件事：相机只给位置，预测要的速度全靠滤。

*系统设定*

状态向量 $bold(x) = vec(p, v)$，真实初始状态为 $vec(0, 20)$（位置 0 m，速度 20 m/s）。

状态转移矩阵（匀速模型，$Delta t = 1$ s）：

$ bold(F) = mat(1, 1; 0, 1) $

观测矩阵（仅测量位置）：

$ bold(H) = mat(1, 0) $

过程噪声协方差（速度可能有小幅波动）：

$ bold(Q) = mat(0.25, 0.5; 0.5, 1) $

测量噪声方差：

$ bold(R) = mat(25) quad (sigma_z = 5 "m") $

初始估计（故意设定错误的速度）：

$ hat(bold(x))_0 = vec(0, 15), quad bold(P)_0 = mat(100, 0; 0, 100) $

*第 1 步迭代*

预测：

$ bold(x)_(1|0) = mat(1, 1; 0, 1) vec(0, 15) = vec(15, 15) $

$ bold(P)_(1|0) = mat(1, 1; 0, 1) mat(100, 0; 0, 100) mat(1, 0; 1, 1) + mat(0.25, 0.5; 0.5, 1) = mat(200.25, 100.5; 100.5, 101) $

卡尔曼增益：

$ bold(H) bold(P)_(1|0) bold(H)^top + bold(R) = 200.25 + 25 = 225.25 $

$ bold(K)_1 = mat(200.25; 100.5) times 1/225.25 = mat(0.889; 0.446) $

测量 $z_1 = 18.3$ m，修正：

$ hat(bold(x))_1 = vec(15, 15) + mat(0.889; 0.446) times (18.3 - 15) = vec(17.9, 16.5) $

$ bold(P)_1 = (mat(1, 0; 0, 1) - mat(0.889; 0.446) mat(1, 0)) mat(200.25, 100.5; 100.5, 101) = mat(22.2, 11.2; 11.2, 56.2) $

*完整追踪结果*

#figure(
table(
  columns: (auto, auto, auto, auto, auto, auto, auto),
  align: center,
  stroke: (x, y) => (
    top: if y == 1 or y == 0 { 1pt } else { 0pt },
    bottom: if y == 6 { 1pt } else { 0pt },
  ),
  inset: 8pt,
  [*t*], [*真实位置*], [*真实速度*], [*测量*], [*位置估计*], [*速度估计*], [*$sigma_p$*],
  [0], [0], [20], [—], [0.0], [15.0], [10.0],
  [1], [20], [20], [18.3], [17.9], [16.5], [4.7],
  [2], [40], [20], [43.1], [41.4], [21.2], [4.5],
  [3], [60], [20], [56.8], [58.3], [19.0], [4.3],
  [4], [80], [20], [82.4], [80.6], [20.3], [4.0],
  [5], [100], [20], [97.6], [99.0], [19.7], [3.8],
),
caption: [多维卡尔曼滤波追踪结果]
)

看速度那两列：没有任何传感器测过速度，估计却从故意设错的 15 m/s 迅速爬到真实值 20 m/s 附近，此后只剩小幅波动。这正是协方差矩阵在干活——位置测量残差顺着 $sigma_(p v)$ 那根关联的线，被牵到了速度估计上。前面承诺过“马上能看见”，就是这一幕。

位置估计的标准差从 10 m 下降到 3.8 m，不确定性持续收紧。继续迭代下去，它会收敛到一个稳态值，高低由 $bold(Q)$ 和 $bold(R)$ 的相对大小说了算。

==== 矩阵维度总结

写代码时你第一个撞上的往往不是理论，而是维度对不上。设状态向量维度为 $n$，测量向量维度为 $m$，控制向量维度为 $l$，把各矩阵的尺寸列成一张速查表：

#table(
  columns: (auto, auto, auto),
  align: (center, center, left),
  stroke: (x, y) => (
    top: if y == 1 or y == 0 { 1pt } else { 0pt },
    bottom: if y == 10 { 1pt } else { 0pt },
  ),
  inset: 8pt,
  [*符号*], [*维度*], [*含义*],
  [$bold(x)$], [$n times 1$], [状态向量],
  [$bold(z)$], [$m times 1$], [测量向量],
  [$bold(u)$], [$l times 1$], [控制向量],
  [$bold(F)$], [$n times n$], [状态转移矩阵],
  [$bold(B)$], [$n times l$], [控制矩阵],
  [$bold(H)$], [$m times n$], [观测矩阵],
  [$bold(Q)$], [$n times n$], [过程噪声协方差],
  [$bold(R)$], [$m times m$], [测量噪声协方差],
  [$bold(P)$], [$n times n$], [状态估计协方差],
  [$bold(K)$], [$n times m$], [卡尔曼增益],
)

多看一眼 $bold(K)$ 的维度 $n times m$：它把 $m$ 维的测量残差映射成 $n$ 维的状态修正。测量比状态少（本例 $m = 1$，$n = 2$）也拦不住滤波器更新全部状态——桥梁还是协方差里的关联信息。

==== 小结

本节把一维卡尔曼滤波推广到了多维。核心概念一一对应：

#table(
  columns: (auto, 1fr, 1fr),
  align: (center, left, left),
  stroke: (x, y) => (
    top: if y == 1 or y == 0 { 1pt } else { 0pt },
    bottom: if y == 5 { 1pt } else { 0pt },
  ),
  inset: 8pt,
  [], [*一维*], [*多维*],
  [状态], [$x$], [$bold(x)$ （向量）],
  [不确定性], [$sigma^2$ （方差）], [$bold(P)$ （协方差矩阵）],
  [增益], [$K = sigma_("pred")^2 / (sigma_("pred")^2 + sigma_z^2)$], [$bold(K) = bold(P)_(k|k-1) bold(H)^top (bold(H) bold(P)_(k|k-1) bold(H)^top + bold(R))^(-1)$],
  [预测方差], [$sigma_("pred")^2 = sigma_(k-1)^2 + sigma_w^2$], [$bold(P)_(k|k-1) = bold(F) bold(P)_(k-1) bold(F)^top + bold(Q)$],
  [修正方差], [$sigma_k^2 = (1 - K) sigma_("pred")^2$], [$bold(P)_k = (bold(I) - bold(K) bold(H)) bold(P)_(k|k-1)$],
)

多维的本质与一维分毫不差：预测阶段不确定性涨，修正阶段不确定性消，增益在预测与测量之间分配信任。协方差矩阵带来的新本事是利用状态间的关联——速度测不到？没关系，它和位置的相关性会替你把它估出来。

到这里，线性卡尔曼滤波的理论框架就齐了。但整套推导压着一个假设：状态转移和观测方程都是线性的。雷达以极坐标测目标是非线性，你把目标坐标换算成云台的 yaw/pitch 也是非线性——非线性一来，标准卡尔曼滤波就不再适用。下一节的扩展卡尔曼滤波（EKF）用局部线性化对付它。


=== 扩展卡尔曼滤波

前两节的全部理论都押在线性假设上：状态转移 $bold(x)_k = bold(F) bold(x)_(k-1)$ 和观测 $bold(z)_k = bold(H) bold(x)_k$ 都是状态的线性函数。很多场景确实如此——匀速运动、弹簧振子、电路系统。但你的工作清单上躺着大把非线性：把目标位置换算成云台 yaw/pitch 是非线性，后面整车建模里 $x_c + r cos theta$ 也是非线性。线性假设一破会出什么事？扩展卡尔曼滤波（Extended Kalman Filter, EKF）用局部线性化把它救回来，本节就讲这一套。

==== 非线性系统的例子

拿一个经典问题开刀：雷达站在原点，追踪一架在二维平面上飞行的飞机。飞机的状态用直角坐标描述：

$ bold(x) = vec(x, y, v_x, v_y) $

其中 $(x, y)$ 是位置，$(v_x, v_y)$ 是速度。假设飞机做匀速直线运动，状态转移方程为：

$ vec(x_k, y_k, v_(x,k), v_(y,k)) = mat(1, 0, Delta t, 0; 0, 1, 0, Delta t; 0, 0, 1, 0; 0, 0, 0, 1) vec(x_(k-1), y_(k-1), v_(x,k-1), v_(y,k-1)) $

这是标准的线性方程，没有问题。麻烦出在观测方程上。

雷达并不直接给出飞机的直角坐标 $(x, y)$，它测的是极坐标：距离 $r$ 和方位角 $theta$。测量值与状态的关系为：

$ r = sqrt(x^2 + y^2) $
$ theta = "atan2"(y, x) $

（atan2 是四象限反正切：它按 $y$、$x$ 各自的符号定出正确象限；朴素的 $arctan(y\/x)$ 在 $x < 0$ 时会差 $180 degree$，通用实现必须用 atan2。）或写成向量形式：

$ bold(z) = vec(r, theta) = vec(sqrt(x^2 + y^2), "atan2"(y, x)) $

认出来了吗？这正是你的自瞄换了皮：雷达测 $(r, theta)$，你的云台看目标是 yaw、pitch 加距离——同一种非线性观测。它没法写成 $bold(z) = bold(H) bold(x)$：平方根和反正切都不是线性组合。

非线性到底坏在哪？回想卡尔曼滤波的核心假设：状态估计的不确定性用高斯分布描述，而高斯分布经过线性变换后仍是高斯分布——预测和修正阶段的协方差计算全靠这条性质才是精确的。

可高斯分布过一遍非线性函数，通常就不再是高斯了。最简单的例子：$x tilde cal(N)(0, 1)$，则 $y = x^2$ 服从卡方分布，和高斯毫不沾边。观测方程一非线性，硬套卡尔曼公式就会产生系统性误差。

==== 线性化的思想

EKF 的主意直白得近乎无赖：处理不了非线性函数，就拿线性函数冒充它——在当前估计点做泰勒展开，只留一阶项。反正滤波器每一步只在估计点附近活动，局部像线性，就够用。

设非线性函数为 $bold(h)(bold(x))$，在点 $bold(x)_0$ 处展开：

$ bold(h)(bold(x)) approx bold(h)(bold(x)_0) + frac(partial bold(h), partial bold(x))|_(bold(x)_0) (bold(x) - bold(x)_0) $

一阶导数 $frac(partial bold(h), partial bold(x))$ 是一个矩阵，称为雅可比矩阵（Jacobian），记为 $bold(J)$。对于 $m$ 维输出、$n$ 维输入的函数，雅可比矩阵的维度是 $m times n$，第 $(i, j)$ 元素为 $frac(partial h_i, partial x_j)$。

以雷达观测为例，观测函数 $bold(h)(bold(x)) = vec(sqrt(x^2 + y^2), "atan2"(y, x))$ 对状态 $bold(x) = vec(x, y, v_x, v_y)$ 的雅可比矩阵为：

$ bold(H)_k = frac(partial bold(h), partial bold(x)) = mat(
  frac(partial r, partial x), frac(partial r, partial y), frac(partial r, partial v_x), frac(partial r, partial v_y);
  frac(partial theta, partial x), frac(partial theta, partial y), frac(partial theta, partial v_x), frac(partial theta, partial v_y)
) $

$r$ 和 $theta$ 都不依赖速度 $(v_x, v_y)$，后两列直接是零。算非零的偏导数（atan2 与 $arctan(y\/x)$ 只差一个常数的象限偏移，偏导数完全相同）：

$ frac(partial r, partial x) = frac(x, sqrt(x^2 + y^2)) = frac(x, r), quad frac(partial r, partial y) = frac(y, r) $

$ frac(partial theta, partial x) = frac(-y \/ x^2, 1 + (y\/x)^2) = frac(-y, x^2 + y^2) = frac(-y, r^2), quad frac(partial theta, partial y) = frac(1\/x, 1 + (y\/x)^2) = frac(x, r^2) $

因此，雅可比矩阵为：

$ bold(H)_k = mat(
  x\/r, y\/r, 0, 0;
  -y\/r^2, x\/r^2, 0, 0
) $

注意这个矩阵要在当前状态估计 $(hat(x), hat(y))$ 处取值，所以它是时变的——每一步都得重算。这正是 EKF 与标准卡尔曼滤波的分水岭：标准滤波里 $bold(H)$ 是常数，EKF 里 $bold(H)_k$ 跟着估计值走。

==== 扩展卡尔曼滤波算法

把线性化塞进滤波器的各个阶段，EKF 就成形了。设非线性状态转移方程为 $bold(x)_k = bold(f)(bold(x)_(k-1), bold(u)_k) + bold(w)_k$，非线性观测方程为 $bold(z)_k = bold(h)(bold(x)_k) + bold(v)_k$。

*预测阶段*

状态预测使用非线性函数：

$ bold(x)_(k|k-1) = bold(f)(hat(bold(x))_(k-1), bold(u)_k) $

协方差预测需要状态转移函数的雅可比矩阵 $bold(F)_k = frac(partial bold(f), partial bold(x))|_(hat(bold(x))_(k-1))$：

$ bold(P)_(k|k-1) = bold(F)_k bold(P)_(k-1) bold(F)_k^top + bold(Q) $

留意这个分工：状态预测直接用非线性函数算（保住精度），协方差传播用线性化后的雅可比（否则算不动）——EKF 只在不得不近似的地方近似。

*修正阶段*

卡尔曼增益使用观测函数的雅可比矩阵 $bold(H)_k = frac(partial bold(h), partial bold(x))|_(bold(x)_(k|k-1))$：

$ bold(K)_k = bold(P)_(k|k-1) bold(H)_k^top (bold(H)_k bold(P)_(k|k-1) bold(H)_k^top + bold(R))^(-1) $

状态修正使用非线性观测函数计算预测测量：

$ hat(bold(x))_k = bold(x)_(k|k-1) + bold(K)_k (bold(z)_k - bold(h)(bold(x)_(k|k-1))) $

协方差修正：

$ bold(P)_k = (bold(I) - bold(K)_k bold(H)_k) bold(P)_(k|k-1) $

流程和标准卡尔曼滤波一模一样，改动就两处：状态预测和测量预测用原始的非线性函数；协方差传播和增益计算用在当前估计点取值的雅可比矩阵。

#note("EKF 算法")[
*系统模型*

状态转移：$bold(x)_k = bold(f)(bold(x)_(k-1), bold(u)_k) + bold(w)_k$

观测方程：$bold(z)_k = bold(h)(bold(x)_k) + bold(v)_k$

*预测*

$ bold(x)_(k|k-1) = bold(f)(hat(bold(x))_(k-1), bold(u)_k) $
$ bold(F)_k = frac(partial bold(f), partial bold(x))|_(hat(bold(x))_(k-1)) $
$ bold(P)_(k|k-1) = bold(F)_k bold(P)_(k-1) bold(F)_k^top + bold(Q) $

*修正*

$ bold(H)_k = frac(partial bold(h), partial bold(x))|_(bold(x)_(k|k-1)) $
$ bold(K)_k = bold(P)_(k|k-1) bold(H)_k^top (bold(H)_k bold(P)_(k|k-1) bold(H)_k^top + bold(R))^(-1) $
$ hat(bold(x))_k = bold(x)_(k|k-1) + bold(K)_k (bold(z)_k - bold(h)(bold(x)_(k|k-1))) $
$ bold(P)_k = (bold(I) - bold(K)_k bold(H)_k) bold(P)_(k|k-1) $
]

==== 示例：雷达目标追踪

回到那架飞机，用 EKF 把它追出来。

*系统设定*

状态向量 $bold(x) = vec(x, y, v_x, v_y)$，采样间隔 $Delta t = 1$ s。

状态转移函数（匀速运动，线性）：

$ bold(f)(bold(x)) = mat(1, 0, 1, 0; 0, 1, 0, 1; 0, 0, 1, 0; 0, 0, 0, 1) bold(x) $

状态转移是线性的，雅可比矩阵 $bold(F)$ 就是系数矩阵本身，且为常数——非线性全堆在观测这边。

观测函数（非线性）：

$ bold(h)(bold(x)) = vec(sqrt(x^2 + y^2), "atan2"(y, x)) $

观测雅可比矩阵（在预测状态 $(x, y)$ 处计算）：

$ bold(H)_k = mat(
  x\/r, y\/r, 0, 0;
  -y\/r^2, x\/r^2, 0, 0
), quad r = sqrt(x^2 + y^2) $

过程噪声协方差：

$ bold(Q) = mat(0.25, 0, 0.5, 0; 0, 0.25, 0, 0.5; 0.5, 0, 1, 0; 0, 0.5, 0, 1) $

测量噪声协方差（距离标准差 5 m，角度标准差 0.01 rad）：

$ bold(R) = mat(25, 0; 0, 0.0001) $

*真实轨迹与测量*

设飞机从 $(100, 200)$ m 出发，以 $(10, 5)$ m/s 的速度飞行。真实轨迹和带噪声的雷达测量如下：

#figure(
table(
  columns: (auto, auto, auto, auto, auto, auto, auto),
  align: center,
  stroke: (x, y) => (
    top: if y == 1 or y == 0 { 1pt } else { 0pt },
    bottom: if y == 6 { 1pt } else { 0pt },
  ),
  inset: 8pt,
  [*t*], [*真实 x*], [*真实 y*], [*真实 r*], [*真实 $theta$*], [*测量 r*], [*测量 $theta$*],
  [0], [100], [200], [223.6], [1.107], [—], [—],
  [1], [110], [205], [232.6], [1.078], [229.3], [1.082],
  [2], [120], [210], [241.9], [1.052], [245.1], [1.048],
  [3], [130], [215], [251.2], [1.027], [248.7], [1.031],
  [4], [140], [220], [260.8], [1.004], [263.2], [1.001],
  [5], [150], [225], [270.4], [0.983], [267.8], [0.979],
),
caption: [雷达追踪：真实轨迹与测量值（角度单位：rad）]
)

*第 1 步迭代详解*

初始估计照旧故意设错，看它怎么爬回来：

$ hat(bold(x))_0 = vec(95, 190, 8, 6), quad bold(P)_0 = mat(100, 0, 0, 0; 0, 100, 0, 0; 0, 0, 25, 0; 0, 0, 0, 25) $

预测：

$ bold(x)_(1|0) = mat(1, 0, 1, 0; 0, 1, 0, 1; 0, 0, 1, 0; 0, 0, 0, 1) vec(95, 190, 8, 6) = vec(103, 196, 8, 6) $

预测状态对应的极坐标：

$ r_("pred") = sqrt(103^2 + 196^2) = 221.4 "m" $
$ theta_("pred") = "atan2"(196, 103) = 1.087 "rad" $

计算观测雅可比矩阵（在预测状态处）：

$ bold(H)_1 = mat(
  103\/221.4, 196\/221.4, 0, 0;
  -196\/221.4^2, 103\/221.4^2, 0, 0
) = mat(
  0.465, 0.885, 0, 0;
  -0.00400, 0.00210, 0, 0
) $

协方差预测（$bold(F)$ 为常数矩阵）：

$ bold(P)_(1|0) = bold(F) bold(P)_0 bold(F)^top + bold(Q) $

矩阵不小，中间数值就不铺开了，直接给结果：$bold(P)_(1|0)$ 的对角元素约为 $(125, 125, 26, 26)$。

卡尔曼增益：

$ bold(S)_1 = bold(H)_1 bold(P)_(1|0) bold(H)_1^top + bold(R) $

$ bold(K)_1 = bold(P)_(1|0) bold(H)_1^top bold(S)_1^(-1) $

测量残差：

$ bold(z)_1 - bold(h)(bold(x)_(1|0)) = vec(229.3, 1.082) - vec(221.4, 1.087) = vec(7.9, -0.005) $

状态修正：

$ hat(bold(x))_1 = vec(103, 196, 8, 6) + bold(K)_1 vec(7.9, -0.005) $

*完整追踪结果*

#figure(
table(
  columns: (auto, auto, auto, auto, auto, auto, auto),
  align: center,
  stroke: (x, y) => (
    top: if y == 1 or y == 0 { 1pt } else { 0pt },
    bottom: if y == 6 { 1pt } else { 0pt },
  ),
  inset: 8pt,
  [*t*], [*真实 x*], [*估计 x*], [*真实 y*], [*估计 y*], [*真实 $v_x$*], [*估计 $v_x$*],
  [0], [100], [95.0], [200], [190.0], [10], [8.0],
  [1], [110], [107.0], [205], [201.3], [10], [8.8],
  [2], [120], [120.7], [210], [210.8], [10], [12.1],
  [3], [130], [129.4], [215], [215.3], [10], [10.4],
  [4], [140], [141.2], [220], [221.6], [10], [11.0],
  [5], [150], [150.7], [225], [224.6], [10], [10.5],
),
caption: [EKF 追踪结果（部分状态）]
)

初始估计明明白白地错着（位置差约 11 m，速度差约 2 m/s），EKF 几步之内就收敛到真值附近，此后只剩测量噪声引起的小幅波动。非线性观测方程，就这样被雅可比矩阵驯服了。

==== 线性化误差与 EKF 的局限性

一阶泰勒展开在函数“足够接近线性”时效果不错，但它终究是在冒充——非线性一强，账就开始算错。

一个极端例子看清底细：观测函数 $h(x) = x^2$ 在 $x_0 = 0$ 处线性化，得到 $h(x) approx 0$（导数为零），原函数的信息一点不剩。实际系统很少这么极端，但非线性较强或估计误差较大时，线性化误差会一步步累积，轻则精度下降，重则滤波器直接发散。

EKF 的另一桩苦差是雅可比矩阵本身。本节的雷达例子求导不难，可你要是把观测模型做复杂——多传感器、带畸变、带弹道——手推雅可比既繁琐又容易错，错一个符号滤波器就疯。数值差分能顶替，但又引入额外的计算误差。

还有一层理论上的不安：EKF 假装线性化后的系统仍是高斯的，可非线性变换早就把高斯分布拧变形了，强行用高斯去套，等于把分布的高阶矩信息全扔了。

这些局限催生了别的路子。下一节的无迹卡尔曼滤波（UKF）思路完全不同：与其线性化函数，不如拿采样点直接去“探”非线性变换的效果。

==== 小结

扩展卡尔曼滤波用局部线性化把标准卡尔曼滤波带进了非线性世界：在当前估计点做一阶泰勒展开，用雅可比矩阵顶替原来的常数矩阵。

与标准卡尔曼滤波相比，EKF 的变化就这几处：

#table(
  columns: (auto, 1fr, 1fr),
  align: (center, left, left),
  stroke: (x, y) => (
    top: if y == 1 or y == 0 { 1pt } else { 0pt },
    bottom: if y == 4 { 1pt } else { 0pt },
  ),
  inset: 8pt,
  [], [*标准 KF*], [*EKF*],
  [状态转移], [$bold(F)$（常数矩阵）], [$bold(f)(dot)$（非线性函数）+ $bold(F)_k$（雅可比）],
  [观测方程], [$bold(H)$（常数矩阵）], [$bold(h)(dot)$（非线性函数）+ $bold(H)_k$（雅可比）],
  [协方差传播], [精确], [近似（一阶）],
  [计算复杂度], [较低], [较高（需计算雅可比）],
)

EKF 的适用前提是非线性适中、估计误差不大，线性化近似才兜得住。前提破了，就该换更硬的家伙——无迹卡尔曼滤波。


=== 无迹卡尔曼滤波

上一节的 EKF 靠线性化过日子——本质是用简单函数冒充复杂函数，弱非线性时够用，非线性一强，一阶近似的误差就压不住了。本节换一条完全不同的路：无迹卡尔曼滤波（Unscented Kalman Filter, UKF）压根不去近似函数，而是用几个精心挑选的采样点，直接去捕捉概率分布过完非线性变换后的统计特性。

==== 从函数近似到分布近似

EKF 的病根在哪？非线性函数会扭曲概率分布的形状：高斯进去，出来的通常已经不是高斯。EKF 的对策是把函数掰直，好让输出保持高斯——精度就是这么赔进去的。

UKF 的创始人 Jeffrey Uhlmann 换了个角度看问题：近似概率分布，比近似非线性函数容易。与其拿线性函数冒充 $bold(h)(bold(x))$，不如挑一组点代表输入分布，把这些点原原本本地送过真实的非线性函数，再从变换后的点把输出分布的统计量捡回来——函数本身从头到尾没被动过。

拿老例子对比一下。设 $x tilde cal(N)(0, 1)$，非线性变换 $y = x^2$。EKF 在 $x = 0$ 处线性化，得 $y approx 0$（导数为零），信息全灭。UKF 呢？挑几个代表点，比如 $x in {-1, 0, 1}$，算出变换值 $y in {1, 0, 1}$，再从这些点估计 $y$ 的均值和方差——不必知道 $y$ 的精确分布，统计量已经比 EKF 准得多。

这就是无迹变换（Unscented Transform）的基本思想：用确定性采样点代替随机采样，花很少的计算量，把非线性变换的效果“探”出来。

==== 无迹变换

把上面的思路做成标准零件。给定 $n$ 维随机变量 $bold(x)$，均值 $bold(mu)$，协方差 $bold(P)$，目标：算出经非线性函数 $bold(y) = bold(f)(bold(x))$ 变换后 $bold(y)$ 的均值和协方差。

*Sigma 点的选取*

无迹变换选取 $2n + 1$ 个确定性采样点，称为 Sigma 点：

$ chi_0 = bold(mu) $

$ chi_i = bold(mu) + (sqrt((n + lambda) bold(P)))_i, quad i = 1, ..., n $

$ chi_(i+n) = bold(mu) - (sqrt((n + lambda) bold(P)))_i, quad i = 1, ..., n $

其中 $(sqrt((n + lambda) bold(P)))_i$ 表示矩阵平方根的第 $i$ 列，$lambda = alpha^2 (n + kappa) - n$ 是缩放参数。$alpha$ 控制 Sigma 点的分散程度，通常取较小的正数（如 $10^(-3)$）；$kappa$ 通常取 $0$ 或 $3 - n$。

矩阵平方根 $sqrt(bold(P))$ 定义为满足 $sqrt(bold(P)) (sqrt(bold(P)))^top = bold(P)$ 的矩阵，通常用 Cholesky 分解计算。

Sigma 点的几何图景值得在脑子里画一遍：$chi_0$ 站在分布中心，其余 $2n$ 个点沿协方差矩阵的主轴对称排开，离中心多远由 $lambda$ 说了算。这一小队点足以精确复刻原分布的均值和协方差。

*权重分配*

每个 Sigma 点带着自己的权重上场，用于合成变换后的统计量。均值权重和协方差权重分别为：

$ W_0^m = frac(lambda, n + lambda) $

$ W_0^c = frac(lambda, n + lambda) + (1 - alpha^2 + beta) $

$ W_i^m = W_i^c = frac(1, 2(n + lambda)), quad i = 1, ..., 2n $

参数 $beta$ 用来融入对分布形状的先验，高斯分布下 $beta = 2$ 最优。

*变换与恢复*

把所有 Sigma 点送过非线性函数：

$ cal(Y)_i = bold(f)(chi_i), quad i = 0, 1, ..., 2n $

变换后分布的均值和协方差估计为：

$ bold(mu)_y = sum_(i=0)^(2n) W_i^m cal(Y)_i $

$ bold(P)_y = sum_(i=0)^(2n) W_i^c (cal(Y)_i - bold(mu)_y)(cal(Y)_i - bold(mu)_y)^top $

整个过程没碰过一次线性化，统计量直接从采样点的变换结果里长出来。由于 Sigma 点精确匹配原分布的前两阶矩，对高斯输入，无迹变换能把输出的均值和协方差捕捉到二阶精度（泰勒展开的二阶项）——比 EKF 的一阶高一档。

==== 无迹卡尔曼滤波算法

把无迹变换往预测和修正两个阶段一装，UKF 就成了。

*预测阶段*

先根据上一时刻的状态估计 $hat(bold(x))_(k-1)$ 和协方差 $bold(P)_(k-1)$ 生成 Sigma 点：

$ chi_(k-1)^((i)), quad i = 0, 1, ..., 2n $

把每个 Sigma 点送过状态转移函数：

$ chi_(k|k-1)^((i)) = bold(f)(chi_(k-1)^((i)), bold(u)_k) $

再从变换后的 Sigma 点合成预测均值和协方差：

$ bold(x)_(k|k-1) = sum_(i=0)^(2n) W_i^m chi_(k|k-1)^((i)) $

$ bold(P)_(k|k-1) = sum_(i=0)^(2n) W_i^c (chi_(k|k-1)^((i)) - bold(x)_(k|k-1))(chi_(k|k-1)^((i)) - bold(x)_(k|k-1))^top + bold(Q) $

过程噪声协方差 $bold(Q)$ 最后加上，处理方式与 EKF 相同。

*修正阶段*

把预测 Sigma 点送过观测函数：

$ cal(Z)_k^((i)) = bold(h)(chi_(k|k-1)^((i))) $

计算预测测量的均值：

$ bold(z)_(k|k-1) = sum_(i=0)^(2n) W_i^m cal(Z)_k^((i)) $

计算测量预测的协方差和状态-测量的互协方差：

$ bold(P)_(z z) = sum_(i=0)^(2n) W_i^c (cal(Z)_k^((i)) - bold(z)_(k|k-1))(cal(Z)_k^((i)) - bold(z)_(k|k-1))^top + bold(R) $

$ bold(P)_(x z) = sum_(i=0)^(2n) W_i^c (chi_(k|k-1)^((i)) - bold(x)_(k|k-1))(cal(Z)_k^((i)) - bold(z)_(k|k-1))^top $

卡尔曼增益：

$ bold(K)_k = bold(P)_(x z) bold(P)_(z z)^(-1) $

状态和协方差修正：

$ hat(bold(x))_k = bold(x)_(k|k-1) + bold(K)_k (bold(z)_k - bold(z)_(k|k-1)) $

$ bold(P)_k = bold(P)_(k|k-1) - bold(K)_k bold(P)_(z z) bold(K)_k^top $

#note("UKF 算法")[
*生成 Sigma 点*

$ chi_0 = hat(bold(x))_(k-1), quad chi_i = hat(bold(x))_(k-1) plus.minus (sqrt((n+lambda)bold(P)_(k-1)))_i $

*预测*

$ chi_(k|k-1)^((i)) = bold(f)(chi_(k-1)^((i))), quad i = 0, ..., 2n $
$ bold(x)_(k|k-1) = sum W_i^m chi_(k|k-1)^((i)) $
$ bold(P)_(k|k-1) = sum W_i^c (chi_(k|k-1)^((i)) - bold(x)_(k|k-1))(dot)^top + bold(Q) $

*修正*

$ cal(Z)_k^((i)) = bold(h)(chi_(k|k-1)^((i))) $
$ bold(z)_(k|k-1) = sum W_i^m cal(Z)_k^((i)) $
$ bold(P)_(z z) = sum W_i^c (cal(Z)_k^((i)) - bold(z)_(k|k-1))(dot)^top + bold(R) $
$ bold(P)_(x z) = sum W_i^c (chi_(k|k-1)^((i)) - bold(x)_(k|k-1))(cal(Z)_k^((i)) - bold(z)_(k|k-1))^top $
$ bold(K)_k = bold(P)_(x z) bold(P)_(z z)^(-1) $
$ hat(bold(x))_k = bold(x)_(k|k-1) + bold(K)_k (bold(z)_k - bold(z)_(k|k-1)) $
$ bold(P)_k = bold(P)_(k|k-1) - bold(K)_k bold(P)_(z z) bold(K)_k^top $
]

==== 示例：雷达追踪问题的 UKF 实现

还是上一节那架飞机、那串雷达测量，换 UKF 重新求解，和 EKF 掰掰手腕。

*系统设定*

状态向量 $bold(x) = vec(x, y, v_x, v_y)$（$n = 4$），需要 $2n + 1 = 9$ 个 Sigma 点。

为让 Sigma 点便于手算展示，本例取 $alpha = 0.1$，$kappa = 0$，$beta = 2$；若按 $alpha = 0.001$ 取值，$lambda = alpha^2 (n + kappa) - n approx -4$，Sigma 点几乎重叠在均值上、权重达 $plus.minus 10^6$ 量级，不便展示。

其他参数（$bold(Q)$、$bold(R)$、初始条件）与上一节相同。

*第 1 步迭代*

初始估计 $hat(bold(x))_0 = vec(95, 190, 8, 6)$，初始协方差 $bold(P)_0 = "diag"(100, 100, 25, 25)$。

生成 Sigma 点。由 $alpha = 0.1$ 得 $lambda = 0.01 times 4 - 4 = -3.96$，$n + lambda = 0.04$。

矩阵平方根 $sqrt(0.04 times bold(P)_0) = 0.2 times "diag"(10, 10, 5, 5) = "diag"(2, 2, 1, 1)$。

9 个 Sigma 点为：

$ chi_0 = vec(95, 190, 8, 6) $

$ chi_1 = vec(97, 190, 8, 6), quad chi_5 = vec(93, 190, 8, 6) $

$ chi_2 = vec(95, 192, 8, 6), quad chi_6 = vec(95, 188, 8, 6) $

$ chi_3 = vec(95, 190, 9, 6), quad chi_7 = vec(95, 190, 7, 6) $

$ chi_4 = vec(95, 190, 8, 7), quad chi_8 = vec(95, 190, 8, 5) $

把每个 Sigma 点送过状态转移函数（匀速运动）：

$ chi_(1|0)^((i)) = bold(F) chi_0^((i)) $

例如 $chi_(1|0)^((0)) = vec(95 + 8, 190 + 6, 8, 6) = vec(103, 196, 8, 6)$。

计算预测均值（各 Sigma 点变换结果的加权平均）：

$ bold(x)_(1|0) = sum_(i=0)^8 W_i^m chi_(1|0)^((i)) $

状态转移是线性的，预测均值与 EKF 相同：$bold(x)_(1|0) = vec(103, 196, 8, 6)$。

把预测 Sigma 点送过观测函数：

$ cal(Z)^((i)) = vec(sqrt(x_i^2 + y_i^2), "atan2"(y_i, x_i)) $

例如 $cal(Z)^((0)) = vec(sqrt(103^2 + 196^2), "atan2"(196, 103)) = vec(221.4, 1.087)$。

从变换后的点合成预测测量均值 $bold(z)_(1|0)$、协方差 $bold(P)_(z z)$ 和互协方差 $bold(P)_(x z)$，进而得到卡尔曼增益和状态修正。

*追踪结果对比*

#figure(
table(
  columns: (auto, auto, auto, auto, auto, auto),
  align: center,
  stroke: (x, y) => (
    top: if y == 1 or y == 0 { 1pt } else { 0pt },
    bottom: if y == 6 { 1pt } else { 0pt },
  ),
  inset: 8pt,
  [*t*], [*真实 x*], [*EKF x*], [*UKF x*], [*EKF $v_x$*], [*UKF $v_x$*],
  [0], [100], [95.0], [95.0], [8.0], [8.0],
  [1], [110], [107.0], [106.9], [8.8], [8.8],
  [2], [120], [120.7], [120.6], [12.1], [12.1],
  [3], [130], [129.4], [129.3], [10.4], [10.4],
  [4], [140], [141.2], [141.2], [11.0], [11.0],
  [5], [150], [150.7], [150.7], [10.5], [10.5],
),
caption: [EKF 与 UKF 追踪结果对比：六个时刻的位置与速度估计至多相差 0.1——弱非线性下一阶线性化已经够准，UKF 的优势要到强非线性场景才兑现。]
)

两列几乎一个模子：这个场景的雷达观测虽然非线性，但在估计点附近弯得不厉害，一阶线性化已经够准。别急着替 UKF 喊冤——目标一旦靠近雷达站（$r$ 变小），角度观测的非线性陡然增强，两者的差距就拉开了。你的自瞄同理：目标贴脸时角度变化最剧烈，恰恰是非线性最咬人的时候。

==== EKF 与 UKF 的对比

两种方法是处理非线性的两种哲学：EKF 近似函数，UKF 近似分布。摆一张对照表：

#table(
  columns: (auto, 1fr, 1fr),
  align: (center, left, left),
  stroke: (x, y) => (
    top: if y == 1 or y == 0 { 1pt } else { 0pt },
    bottom: if y == 8 { 1pt } else { 0pt },
  ),
  inset: 8pt,
  [], [*EKF*], [*UKF*],
  [核心思想], [线性化非线性函数], [采样传播概率分布],
  [近似对象], [函数（泰勒展开）], [分布（Sigma 点）],
  [精度], [一阶], [二阶],
  [雅可比矩阵], [需要解析求导], [不需要],
  [计算量], [较低], [较高（$2n+1$ 次函数求值）],
  [实现难度], [需推导雅可比], [仅需实现系统函数],
  [数值稳定性], [一般], [较好],
  [适用场景], [弱非线性、雅可比易求], [强非线性、雅可比难求],
)

选型的经验法则就几条。非线性弱、雅可比好求，用 EKF，简单高效。非线性强，或者雅可比根本没法解析求导——比如你的观测模型来自弹道仿真表或神经网络，导数无从下笔——UKF 更稳健：它只要求你会调用函数。状态维度很高（$n > 100$）时，$2n + 1$ 个 Sigma 点的函数求值成了负担，那就该看粒子滤波之类的路子了。

==== 参数选择指南

UKF 的表现挂在 $alpha$、$beta$、$kappa$ 三个旋钮上，逐个说。

$alpha$ 控制 Sigma 点撒多开。取小（如 $10^(-3)$）则紧贴均值，适合近似局部非线性；取大（如 $1$）撒得广，能捕到更大范围的非线性效应，但可能引入高阶误差。实践中 $alpha = 0.001$ 到 $0.01$ 是常见选择。

$beta$ 融入对分布形状的先验。高斯分布下 $beta = 2$ 理论最优，能精确捕捉四阶矩；分布明显偏离高斯可以调，但通常影响不大。

$kappa$ 是次要的缩放参数，常取 $kappa = 0$（省事）或 $kappa = 3 - n$（保正定）。$n + lambda$ 接近零或为负会闹数值问题，那时再动它。

大多数应用从 $alpha = 0.001$，$beta = 2$，$kappa = 0$ 起步，按实际效果微调即可。

==== 小结

UKF 给非线性问题换了一条思路：不对函数动刀，拿 Sigma 点代表概率分布，把点送过真实的非线性函数，再从变换后的点恢复统计特性。省掉了雅可比，精度还高一阶。

优势：不需要解析求导，实现更省心；二阶精度压过 EKF 的一阶；对强非线性更稳健。代价：$2n + 1$ 次函数求值，计算量更大。

到这里，你已经从体重秤一路走到了 UKF。回望整条来路：

#table(
  columns: (auto, 1fr, 1fr),
  align: (center, left, left),
  stroke: (x, y) => (
    top: if y == 1 or y == 0 { 1pt } else { 0pt },
    bottom: if y == 6 { 1pt } else { 0pt },
  ),
  inset: 8pt,
  [*方法*], [*解决的问题*], [*核心思想*],
  [递推平均], [静态量估计], [历史数据等权平均],
  [g-h 滤波器], [动态系统追踪], [预测 + 修正（固定增益）],
  [卡尔曼滤波], [最优增益选取], [基于方差最小化动态计算增益],
  [多维 KF], [多状态关联估计], [协方差矩阵描述状态间关联],
  [EKF], [非线性系统], [局部线性化（雅可比矩阵）],
  [UKF], [强非线性系统], [采样传播（Sigma 点）],
)

每一步演进都在补上一步的短板：递推平均追不动动态目标，g-h 的增益靠手感，标准卡尔曼只吃线性，EKF 在强非线性下失准。这条演进逻辑比公式本身更值得带走——从具体问题出发，逐步放松假设，构建更普适的解法，这正是工程方法论的样子。带着它，去看这套理论怎么落到你的自瞄上。


=== 卡尔曼滤波在自瞄里的落地

前面的推导一直拿赛车和雷达当例子，现在把主角换回装甲板。本节不引入新数学，而是把全章理论对应到自瞄系统的四个工程问题上：状态怎么选，$bold(Q)$ 和 $bold(R)$ 怎么定，量测断了怎么办，小陀螺怎么破。

==== 匀速模型：跟踪一块装甲板

先明确滤波发生在哪个坐标系。「坐标变换」一章已经论证过：预测必须在惯性系里做——云台在转，相机系里连静止的目标都在“运动”，把相机系坐标直接喂给运动模型，等于让滤波器去追一个由云台转动虚构出来的目标。因此数据链路是：PnP 解算出装甲板在相机系下的位置（「相机模型」一章），沿坐标链变换到惯性系（「坐标变换」一章），再交给滤波器。

状态取目标在惯性系下的位置与速度：

$ bold(x) = (x, y, z, v_x, v_y, v_z)^top $

图像能测到的只有位置，速度进不了观测方程。但“多维卡尔曼滤波”一节已经证明：只要协方差把两者关联起来，速度就能从位置序列中被间接估计出来。这正是自瞄采用卡尔曼滤波的第一动机——预测需要速度，而没有任何传感器直接测量目标的速度。

匀速（Constant Velocity, CV）模型假设目标在相邻两次更新之间匀速直线运动，把实际存在的加减速全部归入过程噪声。它的 $bold(F)$ 和 $bold(H)$ 就是本章位置-速度例子的三维分块版本：

$ bold(F) = mat(bold(I)_3, Delta t bold(I)_3; bold(0), bold(I)_3), quad bold(H) = mat(bold(I)_3, bold(0)) $

其中 $bold(I)_3$ 为 $3 times 3$ 单位阵。取出任意一个轴，就还原成熟悉的 $mat(1, Delta t; 0, 1)$ 与 $mat(1, 0)$。有一个容易被忽略的实车细节：*$Delta t$ 不是常数*。它是相邻两次滤波更新之间的实际时间差——帧率会波动、图像会丢帧，每一步都必须用当前时间戳算出 $Delta t$，重新构造 $bold(F)$（以及下面的 $bold(Q)$）。把 $Delta t$ 写成固定值，是实车上最常见的滤波 bug 之一。

==== Q 与 R：两种不确定性各自从哪来

$bold(R)$ 描述测量有多不可信，它是可以测出来的：把机器人和目标都固定，录一段识别数据，统计解算位置的方差，就得到 $bold(R)$ 的量级。做过这个实验你会发现 $bold(R)$ 不该是常数：目标越远，灯条在图像里越小，PnP 的误差越大——深度方向尤甚（「相机模型」一章分析过这种病态性），所以工程实现常把 $bold(R)$ 写成随距离增大的函数。更严格的做法还要注意：噪声诞生在相机系（深度方向的方差远大于横向），变换到惯性系之后各轴不再独立。协方差随坐标旋转的变化正是“协方差变换公式”那条注记的直接应用：设 $bold(C)$ 为相机系到惯性系的旋转矩阵，则 $bold(R)^n = bold(C) bold(R)^c bold(C)^top$；平移不改变离散程度，所以只有旋转参与变换。

$bold(Q)$ 描述模型撒了多大的谎。CV 模型宣称“目标匀速”，可实际目标会加减速——设未建模的加速度是标准差为 $sigma_a$ 的零均值噪声，一步 $Delta t$ 内它带来 $1/2 a Delta t^2$ 的位置偏差和 $a Delta t$ 的速度偏差，即噪声通过向量 $bold(Gamma) = (Delta t^2\/2, quad Delta t)^top$ 进入每个轴的状态，因此

$ bold(Q)_"单轴" = sigma_a^2 bold(Gamma) bold(Gamma)^top = sigma_a^2 mat(Delta t^4\/4, Delta t^3\/2; Delta t^3\/2, Delta t^2) $

$sigma_a$ 按目标可能达到的加速度量级取值。顺带一个工程细节：这个 $bold(Q)$ 是按“每帧一步”推出来的，按 1 kHz 小步长反复预测时，工程上会改用连续白噪声离散化的 $bold(Q)$，使注入的不确定性与步长无关（本章异步图的脚本正是这么做的）。真正需要建立的直觉是：*$bold(Q)$ 与 $bold(R)$ 的比值是滤波器唯一的“旋钮”*。调大 $bold(Q)$（或调小 $bold(R)$），滤波器承认模型不可靠，增益变大、更信测量——跟踪变快，但测量噪声更多地漏进输出；反过来则输出平滑，但目标一变速估计就拖在后面。这与低通滤波器选截止频率是同一个权衡，只是换成了概率语言。对自瞄来说，两种失败模式都有俗名：滞后叫“甩枪”，噪声叫“云台抖”，调参就是在这两种失败之间找平衡点。

==== 异步更新：量测会断，预测不停

自瞄系统里有几个时钟在同时走：电控与 IMU 的数据以 1 kHz 量级到来，相机只有一两百帧，目标还随时可能被遮挡、被打灭、转出视野。好在预测和修正本来就是两个独立的步骤，可以拆开执行：有量测时，跑完整的“预测 + 修正”；没有量测时，只跑预测两式——状态照常外推，$bold(P)$ 照常增长。

这一拆解一次解决三件事。其一是*插帧*：控制回路可以随时向滤波器要“此刻”的目标状态，哪怕两帧图像之间隔了 10 ms，云台的目标角也在平滑变化，而不是每来一帧图像跳一下。其二是*丢失容忍*：目标被挡住几帧，滤波器靠模型硬推，$bold(P)$ 的增长如实记录着“我越来越不确定”；等目标重新出现，变大的 $bold(P)$ 使增益 $bold(K)$ 自动变大，新测量被大权重采纳，估计迅速收回——这正是“增益随不确定性自适应”在实战里的样子。当然模型不能硬推太久：纯预测下 $bold(P)$ 无界增长，工程上会给丢失时长设上限，超时就废弃该目标、重新初始化。其三是*向未来外推*：预测不仅能推到“此刻”，还能推到任意未来时刻——拿当前状态沿模型向前推一个弹丸飞行时间，就得到瞄准点。飞行时间与弹道补偿怎么算，是「实战技术篇」弹道解算一章的主场；多路数据的时间戳如何对齐，也在「实战技术篇」单独讨论。

#figure(
  image("images/theory-kalman-async-update.png", width: 100%),
  caption: [异步更新的仿真：CV 模型跟踪一个变速目标，预测以 1 kHz 运行，量测以 100 Hz 到来，中段灰色区域为 0.6 s 的量测中断（脚本 `examples/theory/kalman_async_update.py`）。上图：中断期间估计（粗蓝线）沿匀速模型外推，与真值（细黑线）的偏差最大约 28 cm，同时 $plus.minus 3 sigma$ 置信带（浅蓝）张开到约 58 cm——滤波器不仅在漂移，还“知道”自己在漂移；量测恢复的第一帧，变大的增益把估计一步拉回真值附近。下图：速度从未被直接测量，却被滤波器从位置序列中恢复出来；中断期间速度估计冻结在最后的外推值上，这正是位置漂移的根源。],
)

==== 整车建模：从跟一块板到跟一台车

CV 模型对平动目标够用了，可对手一开小陀螺就会露馅：底盘以每秒一转以上的角速度自旋，装甲板绕车体中心画圈——正对你的那块转走，另一块从另一侧转进来。逐板跟踪的滤波器看到的是一串短命目标：每块板在视野里只存活零点几秒，速度方向不停翻转，CV 模型的预测点常常甩到车体之外。基础篇 OpenCV 一节留下的“整车建模”伏笔，就在这里兑现。

出路是换一个状态空间：跟踪的对象不再是装甲板，而是整台车。常规底盘的四块装甲板对称地安装在四周，于是一台开陀螺的车可以建模成“中心平动 + 四块板绕中心匀速旋转”。以水平面内的运动为例，状态取

$ bold(x) = (x_c, y_c, v_x, v_y, theta, omega, r)^top $

其中 $(x_c, y_c)$ 与 $(v_x, v_y)$ 是车体中心的位置与速度，$theta$ 是某一块装甲板相对中心的方向角，$omega$ 是自旋角速度，$r$ 是旋转半径。观测仍然只有一块可见装甲板的位姿——PnP 给出它的位置和朝向。设 $theta$ 为从中心指向该装甲板的方向（装甲板法向朝外，与它一致），几何关系为

$ x_a = x_c + r cos theta, quad y_a = y_c + r sin theta $

其余三块板的方向角依次相差 90°、180°、270°，所以看到任何一块板，更新的都是同一组 $(theta, omega, r)$。注意 $r cos theta$ 是状态的非线性函数——EKF 一节推导的雅可比矩阵在这里正式上岗。实车还要更细一点：相邻两组装甲板的安装半径与高度可以不同，工程实现会在状态里维护两个半径交替使用，但骨架就是上面这一行几何。

这套模型的回报是巨大的。观测一块板，修正的是整台车的状态；板转出视野后，$theta$ 和 $omega$ 仍在预测中推进，模型随时知道每一块板将在何时、何地转到弹道上——于是可以把枪口指向车体中心，等某块板转过来的瞬间开火。对单板滤波器无从下手的小陀螺，在整车状态空间里不过是一个匀速转动。至于如何判定对手进入陀螺状态、开火时机怎么选，是「RoboMaster应用篇」自瞄算法设计一章的内容。

#figure(
  image("images/theory-kalman-spin-model.png", width: 100%),
  caption: [为什么要整车建模（脚本 `examples/theory/kalman_spin_model.py`）。左图：把一台车抽象为中心 $(x_c, y_c)$ 加四块绕中心旋转的装甲板，状态含方向角 $theta$、角速度 $omega$ 与半径 $r$；相机每一时刻只能看到朝向它的板（蓝色实线），但每一次观测更新的都是整台车的状态。右图：小陀螺（1.5 转/秒）叠加平移（1 m/s）时，装甲板在惯性系里划出打圈的轨迹（蓝色），速度方向不停翻转，CV 滤波器无从跟踪；而车体中心（品红）走的只是一条匀速直线——把状态空间换到中心上，难题重新变简单。],
)

==== 回到基础篇的那段代码

基础篇「Eigen 矩阵库」一节给出过一个完整的 EKF 类，当时你多半只能把它当模板抄。现在回头看，每一行都有了名字：状态 $(x, y, theta, v_x, v_y, omega)$ 是一个带朝向角的平面运动模型；`predict()` 先用非线性运动学推进状态、再用手写的雅可比 $bold(F)$ 传播协方差，正是 EKF 的预测两式；`updatePosition()` 里的 $bold(S)$、$bold(K)$、状态修正与协方差修正，正是修正三式。那段代码里还有一个当时没解释的细节：残差的角度分量被归一化回 $plus.minus pi$ 之间。角度住在圆上，$359 degree$ 和 $-1 degree$ 是同一个方向——若预测朝向为 $-1 degree$、量测为 $359 degree$，朴素相减会得到 $360 degree$ 的假残差，而真实偏差是零，一次这样的错误修正就足以让滤波器发散。凡是把角度放进状态或观测（比如整车模型里的 $theta$），这一步都不可省略。

到这里，理论篇的链条就闭合了：「相机模型」回答观测从哪里来，「坐标变换」回答在哪个坐标系里估计，本章回答如何从带噪的观测中恢复状态、预测未来。把这条链变成跑在实车上的程序——标定、对时、弹道、决策——是接下来两篇的事。
