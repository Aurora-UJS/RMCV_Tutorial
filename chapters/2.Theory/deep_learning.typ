#import "/template/template.typ": *

基础篇的识别流水线走到最后一步，把一张 20×28 的二值小图交给了 `mlp.onnx`：它给出“guard 99.9%”，也把两个错误配对判断为“negative 100.0%”。当时我们只说明它是一个 MLP，没有展开网络怎样从训练数据得到这些输出。本章从单个神经元讲到多层网络，从梯度下降讲到反向传播，再说明卷积网络为什么适合处理图像、YOLO 如何把检测写成回归问题，以及关键点网络怎样输出 PnP 所需的灯条角点。最后回到 `mlp.onnx`，逐层查看它的计算图与 78249 个参数。

=== 从一个神经元说起

回想基础篇的灯条判据：宽长比在 0.1 到 0.4 之间、倾角小于 40 度。它本质上是在由宽长比和倾角组成的二维特征空间里画出几条边界，边界一侧判为灯条，另一侧则不是。这些边界不是由程序自己学出来的，而是工程人员根据长期测试经验手动设定的。

*感知机*（perceptron）做的是同一件事，区别只有一个：直线的位置由数据说了算。它接收一个特征向量 $bold(x)$，输出一个由线性函数决定的判断：

$
y = cases(1 quad bold(w) dot bold(x) + b > 0, 0 quad "否则")
$

权重 $bold(w)$ 决定分界面的方向，偏置 $b$ 决定它到原点的距离。在二维中，分界面是一条直线；三维中是一个平面；560 维中则是 559 维超平面。维数再高，它做的仍是同一件事：用一个平面把特征空间“一刀切”成两部分。训练感知机，就是根据标注数据自动调整 $bold(w)$ 和 $b$，让这个平面尽量分开两类样本。感知机在 20 世纪 50 年代末提出，是早期从样本学习分类边界的代表性模型。

但一个平面也有分不开的情况。最简单的反例是平面上的四团点：位于对角线上的两团属于同一类，也就是逻辑 XOR 关系。你可以拿笔试一试——无论怎样画直线，都无法把两类点完全分开。1969 年，Minsky 和 Papert 在《Perceptrons》一书中系统讨论了这类局限。问题的关键在于：单层模型仍然是线性的。

=== 多层感知机：非线性从哪里来

一种自然的想法是把感知机一层层摞起来：第一层的输出交给第二层，逐层传递，这就是*多层感知机*（Multi-Layer Perceptron，MLP）。不过，这里藏着一个很值得亲手推一遍的陷阱——只增加线性层，仍然解决不了 XOR。设第一层做线性变换 $bold(h) = bold(W)_1 bold(x) + bold(b)_1$，第二层再做 $bold(y) = bold(W)_2 bold(h) + bold(b)_2$，代入展开：

$
bold(y) = bold(W)_2 (bold(W)_1 bold(x) + bold(b)_1) + bold(b)_2 = (bold(W)_2 bold(W)_1) bold(x) + (bold(W)_2 bold(b)_1 + bold(b)_2)
$

两层线性层的复合仍然等价于一个线性层，其权重只是换成 $bold(W)_2 bold(W)_1$。即使堆叠一百层，线性函数的复合仍然是线性函数，XOR 依旧切不开。可见，网络层数本身不会自动带来表达能力，真正关键的是非线性变换。

因此，每层线性变换后还要逐元素经过一个非线性的*激活函数*。经典选择是 sigmoid 函数 $sigma(z) = 1\/(1 + e^(-z))$，把任意实数映射到 $(0, 1)$；现代网络更常使用 ReLU：$"ReLU"(z) = max(0, z)$，也就是负数归零、正数原样通过。这样，“线性变换 → 弯折 → 线性变换 → 再弯折”的交替就能拼出非线性决策边界。通用近似定理进一步说明，只要隐层神经元足够多，单隐层 MLP 就能以任意精度逼近紧集上的任意连续函数。需要注意，这只是存在性结论：它保证某组权重存在，并不保证训练算法一定能找到这组权重。

下面在刚才的 XOR 数据上分别训练线性模型和带非线性激活的 MLP：

#figure(
  image("images/theory-dl-nonlinearity.png", width: 100%),
  caption: [线性模型与 MLP 在脚本生成的 XOR 形数据上的决策边界。左：逻辑回归只能给出一条直线，在这组训练样本上的准确率为 50%；右：含 8 个隐层神经元和 tanh 激活的 MLP 得到两块对角预测区域，训练准确率为 100%。背景颜色表示预测类别，黑色曲线是决策边界。这个受控例子说明非线性激活可以表达线性边界无法表示的区域，但不比较两类模型在其他数据上的泛化能力。生成脚本：`examples/theory/dl_nonlinearity.py`。],
)

隐层还可以理解为一组*从数据中学到的特征*。每个隐层神经元对输入做一次线性组合，再经过非线性激活；多个神经元的输出共同构成新的特征表示。原始空间中无法线性分开的两类，在这个新表示中可能变得容易分开。基础篇由人工设计宽长比和倾角等特征，而 MLP 则在隐层中自动学习适合当前分类任务的组合。

==== softmax：把分数归一化

分类网络的最后一层输出 $K$ 个实数 $z_1, ..., z_K$（行话叫 *logits*，未归一化的“证据分数”），每类一个。分数有正有负、总和也不是 1，不好直接当概率用。softmax 负责这最后一步转换：

$
p_k = frac(e^(z_k), sum_(j=1)^K e^(z_j))
$

指数保证每一项为正，分母保证总和为 1，且指数单调，所以分数的大小顺序保持不变。举个可以直接验算的例子：logits 为 $(2, 1, -1)$，softmax 后约为 $(0.71, 0.26, 0.04)$。任意两类输出之比为 $e^(z_i-z_j)$，因此 logit 差值决定了归一化后的相对大小。

softmax 还有一条实用性质：给所有 logits 同时加减一个常数，分子和分母会乘上相同因子，输出不变。工程上通常先减去最大 logit 再取指数，以免 $e^z$ 上溢；基础篇 `lightbar_pipeline.cpp` 中的 `cv::exp(out - max)` 正是在做这一步。`mlp.onnx` 的最后一层输出 9 个原始 logits，softmax 由网络外的 C++ 代码计算。softmax 给出的是总和为 1 的分类分数；它是否与经验正确率一致，还需要在独立验证集上评估和校准，不能把单次输出的 99.9% 直接理解为实际必有 99.9% 的正确概率。

==== 交叉熵：衡量分类误差

有了归一化输出，还需要用*损失函数*衡量预测与标签的差异。单标签分类常用*交叉熵*（cross-entropy）：设正确类别是 $c$，损失为

$
L = -ln p_c
$

它只取模型分给正确类别的输出。$p_c = 1$ 时损失为 0；$p_c = 0.71$ 时损失约 0.35；$p_c = 0.01$ 时损失约 4.6，且 $p_c arrow.r 0$ 时损失趋于无穷。因此，给错误类别很高分会产生较大的损失。

交叉熵与 softmax 配合使用时，梯度形式很简单。把 softmax 代入损失：$L = -z_c + ln sum_j e^(z_j)$，对任意一个 logit $z_k$ 求偏导：

$
frac(partial L, partial z_k) = frac(e^(z_k), sum_j e^(z_j)) - cases(1 quad k = c, 0 quad k eq.not c) = p_k - y_k
$

其中 $y_k$ 是 one-hot 标签，正确类为 1，其余为 0。梯度就是*预测概率减标签*：预测为 0.71 而标签为 1 时，梯度为 $-0.29$，更新会提高对应 logit；预测为 0.26 而标签为 0 时，梯度为 $+0.26$，更新会降低对应 logit。指数和对数在求导时相互抵消，不会引入额外系数，也避免了“sigmoid 加均方误差”在饱和区容易出现的梯度过小问题。后面讨论训练时还会继续使用这个 $p - y$。

=== 训练：优化 78249 个参数

网络结构定了，剩下的问题只有一个：权重取什么值。`mlp.onnx` 有 78249 个参数，也就是说我们要在一个 78249 维的空间里找一个点，使得网络在训练集上的平均损失尽可能小。这是一个纯粹的优化问题。

==== 梯度下降：沿负梯度更新

把损失 $L$ 看成参数 $theta$ 的函数，可以借用地形来理解：参数位置对应地形上的一点，损失是高度，训练要寻找较低的位置。高维参数空间无法完整画出，但在当前参数处可以计算梯度；$nabla L$ 指向局部上升最快的方向，因此沿相反方向更新：

$
theta arrow.l theta - eta nabla L
$

步长 $eta$ 叫*学习率*，是深度学习中的关键超参数：迈得太大，可能一步跨过低谷，甚至越走损失越高；迈得太小，又要走很多步才能取得明显进展。严格的梯度要在全部训练样本上求平均，计算开销较高，因此实践中通常每步只抽取一小批样本（mini-batch，常见为 32 到 256 个）来估计梯度。单步便宜了，代价是方向中会带有采样噪声。基于小批量的随机梯度下降（SGD）及其变体，正是在这项权衡下成为常用方法。

==== 反向传播：链式法则与中间量复用

梯度下降每步都需要 $nabla L$，也就是损失对全部 78249 个参数的偏导数。直接使用数值微分时，需要把每个权重分别改变一个很小的量，再运行前向传播观察损失变化；对 78249 个参数逐一重复，意味着每次更新都要运行数万次前向传播，计算开销无法接受。

*反向传播*（backpropagation）利用链式法则和中间结果复用，使全部参数梯度的计算量通常与少数几次前向传播同量级，而不必为每个参数单独重算网络。具体开销取决于算子与实现，并且需要保存或重算前向中间量。下面用一个最小的两层网络把数字走通：输入 $x = 1$，隐层 $h = sigma(w_1 x)$，输出 $hat(y) = w_2 h$，损失 $L = 1/2 (hat(y) - y)^2$，目标 $y = 1$，初值 $w_1 = 0.5$、$w_2 = 0.8$。

前向传播，顺着算并*记下每个中间量*：

$
z = w_1 x = 0.5, quad h = sigma(0.5) approx 0.6225, quad hat(y) = w_2 h approx 0.4980, quad L approx 0.1260
$

反向传播，从损失出发逐层往回求导：

$
frac(partial L, partial hat(y)) &= hat(y) - y approx -0.5020 \
frac(partial L, partial w_2) &= frac(partial L, partial hat(y)) dot frac(partial hat(y), partial w_2) = -0.5020 times underbrace(0.6225, h "（前向存的）") approx -0.3125 \
frac(partial L, partial h) &= frac(partial L, partial hat(y)) dot w_2 = -0.5020 times 0.8 approx -0.4016 \
frac(partial L, partial w_1) &= frac(partial L, partial h) dot underbrace(sigma'(z), h(1-h) approx 0.2350) dot x approx -0.0944
$

两个梯度都是负的，因此按负梯度方向更新会增大两个权重。取 $eta = 0.5$：$w_1: 0.5 arrow.r 0.5472$，$w_2: 0.8 arrow.r 0.9562$；再次前向计算，损失从 0.1260 降到 0.0777。这展示了一次参数更新。真实训练还会采用批量张量运算、不同损失与正则化，并重复执行前向、反向和更新过程。

注意反向过程的结构：计算 $partial L \/ partial w_1$ 时，直接使用上一行已经得到的 $partial L \/ partial h$，而不是从头展开整条链式法则。深层网络也采用相同方式，每层复用后一层传回的梯度，一次反向遍历即可得到所有参数的偏导。反向传播的数学基础是链式法则，计算效率则来自中间量复用；从算法角度看，这与动态规划中“中间结果只计算一次”的思想相同。

深层网络还可能出现*梯度消失*。观察 $sigma'(z) = h(1-h)$，其最大值只有 0.25；反向传播每经过一个 sigmoid，局部导数都会带来一个不超过 0.25 的乘子。如果其余权重雅可比没有补偿，这些乘子连续相乘会使靠近输入端的梯度迅速变小。ReLU 在正区间的导数为 1，避免了 sigmoid 导数这一项造成的连续衰减，这是它在深层网络中更常用的原因之一；网络权重、归一化、残差连接等结构仍会共同影响实际梯度。

==== 优化器：利用历史梯度

朴素梯度下降在狭长谷形的损失面上容易收敛缓慢。某些参数方向很陡，另一些方向较平缓，两个方向的曲率相差上百倍并不少见。梯度主要指向当前最陡的方向，因此更新会在陡峭方向反复振荡，而沿平缓方向前进得很慢：

#figure(
  image("images/theory-dl-optimizers.png", width: 100%),
  caption: [两种算法在条件数为 100 的二维二次损失上各更新 60 步（脚本 `examples/theory/dl_optimizers.py`）。灰色椭圆是损失等高线，五角星表示最小值。按脚本给定的同一起点和两组学习率，朴素梯度下降在高曲率方向反复换向，终点损失约为 0.05；动量法累积历史梯度，终点损失约为 0.0016。该图比较的是这组受控参数，不表示动量法在任意目标函数和超参数下都更快。],
)

*动量法*（momentum）不只使用当前梯度，而是维护一个常称为“速度”的向量，让历史梯度按指数衰减累积：

$
bold(v) arrow.l beta bold(v) + nabla L, quad theta arrow.l theta - eta bold(v)
$

$beta$ 常以 0.9 作为起始值。在连续更新中反复变号的梯度分量会部分抵消，而长期同号的分量会累积。这个效果依赖学习率、$beta$ 和损失面的局部形状，仍需结合训练曲线调参。

*Adam* 在一阶动量之外再估计梯度的二阶矩，为不同参数调整更新尺度，并在训练初期进行偏置修正。它常作为工程实验的起点，但不保证在所有任务上比带动量的 SGD 收敛更快或泛化更好。例如，训练装甲板分类器时可以从 `Adam(lr=1e-3)` 开始试验，再根据训练与验证曲线比较学习率和优化器。

=== 过拟合：训练表现不等于泛化

到这里，结构、损失、梯度和优化器已经连成训练过程。但把训练损失降到 0 并不是最终目标，因为部署画面不会与训练集完全相同。模型在未参与训练的数据上的表现称为*泛化*；模型若过度适应训练样本及其中的噪声，便会出现*过拟合*。

比较一下模型容量和数据量：`mlp.onnx` 有 78249 个参数，而一个队伍能够标注的装甲板样本通常只有几千到几万张。参数多于样本并不必然导致过拟合，但模型确实有能力把训练图中的噪声和标注误差一并记住，表现为训练准确率很高，在新画面上却明显下降。这类似只记住题库而没有掌握规律。常用的判断方法是从数据中留出一部分*验证集*，不参与参数更新；如果训练损失继续下降，而验证损失开始回升，就说明模型正在过度适应训练数据。

常见对策包括：

- *数据增广*：对已有样本做平移、缩放和亮度扰动，构造更多符合任务假设的输入变化。它不能代替真正独立的新场景数据，但可以减少模型对某个位置或亮度的依赖。对 RM 任务，亮度和对比度抖动尤其值得尝试，因为它模拟了不同场馆和曝光参数下的成像差异。增广必须尊重任务本身的不变性：装甲板贴图水平镜像后“5”就不再是“5”，这类数字样本不能沿用镜像前的标签。
- *早停*：在验证损失达到最低点时保存权重，避免继续训练后过拟合加重。
- *Dropout*：训练时每一步随机让一部分隐层神经元失活，降低特征之间的共适应，可作为一种正则化方法。推理时不再随机丢弃神经元，并按所采用的实现约定处理幅度缩放。丢弃比例需要验证，并非越大越好。
- *权重衰减*：在更新中抑制过大的权重；常见的 L2 正则化形式是在损失中加入 $lambda norm(theta)^2$。它可以降低某些模型的有效复杂度，但不保证所有深层网络的输出都更平缓，强度仍需用验证集选择。

这些方法各有适用条件，实践中往往组合使用。训练技巧无法弥补数据分布中完全缺失的情况；增加覆盖不同场地、曝光和目标状态的标注样本，通常比只在现有数据上反复调参更有效。

=== 拆解 mlp.onnx：级联末端的分类器

现在回到基础篇中的 `mlp.onnx`。它位于几何筛选流程的最后一步：两根灯条完成配对后，中间区域经过透视矫正和 Otsu 二值化，变成 20×28 的小图（Otsu 原理见「计算机视觉基础」一章），再由分类器判断它属于数字、特殊图案还是负样本。读取 ONNX 计算图，可以得到下面的结构表：

#figure(
  table(
    columns: (auto, auto, auto, auto),
    align: (left, center, center, right),
    inset: 7pt,
    stroke: (x, y) => (
      top: if y == 0 or y == 1 { 1pt } else { 0pt },
      bottom: if y == 4 { 1pt } else { 0pt },
    ),
    [*层*], [*输出维度*], [*激活*], [*参数量*],
    [Flatten（20×28 展平）], [560], [—], [0],
    [全连接 1], [120], [ReLU], [$560 times 120 + 120 = 67320$],
    [全连接 2], [84], [ReLU], [$120 times 84 + 84 = 10164$],
    [全连接 3（输出 logits）], [9], [—], [$84 times 9 + 9 = 765$],
  ),
  caption: [`mlp.onnx` 包含三层全连接，共 78249 个参数。全连接层参数量的计算方式为：输入维数 × 输出维数（权重）+ 输出维数（偏置）。隐层尺寸 120 和 84 与 1998 年 LeNet-5 分类头采用的经典配置相同。],
)

仓库中的 `mlp.onnx` 共 314058 字节，其中 $78249 times 4 = 312996$ 字节对应 float32 参数；其余空间还包含计算图、张量元数据和序列化开销。这个文件保存的核心内容就是计算结构和训练得到的参数。

它的输入输出设计也体现了这条流水线的任务分工：

*输入端：预处理先减少无关变化。*网络收到的不是原始画面，而是 560 个取值 0 或 1 的数。透视矫正减小了视角和尺度变化，二值化降低了颜色与绝对亮度的影响，几何级联则尽量让图案居中、方向统一。于是网络主要学习二值形状，而不必从整幅彩色图中同时学习定位、矫正和分类。这种分工让约 8 万参数的轻量模型成为可能，也便于在 CPU 上低延迟运行；具体耗时仍取决于处理器、推理引擎和线程配置，需要在目标硬件上测量。手工设计没有消失，而是成为网络前的预处理，与学习模型各自承担一部分任务。

*输出端：为什么这个模型加入第 9 个类别。*类别表包含 1 到 5 号、outpost（前哨站）、guard（哨兵）、base（基地），以及特殊的 negative（负样本）。softmax 的 $K$ 个输出恒为正且总和为 1，网络本身不会留下一个“以上都不是”的空白选项。假如只有 8 个正类，一张灯带反光图仍然会把全部分数分配给这 8 类；不属于任何已知类别的输入也可能让某一类获得较高分数，仅设最大分数阈值未必足够。这个模型选择加入 negative 类，把场地反光、错误配对的灯条和模糊残影等样本作为负类参与训练。它不是开放集识别的唯一方案，但能让分类器在训练中直接看到流水线常见的误检。此类样本也比较容易收集：在录像上运行几何筛选流程，把通过筛选但最终确认是误检的候选保存下来即可。

下图使用仓库视频 `examples/test.mp4` 跑一遍 Python 版提取流程，并把三个候选交给同一个 `mlp.onnx`：

#figure(
  image("images/theory-dl-mlp-dissect.png", width: 100%),
  caption: [三个候选在 `mlp.onnx` 上的前向结果（脚本 `examples/theory/dl_mlp_dissect.py` 从 `examples/test.mp4` 提取并推理）。上排是网络收到的 20×28 二值输入，下排是 9 类 softmax 输出，蓝色表示最高分。左列为哨兵图案，显示为 guard 99.9%；中列为数字 3，显示为 100.0%；右列是跨两块装甲板的错误配对，显示为 negative 100.0%。百分比按一位小数显示，100.0% 是舍入结果，不表示数学上等于 1，也不代表对未测试样本的正确率。这三例只展示第 9 类在所选候选上的行为。],
)

左、右两列正是基础篇输出“guard 99.9%”和“negative 100.0%”对应的输入图。中间的数字“3”来自基础篇中被“不许夹灯”规则错误排除、未能进入分类器的步兵装甲板；绕过配对规则直接送入网络后，这个样本被正确分类。

=== 卷积神经网络：在结构中利用图像先验

`mlp.onnx` 能保持较小规模，是因为输入图像已经完成裁剪、透视矫正和二值化。如果让网络直接在整幅图像中寻找目标，参数规模会怎样变化？一帧 1280×1024 彩色图展平后有 $1280 times 1024 times 3 approx 393$ 万维；即使只连接一个含 1000 个神经元的全连接隐层，参数量也达到 $393 "万" times 1000 approx 39$ 亿，float32 存储约需 15GB，通常超过车载计算机可用内存。全连接层的参数量随输入像素数线性增长；若隐层宽度也按输入规模同比增加，则总参数量会按像素数的平方增长。更重要的是，其中大量参数没有利用图像的局部结构，会造成明显冗余。

浪费在哪？全连接结构对图像的两条基本性质视而不见：

- *局部性*。边缘、角点和纹理等基础模式主要由局部邻域决定。全连接层让每个神经元同时连接全部像素，没有把这种局部结构写进模型，因此需要用更多参数从数据中学出相似约束。
- *平移重复性*。灯条出现在画面左边和右边时，局部外观仍然相似。全连接层会为不同位置分配不同权重，而卷积可以让同一组检测局部模式的参数在各位置复用。

*卷积神经网络*（CNN）把这两条性质直接写进了结构。「计算机视觉基础」一章已经用 3×3 的 Sobel 核提取过竖直边缘，其中 9 个权重由人工设计。卷积层沿用同一种局部运算，只把核中的数改成可训练参数，交给反向传播根据数据更新。于是，一个卷积核可以看作可学习的滤波器：小窗口利用局部性在整幅图上滑动，又在各位置共享同一组权重，最后输出“这种特征在各处有多强”的响应图，也就是特征图。

卷积层的参数量可以直接计算。设它有 $C_"in"$ 个输入通道、$C_"out"$ 个卷积核，每个核都覆盖全部输入通道，则

$
"参数量" = k times k times C_"in" times C_"out" + C_"out"
$

末项表示每个卷积核对应一个偏置。代入一个典型的第一层：64 个 3×3 核处理三通道彩色图，共有 $3 times 3 times 3 times 64 + 64 = 1792$ 个参数，而且参数量*与图像分辨率无关*，1280×1024 图和 20×28 图使用的是同一组卷积参数。分辨率变大仍会增加特征图大小和计算量，只是不需要为每个新位置增加一套权重。与前面的 39 亿个全连接参数相比，1792 个卷积参数避免了在每个位置分别学习同类局部特征。这也是现代分类网络通常以卷积层提取特征，只在末端保留较小分类头的原因。

还需要计算特征图的形状。设输入特征图边长为 $W$、核边长为 $k$、边缘补零为 $p$ 圈（padding，使卷积核也能覆盖边界附近的位置）、滑动步长为 $s$（stride），输出边长为

$
W' = floor(frac(W - k + 2p, s)) + 1
$

这里默认普通的整数步长卷积，不能整除时向下取整。两个常用配置背下来能省很多事：$k = 3, p = 1, s = 1$ 时尺寸不变；在常见的偶数尺寸输入上取 $s = 2$ 时，尺寸约减半，可用于代替池化做降采样。拿 20×28 的输入验算：3×3 核、补一圈零、步长 1，输出仍是 20×28，一格不差。

单层 3×3 卷积只能利用局部邻域，CNN 主要通过两种方式扩大有效范围。第一种是*堆叠*：第二层卷积作用于第一层特征图，其每个输出会间接覆盖原图 5×5 的区域；层数增加后，*感受野*逐渐扩大，学习到的特征也可能从边缘和色块，组合成条纹、角、圆弧，再进一步组合成“灯条”或“数字”等整体结构。具体形成哪些层次由训练数据和目标函数共同决定。第二种是*池化*（pooling）：每个 2×2 窗口只保留最大响应，使特征图边长减半。这样，后续卷积核对应的原图范围变大；特征平移一两个像素时，窗口最大值也可能保持不变，因此网络会获得一定的小平移容忍度。经典网络常交替使用若干“卷积 + 池化”模块，最后接较小的全连接层完成分类。1998 年的 LeNet-5 就用这一结构识别支票上的手写数字，`mlp.onnx` 的 120 和 84 两个隐层尺寸也沿用了 LeNet-5 分类头的配置。

=== 目标检测：从“是什么”到“在哪里”

分类器回答“这张图是什么”，检测器还要回答“画面里有几块装甲板、各在哪里”。分类输出是固定的 $K$ 类分数，而目标数量随图像变化；神经网络的输出张量通常需要固定形状，因此检测方法要设计一种编码，在固定数量的网格、查询或候选槽位中表示可变数量的有效目标，并把其余位置标为背景或无目标。

最直接的方法是滑动窗口：让固定大小的窗口逐位置扫过图像，每到一处就裁出图像块并运行一次分类器；为了适应不同大小的目标，还要用多个窗口尺度重复处理。一帧 1280×1024 图像按 8 像素步长约有两万个位置，再乘以尺度数量，计算开销很高。R-CNN 家族等*两阶段*检测方法先提出一组可能包含目标的候选框，再对候选分类和修正，从而减少无效窗口。两阶段与单阶段方法的精度和延迟取决于具体骨干网络、候选数量、输入尺寸和硬件，不能只按阶段数判断是否满足实时要求。

2016 年的 YOLO（You Only Look Once）采用单阶段方法，把数量不定的检测结果表示成固定形状的回归输出。第一版 YOLO 将图像划分为 $S times S$ 个格子，规定每个格子固定输出 $B$ 个候选框；每个框包含 5 个数，即中心偏移 $x, y$、宽高 $w, h$ 和目标置信度，此外每个格子还输出 $C$ 个类别概率。因此，无论画面中实际有多少目标，网络输出都固定为 $S times S times (5B + C)$ 个数。第一版取 $S = 7$、$B = 2$、$C = 20$，输出即为 $7 times 7 times 30 = 1470$ 个数的张量。训练时，目标中心落在哪个格子，就由该格子负责预测这个目标。

推理时，每个格子都会输出固定数量的候选框。包含目标的格子应给出高置信度框，不包含目标的格子则应给出低置信度框，后者可以直接用阈值过滤。同一目标还可能被多个相邻候选框重复覆盖，这时使用*非极大值抑制*（NMS）：按置信度排序，保留最高分框，再删除与它重叠过大的其他框。「计算机视觉基础」一章的 Canny 也使用了“在相邻响应中保留最强者”的处理，只是那里的对象是边缘像素，这里是候选框。这样，检测过程可以由一次网络前向传播和少量后处理完成。YOLO 及其轻量变体因此常用于需要实时部署的场景，RM 的整图装甲板检测中也有不少方案采用这一类模型。

=== 关键点回归：预测 PnP 所需的四个点

但对自瞄来说，检测框还不是终点。YOLO 的普通 bounding box 与图像坐标轴对齐，而装甲板在画面中常有倾斜和透视变形；框可以包住目标，框的四个角却不是灯条的四个角点。下一章会讲到，从像素解算三维位姿的 PnP 算法需要“图像上四个点 ↔ 装甲板实物四个角”的准确对应。角点偏差如何影响位姿，会在「相机模型」一章中定量分析。

检测头的输出形式和损失函数可以按任务修改。若让检测头输出四个角点的八个坐标 $(x_1, y_1, ..., x_4, y_4)$，并使用四点标注与相应的角点损失，同一类骨干网络就可以同时定位目标和预测 PnP 输入。实现时不只是把四个框参数机械改成八个数：还要固定角点顺序，定义坐标归一化、正负样本分配、遮挡点处理和置信度，并保留必要的分类与目标存在性损失。已有多支队伍的开源方案采用这类思路，修改 YOLO 系检测头直接输出四个点。

这就是基础篇结尾所说“直接回归灯条角点”的具体做法。传统链路“二值化 → 查找轮廓 → 拟合矩形 → 几何配对”的各个环节都包含阈值；曝光变化、灯条残缺或发光图案混入时，某个环节就可能错误排除目标，基础篇中三号装甲板被配对规则排除就是一个例子。关键点网络把这些判断合并到由数据学习的模型中；如果训练数据覆盖了过曝、灯条部分熄灭或被遮挡等情况，模型有机会学会在这些条件下预测角点，但能否泛化到未覆盖条件仍需单独验证。代价包括四点标注、训练算力和推理资源；轻量模型可能在 CPU 上运行，更大的模型则可能需要 GPU 或 NPU，必须在目标硬件上测量。传统几何更依赖规则设计和阈值调试，神经网络更依赖数据覆盖与训练，两者也可以在同一流水线中组合使用，并不是只能二选一。

=== 部署一瞥：从训练框架到车载推理

最后看一下模型部署过程：PyTorch 训练出的模型，如何变成车载程序使用的 `mlp.onnx`？

训练和推理是两种不同的工作负载。PyTorch 等训练框架提供*自动微分*：开发者定义前向计算后，框架按链式法则组织反向传播；它们还支持动态图、多卡并行等研发功能，通常依赖较多。车载推理只执行前向传播，更关注端到端延迟、软件体积和目标硬件利用率，因此部署时常把模型转换为推理引擎支持的格式，而不必携带完整训练环境。

训练和推理之间因此需要通用的模型交换格式。*ONNX*（Open Neural Network Exchange）把网络表示为标准化计算图：节点是矩阵乘、ReLU、卷积等算子，边表示张量流向，权重与计算图一起保存。前文拆解的 `mlp.onnx` 就包含 Flatten、三个矩阵乘、两个 ReLU，以及约 313KB 权重。不同*推理引擎*读取 ONNX 后，会针对目标硬件优化执行：OpenCV DNN 的优点是不必在现有 OpenCV 项目中增加新的推理依赖，基础篇用一行 `readNetFromONNX` 即可加载；ONNX Runtime 支持多种平台；TensorRT 面向 NVIDIA GPU 和 Jetson；OpenVINO 主要优化 Intel CPU 与核显。推理引擎还常配合*量化*，把 float32 权重转换为 int8 后，权重存储可缩小到约四分之一，并可能利用整数运算提高速度；实际加速幅度和精度变化取决于模型、硬件与校准数据，需要测量后判断。ONNX 只定义一组标准算子，训练框架中的新算子不一定受到目标推理引擎支持，因此设计网络时就应检查算子兼容性，把部署约束纳入训练前的方案选择。具体如何针对某块板卡选择引擎和测试帧率，将在应用篇讨论。

==== 本章小结

回望全程，单个神经元只能给出线性边界，非线性激活让多层网络能够表示更复杂的边界；softmax 与交叉熵分别形成归一化分类输出和训练损失，梯度再沿链式法则回传，由动量与 Adam 等优化器更新参数。训练损失下降后，还要用独立数据、数据增广和正则化检查并抑制过拟合。卷积网络把图像的局部性与位置间参数共享写进结构：示例中的卷积层只需 1792 个参数，而前述全连接方案约需 39 亿。YOLO 把数量不定的检测结果编码进固定形状的输出，修改检测头后还可以预测角点。`mlp.onnx` 展示的是另一条轻量路线：透视矫正和二值化等预处理先减少输入变化，negative 类用于接收训练中覆盖的非目标候选，约 8 万个参数完成级联末端的分类任务。

至此，无论走传统几何还是神经网络，交到下一环的都是同一件东西：装甲板四个角点的像素坐标。可像素坐标只是三维场景留下的二维投影，火控真正需要的是目标在三维空间中的位置。从四个像素点走到一个位姿，中间隔着整套相机几何——这就是下一章「相机模型」要回答的问题。
