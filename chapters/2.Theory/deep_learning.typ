#import "/template/template.typ": *

基础篇的识别流水线走到最后一级，把一张 20×28 的二值小图交给了一个叫 `mlp.onnx` 的黑箱，黑箱吐出“guard 99.9%”，两个错误配对被“negative 100.0%”当场处决。当时我们只说它是“MLP 网络”，没有解释这三十多万字节的权重是怎么学会认哨兵图案的。本章就来打开这个黑箱：从单个神经元讲到多层网络，从梯度下降讲到反向传播，再讲清卷积网络为什么天生适合图像、YOLO 怎么把检测变成一次回归、关键点网络怎么交出 PnP 需要的灯条角点。最后你会发现，`mlp.onnx` 里没有任何魔法——只有 78249 个被数据调教过的数字。

=== 从一个神经元说起

回想基础篇的灯条判据：宽长比在 0.1 到 0.4 之间、倾角小于 40 度。它本质上是在一个二维特征空间（宽长比、倾角）里画了几条直线，直线一侧是灯条，另一侧不是。这些直线的位置是人拍脑袋定的——社区多年实战沉淀出的经验值。

*感知机*（perceptron）做的是同一件事，区别只有一个：直线的位置由数据说了算。它接收一个特征向量 $bold(x)$，输出一个由线性函数决定的判断：

$
y = cases(1 quad bold(w) dot bold(x) + b > 0, 0 quad "否则")
$

权重 $bold(w)$ 决定分界面的方向，偏置 $b$ 决定它离原点多远。在二维里这是一条直线，三维里是一个平面，560 维里是一个 559 维的超平面——但无论维数多高，它都是“一刀切”：特征空间被一个平面劈成两半。训练感知机，就是根据标注数据自动调整 $bold(w)$ 和 $b$，让这一刀尽量把两类样本切开。上世纪五十年代末它刚被提出时轰动一时：机器第一次“从例子中学习”了。

但一刀切有一刀切不动的东西。考虑最简单的反例：平面上四团点，对角的两团算一类（也就是逻辑上的 XOR 关系）。你可以拿笔试试——不存在任何一条直线能把对角的两团分到两侧。1969 年 Minsky 和 Papert 在《Perceptrons》一书中系统论证了这类局限，给当时的神经网络研究泼了一盆冷水。单层的线性模型，表达能力就到此为止。

=== 多层感知机：非线性从哪里来

直觉的出路是把感知机摞起来：第一层的输出当第二层的输入，层层传递——这就是*多层感知机*（Multi-Layer Perceptron，MLP）。但这里藏着一个所有初学者都该亲手推一遍的陷阱。设第一层做线性变换 $bold(h) = bold(W)_1 bold(x) + bold(b)_1$，第二层再做 $bold(y) = bold(W)_2 bold(h) + bold(b)_2$，代入展开：

$
bold(y) = bold(W)_2 (bold(W)_1 bold(x) + bold(b)_1) + bold(b)_2 = (bold(W)_2 bold(W)_1) bold(x) + (bold(W)_2 bold(b)_1 + bold(b)_2)
$

两层线性层的复合仍然是一个线性层，权重是 $bold(W)_2 bold(W)_1$ 而已。摞一百层也一样——线性函数的复合永远是线性函数，XOR 依旧切不开。深度本身不产生表达能力，非线性才产生。

所以每层线性变换之后，必须逐元素过一个非线性的*激活函数*。经典选择是 sigmoid 函数 $sigma(z) = 1\/(1 + e^(-z))$，把任意实数压进 $(0, 1)$；现代网络的主力是更朴素的 ReLU：$"ReLU"(z) = max(0, z)$——负数归零，正数原样通过。有了它，“线性变换 → 弯折 → 线性变换 → 弯折”的交替就能拼出高度弯曲的函数。数学上有严格结论：只要隐层神经元足够多，单隐层的 MLP 就能以任意精度逼近紧集上的任意连续函数（通用近似定理）。注意这只是存在性——它保证“有一组权重能做到”，不保证你训练得出来。

空口无凭，用刚才的 XOR 数据实际训练一次：

#figure(
  image("images/theory-dl-nonlinearity.png", width: 100%),
  caption: [线性模型与 MLP 在 XOR 形数据上的决策边界。左：逻辑回归无论怎么训练，一条直线最多对半猜，准确率 50%；右：仅 8 个隐层神经元加 tanh 激活，就把平面弯折出两块对角区域，准确率 100%。读图规则：背景颜色是模型的预测区域，黑色曲线是决策边界——非线性激活的全部意义，就是让这条边界能弯。生成脚本 `examples/theory/dl_nonlinearity.py`（实际训练所得）。],
)

隐层还有第二重身份：*学出来的特征*。隐层的每个神经元都是一个小感知机，各自学到一个方向的“一刀”；非线性把这些刀口的结果重新组合成新的坐标。原始空间里切不开的两类，在隐层构造的新空间里往往一刀就开。基础篇手工设计“宽长比、倾角”特征的活儿，MLP 在隐层里自己干了。

==== softmax：把分数变成概率

分类网络的最后一层输出 $K$ 个实数 $z_1, ..., z_K$（行话叫 *logits*，未归一化的“证据分数”），每类一个。分数有正有负、总和也不是 1，不好直接当概率用。softmax 负责这最后一步转换：

$
p_k = frac(e^(z_k), sum_(j=1)^K e^(z_j))
$

指数保证每一项为正，分母保证总和为 1，且指数单调所以分数的大小顺序原样保留。举个能口算验证的例子：logits 为 $(2, 1, -1)$，softmax 后约为 $(0.71, 0.26, 0.04)$——第一名拿走七成概率，差距被指数放大了。

softmax 还有一条好用的性质：给所有 logits 同时加减一个常数，分子分母同乘一个因子，结果纹丝不动。工程上总是先减去最大值再取指数，防止 $e^z$ 上溢——基础篇 `lightbar_pipeline.cpp` 里 `cv::exp(out - max)` 那一行做的正是这件事。顺带揭一个底：`mlp.onnx` 的最后一层输出的就是 9 个裸 logits，softmax 是在网络外面、由那几行 C++ 补上的。

==== 交叉熵：给“错”定价

有了概率输出，还差一把衡量“错得多离谱”的尺子——*损失函数*。分类任务的标准答案是*交叉熵*（cross-entropy）：设正确类别是 $c$，损失就是

$
L = -ln p_c
$

只看模型分给正确答案的概率。$p_c = 1$ 时损失为 0；$p_c = 0.71$ 时损失约 0.35；而 $p_c = 0.01$ 时损失飙到 4.6，$p_c arrow.r 0$ 时直奔无穷——猜错不可怕，自信地猜错才罚得重。

交叉熵和 softmax 是天造地设的一对。把 softmax 代入损失：$L = -z_c + ln sum_j e^(z_j)$，对任意一个 logit $z_k$ 求偏导：

$
frac(partial L, partial z_k) = frac(e^(z_k), sum_j e^(z_j)) - cases(1 quad k = c, 0 quad k eq.not c) = p_k - y_k
$

其中 $y_k$ 是 one-hot 标签（正确类为 1，其余为 0）。梯度就是*预测概率减标签*，干净得不像话：预测 0.71 而标签是 1，梯度 $-0.29$，往上推；预测 0.26 而标签是 0，梯度 $+0.26$，往下压。指数和对数在求导时恰好抵消，既没有多余的系数，也不会像“sigmoid 加均方误差”那样在饱和区梯度趋零、学不动。后面讲训练时，这个 $p - y$ 会反复出场。

=== 训练：让 78249 个数字各就各位

网络结构定了，剩下的问题只有一个：权重取什么值。`mlp.onnx` 有 78249 个参数，也就是说我们要在一个 78249 维的空间里找一个点，使得网络在训练集上的平均损失尽可能小。这是一个纯粹的优化问题。

==== 梯度下降：盲人下山

把损失 $L$ 看成参数 $theta$ 的函数，它是一片 78249 维的“地形”，训练就是在这片地形上找低谷。你看不见全貌（把整片地形算出来等于遍历所有可能的权重组合），但站在任何一点，微积分能告诉你脚下的坡度——梯度 $nabla L$ 指向上升最快的方向。那就朝反方向迈一步：

$
theta arrow.l theta - eta nabla L
$

步长 $eta$ 叫*学习率*，它是深度学习里最重要的超参数：迈太大，一步跨过谷底甚至越走越高；迈太小，一万步还在半山腰。实践中还有一层折衷：严格的梯度要在全部训练样本上算平均，太贵。于是每步只抽一小批样本（mini-batch，常见 32 到 256 个）估计梯度——单步便宜了上百倍，代价是方向带噪声。实践反复表明这笔买卖极其划算，带噪声的梯度下降（SGD）正是深度学习的主力引擎。

==== 反向传播：链式法则加“记住中间账”

梯度下降每步都要 $nabla L$——损失对全部 78249 个参数的偏导数。最笨的算法是数值微分：把某个权重挪动一丁点，跑一遍前向传播看损失变了多少，如此重复 78249 次。每步训练要跑几万次前向，这条路是死的。

*反向传播*（backpropagation）把代价压到大约*两三次*前向传播：一次正着算输出，一次反着算梯度。它没有任何新数学，就是链式法则，加上一个关键的工程意识——中间结果复用。用一个最小的两层网络把数字走通：输入 $x = 1$，隐层 $h = sigma(w_1 x)$，输出 $hat(y) = w_2 h$，损失 $L = 1/2 (hat(y) - y)^2$，目标 $y = 1$，初值 $w_1 = 0.5$、$w_2 = 0.8$。

前向传播，顺着算并*记下每个中间量*：

$
z = w_1 x = 0.5, quad h = sigma(0.5) approx 0.6225, quad hat(y) = w_2 h approx 0.4980, quad L approx 0.1260
$

反向传播，从损失出发逐层往回求导：

$
frac(partial L, partial hat(y)) &= hat(y) - y approx -0.5020 \
frac(partial L, partial w_2) &= frac(partial L, partial hat(y)) dot frac(partial hat(y), partial w_2) = -0.5020 times underbrace(0.6225, h "（前向存的）") approx -0.3125 \
frac(partial L, partial h) &= frac(partial L, partial hat(y)) dot w_2 = -0.5020 times 0.8 approx -0.4016 \
frac(partial L, partial w_1) &= frac(partial L, partial h) dot underbrace(sigma'(z), h(1-h) approx 0.2350) dot x approx -0.0944
$

两个梯度都是负的，说明两个权重都该调大。按 $eta = 0.5$ 更新：$w_1: 0.5 arrow.r 0.5472$，$w_2: 0.8 arrow.r 0.9562$，重跑一遍前向，损失从 0.1260 降到 0.0777。这就是一步完整的训练。真实训练不过是把它重复几十万次，且每层换成矩阵形式而已。

注意反向过程的结构：算 $partial L \/ partial w_1$ 时直接取用了上一行刚算好的 $partial L \/ partial h$，而不是从头展开链式法则。在深网络里这就是复杂度的分水岭——每层的梯度都站在下一层的肩膀上，一趟回传把所有层的偏导全部收齐。反向传播 = 链式法则 + 中间量复用，前者是数学，后者是它快的原因（本质上就是动态规划的思想：中间结果算一次、处处复用）。

这套机制也暴露了深网络的一个经典病灶。观察 $sigma'(z) = h(1-h)$，它最大只有 0.25。梯度每穿过一层 sigmoid 就要乘上一个不超过 0.25 的因子，穿过十层就衰减到百万分之一以下——底层权重收到的梯度约等于零，学不动，这就是*梯度消失*。ReLU 在正区间导数恒为 1、连乘不衰减，正是它取代 sigmoid 成为默认激活的核心原因之一。

==== 优化器：给下山装上惯性

朴素梯度下降还有一个地形上的克星：狭长山谷。损失面在某些参数方向陡峭、另一些方向平缓（两个方向曲率相差百倍稀松平常），而梯度只顾眼下最陡的方向——结果是在陡壁之间来回撞，真正通向谷底的平缓方向上每步只挪一点点：

#figure(
  image("images/theory-dl-optimizers.png", width: 100%),
  caption: [条件数 100 的二次谷地上两种算法各走 60 步（`examples/theory/dl_optimizers.py` 实际迭代生成）。读图规则：灰色椭圆是损失等高线，谷底在五角星处。蓝色的朴素梯度下降在陡峭方向剧烈锯齿、在平缓方向蠕动，60 步后损失仍有 0.05；橙色的动量法把来回振荡的梯度分量抵消掉、把一致向前的分量越积越多，以更大的摆幅更快逼近谷底，损失 0.0016。],
)

*动量法*（momentum）的修法充满物理直觉：不直接用当前梯度迈步，而是维护一个“速度”向量，让历史梯度指数衰减地累积进来：

$
bold(v) arrow.l beta bold(v) + nabla L, quad theta arrow.l theta - eta bold(v)
$

$beta$ 常取 0.9。振荡方向的梯度一正一负，累积时相互抵消；一致方向的梯度符号不变，越滚越快——像一颗有质量的球滚下山谷，撞壁的横向速度自己消耗掉，向前的速度一路保留。

*Adam* 在动量之上再加一层自适应：为每个参数单独统计近期梯度的均方大小，梯度一贯很大的参数自动用小步长，梯度稀疏微弱的参数自动用大步长，再配合初期的偏置修正。效果是对学习率的选择相当宽容，不怎么调参也能收敛得不错——这让它成了工程上最常见的默认选择。你未来训练装甲板分类器的第一行优化器代码，多半就是 `Adam(lr=1e-3)`。

=== 过拟合：模型会背题

到这里，训练机器已经齐了：结构、损失、梯度、优化器。但把训练损失降到 0 不是目的——比赛画面永远是网络没见过的。模型在新数据上的表现叫*泛化*，而泛化最大的敌人，是模型把训练集*背*下来了。

想想容量对比：`mlp.onnx` 有 78249 个参数，而一个队伍能标注的装甲板样本通常是几千到几万张。参数比样本多，模型完全有能力给每张训练图“开小灶”——连图里的噪声和标注误差一起记住，训练准确率 100%，上场就露馅。这就像一个只背了题库的学生：题库内百发百中，换个数字就交白卷。判断是否过拟合的标准做法，是从数据里留出一部分*验证集*不参与训练：训练损失还在降、验证损失却开始回升，就是背题的铁证。

对策都很朴素，却都有效：

- *数据增广*：把已有样本翻转、平移、缩放、加亮度扰动，白赚几倍数据。对 RM 任务，亮度和对比度抖动尤其值得做——它模拟的正是不同场馆、不同曝光参数下的成像差异。但增广必须尊重任务本身的不变性：装甲板贴图水平镜像后“5”就不再是“5”，数字类样本乱做镜像等于往训练集里掺毒。
- *早停*：既然验证损失回升标志着背题开始，那就在最低点收手，保存那一轮的权重。
- *Dropout*：训练时每一步随机让一部分（如一半）隐层神经元失活。网络无法指望任何单个神经元一定在场，只能把特征学得冗余而分散——变相逼出鲁棒性。推理时全员上岗，并做相应的幅度缩放。
- *权重衰减*：往损失里加一项 $lambda norm(theta)^2$，惩罚过大的权重，让拟合出的函数更平缓。

没有哪一招是银弹，实践中通常全套一起上。真正的第一性原理只有一条：数据是泛化的上限，技巧只是逼近它。多标一千张覆盖不同场地、不同曝光的样本，往往胜过任何调参。

=== 拆解 mlp.onnx：一个真实上场的分类器

现在把本章所有零件对准基础篇那个黑箱。回忆它的岗位：几何级联宽进严出的最后一关——两根灯条配上了，中间区域透视矫正、Otsu 二值化成 20×28 的小图（Otsu 的原理见「计算机视觉基础」一章），由分类器裁决这是数字、图案，还是杂光。用工具读出 `mlp.onnx` 的完整结构，它小得可以整个印在书页上：

#figure(
  table(
    columns: (auto, auto, auto, auto),
    align: (left, center, center, right),
    inset: 7pt,
    stroke: (x, y) => (
      top: if y == 0 or y == 1 { 1pt } else { 0pt },
      bottom: if y == 4 { 1pt } else { 0pt },
    ),
    [*层*], [*输出维度*], [*激活*], [*参数量*],
    [Flatten（20×28 展平）], [560], [—], [0],
    [全连接 1], [120], [ReLU], [$560 times 120 + 120 = 67320$],
    [全连接 2], [84], [ReLU], [$120 times 84 + 84 = 10164$],
    [全连接 3（输出 logits）], [9], [—], [$84 times 9 + 9 = 765$],
  ),
  caption: [`mlp.onnx` 的全部家当：三层全连接共 78249 个参数。参数量算法是本章该会的基本功：全连接层参数 = 输入维数 × 输出维数（权重）+ 输出维数（偏置）。隐层尺寸 120 和 84 恰好是 1998 年 LeNet-5 分类头的经典配置。],
)

文件本身就是这张表的证据：`mlp.onnx` 共 314058 字节，其中 $78249 times 4 approx 313000$ 字节是 float32 权重，剩下约 1KB 是计算图的描述。所谓模型，就是一张计算图加一堆数字，没有别的了。

再看它的输入输出设计，处处是工程判断：

*输入端：预处理替网络扛下了不变性。*网络收到的不是原始画面，而是 560 个取值 0 或 1 的数。透视矫正消掉了视角变化，二值化消掉了光照、颜色和曝光差异，几何级联保证了图案居中、方向摆正。困难的不变性全在进网络之前被传统视觉解决了，网络只剩一个干净的活儿：认二值形状。这正是它 8 万参数就够用、CPU 上微秒级跑完的原因。手工特征工程没有死——它变成了预处理，和网络分工协作。

*输出端：9 个类别里藏着一个哲学问题。*类别表是 1 到 5 号、outpost（前哨站）、guard（哨兵）、base（基地），外加一个特殊的 negative（负样本）。为什么要专门设一个“什么都不是”的类？回看 softmax 的定义：$K$ 个概率恒为正、总和恒为 1——softmax 的世界里没有弃权票。假如只有 8 个正类，一张灯带反光的图进来，概率仍然必须全部分给这 8 个类；更糟的是 softmax 只比较各类之间的相对证据，垃圾输入完全可能让某一类的相对分数一枝独秀，置信度高得理直气壮，靠阈值根本拦不住。负样本类给了所有垃圾一个家：训练时把场地反光、误配对的灯条、模糊残影都标成 negative 喂进去，网络才学得会“这堆白斑不是任何图案”。而这类样本的采集几乎是免费的——把几何级联在录像上跑一遍，它放行的误检就是现成的负样本，流水线自己生产自己的疫苗。

空谈不如实测。下图用本书仓库里的比赛视频真跑了一遍完整链路——Python 复刻基础篇的提取流程，喂给同一个 `mlp.onnx`：

#figure(
  image("images/theory-dl-mlp-dissect.png", width: 100%),
  caption: [三个真实候选在 `mlp.onnx` 上的前向结果（`examples/theory/dl_mlp_dissect.py` 从 `examples/test.mp4` 实际提取并推理）。读图规则：上排是网络实际收到的 20×28 二值输入，下排是 9 类 softmax 概率（蓝色为胜者）。左：哨兵图案，guard 99.9%；中：后方步兵的数字 3，100%；右：一次跨装甲板的错误配对（两根真灯条各属一块装甲板），画面语义上什么都不是——negative 类以 100% 把它收留，这正是第 9 个类别存在的意义。],
)

左、右两列正是基础篇输出里“guard 99.9%”和“negative 100.0%”的当事图；中间那张“3”，是基础篇里被“不许夹灯”规则误杀、没能走到分类器面前的那块步兵装甲板——绕过配对规则直接喂给网络，它认得清清楚楚。

=== 卷积神经网络：把视觉先验刻进结构

`mlp.onnx` 的舒适生活建立在一个前提上：图已经裁好、摆正、洗干净。要是想让网络直接面对整幅画面自己找目标呢？先算一笔账。相机一帧 1280×1024 的彩色图展平是 $1280 times 1024 times 3 approx 393$ 万维；哪怕只接一个 1000 神经元的全连接隐层，参数量就是 $393 "万" times 1000 approx 39$ 亿，float32 存下来约 15GB——权重比整机内存还大。注意这个增长规律：全连接参数量随像素数*线性*增长，若隐层宽度也按输入等比例加宽，则按像素数的*平方*增长。增长得很凶，但它是多项式，不是什么天文级的爆炸——真正的问题不在算不动，而在这些参数买来的几乎全是浪费。

浪费在哪？全连接结构对图像的两条基本性质视而不见：

- *局部性*。一个像素只和它的邻居有强相关，左上角的像素和右下角的像素几乎互不相干——全连接却给每一对“像素–神经元”关系单独配参数，绝大多数参数注定学成零。
- *平移重复性*。灯条出现在画面左边和右边，长的是同一个样子——全连接却要在每个位置把“灯条长什么样”从头学一遍，同一知识存了几万份。

*卷积神经网络*（CNN）把这两条性质直接刻进结构。「计算机视觉基础」一章你已经用 3×3 的 Sobel 核提取过竖直边缘——那 9 个权重是先驱们手工设计的。*卷积层做的唯一改动，是把这 9 个数改成待学习的参数，交给反向传播去填。*一个卷积核就是一个可学习的滤波器：小窗口（局部性）在整幅图上滑动、处处用同一组权重（平移共享），输出一张“该特征在各处的响应强度”图，即特征图。

参数账立刻改写。先把通用公式立好，这是读任何网络结构图的基本功：一个卷积层有 $C_"in"$ 个输入通道、$C_"out"$ 个卷积核，每个核必须同时看全部输入通道，所以

$
"参数量" = k times k times C_"in" times C_"out" + C_"out"
$

末项是每核一个的偏置。代入一个典型的第一层：64 个 3×3 核处理三通道彩图，$3 times 3 times 3 times 64 + 64 = 1792$ 个参数——而且*与图像分辨率完全无关*，1280×1024 的图和 20×28 的图用的是同一份参数。从 39 亿到 1792，砍掉的不是信息，而是全连接结构里那些注定冗余的自由度。这也是现代分类网络绝大多数层都是卷积层、全连接只留在最后当分类头的原因。

形状这本账也要会算。设输入特征图边长 $W$、核边长 $k$、边缘补零 $p$ 圈（padding，为的是让核能骑在边界上）、滑动步长 $s$（stride），输出边长为

$
W' = frac(W - k + 2p, s) + 1
$

两个常用配置背下来能省很多事：$k = 3, p = 1, s = 1$ 时尺寸不变；$s = 2$ 时尺寸减半，常用来代替池化做降采样。拿 20×28 的输入验算：3×3 核、补一圈零、步长 1，输出仍是 20×28，一格不差。

单层卷积只能看见 3×3 的天地，CNN 靠两个手段把视野撑大。一是*堆叠*：第二层卷积作用在第一层的特征图上，它的每个输出间接看到原图 5×5 的范围；层数越深，*感受野*越大，特征也越抽象——第一层学边缘和色块，中层把边缘拼成条纹、角和圆弧，深层把部件拼成“灯条”“数字”这样的整体。这个“低级特征逐层组合成高级语义”的秩序不是人规定的，是训练自己长出来的。二是*池化*（pooling）：每 2×2 窗口只保留最大响应，特征图边长减半。它让后续层用同样的核看到两倍大的范围，还顺手赋予网络对小平移的容忍——特征挪动一两个像素，最大值多半不变。经典配方就此成型：若干“卷积 + 池化”块层层提炼，最后接上小小的全连接层汇总判决。1998 年的 LeNet-5 用这个配方认支票上的手写数字，`mlp.onnx` 那对 120/84 的隐层正是从它那里传下来的香火。

=== 目标检测：从“是什么”到“在哪里”

分类器回答“这张图是什么”，比赛需要的却是“画面里有几块装甲板、各在哪里”。这一步跨越比看上去大：分类的输出是定长的（$K$ 类概率），而一帧画面里可能有零块、一块、五块装甲板——输出长度不定的函数，没法直接塞进“固定维数进、固定维数出”的网络框架。

最朴素的绕法是滑动窗口：拿一个固定大小的窗口在图上逐位置滑动，每个位置裁下来跑一遍分类器；目标有大有小，还得换几种窗口尺寸再来几遍。一帧 1280×1024 的图按 8 像素步长就是两万多个窗口，乘上尺度数，计算量直接爆炸。第一代深度检测方案（R-CNN 家族，即*两阶段*方法）的改良是先用一个专门模块从图中提出几百个“可能有东西”的候选框，再对候选框逐个分类和修正——精度高，但两步走，实时性始终吃紧。

2016 年的 YOLO（You Only Look Once）给出了单阶段的答案，思路可以用一句话说完：*把不定长的检测问题，铸造成定长的回归问题。*做法是把图像划成 $S times S$ 个格子，规定每个格子固定输出 $B$ 个候选框（每框 5 个数：中心偏移 $x, y$、宽高 $w, h$、这框里有没有东西的置信度）加 $C$ 个类别概率。于是无论画面里实际有几个目标，网络输出恒为 $S times S times (5B + C)$ 个数——第一版取 $S = 7$、$B = 2$、$C = 20$，输出就是 $7 times 7 times 30 = 1470$ 个数的定长张量。训练时的分工规则同样简单：目标中心落在哪个格子，就由哪个格子负责学它。

推理时不需要知道目标在哪、有几个——每个格子都无条件吐出自己的框，“算就完事了”。真有目标的格子给出高置信度的框；没目标的格子给出的框置信度趋零，被阈值直接滤掉；同一目标被相邻格子重复框住的，由*非极大值抑制*（NMS）收尾——按置信度排序，与胜者重叠过大的框逐一出局。「计算机视觉基础」一章 Canny 流水线里“只留局部最强”的哲学，在这里从像素换成了候选框，如约再次登场。整个检测变成一次前向传播加一点后处理，实时性由此而来，此后单阶段路线（YOLO 的历代演进、各种轻量变体）成为工程部署的绝对主流。RM 赛场上的整图装甲板检测，如今基本是毫瓦级算力预算下的轻量 YOLO 系模型的天下。

=== 关键点回归：交出 PnP 要的四个点

但对自瞄来说，检测框还不是终点。YOLO 输出的 bounding box 是一个正矩形，装甲板在画面里却总是倾斜的——框住了目标，框的四个角却不是灯条的四个角点。而下一章会讲到，从像素解算三维位姿的 PnP 算法，要的恰恰是“图像上四个点 ↔ 装甲板实物四个角”的精确对应。差之几个像素的角点，位姿就能差出一截。

修法是本章最能体现“网络是万能函数拟合器”的一处：*检测头输出什么，完全由你的损失函数定义。*把回归 $(x, y, w, h)$ 的四个数改成回归四个角点的八个坐标 $(x_1, y_1, ..., x_4, y_4)$，损失从框的重叠度改成预测角点到标注角点的距离，数据集换成四点标注——同一套骨干网络，就从“框目标的检测器”变成了“量角点的测量仪”。多支强队的开源方案（改造 YOLO 系检测头输出四点）走的都是这条路。

这正是基础篇结尾“军备竞赛”里那句“直接回归灯条角点”的技术内幕，现在可以把它讲透了：传统链路“二值化 → 找轮廓 → 拟合矩形 → 几何配对”的每一环都有阈值，每个阈值都是一个失效面——曝光变了、灯条残了、图案发光笔画混进来了，都可能在某一环翻车（基础篇那块被误杀的三号装甲板就是证据）。关键点网络把整条链路压进一个函数，边界由数据划定：过曝的、半灭的、被遮一半的灯条，只要训练集里见过同类，网络就能给出角点。代价也明明白白——上万张四点标注的数据、训练算力，以及推理端的 GPU/NPU 预算。传统几何与神经网络之争，本质是把工程精力花在调阈值上还是花在攒数据上。

=== 部署一瞥：训练在云端，上场在车上

最后补上一块拼图：PyTorch 训练出的模型，怎么变成车上那个 `mlp.onnx`？

训练和推理是两种截然不同的工作负载。训练框架（PyTorch 等）的本职是*自动微分*——你只写前向计算，反向传播由框架按链式法则自动生成——外加动态改网络、多卡并行这些研发期刚需，代价是庞大的依赖和运行时开销。而上场推理只做一件事：前向传播，一遍又一遍。它要的是低延迟、小体积、吃满手头的硬件。把整个训练框架装上车去跑前向，相当于拖着整间厨房去野餐。

于是训练和推理之间需要一个交接格式。*ONNX*（Open Neural Network Exchange）就是这个交接单：把网络导出成一张标准化的计算图——节点是算子（矩阵乘、ReLU、卷积……），边是张量流向，权重打包随行。你其实已经亲手拆过一份：`mlp.onnx` 里就是 Flatten、三个矩阵乘和两个 ReLU 组成的图，加上 313KB 权重。图的另一端是各家*推理引擎*，认领 ONNX 后针对自家硬件优化执行：OpenCV DNN 胜在零额外依赖（基础篇的 `readNetFromONNX` 一行就跑）、ONNX Runtime 通吃各平台、TensorRT 在 NVIDIA GPU 与 Jetson 上榨取极限帧率、OpenVINO 专精 Intel CPU 和核显。引擎还常配合*量化*——把 float32 权重压成 int8，模型缩到四分之一、速度翻倍，精度只掉一点——这在算力按瓦计的车载平台上往往是刚需。这条链路也有自己的坑：ONNX 定义的是一套标准算子集，训练框架里花哨的新算子未必在里面，导出时报“不支持的算子”是家常便饭——工程上的对策是在设计网络时就只用主流算子，部署的约束要前移到训练之前考虑。一句话记住这个分工：*Python 里炼丹，ONNX 里交货，C++ 里上场。*具体到某块板卡怎么选引擎、怎么调帧率，是应用篇实战的话题。

==== 本章小结

回望全程：单个神经元是一刀切的线性判据；非线性激活让层层堆叠产生表达力，softmax 与交叉熵把输出接到概率与损失上，梯度顺着链式法则一趟回传，再由动量与 Adam 护送下山；过拟合提醒你数据才是泛化的上限。卷积网络把“局部性、平移重复性”两条图像先验刻进结构，用 1792 个参数干掉 39 亿；YOLO 把不定长的检测铸成定长回归，关键点头再把检测器改造成角点测量仪。而 `mlp.onnx` 的拆解展示了工程的另一面：预处理消掉不变性、负样本类堵住 softmax 的漏洞，8 万参数就稳稳站住了级联的最后一班岗。

至此，无论走传统几何还是神经网络，你都拿到了同样的东西：装甲板四个角点的像素坐标。可像素坐标只是二维投影，炮口需要的是三维空间里的位置。从四个像素点到一个位姿，中间隔着一台相机的全部几何——这就是下一章「相机模型」的主场。

