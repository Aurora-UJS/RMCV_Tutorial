#import "/template/template.typ": *

上一章结束时，原始图像已经被整理成一组轮廓，每个轮廓都是一串按顺序排列的像素坐标。但轮廓还只是“点”，自瞄真正要用的是明确的几何量：灯条的中心、长度和倾角，能量机关的圆心、半径和当前转角。本章就来完成这一步——*从像素点集中量出几何结构*。其中会反复出现两种思路：一种是拟合，用参数化的几何模型概括一组点；另一种是投票，让各个观测分别支持与自己相容的模型，再从中找出共识最多的结果。两种思路都会用于灯条和能量机关，也会在 RANSAC 中汇合。等这些像素几何量准备好后，「相机模型」一章再把它们送进三维空间。

=== 从轮廓到几何量：拟合的思想

轮廓是一串离散点，直接拿它做判断很笨拙：点数随距离变化、起点位置随机、还带着量化噪声。工程上的通行做法是把点集*拟合*成一个低参数的几何模型——矩形、直线、圆、椭圆——然后在模型参数上判断。拟合可以看作一种压缩：几百个轮廓点被归纳成三五个数，独立的随机扰动往往会在整体估计中部分抵消；若模型选错或噪声带有系统偏差，拟合也会产生偏差。

==== 最小外接旋转矩形

灯条在图像中表现为细长亮斑，适合用*旋转矩形*描述：中心 $(x, y)$、宽 $w$、长 $ell$、倾角 $theta$，共五个参数。"最小外接"对应一个优化问题：在所有能完全包住点集的矩形中，寻找面积最小的一个。最小面积外接矩形必有一条边与点集凸包（包住点集的最小凸多边形）的某条边共线。因此，算法只需沿凸包的各条边依次旋转矩形（旋转卡壳），计算面积并取最小值，而不必在连续角度上搜索。OpenCV 的 `minAreaRect` 实现了这一过程，基础篇 OpenCV 一节的灯条判据使用的正是它输出的五个参数。

使用时要注意接口约定：OpenCV 的版本和语言绑定不同，资料中可能见到 $[-90degree, 0degree)$ 或 $(0degree, 90degree]$ 等角度范围；矩形交换两条边后，也可以用另一组宽、高和角度表示同一个形状，因此 `width` 不一定对应长边。稳妥的做法是先读取四个顶点，根据长短边和坐标轴方向，把结果归一化为无歧义的量，例如“长边与图像竖直方向的夹角”，再设置阈值。跨版本或更换语言接口时，还应当用几组已知角度的矩形核对实际返回值。

==== 直线拟合：方向就是主轴

想要灯条的方向，还可以直接利用点集的空间分布：把轮廓点看作平面上的质点，求它的*主轴*。设点集为 $bold(p)_i = (x_i, y_i)$，均值为 $macron(bold(p))$，构造协方差矩阵：

$
bold(C) = frac(1, N) sum_(i=1)^N (bold(p)_i - macron(bold(p))) (bold(p)_i - macron(bold(p)))^top
$

这是一个 $2 times 2$ 对称矩阵，它的最大特征值对应的特征向量，就是点集散布最开的方向——对细长的灯条来说就是长边方向。当 `fitLine` 选择 `DIST_L2` 距离时，这与二维 PCA 给出的直线方向等价。它最小化的是点到直线的*垂直距离*，而不是教科书线性回归里的竖直残差 $y - (k x + b)$：普通线性回归把解释变量 $x$ 视为给定值，正交拟合则对两个坐标轴作对称处理。对于两个坐标方向都受像素定位影响的轮廓点，后者通常更符合建模意图；若误差明显各向异性，还需要进一步使用相应的权重或噪声模型。

后文还会在图像矩和 Harris 角点中遇到形式相同的 $2 times 2$ 对称二阶矩矩阵及其特征分解，只是统计对象和是否中心化不同。

==== 圆与椭圆：把非线性问题改写为线性

圆有三个参数：圆心 $(a, b)$ 和半径 $r$。最自然的目标是让每个点到圆周的几何距离最小：

$
min_(a, b, r) sum_i (sqrt((x_i - a)^2 + (y_i - b)^2) - r)^2
$

根号中含有待求参数，因此这是一个通常需要迭代求解的非线性最小二乘问题。常用的简化方法是改写圆方程。将 $(x - a)^2 + (y - b)^2 = r^2$ 展开可得

$
x^2 + y^2 + D x + E y + F = 0, quad D = -2a, quad E = -2b, quad F = a^2 + b^2 - r^2
$

$D, E, F$ 在式中是*线性*出现的，因此可以改为最小化代数残差 $sum_i (x_i^2 + y_i^2 + D x_i + E y_i + F)^2$。这是标准线性最小二乘问题，求出 $D, E, F$ 后，再反解圆心 $(-D\/2, -E\/2)$ 和半径 $sqrt(D^2\/4 + E^2\/4 - F)$。这套代数圆拟合通常称为 Kåsa 方法。需要注意，"代数距离"并不严格等于几何距离；噪声较大、观测弧段较短或点的几何分布接近退化时，半径可能出现明显偏差，根号内的数值也应在实现中检查。工程上常把代数解作为初值，需要更高精度时再进行几何最小二乘迭代，基础篇 Ceres 一节已经介绍了相应求解工具。椭圆拟合沿用类似思路：一般二次曲线 $A x^2 + B x y + C y^2 + D x + E y + F = 0$ 有六个齐次参数，还要加入排除平凡解并保证椭圆类型的约束；经典直接最小二乘方法使用 $4 A C - B^2 = 1$。这类方法还需要关注坐标归一化和退化点集，不能只凭线性方程有解就认定拟合结果有效。通过调整残差定义把问题改写为线性形式，往往能得到快速初值，但代数残差与任务真正关心的几何误差仍要区分。

椭圆拟合在灯条上还有一个具体用途：灯条两端的亮斑往往带有圆弧和光晕，旋转矩形的直角未必贴合边界，角点位置也可能随阈值变化。一些实现会改用椭圆拟合，取长轴两端作为灯条端点；它是否比旋转矩形稳定，需要用实际成像数据比较。端点误差如何传到 PnP 位姿中，「相机模型」一章会进一步分析。

==== 灯条判据的量纲分析：为什么阈值都是比值

再看基础篇使用的灯条判据：宽长比为 $0.1 tilde 0.4$、倾角小于 $40 degree$、配对间距为灯条长度的 $0.8 tilde 3.2$ 倍（小板）或 $3.2 tilde 5.5$ 倍（大板）。这些阈值看似各不相同，却有一个共同点：没有一个直接写成“多少像素”。这不是巧合，下面用量纲分析看看它们为什么能适应较大的距离变化。

给图像中的量标上量纲：长度是 $"px"^1$，面积是 $"px"^2$，角度和比值是 $"px"^0$（无量纲）。相机成像近似满足 $ell approx f L \/ Z$，即物理长度为 $L$ 的物体在深度 $Z$ 处成像为 $ell$ 个像素，$f$ 是以像素计的焦距，完整推导见「相机模型」一章。当目标从 2 m 移动到 8 m 时，$Z$ 变为四倍，同一根灯条的像素长度缩为四分之一，面积缩为十六分之一。任何以 $"px"^1$ 或 $"px"^2$ 为量纲的固定阈值，例如"长度大于 20 px"或"面积大于 500 px²"，都会隐含一个工作距离范围：距离改变后，判据可能漏掉远处的真灯条；若把阈值放宽，又可能接受过多杂点。相比之下，宽长比和配对间距除以灯条长度都是 $"px"^1 \/ "px"^1$，量纲为 $"px"^0$。分子和分母随距离近似同比例缩放，因此比值保持不变。这就是尺度不变性的量纲表述：要降低判据对距离的敏感性，可以让阈值作用于无量纲量。小装甲板的灯条间距与灯条长度物理比值约为 2.5，大板约为 4.2；$0.8 tilde 3.2$ 与 $3.2 tilde 5.5$ 两个区间覆盖了对应真值，并为透视变化留出余量。

余量为什么要留这么宽？前面的同比例缩放成立于理想针孔模型，并隐含了目标近似正对相机、各部分深度接近等条件。装甲板绕竖直轴转动后，在弱透视近似下，两灯条的横向间距主要随视角余弦缩短，而竖直灯条的长度变化通常较小，间距比会向区间下沿移动；更一般的透视投影并不保持长度比。判据带宽因此既要容纳这些姿态变化，也要通过实测数据限制误检，不能只由正视时的物理尺寸推出。

倾角判据则*不是*旋转不变的，超过 $40 degree$ 的细长亮斑会被拒绝。在相机安装姿态稳定、机器人和目标大致保持直立的工作条件下，灯条在图像中接近竖直，"倾角小"就是有用的先验；若相机滚转、车体大幅倾斜或规则目标发生变化，这个先验也要重新验证。这提示了一条比"不变性越多越好"更准确的设计原则：对与任务无关的变化降低敏感性，对含有判别信息的变化保留敏感性。后面讲 Hu 矩时会看到相反方向的例子。

=== 图像矩：把形状当质量分布来称

拟合是把点集套进一个预设的几何模型。如果形状本身不规则，套不进任何模型呢？图像矩换了一个视角：把区域看作一块*质量分布*——每个像素是一个单位质点——然后借用力学里现成的一整套语言来描述它。

$(p + q)$ 阶原点矩定义为：

$
m_(p q) = sum_(x, y) x^p y^q I(x, y)
$

对二值图，$I$ 取 0 或 1，求和就是在区域内做统计。零阶矩 $m_(00)$ 是像素总数——面积，或者说"总质量"；一阶矩除以总质量给出质心：

$
macron(x) = frac(m_(10), m_(00)), quad macron(y) = frac(m_(01), m_(00))
$

"灯条中心"和"扇叶中心"都可以用上述质心定义计算。像素坐标虽然是整数，许多坐标的加权平均却可以得到小数，因此质心具有亚像素的*数值分辨率*。当边界量化误差近似独立且没有明显偏向时，平均还可能降低随机波动；但小数坐标本身并不证明真实定位已达到亚像素精度，阈值偏差、光晕和遮挡仍会造成系统误差。「卡尔曼滤波」一章开头还会用多次带噪观测的平均说明同样的条件区别。

把坐标原点挪到质心上，得到中心矩 $mu_(p q) = sum (x - macron(x))^p (y - macron(y))^q I(x, y)$；整体平移时相对质心的坐标不变，所以中心矩具有平移不变性。这里还要区分两种输入：对二值栅格图像，求和统计的是被选中的像素；把一条轮廓传给 OpenCV `moments` 时，函数则利用格林公式计算轮廓所围多边形的连续区域矩。两者在分辨率较高、边界规则时可以接近，但面积和质心不必逐项相同，代码中应清楚自己使用的是哪一种定义。

==== 二阶矩：惯量与主轴

二阶中心矩 $mu_(20), mu_(02), mu_(11)$ 合起来是个老熟人：

$
frac(1, mu_(00)) mat(mu_(20), mu_(11); mu_(11), mu_(02))
$

若把区域看作均匀分布，上式就是其坐标协方差矩阵；它与前面轮廓点的协方差形式相同，只是统计对象从边界采样点换成了区域。它也与二维转动惯量密切相关，特征分解可以给出形状的主轴，主轴方向有闭式解：

$
theta = frac(1, 2) op("atan2")(2 mu_(11), space mu_(20) - mu_(02))
$

这与前面直线拟合所用的是同一个数学对象。计算灯条方向时，可以拟合旋转矩形，可以对轮廓点做 PCA，也可以计算区域的二阶矩；三种方法的输入和约束不同，但都利用了点或像素在主轴方向上的分布。

==== 归一化与 Hu 矩：不变性的阶梯

中心矩平移不变，但不尺度不变。在连续区域模型中，把形状放大 $s$ 倍，坐标因子 $(x - macron(x))^p$ 贡献 $s^p$，$(y - macron(y))^q$ 贡献 $s^q$，面积元还贡献 $s^2$，于是 $mu_(p q) arrow.r s^(p + q + 2) mu_(p q)$。零阶矩 $mu_(00)$ 按 $s^2$ 缩放，因此可以用它消去这一尺度因子：要凑平指数 $p + q + 2$，就除以 $mu_(00)$ 的 $(p + q + 2)\/2$ 次方：

$
eta_(p q) = frac(mu_(p q), mu_(00)^(1 + (p + q)\/2))
$

只要区域面积非零，分子和分母的缩放因子就在上述连续模型中相消，$eta_(p q)$ 因而具有尺度不变性。实际栅格图像经过缩放还会发生重采样和边界量化，所以数值通常只是近似不变。这仍是量纲分析，与灯条判据"让阈值作用于无量纲量"使用了同一种论证，只是这里处理的是高阶矩。

最后一级阶梯是旋转。Hu 在 1962 年给出了七个由二、三阶归一化矩组合成的旋转不变量，第一个最容易看穿：

$
phi_1 = eta_(20) + eta_(02)
$

$mu_(20) + mu_(02)$ 是二阶矩矩阵的*迹*，而迹在旋转下不变；它对应极转动惯量，描述质量相对质心的总体分散程度，不依赖坐标轴朝向。七个不变量里前六个对镜像也不变，第七个在镜像下变号，可以用来区分手性。至此可以串起三个步骤：中心化消平移，归一化消尺度，取旋转不变组合消旋转。

Hu 矩也有明确的适用边界。对常见字形而言，数字"6"旋转 $180 degree$ 后可能接近"9"，旋转不变特征会丢掉区分它们所需的方向信息。Hu 矩对理想连续形状的平移、尺度和旋转具有上述不变性，却不自动处理透视形变；高阶矩还可能对边界噪声和离散化较敏感。基础篇的数字识别采用了另一种方式：先用透视变换把装甲板矫正为正视图，通过几何预处理减小视角变化，再让分类器在方向已经统一的图像上使用方向信息。

=== 模板匹配：最朴素的"找相同"

拟合和矩都在描述"形状本身"。换一个更直接的问题：我手里有一张目标的小图（模板），怎么在大图里找到它？最朴素的答案是滑窗比对：把模板 $T$ 贴在大图 $I$ 的每个位置 $(u, v)$ 上，逐像素算差异：

$
"SSD"(u, v) = sum_(x, y) [T(x, y) - I(x + u, y + v)]^2
$

差异最小处就是候选目标。先算一笔开销账：$1280 times 1024$ 的图上滑一个 $64 times 64$ 的模板，大约有 $1.17 times 10^6$ 个有效位置，每个位置直接计算 4096 项——朴素实现单帧接近 48 亿项，开销很高。常见加速办法包括限定感兴趣区域、用图像金字塔由粗到细搜索，以及对适合的相关形式采用频域或积分图方法。以金字塔为例，可以先在低分辨率层寻找候选，再回到高分辨率层只搜索候选邻域；代价是下采样和尺度离散也可能漏掉小目标，因此层数与候选范围仍需验证。

除了计算开销，SSD 对光照也很敏感：整幅图变亮后，差方和会整体增大。一种处理方法是先让模板和图像块各自减去均值、除以标准差，再进行比较，这就是归一化互相关（NCC）：

$
"NCC"(u, v) = frac(sum_(x, y) (T - macron(T)) (I - macron(I)), sqrt(sum_(x, y) (T - macron(T))^2 sum_(x, y) (I - macron(I))^2))
$

式中的 $I$ 表示当前位置覆盖的图像块。只要模板和图像块方差非零、$a > 0$，而且没有饱和裁剪、量化等非线性影响，减均值与除标准差就能抵消 $I arrow.r a I + b$；在这些条件下，NCC 对正比例的亮度和对比度变化不敏感。其取值位于 $[-1, 1]$，1 表示完全正相关，负比例变化则会翻转相关符号。模板匹配与上一章的相关运算有直接联系：模板充当固定核，NCC 在局部相关的基础上加入归一化。「深度学习和神经网络」一章中的卷积层也计算局部加权组合，但卷积核由数据学习，而不是手工指定。

模板匹配本身不具备尺度、旋转或透视不变性，目标尺寸或姿态发生变化时，相关峰可能明显降低。在 RM 的平面装甲板场景中，可以先根据检测到的角点把候选区域透视矫正为统一大小的正视小图，再进行匹配。角点准确且平面假设成立时，矫正能减小尺度、旋转和透视差异，从而保留模板匹配简单、快速且不需要训练的优点；矫正误差仍会传入匹配结果。在深度模型普及前，模板匹配和 SVM 等传统分类方法都曾用于装甲数字识别。这里体现了传统视觉中两种常见策略：先通过几何预处理减小变化，或者构造对变化不敏感的特征；本章后半部分主要介绍后一种方法。

=== Hough 变换：让每个像素投票

普通最小二乘拟合隐含了一个重要前提：输入点都由同一模型及其噪声产生。真实边缘图中，目标直线的点常与无关边缘混在一起，而且事先不知道有几条线、哪些点属于哪一条。若直接把所有点拟合成一条线，模型就不再符合数据。Hough 变换换了一个方向：暂不分组，让每个点为所有经过自己的候选直线投票。

==== 参数空间：共线即共识

斜率截距式 $(k, b)$ 无法用有限斜率表示竖直线（此时 $k arrow.r infinity$），所以 Hough 常改用法线参数：

$
rho = x cos theta + y sin theta
$

$theta$ 是直线法线方向（落在 $[0, pi)$），$rho$ 是原点到直线的带号距离（有界，不超过图像对角线长）。固定一个点 $(x, y)$，让 $(theta, rho)$ 变化，可以看到*一个图像点对应参数空间里的一条正弦曲线*，曲线上每一点都表示一条可能经过该点的直线。若干个点共线时，它们的正弦曲线会穿过同一个 $(theta, rho)$，这个交点就是公共直线的参数。算法据此把参数空间量化为网格（累加器），让每个边缘点沿对应的正弦曲线给网格计数，最后寻找计数最高的格子。

#figure(
  image("images/theory-2-hough.png", width: 100%),
  caption: [Hough 投票的合成示例：左图 200×200 的场景中有 29 个带定位噪声的共线点（蓝）和 60 个均匀随机杂点（红叉）；右图是以 1° 和 2 px 为网格间隔得到的累加器。随机杂点的票分散在参数空间，共线点则在 $(61 degree, 79)$ 附近形成 18 票的峰；生成真值为 $(60 degree, 80)$。当前偏差同时受点噪声和网格量化影响，不能只归因于其中一项。读图时先看右图的局部峰，再把该参数反解为左图中的虚线。],
)

图中峰值只有 18 票而不是 29 票，因为带噪点经过参数网格量化后，部分票落入了相邻格子。这体现了累加器的基本权衡：格子较细时参数分辨率高，但同一结构的票更容易分散；格子较粗时票更集中，参数分辨率却会下降。格宽、峰值阈值和是否合并相邻峰，都应结合定位噪声与待检测结构的最小间隔设置。

==== 投票为什么鲁棒

最小二乘把所有点的误差汇总后优化，因此不属于模型的点也会影响直线参数。Hough 投票不直接最小化这一全局误差：独立随机杂点对应的曲线通常分散在参数空间，共线点即使只占一部分或沿直线断续分布，其曲线仍可在公共参数附近集中。因此，在网格和阈值合适时，投票方法能从一定程度的随机杂点与断裂边缘中保留直线候选。不过，栏杆等结构化背景也会形成真实峰值，Hough 并不会自动判断哪个峰属于任务目标。基础篇给灯条判红蓝时同样汇总了多个像素的通道证据；后文的 RANSAC 则让抽样产生的候选模型接受全部数据检验。

==== 从直线到圆：维度的代价

圆有三个参数 $(a, b, r)$，照搬直线 Hough 的做法需要三维累加器。若对一幅 $1280 times 1024$ 图像按 1 px 间隔枚举圆心，并枚举 1～600 px 的半径，格子数约为 $1280 times 1024 times 600 approx 7.9 times 10^8$；仅用 32 位整数存储就接近 3 GiB，遍历投票的开销也很高。梯度 Hough 因此利用了额外信息：在理想、对比清楚的圆周上，边缘梯度沿法向，圆心应位于边缘点的梯度方向或反方向上。算法先在二维圆心空间积累候选，再根据边缘点到候选圆心的距离估计半径，把朴素三维搜索拆成较低维的步骤。OpenCV `HoughCircles` 的 `HOUGH_GRADIENT` 使用这一类思路；实际结果还取决于边缘检测、梯度方向、半径范围和累加器分辨率。

Hough 尤其适合目标具有明确参数模型，而边缘又存在断裂、混杂，难以先得到完整轮廓的场合。RM 场上的灯条和扇叶含有主动发光区域；在曝光、背景和反光得到控制时，往往可以先二值化，再对较干净的轮廓进行拟合，从而省去参数空间投票。若分割条件不成立，这个优势也会消失。两条路线应在目标数据上比较检出率、误检率和计算开销，而不是预先认定一种方法更好。

=== 角点与描述子一瞥

到目前为止，我们使用的都是为特定目标手工设计的特征，例如灯条判据和扇叶轮廓。通用视觉采用另一种思路：不预设具体目标，先在图像中寻找一批在视角或光照变化后仍能稳定重现的点，再为每个点计算可比较的局部描述子，据此在两幅图之间建立对应关系。"特征点 + 描述子 + 匹配"是标定、图像拼接和同步定位与地图构建（simultaneous localization and mapping，SLAM）常用的基础流程，这里只介绍其基本思想。

==== 什么样的点适合被追踪

想在两幅图里认出"同一个点"，这个点先得*可定位*。平坦区域不行：窗口挪到哪里画面都一样，定位无从谈起。边缘也不行：沿边缘滑动窗口，画面不变——你只能确定它在边上，定不了在边上的哪里。只有*角点*，窗口朝任何方向挪动画面都会变，两个自由度都被锁死。把"挪动窗口画面变多少"写成数学，就是 Harris 的出发点。

==== Harris：梯度的二阶矩矩阵

设窗口平移 $(u, v)$ 引起的画面变化为：

$
E(u, v) = sum_(x, y) w(x, y) [I(x + u, y + v) - I(x, y)]^2
$

$w$ 是窗口权重（常取高斯）。对小位移做一阶泰勒展开 $I(x + u, y + v) approx I + I_x u + I_y v$，平方求和后整理成二次型：

$
E(u, v) approx vec(u, v)^top bold(M) vec(u, v), quad bold(M) = sum_(x, y) w(x, y) mat(I_x^2, I_x I_y; I_x I_y, I_y^2)
$

观察 $bold(M)$ 的构造可以发现，它是梯度向量 $(I_x, I_y)$ 的*加权二阶矩矩阵*，也称结构张量。它没有减去窗口内的平均梯度，因此严格来说不是中心化的协方差矩阵；不过，它与前面两种 $2 times 2$ 对称矩阵仍可用相同的特征分解方法理解。这里的两个特征值 $lambda_1, lambda_2$ 表示梯度能量在两个主方向上的强度：两者都小表示平坦区域；一大一小表示梯度主要朝一个方向，即边缘；两者都大则表示角点。为了避免逐点求解特征值，Harris 用行列式和迹构造了响应函数：

$
R = det bold(M) - k (op("tr") bold(M))^2 = lambda_1 lambda_2 - k (lambda_1 + lambda_2)^2
$

$k$ 常在 $0.04 tilde 0.06$ 附近选取：两个特征值都较大且量级接近时，乘积项占优，$R$ 为较大的正值；一大一小时，平方项往往使 $R$ 变为负值。后来 Shi 和 Tomasi 提出直接用 $min(lambda_1, lambda_2)$ 作为响应；OpenCV `goodFeaturesToTrack` 在不启用 Harris 模式时使用的就是这一类判据。棋盘格标定也需要稳定的二维角点，但具体粗检测器不必等同于 Harris；得到初始位置后，可以用 OpenCV `cornerSubPix` 根据邻域图像迭代细化坐标。这里的“小数坐标”仍需通过重投影误差和重复拍摄验证，不能只凭算法名称认定精度。实操流程见「实战技术篇」的相机标定与手眼标定一章。

Harris 本身不具备尺度不变性。同一个圆角在较大尺度下可能呈现为缓弯边缘，在较小尺度下才形成明显角点；检测窗口固定时，尺度变化会改变响应。尺度空间和 SIFT 等系统性方法超出本章篇幅，下面只看 ORB 如何用图像金字塔提供有限的尺度适应性。

==== ORB：面向实时性的组合方案

检测到特征点后，还需要为它计算描述子，才能在另一幅图中比较局部区域。ORB 是实时场景中常用的方案，由三个强调效率的部分组成。*检测*使用 FAST：不计算梯度矩阵，而是检查候选点半径 3 的圆周上 16 个像素；FAST-9 要求存在连续 9 个像素都比中心亮或暗超过阈值。ORB 在图像金字塔的多个缩放层上检测，以获得有限范围内的尺度适应性，并可再用 Harris 响应等规则给候选排序。*方向*使用灰度质心：对角点邻域计算一阶矩，以 $op("atan2")(m_(01), m_(10))$ 作为主方向。*描述*采用旋转后的 BRIEF：在默认配置下，邻域内的 256 对采样点各产生一个比较比特，组成 256 位二进制串；旋转采样模式能减小平面内旋转的影响，但插值、尺度和视角变化仍会带来误差。比较这类二进制串只需异或与位计数（popcount），现代 CPU 可以高效执行。ORB 的各环节都在识别稳定性与计算成本之间取舍，这一点与自瞄流水线的设计相似。

=== 特征匹配：在描述子空间里找最近邻

两幅图各自提了几百个特征点，怎么配对？描述子就是向量——浮点描述子比欧氏距离，二进制描述子比汉明距离——最直接的做法是暴力最近邻：对左图每个描述子，去右图里找距离最近的那个。点数多时用近似最近邻加速（KD 树、哈希一族，OpenCV 里的 FLANN），思想不变。

找到最近邻之后，还要判断它与其他候选相比是否足够明确。固定的绝对距离阈值在某些数据上有用，但其合适数值会随描述子类型、成像条件和场景改变。Lowe 提出的*比值检验*比较最近邻距离 $d_1$ 与次近邻距离 $d_2$：只有 $d_1 \/ d_2$ 小于设定阈值时才接受；$0.7 tilde 0.8$ 是常见起点，并不是适合所有描述子和数据集的常数。若 $d_1$ 明显小于 $d_2$，最近候选在当前候选集中较为突出；若两者接近，栅栏、地砖等重复纹理可能提供了多个相似候选。比值检验只衡量相对区分度，并不能证明对应关系正确。还可以要求互为最近邻，即左图 A 的最近候选是右图 B，且 B 反查也选回 A；这会拒绝一部分单向歧义，也可能同时损失有效匹配。实际系统常组合距离、比值、互查和后续几何约束，并在验证数据上选择阈值。

本书的低曝光灯条流水线不使用通用特征：大部分背景接近全黑，可重复的纹理点可能不足，而灯条的形状和发光先验可以直接利用。这并不说明 ORB 不能用于任何 RM 图像；提高环境纹理与曝光后，条件会改变。ORB 等通用特征更常见于视觉 SLAM 的帧间对应和多视角图像拼接。专用判据与通用描述子利用的信息不同，应根据目标数据选择。

=== RANSAC：用最小样本寻找共识

比值检验后的匹配集中仍可能混有错配，能量机关圆周的采样点中也可能出现误检。这类数据称为*离群点*（outlier）：它们不只是误差较大的有效观测，而是根本不属于当前模型的数据。

==== 最小二乘对离群点的敏感性

普通最小二乘对离群点很敏感，原因就在平方损失中：一个大小为 100 的残差贡献 10000，而一万个大小为 1 的残差总贡献也是 10000。为降低这个单点造成的巨大损失，估计结果可能明显偏离其余数据。对有限的 $N$ 个样本，只替换一个点就可能让普通最小二乘估计任意偏离，因此其有限样本替换崩溃点为 $1\/N$，当样本数增加时趋近于零。这个结论针对未使用鲁棒损失、截断或离群点模型的普通最小二乘。

#figure(
  image("images/theory-2-ransac.png", width: 100%),
  caption: [固定随机种子的合成示例：70 个高斯噪声内点（蓝）与 30 个额外生成的离群点（红叉）共用同一坐标轴。对全部点作普通最小二乘，红虚线相对生成真值偏了 10.2°；以 2 点为最小样本、容差 2.5、抽样 100 轮的 RANSAC 在最大共识集上重拟合后，绿线与真值相差 0.10°。脚本直接复算得到的共识集恰好包含全部 70 个生成内点，且没有纳入生成离群点；这些数值只描述当前合成数据，不代表其他离群比例、分布或阈值下仍有相同结果。],
)

==== 抽样—共识

RANSAC（随机抽样一致，RANdom SAmple Consensus）的做法是：在暂时无法区分内点和离群点时，先*随机抽取最小样本*产生候选模型，再用全部数据检验这些模型。一轮循环包括四步：

- 随机抽 $s$ 个点，$s$ 取非退化情况下能确定模型的最小数目——直线需要 2 个不同点，圆需要 3 个不共线点，单应矩阵通常需要 4 对处于一般位置的对应点；
- 用这 $s$ 个点通过最小求解器产生一个或若干候选模型；这一阶段不使用更多数据做超定最小二乘拟合；
- 用全体数据检验：到模型距离小于容差 $epsilon$ 的点记为"共识"（内点），统计其数量；
- 记住共识最多的模型，重复抽样；结束后用最大共识集整体重拟合一次，利用全部内点信息。

和 Hough 对照着看，两者的表决方向正好相反：Hough 让*每个数据点*给所有相容的模型投票，需要离散并遍历参数空间，因此更适合较低维的模型；RANSAC 则让*每个候选模型*接受全部数据检验，候选由最小样本产生，不需要建立完整的高维累加器。不过，参数维数不是唯一的成本指标；最小样本数、退化概率、单次求解和验证开销都会影响 RANSAC。两者共享的鲁棒性直觉是：与目标模型无关的观测较难持续支持同一个候选。

==== 为什么样本要最小：一道概率题

样本数直接影响抽中全内点集合的概率。设数据中内点占比为 $w$；在独立抽样近似下，抽取的 $s$ 个点*全部*是内点的概率为 $w^s$。当 $0 < w < 1$ 时，每增加一个样本都会再乘一次 $w$，所以在模型可确定且样本不退化的前提下，应优先使用最小样本。若希望至少一次抽到全内点样本的概率不低于 $p$，就令 $N$ 轮全部未命中的概率不超过 $1 - p$：

$
(1 - w^s)^N <= 1 - p quad arrow.r.double quad N >= frac(log(1 - p), log(1 - w^s))
$

代入 $p = 0.99$ 算一张表：

#figure(
table(
  columns: (auto, auto, auto, auto),
  align: (left, center, center, center),
  stroke: (x, y) => (
    top: if y == 1 or y == 0 { 1pt } else { 0pt },
    bottom: if y == 3 { 1pt } else { 0pt },
  ),
  inset: 8pt,
  [*模型（最小样本 $s$）*], [*$w = 0.7$*], [*$w = 0.5$*], [*备注*],
  [直线（2）], [7], [17], [圆是 3 点，介于两行之间],
  [单应矩阵（4）], [17], [72], [平面到平面的投影对应],
  [基础矩阵（7）], [54], [588], [七点法，两视图几何],
),
caption: [在独立抽样近似下，置信度 99% 所需的最少轮数 $N$。横向比较可见：内点率从 0.7 降到 0.5 后，所有模型都需要更多抽样；纵向比较可见：在相同内点率下，最小样本数 $s$ 从 2 增至 7，所需轮数快速上升。因此，模型是否足以描述数据与其最小样本规模，需要一并权衡。],
)

实践中 $w$ 事先未知，常见做法是先采用保守估计，每当刷新最大共识集，就用当前共识比例重新计算剩余轮数。只有当该共识确实对应目标模型时，这个比例才是内点率的合理估计。容差 $epsilon$ 要依据残差定义和量测噪声设定：太紧会拒绝有效观测，太松则会把不相容的数据纳入共识。上面的概率还假设抽样近似独立、最小样本不退化，并且全内点样本能够产生可识别的目标模型；无放回抽样时，精确概率还会随有限数据量略有变化。内点率较低或最小样本较大时，所需轮数会迅速增加，有限轮随机抽样也不保证找到共识数全局最大的模型。因此，RANSAC 提供的是在明确假设下可计算的命中概率，而不是确定性保证。

=== 综合演练：能量机关的圆拟合与转速量测

最后用能量机关做一个综合示例。能量机关的五片扇叶绕中心 R 标旋转，识别程序可以在连续帧中记录待击打扇叶上装甲板的中心点；轮廓、拟合和图像矩都能参与这一过程。要预测装甲板之后的位置，需要先估计旋转圆心、半径和当前角度，再结合时间戳计算运动参数与超前量。下面只讨论量测与拟合，完整规则和击发流程仍要以相应赛季手册及后续应用篇为准。

==== 该在哪个空间里拟合圆

首先要决定在三维空间还是像平面中拟合轨迹。以相机位置基本固定、目标平面近似正对相机的调试场景为例，空间圆在像面上接近圆，可以先在二维中建模。这样做还有一个量测上的理由：「相机模型」一章的简化算例取 $f_x = 1739$ px、小装甲板宽 $W = 135$ mm；在 7 m 处，一个像素的宽度误差按 $Z^2 \/ (f_x W)$ 换算约为 0.21 m。这个数值只是正视尺寸测距模型的灵敏度，不是对所有 PnP 实现的实测误差，但它说明远距离深度可能比像面横向量更敏感。

二维圆模型的条件也很严格：目标平面明显倾斜时，圆的透视投影一般是椭圆；云台或相机运动后，不同帧中的像素坐标也不再位于同一个固定圆上。此时不能靠“每帧重新拟合一遍历史原始点”消除几何不一致，而应限制时间窗口、利用同帧多个结构，或根据相机姿态把各帧观测补偿到共同坐标系。量测质量足够时，也可以直接在三维坐标系中估计转轴与圆。选择哪条路线，应由视角范围、标定误差和回放数据上的预测指标决定；坐标补偿将在「坐标变换」一章继续讨论。

==== 代数拟合与 RANSAC 剔除离群点

在像平面模型成立时，可以把连续帧的装甲板中心作为圆周采样点。存在误认扇叶等离群点时，RANSAC 每轮用 3 个不共线点产生圆候选，以几何残差选出最大共识集；最后再对共识集使用 Kåsa 代数拟合或几何最小二乘重估参数。先对全部数据作一次 Kåsa 拟合可以用于观察或提供尺度初值，却不会替代 RANSAC 的离群点筛选。

圆心还有另一个观测来源：直接检测中心 R 标。对近似圆形的连通区域，圆形度 $(4 pi A) \/ P^2$ 是一个无量纲判据；理想连续圆取 1，栅格边界、遮挡和周长算法会改变实际数值，因此阈值仍需由数据确定。R 标检测与轨迹拟合可能共享同一幅图像和分割误差，不能默认统计独立。只有在坐标定义一致，并估计了各自的不确定性与相关性后，才适合设计加权融合；是否优于单一来源应由独立回放数据验证。

==== 短弧的病态与转速量测

圆拟合还有一个重要条件：采样弧段的长度决定圆心的可观性。装甲板每帧只移动一小段，如果最近几十帧仍集中在短弧上，这段弧会接近直线，曲率信息很弱，圆心沿“弧中点到圆心”的方向约束较差，点噪声因而可能被明显放大。

#figure(
  image("images/theory-2-rune-circle.png", width: 100%),
  caption: [固定随机种子的 Kåsa 拟合实验：每轮在相应弧段内随机取 40 个点，并给 $x$、$y$ 坐标分别加入标准差 2 px 的独立高斯噪声，重复 300 次。45° 短弧的圆心 RMS 误差为 39.69 px，估计点沿弧中点—圆心方向形成长带；240° 长弧的 RMS 为 0.82 px，在当前噪声模型下相差约 48.5 倍。灰点云只反映这组模拟和代数拟合器的经验分布；在视角与运动模型仍有效的时间窗口内扩大弧段，通常能增强圆心约束。],
)

为避免与转速模型的参数混淆，下面把圆心记为 $(c_x, c_y)$。第 $k$ 帧的角度可写成 $theta_k = op("atan2")(y_k - c_y, x_k - c_x)$。跨越 $plus.minus pi$ 时需要相位解缠绕；它通常还假设相邻有效观测的真实转角小于 $pi$，长时间丢帧后不能只靠最近一次角度判断绕过了几圈。相邻半径向量的二维叉积符号可以区分图像坐标中的转向，但 OpenCV 图像的 $y$ 轴向下，映射到物理顺、逆时针前必须统一坐标约定。

角速度最直接的估计是差分 $hat(omega)_k = (theta_k - theta_(k-1)) \/ Delta t$，但差分会放大高频定位噪声。假设圆心准确、切向位置误差在相邻帧独立且标准差为 2 px，半径 150 px 时，单帧角度标准差近似为 $2\/150 approx 0.013$ rad；以 100 fps 采样，差分噪声标准差约为 $sqrt(2) times 0.013 \/ 0.01 approx 1.9$ rad/s。若误差跨帧相关或圆心也在波动，实际数值会不同。

为了比较量级，本书后续示例采用一组规则参数：小符匀速约为 $pi\/3 approx 1.05$ rad/s，即每分钟 10 转；大符使用

$
dot(theta)(t) = A sin(Omega t + phi) + B, quad B = 2.090 - A
$

并取 $A in [0.780, 1.045]$。在这个模型中，峰值恒为 2.090 rad/s，谷值 $2.090 - 2 A$ 约在 0～0.53 rad/s，因而不会反向。这里的区间服务于本书算例，不应代替当季规则手册。与前面的 1.9 rad/s 噪声估算相比，逐帧直接差分的波动可能与信号同量级。实际处理可以对角度序列进行滤波和状态估计（见「卡尔曼滤波」一章），也可以直接拟合积分后的角度模型

$
theta(t) = -frac(A, Omega) cos(Omega t + phi) + B t + C
$

或拟合带时间间隔的角度增量，从而避免把逐帧瞬时差分当作最终转速。参数估计仍是非线性最小二乘或系统辨识问题，可以使用基础篇 Ceres 一节介绍的工具。完整的打符链路，包括规则读取、弹道、时延与择机击发，将在应用篇介绍。

==== 本章小结

本章的工具可以沿三条线联系起来。第一条是 $2 times 2$ 对称二阶矩矩阵：它作为轮廓点的协方差描述直线方向，作为区域相对质心的二阶矩描述形状主轴，又作为窗口梯度的未中心化二阶矩用于 Harris 角点。统计对象不同，特征值和特征向量的读法却很相似。第二条是证据汇总：灯条判色统计区域内红、蓝通道的总体支持，Hough 在参数空间统计支持某条直线的边缘点，RANSAC 则统计支持候选模型的内点；它们的共同前提是无关观测不会大量支持同一个错误候选，结构化干扰出现时还需要额外判据。第三条是“不变性该留多少”：对距离、光照等与任务无关的变化，可以用比值、归一化和不变矩降低敏感性；对倾角、数字方向等包含分类信息的变化，则要保留区分能力。量纲分析是检查尺度依赖的一种直接工具。

经过这些处理，可以得到灯条中心与四个角点的像素坐标、装甲板的配对关系，以及能量机关的圆心、半径和相位。它们仍然位于像素坐标系中，如何进一步估计三维位置和姿态，将在「相机模型」一章说明。下一章先介绍另一种特征提取方式：不再完全依赖手工设计判据和不变性，而是让模型从数据中学习所需特征。
