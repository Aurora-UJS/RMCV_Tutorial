#import "/template/template.typ": *

基础篇的 OpenCV 一节带你跑通了一条完整的识别流水线：二值化、找轮廓、几何判据、分类器。那一节回答的是"怎么做"，本章回到每个函数的背后，回答"为什么这样做是对的"。为什么低曝光加二值化能把灯条从赛场里干净地切出来？为什么分类器前的数字小图用 Otsu 自动选阈值，整幅画面却写死一个 160？为什么 H 通道在真实画面里是一片噪声？这些问题在基础篇里都是"眼见为实"的现象，本章把它们变成可以推导的数学。流水线本身不再重复；从轮廓里量出几何特征是下一章的事——本章只走到"图像被整理成可以测量的对象"为止。

=== 从光到数字：图像的形成

相机不产生"图像"，它测量光。传感器表面排着一片光电二极管阵列，曝光窗口打开的那几毫秒里，每个二极管各自数落在自己头上的光子：光子撞进硅片激发出电荷，电荷在势阱里积累；曝光结束后被读出、放大，再由模数转换器变成一个整数。一张所谓的"图像"，就是这上百万个计数结果按行列排好的矩阵。

==== 采样与量化

从连续的光场到离散的矩阵，发生了两次离散化。*采样*把空间切成格子：镜头投在传感器上的亮度分布本是连续的（光线为什么恰好落在这个格子而不是那个格子——那是投影几何，「相机模型」一章的主场），传感器只在 $W times H$ 个位置上取值。*量化*把亮度切成台阶：8 位深度下每个计数被压成 0 到 255 的整数，共 256 级。于是一幅灰度图在数学上就是一个函数：

$
I: {0, 1, ..., W - 1} times {0, 1, ..., H - 1} -> {0, 1, ..., 255}
$

后文的一切——直方图、卷积、梯度、阈值——都只是对这个函数的操作。这个视角值得反复回味：图像处理不是"看图"，是对一个二元函数做数学。位深不必是 8：不少工业相机提供 10 位或 12 位输出，量化台阶更细、暗部层次更多，代价是数据量与处理开销上涨。自瞄流水线通常 8 位就够——后面你会看到，它的关键判决只是"亮不亮"这一个比特。

彩色图像是三个这样的函数叠在一起，但多数相机并不在每个像素上测三种颜色：传感器覆盖着一层红绿蓝相间的滤色片（Bayer 阵列），每个像素只测一个通道，另外两个通道靠邻居插值补出（去马赛克）。所以彩图里约三分之二的颜色数值是算出来的，不是测出来的——像素级的颜色判断不宜太较真，这是一个底层原因。

==== 噪声：图像天生不完美

对着完全静止的场景连拍两张，像素值也不会完全相同。噪声来源很多，最根本的一条来自光的物理本性：光子到达是随机事件，服从泊松统计——曝光期间平均收到 $N$ 个光子的像素，计数的标准差约为 $sqrt(N)$，信噪比 $N \/ sqrt(N) = sqrt(N)$ 只随光子数的平方根增长。光越少，图越"脏"；这就是为什么低光照画面加大增益后噪点满天飞——增益放大的是已经带着噪声的信号，救不回来没收到的光子。

这笔账正是自瞄相机低曝光策略的物理基础。灯条是自发光的 LED，直射进镜头；赛场环境靠反射照明，像面照度比灯条低若干个数量级。把曝光时间压到 1～4 毫秒，环境收到的光子数掉到接近量化台阶，整幅画面近乎全黑，而灯条依然亮得饱和——对比度不是靠算法算出来的，是靠曝光在物理上做出来的。基础篇说"视觉算法的判据永远和成像条件绑定"，根子就在这里。

=== 直方图：一张图的亮度账本

把每个灰度值在图中出现的次数 $h(v)$ 数一遍，再除以像素总数，就得到归一化直方图：

$
p(v) = frac(h(v), W H), quad sum_(v=0)^(255) p(v) = 1
$

直方图把整幅图压缩成 256 个数，代价是丢掉全部空间信息：一张左黑右白的图和一张黑白棋盘格，直方图完全相同。但留下来的分布信息已经能回答很多问题——画面偏暗还是偏亮（分布的重心）、对比度高还是低（分布的展宽）、有没有过曝（255 处的尖峰）。对一幅低曝光的赛场画面，直方图的形状极具特征：绝大多数像素挤在 0 附近，另有一小撮饱和像素贴着 255，两团之间隔着宽阔的无人区。看到这个形状你就该明白：把灯条切出来，只需要在无人区里立一刀。这一刀怎么立、什么时候不好立，是二值化一节的主题；在那之前，先解决噪声。

=== 卷积与滤波

==== 卷积：滑动的加权平均

对图像做局部操作的标准形式是卷积：拿一个小矩阵（核，kernel）在图像上滑动，每到一处，把核的元素与它盖住的像素逐位相乘再求和，作为输出图像在该处的值。核的半径记为 $r$（$3 times 3$ 核即 $r = 1$）：

$
(I * K)(x, y) = sum_(i=-r)^(r) sum_(j=-r)^(r) K(i, j) thin I(x + i, y + j)
$

严格说，数学定义的卷积要先把核旋转 180 度，图像库实际算的是不翻转的"相关"；本章用到的核都是对称的，两者没有区别。图像边缘处核会伸出界外，工程上用补零或镜像延拓处理，不再展开。

一个 $k times k$ 的核，每个输出像素要做 $k^2$ 次乘加；对一幅 $1280 times 1024$ 的图用 $5 times 5$ 核，就是约 3300 万次乘加。「计算机系统基础」一章讲过的行主序访存规律在这里同样生效：卷积是典型的规则访存、乘加密集的计算模式。记住这个模式——「深度学习和神经网络」一章里它会以"卷积层"的身份重新登场，唯一的区别是：本章的核由人手工设计，那里的核由数据学出来。

==== 三种滤波器，各治什么病

*均值滤波*的核最朴素：$3 times 3$ 全一除以 9，输出就是邻域平均。它压噪声的原理和「卡尔曼滤波」一章开头的递推平均一模一样：对独立同分布、标准差为 $sigma$ 的噪声，9 个样本的平均把噪声压到 $sigma \/ 3$。代价是所有细节被一视同仁地抹平——边缘也是"细节"。

*高斯滤波*把"全一"换成从二维高斯函数上采样的权重：

$
K(i, j) prop e^(-(i^2 + j^2) / (2 sigma_s^2))
$

离中心越远权重越小，对应一个朴素的先验：越近的像素越可能属于同一个表面。参数 $sigma_s$ 控制平滑力度。高斯核还有一个漂亮的工程性质——*可分离*：二维高斯是两个一维高斯的乘积，先做一遍水平方向的一维卷积、再做一遍垂直方向，结果与二维卷积完全相同，每像素乘加次数从 $k^2$ 降到 $2k$（$5 times 5$ 核：25 次变 10 次）。

*中值滤波*不做加权求和，而是把窗口里的像素排序取中位数。它是非线性的，专治一类线性滤波束手无策的噪声：椒盐噪声——坏点、传输错误造成的孤立纯黑纯白点。算笔账就明白了：背景灰度约 30 的暗区里出现一个 255 的白噪点，$3 times 3$ 均值滤波的输出被它抬高 $(255 - 30) \/ 9 approx 25$ 个灰度级——噪点没有消失，只是摊成了一个 $3 times 3$ 的亮斑；而在排序的视角下，极端值只要不占据窗口的半数以上，就根本轮不到它出场。中值滤波还有一个额外的好处：理想的阶跃边缘取中位数后仍落在阶跃的某一侧，边缘不会像线性滤波那样被磨成斜坡。

#figure(
  image("images/theory-cv-filters.png", width: 100%),
  caption: [同一块装甲板区域加两种噪声（上排：高斯噪声 $sigma = 15$；下排：5% 椒盐噪声），分别过三种 $5 times 5$ 滤波器，每格标注与干净原图的 RMSE（灰度级）。读图规则：横向比较看哪种滤波对该噪声最有效——高斯噪声下高斯滤波最优（6.2），椒盐噪声下唯有中值（6.8）真正移除了噪点，两种线性滤波只是把白点摊开（11.7 与 11.8）。素材为 `examples/test.mp4` 第 7 秒帧，生成脚本 `examples/theory/cv_filters.py`。],
)

读一读图里的数字，比背结论有用。对高斯噪声，三种滤波都有效，高斯滤波最好（RMSE 从 13.5 降到 6.2）；注意均值滤波反而不如高斯——$5 times 5$ 平均把纯噪声压到 $sigma \/ 5 = 3$，输出误差却有 8.4，多出来的部分是模糊本身造成的。滤波的总误差 = 残余噪声 + 被抹掉的真实细节，核越大前者越小、后者越大，这是一切平滑滤波逃不掉的权衡。对椒盐噪声，两种线性滤波只把 RMSE 压回 11.7 左右，中值滤波一举压到 6.8，比它处理高斯噪声还干净。*先诊断噪声，再选滤波器*，顺序不能反。

顺带认识一下这个家族里更聪明的成员：双边滤波在高斯滤波的空间权重之外再乘一项*亮度相似度*权重——邻居不但要离得近，还要亮度相仿才有发言权。于是平坦区域照常平滑，跨越边缘的两侧却互不干涉，边缘得以保全。它揭示了一个重要方向：*让权重随图像内容自适应*——这个思想推到极致，就是「深度学习和神经网络」一章里从数据中学出来的卷积核。代价同样明显：权重逐像素现算，速度比可分离的高斯滤波慢得多，实时流水线里它出场不多。

=== 梯度与边缘

灯条与黑暗背景的分界、数字图案的笔画轮廓，在函数视角下是同一件事：亮度的剧烈变化。变化率由导数度量，离散函数没有导数，用差分近似：

$
frac(partial I, partial x) approx frac(I(x + 1, y) - I(x - 1, y), 2)
$

差分对噪声极其敏感：平坦区域里真实的亮度差接近零，而两个像素的噪声相减，标准差反而变成单像素的 $sqrt(2)$ 倍。所以实用的梯度算子都是"先平滑、再差分"的合体。Sobel 算子是最常用的一个：

$
bold(S)_x = mat(delim: "[", -1, 0, 1; -2, 0, 2; -1, 0, 1) = mat(delim: "[", 1; 2; 1) mat(delim: "[", -1, 0, 1)
$

分解式写明了它的构造：水平方向做 $[-1, 0, 1]$ 的中心差分，垂直方向同时做 $[1, 2, 1]$ 的加权平滑——在不模糊边缘本身的方向上把噪声压下去。$bold(S)_y$ 是它的转置。两个方向的响应 $g_x$、$g_y$ 合成梯度向量，其模长与方向：

$
g = sqrt(g_x^2 + g_y^2), quad theta = op("atan2")(g_y, g_x)
$

模长大的地方是边缘，方向 $theta$ 垂直于边缘走向。Sobel 省掉了差分的 $1\/2$ 和平滑权重的归一化，输出整体放大 8 倍——找边缘只关心相对大小，常数无所谓。二阶差分的 Laplacian 算子这里只提一句：边缘中心处二阶导过零，它定位更尖锐，也更怕噪声。

==== Canny：把"找边缘"做成一条流水线

拿到梯度模长图，离"干净的边缘"还差得远：噪声会产生零星的强响应，一条真实边缘会在梯度图上糊成好几个像素宽的亮带。Canny 边缘检测的贡献是把"好边缘"写成明确的准则——检得全、定位准、一条边只响应一次——并给出一条四步流水线：

+ *高斯平滑*：差分放大噪声，先把噪声压下去；
+ *Sobel 求梯度*：得到每个像素的模长与方向；
+ *非极大值抑制*：沿梯度方向（垂直于边缘）与相邻像素比较，只保留局部最大的那个，把几像素宽的亮带削成单像素细线；
+ *双阈值滞后连接*：高于高阈值的是确定边缘，低于低阈值的直接丢弃，夹在中间的"弱边缘"只有与确定边缘相连时才保留——依据一条朴素的先验：真实边缘是连续曲线，噪声响应是孤立的。

"只留局部最强、抑制其余响应"这个思想值得单独记住，它在「深度学习和神经网络」一章会以另一个 NMS（候选框的非极大值抑制）的名字再次出现——问题不同，哲学相同。

也要诚实交代：自瞄的主流水线里其实见不到 Canny。低曝光加二值化之后，前景背景已经黑白分明，直接找轮廓得到的就是干净的边界，不需要再从灰度梯度里辛苦提取。Canny 属于"没有条件控制成像"的一般场景——处理一段别人拍的视频、一张自然光照的照片时，它才是主角。知道什么时候不需要一个工具，和会用它同样重要。

=== 二值化：一刀下在哪里

二值化把灰度图变成非黑即白的判决：

$
B(x, y) = cases(255 quad & I(x, y) > t, 0 quad & "其他")
$

整条识别流水线的地基就是这一刀：切完之后，"图像处理"变成"对象处理"。问题只剩一个——阈值 $t$ 取多少？

低曝光画面里这不成问题：直方图两团分布之间是宽阔的无人区，刀立在无人区里的哪个位置，切出来的都是同一批像素。诚实交代一句：本书的演示视频是普通曝光的录像，二值图本身对阈值仍有明显变化，但灯条检测的最终结果在 100～200 内几乎不动（实测四个阈值下检出 11/10/11/10 根灯条）；真正的低曝光相机画面才有整段无人区，基础篇流水线写死 160 的底气来自那个域。鲁棒不是因为阈值选得准，而是因为成像把两类分得开——参数不敏感，是曝光策略送给算法的礼物。

==== Otsu：让直方图自己选阈值

分类器前的那张 20×28 数字小图就没这么好命了。透视矫正出来的小图里，亮笔画和暗底的绝对亮度随距离、随目标本身的亮度剧烈变化，写死一个阈值必然顾此失彼——需要每张小图自己算自己的阈值。这正是基础篇配套 `lightbar_pipeline.cpp` 里 `THRESH_OTSU` 那个 flag 干的事。

Otsu 方法的出发点：把归一化直方图 $p(v)$ 当作概率分布，一个阈值 $t$ 把像素分成两类，类的权重（像素占比）为

$
omega_0 (t) = sum_(v=0)^(t) p(v), quad omega_1 (t) = 1 - omega_0 (t)
$

两类各自的均值记为 $mu_0 (t)$、$mu_1 (t)$，方差记为 $sigma_0^2 (t)$、$sigma_1^2 (t)$。整幅图的总方差 $sigma^2$ 可以精确分解成两部分：

$
sigma^2 = underbrace(omega_0 sigma_0^2 + omega_1 sigma_1^2, "类内方差" sigma_W^2 (t)) + underbrace(omega_0 omega_1 (mu_0 - mu_1)^2, "类间方差" sigma_B^2 (t))
$

把全图均值 $mu = omega_0 mu_0 + omega_1 mu_1$ 代入展开即可验证。左边的总方差与 $t$ 无关，于是"类内尽量纯净"与"两类尽量拉开"是同一件事：最小化 $sigma_W^2$ 等价于最大化 $sigma_B^2$。后者算起来便宜得多——只依赖两类的权重和均值，用累积和递推，把 256 个候选阈值全部试一遍取最大：

$
t^* = op("arg max", limits: #true)_(0 <= t < 256) thin omega_0 (t) thin omega_1 (t) thin (mu_0 (t) - mu_1 (t))^2
$

整个搜索的开销远小于统计直方图时扫的那一遍图像。数字小图的直方图天然双峰——亮笔画和暗底各占一团——$sigma_B^2$ 的最大值恰好落在两峰之间，而且距离变了、亮度变了，它自动跟着直方图走。一个 flag 背后，就是这三行数学。

那为什么整幅画面不也用 Otsu，省掉手调的 160？看一眼 $sigma_B^2$ 的表达式就能预言结果：$omega_0 omega_1$ 这个因子在两类各占一半时最大，类别越悬殊越小。灯条像素占全图不到 1%，把阈值立在灯条与背景之间意味着 $omega_1 approx 0.006$，乘积被死死压住；反倒是把庞大的暗背景从中间劈开，两个"类"势均力敌，$sigma_B^2$ 更大。在真实赛场画面上验证：

#figure(
  image("images/theory-cv-otsu.png", width: 100%),
  caption: [Otsu 在一幅普通曝光赛场画面上的实际行为。上排：灰度原图、Otsu 阈值（$t^* = 54$，34% 像素变白）与流水线固定阈值（$t = 160$，0.6% 像素变白）的二值化结果；下两行共享横轴：对数坐标的直方图 $p(v)$ 与类间方差曲线 $sigma_B^2 (t)$，实线标 Otsu 的最大值位置，虚线标 160。读图规则：$sigma_B^2$ 的峰落在暗背景内部而不是背景与灯条之间——Otsu 认真地分开了"暗地板"与"亮地板"，灯条被归入亮类的汪洋里。生成脚本 `examples/theory/cv_otsu.py`。],
)

这不是 Otsu 的 bug，而是它的假设：类间方差准则天然偏好两类都有分量的划分。教训有一般性：一个自适应方法失效，通常不是数学错了，而是数据不满足它的假设。数字小图两类均衡，Otsu 是对的工具；整幅画面 1% 对 99%，写死的阈值反而是对的工具。

顺带补全第三种场景：若光照不均（半幅逆光、局部阴影），任何全局阈值——手选的还是 Otsu 选的——都无法两头兼顾，这时用自适应阈值：每个像素与自己邻域的均值减一个偏置比较，相当于把"一刀"换成一张随光照起伏的曲面。低曝光的赛场画面本身近乎全黑，基本用不上它。

=== 形态学：修补二值图

二值化的产物很少完美：灯条中间可能被贴纸或反光割出一道黑缝，背景里可能残留孤立的白色小点。形态学操作用一个小的形状模板——结构元 $B$，常见 $3 times 3$ 方形或椭圆——去规整这些斑块。对二值图（前景 255、背景 0），两种基本操作就是滑动的极值滤波：

$
(I ⊖ B)(bold(x)) = min_(bold(b) in B) I(bold(x) + bold(b)), quad (I ⊕ B)(bold(x)) = max_(bold(b) in B) I(bold(x) + bold(b))
$

*腐蚀*（$⊖$）取邻域最小值：一个前景像素只有当结构元完全落在前景内时才幸存——白色区域整体收缩一圈，比结构元还小的白点直接消失。膨胀（$⊕$）取最大值：结构元碰得到前景的位置都变白——白色区域外扩一圈，比结构元窄的黑缝被填平。两者互为对偶：腐蚀前景，就是膨胀背景。

单用哪个都会改变斑块的尺寸，实用的是它们的组合。*开运算*（先腐蚀后膨胀）清除小于结构元的白色噪点和细亮线，大斑块先缩后胀、尺寸大致复原；闭运算（先膨胀后腐蚀）填补小于结构元的黑洞黑缝，把断裂的斑块接回一体。名字来自效果：开运算"打开"细窄的连接，闭运算"闭合"细窄的断口。两者都幂等——同一操作做第二遍不会有任何新变化，和"平滑滤波做两遍会更糊"形成有趣的对照。

在灯条场景里对号入座：被反光割裂的灯条，用竖条形结构元做闭运算接回来；满地零星的小反光点，开运算一遍扫清。但要清醒：形态学是拿几何模板去修改真实测量——膨胀让灯条轮廓系统性外扩，闭运算可能把两根靠得近的灯条粘成一根。灯条角点之后要喂给 PnP 解算位姿（「相机模型」一章），角点偏一个像素，远距离下的位姿就要偏出一截。所以成熟的流水线对形态学的使用非常克制：能靠调曝光在成像端解决的问题，不留给形态学。

=== 连通域与轮廓：从像素到对象

二值图上说"这是一根灯条"，数学上说的是：一堆白色像素彼此连通，构成一个连通域。"连通"需要先定义谁是邻居：4 连通只认上下左右，8 连通把四个对角也算上。这个选择不是随口定的：一根倾斜的细灯条，像素沿对角方向排列，相邻像素常常只有角对角接触——4 连通会把一根灯条判成好几段，所以前景用 8 连通。而背景要反过来用 4 连通，否则会出拓扑悖论：一条 8 连通的闭合白色曲线，它围出的内外黑色区域能从曲线的对角缝隙"漏"成一片——曲线围了等于没围。前景 8、背景 4（或者对调），数字世界里的若尔当曲线定理才成立。

把每个连通域找出来并编号，叫连通域标记，思路有两条。一是漫水法：从一个白色像素出发，沿邻接关系把整个斑块搜一遍（广度或深度优先），搜到的标同一个号。二是两遍扫描：第一遍逐行发临时标号并记录哪些标号相邻，用并查集（高效合并等价类的小数据结构）归并这些标号，第二遍统一改写。两者都是线性复杂度，一幅图扫一两遍就完。

轮廓是更紧凑的表示：不存斑块的全部像素，只存边界，而且是*有序*的边界——沿着斑块外沿一步步走一圈，记下途经的坐标，得到一条首尾相接的点链。`findContours` 输出的就是它（内部是 Suzuki–Abe 边界跟随算法）：每个斑块一条外轮廓，斑块里若有洞再加内轮廓，并记录内外的从属关系。从"像素集合"到"有序点链"，信息被压缩到刚好够描述形状的程度：一根灯条上千个白像素，轮廓只需要几十个点。

到这里，本章的加工链条完成了：光变成数字，数字被压噪、切割、修补，最后整理成一条条有序点链。但"这条点链是不是灯条""它朝哪边斜、有多长"，要靠对点链做几何测量与拟合来回答——那是「传统视觉算法」一章的开场白。

=== 颜色的几何：RGB 立方体与 HSV 圆柱

前面的一切都发生在灰度世界，现在把颜色请回来——分红蓝是自瞄绕不开的问题。一个 RGB 像素是三维向量 $(R, G, B)$，全部颜色构成一个 $[0, 255]^3$ 的立方体：原点是黑，对角是白，连接两者的主对角线上 $R = G = B$，是全部的灰色。

在这个立方体里"按颜色选像素"为什么难？考虑同一根蓝色灯条从亮到暗的样子。理想的光照模型下，光强变化近似等比缩放三个分量——颜色向量沿着过原点的射线滑动。"所有亮度下的蓝色"因此是一个从原点张开、斜穿立方体的锥形区域；而逐通道阈值（`inRange`）能框出的只是棱平行于坐标轴的长方体盒子。用盒子框锥体，要么漏掉暗部，要么混进邻色，怎么框都不贴合。

HSV 的构造正是冲着这个几何困境去的：颜色的"身份"在向量的方向里，亮度在长度里，那就换一组坐标把两者拆开。把立方体沿灰色对角线立起来，灰轴竖直向上，每个颜色点改用柱坐标描述：绕灰轴的方位角是色相 $H$（哪种颜色），到灰轴的相对距离是饱和度 $S$（颜色有多纯），高度是明度 $V$（有多亮）。落到公式上（$R, G, B$ 归一化到 $[0, 1]$，记 $Delta = max(R, G, B) - min(R, G, B)$）：

$
V = max(R, G, B), quad S = cases(Delta \/ V quad & V > 0, 0 quad & V = 0)
$

$
H = 60 degree times cases(
  (G - B) \/ Delta quad & max "为" R,
  (B - R) \/ Delta + 2 quad & max "为" G,
  (R - G) \/ Delta + 4 quad & max "为" B,
)
$

结果对 $360 degree$ 取模：红在 $0 degree$、绿在 $120 degree$、蓝在 $240 degree$。8 位存储时 OpenCV 把 $H$ 除以 2 存进 0～179，$S$、$V$ 拉伸到 0～255。理论上，"选颜色"从此变成"选一段角度区间"——教科书讲到这里就翻页了。顺带一个工程细节：红色横跨 $0 degree$，角度阈值要写成两段区间的并。

==== H 在低饱和度下为什么没有意义

基础篇里那张"H 通道是一片噪声"的图，现在可以给出数学解释。看 $H$ 的公式：分母是 $Delta$。当像素接近灰色——$R approx G approx B$，即 $S -> 0$——分子分母同时趋于零，$H$ 成为 $0 \/ 0$ 型：*灰色没有色相*。几何上这再自然不过：$H$ 是绕灰轴的方位角，而灰轴上的点谈不上任何方位角，就像站在北极点问"哪边是东"。

奇异点附近，数值稳定性同样崩坏：$H$ 对通道噪声的敏感度正比于 $1 \/ Delta$。以蓝色扇区为例，$H = 240 degree + 60 degree (R - G) \/ Delta$，若 $Delta = 4$ 而噪声让 $R - G$ 抖动 $plus.minus 2$，色相就抖动 $plus.minus 30 degree$——从蓝紫抖到青绿；同样的噪声打在 $Delta = 100$ 的鲜艳像素上，只让 $H$ 抖动约 $1 degree$。用蒙特卡洛把这条规律完整画出来：

#figure(
  image("images/theory-cv-hue-noise.png", width: 100%),
  caption: [色相噪声随饱和度的发散（蓝色像素，$V = 200$，每通道叠加标准差 2 个灰度级的高斯噪声，每点 4 万次采样）。左：色相的圆标准差对 $S$ 的双对数图，散点沿一阶预测 $60 sqrt(2) sigma \/ Delta$ 排成直线——噪声按 $1 \/ Delta$ 发散，灰色区（$S lt.approx 20$）内标准差超过约 $10 degree$，色相不再可靠。右：同样的噪声下，$S = 150$ 时测得的色相集中在真值附近（标准差 $1.4 degree$），$S = 12$ 时摊成一座矮山（标准差 $16 degree$）。生成脚本 `examples/theory/cv_hue_noise.py`。],
)

现在回头看赛场画面，一切都对上了：灰色地板、黑色车体、白墙，饱和度全都贴地，它们的 $H$ 是纯噪声；更微妙的是灯条自己——低曝光下灯条中心过曝成白，白色同样在灰轴上（$S approx 0$），灯条最亮的部分恰恰是色相最不可信的部分。基础篇那套"用亮度找灯条、在 RGB 里数 B 与 R 谁大来分红蓝"的设计，现在可以从数学上盖章：找发光体用 $V$，它没有奇异点；判红蓝对斑块内所有像素的 $B$、$R$ 求和再比较，靠远离灰轴的带色像素投票，全程躲开除以 $Delta$ 的雷区。工程直觉的背后，是坐标变换的奇异性分析。

==== 本章小结

本章沿着基础篇流水线的暗线重新走了一遍，把每个环节的"为什么"补上：图像是采样加量化得到的二元函数，噪声由光子统计注定，低曝光在物理层面制造对比度；直方图丢掉空间、留下分布，Otsu 在类间方差最大处落刀，前提是两类都有分量；卷积是滑动加权平均，先诊断噪声再选滤波器；梯度是平滑加差分，Canny 用非极大值抑制和滞后阈值把梯度图收拾成单像素边缘；形态学拿结构元修补二值斑块，代价是改动真实几何；连通域与轮廓把像素整理成有序点链；HSV 是绕灰轴的柱坐标换元，灰轴是它的奇异轴。

三个接口通往后面的章：有序点链交给「传统视觉算法」一章去测量与拟合；"卷积核可以学出来"这颗种子在「深度学习和神经网络」一章发芽；而"灯条在图像里的位置"如何变成"装甲板在三维空间里的位姿"，从「相机模型」一章开始回答。
