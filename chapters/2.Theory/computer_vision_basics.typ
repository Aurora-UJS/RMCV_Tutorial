#import "/template/template.typ": *

基础篇的 OpenCV 一节带你跑通了一条完整的识别流水线：二值化、查找轮廓、应用几何判据，最后交给分类器。那一节主要回答“怎么做”，本章则回过头追问“为什么”。为什么低曝光加二值化能把灯条从赛场背景中分离出来？为什么分类器前的数字小图用 Otsu 自动选阈值，整幅画面却固定使用 160？为什么 H 通道在真实画面里会出现大片噪声？基础篇先用实际图像展示了这些现象，这里再把背后的数学依据一层层补上。识别流水线本身不再重跑；本章先讲到如何把原始图像整理成可以测量的对象，至于怎样从轮廓中提取几何量，留给下一章接着回答。

=== 从光到数字：图像的形成

在传感器这一层，相机先测量曝光期间落到各像素区域的光。入射光子以一定概率在光电二极管中产生电荷，像素把这些电荷积分起来；曝光结束后，读出电路再对信号放大并做模数转换，得到离散数值。数字图像就是这些像素读数按行列组织成的矩阵。它们与入射光子数相关，但还会受到量子效率、模拟增益、读出电路和后续图像处理影响，不能简单理解为逐个光子的直接计数。

==== 采样与量化

从连续的光场到离散的矩阵，需要经历两次离散化。*采样*把空间切成格子：镜头投在传感器上的亮度分布原本是连续的，传感器只在 $W times H$ 个位置上取值；光线如何投影到这些位置，将在「相机模型」一章说明。*量化*则把亮度切成台阶：8 位深度下，每个计数被表示为 0 到 255 的整数，共 256 级。于是一幅灰度图在数学上可以写成一个函数：

$
I: {0, 1, ..., W - 1} times {0, 1, ..., H - 1} -> {0, 1, ..., 255}
$

后文的直方图、卷积、梯度和阈值都可以写成对这个离散函数的操作。这个视角很重要：程序处理的不是人眼印象中的“画面”，而是带有坐标和值域约束的数据。位深不必是 8：不少工业相机提供 10 位或 12 位输出，量化台阶更细、可记录的层次更多，代价是数据带宽、存储和处理开销上涨。对以亮度阈值为主要判据、成像条件又可控的自瞄流水线，8 位输出可能已经够用；遇到暗部细节或高动态范围任务，仍应按实际数据验证。

处理后的彩色图像通常包含三个通道，但采用 Bayer 阵列的传感器在每个感光单元上只通过一种颜色的滤色片。去马赛克算法会结合邻域样本估计该输出位置缺少的通道；白平衡、颜色校正和相机 ISP 还可能继续改变数值。因此，每个输出像素的三个通道并非三次彼此独立的直接测量，像素级颜色判断也会同时受到插值、饱和与相机处理链影响。

==== 噪声：重复拍摄为何不同

即使场景和相机保持不动，连续两次曝光的像素值也不会完全相同。在光通量稳定、光子到达可近似为泊松过程的模型中，曝光期间平均产生 $N$ 个有效光电子时，散粒噪声的标准差约为 $sqrt(N)$；若其他噪声可以忽略，信噪比约为 $N \/ sqrt(N) = sqrt(N)$，只随有效光子数的平方根增长。真实传感器还会叠加读出噪声、暗电流、像素响应差异和量化误差。提高模拟或数字增益可以放大读出值，也会放大信号中已有的波动，却不会增加曝光期间已经收集到的光子。

这也是自瞄相机评估低曝光策略时要考虑的一项物理因素。灯条由 LED 主动发光，许多背景区域则主要依靠反射照明；在给定镜头、滤光片、增益和赛场光照下，两者可能在传感器上形成明显不同的信号范围。把曝光时间调到数毫秒量级时，背景可能接近黑场，而灯条仍保持较高读数甚至饱和，于是部分前景/背景分离由成像条件提前完成。具体曝光值和对比度不能从“LED 自发光”直接推出，还要用目标相机的直方图、饱和比例、运动模糊和检测结果共同验证。更一般的结论是：视觉判据依赖成像条件，算法参数与曝光、增益和光学配置应当一起记录。

=== 直方图：一张图的亮度账本

把每个灰度值在图中出现的次数 $h(v)$ 数一遍，再除以像素总数，就得到归一化直方图：

$
p(v) = frac(h(v), W H), quad sum_(v=0)^(255) p(v) = 1
$

直方图把整幅图压缩成 256 个数，代价是丢掉全部空间信息：一张左黑右白的图和一张黑白棋盘格，直方图可能完全相同。但保留下来的分布信息已经能回答不少问题，例如画面偏暗还是偏亮（分布的重心）、对比度高还是低（分布的展宽）、是否存在过曝（255 处的尖峰）。低曝光赛场画面的直方图通常有很明显的结构：绝大多数像素集中在 0 附近，另有少量饱和像素靠近 255，两团之间留出较宽的空白区间。此时只要把阈值放在这个区间内，就能分离灯条。阈值如何选择、什么时候这种方法会失效，将在二值化一节讨论；在那之前，先看噪声如何处理。

=== 卷积与滤波

==== 卷积：滑动的加权平均

对图像做局部操作的标准形式是卷积：拿一个小矩阵（核，kernel）在图像上滑动，每到一处，把核的元素与它盖住的像素逐位相乘再求和，作为输出图像在该处的值。核的半径记为 $r$（$3 times 3$ 核即 $r = 1$）：

$
(I * K)(x, y) = sum_(i=-r)^(r) sum_(j=-r)^(r) K(i, j) thin I(x + i, y + j)
$

严格说，数学定义的卷积要先把核旋转 180 度，OpenCV 的 `filter2D` 等接口通常计算不翻转核的相关。均值核和高斯核这类中心对称核在两种定义下相同；Sobel 等导数核并不满足这一条件，其响应符号和方向要按实际核的约定解释。图像边缘处核会伸出界外，工程上可用常数填充、复制或不同形式的镜像延拓；边界规则也会改变边缘附近的输出。

一个 $k times k$ 的核，每个输出像素要做 $k^2$ 次乘加；对一幅 $1280 times 1024$ 的图用 $5 times 5$ 核，就是约 3300 万次乘加。「计算机系统基础」一章讲过的行主序访存规律在这里同样生效：卷积是典型的规则访存、乘加密集的计算模式。「深度学习和神经网络」一章还会把同一种计算写成卷积层；区别在于，本章的核由人手工设计，卷积层的核则由数据学习。

==== 三类滤波器与适用噪声

*均值滤波*的核最直接：$3 times 3$ 全一除以 9，输出就是邻域平均。若邻域内真实信号近似不变，九个噪声样本又独立、零均值且具有相同标准差 $sigma$，平均后的噪声标准差会降到 $sigma \/ 3$。这和「卡尔曼滤波」一章开头的递推平均共享“组合多次观测以降低独立波动”的思路；空间邻域不满足这些假设时，结论不能直接套用。均值滤波也会平滑真实细节，边缘同样会受到影响。

*高斯滤波*把"全一"换成从二维高斯函数上采样的权重：

$
K(i, j) prop e^(-(i^2 + j^2) / (2 sigma_s^2))
$

离中心越远权重越小，可以理解为在没有其他边缘信息时，空间上较近的像素获得更大参考权重。参数 $sigma_s$ 与核尺寸共同控制平滑范围。按两个一维高斯向量的外积构造二维核时，它具有*可分离*性：先做水平方向的一维滤波，再做垂直方向的一维滤波，在相同边界规则和理想算术下等价于二维滤波，每像素乘加次数从 $k^2$ 降到 $2k$（$5 times 5$ 核：25 次变 10 次）。实际浮点舍入和实现细节可能带来微小差异。

*中值滤波*不做加权求和，而是把窗口里的像素排序后取中位数。它是非线性的，常用于处理孤立的脉冲噪声，例如表现为纯黑或纯白点的椒盐噪声。以背景灰度约 30、窗口内出现一个 255 白点为例，$3 times 3$ 均值滤波会把输出抬高 $(255 - 30) \/ 9 approx 25$ 个灰度级，把单点影响扩散到邻域；如果窗口内其余值仍代表背景，单个极端值则不会改变中位数。对理想阶跃且窗口两侧各自近似常值的情况，中值仍取自阶跃的一侧，通常比线性平均更能保留突变；纹理、细线和高密度脉冲噪声下仍要单独检查。

#figure(
  image("images/theory-cv-filters.png", width: 100%),
  caption: [同一块装甲板区域加两种噪声（上排：高斯噪声 $sigma = 15$；下排：5% 椒盐噪声），分别经过三种 $5 times 5$ 滤波器，每格标注与干净原图的 RMSE（灰度级）。在这组输入与参数下，高斯噪声对应的高斯滤波 RMSE 最低（6.2）；椒盐噪声对应的中值滤波最低（6.8），两种线性滤波把孤立白点扩散后分别得到 11.7 与 11.8。素材为 `examples/test.mp4` 第 7 秒帧，生成脚本 `examples/theory/cv_filters.py`。],
)

图里的数字也说明，不能只用“核越大，画面越干净”概括滤波效果。在这次合成高斯噪声试验中，三种滤波都降低了 RMSE，其中高斯滤波从 13.5 降到 6.2。$5 times 5$ 均值在局部常值、独立噪声模型下会把噪声标准差降到 $sigma \/ 5 = 3$，但图中总 RMSE 仍有 8.4，因为它还计入滤波造成的真实边缘和纹理变化。核变大时，残余随机噪声可能下降，细节误差也可能上升。对本图的椒盐噪声，中值滤波得到 6.8，两种线性滤波约为 11.7。它们是这张图、这两种合成噪声和指定参数下的结果；实际选择还应根据噪声来源、细节尺度和下游指标验证。

双边滤波在高斯滤波的空间权重之外，再加入*亮度相似度*权重。相邻像素既要距离较近、亮度也要相近，才会获得较大权重。这样可以减少边缘两侧像素的相互混合，在平滑区域噪声的同时保留一部分边缘。它体现了*让权重随图像内容变化*的思路；「深度学习和神经网络」一章还会看到由数据学习卷积核的方法。双边滤波需要按像素和邻域计算额外权重，通常比同尺寸的可分离高斯滤波开销更高；是否满足实时预算应在目标分辨率、参数、实现和硬件上测量。

=== 梯度与边缘

灯条与黑暗背景的分界、数字图案的笔画轮廓，在函数视角下是同一件事：亮度的剧烈变化。变化率由导数度量，离散函数没有导数，用差分近似：

$
frac(partial I, partial x) approx frac(I(x + 1, y) - I(x - 1, y), 2)
$

差分会放大高频波动：平坦区域里真实的亮度差接近零；若相减的两个像素带有相互独立、标准差相同的噪声，差值噪声的标准差会变成单像素的 $sqrt(2)$ 倍。Sobel 等常用梯度算子因此把一个方向的差分与垂直方向的局部平滑组合起来：

$
bold(S)_x = mat(delim: "[", -1, 0, 1; -2, 0, 2; -1, 0, 1) = mat(delim: "[", 1; 2; 1) mat(delim: "[", -1, 0, 1)
$

分解式写明了它的构造：水平方向用 $[-1, 0, 1]$ 做中心差分，垂直方向同时用 $[1, 2, 1]$ 加权平滑。对近似竖直的边缘，平滑主要沿边缘方向进行；任意纹理和边缘方向下的实际响应仍由二维核共同决定。$bold(S)_y$ 是它的转置。两个方向的响应 $g_x$、$g_y$ 合成梯度向量，其模长与方向为：

$
g = sqrt(g_x^2 + g_y^2), quad theta = op("atan2")(g_y, g_x)
$

在亮度变化符合局部阶跃近似时，较大的模长提示边缘，梯度方向 $theta$ 指向亮度增长最快的方向，因而通常与边缘切向近似垂直。图像坐标的 $y$ 轴方向和库的核约定还会影响角度符号。这里的 Sobel 核没有除以中心差分的 2 和平滑权重和 4，因此相对“归一化平滑后求导”的尺度整体大 8 倍；相对排序不受这个常数影响，但绝对阈值和不同算子间的数值不能忽略尺度。Laplacian 使用二阶差分，对理想平滑阶跃可通过零交叉定位变化位置，但也会更强地响应高频噪声，通常要与平滑和明确的检测规则配合。

==== Canny：把"找边缘"做成一条流水线

拿到梯度模长图，还不能直接得到细而连续的边缘：噪声可能产生零星强响应，真实边缘也可能在梯度图上形成几个像素宽的响应带。Canny 方法以较高检测率、较好定位和单边缘单响应为设计目标，并把处理组织成四个主要步骤：

+ *高斯平滑*：差分放大噪声，先把噪声压下去；
+ *Sobel 求梯度*：得到每个像素的模长与方向；
+ *非极大值抑制*：沿梯度方向（垂直于边缘）与相邻像素比较，只保留局部最大的那个，把几像素宽的亮带削成单像素细线；
+ *双阈值滞后连接*：高于高阈值的作为强边缘，低于低阈值的丢弃，夹在中间的弱响应只有能沿邻接关系连接到强边缘时才保留。它利用真实结构常具有一定连续性这一假设，但纹理噪声也可能连续，断裂的真实边缘也可能被删去。

"只保留局部最强响应、抑制其余响应"这一处理还会在「深度学习和神经网络」一章出现。那里的 NMS 作用于候选框而不是边缘像素，但判断方式相似：在一组相互重叠的响应中保留最有代表性的一个。

本书的灯条识别示例没有使用 Canny：在其低曝光与固定阈值条件下，二值前景已经较为分明，可以直接从二值图查找轮廓。无法主动控制成像、灰度过渡或纹理更复杂的任务可能更需要梯度边缘，但仍应根据输入和下游目标比较。选择算法时，除了了解它如何使用，也要判断当前数据是否真的需要它。

=== 二值化：一刀下在哪里

二值化把灰度图变成非黑即白的判决：

$
B(x, y) = cases(255 quad & I(x, y) > t, 0 quad & "其他")
$

二值化这一刀下去，连续灰度被分成前景和背景，后续处理也从“像素有多亮”转向“这里有几个区域、轮廓长什么样”。问题于是集中到一点：这一刀，也就是阈值 $t$，究竟该落在哪里？

在经过调节的低曝光画面中，前景与背景的直方图分布之间可能出现较宽的低频区间；阈值落在其中时，前景集合对小幅参数变化不敏感。需要说明的是，本书的演示视频采用普通曝光，二值图本身仍会随阈值明显变化；在这段视频和当前几何判据下，100～200 范围内四个实测阈值分别检出 11、10、11、10 根灯条。这个结果只说明最终计数在这些测试点变化较小，不代表所有像素、目标质量或其他视频都稳定。基础篇固定使用 160，是针对其示例和预期成像条件的工程选择，不是由公式推出的通用最优值。

==== Otsu：让直方图自己选阈值

分类器前的 20×28 数字小图就没这么简单了。透视矫正后，亮笔画和暗背景的绝对亮度仍会随距离和目标亮度明显变化，同一个固定阈值很难兼顾全部样本，因此需要让每张小图自己估计阈值。基础篇配套 `lightbar_pipeline.cpp` 中的 `THRESH_OTSU` 标志，做的正是这件事。

Otsu 方法的出发点：把归一化直方图 $p(v)$ 当作概率分布，一个阈值 $t$ 把像素分成两类，类的权重（像素占比）为

$
omega_0 (t) = sum_(v=0)^(t) p(v), quad omega_1 (t) = 1 - omega_0 (t)
$

两类各自的均值记为 $mu_0 (t)$、$mu_1 (t)$，方差记为 $sigma_0^2 (t)$、$sigma_1^2 (t)$。整幅图的总方差 $sigma^2$ 可以精确分解成两部分：

$
sigma^2 = underbrace(omega_0 sigma_0^2 + omega_1 sigma_1^2, "类内方差" sigma_W^2 (t)) + underbrace(omega_0 omega_1 (mu_0 - mu_1)^2, "类间方差" sigma_B^2 (t))
$

把全图均值 $mu = omega_0 mu_0 + omega_1 mu_1$ 代入展开即可验证。左边的总方差与 $t$ 无关，于是"类内尽量纯净"与"两类尽量拉开"是同一件事：最小化 $sigma_W^2$ 等价于最大化 $sigma_B^2$。后者算起来便宜得多——只依赖两类的权重和均值，用累积和递推，把 256 个候选阈值全部试一遍取最大：

$
t^* = op("arg max", limits: #true)_(0 <= t < 256) thin omega_0 (t) thin omega_1 (t) thin (mu_0 (t) - mu_1 (t))^2
$

对 8 位图像只需检查有限的 256 个灰度级，因此阈值搜索通常比首次遍历全部像素统计直方图便宜。若数字小图确实由分离较好的亮笔画和暗背景组成，直方图会呈现近似双峰，$sigma_B^2$ 的最大值常落在两峰之间；距离、曝光和目标亮度变化时，估计阈值也会随分布移动。`THRESH_OTSU` 标志调用的正是这类间方差最大化过程，但“双峰且对应所需语义类别”仍是要由样本检查的条件。

那么，为什么不在整幅画面上也使用 Otsu？从 $sigma_B^2$ 的表达式可以看到，目标很小时，$omega_0 omega_1$ 会压低“目标/背景”划分的得分；只有均值差足够大时，平方项才可能抵消这种影响。在本书这帧图像中，灯条像素占比约 0.6%，而暗背景内部又存在不同亮度区域，于是某个背景内部划分获得了更大的类间方差。这个机制并不意味着 Otsu 对所有小目标都会失败，但说明类别占比和背景多峰都可能让最大值偏离任务需要的分界。实测结果如下：

#figure(
  image("images/theory-cv-otsu.png", width: 100%),
  caption: [Otsu 在一幅普通曝光赛场画面上的实际行为。上排：灰度原图、Otsu 阈值（$t^* = 54$，34% 像素变白）与流水线固定阈值（$t = 160$，0.6% 像素变白）的二值化结果；下两行共享横轴：对数坐标的直方图 $p(v)$ 与类间方差曲线 $sigma_B^2 (t)$，实线标 Otsu 的最大值位置，虚线标 160。$sigma_B^2$ 的峰落在暗背景内部，而不是背景与灯条之间：Otsu 分开的是两组亮度不同的背景像素，灯条则和大量背景一起被归入亮类。生成脚本 `examples/theory/cv_otsu.py`。],
)

这不是 Otsu 实现错误，而是优化准则与任务目标并不相同：算法寻找类间方差最大的灰度划分，并不知道哪一类代表灯条。数字小图若近似满足双峰、类内较集中等条件，Otsu 可以成为合适候选；整幅画面中小目标占比悬殊且背景结构复杂时，经标定的固定阈值可能更符合本书流水线。两种选择都要用当前数据和下游检测结果验证，不能只凭“自适应”或“固定”判断优劣。

还有第三种场景：若光照不均（半幅逆光、局部阴影），目标与背景的亮度范围可能在全图中相互重叠，此时手选或 Otsu 得到的单个全局阈值都难以兼顾各区域。自适应阈值让每个像素与其邻域统计量减去偏置后的结果比较，相当于使用随局部亮度变化的阈值面。它也会受窗口尺寸、局部纹理和噪声影响；低曝光画面是否需要这一步，应由实际分割误差和计算预算决定。

=== 形态学：修补二值图

二值化结果可能包含灯条内部的黑缝或背景中的孤立白点。形态学操作用一个小的形状模板——结构元 $B$，常见 $3 times 3$ 方形或椭圆——按邻域关系调整这些区域。对前景为 255、背景为 0 的二值图和平坦结构元，两种基本操作可以写成滑动的极值滤波：

$
(I ⊖ B)(bold(x)) = min_(bold(b) in B) I(bold(x) + bold(b)), quad (I ⊕ B)(bold(x)) = max_(bold(b) in B) I(bold(x) + bold(b))
$

*腐蚀*（$⊖$）取结构元覆盖范围内的最小值：一个输出位置只有在对应邻域全部属于前景时才保持为白，因此区域边界会按结构元形状向内移动，某些小前景会消失。*膨胀*（$⊕$）取最大值：邻域内只要存在前景，输出就变白，因此区域边界向外移动，某些窄黑缝会被填补。结构元形状、锚点和边界规则都会影响具体结果；在集合定义和互补结构元下，腐蚀与膨胀具有对偶关系。

单独使用腐蚀或膨胀都会系统性改变区域尺寸，工程中常把它们组合。*开运算*（先腐蚀后膨胀）可以去除结构元容不下的部分小前景和细连接，同时尽量恢复保留下来的大区域；*闭运算*（先膨胀后腐蚀）可以填补部分小孔洞和窄断口。具体能否去除或连接取决于结构元相对于目标的尺寸与方向，不能只比较面积。理想集合形态学中，使用同一结构元的开、闭运算都是幂等的；数字图像实现还要保持锚点和边界约定一致。

在灯条场景中，竖条形结构元的闭运算可能重新连接被反光分开的区域，开运算则可能去除零星小亮点。但形态学会直接修改测得的几何形状：膨胀使轮廓外移，闭运算也可能把两根相近灯条连成一块。灯条角点之后还要交给 PnP 解算位姿（见「相机模型」一章），像素级偏移对最终位姿的影响取决于距离、焦距、目标姿态和点的布局，需要通过重投影与位姿误差评估。因此，加入形态学操作前既要检查分割改善，也要检查几何测量是否被系统性改变；能稳定控制曝光时，还可以比较从成像端改善输入的方案。

=== 连通域与轮廓：从像素到对象

二值图上说“这是一根灯条”，首先要把彼此相连的白色像素归为一个连通域。连通性必须先定义邻居：4 连通只包含上下左右，8 连通再加入四个对角。倾斜细线的相邻像素可能只在角点接触，此时 4 连通会把它分段，而 8 连通能把这些像素归在一起。数字拓扑中通常为前景和背景选择互补的连通性，例如前景 8 连通、背景 4 连通，避免前景与背景同时把同一对角接触视为连通而产生边界内外不一致。具体选择仍要与目标最小宽度、噪声和后续 API 约定一致。

把每个连通域找出来并编号，叫连通域标记。常见思路有两类：一是从未访问的前景像素出发，沿邻接关系做广度或深度搜索；二是两遍扫描，第一遍逐行分配临时标号并记录等价关系，用并查集合并等价类，第二遍写回统一标号。在固定邻域、每个像素只处理常数次的实现中，时间复杂度随像素数近似线性；具体遍历次数、并行方式和缓存行为由实现决定。

轮廓不保存区域内的全部像素，而是按顺序记录边界点，形成闭合点链。OpenCV `findContours` 返回哪些轮廓和层级，取决于检索模式：`RETR_EXTERNAL` 只保留最外层轮廓，`RETR_CCOMP` 或 `RETR_TREE` 才提供不同层次的孔洞关系。点数还取决于近似模式：`CHAIN_APPROX_NONE` 保留相邻边界步，`CHAIN_APPROX_SIMPLE` 会压缩水平、垂直和对角直线段。OpenCV 的经典实现基于边界跟随方法，但调用者应依赖公开的模式与输出契约，而不是假定固定内部算法。规则形状经过简化后可能只需少量点；复杂或锯齿状边界仍可能包含很多点。

到这里，处理链已经从传感器读数推进到有序的轮廓点链：中间可按数据需要加入降噪、分割和形态学处理。至于“这条点链是不是灯条”“它朝哪个方向倾斜、有多长”，还要靠几何测量与拟合回答，这正是「传统视觉算法」一章的开场。

=== 颜色的几何：RGB 立方体与 HSV 坐标

前面的处理主要使用灰度信息，而自瞄任务还可能需要区分红蓝。一个 8 位 RGB 像素可以看作三维向量 $(R, G, B)$，其取值范围构成 $[0, 255]^3$ 的立方体：原点是黑色，对角顶点是白色，满足 $R = G = B$ 的主对角线对应各级中性灰。

在这个立方体里，逐通道阈值为什么不容易同时覆盖亮暗变化？在没有通道偏置、饱和、白平衡变化等影响的简化模型中，同一种光谱组成只改变强度时，三个分量会近似同比缩放，颜色向量沿过原点的射线移动。一族相近颜色与不同亮度可近似形成从原点展开的区域，而 RGB `inRange` 选择的是轴对齐长方体；用后者近似前者时，覆盖暗部与排除邻色之间会有取舍。真实相机偏离这个简化模型时，区域还会进一步变形。

HSV 用色相 $H$、饱和度 $S$ 和明度 $V$ 把色彩类别、接近灰色的程度与最大通道值分开描述。它常被画成圆柱或六棱锥来建立直觉：$H$ 类似绕灰轴的角度，$S$ 表示偏离灰色的相对程度，$V$ 表示亮度尺度。但标准 HSV 是按 RGB 最大值、最小值分段定义的坐标变换，不是 RGB 立方体中的欧氏柱坐标，示意图不能代替公式。把 $R, G, B$ 归一化到 $[0, 1]$，记 $Delta = max(R, G, B) - min(R, G, B)$：

$
V = max(R, G, B), quad S = cases(Delta \/ V quad & V > 0, 0 quad & V = 0)
$

$
H = 60 degree times cases(
  (G - B) \/ Delta quad & max "为" R,
  (B - R) \/ Delta + 2 quad & max "为" G,
  (R - G) \/ Delta + 4 quad & max "为" B,
) quad (Delta > 0)
$

结果对 $360 degree$ 取模：红在 $0 degree$、绿在 $120 degree$、蓝在 $240 degree$。当 $Delta = 0$ 时色相在数学上未定义；具体库可写入一个约定值。对默认的 8 位 OpenCV HSV 表示，$H$ 缩放到 0～179，$S$、$V$ 缩放到 0～255。这样，按颜色选择像素可以转化为选择色相区间并同时限制饱和度和明度。红色区间跨越角度起点，因此通常要写成两个区间的并集。

==== H 在低饱和度下为何不可靠

基础篇里低饱和区域的 H 通道看起来杂乱，可以从公式解释：$H$ 的分母是 $Delta$。当像素接近灰色，即 $R approx G approx B$、$S -> 0$ 时，分子分母同时趋于零，色相在 $Delta = 0$ 处没有定义。用角度示意来理解，就像在北极点询问“哪边是东”：实现可以写入约定数值，却没有稳定的颜色方向可测。

在固定色相分段且远离分段边界的近似下，$H$ 对通道扰动的敏感度包含 $1 \/ Delta$ 因子。以蓝色分段为例，$H = 240 degree + 60 degree (R - G) \/ Delta$；若 $Delta = 4$ 而噪声让 $R - G$ 变化 $plus.minus 2$，这一项就变化约 $plus.minus 30 degree$，而 $Delta = 100$ 时约为 $plus.minus 1.2 degree$。实际噪声还会同时改变 $Delta$ 和最大通道，跨越分段边界时行为更复杂。下面用蒙特卡洛试验展示这一趋势：

#figure(
  image("images/theory-cv-hue-noise.png", width: 100%),
  caption: [色相噪声随饱和度的发散（蓝色像素，$V = 200$，每通道叠加标准差 2 个灰度级的高斯噪声，每点 4 万次采样）。左：色相的圆标准差对 $S$ 的双对数图，散点沿一阶预测 $60 sqrt(2) sigma \/ Delta$ 排成直线——噪声按 $1 \/ Delta$ 发散，灰色区（$S lt.approx 20$）内标准差超过约 $10 degree$，色相不再可靠。右：同样的噪声下，$S = 150$ 时测得的色相集中在真值附近（标准差 $1.4 degree$），$S = 12$ 时摊成一座矮山（标准差 $16 degree$）。生成脚本 `examples/theory/cv_hue_noise.py`。],
)

这可以解释赛场画面中的一种常见现象：灰色地板、黑色车体和白墙往往饱和度较低，其 H 数值容易被小幅通道噪声显著改变。灯条中心若过曝成近白色，也会落到低饱和区域，因而未必提供可靠色相。基础篇的 C++ 示例先用 `COLOR_BGR2GRAY` 得到加权灰度并寻找亮区，再在候选斑块内累加 B、R 通道判断红蓝；这两步都没有除以 $Delta$。累加还能让多个像素共同提供证据，但结果仍会受饱和、白平衡、彩色噪声和区域污染影响，需要在目标相机上验证。

==== 本章小结

本章沿着基础篇的识别流水线重新走了一遍，把每个环节背后的“为什么”补了起来。图像是连续光场经过采样和量化后得到的离散函数；泊松模型解释了散粒噪声的一部分规律，真实传感器还会叠加其他噪声；合适的低曝光设置则可能从成像阶段增强灯条与背景的对比。直方图舍弃空间、保留分布；Otsu 在类间方差最大处选阈值，但目标像素占比很小时，这个最大值未必落在目标与背景之间。卷积可以看作滑动的加权求和，滤波器要按噪声类型选择；梯度算子把平滑和差分结合起来，Canny 再用非极大值抑制与滞后阈值把宽响应整理成单像素边缘。形态学能修补二值区域，也会改变测得的几何；连通域与轮廓则把像素整理成有序点链。最后，HSV 以分段坐标变换描述色相、饱和度和明度，而低饱和区域恰好是色相不稳定的区域。

这些内容与后续三章直接衔接：有序点链将在「传统视觉算法」一章中用于测量与拟合；由数据学习卷积核的方法将在「深度学习和神经网络」一章展开；灯条的像素位置如何转换成装甲板的三维位姿，则从「相机模型」一章开始说明。
