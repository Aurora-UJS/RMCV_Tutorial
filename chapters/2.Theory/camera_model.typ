#import "/template/template.typ": *

基础篇 OpenCV 一节的结尾留过一个问题：识别出装甲板只是第一步，接下来还要“用两灯条的像素几何加相机内参解算三维位姿”。识别流水线给出若干像素坐标，火控需要的却是目标在三维空间中的位置。相机把三维点投影到二维图像，投影会丢失一部分信息，但其几何关系可以明确建模。本章先介绍针孔模型、内参、投影链与畸变，再说明怎样通过标定估计模型参数，最后用 PnP 从四个角点估计装甲板位姿，并分析不同方向的误差敏感性。

#definition()[
#set list(indent: 2em)
#{
  set par(first-line-indent: 0pt)
  [本章会反复用到四个坐标系，先把名字和方向认清：]
}

- *世界坐标系* $(X_w, Y_w, Z_w)$：场地或机器人上选定的参考系，单位米
- *相机坐标系* $(X_c, Y_c, Z_c)$：原点在相机光心，$z$ 轴沿光轴朝外，单位米
- *图像坐标系* $(x, y)$：像平面上以主点（光轴与像平面交点）为原点，单位毫米
- *像素坐标系* $(u, v)$：原点在图像左上角，$u$ 向右、$v$ 向下，单位像素
]

=== 针孔相机：把三维世界压成二维

先问一个比“相机怎么成像”更早的问题：为什么只把一块感光芯片对着外界，得不到清晰图像？因为物体上各点发出的光会同时落到芯片上的许多位置，彼此叠加后只剩一片模糊。成像系统需要限制到达每个像素的光线方向。针孔模型在暗箱上只留一个小孔，使同一空间方向的光落到像平面的同一位置。沿同一视线的多个空间点仍会投到同一点，所以这不是空间点与像素的一一对应。孔径减小时几何模糊通常减小，但进光量也会降低，继续缩小还会受到衍射影响；真实相机用透镜收集更多光线，也引入了后文要处理的畸变。

小孔成的是倒像：上方的点会投到像平面下方。若照着真实光路推导，后面就得一直背着负号。通常的做法是把像平面镜像到光心前方一个焦距 $f$ 处，得到正立的“虚拟像平面”。这个处理不改变几何关系，只是让公式干净一些；后文都采用这一约定。

#figure(
  image("images/theory-camera-pinhole.png", width: 100%),
  caption: [针孔模型的侧视图。品红点 $P$ 与光心 $O$ 的连线穿过前置虚拟像平面（蓝色，位于 $z=f$），交点为像点 $p$；灰色虚线表示光心后的倒立像平面。两个像平面的符号约定不同，但给出相同的投影几何。由图中的相似三角形可得 $y\/f=Y\/Z$。],
)

侧视图里能看到两个共享顶点 $O$ 的相似三角形：大的以 $Z$ 为底、$Y$ 为高，小的以 $f$ 为底、$y$ 为高。相似三角形对应边成比例：

$
Y / Z = y / f
quad => quad
y = f dot Y / Z
$

水平方向同理，$x = f dot X \/ Z$。这两行就是针孔模型的全部：

$
x = f X / Z, quad y = f Y / Z
$

这两个公式给出两条重要性质。第一，在目标正对相机且物理尺寸不变等条件下，像的尺寸与光轴深度 $Z$ 成反比；深度加倍时，像的线性尺寸减半。第二，投影会丢失深度信息：把 $(X,Y,Z)$ 同时乘以同一个正数，$(x,y)$ 不变。因此，单个像点只确定一条视线，无法独立区分同一视线上的近小目标与远大目标。后文的 PnP 能估计深度，是因为它额外使用了目标几何尺寸已知这一先验以及多个点的对应关系。

还有一处数学上的别扭要先处理：“除以 $Z$”不是线性运算，不能直接写成矩阵乘法；可我们马上要把多次变换连成一条矩阵链。标准做法是引入*齐次坐标*：给向量增加一个维度，并规定整体乘以非零常数后仍表示同一个点。于是投影可以写成：

$
Z vec(x, y, 1) = mat(f, 0, 0; 0, f, 0; 0, 0, 1) vec(X, Y, Z)
$

右边现在是矩阵乘法，非线性的除法由左边的系数 $Z$ 表示。完成矩阵乘法后，再把结果除以第三个分量，使其归一成 1，前两个分量就是像坐标。这一步称为齐次除法，也是这条理想针孔投影链中的非线性步骤。

=== 内参矩阵：从物理长度到像素坐标

上一节的 $(x, y)$ 还使用毫米等物理长度作单位，原点位于主点；相机通过接口交给你的却是一个像素阵列，原点在左上角。要从图像坐标走到像素坐标，只需做两件事：*换单位*和*移动原点*。

换单位：感光芯片由一格一格的像元铺成，设每个像元宽 $d_x$ 毫米、高 $d_y$ 毫米，那么 $x$ 毫米对应 $x \/ d_x$ 个像素。搬原点：主点大约在图像中心，设它的像素坐标为 $(c_x, c_y)$，加上去即可：

$
u = x / d_x + c_x, quad v = y / d_y + c_y
$

把针孔投影代入，两步合成一步：

$
u = underbrace(f / d_x, f_x) dot X_c / Z_c + c_x, quad
v = underbrace(f / d_y, f_y) dot Y_c / Z_c + c_y
$

这里得到视觉几何中最常用的两个参数 $f_x, f_y$。它们等于物理焦距除以对应方向的像元尺寸，也就是用像素表示的焦距，因此单位为像素。以一台 1440×1080 的工业相机为例：6 mm 定焦镜头配合 3.45 µm 像元时，标称计算给出 $f_x = 6 "mm" \/ 3.45 "µm" approx 1739$ 像素；实际值还会受对焦与镜头公差影响，应以标定结果为准。若一米宽的目标近似正对相机且横向尺寸与成像平面平行，它在光轴深度 $Z$ 处约占 $f_x \/ Z$ 个像素。例如，$Z=2$ m 时约为 870 像素。后文分析深度敏感性时还会用到这一关系。

写成矩阵，就是内参矩阵（camera matrix）：

$
Z_c vec(u, v, 1) = underbrace(mat(f_x, s, c_x; 0, f_y, c_y; 0, 0, 1), bold(K)) vec(X_c, Y_c, Z_c)
$

各元素的含义如下：

- $f_x, f_y$：以像素为单位的焦距。像元不一定是正方形，因此可能有 $d_x eq.not d_y$；现代工业相机的像元通常接近正方形，标定时仍会分别估计这两个参数。
- $c_x, c_y$：主点位置。理想值是图像几何中心（如 1440×1080 的图约为 (720, 540)），但镜头和芯片的装配做不到光轴严格穿过芯片中心，实测往往偏离中心若干像素——所以它也要标定，不能想当然用图像中心。
- $s$：倾斜因子（skew），描述像素阵列两轴不垂直的程度。对现代 CMOS 相机通常可取 0，OpenCV 的常用标定模型也不估计这个参数。

$bold(K)$ 是上三角矩阵；当 $f_x$ 与 $f_y$ 非零时可逆。像素点去除畸变后，计算 $bold(K)^(-1) (u,v,1)^top$ 可以得到与该像素对应的相机系视线方向，向量长度仍是任意的。这是后面 PnP 使用的基本关系。

=== 完整投影链：从世界点到像素

针孔模型的输入是相机坐标系下的点，但目标点最初可能表示在世界坐标系中，也可能表示在装甲板自身的坐标系中。要把它转换到相机坐标系，需要经过一次刚体变换：先旋转，再平移：

$
vec(X_c, Y_c, Z_c) = bold(R) vec(X_w, Y_w, Z_w) + bold(t)
$

$bold(R)$ 是一个 3×3 的旋转矩阵。它为什么用 9 个数表示 3 个自由度、还有哪些常用表示方法，将在下一章「坐标变换」中介绍。$bold(R)$ 和 $bold(t)$ 合称*外参*（extrinsics），描述两个坐标系之间的相对位姿；相机相对世界移动时，外参也随之改变。$bold(K)$ 和畸变系数则属于*内参*（intrinsics），在镜头、对焦和成像配置保持不变时通常也保持不变。

借助齐次坐标，可以把平移也写进矩阵乘法，得到完整的投影关系：

$
s vec(u, v, 1) =
underbrace(bold(K), "内参") thick
underbrace(mat(bold(R), bold(t)), "外参") thick
vec(X_w, Y_w, Z_w, 1)
$

其中 $mat(bold(R), bold(t))$ 是把 $bold(R)$ 与 $bold(t)$ 并排组成的 3×4 矩阵，$s=Z_c$ 是齐次归一化的尺度。可以把公式分成三部分理解：

- *外参块*把点从世界坐标系变换到相机坐标系。式中采用 3×4 投影块；对应的 4×4 齐次刚体变换可逆。
- *齐次除法*由左侧尺度 $s$ 表示，把 $(X_c,Y_c,Z_c)$ 归一化为 $(X_c/Z_c,Y_c/Z_c)$。在理想针孔链中，深度尺度在这一步无法由单个像素恢复。
- *内参块*把归一化坐标转换成像素坐标；在 $f_x,f_y$ 非零时可逆。

因此，单个理想像素保留了视线方向，却没有保留沿视线的尺度。各种从图像估计三维位置的方法都需要额外观测或先验，例如目标尺寸、双目视差、多帧运动或深度传感器。

实际计算时，通常显式写出相机坐标与像素坐标之间的*归一化平面*：$(x_n,y_n)=(X_c/Z_c, thin Y_c/Z_c)$，相当于焦距为 1 的虚拟像平面。这样可以把齐次除法与内参变换分开；下一节的常用畸变模型也定义在归一化坐标上，再由内参换算为像素。

=== 畸变：真实镜头对针孔的偏离

针孔模型假设光线通过一个理想小孔直线投影。真实相机使用透镜组汇聚光线，光线的偏折程度会随入射位置变化，因此图像会产生*畸变*（distortion）。常用模型将畸变分为*径向畸变*与*切向畸变*；桶形和枕形是径向畸变的两种表现，并不是与切向畸变并列的类别。

*径向畸变*源于透镜本身：放大率随离光轴的距离 $r$ 变化。放大率随 $r$ 递减，图像边缘向内收，直线外凸如木桶，是*桶形畸变*，广角镜头常见；放大率随 $r$ 递增，边缘向外拉，直线内凹如枕头，是*枕形畸变*，长焦端常见。*切向畸变*则源于装配：透镜组光轴与感光芯片不严格垂直（芯片贴装总有公差），图像整体产生一种不对称的偏斜。

畸变与 $r$ 的关系是复杂的非线性函数，工程上通常用低阶多项式近似。常用的 Brown 模型在归一化平面上描述理想点 $(x, y)$ 畸变后的位置，记 $r^2 = x^2 + y^2$：

$
x_d &= x (1 + k_1 r^2 + k_2 r^4 + k_3 r^6) + 2 p_1 x y + p_2 (r^2 + 2 x^2) \
y_d &= y (1 + k_1 r^2 + k_2 r^4 + k_3 r^6) + p_1 (r^2 + 2 y^2) + 2 p_2 x y
$

带 $k$ 的是径向项，带 $p$ 的是切向项。当一阶径向项在有效视场内占主导时，$k_1<0$ 常表现为桶形，$k_1>0$ 常表现为枕形；若高阶项较大，不能只凭 $k_1$ 的符号判断整幅图的形状。带畸变的完整投影链是：世界系 → 相机系 → 归一化平面 → *畸变* → 内参 → 像素。基础篇 Ceres 一节相机标定示例里的残差函数按这条链计算：先归一化、再应用畸变、最后乘 $f_x,f_y$ 并加上 $c_x,c_y$。

#figure(
  image("images/theory-camera-distortion.png", width: 100%),
  caption: [Brown 模型作用于规则归一化方格（灰）后的结果（蓝），系数经过夸大以便印刷观察。左：以负 $k_1$ 为主的桶形示例；中：以正 $k_1$ 为主的枕形示例；右：非零 $p_1,p_2$ 产生的不对称切向示例。三组模型在主点处位移为零，离主点较远时差异更明显；径向项与切向项的方向和阶次并不相同。],
)

低阶模型是否够用，首先取决于视场和残差。归一化半径 $r$ 是离轴角正切；半对角视场小于 45° 时，理想针孔模型中 $r<1$。以 1440×1080 且 $f_x approx f_y approx 1739$ 的相机为例，从主点位于图像中心近似计算，最远角点约有 $r approx 0.52$，径向高次项分别带有 $r^4,r^6$。广角或鱼眼镜头中的 $r$ 可能更大，Brown 低阶模型也可能不再合适，需要比较面向大视场的模型。另一方面，畸变系数由有限标定数据估计；增加参数会提高表达能力，也可能降低可辨识性或拟合噪声。因此，可以从 $k_1,k_2,p_1,p_2$ 等较低阶配置开始，再根据覆盖全视场的残差、留出图像和下游误差决定是否加入 $k_3$ 或更换模型，而不能只按镜头类别预先断定。

为了看数量级，只保留 $k_1=-0.1$ 并取 $r approx 0.52$ 时，径向缩放相对 1 偏离约 $|k_1|r^2 approx 2.7%$，对应的径向位移约为 24 像素。对这个仅含 $k_1$ 的例子，归一化位移从 $r^3$ 项开始；完整 Brown 模型中的切向位移则含二阶项，不能统一概括为 $r^3$。角点是否需要显式去畸变，应由有效视场内的位移和下游误差预算判断。若只需要少量角点，可以把原始点与畸变系数直接交给 `solvePnP`，也可以用 OpenCV 的 `undistortPoints` 得到一致坐标后再求解；后一种做法通常比逐帧重采样整幅图像开销小，但必须正确匹配输出坐标所用的相机矩阵，且不能重复去畸变。

=== 相机标定：参数从哪里来

$bold(K)$ 的四个数加畸变的四五个数，从哪来？查数据手册不行：手册给的是标称焦距和像元尺寸，但对焦位置会改变有效焦距，装配公差决定了主点，镜头个体差异决定了畸变——这些只能*测*。用数据估计相机参数的过程就是*标定*（calibration）。

标定可以看作投影过程的逆问题：参数全部已知时，可以计算任意三维点对应的像素位置；反过来，给定一批*三维坐标已知*的点及其像素观测，就能估计模型参数。常用的棋盘格正好提供了这样的点。以左上角点为原点、板面为 $Z = 0$ 平面，格子边长 $a$ 已知，则第 $i$ 行第 $j$ 列的角点坐标就是 $(j a, i a, 0)$。一张棋盘格因此可以提供几十个位置已知的平面点。

张正友标定法利用了棋盘格的平面结构。棋盘上的点都满足 $Z=0$，代入投影链后，3×4 外参中与 $Z_w$ 相乘的一列消失，投影可以写成一个 3×3 的单应矩阵 $bold(H)$（homography）。$bold(H)$ 有 8 个自由度，至少 4 对非退化点可以求解，更多角点构成超定方程组。每张有效棋盘格图像可估计一组 $bold(H)$。它具有 $bold(K) thin (bold(r)_1 thin bold(r)_2 thin bold(t))$ 的结构，其中旋转矩阵前两列 $bold(r)_1,bold(r)_2$ 单位长度且相互垂直，因此每张图为内参提供两个约束。在理想、无退化且暂不考虑稳定性的线性初始化中，含 skew 的 5 参数模型至少需要三张视图；固定 $s=0$ 后约束计数可以降到两张。这个理论下限不是实用采集建议，也没有检查畸变可辨识性；实际应采集更多位置、倾角和尺度分布不同的有效视图，并用稳定性与留出数据验收。

用这套线性方法解出内参初值后，剩下的交给非线性最小二乘收尾：把内参、畸变系数、每张图的外参全部作为未知数，一起优化，目标函数就是*重投影误差*——用当前参数把已知三维角点投影回图像，与实际检测到的角点位置求差：

$
"RMS" = sqrt(1/N sum_(i=1)^N ((u_i - hat(u)_i)^2 + (v_i - hat(v)_i)^2))
$

其中 $(u_i,v_i)$ 是检测到的角点像素，$(hat(u)_i,hat(v)_i)$ 是模型预测的投影位置，$N$ 是全部图像中的角点总数。重投影误差定义在像平面上，单位是像素；它直接比较像素观测，却不会自动换算成特定距离处的毫米误差。残差大小仍可能随标定板距离、姿态、图像区域和角点质量变化。如果初次标定时只是想先有个排查刻度，可以拿 0.1～0.5 像素这个示例量级作对照；明显超过 1 像素时，优先检查棋盘平整度、角点检测和姿态覆盖。这里的数值没有可跨设备套用的统一来源或合格线，真正验收还应结合分辨率、镜头模型、逐视图与空间分布、参数稳定性、留出数据和下游误差预算。基础篇 Ceres 一节的相机标定示例实现了这一精化步骤，残差函数返回上式根号中的两个分量；实际采集与验收流程将在「实战技术篇」的相机标定与手眼标定一章介绍。

=== PnP：从像素反推位姿

到此为止，投影链一直是正着走：已知三维点和全部参数，求它落在哪个像素。PnP（Perspective-n-Point）把问题反过来：内参已经标定，已知 $n$ 个点的三维坐标及其像素观测，反求外参 $bold(R), bold(t)$。这里有个很实用的地方：这 $n$ 个点不必预先知道世界坐标，只要它们在某个刚体自身坐标系中的位置已知即可；解出的 $(bold(R), bold(t))$ 就表示这个刚体相对相机的位姿。对装甲板而言，四个角点在自身坐标系中的位置由物理尺寸决定，因此 PnP 正好可以直接估计装甲板在相机坐标系下的位姿。

位姿包含旋转和平移各 3 个自由度，共 6 个未知量。一对"三维点—像素"对应提供 2 个方程，因此最少需要 3 对点，这就是 P3P。不过方程是非线性的，3 对点并不保证唯一解。可以从几何约束的数量来理解：

- *1 对点*：只知道目标点在"光心与该像素连成的射线"上，深度任意——解是一条射线，无穷多。
- *2 对点*：两条射线，加上"两点间物理距离已知"这一条刚性约束。在第一条射线上任取一个深度，第二个点被限制在球面与射线的交点上——仍剩一个连续自由度，解是一族。
- *3 对点*：三条射线、三条两两距离约束，连续自由度被耗尽，只剩*有限多个*解——最多 4 个。

P3P 的经典解法可以用余弦定理理解。设三个已知点 $A,B,C$，光心 $O$ 到它们的距离（即三个未知深度）为 $s_1,s_2,s_3$。像素先按相机模型去畸变，再经 $bold(K)^(-1)$ 还原成视线方向，两两视线的夹角 $alpha,beta,gamma$ 便可计算；$A,B,C$ 两两之间的物理距离 $d_(A B),d_(B C),d_(A C)$ 则由物体模型给定。对三角形 $O A B$、$O B C$、$O A C$ 分别应用余弦定理：

$
s_1^2 + s_2^2 - 2 s_1 s_2 cos alpha &= d_(A B)^2 \
s_2^2 + s_3^2 - 2 s_2 s_3 cos beta &= d_(B C)^2 \
s_1^2 + s_3^2 - 2 s_1 s_3 cos gamma &= d_(A C)^2
$

三个二次方程约束三个未知深度。经典消元可得到一个一元四次多项式，因此一般位置下最多产生 4 组候选解。几何上，每个物点必须位于对应视线上，同时三点间距离必须等于已知边长；不同的正深度组合可能同时满足这些约束。解出深度后，三个非共线点在相机系中的坐标随之确定，再由物体系与相机系的三维点对应恢复 $(bold(R),bold(t))$。退化构型、重根和数值误差会影响实际候选数量与稳定性。

第 4 个对应点可以帮助候选排序：把它按各组候选位姿重投影，比较与观测的误差。噪声、近退化构型或对称目标下仍可能有多个相近候选，因此还可结合正深度、全部点误差和时序先验判断。OpenCV 的 `SOLVEPNP_P3P` 接口要求 4 对点，前三对用于生成候选，第 4 对用于消歧。装甲板恰好有 4 个角点；对于这种共面目标，还可以使用平面专用方法。

点更多时可以使用其他方法。EPnP（Efficient Perspective-n-Point Camera Pose Estimation，高效 PnP 相机位姿估计）把 $n$ 个点表示为 4 个虚拟控制点的加权组合，将核心计算化为线性复杂度的形式，常用于多点初值。IPPE（Infinitesimal Plane-Based Pose Estimation，无穷小平面位姿估计）则针对*所有点共面*的目标，利用平面结构返回按重投影误差排序的两个候选位姿；平面目标在单视角和噪声下可能出现姿态歧义。OpenCV 的 `solvePnP` 只返回一个结果；若要检查两个 IPPE 候选并结合上一帧姿态等先验消歧，应使用 `solvePnPGeneric`。初值之后还可做迭代精化，以全部点的重投影误差为目标；基础篇 Ceres 一节的 PnP 示例实现的就是这一步。具体方法支持的点数、共面条件和返回值应按所用 OpenCV 版本核对。

=== 装甲板 PnP：自瞄的第一段三维

把上面的一般方法落到装甲板上，PnP 需要两组一一对应的输入，逐项看清楚。

*2D 侧：像素角点。*四个角点取两根灯条的上下端点，来源本篇前两章都讲过：传统路线从灯条的旋转矩形几何中提取端点（「传统视觉算法」一章），神经网络路线直接回归四个角点坐标（「深度学习和神经网络」一章）。畸变有两种等价处理方式：把原始像素和畸变系数一起传给 `solvePnP`，或者先用 `undistortPoints` 得到无畸变点，再按相应的相机矩阵或归一化坐标调用求解器；不要重复去畸变。

*3D 侧：物体系坐标。*在装甲板上定义坐标系：原点取两灯条中心连线的中点（即装甲板中心），$x$ 轴沿两灯条连线方向，$y$ 轴沿灯条方向，$z$ 轴垂直板面。四个角点的坐标由两个物理量完全确定——两灯条中心距 $W$ 和灯条长度 $H$：

$
(plus.minus W / 2, quad plus.minus H / 2, quad 0)
$

小装甲板 $W approx 135 "mm"$，大装甲板 $W approx 225 tilde 230 "mm"$（社区实现多取 225），灯条长 $H approx 55 "mm"$（具体数值以当季规则手册和实车测量为准）。注意 $z$ 坐标全为 0：四点共面，因此可以采用 IPPE；若要显式比较两个候选，应按前文使用 `solvePnPGeneric`。

将两组输入传给 `solvePnP`，可以得到 $(bold(R),bold(t))$。若物体系原点定义在装甲板中心，平移 $bold(t)$ 就是该中心在相机系下的坐标；物体点使用米时，$bold(t)$ 也以米为单位。旋转 $bold(R)$ 表示装甲板平面的朝向，其中 yaw 可作为整车建模的观测之一。装甲板相对车体的安装倾角会反映在板面位姿中，所以求装甲板自身位姿时不必先把它“校平”；若要从板面位姿推算车体中心或车体系姿态，仍需在车体几何模型中显式加入板面安装角与原点偏移。相关时序模型将在「卡尔曼滤波」一章与应用篇介绍。

实现时有两个很容易让结果整体跑偏的问题，必须单独检查：

- *角点顺序错位。*PnP 按"第 $i$ 个物体点对应第 $i$ 个像素点"建立方程，顺序是接口约定的一部分。应规定固定顺序（例如从左灯条顶端起逆时针），并让 2D 检测端与 3D 模型端保持一致。顺序错位会得到完全错误的位姿，而不只是增加少量误差。
- *板型判错。*「传统视觉算法」一章介绍过用归一化灯条间距判别大小板。在下一节的正对相机、只按宽度测距的简化模型中，$Z=f_x W\/w$，把 135 mm 代成 230 mm 会使深度尺度变为约 1.7 倍，5 m 对应约 8.5 m。实际四点 PnP 同时使用宽、高和姿态，大小板模型也不是各坐标的完全等比例缩放，因此具体偏差不必严格等于 1.7；但错误物理尺寸会产生系统性尺度偏差，应通过重投影、板型状态和距离合理性共同检查。

至此，我们得到了*装甲板在相机坐标系下的位姿*。相机固定在云台上，它的坐标系会随云台一起转动；若运动模型没有显式补偿这部分运动，直接在相机系中预测就会混入云台转动的影响。下一章「坐标变换」将把位姿转换到惯性坐标系，之后再由「卡尔曼滤波」一章处理时序估计。

=== 单目深度的敏感性：一个像素对应多少厘米

PnP 一次给出 6 个位姿参数，但它们的可观测性和不确定度随目标姿态、尺寸与成像位置变化。远距离平面目标的光轴深度通常对角点误差较敏感。下面用正对相机的尺寸测距模型估算量级；它不是任意姿态下四点 PnP 的完整协方差分析。

用一个足够说明问题的简化模型：装甲板正对相机，宽 $W$，距离 $Z$，由"距离 $Z$ 处一米宽占 $f_x \/ Z$ 像素"的读法，它在图上宽

$
w = (f_x W) / Z "像素"
$

代入真实数字（$f_x = 1739$，小板 $W = 135 "mm"$）：2 米处 117 像素，5 米处只剩 47 像素，8 米处 29 像素。反解深度：

$
Z = (f_x W) / w
$

这一行给出了单目尺寸测距的基本关系：深度等于已知物理尺寸除以像素尺寸，再乘以焦距完成单位换算。为了分析它对像素测量误差的敏感程度，对 $w$ 求导：

$
abs((dif Z) / (dif w)) = (f_x W) / w^2 = Z^2 / (f_x W)
$

导数中的 $Z^2$ 表示：在这组固定 $f_x,W$ 的简化条件下，图像宽度的小误差对应的深度一阶变化量随距离平方增长。代入数字，宽度测量每变化 1 像素，局部线性化得到 2 m 处约 1.7 cm、5 m 处约 10.6 cm、8 m 处约 27.3 cm。作为对照，在深度已知时，图像位置沿 $u$ 方向变化 1 像素对应的横向位移为 $Z\/f_x$，5 m 处约 2.9 mm。两者一个是目标*宽度*误差，一个是目标*位置*误差，不能当作同一随机变量；它们的一阶灵敏度之比在该模型下为 $Z\/W approx 37$（5 m、小装甲板）。

#figure(
  image("images/theory-camera-depth.png", width: 100%),
  caption: [正对相机的尺寸测距模型中，图像宽度对深度的一阶灵敏度 $abs(dif Z\/dif w)=Z^2\/(f_x W)$（$f_x=1739$ 像素；脚本 `examples/theory/camera_depth_sensitivity.py`）。蓝线使用小装甲板 $W=135$ mm，品红线使用大装甲板 $W=230$ mm；灰色虚线为深度已知时图像位置每变化 1 像素对应的横向位移 $Z\/f_x$，只用于量级对照。实线是局部导数，不是任意角点噪声下 PnP 误差的严格界限。],
)

这项平方关系来自上述投影与尺寸测距模型，而不是某个求解器的代码实现。目标越远，在图像中占的像素越少，同样的绝对宽度误差所占比例越大。一般 PnP 还受目标倾角、角点相关性、畸变、内参和求解器条件数影响，因此工程上应把这条关系当作量级估算，再用目标姿态与真实噪声做仿真或实测。可以得到几项有边界的结论：

- *亚像素角点有明确价值。*若其他误差不变，并把图像宽度误差的标准差从 1 像素降到 0.1 像素，线性化模型在 5 m 处给出的深度标准差量级会从约 10.6 cm 降到约 1.1 cm。实际收益还受系统偏差、两端角点误差相关性和姿态影响，需要用标注或固定靶测试确认。
- *远距离测量更敏感。*在 5 到 8 m，图像宽度变化一两个像素已对应十几到几十厘米的一阶深度变化。排查远距离波动时，可以先与该量级比较，再分别检查检测、标定、板型、时间同步和运动模型；符合平方量级并不能单独证明噪声来自角点。
- *姿态角也会遇到弱可观方向。*装甲板接近正对相机时，小幅 yaw 变化引起的角点位移可能较小，噪声会使候选姿态接近。具体敏感性取决于目标姿态和点分布，应检查两个 IPPE 候选、重投影误差和时序连续性，而不是把所有 yaw 波动归因于同一机制。
- *连续观测可以降低部分随机不确定度。*提高分辨率或使用长焦镜头可以增大 $f_x$，但通常会缩小视场。时序滤波可以在运动模型合理、时间戳正确且噪声近似满足假设时融合多帧，并加入“四块板绕同一中心旋转”等结构约束；它不能自动消除错误板型、内参偏差或固定时间偏差等系统误差。

==== 本章小结

本章从正向投影讲到位姿反解。外参把世界点变换到相机系，齐次除法保留视线方向而去掉沿视线的尺度，内参再把归一化坐标换成像素坐标。真实镜头还会在归一化平面上产生畸变；模型阶数应由视场覆盖、残差和下游误差选择。棋盘格标定利用多视图单应获得内参初值，再以像素域重投影误差做非线性精化。PnP 借助已知三维点与像素对应估计物体位姿；P3P 可能产生至多 4 组候选，共面装甲板可以使用 IPPE 并检查两个候选。简化尺寸模型还说明远距离深度对图像宽度误差呈平方级灵敏度，但一般 PnP 的不确定度仍取决于姿态、点分布和其他误差源。下一章将把相机系位姿转换到更适合预测的参考系，再由卡尔曼滤波处理连续观测。
