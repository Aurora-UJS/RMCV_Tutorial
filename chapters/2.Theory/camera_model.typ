#import "/template/template.typ": *

基础篇 OpenCV 一节的结尾留过一个承诺：识别出装甲板只是第一步，接下来要"用两灯条的像素几何加相机内参解算三维位姿"，那是本篇相机模型与坐标变换两章的主场。现在开场。识别流水线交出来的是几个像素坐标，而火控需要的是目标在三维空间里的位置——中间隔着一台相机。相机是一台"降维机器"：它把三维世界压成二维图像，压缩过程有一套干净的几何。本章先把这套几何一块一块讲清（针孔模型、内参、投影链、畸变），再讲参数从哪里来（标定），最后逆着整条链走回去——从四个角点把装甲板拉回三维（PnP），并诚实地回答：拉回来的三维，哪个方向可信、哪个方向脆。

#definition()[
#set list(indent: 2em)
#{
  set par(first-line-indent: 0pt)
  [本章会反复用到四个坐标系，先立好牌子：]
}

- *世界坐标系* $(X_w, Y_w, Z_w)$：场地或机器人上选定的参考系，单位米
- *相机坐标系* $(X_c, Y_c, Z_c)$：原点在相机光心，$z$ 轴沿光轴朝外，单位米
- *图像坐标系* $(x, y)$：像平面上以主点（光轴与像平面交点）为原点，单位毫米
- *像素坐标系* $(u, v)$：原点在图像左上角，$u$ 向右、$v$ 向下，单位像素
]

=== 针孔相机：把三维世界压成二维

先问一个比"相机怎么成像"更早的问题：为什么随便拿一块感光芯片对着世界，得不到图像？因为物体上*每一个点*发出的光会打到芯片上*每一个位置*——所有点的光叠在一起，芯片上只有一片均匀的糊。成像的本质不是接收光，而是*筛选*光：让芯片上每个位置只接收来自一个方向的光。针孔就是最朴素的筛子——在暗箱上开一个小孔，物体上每个点只有沿"该点—小孔"直线传播的那一束光能进来，一一对应建立了，图像就出现了。孔越小图像越锐，但进光也越少；真实相机用透镜代替小孔，既开大了"孔径"又把光聚回一点——代价后面讲畸变时再算。

小孔成的是倒像：上方的点投到像平面下方。数学上处理倒像要一直背着负号，所以约定俗成的做法是把像平面*镜像翻到光心前方*一个焦距 $f$ 处，得到一个正立的"虚拟像平面"——几何关系完全等价，只是干净了。后文所有推导都在这个前置像平面上进行。

#figure(
  image("images/theory-camera-pinhole.png", width: 100%),
  caption: [针孔模型的侧视图。品红点 $P$ 是空间点，它与光心 $O$ 的连线穿过前置虚拟像平面（蓝色，位于 $z = f$ 处），交点就是像点 $p$；灰色虚线是真实的倒立像平面，与前置平面镜像等价。读图抓一件事：品红大三角形与蓝色小三角形相似，所以 $y\/f = Y\/Z$——整个针孔模型就是这一对相似三角形。],
)

侧视图里能看到两个共享顶点 $O$ 的相似三角形：大的以 $Z$ 为底、$Y$ 为高，小的以 $f$ 为底、$y$ 为高。相似三角形对应边成比例：

$
Y / Z = y / f
quad => quad
y = f dot Y / Z
$

水平方向同理，$x = f dot X \/ Z$。这两行就是针孔模型的全部：

$
x = f X / Z, quad y = f Y / Z
$

别看它短，两条重要性质都在里面。第一，*远小近大有了精确的数学形式*——像的尺寸反比于距离 $Z$，一台车开远一倍，像上就缩小一半。第二，也是本章的病根：*深度被除掉了*。$(X, Y, Z)$ 三个数进去，$(x, y)$ 两个数出来，$Z$ 只以分母的身份留下一点痕迹。把 $(X, Y, Z)$ 整体乘以 2，投影结果一个像素都不变——单看一张图，你永远无法区分"近处的小目标"和"远处的大目标"。后面 PnP 一节能从图像里解出深度，靠的不是投影本身，而是偷偷塞进去的额外信息：目标的物理尺寸已知。

还有一处数学上的别扭要现在处理掉。"除以 $Z$"不是线性运算，没法写成矩阵乘法——而我们马上要把一串变换连成链，链上每一环都想用矩阵。标准解法是*齐次坐标*：给向量补一个维度，规定整体乘以非零常数不改变它代表的点。于是投影可以写成：

$
Z vec(x, y, 1) = mat(f, 0, 0; 0, f, 0; 0, 0, 1) vec(X, Y, Z)
$

右边是纯粹的矩阵乘法，非线性的除法被藏进了左边的系数 $Z$ 里——算完矩阵乘法后，把结果的第三个分量归一成 1（即除以 $Z$），前两个分量就是像坐标。这一步"最后再除"的操作叫齐次除法，整条投影链里唯一的非线性就它一个。

=== 内参矩阵：从毫米到像素的翻译官

上一节的 $(x, y)$ 还活在物理世界里：单位是毫米，原点在主点。但相机通过 USB 线交给你的是一个像素阵列，原点在左上角，坐标只能是第几行第几列。从图像坐标到像素坐标要做两件事：*换单位*和*搬原点*。

换单位：感光芯片由一格一格的像元铺成，设每个像元宽 $d_x$ 毫米、高 $d_y$ 毫米，那么 $x$ 毫米对应 $x \/ d_x$ 个像素。搬原点：主点大约在图像中心，设它的像素坐标为 $(c_x, c_y)$，加上去即可：

$
u = x / d_x + c_x, quad v = y / d_y + c_y
$

把针孔投影代入，两步合成一步：

$
u = underbrace(f / d_x, f_x) dot X_c / Z_c + c_x, quad
v = underbrace(f / d_y, f_y) dot Y_c / Z_c + c_y
$

这里诞生了整个视觉几何里最常用的两个参数 $f_x, f_y$。它们是物理焦距除以像元尺寸——*用"像元的个数"来度量的焦距*，所以单位是像素。这不是抽象定义，拿真实器件算一遍——本章起换一台更典型的工业相机当例子（分辨率 1440×1080，不再是前几章的 1280×1024）：一支 6 mm 定焦镜头配 3.45 µm 像元的工业相机，$f_x = 6 "mm" \/ 3.45 "µm" approx 1739$ 像素。这个数有直白的物理读法：*距离 $Z$ 处一米宽的东西，在图上占 $f_x \/ Z$ 个像素*——2 米外一米宽的车体，占 $1739 \/ 2 approx 870$ 像素。后文算深度误差全靠这条读法。

写成矩阵，就是内参矩阵（camera matrix）：

$
Z_c vec(u, v, 1) = underbrace(mat(f_x, s, c_x; 0, f_y, c_y; 0, 0, 1), bold(K)) vec(X_c, Y_c, Z_c)
$

逐个元素过一遍：

- $f_x, f_y$：像素单位的焦距。为什么要分开两个？历史上像元不一定是正方形，$d_x eq.not d_y$；现代工业相机像元基本正方，标出来的 $f_x, f_y$ 通常只差千分之几，但标定时仍分开估计，让数据自己说话。
- $c_x, c_y$：主点位置。理想值是图像几何中心（如 1440×1080 的图约为 (720, 540)），但镜头和芯片的装配做不到光轴严格穿过芯片中心，实测往往偏离中心若干像素——所以它也要标定，不能想当然用图像中心。
- $s$：倾斜因子（skew），描述像素阵列两轴不垂直的程度。现代 CMOS 制造工艺下它可以放心取 0，OpenCV 的标定模型干脆不设这个参数。

$bold(K)$ 是一个上三角矩阵，且可逆。可逆这件事很关键：给定一个像素 $(u, v)$，算 $bold(K)^(-1) (u, v, 1)^top$ 就能还原出它对应的视线方向——这是后面 PnP 的第一步。

=== 完整投影链：世界点到像素的三步旅程

针孔模型的输入是相机坐标系下的点，但目标点原本活在世界坐标系里（或者活在"装甲板自己的坐标系"里——后面会看到这正是 PnP 的用法）。两个坐标系之间差一个刚体变换：先旋转、再平移：

$
vec(X_c, Y_c, Z_c) = bold(R) vec(X_w, Y_w, Z_w) + bold(t)
$

$bold(R)$ 是一个 3×3 的旋转矩阵。它凭什么用 9 个数表示 3 个自由度的旋转、有哪些更好的表示法、什么时候会咬人——这些是下一章「坐标变换」的主角，本章只把它当"某个旋转"用。$bold(R)$ 和 $bold(t)$ 合称*外参*（extrinsics）：它们描述相机在世界中的位姿，相机一动它们就变；而 $bold(K)$ 和畸变系数是*内参*（intrinsics）：它们长在相机身上，镜头不换、对焦不动就不变。

借助齐次坐标，把平移也收进矩阵乘法，整条链一气呵成：

$
s vec(u, v, 1) =
underbrace(bold(K), "内参") thick
underbrace(mat(bold(R), bold(t)), "外参") thick
vec(X_w, Y_w, Z_w, 1)
$

其中 $mat(bold(R), bold(t))$ 是把 $bold(R)$ 与 $bold(t)$ 并排拼成的 3×4 矩阵，$s = Z_c$ 是齐次除法要除掉的深度。这一行公式值得逐块读：

- *外参块*把点从世界系搬进相机系。刚体变换，可逆，不丢任何信息。
- *齐次除法*（藏在左边的 $s$ 里）把三维压到二维。整条链唯一的非线性，也是唯一*不可逆*的一步——深度在这里丢失。
- *内参块*把"以焦距为单位的方向"翻译成"第几行第几列"。上三角矩阵，可逆，也不丢信息。

三步里两步可逆、一步不可逆，所以整条链的信息账本很清楚：*从三维到二维丢掉的，恰好是一个数——深度*。一切"从图像恢复三维"的方法，本质上都是在用别的信息把这个数补回来。

实际计算时，习惯在压缩和贴标签之间再设一站：把相机系坐标除以 $Z_c$，得到 $(x_n, y_n) = (X_c \/ Z_c, thin Y_c \/ Z_c)$，称为*归一化平面*坐标——相当于焦距为 1 的虚拟相机的像平面。专设这一站有两个理由：一是它把唯一的非线性单独拎出来，前后各环节都是干净的线性运算；二是下一节的畸变模型就定义在这块平面上——畸变是镜头的光学行为，与芯片上有多少像素无关，自然要在"贴像素标尺之前"描述。

=== 畸变：真实镜头对针孔的偏离

针孔模型有一条隐含承诺：光沿直线穿过一个点。真实相机为了进光量用透镜组代替了针孔，透镜一来，承诺就打了折扣——光线的偏折程度随入射位置变化，图像便产生*畸变*（distortion）。畸变分两大类：*径向畸变*与*切向畸变*；常听说的桶形、枕形都是径向畸变的两种表现，不是与切向并列的类别。

*径向畸变*源于透镜本身：放大率随离光轴的距离 $r$ 变化。放大率随 $r$ 递减，图像边缘向内收，直线外凸如木桶，是*桶形畸变*，广角镜头常见；放大率随 $r$ 递增，边缘向外拉，直线内凹如枕头，是*枕形畸变*，长焦端常见。*切向畸变*则源于装配：透镜组光轴与感光芯片不严格垂直（芯片贴装总有公差），图像整体产生一种不对称的偏斜。

畸变与 $r$ 的真实关系是复杂的非线性函数，工程上的处理是老一套：泰勒展开取低阶项。通行的 Brown 模型在归一化平面上描述"理想点 $(x, y)$ 被畸变搬到哪里"，记 $r^2 = x^2 + y^2$：

$
x_d &= x (1 + k_1 r^2 + k_2 r^4 + k_3 r^6) + 2 p_1 x y + p_2 (r^2 + 2 x^2) \
y_d &= y (1 + k_1 r^2 + k_2 r^4 + k_3 r^6) + p_1 (r^2 + 2 y^2) + 2 p_2 x y
$

带 $k$ 的是径向项（$k_1 < 0$ 桶形、$k_1 > 0$ 枕形），带 $p$ 的是切向项。于是带畸变的完整投影链是：世界系 → 相机系 → 归一化平面 → *畸变* → 内参 → 像素。基础篇 Ceres 一节相机标定示例里的残差函数，就是严格按这条链写的：先归一化、再套畸变、最后乘 $f_x, f_y$ 加 $c_x, c_y$。

#figure(
  image("images/theory-camera-distortion.png", width: 100%),
  caption: [Brown 模型把规则方格网（灰）搬到的位置（蓝），系数经过夸大以便印刷可见。左：桶形（$k_1 < 0$），边缘向内收；中：枕形（$k_1 > 0$），边缘向外拉；右：切向（$p_1, p_2 eq.not 0$），整体不对称地偏斜。读图规则：看每幅图的*边缘*——三种畸变在中心附近都几乎不动，离中心越远偏离越大，这正是"低阶多项式乘 $r^2$"的形状。],
)

为什么敢只取低阶？两笔账。第一笔是量级：RM 自瞄常用的窄视场镜头下，归一化坐标里视场内 $r$ 不超过 1（$r$ 就是离轴角的正切，半对角视场小于 45° 就不超 1）——以 1440×1080、$f_x approx 1739$ 的相机为例，图像最角落 $r approx 0.52$——高次项按 $r^4, r^6$ 衰减，越高越无关紧要；反过来，广角/鱼眼镜头的 $r$ 会超过 1，低阶截断随之失效——这正是鱼眼要换一套畸变模型的原因。第二笔是数据：畸变系数是从几十张标定图里估出来的，参数越多越容易把标定数据里的噪声当成规律（「深度学习和神经网络」一章讲过的过拟合，在这里换了身衣服出现），所以 RM 常用的窄视场镜头往往只标 $k_1, k_2, p_1, p_2$ 四个，连 $k_3$ 都不要。

畸变到底多大？拿 $k_1 = -0.1$（普通定焦镜头的常见量级）算一笔：图像角落 $r approx 0.52$，径向缩放偏离 $|k_1| r^2 approx 2.7%$，换算成位移约 24 像素——足够让角落里的直线肉眼可见地弯掉。而在图像中心附近畸变几乎为零（位移按 $r^3$ 起步）。这给 RM 一个重要的工程推论：自瞄目标常在画面中部，畸变不大，但*不是零*——几个像素的角点偏移，经过下文的深度放大，就是厘米级的位置误差。好在自瞄根本不需要矫正整幅图像（每帧全图重采样，白白烧掉几毫秒），只需要对识别出的 4 个角点做去畸变（OpenCV 的 `undistortPoints`，对每个点迭代反解 Brown 模型），代价几乎为零。

=== 相机标定：参数从哪里来

$bold(K)$ 的四个数加畸变的四五个数，从哪来？查数据手册不行：手册给的是标称焦距和像元尺寸，但对焦位置会改变有效焦距，装配公差决定了主点，镜头个体差异决定了畸变——这些只能*测*。用数据估计相机参数的过程就是*标定*（calibration）。

标定的思路是把投影链倒过来用：如果参数全部已知，任何一个三维点投影到哪个像素是可以精确算出来的；那么反过来，找一批*三维坐标已知*的点，观察它们实际落在哪些像素上，就能反推参数。三维坐标已知的点从哪来？打印一张棋盘格。棋盘格的妙处在于它自带坐标系：以左上角点为原点、板面为 $Z = 0$ 平面，格子边长 $a$ 印刷时就定了，于是第 $i$ 行第 $j$ 列的角点坐标就是 $(j a, i a, 0)$——一张几块钱的打印纸，就这样提供了几十个位置已知的三维点。

张正友标定法的核心洞察正是从"平面板"里长出来的。棋盘所有点都在 $Z = 0$ 平面上，代入投影链，3×4 的外参块中与 $Z_w$ 相乘的那一列被消掉，整条链退化成一个 3×3 矩阵 $bold(H)$（称为单应，homography）——平面到平面的投影。$bold(H)$ 只有 8 个自由度，4 对点就能解出，几十个角点更是超定得稳稳当当；也就是说，*每拍一张棋盘，就能直接从角点算出这张图的 $bold(H)$*。而 $bold(H)$ 的结构是 $bold(K) thin (bold(r)_1 thin bold(r)_2 thin bold(t))$——其中 $bold(r)_1, bold(r)_2$ 是旋转矩阵的前两列，它们必须是单位长且互相垂直的（下一章会讲这正是旋转矩阵的本性）。这两条"必须"，就是每张图向内参贡献的两个约束方程。一张图两个约束，最少拍几张便钉在内参模型上：含 skew 的完整 5 参数模型至少要拍三张；像 OpenCV 那样固定 $s = 0$ 时理论最少两张——但*实际标定总要拍几十张、姿态铺开*：姿态雷同的图给出的约束近乎重复，这就是标定时要求你把棋盘举得东倒西歪、覆盖各个角度的数学原因。

用这套线性方法解出内参初值后，剩下的交给非线性最小二乘收尾：把内参、畸变系数、每张图的外参全部作为未知数，一起优化，目标函数就是*重投影误差*——用当前参数把已知三维角点投影回图像，与实际检测到的角点位置求差：

$
"RMS" = sqrt(1/N sum_(i=1)^N ((u_i - hat(u)_i)^2 + (v_i - hat(v)_i)^2))
$

其中 $(u_i, v_i)$ 是检测到的角点像素，$(hat(u)_i, hat(v)_i)$ 是模型投影出的预测位置，$N$ 是全部图片上的角点总数。注意量纲：*重投影误差活在像平面上，单位是像素*，不是毫米——它衡量的是"模型预测的像"与"实际的像"差几个像素，与目标离相机多远无关。用像素域做误差也是有意为之：像素是所有观测的原生量纲，若换算到三维空间反而要引入深度，把误差搅浑。工程经验上，一次合格的标定，RMS 重投影误差在 0.1 到 0.5 像素的量级；明显超过 1 像素就该怀疑棋盘不平、角点检测出错或姿态覆盖不足。基础篇 Ceres 一节的相机标定示例实现的正是这个精化步骤，残差函数返回的就是上式根号里的两个分量；至于实际拍多少张、怎么举板子、怎么验收，实操流程在「实战技术篇」相机标定一章。

=== PnP：从像素反推位姿

到此为止，投影链是正着走的：已知三维点和全部参数，算像素。PnP（Perspective-n-Point）把问题反过来：*内参已标定，已知 $n$ 个点的三维坐标和它们的像素观测，求外参* $bold(R), bold(t)$。这里有个容易被忽略的妙处：那 $n$ 个点的三维坐标不必是世界系坐标——只要在*某个你自己定义的刚体坐标系*里已知就行，解出的 $(bold(R), bold(t))$ 就是这个刚体相对相机的位姿。对装甲板而言：四个角点在"装甲板自己的坐标系"里的位置由物理尺寸决定，是先验已知的，于是 PnP 直接交出装甲板在相机系下的位姿——这正是自瞄要的东西。

位姿有多少未知数？旋转 3 个自由度加平移 3 个，共 6 个。一对"三维点—像素"对应给出 2 个方程（$u$ 一个、$v$ 一个），所以最少需要 3 对点——这就是 P3P。但方程是非线性的，3 对点不保证唯一解。解的个数随点数递减的过程很有几何味道，一步一步看：

- *1 对点*：只知道目标点在"光心与该像素连成的射线"上，深度任意——解是一条射线，无穷多。
- *2 对点*：两条射线，加上"两点间物理距离已知"这一条刚性约束。在第一条射线上任取一个深度，第二个点被限制在球面与射线的交点上——仍剩一个连续自由度，解是一族。
- *3 对点*：三条射线、三条两两距离约束，连续自由度被耗尽，只剩*有限多个*解——最多 4 个。

P3P 的经典解法把这个几何图景直接翻译成初中就认识的余弦定理。设三个已知点 $A, B, C$，光心 $O$ 到它们的距离（即三个未知深度）为 $s_1, s_2, s_3$。每个像素经 $bold(K)^(-1)$ 还原成一条视线方向，两两视线的夹角 $alpha, beta, gamma$ 就都可以从图像算出来；而 $A, B, C$ 两两之间的物理距离 $d_(A B), d_(B C), d_(A C)$ 是先验已知的。对三角形 $O A B$、$O B C$、$O A C$ 分别用余弦定理：

$
s_1^2 + s_2^2 - 2 s_1 s_2 cos alpha &= d_(A B)^2 \
s_2^2 + s_3^2 - 2 s_2 s_3 cos beta &= d_(B C)^2 \
s_1^2 + s_3^2 - 2 s_1 s_3 cos gamma &= d_(A C)^2
$

三个二次方程解三个未知深度。消元后化成一个一元四次方程，四次方程最多 4 个实根——这就是"最多 4 组解"的代数出处。它也有直观读法：三条视线张成一个从光心射出的无限延伸的三棱锥，求解 P3P 等于把边长已知的刚性三角形 $A B C$ 严丝合缝地卡进这个棱锥里，让三个顶点各自落在一条棱上——三角形可以以不同的倾斜姿态卡进去，但至多四种。解出深度后，三个点在相机系的坐标随之确定，$(bold(R), bold(t))$ 由两组三维点的对应直接恢复。

真解只有一个，剩下最多三个"数学上成立、物理上没发生"的假解怎么排除？*用第 4 个点投票*：对每组候选解，把第 4 个已知点重投影回图像，与它的实际观测比较，误差最小的那组当选。所以 P3P 实用上需要 4 对点——3 对解算、1 对消歧，OpenCV 的 `SOLVEPNP_P3P` 要求恰好传 4 个点就是这个原因。装甲板恰好有 4 个角点，刚刚够用；不过对装甲板还有更合身的解法，马上讲到。

点更多时有各自的招数，各记一句话即可：*EPnP* 把 $n$ 个点全部表示为 4 个虚拟控制点的加权组合，将问题化成线性方程组，复杂度 $O(n)$，是点多时的标准选择；*IPPE* 专为*所有点共面*的目标设计（装甲板正是），利用平面结构给出解析解，并按重投影误差排序返回两个候选位姿——平面目标从单视角看存在"绕板面翻折"的两义性，IPPE 把两种可能都摆到明面上让你选。不过 `solvePnP` 接口只交出重投影误差较小的那一个候选；想拿到两个候选自己消歧（比如用上一帧的 yaw 当先验），要用 `solvePnPGeneric`。无论哪种解法起步，工程上通常都再接一步迭代精化：以解出的位姿为初值，最小化这 $n$ 个点的重投影误差——基础篇 Ceres 一节的 PnP 示例写的就是这一步。

=== 装甲板 PnP：自瞄的第一段三维

把上面的一般理论落到装甲板上，就是自瞄链路里第一次出现"三维"的地方。PnP 需要两份输入，逐一清点。

*2D 侧：像素角点。*四个角点取两根灯条的上下端点，来源本篇前两章都讲过：传统路线从灯条的旋转矩形几何中提取端点（「传统视觉算法」一章），神经网络路线直接回归四个角点坐标（「深度学习和神经网络」一章）。别忘了先对这 4 个点做上一节说的去畸变。

*3D 侧：物体系坐标。*在装甲板上定义坐标系：原点取两灯条中心连线的中点（即装甲板中心），$x$ 轴沿两灯条连线方向，$y$ 轴沿灯条方向，$z$ 轴垂直板面。四个角点的坐标由两个物理量完全确定——两灯条中心距 $W$ 和灯条长度 $H$：

$
(plus.minus W / 2, quad plus.minus H / 2, quad 0)
$

小装甲板 $W approx 135 "mm"$，大装甲板 $W approx 225 tilde 230 "mm"$（社区实现多取 225），灯条长 $H approx 55 "mm"$（具体数值以当季规则手册和实车测量为准）。注意 $z$ 坐标全为 0：四点共面，所以求解器选 IPPE 正合适。

两份输入喂给 `solvePnP`，输出 $(bold(R), bold(t))$，读法如下。*平移 $bold(t)$ 就是装甲板中心在相机系下的三维坐标*——这就是火控要瞄的那个点，物体点用米作单位，$bold(t)$ 出来就是米。*旋转 $bold(R)$ 是装甲板的朝向*，其中当下最值钱的分量是板面绕竖直方向的偏转角（yaw）：对手小陀螺时，正是靠每块板的 yaw 和位置推算整车旋转中心与半径——那是整车建模的故事（「卡尔曼滤波」一章与应用篇）。顺带一提，官方装甲板带约 15 度的安装倾角，你不需要为它做任何特殊处理，它会自动体现在解出的 $bold(R)$ 里。

两个能让整套系统当场翻车的工程死穴，必须点名：

- *角点顺序错位。*PnP 靠"第 $i$ 个物体点对应第 $i$ 个像素点"建立方程，顺序是接口契约的一部分。约定一个固定顺序（比如从左灯条顶端起逆时针），2D 检测端和 3D 模型端严格一致；一旦错位，解出的位姿不是差一点，而是完全离谱。
- *板型判错。*「传统视觉算法」一章讲过用灯条间距归一化判别大小板，判错的代价现在可以算出来了：深度正比于喂进去的物理宽度（下一节马上推导 $Z = f_x W \/ w$），把 135 mm 的小板当成 230 mm 的大板，深度整体放大 1.7 倍——5 米的目标被解算到 8.5 米，弹道直接打飞。

本章的输出到此交付：*装甲板在相机系下的位姿*。但相机系是一个焊在云台上、随云台疯转的坐标系，预测和弹道没法在里面做——把位姿搬进世界系是下一章「坐标变换」的开场白；搬进世界系之后对它做时序滤波，是「卡尔曼滤波」一章的正题。

=== 单目深度的病态性：一个像素值多少厘米

PnP 交出 6 个数，但这 6 个数不是同等可信的。收尾之前，必须诚实地量一量最脆的那个方向：深度。

用一个足够说明问题的简化模型：装甲板正对相机，宽 $W$，距离 $Z$，由"距离 $Z$ 处一米宽占 $f_x \/ Z$ 像素"的读法，它在图上宽

$
w = (f_x W) / Z "像素"
$

代入真实数字（$f_x = 1739$，小板 $W = 135 "mm"$）：2 米处 117 像素，5 米处只剩 47 像素，8 米处 29 像素。反解深度：

$
Z = (f_x W) / w
$

这一行就是单目测距的全部原理：*深度 = 已知物理尺寸 ÷ 像素尺寸*（乘上焦距换算单位）。它对像素测量误差有多敏感？对 $w$ 求导：

$
abs((dif Z) / (dif w)) = (f_x W) / w^2 = Z^2 / (f_x W)
$

关键在分子上的 $Z^2$：*每个像素的测量误差换算成深度误差，随距离平方增长*。代入数字——2 米处每像素 1.7 cm，5 米处 10.6 cm，8 米处 27.3 cm。作为对照，横向（图像 $u$ 方向）的误差换算是 $Z \/ f_x$，5 米处每像素才 2.9 mm，随距离只线性增长。同样一个像素的角点误差，深度方向的代价是横向的 $Z \/ W approx 37$ 倍（5 米处）。

#figure(
  image("images/theory-camera-depth.png", width: 100%),
  caption: [一个像素的宽度测量误差换算成深度误差，随距离的变化（$f_x = 1739$ 像素）。蓝线为小装甲板（$W = 135 "mm"$）、品红线为大装甲板（$W = 230 "mm"$），灰色虚线是横向误差 $Z \/ f_x$ 作对照。读图规则：两条实线按 $Z^2$ 弯曲上翘、灰线几乎贴地——深度是单目测量中唯一病态的方向；大板曲线低于小板，因为目标物理尺寸越大、同样的像素误差摊得越薄。],
)

这不是实现瑕疵，是几何本性。回到本章开头：投影把深度除掉了，PnP 找回深度的唯一凭据是"物理尺寸 ÷ 像素尺寸"这个*比值*，比值的相对误差直接成为深度的相对误差；目标越远、像素越少，同样的绝对像素误差占比越大——两个"越"相乘，就是平方律。这个账算清楚之后，几件工程事实立刻顺理成章：

- *亚像素角点精度是值得付出的。*把角点精度从 1 像素做到 0.1 像素，5 米处的深度不确定度就从 10 厘米量级压到 1 厘米量级——「传统视觉算法」一章的亚像素精化和「深度学习和神经网络」一章的关键点回归，在这里兑现价值。
- *远距离位姿抖动是常态而非 bug。*角点检测在相邻帧之间抖动一两个像素稀松平常，按上面的换算，5 到 8 米外深度就要抖十几到几十厘米。看到测距曲线远距离发毛，先想到的应该是这条平方律，而不是"代码写错了"。
- *同样的病根还坑姿态角。*装甲板近乎正对相机时，板面绕竖直轴偏转几度，四个角点的像素位置几乎不动——观测对 yaw 不敏感，解算出的 yaw 就抖得厉害。偏偏整车建模最依赖 yaw，这是它必须靠滤波稳住的原因之一。
- *出路不在单帧里。*提高分辨率或换长焦能把 $f_x$ 做大，但以视场为代价，是 trade-off 不是免费午餐；真正的出路是承认单帧深度就是脆的，把测量交给时间——用「卡尔曼滤波」一章的滤波器在连续帧上积累证据，用整车建模给观测加上"四块板绕同一中心旋转"的结构约束。单帧几何算出"在哪"，时序滤波负责"信多少"。

==== 本章小结

把本章倒着串一遍，脉络自然浮现。相机把三维世界压成二维：外参搬坐标系（可逆）、齐次除法压深度（不可逆）、内参贴像素标尺（可逆）——整条链只丢一个数，深度。真实镜头在归一化平面上叠加 Brown 畸变，低阶径向加切向足以描述。这些参数靠标定获得：棋盘格提供已知平面点，每张图的单应给内参两条约束，最后以像素域 RMS 重投影误差做非线性精化。PnP 把链条倒过来走：用目标的已知物理尺寸补回被除掉的深度，P3P 的余弦定理给出至多 4 解、第 4 点消歧，装甲板四个共面角点配 IPPE 解出相机系位姿。而找回的深度天生病态——误差随 $Z^2$ 增长，这决定了自瞄不能只靠单帧几何。位姿已经在手，下一章把它从疯转的相机系搬进安稳的世界系，「卡尔曼滤波」一章再教它在时间里站稳。
