#import "/template/template.typ": *

=== 一个名字，两种写法

先把名字对上。本章标题写的「同济自喵」是社区里的俗称，本书前面几篇一路提到的「同济 SuperPower 战队」与它的开源框架 sp_vision，说的是同一个东西：同济大学 SuperPower 战队公开的 25 赛季自瞄代码，仓库 `sp_vision_25`，MIT 许可证，Copyright (c) 2025 TongjiSuperPower。「自瞄算法设计」一章里那套*轨迹视角自瞄理论*、那个 39.6% 的命中率、那句"一个相机只能被一个进程打开"，源头都在这里。此后本章统一用 sp_vision 指代。

它和前两章的解剖对象有三处不一样，值得先摆出来：

- *它是一支战队的东西，不是一个人的作品。* readme 末尾列了七个人的名字。这意味着代码里能看到"多人协作"的痕迹——好的坏的都有：分层清晰、工具函数齐备，同时也有新旧两套通信实现共存、同一个默认值散落三处。
- *它还活着。* 截至本书写作时（2026 年 7 月），仓库最后一次推送在 2026-07-15，514 星 136 fork。对比 `rm_auto_aim` 算法本体停在 2023-05-19，你读 sp_vision 时读的是活代码，提问还有人答。
- *它是三个里唯一给出赛场量化战绩的。* 这一点最要紧，下面单说。

readme 第 2 节给了一组数字：25 赛季国赛，搭载该算法的 3、4 号步兵*最高命中率 39.6%，发挥正常时不低于 30%*；击杀时间方面，面对 2 m 处 300 血的敌方步兵，7 rad/s 转速下约 8 s，14 rad/s 转速下约 10 s。

引用这组数字时，*那三个前提一个都不能丢*——2 m、300 血、转速。脱离距离和目标运动状态谈击杀时间没有意义，作者自己在文中就强调过"测量时需要控制敌方血量、距离和运动状态等变量"。这不是学究气：转速从 7 翻到 14，击杀时间只涨了约四分之一，这个"涨得不多"本身就是结论（云台加速度天花板还没被打穿），而它只在同一距离、同一血量下才成立。

同样要小心的是演进史。readme 说前身 `sp_vision_23` 用 Python 写成，改用 C++ 重写的理由是"因其运行效率不佳（*在导航运行时*，帧率仅 60FPS）"——那个 60 FPS 是*导航程序同时在跑的条件下*测得的，不是 Python 版的裸帧率。而 24 赛季 C++ 重写后"帧率约 100FPS"，原文没有标注同样的条件。所以严格说，这两个数字不能直接相减，写成"Python 只有 60 帧、C++ 涨到 100 帧"就是丢掉限定语的误述。能安全说的只有：重写后帧率显著提升，且 24 赛季补上了参数文件加载、日志、离线检测与重启、类 rosbag 录制（时间戳＋视频＋四元数）和各模块独立测试程序。

这个小陷阱值得记住，因为它在读开源项目时反复出现：一个性能数字的价值，全在它的测试条件里。条件没写，数字就只是个传说。

=== 架构：一个 main 撑起所有兵种

==== 为什么不能"一功能一程序"

sp_vision 的架构决策里，最朴素也最有说服力的一条写在 readme 的软件架构一节。步兵既要自瞄又要打符，很自然的想法是写两个程序、各跑各的。作者给出的否决理由只有一句：

#blockquote[
  例如步兵需要自瞄和打符，显然，这两个功能都需要从相机获取图像，但是相机只能被一个程序打开，这会导致另一个程序无法正常工作（相机被占用）。
]

一句硬件事实，把一整类架构方案直接砍掉了。工业相机的 SDK 独占设备句柄，第二个进程去打开只会失败——这不是设计品味问题，是物理约束（「工业相机」一章讲过由此长出的"一相机一进程"架构）。于是架构被反推出来：功能拆成"功能组"（一组类和函数），程序只有一个。`main` 负责取图，再根据下位机发来的档位信号选择执行哪个功能组；不同兵种就是不同的 `main`。

这条推理链值得单独拎出来欣赏：从一个不可谈判的约束出发，反推出唯一可行的软件结构。比"我们选择了模块化架构"这种话有价值得多。你读任何一个陌生工程时，都该找一找这样的句子——它们是整个架构的地基，找到了，剩下的设计就都讲得通了。

==== 六层目录与一个入口

仓库的分层一眼能看懂：

- `io/`：硬件抽象层。相机（海康、迈德威视、USB）、下位机（CAN 与串口两套）、IMU、ROS 接口各一个子目录。
- `tasks/`：功能层。`auto_aim/`、`auto_buff/`、`omniperception/`（哨兵全向感知）。
- `src/`：应用层，每个兵种一个 `main`——`standard.cpp`、`sentry.cpp`、`uav.cpp`、`mt_standard.cpp`（多线程版步兵）等 13 个。
- `tools/`：轮子。EKF、CRC、线程安全队列、弹道、日志、YAML、绘图、录制器。
- `tests/`：从 `camera_test` 到 `planner_test_offline`，21 个可独立编译运行的测试程序。
- `configs/`：*每台车一个 YAML*——`standard3.yaml`、`standard4.yaml`、`sentry.yaml` 等等。

想快速看清数据流，读 `src/mt_standard.cpp`，一共 137 行，主循环十几行就把全链路摆完了。其中一行值得停下来：

```cpp
auto [img, armors, t] = detector.debug_pop();   // 图像 + 识别结果 + 时间戳
Eigen::Quaterniond q = cboard.imu_at(t - 1ms);  // 按时间戳取当时的云台姿态
```

#align(center)[#text(size: 9pt)[（本章此后引用的全部代码片段均摘自 `sp_vision_25`，Copyright (c) 2025 TongjiSuperPower，MIT 许可证；为排版做过删节与换行调整，行内注释有中译。）]]

`imu_at(t)` 是「时间戳对齐」一章那套方法的一份直接实现：下位机以高频往上灌四元数，每个都盖上本地 `steady_clock` 时间戳存进队列；查询时找到夹住目标时刻的前后两帧，做四元数球面线性插值（`slerp`）。和 rm_vision 把这件事交给 tf2 的缓存去查相比，这里*插值逻辑完全暴露在自己代码里*——少了一层框架，多了一份可控。那个 `- 1ms` 就是「时间戳对齐」一章说的固定偏置项：图像时间戳标的是"接收完成"时刻，真正的曝光时刻要往前推一点。

==== 协议长大了，于是换了根线

`io/` 下同时躺着两套下位机通信实现，这是本仓库最好的"协议演化"标本。

旧的是 `io::CBoard`，走 SocketCAN（USB2CAN 转接）。下发的数据结构 `io::Command` 只有五个字段：`control`、`shoot`、`yaw`、`pitch`、`horizon_distance`，打包进一帧 8 字节 CAN 报文——两个布尔各占一字节，三个量各按 $times 10^4$ 存成 `int16_t`，正好 8 字节，一个字节不剩。

新的是 `io::Gimbal`，走 MicroUSB 虚拟串口，两个结构体定义得清清楚楚：

```cpp
struct __attribute__((packed)) VisionToGimbal {
  uint8_t head[2] = {'S', 'P'};
  uint8_t mode;   // 0: 不控制, 1: 控制云台但不开火, 2: 控制云台且开火
  float yaw, yaw_vel, yaw_acc;      // 三阶前馈
  float pitch, pitch_vel, pitch_acc;
  uint16_t crc16;
};
```

为什么必须换线？把字节数算一遍就知道了：帧头 2 ＋ 模式 1 ＋ 六个 `float` 共 24 ＋ CRC 2 ＝ 29 字节。经典 CAN 一帧的数据场上限是 8 字节。*29 装不进 8*，所以只要决定"下发的不止是角度，还要带上速度和加速度作为前馈"，CAN 这条路就走到头了。协议内容一变，物理层跟着换——这就是「通信协议设定」一章说的"先定契约再选载体"在真实项目里的样子，只不过这里是反过来演的：契约长大了，载体被迫升级。

还要留意一处细节：开火并不是一个独立的标志位，而是折进了 `mode`——0 不控制、1 控制云台但不开火、2 控制云台且开火。代码里是 `tx_data_.mode = control ? (fire ? 2 : 1) : 0;`。把两个逻辑上有从属关系的布尔（"控不控"与"开不开火"）合成一个三值枚举，从协议上就杜绝了"不控制云台但要求开火"这种非法组合。这是「通信协议设定」一章说的*用类型消灭非法状态*的一个小而漂亮的实例。

回传方向的 `GimbalToVision` 更能说明问题：模式、四元数 `q[4]`、`yaw`／`yaw_vel`／`pitch`／`pitch_vel`、`bullet_speed`，再加一个 `bullet_count`（子弹累计发射次数），共 43 字节，同样塞不进 CAN。而那个 `bullet_count` 是个很聪明的字段：它让视觉端*知道自己那条开火指令到底有没有变成一发子弹*——热量超限、弹仓空了、拨弹卡住，下位机都可能吞掉开火指令。没有这个回传，视觉就是在开环喊话。

顺带把三家的下发内容对照一下，分歧一目了然：rm_vision 发的是*整车状态*（把弹道、选板、开火全推给下位机），rm.cv.fans 发的是*补偿后的角度*（还替电控的 PID 稳态误差擦了屁股），sp_vision 发的是*位置＋速度＋加速度三元组＋开火模式*。三种切法对应三种"视觉和电控的分界线该划在哪"的答案，没有对错，但*分界线划在哪，调试链路就断在哪*——这一点在最后的横向对比里再收。

=== yaw 优化：把规则先验写进搜索

单块装甲板做 PnP，最不准的自由度是绕竖直轴的那个 yaw：四个共面角点在图像上的投影对 yaw 变化不敏感，稍有像素噪声解就翻车（「相机模型」一章讲过共面点 PnP 的病态性）。sp_vision 的解法是 `Solver::optimize_yaw()`：

```cpp
constexpr double SEARCH_RANGE = 140;  // degree
auto yaw0 = tools::limit_rad(gimbal_ypr[0] - SEARCH_RANGE / 2 * CV_PI / 180.0);
for (int i = 0; i < SEARCH_RANGE; i++) {          // 1° 步长，暴力扫 140 次
  double yaw = tools::limit_rad(yaw0 + i * CV_PI / 180.0);
  auto error = armor_reprojection_error(armor, yaw, ...);
  if (error < min_error) { min_error = error; best_yaw = yaw; }
}
armor.yaw_raw = armor.ypr_in_world[0];   // 留档：PnP 的原始输出
armor.ypr_in_world[0] = best_yaw;        // 覆盖
```

以云台当前 yaw 为中心、左右各 70°，1° 一格扫 140 次，取重投影误差最小的那个覆盖掉 PnP 的原始 yaw。没有梯度、没有迭代、没有初值敏感性——一维搜索用暴力是完全正当的工程选择，140 次重投影的代价远小于一次滤波器发散。注意它把原始解留在了 `yaw_raw` 里没扔，这是个好习惯：调试时你能一眼看出优化到底改了多少。

关键在重投影函数里那一行：

```cpp
auto pitch = (name == ArmorName::outpost) ? -15.0 * CV_PI / 180.0 : 15.0 * CV_PI / 180.0;
```

重投影时装甲板的 pitch 是按规则写死的：普通装甲板*后仰* 15°（上沿向车内倒、法线朝上），前哨站装甲板反过来*前倾* 15°（法线朝下）；数值随赛季变动，以当季手册为准。这就把 6 自由度的位姿问题降成了"只有 yaw 未知"的一维问题——和「rm.cv.fans」一章讲的降自由度求姿态是同一个思想，来源在后面的谱系一节里说。

==== 把假设的适用边界写进代码

真正的工程素养范例在 `Solver::solve()` 的末尾：

```cpp
// 平衡不做yaw优化，因为pitch假设不成立
auto is_balance = (armor.type == ArmorType::big) &&
                  (armor.name == ArmorName::three || armor.name == ArmorName::four ||
                   armor.name == ArmorName::five);
if (is_balance) return;
optimize_yaw(armor);
```

平衡步兵的装甲板是竖直安装的，不带那 15° 后仰。整套 yaw 优化的地基——"pitch 已知"——在平衡步兵身上不成立，于是*代码直接跳过优化、退回 PnP 的原始解*，注释白纸黑字写着为什么。

这短短四行，做对了两件很多人做不对的事。第一，*识别出自己的方法有前提*——绝大多数实现只会写下 `pitch = 15°` 然后忘掉它是个假设。第二，把前提的适用边界编码进代码，而不是写在文档里或留在某个人脑子里。文档会漂移（后面就有活例子），人会毕业，只有 `if` 会一直在那儿。

假设失效时会有多糟？下图用一次数值实验回答了这个问题：pitch 先验一旦偏离真值，重投影代价函数会从"一个清晰的极小值"退化成"两个几乎等高的极小值"，暴力搜索照样能找到全局最小，但*最小值的领先优势会塌到与角点噪声可比*，谁高谁低就成了抛硬币。这正是那句注释背后的物理。

#figure(
  image("images/proj-sp-yaw-optimize.png", width: 100%),
  caption: [pitch 先验失效时 yaw 优化会怎么塌掉（脚本 `proj_sp_yaw_optimize.py`）。读图规则：左图看代价函数的形状，右图看它的后果。左图是同一块装甲板在三种真实 pitch 下的重投影代价随候选 yaw 的变化，搜索网格与代码一致（以云台 yaw 为中心 $plus.minus 70 degree$、$1 degree$ 步长）——先验正确时（蓝）只有一个深谷；先验偏 $15 degree$（橙）已长出第二个"镜像"极小值，仍以 3.2 px 的优势领先；偏 $30 degree$（品红）时领先优势只剩 1.0 px，与角点噪声同量级。右图把这件事量化成失败率（每个 pitch 取 600 次 1 px 角点噪声试验）：横轴是装甲板真实 pitch，粉色阴影标出*先验已被明显违反的区间*（图内标注 assumption violated）。先验正确（$15 degree$）时选错极小值的概率只有 1.7%、yaw 的 RMS 误差 $4.0 degree$；而平衡步兵的装甲板竖直、真实 pitch 就落在 $0 degree$ 附近，此处概率已涨到 15.3%、RMS 涨到 $10.8 degree$——先验才偏了 $15 degree$，选错率就翻了近十倍；再往负向走到 $-15 degree$ 便是 47.7% 与 $18.8 degree$，几乎一半的帧会把 yaw 解到镜像位置上去。代码里那句 `if (is_balance) return;` 挡掉的正是从 $0 degree$ 一路向左的这整片退化区：不做优化只是不准，做了反而可能给出一个自信的错误答案。],
)

这里还藏着一个更普适的教训：一个会"自信地给出错误答案"的优化器，比一个不那么准的直接解更危险。PnP 原始解的误差是连续的、有界的，而选错极小值的误差是跳变的、量级完全不同的——后者会直接把整车 EKF 带飞。这一点和「相机标定与手眼标定」一章说的"欠约束照样给你一个数"是同一类病。

=== 十一维整车模型与三道自检

==== 状态、噪声与前哨站先验

sp_vision 的整车 EKF 是 11 维，状态定义就写在 `target.cpp` 的注释里：

```text
x vx y vy z vz a w r l h
a: angle    w: angular velocity
l: r2 - r1  h: z2 - z1
```

前八维和 rm_vision 那套九维的前八维一一对应（中心平动 ＋ 自旋角与角速度），第九维 `r` 也一样是旋转半径。多出来的是 `l` 和 `h`：相邻两块装甲板的半径差与高度差。「装甲板的跟踪」一章解释过为什么需要它们——一台车的四块装甲板并不严格在同一个圆上、也不严格等高，机械误差让它们两两成对。rm_vision 用"两个半径交替交换"来处理，sp_vision 则把差值显式建成状态量，让滤波器自己去估。观测方程里的开关就是这句：

```cpp
auto use_l_h = (armor_num_ == 4) && (id == 1 || id == 3);
auto r = (use_l_h) ? x[8] + x[9] : x[8];
```

只有四块装甲板的车才分长短轴，且只对 1、3 号板加 `l`、`h`——三块板的前哨站和基地天然对称，用不上。

过程噪声 $bold(Q)$ 用的是分段白噪声模型（Piecewise White Noise），代码注释里直接给了出处链接。它的形式很干净：每个二阶子块都是 $v dot bold(g) bold(g)^T$，其中 $bold(g) = [(Delta t)^2 \/ 2, Delta t]^T$——也就是"一段常值加速度在 $Delta t$ 内造成的位置增量与速度增量"。这解释了 $bold(Q)$ 里那三个系数 $(Delta t)^4 \/ 4$、$(Delta t)^3 \/ 2$、$(Delta t)^2$ 从哪来，也顺带说明一件后面要用的事：既然 $bold(Q) = v dot bold(g) bold(g)^T$ 是个外积，它就恒是秩 1 半正定的（行列式恒为 0、迹恒为正），$Delta t$ 取负时也不例外——负步长回推不会把协方差算成非法值。这不等于 $bold(Q)$ 与 $Delta t$ 的符号无关：$Delta t$ 变号时非对角的 $(Delta t)^3 \/ 2$ 会跟着变号，翻转的是位置误差与速度误差的相关方向，而这正是往回推该有的样子。

前哨站的特判有两层，都是*把规则手册里的已知量当先验注入*：

```cpp
if (name == ArmorName::outpost) { v1 = 10;  v2 = 0.1; }   // 加速度方差 / 角加速度方差
else                            { v1 = 100; v2 = 400; }
// 收敛后，转速超过 2 rad/s 就直接钉到规则值
if (this->convergened() && this->name == ArmorName::outpost && std::abs(this->ekf_.x[7]) > 2)
  this->ekf_.x[7] = this->ekf_.x[7] > 0 ? 2.51 : -2.51;
```

第一层：前哨站底盘不动、转速恒定，所以过程噪声给得极小（角加速度方差 0.1 对 400，差了三个半数量级），滤波器几乎不允许 $omega$ 变化。第二层更直接：一旦*已收敛*且 $|omega| > 2$，就把角速度赋成 $plus.minus 2.51$ rad/s——规则规定的前哨站转速（近年为 $0.8 pi approx 2.51$ rad/s，随赛季变动、以当季手册为准）。两个条件缺一不可：没收敛就锁，等于把一个还在乱跳的估计钉死；不设 $|omega| > 2$ 的门槛，慢速转动或静止的前哨站会被强行赋上一个不存在的转速。

还有第三层藏在初始化里。`tracker.cpp` 给不同兵种配不同的初始协方差对角：

```cpp
else if (armor.name == ArmorName::outpost) {
  Eigen::VectorXd P0_dig{{1, 64, 1, 64, 1, 81, 0.4, 100, 1e-4, 0, 0}};
  target_ = Target(armor, t, 0.2765, 3, P0_dig);   // 半径 0.2765 m，3 块装甲板
}
```

末尾那两个 `0` 不是随手写的：`l` 和 `h` 的初始方差为零，加上 $bold(Q)$ 里对应位置也是零，这两维在整个生命周期里永远不会被更新。前哨站三块板严格对称，长短轴差不存在——这个结构性事实被编码成了一个零。同理 `r` 的方差给 $10^(-4)$，等于说"半径我已经知道了，别乱估"。

这是很值得学的一手：已知的东西不要让滤波器去猜。每多一个被估计的自由度，就多一条发散的路径。规则手册白纸黑字写着的转速、装甲板数量、半径，直接钉死，把滤波器的自由度留给真正未知的量。

==== 自适应 R：斜了别信测距，远了别信角度

观测噪声 $bold(R)$ 的写法比 $bold(Q)$ 更值得看。观测量是 4 维——方位角、俯仰角、距离 $d$、装甲板朝向角。代码里被注释掉的旧版是四个常数，现役版本是这样：

```cpp
// Eigen::VectorXd R_dig{{4e-3, 4e-3, 1, 9e-2}};          // 旧：四个常数
auto center_yaw = std::atan2(armor.xyz_in_world[1], armor.xyz_in_world[0]);
auto delta_angle = tools::limit_rad(armor.ypr_in_world[0] - center_yaw);
Eigen::VectorXd R_dig{
  {4e-3, 4e-3, log(std::abs(delta_angle) + 1) + 1,
   log(std::abs(armor.ypd_in_world[2]) + 1) / 200 + 9e-2}};
```

`delta_angle` 是装甲板朝向与视线方向之差，也就是*这块板对着你有多斜*。两个自适应项分别是：

- *距离的方差随"斜"增长*：$log(|Delta theta| + 1) + 1$。板越斜，PnP 测距越不可信。
- *朝向角的方差随"远"增长*：$log(d + 1) \/ 200 + 9 times 10^(-2)$。目标越远，单板朝向估计越不可信。

两个式子都设计成"$Delta theta = 0$ 或 $d = 0$ 时退化回旧版常数"——$log(1) = 0$，于是第三项回到 $1$、第四项回到 $0.09$，和注释掉的那行分毫不差。新版是在老常数上做膨胀，不是推倒重来，所以升级不需要重调其余参数。这个改法很讲究，值得抄。

更值得抄的是它背后的判断。「rm.cv.fans」一章里那份工程日志实测过：装甲板较斜时 PnP 测距偏差可达 20~30 cm，而半径估计依赖测距、位置估计又依赖半径，误差会一路放大；那个项目给出的对策是"两板像素面积差距大时拒绝更新半径"。sp_vision 处理的是同一个物理现象，对策却更平滑——不是硬拒绝，而是*调大方差让卡尔曼增益自己把这次观测的权重压下去*。

这是「卡尔曼滤波」一章那句话最漂亮的一次落地：$bold(R)$ 不是一个要调的常数，是你对每一次观测有多信任的诚实表述。你知道斜的板测距不准，就把它写进 $bold(R)$，剩下的交给增益公式。硬阈值拒绝是"要么全信要么不信"，自适应 $bold(R)$ 是"按可信度加权"——后者没有阈值抖动问题，也不会在临界点附近反复横跳。

==== 三道自检，其中一道是三家里唯一的统计检验

跟踪器的主函数里串着三道健康检查，一道比一道深：

```cpp
if (state_ != "lost" && dt > 0.1) {              // 一、两帧间隔超 100 ms
  tools::logger()->warn("[Tracker] Large dt: {:.3f}s", dt);
  state_ = "lost";
}
if (state_ != "lost" && target_.diverged()) {    // 二、物理量跑出合理范围
  state_ = "lost"; return {};
}
if (std::accumulate(target_.ekf().recent_nis_failures.begin(),   // 三、统计一致性
                    target_.ekf().recent_nis_failures.end(), 0)
    >= (0.4 * target_.ekf().window_size)) {
  tools::logger()->debug("[Target] Bad Converge Found!");
  state_ = "lost"; return {};
}
```

第一道是*外部性检查*：两帧图像间隔超过 100 ms，说明相机大概率掉线了，此时 $Delta t$ 巨大，硬推一步 EKF 只会把协方差炸上天，不如直接放弃目标。第二道是*物理合理性检查*，`diverged()` 判的是半径 $r$ 和 $r + l$ 是否都落在 $(0.05, 0.5)$ m 内——超出就是发散。注意它和 rm_vision 的做法不同：rm_vision 是把半径*硬夹*回 $[0.12, 0.4]$ 继续跑，sp_vision 是*判定发散、丢弃目标重来*。硬夹的好处是不丢跟踪，坏处是掩盖病症——一个被夹住的半径会安静地给出错误的装甲板位置；丢弃重来的代价是几帧空窗，好处是不会带着错误状态一直打下去。

第三道最有意思，也是*三个项目里唯一一个统计意义上的滤波器一致性检验*。思路来自「卡尔曼滤波」一章讲过的新息一致性：残差应当服从由 $bold(S) = bold(H) bold(P) bold(H)^T + bold(R)$ 描述的分布，把它归一化成标量 $epsilon = bold(nu)^T bold(S)^(-1) bold(nu)$ 再和卡方分位数比较；单帧超标是正常涨落，但一个滑动窗口内持续超标，就说明模型和现实对不上了。代码在长度 100 的窗口里数失败次数，超过 40% 就判 "Bad Converge"、强制丢目标。

这比"半径跑出范围"高明在哪？它能抓到物理量还正常、但模型已经错了的情况。目标突然变速、装甲板匹配错板、观测被反光污染——这些时候 $r$ 可能一直老老实实待在 $(0.05, 0.5)$ 里，可滤波器给出的预测已经系统性偏离观测了。前两道检查看不见这种病，第三道能。

*机制值得抄，但常数别照抄*，有两处需要你自己重新推：

其一，代码里的阈值是 `constexpr double nis_threshold = 0.711;`，注释写"自由度=4，取置信水平95%"。0.711 确实和卡方分布挂得上钩，但挂的是*下 5% 分位点*——查表可得 $P(chi^2_4 <= 0.711) = 5%$，反过来说 $P(chi^2_4 > 0.711) = 95%$。若它真是教科书意义上的 NIS，一个健康滤波器会有 95% 的帧"失败"，40% 的窗口规则将永远触发。

其二，代码计算残差用的是*更新之后*的状态：先完成 $bold(x) <- bold(x) + bold(K) bold(nu)$，再算 `residual`，而 $bold(S)$ 用的也是更新后的 $bold(P)$。教科书的 NIS 用的是更新*之前*的新息与先验协方差。后验残差系统性地小于新息（更新的作用正是把残差往下压），所以这里算出来的统计量本就比真 NIS 小一大截，配一个偏小的阈值，两个偏差方向相反、大致抵消——*于是它在实践中能用*，这套代码带着它打完了国赛。

把这两点摆出来不是挑错，是给你一条读代码的准则：一个能用的启发式，不等于一个可移植的公式。这里的 0.711 是在"后验残差"这个特定统计量上经验标定出来的数，换一个滤波器、换一个观测维度、把残差改回先验新息，它立刻失效。抄这段代码时，抄"滑动窗口数失败率、超阈值就强制丢目标"这个机制，然后拿自己的录制数据把阈值重标一遍。

=== 规划器在这个仓库里长什么样

轨迹规划器的*理论*——目标／云台／射击三条轨迹、重合度决定命中率上限、提前减速、$alpha_max = tau_max \/ J$ 这道物理天花板、以及逐点的代码映射——「自瞄算法设计」一章已经完整讲过，本节不重复。这里只看它作为一段真实代码，有哪些理论之外的工程决定。

时间窗是怎么对齐到"现在"的。 采样参考轨迹的函数开头有个容易看漏的动作：

```cpp
target.predict(-DT * (HALF_HORIZON + 1));   // 先把目标状态往回倒
auto yaw_pitch_last = aim(target, bullet_speed);
target.predict(DT);   // [0] = -HALF_HORIZON * DT -> [HALF_HORIZON] = 0
```

先用一个*负的* $Delta t$ 把目标状态往回推半个窗口，再逐步向前采样。这样采出来的 100 点轨迹里，索引 0 对应"半个窗口之前"，索引 `HALF_HORIZON` 才对应"现在"。为什么要这么绕？因为求解器需要一个初始状态，而它取的正是*参考轨迹自己在半个窗口之前的那一点*——`x0 << traj(0, 0), traj(1, 0);`（`planner.cpp:59-60`，pitch 同理 :67-68）。这等于*假设云台在半个窗口之前正贴在射击轨迹上*，再让那个受加速度约束的二重积分从这一点滚 50 步到"现在"：索引 `HALF_HORIZON` 处的解于是天然可达，求解器没机会在第一步瞬移。而且 `Xref` 铺满整个窗口（`traj.block(0, 0, 2, HORIZON)`），过去那半个窗口不只是个初始条件，它同样进了代价函数——解在抵达"现在"之前已经跑了半秒"助跑"，该减的速在这半秒里就减掉了。

要紧的是：这个初始状态是*假设出来的过去*，不是实测的云台角。仓库里没有任何一处把云台状态喂进求解器，所以这仍然*不是闭环*——本章后面引用的 readme 原文会正面确认这一点。前面那个负 $Delta t$ 之所以合法，正是因为分段白噪声的 $bold(Q)$ 是个外积，$Delta t$ 取负时它依然半正定（上一节推过）。

*求解器的预算是硬定的。* `yaw_solver_->settings->max_iter = 10;`——最多迭代 10 次，不管收没收敛。这是实时系统里非常典型的取舍：宁可要一个次优但准时的解，也不要一个最优但迟到的解。云台每 10 ms 就要一条新指令，一个算了 30 ms 的完美轨迹已经过期了。

延迟按角速度分档，但数值是每台车标出来的。 机制在规划器里：

```cpp
double delay_time = std::abs(target->ekf_x()[7]) > decision_speed_
                    ? high_speed_delay_time_ : low_speed_delay_time_;
```

目标转得快就用大延迟、转得慢用小延迟。为什么要分档？因为高速小陀螺时装甲板切换频繁，射击轨迹上的锯齿更密，需要更靠前地"看"。而真正有意思的是各个 YAML 里的实际数值：

#block(breakable: false)[
#figure(
table(
  columns: (auto, auto, auto, auto),
  align: (left, right, right, right),
  stroke: (x, y) => (
    top: if y == 0 { 1pt } else { 0pt },
    bottom: if y == 6 { 1pt } else { 0pt },
  ),
  inset: 7pt,
  table.header([*配置文件*], [`decision_speed`], [高速档], [低速档], table.hline(stroke: 1pt)),
  [`standard3.yaml`], [7 rad/s], [0 ms], [0 ms],
  [`standard4.yaml`], [8 rad/s], [15 ms], [15 ms],
  [`sentry.yaml`], [10 rad/s], [26 ms], [10 ms],
  [`uav.yaml`], [12 rad/s], [5 ms], [5 ms],
  [`demo.yaml`], [8 rad/s], [30 ms], [15 ms],
  [`ascento.yaml`], [7 rad/s], [50 ms], [15 ms],
),
caption: [同一份规划器代码，六台车六组延迟参数（数据取自仓库 `configs/` 目录下各 YAML）。读表规则：看竖直方向的离散程度——高速档从 0 ms 到 50 ms 横跨一个数量级，连同一支队伍的 3 号和 4 号步兵都不一样；有三台车干脆把两档设成同一个值，等于关掉了分档机制。这说明这几个数不是从物理推出来的设计量，而是*每台车实测标定出来的*，取决于它的电控响应、相机延迟、云台惯量。抄这套代码时，机制可以照搬，数值必须重标。],
)
]

这张表比任何说教都有力：同一份代码里，一个参数在不同车上差一个数量级，说明它是标定量不是设计量。读开源项目时看到一个具体数字，先去 `configs/` 里翻翻它在别的配置里是多少——处处相同，那是设计；处处不同，那是标定，你必须自己重来一遍。

顺带一提，`standard3.yaml` 把两档都设成 0，意味着这台车上除了子弹飞行时间，没有额外的延迟提前量；而 readme 讨论预测时间时说，除图像处理延迟外"其余延迟时间的总和作为一个调试参数……约 15ms"。两处对不上——这属于*文档与配置的漂移*，本章末尾会把它和另外两家的同类问题一起归并。

=== 三条被实测否定的记录

一份开源代码最稀缺的东西不是能跑的方案，是*试过但没成的方案*。绝大多数项目只把成功的那条路交出来，读者看到一条笔直的技术路线，误以为作者一次就想对了。sp_vision 的 readme 里留了三条反向记录，这是全章最有价值的部分。

一、MPC 只当求解器，不当闭环控制器。 原文：

#blockquote[
  这里MPC并非闭环控制器，而是作为求解器寻找可行轨迹解，其输入不包含云台的实际状态。我们也尝试过使用MPC直接作为闭环控制器，通过下位机转发MPC求解后的力矩指令给电机，当自身小陀螺时，控制效果不如下位机方案。
]

"MPC" 三个字容易让人以为这里在做模型预测*控制*，其实它只被当成一个二次规划求解器用——输入里根本没有云台的实际状态，谈不上闭环。而真去做闭环时，*自身小陀螺*这个工况把它打败了：自旋会让云台的动力学（惯量分布、耦合力矩）偏离上位机模型里的那一套，而下位机的 PID 虽然"笨"，却直接跑在电机旁边、带着高频编码器反馈，对模型失配的鲁棒性反而更好。

这条记录的普适价值：控制回路离被控对象越近越好。上位机能贡献的是规划和预测（它有全局信息、有算力），控制留在下位机（它有实时性、有高频反馈）。这也正是「自瞄算法设计」一章那条"感知与决策在小电脑、控制在下位机"铁律的一次实证。

二、自评更优的方案，因为没时间验证而没上场。 提前减速有两种实现，作者对方案二（五次多项式显式搜索）的评价是"确定性高"、"跟随段不会参与优化，和射击轨迹重合"、"高转速下更加稳定"、"实现更加轻量，无需依赖第三方库"——四条全是优点。结论却是：

#blockquote[
  由于时间有限，方案二仅进行了仿真验证，未上车测试，国赛上场采用了方案一。
]

一个各方面自评都更好的方案，因为没时间上车验证而被搁置。 这是战队工程最真实的样子，也是给读者最重要的一课：未经实车验证的"更优"不是更优，是一个待验证的假设。方案一虽然依赖第三方库、参数更多，但它跑过整个赛季、暴露过问题、被修过——这份"里程数"本身就是巨大的价值。技术选型从来不只比较方案的理论优劣，还要比较*你还剩多少时间去验证它*。

顺带说一句，方案一那个第三方求解器后来确实成了故障源（见下一节的 issue #8）。这没有推翻上面的判断，反而印证了它：即便是跑过赛季的方案，也仍在暴露新问题——而没跑过的那个，你连它会在哪炸都不知道。

三、提前开火决策的前提是开火延迟不抖。 规划器判断开火时，查的不是"现在"，而是往后偏移两步的那一点——因为从开火命令发出到子弹真正出膛（摩擦轮掉速）有一段延迟 $t_"fire"$，要判断的是*子弹出膛那一刻*会不会命中。原文对这个做法的自评是：

#blockquote[
  该方案的前提是开火延迟无波动，对机械和电控的要求较高。
]

这句自我限定非常诚实，因为*它的前提相当强*。一个可以对照的数据点在进阶篇「实战经验与坑点总结」一章：那里介绍过怎么用裁判系统的发弹事件回填时间戳，把从曝光到出膛的全程延迟闭环量出来，某个自研项目实测的分布是 35～110 ms、均值约 82 ms。那是另一台车、另一套电控，不能直接当作对 sp_vision 的反驳；但它说明*开火延迟的抖动在真实机器人上可以大到与均值同量级*。若你的拨弹机构也这么抖，那么"提前若干毫秒判断"里的那个提前量就是个随机数，精细的提前开火决策反而不如就地判断稳。

三条记录连起来看，能读出一种态度：把没成的、没验证的、有强前提的，全都写下来。这比多开源一个算法有用得多——因为算法你能从代码里读出来，而"这条路我们走过、走不通"只能靠人告诉你。

=== 坑与未竟之事

这个项目自己踩的、还留在 issue 和 TODO 里的坑，同样有教学价值。

上场方案的求解器成了新故障源。 issue #8（至今 open）标题是"tinympc 求解器始终返回'未收敛'"。国赛上场用的正是这条路径。这不必然意味着规划失效——`max_iter = 10` 本来就允许提前截断返回次优解，"未收敛"是预期内的状态；但它确实说明*一个作为黑箱引入的第三方求解器，其内部状态你既不完全理解也不容易调试*。这是"隐式搜索"路线相对方案二那条"无需依赖第三方库"的真实代价，前一节作者列的优点在这里得到了印证。

标定结果与物理安装对不上。 issue #9（open）："手眼标定时的计算的旋转矩阵与实际安装坐标系关系差别很大，不能说是精度问题了。" 这正是「相机标定与手眼标定」一章反复警告的失效模式：手眼标定欠约束时照样给你一个数，而且是一个看起来完全正常、拿去用会一路错下去的数。判断标准很简单也很有效——把解出来的旋转矩阵和你用尺子量出来的安装关系比一比，量级对不上就是标定错了，不是精度问题。

*两处写死的图像中心。* 跟踪器里有两处一模一样的代码：

```cpp
cv::Point2f img_center(1440 / 2, 1080 / 2);  // TODO
```

作用是"优先选择靠近图像中心的装甲板"。分辨率被硬编码成 1440×1080，作者自己挂了 `TODO`。换一台不同分辨率的相机，排序依据就悄悄错了——而且*不会报错、不会崩溃，只是选板策略变得莫名其妙*。这类坑的共同特征是"错得很安静"，比崩溃难查得多。仓库里 `io/` 支持海康、迈德威视、USB 三类相机，分辨率本就不统一，这个 TODO 是真的欠着。

*同一个默认弹速三处不同。* 规划器里是 22、瞄准器里是 23、打符里是 24。这个坑「自瞄算法设计」一章已经完整讲过，此处不重复；只补一层那里没说的：连兜底的触发条件都不一致——规划器判"小于 10 或大于 25"，瞄准器判"小于 14"，打符判"小于 10"。也就是说，当裁判系统报出一个 12 m/s 的异常弹速时，规划器认为它有效、原样拿去算弹道，瞄准器认为它无效、替换成 23。这两条路径分属不同的可执行文件（`standard_mpc.cpp` 装的是规划器，`standard.cpp` 装的是瞄准器），不会同时跑；但分歧并不因此消失——`mt_standard.cpp` 那个 main 里同时装着自瞄瞄准器与打符瞄准器，同一个 12 m/s，在自瞄档被判为无效换成 23，在打符档被判为有效原样使用。同一个可执行文件，同一场比赛，换一个档位，同一个物理量就换一个值。同一个物理量全系统只能有一个来源，连"什么算无效"的判据也必须只有一份。

模型权重来自别家，且没给原始权重。 readme 坦诚标注识别模型来自深圳大学 RobotPilots 与北京科技大学 Reborn 两支战队的开源成果（参考文献 [1][2]），仓库 `assets/` 里放的是转换后的 `.onnx` 与 OpenVINO 的 `.xml`／`.bin`。issue #7 有人请求开源原始权重文件，最终关闭。这不是指责——标注来源已经比很多项目规范——而是提醒你：拿到推理文件不等于拿到模型。你没法微调、没法换骨干网络、没法在自己的数据上继续训练（「装甲板识别」一章讲过推理引擎与硬件绑定的问题）。用别家的权重是合理的起点，但要清楚自己拿到的是什么。

*未竟之事：边跑边打。* readme 最后一节说得很具体：目前自瞄不考虑自身移动，"步兵经常需要从狗洞冲下去杀对面静止的英雄，而此时自瞄会认为对面在移动，导致前几发打不准"。根因很清楚：整车 EKF 估计的是目标在*世界系*下的运动，而这个世界系的姿态只由云台 IMU 的四元数确定——IMU 给的是姿态，不含底盘平移。自己以每秒几米冲下坡时，一个静止的英雄在这套坐标系里就是在反向平移，滤波器忠实地估出了一个不存在的速度，预测器再据此加一个提前量，前几发自然打偏。作者的计划是引入轮式里程计。这个例子很值得琢磨：不是算法错了，是坐标系的定义里少了一项（「坐标变换」一章讲过，一个跟着载体走的参考系并不是惯性系）。

=== 全篇收尾：三个项目的谱系

三章读完，该把它们放在一起看了。这三个项目不是三个孤立样本，而是*一条有明确传承关系的链*。

证据不是靠感觉。sp_vision 的参考文献里，[3] 是 rm.cv.fans、[4] 是 rm_vision，两条都在。而且引用位置很具体：readme 讨论预测时间时写"这里不需要考虑开火延迟，因为在射击轨迹上任意时间发射均会命中[3]"——这条"子弹如水流般连续发射"的火控假设，正是 rm.cv.fans 那份延迟文档的核心论断。

更硬的证据在代码里：`solver.cpp` 有一个函数直接叫 `SJTU_cost`（SJTU 即上海交通大学，rm.cv.fans 作者的背景），算的是一套"长度差 ＋ 起点差 ＋ 角度差"的加权重投影代价。把它和 rm.cv.fans 里那个同功能的 `get_pts_cost` 逐行对照，两者的函数体逐行对应，连中文注释都一字不差——"先移动起点，再补长度，再旋转"、"dis 是指方差平面内到原点的距离"、"平方可能是为了配合 sin 和 cos"、"重投影像素误差越大，越相信斜率"，一句不落地留在了新仓库里。改动只有三处纯适配：两个工具函数换了命名空间、一个具名常量被内联成字面量 `2.0`（原来的常量名还留在行尾注释里）。这已经不是"受启发"，是原样搬运。更有意思的是它现在没在用：重投影误差函数里那一行 `// auto error = SJTU_cost(...)` 被注释掉了，实际跑的是朴素的四角点距离和。*借鉴过、试过、又退回去了*，退回的痕迹留在代码里没删。

这段搬运顺带给出一个合规上的反面教材，正好接上「rm.cv.fans」一章那节许可证课。事实层面有三条都可核对：来源仓库*没有 LICENSE 文件*（著作权人保留全部权利）；目标仓库是 MIT；sp_vision 在参考文献里*确实署了名*。署名是诚实的，但*署名不是授权*——把一段"保留全部权利"的代码放进 MIT 仓库，等于以自己无权给出的条件把它转授给了所有下游用户。这里不去论断谁对谁错，只提醒你一件很实际的事：你 fork 了 sp_vision 并按 MIT 使用它，也就默认接手了这段来源不清的代码。开源合规的风险是会沿着依赖链往下传的，而 30 行代码的来源，只要有心花十分钟就能查清。

反过来，rm_vision 对 sp_vision 的影响不在某一行代码，而在整个范式：把一台车整体建模成"中心平动 ＋ 装甲板绕中心旋转"，用一个 EKF 估计整车状态。sp_vision 的 11 维状态前 9 维与之同构，只在末尾追加了 `l`、`h`。这个范式在社区里的普及，rm_vision 是最重要的推手。

至于 rm_vision 和 rm.cv.fans 之间，两者是同期作品（都在 2023 年前后），在两边的仓库里都没有找到相互借鉴的证据。它们是从不同起点出发、解决同一问题的两条独立路径。

#figure(
  image("images/proj-lineage.png", width: 100%),
  caption: [三个项目的思想流向与时间线（脚本 `proj_lineage.py`）。读图规则：箭头方向是思想流向，不是质量排序。上方蓝色弧线是 rm_vision 贡献的整车 EKF 范式，在 sp_vision 的参考文献里编号 [4]；下方品红弧线是 rm.cv.fans 的两笔贡献，编号 [3]——一笔是火控词汇与延迟理论（"子弹如水流"这条假设被直接引用在预测时间的推导里），另一笔是*直接搬运的代码*：`get_pts_cost` 到 `SJTU_cost`，函数体逐行对应、中文注释一字不差，而调用点已被注释掉，借鉴、试用、回退的痕迹完整留在文件里。右上橙色框是第三个来源——识别模型权重来自另外两支战队（文献 [1][2]），说明生态里的互取不止于框架。中间虚线表示 rm_vision 与 rm.cv.fans 之间没有找到互引证据，是同期的独立路径。注意每张卡片右下角的许可证角标：一条从"NO LICENSE"指向"MIT"的实线箭头，正是本节末尾那段合规讨论的由来。底部时间轴解释了为什么谱系是这个方向：`rm_auto_aim` 的算法本体停在 2023-05，而 sp_vision 一路演进到 25 赛季，*后来者才有条件综合前人*。],
)

=== 三线横向对比

把三个项目的关键选择摆成一张表。看这张表的正确方式不是找"谁赢了"，而是*看每一行的三个格子有多不一样*——差异越大的行，说明那个问题越没有标准答案，越需要你自己想清楚。

#[
#show table: set par(justify: false)
#figure(
table(
  columns: (auto, auto, auto, auto),
  align: (left, left, left, left),
  stroke: (x, y) => (
    top: if y == 0 { 1pt } else { 0pt },
    bottom: if y == 14 { 1pt } else { 0pt },
  ),
  inset: 6pt,
  table.header([*维度*], [rm_vision], [rm.cv.fans], [sp_vision], table.hline(stroke: 1pt)),
  [中间件], [ROS 2 Humble，DDS ＋ 进程内零拷贝], [自研 UMT，进程内发布订阅], [无中间件，`std::thread` ＋ 线程安全队列],
  [组织与构建], [4 仓库 6 个 ament 包], [单仓，自建构建], [单仓单 CMake，四层目录],
  [坐标变换], [tf2 树，静态链 ＋ 串口发动态链], [自写坐标变换], [解算器内直接乘矩阵，外参从 YAML 读],
  [时间对齐], [依赖 tf2 缓存按时间戳查询], [下位机时间戳绑定图像], [`imu_at(t)` 四元数插值，固定偏置],
  [识别], [灰度二值化 ＋ 传统级联 ＋ MLP 分类], [传统方案，自研], [神经网络四点检测（权重来自他队）],
  [yaw 优化], [无，直接用 PnP 输出], [降自由度重投影搜索（招牌）], [$plus.minus 70 degree$／$1 degree$ 暴力扫 140 次，pitch 按规则写死],
  [跟踪模型], [9 维整车 EKF], [LMTD 运动模型 ＋ 角度滤波器], [11 维整车 EKF（多 `l`、`h`），自适应 $bold(R)$],
  [发散保护], [半径*硬夹* $[0.12, 0.4]$，发散即重置], [两板面积差大时*拒绝*更新半径], [越界丢弃 ＋ NIS 滑窗*统计检验*],
  [弹道在哪端], [下位机], [视觉端（还含自主弹道校正）], [视觉端（`tools` 层自带解算）],
  [下发内容], [整车状态（位置、转速、半径等）], [补偿后的角度（含 PID 稳态误差前馈）], [位置＋速度＋加速度三元组 ＋ 开火模式],
  [测试与回放], [2 个 gtest（仅分类器与节点启动），CI 真跑但滤波器零覆盖], [文档详尽，无独立测试程序], [21 个独立测试程序 ＋ 自研录制器],
  [文档质量], [README 与代码*三方不一致*（维数与顺序都错）], [522 行工程日志，失败全留档], [readme 结构完整，配置与文档有小幅漂移],
  [核心代码量], [`rm_auto_aim` 约 2.0 千行], [约 11.8 千行（全仓，含自研 UMT）], [约 16.8 千行（全仓，含内置 TinyMPC）],
  [一句话画像], [*最规范的框架*], [*最极致的算法*], [*最完整的工程*],
),
caption: [三个开源自瞄项目的横向对比（代码量为对各仓库全部 `.cpp`／`.hpp` 的行数统计，两个大仓都含随仓分发的第三方或自研框架，口径不完全可比，仅示量级）。读表规则：先看"发散保护"和"下发内容"这两行——它们的三个格子差异最大，恰好对应两个最没有标准答案的问题："滤波器出问题时该硬夹、该拒绝、还是该重来"和"视觉与电控的分界线划在哪"。再看"文档质量"那一行：三家在这一项上的差距比在任何算法项上的差距都大，而它直接决定你读这份代码要花一个下午还是一个星期。],
)
]

=== 四条主题归并的教训

跨过项目边界，按主题把三家的经验并起来看，有四条结论比任何单个项目的细节都更值得带走。

一、延迟是有量纲的敌人，而且抖动比均值难对付。 「rm.cv.fans」一章那笔 $720 degree$/s 的账把这件事钉死了：120 转／分的舵轮，每 10 ms 的时间误差对应装甲板转过 $7.2 degree$，误差累到 $30 degree$ 反陀螺命中率直接归零——一段 40 ms 量级的未补偿延迟就足以让你从"能打中"变成"一发不中"。三家的应对各不相同：rm_vision 把一切推给下位机，视觉侧只留一个 6 ms 的常数补偿；rm.cv.fans 把延迟拆成六个可命名的时间点逐段核算；sp_vision 按目标角速度分两档、数值每台车标定。但*均值可以补偿，抖动不能*——一个恒定的延迟只是个加法，一个在几十毫秒范围内乱跳的延迟会直接摧毁"提前开火决策"这类依赖精确时序的设计（sp_vision 自己也承认了这个前提）。延迟预算怎么逐项摊开、哪一段能压哪一段压不动，进阶篇「性能优化技巧」一章已经算过一整张表，这里只留结论：*先测抖动，再谈补偿*。

二、PnP 的测距误差不是噪声，是结构性的，且三家都独立撞上了。 「相机模型」一章推过的深度误差平方律，与 rm.cv.fans 从像素方差那一侧独立推出的四次方律，是同一条定律的两种矩表述——「rm.cv.fans」一章已经把两者对齐过，并提醒了别把指数说串。而实测层面，那份工程日志记录了"装甲板较斜时 PnP 测距偏差可达 20~30 cm"。三家的对策构成一个漂亮的递进：rm_vision 让观测方差随距离缩放（承认远处不可信）；rm.cv.fans 在两板面积差大时拒绝更新半径（硬性拒绝可疑观测）；sp_vision 两手都要——既用降自由度的 yaw 优化从源头改善单板姿态，又用自适应 $bold(R)$ 让"斜"和"远"自动降权。从"事后降权"到"事前纠正"，是这条线上最清晰的技术演进方向。

三、滤波器一定会发散，区别只在你怎么兜底。 这是三家分歧最大、也最有教学价值的一行。rm_vision 把半径*硬夹*在 $[0.12, 0.4]$，注释写着 `// Prevent radius from spreading`；rm.cv.fans 在观测可疑时*拒绝更新*相关状态；sp_vision 用越界判据加 NIS 滑窗*统计检验*，一旦判定就丢弃目标重来。三种做法的哲学不同：硬夹是"不许它跑偏"，拒绝是"不让脏数据进来"，检验是"承认它会坏，但要及时发现"。代价也不同——硬夹会掩盖病症，拒绝需要一个可靠的"可疑"判据，检验会付出几帧空窗。没有哪种更对，但你必须至少选一种：三个跑上过赛场的项目，没有一个是让 EKF 裸奔的。这条经验的价值在于它的一致性——三个独立团队用三种方式给出了同一个答案：*发散是常态，不是意外*。

四、文档一定会漂移，所以以代码为准。 rm_vision 的 `armor_tracker/README.md` 是最极端的例子：README 的散文段说状态是 6 维、README 的公式说是 9 维、代码是 9 维——而且*代码里位置与速度是交错成对排列的，README 的公式却把位置全排在前、速度全排在后*。维数写错读者还有机会发现，顺序写错则会一路默默算出错误结果。sp_vision 的漂移温和得多但同样真实：readme 说残余延迟"约 15ms"，而 `standard3.yaml` 里两档都是 0。这条教训简单到不像教训：读开源项目，文档用来建立直觉，代码用来确认事实。任何你准备写进自己代码里的具体数字、下标、维数、单位，都必须回到源文件确认一次。

=== 该怎么读、抄哪一段、别抄哪一段

最后给一份行动建议。

*先读哪个？* 按 rm_vision → rm.cv.fans → sp_vision 的顺序，也就是本篇的章节顺序。理由不是时间线，是*认知负荷*：rm_vision 的 `rm_auto_aim` 只有约两千行，架构规范、职责清晰，一个下午读得完，读完你就有了整车 EKF 这套范式的完整心智模型；带着这个模型去读 rm.cv.fans 的工程日志，那些"为什么这么做"的讨论才有落脚点；最后读 sp_vision，你会不断认出前两家的影子，读起来快得多。反过来先啃 sp_vision 那一万六千多行，很容易迷失在 `io/` 的三种相机驱动里。

*可以放心抄的：*

- rm_vision 的*仓库组织与接口划分*——包的边界、消息定义、launch 编排里那套用启动顺序编码依赖关系的做法。这是它最好的部分，也是"最规范的框架"这个画像的来源。
- rm.cv.fans 的*方法论*，尤其那句"最好不要以效果为驱动力，应该以逻辑为驱动力"，以及把延迟拆成可命名时间点逐段核算的习惯。注意：*该项目全库无 LICENSE 文件，著作权人保留全部权利，只能学思想、不能抄代码*，这一点在「rm.cv.fans」一章已经详细说过。
- sp_vision 的*工程骨架*——分层目录、每台车一份 YAML、21 个独立测试程序、录制器与离线回放。这部分和算法几乎正交，迁移成本最低、收益最稳。它还有一份最容易被低估的资产：*把"假设失效就跳过"写进代码的习惯*，那四行平衡步兵判断值得你在自己的代码里找十个地方照做。
- 三家共有的*滤波器兜底机制*——任选一种，但必须有。

*别照抄的：*

- *任何具体常数*。延迟档位、NIS 阈值、半径夹紧区间、二值化阈值、观测方差系数——上文那张六台车延迟表已经证明，同一份代码里这些数能差一个数量级。它们是标定量，不是设计量。
- *绑死在特定电控上的补偿*。rm.cv.fans 那个正比于目标角速度的前馈项，修的是它自家下位机 PID 的稳态误差；sp_vision 提前若干毫秒的开火判断，假设你的拨弹延迟不抖。换一套电控，这些补偿会从"修正"变成"引入误差"。
- *写死的分辨率、写死的图案尺寸、散落多处的默认值*。sp_vision 那两处 1440×1080 的 TODO 和三个不同的默认弹速，都是现成的反面教材。
- *带 `TODO` 和已被注释掉的代码路径*。`SJTU_cost` 完整躺在文件里但调用点被注释——那是别人试过又退回去的路，你若不知道他为什么退，抄过去就是重蹈覆辙。

最后一条，也是读完这三个项目最该带走的：*去找那些"没成的记录"*。sp_vision 的三条反向记录、rm.cv.fans 那份五百余行留着全部失败的工程日志，价值高于它们任何一个成功的算法。成功的方案你能从代码里读出来，失败的路径只有作者愿意写，你才知道。等你自己的项目开源那天，记得也这么写。

==== 本章小结

sp_vision（即"同济自喵"）是三个解剖对象里工程完成度最高的一个：仍在维护，且是唯一给出赛场量化战绩的——国赛最高命中率 39.6%、正常发挥不低于 30%，2 m 处 300 血的步兵在 7 rad/s 下约 8 s、14 rad/s 下约 10 s 击杀，三个前提缺一不可。它的架构从一条硬件事实反推而来：*相机只能被一个进程打开*，于是一个 `main` 加多个功能组、每个兵种一份入口；协议从 8 字节的 CAN 长到 29 字节的串口，因为下发内容从"角度"长成了"位置＋速度＋加速度"。算法上，yaw 优化用 140 次暴力搜索把 pitch 先验换成精度，而*平衡步兵那四行 `if (is_balance) return;` 把假设的适用边界编码进了代码*，是全章最该模仿的一笔；11 维 EKF 追加了 `l`、`h` 两维，用自适应 $bold(R)$ 让"斜"和"远"自动降权，用间隔检查、越界判据、NIS 滑窗三道自检兜底——NIS 是三家里唯一的统计检验，机制值得抄，那个 0.711 的阈值和后验残差的口径必须自己重推。规划器的理论「自瞄算法设计」一章已讲透，这里只补了工程层：负 $Delta t$ 回推半个窗口保证起点可达、迭代上限 10 次宁次优不迟到、延迟分档的数值六台车差一个数量级。而最珍贵的是三条反向记录：MPC 当闭环控制器输给了下位机、自评更优的方案二因没时间验证而没上场、提前开火决策的前提是开火延迟不抖。

全篇至此收束。三个项目连成一条谱系：rm_vision 立下整车 EKF 的框架范式，rm.cv.fans 贡献火控词汇与延迟理论，sp_vision 综合两者再加上轨迹规划——证据是参考文献 [3][4] 与那个借鉴后又退回的 `SJTU_cost`。四条跨项目的教训：延迟要先测抖动再谈补偿；PnP 测距误差是结构性的，对策正从"事后降权"走向"事前纠正"；滤波器一定会发散，硬夹／拒绝／检验任选其一但必须有；文档一定会漂移，*读开源项目以代码为准*。

这也是全书的最后一章。前五篇一路在教你*把东西做出来*——从 Linux 上的第一个 C++ 工程，到相机模型与滤波，到一台能上场的步兵，再到雷达、哨兵、性能极限与赛场上的失效模式。这一篇教的是相反的动作：*把别人做出来的东西拆开*。两件事其实是一回事的两面，因为你此后接手的每一个工程，都是别人先做出来的。

所以最后留给你的不是一个结论，是一套动作：摸边界、顺数据流、挑信息密度最高的几十行精读、拿代码去校对文档、然后去找那些没成的记录。这三章的篇幅里，它被完整地走了三遍——rm_vision 用一个下午、rm.cv.fans 从文档读起、sp_vision 顺着 configs 和 issue 反查。换成任何一个陌生仓库，动作都不变。

也该诚实地说这本书没能覆盖什么。它几乎没有讲电控，而「实战经验与坑点总结」一章已经让你看到，自瞄的一半问题活在视觉与电控的接口上。它没有讲机械与结构，而镜头会松、线会断、云台的机械谐振会决定你的控制带宽。它对多机协同只开了个头，对深度学习的训练与数据工程只讲了够用的部分，对仿真与自动化测试着墨很少——而这几样恰恰是强队与普通队伍拉开差距的地方。更根本的是，规则每个赛季都在变，本书里凡是与规则相关的数字，都只是"写作时那一版"的快照。

还有一件事没有单独成章，但它其实是这本书的做法：*去测量*。书里最有价值的部分，无一例外都来自有人真的去量了一次——延迟的分布、内存的曲线、反汇编里指令的条数。观点可以争论，测量不能。如果你从头跟到这里并且动手写过，你带走的就不只是"做一套自瞄"的能力，而是*把一个含糊的物理目标拆成可验证的工程链路*的能力——这个能力和 RoboMaster 没有关系，它在任何一个把算法装进真实机器的行业里都成立。

书到这里就结束了，但这三个仓库还在往前跑，书里写下的行号、星标数和结论都会过时。真到你自己写一份让别人来拆的代码时，请记住本篇最费笔墨讲的两件事：把注释写在离代码最近的地方，以及*把失败也留在档案里*。

祝你上场顺利。打不中的时候，记得先问为什么，再改参数。
