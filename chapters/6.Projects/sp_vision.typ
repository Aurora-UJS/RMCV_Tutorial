#import "/template/template.typ": *

=== 架构：一个 main 组织多项功能

==== 为什么没有采用"一功能一程序"

sp_vision 的 README 用一个具体约束解释了它的软件架构。步兵既要自瞄又要打符，直观做法是分别启动两个程序；但在这个项目的相机驱动与部署方式下，两个程序不能同时打开同一台相机：

#blockquote[
  例如步兵需要自瞄和打符，显然，这两个功能都需要从相机获取图像，但是相机只能被一个程序打开，这会导致另一个程序无法正常工作（相机被占用）。
]

因此，仓库把自瞄、打符等功能拆成若干"功能组"（一组类和函数），由同一个 `main` 获取图像，再根据下位机发来的模式选择功能组；不同兵种各有自己的 `main`。另一种可行方案是单独建立取图服务，再通过进程间通信分发图像，但这会增加数据复制、同步和故障处理的复杂度。本项目选择的是单进程方案，而不是证明所有工业相机都只能这样组织。

这段说明的价值在于把约束和取舍连了起来。阅读陌生工程时，不妨先找这类信息：哪些限制来自当前硬件和驱动，哪些只是团队选定的实现方式。两者分清以后，才知道哪些结构可以照搬，哪些需要重新设计。

==== 六层目录与一个入口

仓库的分层一眼能看懂：

- `io/`：硬件抽象层。相机（海康、迈德威视、USB）、下位机（CAN 与串口两套）、IMU、ROS 接口各一个子目录。
- `tasks/`：功能层。`auto_aim/`、`auto_buff/`、`omniperception/`（哨兵全向感知）。
- `src/`：应用层，每个兵种或调试场景一个 `main`——`standard.cpp`、`sentry.cpp`、`uav.cpp`、`mt_standard.cpp`（多线程版步兵）等，共 13 个源文件。
- `tools/`：可复用工具，包括 EKF、CRC、线程安全队列、弹道、日志、YAML、绘图和录制器。
- `tests/`：从 `camera_test` 到 `planner_test_offline`，21 个可独立编译运行的测试程序。
- `configs/`：机器人与场景的 YAML 配置——`standard3.yaml`、`standard4.yaml`、`sentry.yaml`、`demo.yaml` 等。

想快速看清多线程步兵程序的数据流，可以读 137 行的 `src/mt_standard.cpp`。自瞄分支里，识别结果与图像时间戳一起出队，再按这个时间查询云台姿态：

```cpp
auto [img, armors, t] = detector.debug_pop();   // 图像 + 识别结果 + 时间戳
Eigen::Quaterniond q = cboard.imu_at(t - 1ms);  // 按时间戳取当时的云台姿态
```

#align(center)[#text(size: 9pt)[（本章此后引用的代码片段均摘自 `sp_vision_25` 提交 `58c6278`，Copyright (c) 2025 TongjiSuperPower，仓库根目录采用 MIT 许可证；为排版做过删节与换行调整，行内注释有中译。）]]

`CBoard::imu_at(t)` 会从队列中找到目标时刻前后的两帧四元数，再做球面线性插值（`slerp`）。四元数的时间戳取自 CAN 回调开始时的本地 `steady_clock`；海康与迈德威视相机驱动也都是在 SDK 返回图像缓冲区后读取本地时钟。也就是说，这些是主机侧接收时间，不是相机硬件给出的曝光时间。代码固定查询 `t - 1ms`，但源码没有说明这 1 ms 如何测得，因此只能把它称为固定补偿量，不能仅凭这行代码断定它准确对应曝光偏移。移植时应重新测量。

==== 从单帧 CAN 到串口结构体

`io/` 下保留了两套下位机通信实现，可以直接比较协议内容如何影响载体选择。

旧的是 `io::CBoard`，走 SocketCAN（USB2CAN 转接）。下发的数据结构 `io::Command` 只有五个字段：`control`、`shoot`、`yaw`、`pitch`、`horizon_distance`，打包进一帧 8 字节 CAN 报文——两个布尔各占一字节，三个量各按 $times 10^4$ 存成 `int16_t`，正好 8 字节，一个字节不剩。

新的是 `io::Gimbal`，走 MicroUSB 虚拟串口，发送结构体定义如下：

```cpp
struct __attribute__((packed)) VisionToGimbal {
  uint8_t head[2] = {'S', 'P'};
  uint8_t mode;   // 0: 不控制, 1: 控制云台但不开火, 2: 控制云台且开火
  float yaw, yaw_vel, yaw_acc;      // 三阶前馈
  float pitch, pitch_vel, pitch_acc;
  uint16_t crc16;
};
```

按 `packed` 布局计算，帧头 2 字节、模式 1 字节、六个 `float` 共 24 字节、CRC 2 字节，合计 29 字节；它确实放不进一帧经典 CAN 的 8 字节数据区。不过，这并不等于 CAN 在物理上不可用：还可以自行分包，或改用 CAN FD。仓库选择 MicroUSB 虚拟串口，是在协议长度、实现复杂度与现有硬件之间做出的取舍。能从源码确认的是"当前结构体不适合单帧经典 CAN"，不是"增加前馈后只能换串口"。

开火没有单独占一个线上的标志位，而是编码进 `mode`：0 不控制、1 控制但不开火、2 控制且开火。`send(bool control, bool fire, ...)` 通过 `tx_data_.mode = control ? (fire ? 2 : 1) : 0;`，不会从这个入口产生"不控制但开火"的组合。不过，线上的字段仍是 `uint8_t`，另一个重载也允许调用者直接传入 `VisionToGimbal`；因此这里是 API 入口处的约束映射，还不是由类型系统彻底排除非法值。

回传方向的 `GimbalToVision` 包含模式、四元数 `q[4]`、`yaw`／`yaw_vel`／`pitch`／`pitch_vel`、`bullet_speed`、`bullet_count` 与 CRC，共 43 字节。`standard_mpc.cpp` 和测试程序通过比较当前与上一次 `bullet_count` 判断是否出现了新的发弹事件。计数器能让上位机区分"发送了开火命令"和"下位机报告新增一发"；至于没有递增时究竟是热量限制、弹仓状态还是机构故障，仅靠这个字段无法诊断。

结合前两章可以看到三种接口形态：rm_vision 的视觉侧协议下发目标整车状态；rm.cv.fans 下发补偿后的目标角度；sp_vision 下发 yaw／pitch 的位置、速度、加速度以及开火模式。rm_vision 的接收端如何继续处理选板、弹道和开火，不在本章掌握的源码范围内，不能只凭发送字段补全。一般而言，状态级接口给接收端保留的处理空间更多，角度或运动指令级接口则要求视觉侧完成更多计算；具体职责仍取决于两端实现。调试时首先要确认某个量由哪一侧计算，否则容易在两端重复补偿。

=== yaw 优化：把规则先验写进搜索

装甲板的四个角点共面；在距离较远、接近正视等几何条件下，图像对装甲板 yaw 的约束可能较弱，像素扰动会明显影响姿态解。sp_vision 在 `Solver::optimize_yaw()` 中加入了一个带 pitch 先验的网格搜索：

```cpp
constexpr double SEARCH_RANGE = 140;  // degree
auto yaw0 = tools::limit_rad(gimbal_ypr[0] - SEARCH_RANGE / 2 * CV_PI / 180.0);
for (int i = 0; i < SEARCH_RANGE; i++) {          // 1° 步长，共 140 个候选
  double yaw = tools::limit_rad(yaw0 + i * CV_PI / 180.0);
  auto error = armor_reprojection_error(armor, yaw, ...);
  if (error < min_error) { min_error = error; best_yaw = yaw; }
}
armor.yaw_raw = armor.ypr_in_world[0];   // 留档：PnP 的原始输出
armor.ypr_in_world[0] = best_yaw;        // 覆盖
```

候选相对云台 yaw 的偏移从 $-70 degree$ 到 $+69 degree$，步长为 $1 degree$，共 140 个点。代码取重投影误差最小的候选覆盖 PnP 的 yaw，同时把原始值保存到 `yaw_raw`，便于比较优化前后的结果。穷举网格不会陷入某个局部优化器的收敛问题，但精度受 $1 degree$ 网格限制，运行开销也需要在目标硬件上测量；源码本身没有给出这 140 次重投影的耗时。

关键在重投影函数里那一行：

```cpp
auto pitch = (name == ArmorName::outpost) ? -15.0 * CV_PI / 180.0 : 15.0 * CV_PI / 180.0;
```

重投影时，普通装甲板的 pitch 固定为 $+15 degree$，前哨站固定为 $-15 degree$。正负号对应源码坐标约定，不能脱离坐标系直接解释成前倾或后仰。这个先验把搜索变量缩减为 yaw；其数值和适用兵种都属于赛季相关假设，使用时应对照当季规则与机械安装。

==== 把假设的适用边界写进代码

这个先验的适用边界写在 `Solver::solve()` 末尾：

```cpp
// 平衡不做yaw优化，因为pitch假设不成立
auto is_balance = (armor.type == ArmorType::big) &&
                  (armor.name == ArmorName::three || armor.name == ArmorName::four ||
                   armor.name == ArmorName::five);
if (is_balance) return;
optimize_yaw(armor);
```

源码把"大装甲板且编号为 3、4、5"识别为平衡步兵，并注明其 pitch 假设不成立，因此直接返回，保留 PnP 原始姿态。这里没有尝试让同一组先验覆盖所有兵种，而是把适用范围写进了控制流。

这种写法有两个直接好处：读者能看到优化依赖固定 pitch；新增兵种或机械结构改变时，也有明确位置需要重新检查。边界条件仍可能随赛季过期，所以代码分支不能替代规则和实车数据的复核。

为了观察 pitch 失配可能如何影响这个搜索，下图构造了一个固定的合成几何。脚本采用 `standard4.yaml` 的相机内参、畸变和手眼外参，目标位置、真实 yaw 与角点高斯噪声则由脚本指定。它在计算代价时固定目标平移，没有对每次扰动重新执行 IPPE PnP，因此展示的是*给定位置条件下的搜索敏感性*，不是完整自瞄管线或实车命中率实验。

#figure(
  image("images/proj-sp-yaw-optimize.png", width: 100%),
  caption: [固定合成姿态下的 pitch 先验敏感性（脚本 `proj_sp_yaw_optimize.py`）。左图用稠密采样显示同一代价函数的形状；实际代码仍使用相对云台 yaw 从 $-70 degree$ 到 $+69 degree$ 的整数网格。真实 pitch 为 $0 degree$ 和 $-15 degree$ 时出现第二个局部极小值，两处代价差分别约为 3.2 px 和 1.1 px。右图在每个 pitch 上加入 600 组、每个角点坐标标准差为 1 px 的高斯噪声，并把 $|"yaw error"| > 10 degree$ 定义为大误差。真实 pitch 为 $15 degree$、$0 degree$、$-15 degree$ 时，大误差率分别为 1.8%、15.5%、48.0%，yaw 的 RMS 误差分别为 $4.1 degree$、$10.9 degree$、$18.9 degree$。这些数字只对应图中设定和固定随机种子，不能外推为平衡步兵的实测失败率。],
)

这个实验支持一个有限结论：在所选几何与噪声模型下，pitch 失配与第二个局部极小值及更高的大 yaw 误差率同时出现。它没有证明所有姿态都会如此，也没有证明 PnP 原始误差连续有界、优化结果一定导致 EKF 发散。实际部署时，应在录制数据上同时统计 `yaw_raw`、优化后 yaw、重投影代价差和后续跟踪残差，再决定哪些兵种应跳过优化。

=== 十一维整车模型与跟踪检查

==== 状态、噪声与前哨站先验

sp_vision 的整车 EKF 是 11 维，状态定义就写在 `target.cpp` 的注释里：

```text
x vx y vy z vz a w r l h
a: angle    w: angular velocity
l: r2 - r1  h: z2 - z1
```

前八维分别描述旋转中心的三轴位置与速度，以及自旋角和角速度；第九维 `r` 是一组装甲板的旋转半径。`l` 和 `h` 表示另一组装甲板相对它的半径差与高度差。rm_vision 用两个交替半径表达类似几何，sp_vision 则把差值直接放进状态量。观测方程里的开关是：

```cpp
auto use_l_h = (armor_num_ == 4) && (id == 1 || id == 3);
auto r = (use_l_h) ? x[8] + x[9] : x[8];
```

只有模型含四块装甲板且当前编号为 1 或 3 时，预测位置才使用 `r + l` 与 `z + h`。三块板模型不会进入这个分支；这是代码采用的几何约束，不能仅凭实现进一步断言实物严格对称。

过程噪声 $bold(Q)$ 的代码注释引用了分段白噪声模型（Piecewise White Noise）。每个位置—速度二阶子块都可写成 $v dot bold(g) bold(g)^T$，其中 $bold(g) = [(Delta t)^2 \/ 2, Delta t]^T$。因此会出现 $(Delta t)^4 \/ 4$、$(Delta t)^3 \/ 2$ 与 $(Delta t)^2$ 三类系数。只要 $v >= 0$，这个外积在 $Delta t != 0$ 时秩为 1，在 $Delta t = 0$ 时秩为 0，始终半正定；$Delta t$ 为负时，非对角项会随之变号，但矩阵仍半正定。这个代数性质说明后文的负步长不会单纯因为 $bold(Q)$ 而产生非半正定协方差；它并不验证反向预测的随机模型符合真实运动。

前哨站另有两组硬编码先验：

```cpp
if (name == ArmorName::outpost) { v1 = 10;  v2 = 0.1; }   // 加速度方差 / 角加速度方差
else                            { v1 = 100; v2 = 400; }
// 满足项目定义的 convergened() 后，将较高转速改为固定值
if (this->convergened() && this->name == ArmorName::outpost && std::abs(this->ekf_.x[7]) > 2)
  this->ekf_.x[7] = this->ekf_.x[7] > 0 ? 2.51 : -2.51;
```

前哨站的平动噪声系数 `v1` 从 100 降为 10，角运动噪声系数 `v2` 从 400 降为 0.1，表示代码更信任其匀速旋转模型。随后，`convergened()` 返回真且 $|omega| > 2$ 时，状态会在下一次预测前被改为 $plus.minus 2.51$ rad/s。需要注意，`convergened()` 这个名字容易让人误解：对前哨站，它实际只检查更新次数超过 10 且两个半径落在允许范围，并没有进行统计收敛检验。$2.51 approx 0.8 pi$ 很像赛季规则值，但源码没有附带规则版本；移植时仍应对照当季手册。$|omega| > 2$ 的门槛则避免在较低估计转速下立即套用固定值。

还有第三层藏在初始化里。`tracker.cpp` 给不同兵种配不同的初始协方差对角：

```cpp
else if (armor.name == ArmorName::outpost) {
  Eigen::VectorXd P0_dig{{1, 64, 1, 64, 1, 81, 0.4, 100, 1e-4, 0, 0}};
  target_ = Target(armor, t, 0.2765, 3, P0_dig);   // 半径 0.2765 m，3 块装甲板
}
```

`l` 和 `h` 的初始方差为 0，状态转移和 $bold(Q)$ 也不向这两维注入不确定性，因此它们在这套矩阵结构中保持为 0。代码由此把三块板模型的半径差和高度差固定下来。`r` 的初始方差则是 $10^(-4)$，不是 0，所以半径仍可被观测更新，只是初始置信度较高。

这体现了先验的两种强度：较小方差表示"可以更新，但初始时较相信给定值"；零方差且零过程噪声则表示"不估计这个量"。后一种做法只有在结构约束确实成立时才合理，否则滤波器没有机会从数据中纠正它。

==== 观测噪声 R：随角度与距离调整权重

观测量是 4 维：方位角、俯仰角、距离 $d$ 与装甲板朝向角。代码里保留了一行常量版 $bold(R)$ 的注释，当前实现则按观测几何调整后两项：

```cpp
// Eigen::VectorXd R_dig{{4e-3, 4e-3, 1, 9e-2}};          // 旧：四个常数
auto center_yaw = std::atan2(armor.xyz_in_world[1], armor.xyz_in_world[0]);
auto delta_angle = tools::limit_rad(armor.ypr_in_world[0] - center_yaw);
Eigen::VectorXd R_dig{
  {4e-3, 4e-3, log(std::abs(delta_angle) + 1) + 1,
   log(std::abs(armor.ypd_in_world[2]) + 1) / 200 + 9e-2}};
```

`delta_angle` 是装甲板朝向与视线方向之差。两个调整项分别是：

- 距离方差项随 $|Delta theta|$ 增大：$log(|Delta theta| + 1) + 1$。
- 朝向角方差项随距离增大：$log(d + 1) \/ 200 + 9 times 10^(-2)$。

当 $Delta theta = 0$ 或 $d = 0$ 时，$log(1) = 0$，两项分别回到注释中常量版的 1 和 0.09；其余情况下只会在这个基线上增大。数学上，这是一组连续权重，不会在某个角度阈值处突然切换。

这些公式编码了两个假设：装甲板越侧斜，测距越不可靠；目标越远，朝向角越不可靠。「rm.cv.fans」一章引用的该项目日志记录过侧斜装甲板的测距偏差，并采用条件拒绝更新半径。sp_vision 选择增大 $bold(R)$ 的相应对角项，让卡尔曼增益连续降低观测权重。两者目的相近，但当前仓库没有提供这两个对数公式的标定数据，不能仅从表达式判断哪种策略在实车上更好。

对新手而言，这段代码最重要的提示是：$bold(R)$ 可以描述每次观测不同的可信度，不必始终是常数。具体函数仍需用自己的残差数据验证；函数单调、连续并不自动等于噪声模型正确。

==== 两类硬边界与一项残差窗口规则

常规 `Tracker::track(armors, t)` 路径里有三项检查：

```cpp
if (state_ != "lost" && dt > 0.1) {              // 一、两帧间隔超 100 ms
  tools::logger()->warn("[Tracker] Large dt: {:.3f}s", dt);
  state_ = "lost";
}
if (state_ != "lost" && target_.diverged()) {    // 二、物理量跑出合理范围
  state_ = "lost"; return {};
}
if (std::accumulate(target_.ekf().recent_nis_failures.begin(),   // 三、残差窗口
                    target_.ekf().recent_nis_failures.end(), 0)
    >= (0.4 * target_.ekf().window_size)) {
  tools::logger()->debug("[Target] Bad Converge Found!");
  state_ = "lost"; return {};
}
```

第一项在相邻时间戳间隔超过 100 ms 时把状态改为 `lost`。源码注释把相机离线列为一种可能，但调度停顿、处理阻塞或时间戳异常也能产生同样现象，所以这是一项帧间隔检查，不是相机故障诊断。第二项要求 $r$ 与 $r + l$ 都位于 $(0.05, 0.5)$ m；超出后丢弃当前目标。作为对照，rm_vision 的实现会把半径限制在给定区间。前者会产生重新检测的空窗，后者能保持连续输出；哪种行为更适合，要结合误状态的代价与重新捕获时间评估。

第三项把一个标为 `nis` 的量与阈值比较，并在最近最多 100 个标志中累计失败次数；总数达到 40 时就丢弃目标。持续的大残差可能和模型失配、数据关联错误或观测异常同时出现，但这个标量本身不能区分原因。

这里必须核对统计量的定义。标准 NIS 使用*更新前*的新息 $bold(nu) = bold(z) - h(hat(bold(x))^-)$ 和先验新息协方差 $bold(S) = bold(H) bold(P)^- bold(H)^T + bold(R)$。在观测维数为 4 且模型假设成立时，再用卡方分布评估它。

当前实现与这个定义有两处关键差别：

- 阈值写为 `0.711`，注释却说"自由度 = 4，置信水平 95%"。$0.711$ 约为 $chi^2_4$ 的下 5% 分位点；常用的 95% 上分位点约为 $9.488$。若把标准 NIS 大于 0.711 记为失败，模型成立时理论上也会有约 95% 的样本超过它。
- 代码先更新 $bold(x)$ 与 $bold(P)$，再用后验状态残差 `z - h(x)` 和后验协方差构造 $bold(S)$。这个量不服从标准 NIS 的上述分布，因此不能直接套用 NIS 的卡方阈值。

同一文件还计算了名为 `nees` 的量，但它使用的是本次状态修正 `x - x_prior`；标准 NEES 需要真实状态与估计状态之差。该量也不应按标准 NEES 解读，而且跟踪器没有用它触发重置。

因此，能从源码确认的是"后验残差统计量加滑动窗口阈值"这一启发式，不能确认 0.711 经过经验标定，也不能确认两个定义偏差会相互抵消。若要实现标准 NIS，应保存更新前的新息与 $bold(P)^-$；若保留现有统计量，则需要用录制数据分别统计正常跟踪和错误跟踪的分布，再选择阈值。另一个容易漏掉的范围边界是：全向感知使用的 `track(detection_queue, armors, t, ...)` 重载包含帧间隔与半径检查，却没有这项残差窗口重置。

=== 规划器的代码边界

「自瞄算法设计」一章已经介绍目标轨迹、射击轨迹、加速度约束与提前减速。本节只核对 `Planner` 如何建立时间窗、设置求解器，以及配置延迟参数。

*时间窗如何对齐。* `DT = 0.01` s、`HALF_HORIZON = 50`、`HORIZON = 100`。在进入下列函数前，`plan()` 已先把目标向前预测一个估计的弹丸飞行时间，使后续目标轨迹成为对应的射击参考。采样函数开头再执行：

```cpp
target.predict(-DT * (HALF_HORIZON + 1));   // 先把目标状态往回倒
auto yaw_pitch_last = aim(target, bullet_speed);
target.predict(DT);   // [0] = -HALF_HORIZON * DT -> [HALF_HORIZON] = 0
```

第一次回推是 $-0.51$ s；再前进一步后，轨迹第 0 列位于参考中心前 0.5 s，第 50 列位于参考中心，第 99 列位于其后 0.49 s。求解器把第 0 列直接设为初始状态：`x0 << traj(0, 0), traj(1, 0);`，pitch 同理；整段 100 列也都进入 `Xref`。因此，过去半窗既提供了一个人为初始条件，也参与代价计算，让求解结果在第 50 列输出前已有 50 个受加速度约束的离散步。

这个初始状态来自参考轨迹，不是实测云台角。`standard_mpc.cpp` 虽然读取了 `GimbalState`，调用规划器时只传入目标和弹速；`Planner` 内也没有云台状态输入，所以这里不是以云台反馈闭环更新的 MPC 控制器。负步长下的 $bold(Q)$ 在代数上仍半正定，但这只保证协方差矩阵的形式合法，不代表回推得到的历史状态经过实测验证。

*求解器最多迭代 10 次。* yaw 与 pitch 求解器都把 `max_iter` 从 TinyMPC 的默认 1000 改成 10。`tiny_solve()` 若满足默认的 $10^(-3)$ 原始／对偶残差阈值就返回 0，否则在 10 次后保存当前解并返回 1；`Planner` 没有检查这个返回值，而是继续读取 `work->x` 与 `work->u`。这明确限制了迭代次数，却不保证 10 次内收敛，也不单独保证运行时间。`standard_mpc.cpp` 的规划线程每次发送后 `sleep_for(10ms)`，实际周期还要加上本轮计算时间。

*延迟按角速度分档。* 规划器根据目标角速度绝对值选择两组配置之一：

```cpp
double delay_time = std::abs(target->ekf_x()[7]) > decision_speed_
                    ? high_speed_delay_time_ : low_speed_delay_time_;
```

代码只规定"超过 `decision_speed` 用高速档"，并不强制高速档一定更大。固定仓库中有七个 YAML 包含这三个字段，取值如下：

#block(breakable: false)[
#figure(
table(
  columns: (auto, auto, auto, auto),
  align: (left, right, right, right),
  stroke: (x, y) => (
    top: if y == 0 { 1pt } else { 0pt },
    bottom: if y == 7 { 1pt } else { 0pt },
  ),
  inset: 7pt,
  table.header([*配置文件*], [`decision_speed`], [高速档], [低速档], table.hline(stroke: 1pt)),
  [`standard3.yaml`], [7 rad/s], [0 ms], [0 ms],
  [`standard4.yaml`], [8 rad/s], [15 ms], [15 ms],
  [`sentry.yaml`], [10 rad/s], [26 ms], [10 ms],
  [`uav.yaml`], [12 rad/s], [5 ms], [5 ms],
  [`demo.yaml`], [8 rad/s], [30 ms], [15 ms],
  [`ascento.yaml`], [7 rad/s], [50 ms], [15 ms],
  [`mvs.yaml`], [7 rad/s], [66 ms], [15 ms],
),
caption: [七个配置文件中的角速度阈值与附加延迟。三个配置把高速档和低速档设成相同值，此时角速度分档不改变延迟；其余四个配置的高速档较大。非零高速档从 5 ms 到 66 ms，相差约 13 倍。表格只能证明这些字段存在且取值不同：其中部分文件缺少当前 `Planner` 构造还会读取的其他键，因此不能据此证明七份配置都能直接启用该规划器，更不能证明每组都经过实车标定。README 只明确说国赛时步兵采用了该规划器。],
)
]

README 把除图像处理外的延迟总和称为调试参数，并说可通过慢动作视频、击杀时间等方式调整，约为 15 ms。这为"需要按系统调整"提供了项目方说明；配置差异本身只能说明参数是配置相关的。相同的常数也可能只是复制默认值，不同的常数也可能已经过期，因此仍应记录曝光、处理、通信、控制和出膛事件，按自己的链路重新测量。

`standard3.yaml` 的两档均为 0，表示不再增加这两个配置项提供的额外偏移；`Planner` 仍会把目标从图像时间预测到当前时刻，并考虑弹丸飞行时间。它与 README 所举的 15 ms 不一致，但现有材料无法区分这是车辆差异、后续调参还是文档未同步，使用者需要以目标车的时间测量为准。

=== README 中的负结果与适用条件

sp_vision 的 README 不只描述最终方案，也记录了一次效果不佳的实车尝试、一个只做过仿真的备选方案，以及开火决策所依赖的条件。三者的证据等级不同，不能统称为"已经被实测否定"，但都能帮助读者判断哪些结论可以迁移。

一、MPC 只当求解器，不当闭环控制器。 原文：

#blockquote[
  这里MPC并非闭环控制器，而是作为求解器寻找可行轨迹解，其输入不包含云台的实际状态。我们也尝试过使用MPC直接作为闭环控制器，通过下位机转发MPC求解后的力矩指令给电机，当自身小陀螺时，控制效果不如下位机方案。
]

当前 `Planner` 的源码与这段说明一致：它用 TinyMPC 求一条受加速度约束的参考轨迹，输入中没有实际云台状态，因此不是闭环 MPC。README 还报告，团队曾让 MPC 直接输出力矩并通过下位机转发，但在机器人自身旋转的工况下，控制效果不如下位机方案。

README 没有给出两套控制器的模型、带宽、参数和对比曲线，所以它支持的只是"该团队测试的实现，在该工况下不如其下位机方案"，不能推出所有上位机闭环控制都更差。自旋时的动力学耦合、通信延迟、模型误差和控制器调参都可能影响结果；需要固定同一轨迹与硬件、记录实际状态和力矩，才能区分这些因素。

二、只完成仿真的方案二没有上场。 提前减速有两种实现。README 对方案二（五次多项式显式搜索）的评价包括"确定性高"、"跟随段不会参与优化，和射击轨迹重合"、"高转速下更加稳定"与"实现更加轻量，无需依赖第三方库"，随后说明：

#blockquote[
  由于时间有限，方案二仅进行了仿真验证，未上车测试，国赛上场采用了方案一。
]

"确定性高"、"高转速下更稳定"等是作者对方案二的评价。现有材料只能确认项目方报告它做过仿真、没有上车；本书没有拿到仿真数据、指标和结果，无法独立核验它是否满足设计目标，更不能据此断言它在比赛环境中优于方案一。README 将时间有限、未完成上车验证与国赛采用方案一并列说明，但没有提供更完整的决策记录。这个例子仍能提醒读者：技术选型除了比较理论性质，也要考虑剩余验证时间和回退方案。

三、提前开火决策依赖稳定的开火延迟。 `Planner` 用 `shoot_offset_ = 2` 比较第 52 列的参考与规划轨迹；在 `DT = 10 ms` 下，相当于查看 20 ms 后的误差。README 将这种偏移解释为从开火命令到弹丸出膛的延迟 $t_"fire"$，并明确限定：

#blockquote[
  该方案的前提是开火延迟无波动，对机械和电控的要求较高。
]

进阶篇「实战经验与常见问题」给过一个不同系统的对照：rm.cv.fans 的公开材料自述了 35～110 ms、均值约 82 ms 的实车结果。不过，其取差终点是裁判系统射击数据更新，图像所绑定时钟值的新鲜度、曝光事件位置和报告路径也没有独立标定；因此本书把这三个数字称为*项目报告的延迟代理范围*，不把它们写成已经测得的曝光到出膛物理延迟分布。它既不是 sp_vision 的数据，也不是单独的命令到出膛延迟，因而不能用来估计这里的 20 ms 是否正确；它能说明的是，单看一个平均值可能遗漏代理量的变化范围。对自己的系统，可以先用发弹计数或裁判系统事件构造可重复的"命令发送—报告到达"代理量；若要得到"命令发送—出膛"分布，还需用经过标定的发射传感器或高速相机确定物理出膛时刻，再判断固定偏移是否足够，是否需要给开火门限留出时间不确定度。

把负结果、未验证方案和方法前提写进 README，能防止后来的使用者把当前实现误当作唯一答案。阅读时还要继续追问测试范围：在哪台车、什么工况、比较了哪些指标；缺少这些信息时，就保留项目方报告的原始范围。

=== 代码 TODO、外部问题报告与未完成事项

这一节把源码中可直接确认的 TODO、GitHub 使用者报告的问题和 README 的未来计划分开讨论。issue 能提示检查方向，但除非问题在固定版本中复现并排除其他原因，不能直接当作上游项目的已确认缺陷。

*TinyMPC 的返回状态。* issue #8 在本章核对时（2026-07-24）仍为 open，提问者报告 `tiny_solve()` 持续返回 1。后续评论称残差仍在下降，把迭代数提高到 1000 后可以达到默认 $10^(-3)$ 阈值，而 10 次得到的轨迹效果也尚可。源码确实把 `max_iter` 设为 10 并忽略返回值，这与报告相容；本章没有在提问者的数据和硬件上复现，因而不能把原因定为 TinyMPC 缺陷，也不能称其为国赛中的故障。更直接的改进是记录返回值、迭代次数和四类残差，并明确规定未收敛时继续使用当前解、沿用上次解还是回退。

*一条尚未定位的标定求助。* issue #9 同样为 open，标题说计算出的旋转矩阵与物理安装关系差别很大；正文同时说明提问者修改了手眼标定代码。该 issue 没有维护者回复、数据集或复现步骤，所以不能据此认定固定上游版本存在标定缺陷，更不能把欠约束写成根因。合理的最小检查是：先在未修改程序上用一组已知变换的合成数据验证坐标约定，再用覆盖多轴旋转的实测数据复现；这样才能区分代码改动、坐标系方向与采样激励不足。

*两处写死的图像中心。* 跟踪器里有两处一模一样的代码：

```cpp
cv::Point2f img_center(1440 / 2, 1080 / 2);  // TODO
```

这两处都用于优先选择靠近图像中心的装甲板，并附有 `TODO`。当输入不是 1440×1080 时，程序仍能运行，但排序参照点不再是实际图像中心。仓库包含海康、迈德威视和 USB 相机实现，因此更稳妥的写法是从当前 `cv::Mat` 尺寸或相机配置取得中心，并为不同分辨率加一个排序测试。

*默认弹速与有效范围不一致。* 规划器在弹速小于 10 或大于 25 m/s 时改用 22，普通瞄准器在小于 14 m/s 时改用 23，打符瞄准器在小于 10 m/s 时改用 24。于是输入 12 m/s 时，规划器和打符路径会保留原值，普通瞄准器会替换为 23。`standard_mpc.cpp` 与 `standard.cpp` 分属不同程序，但 `mt_standard.cpp` 同时包含普通自瞄和打符路径，切换模式就会改变同一输入的解释。默认值、有效范围与数据来源应集中定义，并在模式切换测试中覆盖边界值 10、14 和 25 m/s。

*识别模型的可复现范围。* README 引用了深圳大学 RobotPilots 与北京科技大学 Reborn 的识别工作，默认配置选择 `yolov5`；`assets/` 提供 ONNX 和 OpenVINO 的 `.xml`／`.bin` 推理文件。issue #7 请求原始权重，维护者回复其 YOLOv5 使用深圳大学开源模型，团队自己也没有原始权重，随后关闭 issue。仓库提供了推理代码所引用的模型文件，但本次复核没有在项目的 OpenVINO 与目标硬件环境中运行推理。仓库也不包含原始检查点、训练配置和优化器状态，无法据此完整复现训练或继续同一训练过程；若要迁移到其他推理框架，应先确认目标框架能否从现有 ONNX 转换，并核对上游模型的许可条件。

*未完成的自身平移补偿。* README 说明当前自瞄没有考虑机器人自身移动：步兵冲下坡时，静止目标会被估计为正在移动，前几发容易打偏；计划引入轮式里程计。源码中的世界坐标变换确实只使用 IMU 姿态旋转，没有输入底盘平移。若坐标原点随机器人平移，静止目标在该坐标中会呈现反向相对运动，因此这套解释与代码和 README 一致。不过，仓库没有提供该场景的日志或消融实验，尚不能排除延迟、姿态误差等同时存在的影响。加入里程计后，应比较静止目标在移动前后的估计速度，才能验证补偿效果。


==== 本章小结

sp_vision 是同济大学 SuperPower 战队公开的 25 赛季自瞄仓库，根目录采用 MIT 许可证。它用一个进程内的 `main` 和多个功能组共享相机；用 140 个、间隔 $1 degree$ 的候选做 yaw 重投影搜索，并在 pitch 先验不适用的平衡步兵分支跳过优化；用 11 维状态表示中心运动、旋转与两组装甲板的几何差异。跟踪器包含帧间隔和半径硬边界，以及一项不能按标准 NIS 解读的后验残差窗口规则。规划器把 TinyMPC 用作参考轨迹求解器，而非闭环云台控制器，并在 10 次迭代后继续使用当前工作区解。

这一章最适合迁移的不是某个常数，而是核对方法：把源码、配置、README 与 issue 放在一起读，区分代码当前行为、项目方经验报告、合成实验和实车证据。像 $15 degree$ pitch、2.51 rad/s 转速、0.711 阈值与 20 ms 开火偏移这样的数字，都必须重新检查赛季、车型与统计定义。

本书正文到此结束。祝你调试顺利；遇到偏差时，先记录现象和时间链路，再决定要改模型、代码还是参数。
