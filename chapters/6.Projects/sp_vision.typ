#import "/template/template.typ": *

前两章分别从 ROS 2 工程和传统 C++ 工程切入自瞄系统，最后来看 sp_vision，正好可以把两条线接起来：它没有采用 ROS 2 节点拆分整条链路，而是在一个进程里组织相机、识别、跟踪、规划和下位机通信。阅读这种规模的仓库，不必一开始就钻进每个类。我们先顺着主程序理清数据流，再挑四类最值得迁移的设计继续读：通信协议、姿态先验、整车跟踪和轨迹规划。途中遇到的 TODO 与 issue，则用来练习怎样判断“代码已经做了什么”和“项目还缺什么”。

=== 架构：一个 main 组织多项功能

==== 为什么没有采用"一功能一程序"

sp_vision 的 README 用一个具体约束解释了它的软件架构。步兵既要自瞄又要打符，直观做法是分别启动两个程序；但在这个项目的相机驱动与部署方式下，两个程序不能同时打开同一台相机：

#blockquote[
  例如步兵需要自瞄和打符，显然，这两个功能都需要从相机获取图像，但是相机只能被一个程序打开，这会导致另一个程序无法正常工作（相机被占用）。
]

因此，仓库把自瞄、打符等功能拆成若干“功能组”（一组类和函数），由同一个 `main` 获取图像，再根据下位机发来的模式选择功能组；不同兵种各有自己的 `main`。也可以单独建立取图服务，再通过进程间通信分发图像，但那样还要处理数据复制、同步和进程故障。sp_vision 根据自己的相机驱动与部署方式选择了单进程，这是一项工程取舍，不是工业相机程序的通用规则。

这段 README 很值得新手留意，因为它先讲约束，再讲架构。阅读陌生工程时也可以照这个顺序追问：限制来自硬件、驱动还是部署方式？当前结构是不得不这样写，还是团队在几个可行方案中作出的选择？分清以后，才知道哪些部分可以直接借鉴。

==== 六层目录与一个入口

先看目录，不急着看算法。仓库大致分成六层：

- `io/`：硬件抽象层。相机（海康、迈德威视、USB）、下位机（CAN 与串口两套）、IMU、ROS 接口各一个子目录。
- `tasks/`：功能层。`auto_aim/`、`auto_buff/`、`omniperception/`（哨兵全向感知）。
- `src/`：应用层，每个兵种或调试场景一个 `main`——`standard.cpp`、`sentry.cpp`、`uav.cpp`、`mt_standard.cpp`（多线程版步兵）等，共 13 个源文件。
- `tools/`：可复用工具，包括扩展卡尔曼滤波器（EKF）、循环冗余校验（CRC）、线程安全队列、弹道、日志、YAML、绘图和录制器。
- `tests/`：共有 21 个测试源文件，覆盖相机测试、离线规划测试和 ROS 2 通信等入口。相机测试名为 `camera_test`，离线规划测试名为 `planner_test_offline`。ROS 2 的发布、订阅和话题循环测试只在相关依赖齐全时加入构建，对应入口为 `publish_test`、`subscribe_test` 和 `topic_loop_test`。相机、CAN、串口与 IMU 等测试还需要对应硬件，不能把“有独立入口”理解为在任意机器上都能完成有效测试。
- `configs/`：存放机器人与场景的 YAML 配置。示例包括：
  - `standard3.yaml`
  - `standard4.yaml`
  - `sentry.yaml`
  - `demo.yaml`

目录也把本章的取舍说清了：仓库不只有普通自瞄，还包括打符、哨兵全向感知、标定、部署和各类硬件测试。为了和前两个项目比较同一条自瞄主线，下面会重点沿 `mt_standard.cpp` 进入识别、跟踪、规划和通信；没有逐段展开的模块仍然属于仓库，不能据此写成“项目没有提供”。

如果只选一个入口追主线，137 行的 `src/mt_standard.cpp` 很合适。自瞄分支先取出图像、识别结果和时间戳，再按这个时间查询云台姿态：

```cpp
auto [img, armors, t] = detector.debug_pop();   // 图像 + 识别结果 + 时间戳
Eigen::Quaterniond q = cboard.imu_at(t - 1ms);  // 按时间戳取当时的云台姿态
```

#align(center)[#text(size: 9pt)[（本章代码均摘自 `sp_vision_25` 提交 `58c6278`；为排版做过删节与换行，行内注释有中译。）]]

这里先停一下，看看 `t` 究竟代表什么。`CBoard::imu_at(t)` 会从队列中找到目标时刻前后的两帧四元数，再做球面线性插值（`slerp`）。四元数时间戳取自 CAN 回调开始时的本地 `steady_clock`；海康与迈德威视相机驱动同样是在 SDK 返回图像缓冲区后读取本地时钟。它们记录的是主机收到数据的时刻，不是相机曝光时刻。代码固定查询 `t - 1ms`，相当于再加一项 1 ms 补偿；源码没有交代这个数怎样测得，所以移植到自己的链路时要重新测量，而不是照抄。

==== 从单帧 CAN 到串口结构体

`io/` 下保留了新旧两套下位机通信实现。把它们放在一起看，能直观看出一件事：传输方式往往不是先选好的，协议里要装多少信息也会反过来影响选择。

旧的是 `io::CBoard`，走 SocketCAN（USB2CAN 转接）。下发的数据结构 `io::Command` 只有五个字段：`control`、`shoot`、`yaw`、`pitch`、`horizon_distance`，打包进一帧 8 字节 CAN 报文——两个布尔各占一字节，三个量各按 $times 10^4$ 存成 `int16_t`，正好 8 字节，一个字节不剩。

#block(breakable: false)[
新的是 `io::Gimbal`，走 MicroUSB 虚拟串口，发送结构体定义如下：

```cpp
struct __attribute__((packed)) VisionToGimbal {
  uint8_t head[2] = {'S', 'P'};
  uint8_t mode;   // 0: 不控制, 1: 控制云台但不开火, 2: 控制云台且开火
  float yaw, yaw_vel, yaw_acc;      // 三项前馈：位置、速度、加速度
  float pitch, pitch_vel, pitch_acc;
  uint16_t crc16;
};
```
]

在上下位机都采用 4 字节 `float` 的 ABI 时，`packed` 布局中的帧头占 2 字节、模式占 1 字节、六个 `float` 共 24 字节、CRC 占 2 字节，合计 29 字节，已经放不进一帧经典 CAN 的 8 字节数据区。仓库改用 MicroUSB 虚拟串口，省去了自行分包和重组的工作；如果换成 CAN FD 或设计分包协议，CAN 仍然可以承载这些信息。因此，更准确的结论是“当前结构体不适合单帧经典 CAN”，而不是“加入前馈就只能使用串口”。

这里的 ABI 是编译器与平台对类型大小和内存布局的约定。`packed` 只取消成员之间的填充，不会规定多字节字段的端序，也不会保证两端采用相同的浮点编码。源码还只断言两个结构体“不超过 64 字节”，没有断言精确长度。直接发送结构体内存虽然省事，却要求上下位机的这些约定完全一致。

再往下读一层还能看到，发送端没有检查 `serial_.write()` 实际写入多少字节，接收辅助函数也只调用一次 `serial_.read()`，没有一次读满就返回失败。串口在目标机器上是否真的出现过短写或短读，需要运行日志才能回答；从接口设计看，更稳妥的做法是明确字段宽度、端序和浮点表示，断言精确帧长，并循环完成剩余读写；也可以把这些工作交给专门的协议收发层，由它负责收满整帧、寻找帧头并完成校验。

开火没有单独占一个线上的标志位，而是编码进 `mode`。它有三种约定值：0 表示不控制，1 表示控制但不开火，2 表示控制且开火。入口函数 `send(bool control, bool fire, ...)` 通过下面的三元表达式完成映射：

```cpp
tx_data_.mode = control ? (fire ? 2 : 1) : 0;
```

因此，这个入口不会产生“不控制但开火”的组合。不过，它只替调用者避开了这一种无效组合：线上的字段仍是 `uint8_t`，另一个重载也允许直接传入 `VisionToGimbal`，类型本身没有把其他非法值排除掉。

回传方向使用 `GimbalToVision`。它依次携带模式、四元数 `q[4]`、四个云台状态字段（`yaw`、`yaw_vel`、`pitch`、`pitch_vel`）、`bullet_speed`、`bullet_count` 与 CRC。在同一 4 字节 `float` ABI 下，这个包共 43 字节。

再看弹丸计数的使用方式，三个文件并不相同：

- `auto_aim_debug_mpc.cpp` 与 `gimbal_test.cpp`：通过 `current > last` 比较当前值与上一次 `bullet_count`，判断下位机是否报告了新发弹事件。
- `standard_mpc.cpp`：只声明了 `last_bullet_count`，没有实际比较。

这个细节很适合用来检查边界：`bullet_count` 是 `uint16_t`，从 65535 回绕到 0 时，简单的大小比较会漏掉一次变化，长期运行的代码应使用无符号序号差或约定清楚的回绕规则。计数器能区分“上位机发出了开火命令”和“下位机报告新增一发”，但它不能说明未发弹究竟是热量限制、弹仓状态还是机构故障；若要诊断，还需要更具体的状态字段。

把三个项目连起来看，接口层次并不相同：rm_vision 的视觉侧协议下发目标整车状态，rm.cv.fans 下发补偿后的目标角度，sp_vision 则下发 yaw／pitch 的位置、速度、加速度和开火模式。状态级接口通常给接收端留下更多处理空间，角度或运动指令级接口则把更多计算放在视觉侧；最终职责还要看通信两端的实现，不能只凭一侧字段补齐另一侧逻辑。调试前最好先画清“选板、弹道、轨迹和开火分别由哪一侧负责”，否则很容易在两端重复补偿。

=== yaw 优化：把规则先验写进搜索

通信链路理清以后，回到视觉算法本身。装甲板的四个角点共面；在距离较远、接近正视等几何条件下，图像对装甲板 yaw 的约束可能较弱，少量像素扰动就会明显影响姿态解。sp_vision 没有直接接受 PnP 给出的 yaw，而是在 `Solver::optimize_yaw()` 中加入带 pitch 先验的网格搜索：

```cpp
constexpr double SEARCH_RANGE = 140;  // degree
auto yaw0 = tools::limit_rad(gimbal_ypr[0] - SEARCH_RANGE / 2 * CV_PI / 180.0);
for (int i = 0; i < SEARCH_RANGE; i++) {          // 1° 步长，共 140 个候选
  double yaw = tools::limit_rad(yaw0 + i * CV_PI / 180.0);
  auto error = armor_reprojection_error(armor, yaw, ...);
  if (error < min_error) { min_error = error; best_yaw = yaw; }
}
armor.yaw_raw = armor.ypr_in_world[0];   // 留档：PnP 的原始输出
armor.ypr_in_world[0] = best_yaw;        // 覆盖
```

候选相对云台 yaw 的偏移从 $-70 degree$ 到 $+69 degree$，步长为 $1 degree$，共 140 个点。代码用重投影误差最小的候选覆盖 PnP yaw，同时把原始结果保存到 `yaw_raw`，方便后续对比。穷举网格不依赖局部优化器能否从初值收敛，但角度分辨率受 $1 degree$ 步长限制，还要额外计算 140 次重投影；这部分耗时需要在目标硬件上测量。

关键在重投影函数里那一行：

```cpp
auto pitch = (name == ArmorName::outpost) ? -15.0 * CV_PI / 180.0 : 15.0 * CV_PI / 180.0;
```

重投影时，普通装甲板的 pitch 固定为 $+15 degree$，前哨站固定为 $-15 degree$，于是搜索变量只剩 yaw。这里的正负号来自项目坐标约定，不能脱离坐标系直接解释成“前倾”或“后仰”；角度和兵种也与赛季规则、机械安装有关，迁移前都要重新核对。

==== 把假设的适用边界写进代码

这个先验的适用边界写在 `Solver::solve()` 末尾：

```cpp
// 平衡不做yaw优化，因为pitch假设不成立
auto is_balance = (armor.type == ArmorType::big) &&
                  (armor.name == ArmorName::three || armor.name == ArmorName::four ||
                   armor.name == ArmorName::five);
if (is_balance) return;
optimize_yaw(armor);
```

源码把“大装甲板且编号为 3、4、5”识别为平衡步兵，并注明其 pitch 假设不成立，因此直接返回，保留 PnP 原始姿态。也就是说，这个分支不是一个零散特判，而是在代码中标出先验的适用范围。

这样写有两个好处：读者能看出 yaw 优化依赖固定 pitch；新增兵种或机械结构变化时，也知道应从哪里检查。赛季规则变化后，这个分支当然也要随之复核。

固定 pitch 为什么值得单独设分支？下面用一组可重复的合成几何观察它的影响。脚本采用 `standard4.yaml` 的相机内参、畸变和手眼外参，目标位置、真实 yaw 与角点高斯噪声由脚本指定。计算代价时固定目标平移，没有对每次扰动重新执行 IPPE PnP，所以这张图回答的是*给定位置时，网格搜索对 pitch 失配有多敏感*，不是完整自瞄管线或实车命中率。

#figure(
  image("images/proj-sp-yaw-optimize.png", width: 100%),
  caption: [固定合成姿态下的 pitch 先验敏感性（脚本 `proj_sp_yaw_optimize.py`）。左图显示 yaw 搜索代价，右图汇总每种 pitch 下 600 组、角点标准差为 1 px 的噪声试验。],
)

在这组设定里，真实 pitch 为 $0 degree$ 和 $-15 degree$ 时出现第二个局部极小值，两处代价差约为 3.2 px 和 1.1 px。把 $|"yaw error"| > 10 degree$ 记为大误差后，真实 pitch 为 $15 degree$、$0 degree$、$-15 degree$ 时，大误差率分别为 1.8%、15.5%、48.0%，yaw 的均方根（RMS）误差分别为 $4.1 degree$、$10.9 degree$、$18.9 degree$。这些数字只对应图中的几何、噪声模型和固定随机种子；换一组距离、视角或噪声，结果可能不同。实际部署时，可以沿用代码已经保留的 `yaw_raw`，同时记录优化后 yaw、两处重投影代价差和后续跟踪残差，再用自己的数据决定哪些兵种应跳过优化。这样，先验是否合适就从“看起来合理”变成了可检查的问题。

=== 十一维整车模型与跟踪检查

==== 状态、噪声与前哨站先验

yaw 优化给出单块装甲板的观测，跟踪器还要把连续观测组织成整车状态。sp_vision 的 EKF 使用 11 维状态，定义直接写在 `target.cpp` 的注释里：

```text
x vx y vy z vz a w r l h
a: angle    w: angular velocity
l: r2 - r1  h: z2 - z1
```

前八维分别描述旋转中心的三轴位置与速度，以及自旋角和角速度；第九维 `r` 是一组装甲板的旋转半径。`l` 和 `h` 表示另一组装甲板相对它的半径差与高度差。rm_vision 用两个交替半径表达类似几何，sp_vision 则把差值直接放进状态量。观测方程里的开关是：

```cpp
auto use_l_h = (armor_num_ == 4) && (id == 1 || id == 3);
auto r = (use_l_h) ? x[8] + x[9] : x[8];
```

只有模型包含四块装甲板、当前编号又是 1 或 3 时，预测位置才使用 `r + l` 与 `z + h`；三块板模型不会进入这个分支。这是滤波器采用的几何模型，是否与某台实车一致，还要看装甲板安装尺寸。

过程噪声 $bold(Q)$ 的代码注释引用了分段白噪声模型（Piecewise White Noise）。每个位置—速度二阶子块都可写成 $v dot bold(g) bold(g)^T$，其中 $bold(g) = [(Delta t)^2 \/ 2, Delta t]^T$，展开后就得到 $(Delta t)^4 \/ 4$、$(Delta t)^3 \/ 2$ 与 $(Delta t)^2$ 三类系数。只要 $v >= 0$，这个外积始终半正定：$Delta t != 0$ 时秩为 1，$Delta t = 0$ 时秩为 0。即使后文为了生成规划窗口而使用负的 $Delta t$，非对角项虽然变号，$bold(Q)$ 也不会因此变成非半正定；至于反向预测是否符合真实运动，是另一个需要数据验证的问题。

前哨站另有两组硬编码先验：

```cpp
if (name == ArmorName::outpost) { v1 = 10;  v2 = 0.1; }   // 加速度方差 / 角加速度方差
else                            { v1 = 100; v2 = 400; }
// 满足项目定义的 convergened() 后，将较高转速改为固定值
if (this->convergened() && this->name == ArmorName::outpost && std::abs(this->ekf_.x[7]) > 2)
  this->ekf_.x[7] = this->ekf_.x[7] > 0 ? 2.51 : -2.51;
```

前哨站的平动噪声系数 `v1` 从 100 降为 10，角运动噪声系数 `v2` 从 400 降为 0.1，说明代码更信任它接近匀速旋转。随后，只要 `convergened()` 返回真且 $|omega| > 2$，下一次预测前就把角速度改成 $plus.minus 2.51$ rad/s。这里的函数名容易让人多想一步：对前哨站，`convergened()` 实际只检查更新次数超过 10、两个半径落在允许范围，并没有进行统计意义上的收敛检验。$2.51 approx 0.8 pi$ 与赛季规则值接近，移植时仍要对照当季手册；$|omega| > 2$ 的门槛则避免低速阶段过早套用固定值。

还有第三层藏在初始化里。`tracker.cpp` 给不同兵种配不同的初始协方差对角：

```cpp
else if (armor.name == ArmorName::outpost) {
  Eigen::VectorXd P0_dig{{1, 64, 1, 64, 1, 81, 0.4, 100, 1e-4, 0, 0}};
  target_ = Target(armor, t, 0.2765, 3, P0_dig);   // 半径 0.2765 m，3 块装甲板
}
```

`l` 和 `h` 的初始方差为 0，状态转移和 $bold(Q)$ 也不向这两维注入不确定性，因此它们在这套矩阵结构中保持为 0。代码由此把三块板模型的半径差和高度差固定下来。`r` 的初始方差则是 $10^(-4)$，不是 0，所以半径仍可被观测更新，只是初始置信度较高。

这段初始化展示了两种不同强度的先验：较小方差表示“仍可更新，只是初始时更相信给定值”；零方差再配合零过程噪声，则近似于“根本不估计这个量”。如果结构约束写错，后一种状态没有机会从观测中自行纠正，所以应比普通初值更谨慎地核对。

==== 观测噪声 R：随角度与距离调整权重

观测量是 4 维：方位角、俯仰角、距离 $d$ 与装甲板朝向角。代码里保留了一行常量版 $bold(R)$ 的注释，当前实现则按观测几何调整后两项：

```cpp
// Eigen::VectorXd R_dig{{4e-3, 4e-3, 1, 9e-2}};          // 旧：四个常数
auto center_yaw = std::atan2(armor.xyz_in_world[1], armor.xyz_in_world[0]);
auto delta_angle = tools::limit_rad(armor.ypr_in_world[0] - center_yaw);
Eigen::VectorXd R_dig{
  {4e-3, 4e-3, log(std::abs(delta_angle) + 1) + 1,
   log(std::abs(armor.ypd_in_world[2]) + 1) / 200 + 9e-2}};
```

`delta_angle` 是装甲板朝向与视线方向之差。两个调整项分别是：

- 距离方差项随 $|Delta theta|$ 增大：$log(|Delta theta| + 1) + 1$。
- 朝向角方差项随距离增大：$log(d + 1) \/ 200 + 9 times 10^(-2)$。

当 $Delta theta = 0$ 或 $d = 0$ 时，$log(1) = 0$，两项分别回到注释中常量版的 1 和 0.09；其余情况下只会在这个基线上增大。数学上，这是一组连续权重，不会在某个角度阈值处突然切换。

这里的单位也是公式定义的一部分：源码中的 $Delta theta$ 以弧度代入，$d$ 以米代入。对数接收的是按这两种单位写出的数值；若把同一角度改用度、距离改用厘米，公式虽然长得一样，算出的权重却会改变。迁移配置时必须连同单位一起保留。

这些公式表达了两个直观假设：装甲板越侧斜，测距越不可靠；目标越远，朝向角越不可靠。「rm.cv.fans」一章看到的做法是，侧斜超过阈值便拒绝用该观测更新半径；sp_vision 则增大 $bold(R)$ 的相应对角项，让卡尔曼增益连续降低它的权重。两者目的相近，一个直接拒绝超出阈值的观测，一个连续调整权重，哪种更适合仍要看实车残差。

对新手而言，最值得带走的是：$bold(R)$ 不必永远是常数，它可以描述每次观测不同的可信度。不过，“单调、连续”只说明函数好用，不说明噪声模型一定正确；两个对数公式没有随仓库提供标定数据，换相机或换目标后应重新统计残差。

==== 两类硬边界与一项残差窗口规则

模型运行起来以后，还要判断什么时候不该继续相信它。常规路径里有三项检查，入口是：

```cpp
Tracker::track(armors, t)
```

```cpp
if (state_ != "lost" && dt > 0.1) {              // 一、两帧间隔超 100 ms
  tools::logger()->warn("[Tracker] Large dt: {:.3f}s", dt);
  state_ = "lost";
}
if (state_ != "lost" && target_.diverged()) {    // 二、物理量跑出合理范围
  state_ = "lost"; return {};
}
// 三、残差窗口
if (std::accumulate(target_.ekf().recent_nis_failures.begin(),
                    target_.ekf().recent_nis_failures.end(), 0)
    >= (0.4 * target_.ekf().window_size)) {
  tools::logger()->debug("[Target] Bad Converge Found!");
  state_ = "lost"; return {};
}
```

第一项在相邻时间戳间隔超过 100 ms 时把状态改为 `lost`。源码注释举了相机离线，但调度停顿、处理阻塞或时间戳异常也会触发同一分支，所以日志里看到 `Large dt` 时，下一步应查整条时间链，而不是直接更换相机。第二项要求 $r$ 与 $r + l$ 都位于 $(0.05, 0.5)$ m，超出就丢弃当前目标。作为对照，rm_vision 会把半径限制在给定区间：丢弃能尽快摆脱明显错误的状态，限幅则能维持连续输出，取舍取决于错误状态的代价和重新捕获时间。

第三项把一个名为 `nis` 的量与阈值比较，在最近最多 100 个标志中累计失败次数；总数达到 40 就丢弃目标。持续的大残差能提醒我们“预测与观测对不上”，却不能单独区分模型失配、数据关联错误和观测异常。

不过，变量叫 `nis` 不代表它就是教科书中的 NIS。标准归一化新息平方（Normalized Innovation Squared, NIS）使用*更新前*的新息 $bold(nu) = bold(z) - h(hat(bold(x))^-)$ 和先验新息协方差 $bold(S) = bold(H) bold(P)^- bold(H)^T + bold(R)$；观测维数为 4 且模型假设成立时，才可用相应的卡方分布评估。

当前实现与这个定义有两处关键差别：

- 阈值写为 `0.711`，注释却说"自由度 = 4，置信水平 95%"。$0.711$ 约为 $chi^2_4$ 的下 5% 分位点；常用的 95% 上分位点约为 $9.488$。若把标准 NIS 大于 0.711 记为失败，模型成立时理论上也会有约 95% 的样本超过它。
- 代码先更新 $bold(x)$ 与 $bold(P)$，再用后验状态残差 `z - h(x)` 和后验协方差构造 $bold(S)$。这个量不服从标准 NIS 的上述分布，因此不能直接套用 NIS 的卡方阈值。

同一文件还计算了名为 `nees` 的量，但它使用的是本次状态修正 `x - x_prior`；标准归一化估计误差平方（Normalized Estimation Error Squared, NEES）需要真实状态与估计状态之差。这里的量同样不能直接套用标准 NEES 的分布，而且跟踪器也没有用它触发重置。

所以，这段代码更适合称为“后验残差统计量加滑动窗口阈值”的经验规则。若要改成标准 NIS，应保留更新前的新息与 $bold(P)^-$；若保留现有定义，就应在录制数据中分别统计正常跟踪和错误跟踪的分布，再选择阈值。还要留意另一条调用路径：全向感知使用的 `track(detection_queue, armors, t, ...)` 重载有帧间隔和半径检查，却没有这项残差窗口重置。读这类重载较多的代码时，不要看到一个函数有保护，就默认所有入口都有。

=== 规划器的代码边界

「自瞄算法设计」一章已经介绍目标轨迹、射击轨迹、加速度约束与提前减速。现在回到实现，沿着 `Planner` 看三个容易读错的地方：时间窗从哪里开始、TinyMPC 在这里扮演什么角色，以及配置里的延迟怎样进入结果。

*先对齐时间窗。* `DT = 0.01` s、`HALF_HORIZON = 50`、`HORIZON = 100`。进入下面的采样函数前，`plan()` 已把目标向前预测一个估计的弹丸飞行时间，使后续目标轨迹对应预计命中时刻。采样函数开头又执行：

```cpp
target.predict(-DT * (HALF_HORIZON + 1));   // 先把目标状态往回倒
auto yaw_pitch_last = aim(target, bullet_speed);
target.predict(DT);   // [0] = -HALF_HORIZON * DT -> [HALF_HORIZON] = 0
```

第一次回推是 $-0.51$ s；随后先前进一步，轨迹第 0 列便位于参考中心前 0.5 s，第 50 列位于参考中心，第 99 列位于其后 0.49 s。求解器把第 0 列直接设为初始状态：`x0 << traj(0, 0), traj(1, 0);`，pitch 同理；100 列参考也全部写入 `Xref`。这样，过去半窗既提供了人为初始条件，也参与代价计算；等到读取第 50 列时，前面已经安排了 50 个受加速度约束的离散步。

最容易误读的是“初始状态”四个字：它来自参考轨迹，不是实测云台角。`standard_mpc.cpp` 虽然读取了 `GimbalState`，调用规划器时只传入目标和弹速；`Planner` 也没有云台状态输入。因此，TinyMPC 在这里负责求一条满足约束的参考轨迹，不是用云台反馈闭环更新控制量。上一节已经算过，负步长下的 $bold(Q)$ 在代数上仍半正定，但这只说明矩阵形式没有因此失效，不代表回推的历史状态是测量结果。

*再看求解什么时候停。* yaw 与 pitch 求解器都把 `max_iter` 从 TinyMPC 默认的 1000 改成 10。`tiny_solve()` 满足默认的 $10^(-3)$ 原始／对偶残差阈值时返回 0，否则跑完 10 次后保存当前解并返回 1；`Planner` 不检查返回值，仍继续读取 `work->x` 与 `work->u`。因此，“最多 10 次”只是在限制工作量，并不等于每次都已收敛，也不能单独推出固定运行时间。`standard_mpc.cpp` 的规划线程每次发送后还会 `sleep_for(10ms)`，所以一个周期至少包含本轮计算和 10 ms 休眠，线程调度与唤醒还可能继续增加时间。

*延迟按角速度分档。* 规划器根据目标角速度绝对值选择两组配置之一：

```cpp
double delay_time = std::abs(target->ekf_x()[7]) > decision_speed_
                    ? high_speed_delay_time_ : low_speed_delay_time_;
```

代码只规定“超过 `decision_speed` 就用高速档”，并不要求高速档数值一定更大。固定仓库中有七个 YAML 包含这三个字段，取值如下：

#block(breakable: false)[
#figure(
table(
  columns: (auto, auto, auto, auto),
  align: (left, right, right, right),
  stroke: (x, y) => (
    top: if y == 0 { 1pt } else { 0pt },
    bottom: if y == 7 { 1pt } else { 0pt },
  ),
  inset: 7pt,
  table.header([*配置文件*], [`decision_speed`], [高速档], [低速档], table.hline(stroke: 1pt)),
  [`standard3.yaml`], [7 rad/s], [0 ms], [0 ms],
  [`standard4.yaml`], [8 rad/s], [15 ms], [15 ms],
  [`sentry.yaml`], [10 rad/s], [26 ms], [10 ms],
  [`uav.yaml`], [12 rad/s], [5 ms], [5 ms],
  [`demo.yaml`], [8 rad/s], [30 ms], [15 ms],
  [`ascento.yaml`], [7 rad/s], [50 ms], [15 ms],
  [`mvs.yaml`], [7 rad/s], [66 ms], [15 ms],
),
caption: [七个配置文件中的角速度阈值与附加延迟。三个配置的高速档与低速档相同，分档不会改变延迟；其余四个配置的高速档较大。非零高速档从 5 ms 到 66 ms，相差约 13 倍。部分文件缺少当前 `Planner` 构造还会读取的其他键，因此表格表示配置中出现过的取值，不表示七份文件都能直接启用该规划器。README 只明确提到国赛时步兵采用了它。],
)
]

README 把除图像处理外的延迟总和视为调试参数，建议借助慢动作视频、击杀时间等现象调整，并举出约 15 ms。表中的差异也说明这个数与车辆配置有关。真正迁移时，最好分别记录曝光、处理、通信、控制和出膛事件；否则，相同的常数可能只是复制了默认值，不同的常数也可能早已过期。

`standard3.yaml` 的两档均为 0，表示不再增加这两个配置项提供的额外偏移；`Planner` 仍会把目标从图像时间预测到当前时刻，并考虑弹丸飞行时间。它与 README 举出的 15 ms 不同，可能来自车辆差异、后续调参或文档未同步。仅看仓库无法替这三种情况作判断，使用时以目标车辆的时间测量为准。

=== README 里的尝试、取舍与前提

源码告诉我们程序现在怎样运行，README 则补充了“为什么最后这样做”。sp_vision 的 README 记录了一次效果不佳的实车尝试、一个只做过仿真的备选方案，以及开火决策依赖的条件。把这三类信息分开读，能避免把“团队试过”“仿真表现不错”和“已经上场”混成同一种结论。

*MPC 只当求解器，不当闭环控制器。* README 原文：

#blockquote[
  这里MPC并非闭环控制器，而是作为求解器寻找可行轨迹解，其输入不包含云台的实际状态。我们也尝试过使用MPC直接作为闭环控制器，通过下位机转发MPC求解后的力矩指令给电机，当自身小陀螺时，控制效果不如下位机方案。
]

这与刚才读到的 `Planner` 一致：TinyMPC 求的是一条受加速度约束的参考轨迹，输入中没有实际云台状态。README 还提到，团队曾让 MPC 直接输出力矩、通过下位机转发，但机器人自身旋转时，控制效果不如他们的下位机方案。

这个负结果很有参考价值，但范围也很具体：它比较的是该团队的两套实现和自身旋转工况，不是“上位机闭环一定不如下位机”的普遍定律。README 没有附两套控制器的模型、带宽、参数和对比曲线；若想追查差异来自动力学耦合、通信延迟、模型误差还是调参，需要在同一硬件和轨迹下记录实际状态与力矩。

*只完成仿真的方案二没有上场。* 提前减速有两种实现。README 认为方案二（五次多项式显式搜索）确定性较高、跟随段会与射击轨迹重合、高转速下更稳定，而且实现更轻量、无需第三方库，随后又明确说明：

#blockquote[
  由于时间有限，方案二仅进行了仿真验证，未上车测试，国赛上场采用了方案一。
]

因此，“高转速下更稳定”是作者对仿真方案的评价，已知部署事实则是：方案二没有上车，国赛采用方案一。仓库没有附仿真数据和指标，本书也无法替它比较比赛环境中的优劣。这个例子恰好说明，技术选型不只比较纸面性质，还要考虑剩余验证时间和可靠的回退方案；来不及完成上车闭环时，选择验证更充分的方案往往更实际。

*提前开火依赖稳定的开火延迟。* `Planner` 用 `shoot_offset_ = 2` 比较第 52 列的参考与规划轨迹；`DT = 10 ms`，相当于查看 20 ms 后的误差。README 把这个偏移解释为开火命令到弹丸出膛的延迟 $t_"fire"$，并明确限定：

#blockquote[
  该方案的前提是开火延迟无波动，对机械和电控的要求较高。
]

进阶篇「实战经验与常见问题」提到过另一个系统的对照：rm.cv.fans 的公开材料报告 35～110 ms、均值约 82 ms，但它测到的终点是裁判系统射击数据更新，不能直接当作“命令发送到物理出膛”，更不是 sp_vision 的 20 ms。这个对照真正提醒我们的是：只看平均值会漏掉波动范围。

在自己的系统上，可以先利用发弹计数或裁判系统事件，稳定记录“命令发送—报告到达”这一代理量；若要得到“命令发送—弹丸出膛”的分布，还需用经过标定的发射传感器或高速相机确定出膛时刻。拿到分布后，才能判断固定 20 ms 偏移是否合适，以及开火门限是否需要为时间波动留余量。

愿意把失败尝试、未上车方案和方法前提写进 README，本身就是很有用的工程记录。读者接着追问“哪台车、什么工况、比较什么指标”，就能把这些经验放回合适的范围，而不是简单照搬或全盘否定。

=== 从 TODO 和 issue 找到下一步

读完主线后，再集中看 TODO、issue 和 README 的未来计划，效率会比一开始逐条追问题高。源码 TODO 指向作者已经注意到的欠账，issue 记录使用者遇到的现象，README 则表达维护者准备继续做什么；三者都能提供线索，但 issue 中的现象还需要复现，不能直接当成上游代码的定论。

*TinyMPC 的返回状态。* issue #8 在本章核对时（2026-07-24）仍为 open。提问者说 `tiny_solve()` 持续返回 1；后续评论提到残差仍在下降，把迭代数提高到 1000 后可以达到默认的 $10^(-3)$ 阈值，而 10 次迭代得到的轨迹看起来也可用。这与源码“最多 10 次、忽略返回值”的行为对得上，但尚不能说明问题来自 TinyMPC 本身，也没有证据表明它曾在国赛触发故障。最直接的改进不是猜原因，而是记录返回值、实际迭代次数，以及工作区中的状态原始残差、输入原始残差、状态对偶残差和输入对偶残差；同时要规定未收敛时继续使用当前解、沿用上次解还是回退。

*一条尚未定位的标定求助。* issue #9 同样为 open。标题说计算出的旋转矩阵与物理安装关系差别很大，正文也说明提问者修改过手眼标定代码；没有维护者回复、数据集和复现步骤，暂时无法把现象归到固定上游版本。遇到类似问题，最小检查是先在未修改程序上，用一组已知变换的合成数据核对坐标约定，再用覆盖多轴旋转的实测数据复现。这样才能逐步区分代码改动、坐标系方向和采样姿态不足，而不是看到矩阵“不像预期”就猜根因。

*两处写死的图像中心。* 跟踪器里有两处相同代码：

```cpp
cv::Point2f img_center(1440 / 2, 1080 / 2);  // TODO
```

它们用于优先选择靠近图像中心的装甲板，并附有 `TODO`。输入不是 1440×1080 时，程序仍可继续运行，但排序参照点已经偏离实际中心。仓库同时支持海康、迈德威视和 USB 相机，更稳妥的写法是从当前 `cv::Mat` 尺寸或相机配置取得中心，再用不同分辨率的样例验证排序。

*默认弹速与有效范围不一致。* 规划器在弹速小于 10 或大于 25 m/s 时改用 22，普通瞄准器在小于 14 m/s 时改用 23，打符瞄准器在小于 10 m/s 时改用 24。于是同样输入 12 m/s，规划器和打符路径保留原值，普通瞄准器却替换为 23。`standard_mpc.cpp` 与 `standard.cpp` 分属不同程序，影响尚且隔开；`mt_standard.cpp` 同时包含普通自瞄和打符，切换模式就会改变同一输入的解释。最好集中定义默认值、有效范围和数据来源，并用 10、14、25 m/s 这些边界值覆盖模式切换测试。

*识别模型能复现到哪一步。* README 引用了两支团队的识别工作：

- 深圳大学 RobotPilots
- 北京科技大学 Reborn

仓库使用 OpenVINO 的中间表示（Intermediate Representation，IR）部署 YOLOv5。

机器人配置选择 `yolov5` 时，各文件用途如下：

- `assets/yolov5.xml`：网络结构描述，也是程序传给 OpenVINO 的加载入口。
- `assets/yolov5.bin`：与上述 XML 配套的权重数据。
- `tiny_resnet.onnx`：仓库分类器使用的模型。
- `best2-sim.onnx`：没有被固定提交中的源码或配置引用。

因此，不能只看扩展名，就把 `best2-sim.onnx` 当作当前 YOLOv5 的可转换源模型。

issue #7 曾索要原始权重，维护者回复该 YOLOv5 使用深圳大学开源模型，团队自己也没有原始权重，随后关闭 issue。因此，仓库提供的是当前推理代码引用的模型文件，不是完整训练材料；原始检查点、训练配置和优化器状态都不在其中。迁移到其他推理框架时，应先确认目标框架能否导入现有推理产物，或者回到许可条件明确的上游取得可转换模型。本章没有在项目指定的 OpenVINO 和目标硬件上运行这些文件，推理兼容性仍需实际构建验证。

*尚未完成的自身平移补偿。* README 说明当前自瞄没有考虑机器人自身移动：步兵冲下坡时，静止目标会被估计成正在移动，前几发容易打偏，后续计划引入轮式里程计。源码中的世界坐标变换确实只使用 IMU 姿态旋转，没有底盘平移输入；如果坐标原点跟着机器人走，静止目标就会表现出反向相对运动。这与 README 的解释一致，但仓库没有附该场景日志，延迟和姿态误差是否也在影响结果仍不清楚。接入里程计后，可以比较机器人移动前后静止目标的估计速度，直接检查平移补偿是否解决了预期问题。


==== 本章小结

沿着 sp_vision 的主线，我们看到一个进程内的 `main` 怎样让多个功能组共享相机；140 个、间隔 $1 degree$ 的候选怎样配合 pitch 先验修正 yaw；11 维状态怎样同时表示旋转中心、角运动和两组装甲板的几何差异。跟踪器用帧间隔、半径和残差窗口判断何时放弃目标，其中名为 `nis` 的后验残差量不能直接按标准 NIS 解读。规划器则把 TinyMPC 当作参考轨迹求解器，而不是闭环云台控制器；无论提前达到阈值还是跑满 10 次，`Planner` 都继续读取当前迭代结果。

比这些实现细节更值得迁移的，是读仓库的方法：从主程序找到数据流，用配置解释分支，用 README 理解取舍，再借 TODO 和 issue 找到尚未闭合的问题。$15 degree$ pitch、2.51 rad/s 转速、0.711 阈值和 20 ms 开火偏移都不是可以直接照搬的常数，它们各自绑定着赛季、车型、统计定义或时间链路；弄清来源以后，才知道该保留、重测还是改掉。

本书正文到这里就结束了。以后面对新的自瞄仓库，也可以继续沿用同一套顺序：先跑通数据流，再检查坐标与时间，最后才调模型和参数。遇到偏差时先把现象记录完整，通常比立刻改一串常数更接近答案。
