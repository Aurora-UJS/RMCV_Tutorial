#import "/template/template.typ": *

=== 阅读前先确认许可证与版本

本章核对的版本是 rm.cv.fans 当前提交 `6abd1bf`（2025-07-20）。在这个提交中，仓库根目录包含 `aimer`、`base`、`assets`、`docs`、`UltraMultiThread` 和 `README.md`，但没有 `LICENSE`、`COPYING` 或 `NOTICE`；对文本文件搜索也没有发现 copyright 声明。这些是对该提交的直接观察，不代表作者从未在其他渠道给个别使用者授权。

公开可见不等于开源许可。著作权通常在作品创作完成时自动产生；没有许可证时，读者不能仅凭仓库公开就推定自己获得了复制、修改或再分发的权利。GitHub 服务条款允许用户通过平台提供的功能查看和 fork 公开仓库，但这不等同于一份面向任意用途的开源许可证。具体例外与责任还受司法辖区和使用方式影响。

因此，本章只描述架构和算法，引用公开文档中的必要短句与一张测量表，并给出文件位置方便读者核对，不摘录源码。抽象思想、方法和算法通常不属于著作权保护的具体表达，但近似复刻原有代码仍可能涉及表达复制，此外还可能存在专利、商标等其他权利。若要直接使用、修改或发布该仓库代码，应先向权利人取得适合用途的明确许可。这里是工程合规提示，不替代法律意见。

与上一章相比，这个例子也说明了许可证义务需要逐仓库确认。MIT 通常要求随副本保留版权与许可声明；Apache-2.0 还包含明确的专利授权与终止条款，并要求修改文件保留显著的变更说明，若上游提供 NOTICE，分发时还要按许可证要求保留其中适用的归属信息。不能把这些差异概括成一句“项目已经开源”，也不能由同一作者或同一套系统推定所有仓库使用相同许可证。

=== 项目定位：自研基础设施与低延迟目标

仓库位于 julyfun 的个人账户下，README 顶部使用上海交通大学 RoboMaster 战队的标志并链接到战队主页；本章据此说明公开关联，不进一步推断仓库的组织所有权。标题将项目称为 LMTD（Linear Modelled Top Detector），其文档与代码重点都是旋转目标建模和反陀螺火控。

难得的是，作者把这个项目的取舍直接写在了根 README 的第一句（README:17）：

#blockquote[
手搓通信，参数，UI，数学和坐标变换，兼容性一般，但是非常快。
]

这句话明确表达了作者的取舍：通信、参数系统、调试界面、数学工具和坐标变换由项目自行实现，以换取对数据流和时序的直接控制。它可能减少不需要的通用功能，也把适配、并发安全、调试和长期维护责任留给项目自身。本章没有做 ROS 2 与该项目的同负载对照测试，因此不能把“非常快”推广成框架间的普遍性能结论。

下面选择四条可以独立分析的主线：用规则先验约束姿态、把车辆中心搜索降到一维、为不同问题组合不同延迟区间，以及用弹丸图像反馈估计发射偏差。每一条都同时讨论证据范围和失效条件。

=== 思路一：用先验约束旋转自由度

先说清楚要解决什么问题。PnP 给出装甲板的完整 6 自由度位姿：三个平移、三个旋转。「相机模型」一章已经说明，装甲板近乎正对相机时，板面绕竖直轴偏转几度，四个角点的像素位置可能几乎不变，解算出的 yaw 因而容易抖动。问题在于，整车建模又很依赖 yaw。这是单目 PnP 在这一观测构型下的结构性局限：它给出六个自由度的估计，却不会同时说明每个估计有多可信。

根 README 将一项特性概括为“降低自由度计算装甲板 Rotation，相比 PnP 精度显著提升”。工程日志在另一处把装甲板仰角按当时规则固定为 15°；若再假设装甲板 roll 已知或可忽略，就只剩 yaw 需要搜索。README 没有完整给出这一姿态算法的对照实验，因此这里把“更准”保留为作者报告，而不是本章独立复现的结论。角度数值也随赛季和车型变化，应以当年规则与实车安装为准。

在这些假设成立时，可以给定候选 yaw，将已知 pitch、roll 与装甲板尺寸一起重投影到图像，再在可行区间内最小化角点残差。这样把旋转搜索限制为一维。这里的具体组织方式是根据根 README、工程日志和后续同类实现综合还原的；它不是根 README 对算法的逐行定义。项目中的根 README 只有 31 行，本章后文超过 31 的 `README:N` 均指 522 行的 `aimer/auto_aim/README.md`。

硬约束减少了待估参数，因而可能降低弱可观方向的方差；与此同时，它会把先验误差直接带入结果。斜坡、车体侧倾、装甲板安装误差、特殊车型或规则变化都可能让固定 pitch、roll 失效。应分别在正视、斜视、坡面和不同车型数据上比较残差与姿态误差，不能只由自由度减少推断精度必然提高。

下一章的 sp_vision 也保留了名为 `SJTU_cost` 的候选代价函数，但当前调用处使用的是四角点欧氏距离，`SJTU_cost` 调用被注释。这个命名与结构显示两项目之间存在技术关联的线索，却不能单独证明代码来源、授权关系，也不能证明该函数因精度较差而被弃用；注释原因需要提交历史、作者说明或对照实验才能判断。它能确定的信息只是：当前版本保留了一个未启用的候选代价，阅读时要区分“存在于文件中”和“实际执行”。

=== 思路二：把车辆中心约束到一条圆弧

第二条主线仍在利用结构先验，但未知量换成了旋转车辆的中心位置和朝向。4 月 25 日的工程日志（README:499-501）描述了最初版本：同时看到两块装甲板时，先把车辆中心限制在“以两块板中心连线为直径的圆”上；对圆上的候选中心计算两块假想装甲板的投影面积，再以面积比与观测面积比的差作为代价。作者当时假设该代价随圆周参数单峰，因此用三分搜索得到角度估计。

圆约束来自一个明确的几何条件。若四块装甲板中心位于以车心为中心的矩形四边中点，且当前看到的是相邻两块板，那么车心到两块板中心的方向互相垂直。由泰勒斯定理，所有使这条线段张成直角的点都落在以线段为直径的圆上。矩形长宽可以不同，因此前后半径与左右半径不必相等；但“相邻板”“矩形布局”和投影到地面后的直角关系都是前提，特殊车型、坡面姿态和误关联会破坏这些前提。圆约束只把二维候选域降为一维，还要结合可见方向和重投影代价决定圆上的位置。

项目随后改了代价函数。4 月 29 日日志明确写着“删除面积法”（README:483）；4 月 30 日的说明改为比较候选投影与观测投影的差（README:465）。固定提交中的 `top_model.cpp:19-51` 实现 `get_pts_cost`，逐边组合像素距离与斜率角差；`SingleCost`（`:115-134`）和 `DoubleCost`（`:136-160`）都调用它，没有继续使用面积比代价。这段演变很重要：圆上的一维搜索被保留，给候选点打分的方法则随着测试结果替换。它与上一节的共同点不是某个固定公式，而是先用可核对的结构减少自由度，再用观测一致性选择剩余参数。

这里使用三分搜索而不是用于找零点的二分法，是因为目标是在给定区间内寻找单峰代价的极小值。每轮比较两个内点，就能排除较差一侧；它不要求现成的导数，但要求搜索区间内确实近似单峰。遮挡、角点误配或多个相近极小值都会破坏这个条件，因此部署前应画出代表性样本的代价曲线，而不只是观察迭代是否收敛。

5 月 1 日日志又记录“改用 phi 优选法”（README:417），也就是黄金分割搜索。常规三分搜索每轮计算两个新点并把区间缩到 $2\/3$；黄金分割搜索在初始化后可复用一个内点，每轮只计算一个新点并把区间缩到约 $0.618$。在相同停止尺度下，只按新函数求值次数估算，前者约为后者的 2.4 倍。这个比值依赖单峰区间、内点复用、相同终止准则以及“函数求值占主要开销”等条件；若投影代价很便宜而控制逻辑占主导，实际加速不会直接等于该比值。

==== 十组记录能说明什么

工程日志保留了 10 组对照数据（README:503-514），比较面积法与 4 月 25 日版本的三分搜索。第一列在原文中写作“实际 `l_to_zn`（匀速时手算）”，因此它是建立在匀速假设上的手算参考值，不是由独立测量系统得到的地面真值。下面照录三列数值，并在表头保留这一区别：

#block(breakable: false)[
#figure(
  table(
    columns: (auto, auto, auto),
    align: (center, center, center),
    stroke: (x, y) => (
      top: if y == 0 or y == 1 { 1pt } else { 0pt },
      bottom: if y == 10 { 1pt } else { 0pt },
    ),
    inset: 7pt,
    table.header([`l_to_zn` 手算参考值（匀速假设）], [*面积法估计*], [三分法估计]),
    [25.3], [21.3], [31],
    [29.3], [26], [35],
    [34.9], [32], [41],
    [40.5], [41.3], [44],
    [46.2], [46], [50],
    [50.5], [52.8], [52],
    [56.2], [61], [55],
    [61.8], [68], [61],
    [67.5], [73.7], [65],
    [71.7], [78.2], [70],
  ),
  caption: [两种角度估计与手算参考值的对照，单位为度，数据引自 `aimer/auto_aim/README.md:503-514`。在这 10 个点上，面积法由低估逐渐转为高估；三分搜索由高估转为低估。面积法残差在中段多次过零，不能用单个“决定性过零点”概括。],
)
]

把估计值减去手算参考值，可以更直接地看出这组数据的残差结构：

#figure(
  image("images/proj-cvfans-trisection.png", width: 100%),
  caption: [10 个样本上的估计与残差（脚本 `proj_cvfans_trisection.py`）。三列输入逐字转录自上表。绿色曲线是本章事后计算的算术平均，不是项目公开代码中的融合器：同一批样本上，其平均残差为 $1.825 degree$；再用这 10 点估计并扣除该偏置后，样本内残差 RMS 为 $0.510 degree$。面积法与三分搜索相对手算参考值的 RMS 分别为 $4.3 degree$ 和 $3.8 degree$。这些统计没有留出测试集，也没有独立真值，只能用于观察本表，不能当作融合精度验证。],
)

4 月 25 日的日志还说，两种估计会“共同作为角度滤波器的样本”。这说明作者当时并非只做二选一。不过，原文没有公开滤波器如何给两路样本加权，也没有说采用简单平均。图中的绿色曲线只是一个可重算的事后演示：这 10 个点上，两路残差大体呈相反趋势，算术平均后再扣除同批数据拟合出的常值偏置，样本内残差明显变小。

但不能据此得出“找一个反方向出错的估计器就够了”。两路误差可能来自同一组角点和同一套投影模型，因而高度相关；偏置还可能随距离、斜视、曝光和车型变化。更可靠的融合流程是先在标定集上估计偏置与残差相关性，再用不同场次或留出轨迹检查稳定性。只有样本外结果仍改善，才能说明互补残差真正带来了收益。

项目后续的选择也表明，这 10 点不是最终结论：4 月 29 日作者删除面积法，4 月 30 日的设计说明与固定提交都只剩一路投影代价搜索。也就是说，日志完整记录了“先共同送入滤波器，后删除其中一路”的演变。作者另在 README:375 报告，当时车上单次预测耗时 5～6 ms，其中三分搜索约 1.6 ms；这是特定版本与设备上的留存数字，实际采用前应在目标硬件上重新测量。

=== 思路三：把延迟拆成六个可追溯时间点

把整条链路压成一个常数 $t_d$ 很方便，却会把测量值、配置值和模型假设混在一起。rm.cv.fans 的延迟文档（`docs/auto_aim/latency.md:7-14`）先定义六个事件：`img` 是曝光中点，`predict` 是神经网络和预处理完成、开始运动与目标解算的时刻，`send` 是预测结束并准备发出信号的时刻，`control` 是电控接收信号并开始驱动电机的时刻，`fire` 是对应弹丸发射的时刻，`hit` 是弹丸到达目标的时刻。

命名事件的作用，是让每一段的来源可以单独记录。`img → predict → send` 属于本帧计算路径，原则上可以在同一时钟域内逐帧测量；`send → control` 需要上下位机共同观测或时钟映射；`control → fire` 取决于电控与发射机构；`fire → hit` 则来自弹道模型和目标距离。即使最后仍要相加，也应同时保存各段的测量方法、分布和版本，而不是只留下一个无法追溯的总延迟。

同一组时间点在项目中组合成两个不同问题：

- 代码中的 *prediction* 表示预测需要覆盖的时间范围，由 `img_to_predict`、`predict_to_send`、`send_to_control` 与 `fire_to_hit` 四项相加得到，不含 `control_to_fire`；它回答在项目假设下“云台此刻应指向哪里”。
- 为已发射弹丸重现轨迹时，需要估计实际 `fire` 与 `hit`，此时会用到包含 `control_to_fire` 的完整时间关系。

省略 `control_to_fire` 的依据写在 `latency.md:34-36`：在连续发射近似下，不追踪“本条控制命令唯一对应的那颗弹”，而希望云台在 `control` 时刻已经指向一颗此刻出膛的弹丸所需位置。这个近似适用于文档讨论的持续供弹情形；单发、低射频、发射门控明显或需要逐发绑定时，不能照搬这一省略。

#figure(
  image("images/proj-cvfans-latency.png", width: 100%),
  caption: [由项目文档与配置构成的延迟示意图（脚本 `proj_cvfans_latency.py`），不是一次完整事件追踪。日志保留的 `img → fire` 均值约为 82 ms；减去配置中的 `send_to_control = -18 ms` 和 `control_to_fire = 20 ms` 后，可反推出 80 ms 的视觉侧合计项。`predict` 在这 80 ms 内的具体位置没有记录，图中只作示意。负的 `send_to_control` 不能解释为物理传输倒流，它兼有带符号时间校准项的性质，具体由哪些偏差组成仍未确认。80 ms、18 ms、20 ms 和示例飞行时间都是区间或模型项，不是各自的时间误差；下半图另以 $delta t$ 表示独立的时间误差，并用 $abs(omega delta t)$ 近似恒定 120 rpm 时的角度误差。30° 标记只是工程日志对其测试情形的命中率报告，不是通用开火阈值。],
)

图中的 7° 与 30° 来自工程日志（README:319）：

#blockquote[
为什么尝试精准估计延迟？因为有的车转的太快了……对于 120 转的舵轮，每 10 ms 的误差就会导致目标装甲板已经转过去 7 度。反陀螺坐标推算本身的误差尚可接受，但加上这样的 7 度就很可能打不中了。更糟糕的情况是，如果误差达到 30 度以上，反陀螺的命中率会是 0，刚好每发都打在两装甲板之间。
]

换算本身可以直接复核：120 rpm 等于 $720 degree$/s，10 ms 对应 $7.2 degree$；30° 则对应约 41.7 ms 的时间误差。日志没有同时给出样本量、射频、距离和命中判定，因此“30° 时命中率为 0”只能保留为该项目在其测试范围内的报告，不能推广成所有车辆的几何定律或开火判据。

队列积压仍可用来理解量级。若 100 fps 图像按 FIFO 处理，队列中从最新帧向前积压 4 个间隔，最旧帧会落后约 40 ms；在恒定 120 rpm 假设下，对应一级角位移 $28.8 degree$。这只是特定队列策略和恒速模型下的预算示例，实际误差还取决于时间戳、丢帧策略、加速度和预测器是否使用了正确的观测时刻。

==== 跨子系统的速度前馈

延迟文档还记录了一个上下位机联调现象：匀速目标下，视觉持续发送 yaw 命令，稳态时命令没有回到期望的零偏；项目用 `yaw_v * t0` 描述该偏差，并说明 `t0` 随电机与 PID 参数变化（`latency.md:22-28`）。文档示例中，目标角速度为 $10 degree$/s，稳态偏差约 2°，由此得到 $t_0 approx 0.2$ s。

理想的一型线性闭环在稳定、模型结构与输入定义满足条件时，确实会表现出“阶跃零稳态误差、斜坡常值误差”。但项目文档没有给出下位机控制器结构、饱和、摩擦和离散实现，因而不能仅凭这条现象认定实际机制就是标准一型系统，也不能断言增加积分或修改某个 PID 参数一定是正确修复。

项目采用的办法是在视觉侧发送 `yaw + t0 * yaw_v`。从接口作用看，这是跨子系统的速度前馈；原文报告它在当时的匀速实验中使视觉期望的 yaw 回到零。这个结果没有在本章独立复现，适用范围也受匀速近似限制。它的工程代价很具体：更换电机、机械结构或控制参数后需要重新标定；上位机输出的 yaw 还混入了对下位机动态的补偿。较稳妥的做法是给该参数写明单位、标定工况和电控版本，让协议携带或校验配置版本，并在 PID 或机械结构改变后强制重标。是否把补偿移到电控侧，应由联合辨识和阶跃／斜坡测试决定，而不是只按模块归属判断。

=== 思路四：利用弹丸图像校正补偿参数

前三条主线都围绕目标状态，第四条增加了对发射结果的观测。固定提交在 `aimer/auto_aim/predictor/aim/` 下实现了 `aim_corrector`、`detect_bullet` 与 `do_reproj`：电控回传发射 id，视觉侧回溯该 id 对应的瞄准历史，用弹道模型重建弹丸的预期轨迹并投影到图像，同时在图像中检测弹丸。预期投影与弹丸观测之间的残差，随后用于调整补偿参数。

这构成的是“弹丸图像残差 → 补偿参数”的校正环，而不是完整的命中反馈。相机看到了飞行中的弹丸，却没有直接观察它是否击中装甲板；目标检测、运动预测、弹道模型和发射 id 关联仍可能出错。`aim_corrector.hpp` 的注释也主动限定了前提：瞄准计算被假定为正确，剩余发射误差来自机械、电控或视觉补偿参数。这个前提使校正问题可处理，同时意味着它不能用来证明整条“预测—发射—命中”链路正确。

工程上仍然很有价值：它把原先只能由弹着效果间接反映的偏差，变成图像中的可记录残差。验证时应分别测试发射 id 关联、弹丸误检、曝光与拖影、投影模型误差以及多发同时在画面中的情况，并保留校正前后的独立测试序列。否则，参数在训练射击上收敛只能说明优化器找到了一个解释这些样本的补偿量，不能区分机械偏差与模型误差。

=== 自研基础设施：省略了什么，又承担了什么

README 中的“手搓通信，参数，UI”对应三项自研基础设施。它们的共同特点不是功能比通用框架更多，而是只覆盖项目需要的范围，因此代码路径短、行为可直接检查；相应地，并发、接口稳定性和调试工具也由项目自己负责。

*进程内消息分发：UltraMultiThread（UMT）。* 固定提交的 `UltraMultiThread/include/umt/` 三个头文件共 552 行，提供按名称索引的 `Publisher<T>`、`Subscriber<T>` 与 `MessagePipe<T>`，并定义超时、空消息和发布停止等异常。它只处理同一进程内的消息，不提供 DDS 的跨进程传输、发现、网络可靠性和 QoS 协商，因此行数不能直接用来比较实现水平或性能。

源码还给出了需要注意的拷贝语义。`Publisher::push(const T&)` 会对每个订阅者调用 `fifo.push(obj)`，也就是把一个 `T` 复制进每条订阅队列；`pop()` 再把队首对象移动给调用者。因此 UMT 并非“天然零拷贝”。若 `T` 是 `std::shared_ptr`、`cv::Mat` 这类浅拷贝句柄，底层像素缓冲可能仍被共享；若 `T` 是拥有数据的普通容器，则会按类型的复制构造行为复制。是否复制大块数据必须查看实际消息类型。

C++ 的带名称 `Subscriber` 构造函数默认容量为 1，队列满时先丢最旧消息再加入新消息，这对只关心最新状态的图像链很合适。但 Python 绑定把默认 `fifo_size` 写成了 0，而源码中 0 表示不限制长度；默认策略因此随调用接口而异。即使在 C++ 中，也不应把“容量 1”推广给所有主题：控制事件、发射确认和日志可能要求完整顺序，需由消息语义决定队列大小。

*轮询式参数热更新。* 参数管理线程每隔 1 s 重新解析 TOML，并把发现的值写入共享 `std::variant` 对象。它省去了重新编译和重启，参数文件中的 `@brief`、`@param` 注释也记录了方向与用途；但“热更新”不等于“立即且原子地生效”。变更可晚至下一次轮询后出现，多项参数逐个写入，读取者可能看到新旧混合状态；删除配置项不会自动删除已经创建的参数对象。

更关键的是，固定提交中的写入 `*found = res` 与读取 `Param found = *ptr` 之间没有互斥或原子快照。若参数线程与算法线程并发访问同一 `variant`，会形成 C++ 数据竞争，结果不只是“偶尔读到旧值”，而是语言层面的未定义行为。查询不存在的名称或错误类型时，接口会记录错误并返回 `T{}`；调用者还要防止默认的 0、`false` 或空容器悄悄进入控制计算。一个更稳妥的设计是解析完整配置后校验类型与约束，再以不可变快照一次替换，并明确删除项和失败回滚的语义。

*Flask 网页调试界面。* 对无显示器的车载计算机，网页可以集中呈现图像、俯视图和日志。工程日志记录了界面功能的多次调整，也记录过一个导致 Ctrl+C 无法正常终止的 Flask 相关问题（README:84）。这说明调试界面本身也属于需要测试的运行时组件。选择自研管线还是 ROS 2，不能只比较一条消息的开销；还要一并评估进程退出、参数一致性、观测工具、跨机需求和后续维护人力。

=== 工程日志：把判断依据和失败路径留下来

`aimer/auto_aim/README.md` 在固定提交中有 522 行，按日期倒序记录算法改动、上车观察、回退和待办。它不能替代可重复实验，但能告诉读者某项设计在什么问题之后出现、后来是否被撤回。阅读发布代码时把日志日期放在旁边，往往能避免把短暂试验误认为最终架构。

日志在 README:119 留下了一段方法论自述（着重号来自原文）：

#blockquote[
最好不要以 *效果* 为驱动力，应该以 逻辑 为驱动力。当发现 效果 存在问题时，应该溯源找到系统的哪个部分出了问题。效果是这样来的：原因 -> 逻辑 -> 代码 -> 效果。当不能定位系统的哪个部分出问题时，不要通过修改别的部分来达到效果，例如，不要把 距离 - 抬头补偿表格 写入代码的主分支，那最多作为一个放弃更新的实用分支。但也可以通过摧毁系统的若干个不合理部分来达到效果，有时也需要 基于 某个原因引入新的部分。当然不得不说有的时候硬补偿真香。
]

这段话强调先追踪“原因—逻辑—代码—效果”，同时承认赛期内有时会采用硬补偿。它不是要求拒绝所有经验参数，而是要求区分物理参数与临时校正，并记录校正在哪些工况和版本上成立。

README:167 给出了一个具体例子。相机到枪口的安装旋转本应描述坐标系关系，团队却曾根据落点偏差调整这个角。这样做会同时旋转弹道解算所使用的重力方向，使一个局部落点校正改变整套坐标关系。作者随后重新梳理转换，指出抬头补偿只需要枪口坐标系下正确的 xyz。这个例子说明：参数名、单位和坐标系不是文档装饰；当参数被用于设计意图之外的调节时，必须检查它的所有下游消费者。

日志还留下了几组值得分别看待的观察：

- *角点误差的下游放大*（README:353）。作者报告，单板拟合角度的较大、非正态误差会使车辆中心左右漂移 2～3 cm，预测打击点可漂移约 10 cm；随后回退多项修改，并只在双板面积接近时采样角度和半径。这些数字属于当时网络、相机和目标条件，不是所有系统的误差上限。
- *130+ fps 时部分滤波器反而不稳定*（README:387）。作者观察到密集测量可能让角速度估计突升突降，而约 20 fps 时部分滤波器更稳定，并把原因解释为角度和半径数据非正态。这里的性能变化是日志中的观察，机制尚未被区分：非高斯误差、相邻帧强相关、`dt` 处理、过程／测量噪声是否随采样率缩放，以及固定参数只适合某一帧率，都可能产生相似结果。可以对同一段带时间戳数据做受控降采样，检查创新序列并按 `dt` 重标协方差，才能判断是哪一项在起作用。
- *斜视下的 PnP 测距偏差*（README:369、391）。项目在约 1.7 m 的特定斜视条件下报告 20～30 cm 偏差；半径估计又依赖距离，因此作者在双板像素面积差较大时拒绝更新半径。这是该系统的实车观察和防护策略，不能直接推广到其他镜头、标定、角点算法或距离。
- *角度跨越 $plus.minus 180 degree$ 的跳变*（README:190）。若直接对包裹角作差，滤波更新会把接近的两个方向误认为相差近一整圈。这里与「装甲板的跟踪」一章相互呼应：进入滤波器前应统一角度域并连续解缠，同时测试恰好跨边界的轨迹。
- *接线和光圈的赛场问题*（README:312）。原文记录“相机线松了，光圈松了”等担忧。它提醒部署测试覆盖连接器固定、应力释放、镜头锁紧和振动，而不是说明硬件问题必然比算法问题更重要。

日志顶部还列出六项“已发现明显缺陷”（README:4-11）。其中包括装甲板朝向误差的非正态性，以及在当时系统中，PnP 距离方差和侧向装甲板的稳定偏差使 3 m 外半径难以准确测量、远距离反陀螺命中率下降。公开缺陷清单便于读者理解作者已知的边界；没有同类清单的项目也不能据此推断维护者不知道问题，仍需结合 issue、测试和代码检查。

=== 测距方差随距离的四次方增长

`docs/auto_aim/pnp.md` 用针孔模型给出了一段简短推导。设物距为 $u$，目标在图像中的尺度为 $p$，已知物理尺寸与焦距合并为常数 $K$，则

$ u = K \/ p, quad (dif u) \/ (dif p) = -K \/ p^2 = -u^2 \/ K. $

若像素尺度误差足够小，可以做一阶误差传播：

$ "Var"(u) approx u^4 \/ K^2 "Var"(p). $

因此，在 $"Var"(p)$ 近似不随距离变化时，距离标准差随 $u^2$ 增长，距离方差随 $u^4$ 增长。这个结论有四个前提需要一起保留：像素误差小到一阶近似可用；物体尺寸和焦距给出的 $K$ 正确；投影关系适用；像素误差方差在所比较距离上近似恒定。它描述的是随机误差传播，不会自动解释斜视、光晕、错误角点和模型失配造成的系统偏差。

代入「相机模型」一章使用的 $f_x = 1739$ 和小装甲板宽度 $W = 0.135$ m，可得 $K = f_x W approx 235$ px·m。按一阶导数换算，2 m 处 1 px 约对应 1.7 cm 深度变化，5 m 处约为 10.6 cm。距离翻倍时，每像素对应的深度变化约变为四倍；若像素方差保持不变，深度方差约变为十六倍。

这与前文“斜视时出现 20～30 cm 偏差”不能合并成同一个原因。前者是在明确假设下的随机误差敏感度，后者是项目日志中的综合观测，可能包含角点系统偏置和姿态模型误差。两处从同一个反投影关系得到一致的平方项，是代数一致性检查，不是对真实误差机制的独立实验验证。

=== 工程代价与未验证项

上述方案还有几项不能忽略的工程代价与证据缺口。

*强耦合于具体整车配置。* 前馈常数 `t0`、`send_to_control` 与 `control_to_fire` 都与电机、机械结构、电控程序和标定流程有关。换车或更改控制参数后不能沿用旧值，应把配置版本与测量记录绑定，并重新验证稳态误差和时间分布。

*带符号的“延迟”参数。* `assets/base.param.toml:41` 把 `send-to-control` 配为 $-18$ ms，并注明“测量值”。一个纯粹的物理传播时长不应为负，因此该值至少包含时间基准、符号约定或校准偏置中的某一项；仅凭当前文档无法进一步确认具体机制。使用者应把它视为项目定义的带符号模型项，而不是可直接拿秒表解释的链路时长。

*构建完整性尚未在本章验证。* 仓库 issue 中有人报告公开代码缺文件、无法独立编译；本章没有搭建其原始依赖环境并复现该报告，因此只把它列为外部风险记录。没有公开许可证也意味着，即便技术上能构建，复制、修改和分发仍需另行确认授权。

*迭代开销需要在目标机复测。* README:375 报告当时车上单次预测 5～6 ms，其中三分搜索约 1.6 ms。数字缺少本章可运行的基准源码、硬件型号和统计分布，只能作为版本线索；新的硬件、代价函数和编译选项都可能改变结果。

==== 那么该怎么读它

这个仓库适合先按“设计说明 → 时间线 → 当前实现”阅读。推荐顺序是：根 `README.md`（31 行，了解项目自述和入口）→ `docs/auto_aim/latency.md`（延迟事件与两种时间关系）→ `docs/auto_aim/pnp.md`（误差传播假设）→ `aimer/auto_aim/README.md`（522 行倒序工程日志）。读日志时先记日期和“已抛弃”标记，再到相应源文件确认固定提交实际执行哪条路径。

文档解释设计意图，代码显示该提交的实现，测试才说明特定条件下的行为；三者不能互相替代。本章的面积法演变就是例子：4 月 25 日日志记录两路输入，4 月 29 日宣布删除，当前代码只剩投影代价。遇到文档与代码不一致时，应保留时间差和版本差，而不是简单宣布某一方永远更可信。

==== 本章小结

本章从固定提交中整理出四条连续的设计线索：用规则先验约束弱可观的姿态自由度；把车心限制到圆上，再迭代替换代价函数；将延迟拆成命名事件，并按问题组合不同时间项；通过弹丸图像残差校正机械、电控或补偿参数。每条思路都有明确收益，也都有不能省略的前提。

10 点角度表展示了残差可能互补，但简单平均与偏置扣除只是同批样本上的事后演示；项目随后删除了面积法。120 rpm 下 `10 ms → 7.2°` 是直接换算，30° 命中率为零则是日志在其测试范围内的报告。`yaw_v * t0` 是特定整车上的跨子系统前馈，弹丸检测闭合的是参数校正环而非完整命中反馈。把这些层次分开，仍能保留项目的工程启发，而不会把观察写成定律。

基础设施部分同样需要看接口细节：UMT 的 C++ 订阅默认容量为 1，但发布会按值复制 `T` 到每个队列，Python 默认容量又是 0；TOML 参数每秒轮询，却缺少并发读写同步和事务更新。短路径可以带来直接控制，也会把并发安全、退出行为、配置一致性和维护责任交还给项目团队。

最后，$"Var"(u) approx u^4 "Var"(p) \/ K^2$ 只在小误差、一阶传播和像素方差近似恒定时成立。它能解释随机深度误差为何随距离迅速放大，不能单独诊断斜视下的系统偏差。工程日志保留了这些尚未解决的边界，也为后续核查和验证提供了版本线索。

（本章核对的是 julyfun/rm.cv.fans 提交 `6abd1bf`。该提交没有提供公开许可证，故本章不摘录源码，只对公开文档作必要、有限引用并给出文件位置。特定引用是否适用某一司法辖区的法定例外，仍取决于用途与当地法律。文件名和行号基于该固定提交；角速度换算、样本内残差和 80 ms 反推值由本书脚本按引用数据计算，并非原文全部直接给出。）
