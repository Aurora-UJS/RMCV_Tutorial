#import "/template/template.typ": *

=== 先讲一件和代码无关的事

上一章那个仓库，你可以放心大胆地抄。这一章这个，不行。

翻开 rm.cv.fans 的根目录，你会看到 `aimer`、`base`、`assets`、`docs`、`UltraMultiThread` 这些目录，以及一个 `README.md`。你不会看到 `LICENSE`，也不会看到 `COPYING`、`NOTICE`。往深了搜——整个仓库里没有任何一个文件包含 "copyright" 这个词。这不是猜的，是拿 `find` 和 `grep` 在全库跑过一遍的结果。

这件事的法律含义，比大多数人以为的要重：*代码公开在 GitHub 上，不等于作者授权你使用它*。著作权是"自动产生"的——作品一完成，权利就归作者，不需要登记、不需要声明。开源许可证的作用恰恰相反：它是作者*主动让渡*一部分权利的声明。没有许可证，就没有让渡，默认状态是*保留全部权利*（all rights reserved）。你把这份代码复制进自己的仓库、编译进上场的车里、乃至改一改再发出去，严格说都缺少法律依据。

GitHub 自己的服务条款给了一个很小的口子：把仓库托管在 GitHub 上，意味着允许其他用户*查看和 fork*。仅此而已——查看、fork，不包括使用、修改、再分发。所以正确的理解是：这个仓库你可以*读*，可以*学*，可以在自己的实现里重新写一遍你从中学到的想法（*思想不受著作权保护，表达才受*），但不可以把它的源码搬走。

本书据此定了一条规矩，本章通篇遵守：*不出现任何一行 rm.cv.fans 的源码*。本章会大量描述它的算法思想、引用它 README 与文档里的文字段落（合理引用）、引用它公开文档里的实测表格、并给出文件名与行号帮你自己去定位——这些都不构成对源码表达的复制。想真正用它的代码，只有一条路：去仓库开一个 issue，问作者要一份明确的授权。

顺带把上一章那个对照补完，它是很好的许可证入门样本。rm_vision 系的几个仓库里，算法本体是 MIT，而 `rm_serial_driver` 是 Apache-2.0——同一套系统内部混用了两种许可证。两者都允许商用和闭源分发，但 Apache-2.0 额外带了两样东西：一是*明示的专利授权*（贡献者不能一边贡献代码一边回头拿专利告你），二是*更重的转载义务*（你得保留 NOTICE 文件、并标明自己改动了哪些文件）。MIT 只要求保留版权声明和许可证原文。所以"这个项目是开源的"这句话在工程上几乎没有信息量，真正要问的是*哪一个*许可证、*哪些*义务。养成打开仓库先看 LICENSE 的习惯，比读懂任何一个算法都省事，也更可能在某一天救你一次。

=== 画像：把通用性卖了，换极致延迟

作者的 GitHub 账号是 julyfun，项目出自上海交大交龙战队——README 顶部挂着交龙的 logo，链接指向交龙的主页。项目全称写在标题行里：*LMTD，Linear Modelled Top Detector*——线性建模的陀螺检测器。"Top" 在这里不是"顶部"，是*陀螺*，整个项目的重心从名字起就压在反陀螺上。

难得的是，作者把这个项目的取舍直接写在了根 README 的第一句（README:17）：

#blockquote[
手搓通信，参数，UI，数学和坐标变换，兼容性一般，但是非常快。
]

"兼容性一般，但是非常快"——十个字把一份技术选型说完了。上一章那个项目走的是另一条路：用 ROS 2 换生态、换规范、换可复用性，代价是每一层都要交框架的税。这里反过来，通信、参数系统、调试 UI、坐标变换全部自研，不依赖 ROS——具体是哪几件家什、各自换来了什么，后面有一节专门拆。这套东西换一台车、换一支队伍就得重新适配一遍，但它把每一环的时序都攥在自己手里。

所以读这个仓库的正确姿势不是"我要不要用它"——你几乎肯定不该整套用，何况法律上也不允许。正确姿势是：*它有四个想法值得你偷学*。这四个想法都指向同一个目标——在陀螺目标上把弹着点算准——而且其中两个共享同一种思维方式。下面逐个讲透。

=== 招牌一：把六个自由度砍成一个

先说清楚要解决什么问题。PnP 给你的是装甲板的完整 6 自由度位姿：三个平移、三个旋转。但「相机模型」一章已经算过一笔账，其中有一句结论在这里正好接上——装甲板近乎正对相机时，板面绕竖直轴偏转几度，四个角点的像素位置几乎不动，于是解算出的 yaw 抖得厉害。偏偏整车建模最依赖的就是 yaw。这是单目 PnP 结构性的软肋：它把六个数一起交给你，却没告诉你其中哪几个不可信。

rm.cv.fans 的应对（根 README:20，"降低自由度计算装甲板 Rotation，相比 PnP 精度显著提升"）是釜底抽薪：*那六个自由度里，真正未知的根本没有六个*。装甲板在车上的安装倾角由规则决定（常规装甲板后仰约 15°，随赛季变动、以当季手册为准），roll 在车身不侧翻时近似为 0——这两个角是*先验已知*的，不该拿去让优化器猜。剩下真正未知的旋转自由度只有一个：yaw。

于是问题从"六维非线性优化"塌缩成"一维搜索"：给定一个候选 yaw，连同已知的 pitch 与 roll，就能把装甲板的四个角点重投影回图像；把重投影角点与实测角点的偏差作为代价，在 yaw 的可行区间里搜出代价最小的那个值，用它覆盖 PnP 的原始输出。（根 README 对这一条只给了一句话的描述，上面这套"固定 pitch 与 roll、单搜 yaw"的具体形态是结合该项目别处的用法推断的——项目还另有一份 `aimer/auto_aim/README.md`，是 522 行的工程日志，本章后文凡是行号超过 31 的 `README:N` 都出自它，与上面引的 31 行根 README 不是同一份；那份日志在讲三分法时明确写了装甲板仰角取"比赛规则固定 15 度"，见 `auto_aim/README.md:465`；下一章 sp_vision 的同类实现则把这套做法完整地写在了明处，届时可以对照着看。）

为什么这样更准？因为你把先验从"初值"升格成了*硬约束*。PnP 的最小化是在六维空间里找最优，"观测对 yaw 不敏感"这件事不会因为你换个求解器就消失——它会把 yaw 的不确定性摊到别的自由度上，也会让噪声在 yaw 方向自由游走。而一维搜索里，pitch 和 roll 被钉死在规则给定的值上，噪声无处可去，只能在 yaw 这一个方向上表现——*用规则手册里白纸黑字的先验，去换一个本来就测不准的自由度*，这笔交易几乎稳赚。代价当然也在：一旦"pitch 是 15°"这个前提不成立（比如车在坡上，或者是某些特殊车型），这个方法会比老老实实的 PnP 错得更理直气壮。

这条思路的说服力，有一份跨项目的旁证。下一章要拆的 sp_vision，其 `solver.cpp` 里有个函数直接叫 `SJTU_cost`——用交大的名字命名，来源不言自明。但故事还有后半段：这个函数在文件里完整地定义着（`solver.cpp:220`），而真正计算重投影误差的地方（`solver.cpp:260`），调用它的那一行*被注释掉了*，实际生效的是朴素的四角点欧氏距离之和。

这半段比前半段更值得琢磨。它说明的不是"交大这套不行"，而是*开源生态里真实的吸收过程长什么样*：一段代码被另一支队伍读到、认可、*整段搬进*自己的代码库——连中文注释都一字不落地留着，只换了命名空间，也就顺带成了本章开篇那条许可证纪律的活体反例——起了个致敬式的名字，然后在自己的数据和自己的车上一验——没有跑赢更简单的方案，于是留着函数、注释掉调用。这一行注释里藏着一次完整的技术评估。读开源项目时，*被注释掉的代码往往比正在运行的代码信息量更大*：运行的代码只告诉你最终选了什么，注释掉的代码告诉你还试过什么、以及为什么放弃。

=== 招牌二：三分法，和一个比"谁更准"更值钱的结论

第二个招牌解决的是同一件事的另一面：陀螺车的整车中心在哪、当前朝向角是多少。

作者的做法（README:501）是这样的：当视野里同时出现两块装甲板时，*假定车辆中心落在以这两块板中心连线为直径的圆上*。在圆上取一个假想中心，再假定两块板都垂直于地面，就能反推出两块假想装甲板的空间坐标，进而算出它们各自的像素面积。假想点越接近真实中心，算出的*面积比*就越接近实测的面积比。作者的观察是：面积比的接近程度是一个关于角度的*单峰函数*——于是用三分法迭代若干次，就收敛到一个角度估计值。注意这是 4 月 25 日那一版的代价函数——下面那张 10 组实测表正是这一版测的。

作者在稍晚的一条日志（README:465，4 月 30 日）里把这套推理的出发点写得更明白：四块装甲板落在*以车辆中心为几何中心的矩形*上，所以车辆中心在两块可见板连线为直径的圆上；而算假想投影时用到的装甲板仰角，他明确标注为"比赛规则固定 15 度"——这正是招牌一那个先验，在这里第二次被当成硬约束用。

这条更晚的日志同时改了代价函数：不再比面积比，而是"该假设投影与真实投影的相差越小，假设的圆心就越接近真实圆心"——前一天的 4 月 29 日那条已经写明"更新三分法求取角度，删除面积法"（README:483）。发布代码坐实了这一步：`top_model.cpp:19-51` 的 `get_pts_cost` 是逐边计算的"像素距离 + 斜率角差"重投影代价，`SingleCost`（`:115-134`）与 `DoubleCost`（`:136-160`）都调用它，全库找不到任何面积比代价。也就是说招牌一和招牌二在最终代码里不只是"同一种思维方式"，而是同一个函数、同一条代码路径。

这里的几何值得补一句原文没展开的话，因为它其实是个漂亮的初等结论，而且比"矩形"这个说法更强。把车投影到地面上，四块板位于矩形四条边的中点，车心在矩形中心；相邻两块板的中心因此落在过车心的两条*互相垂直*的轴上——从车心看过去，这两块板张开的角恰好是直角。而"从哪些点看一条定长线段恰好成直角"，正是*泰勒斯定理*的内容：轨迹是以该线段为直径的圆。

这个直角与矩形是不是正方形*无关*：哪怕前后两对板的安装半径不等（矩形长宽不同），两条轴依然垂直，直角依然成立。所以"中心在圆上"不是一个凑出来的近似，而是四板车 $90 degree$ 布局的严格推论，且天然容忍双半径不等这件真实存在的事——「装甲板的跟踪」一章为此在状态里维护两个交替半径，正是同一个物理。这一步把车心的可行域从整个平面压成一条一维曲线，剩下的未知量只有"在圆上的哪个位置"这一个参数。

看出来了吗——*这和招牌一是同一种思维方式的第二次施展*。都是先把规则手册给定的结构（装甲板倾角 15°、相邻板相差 90°）翻译成硬几何约束，把搜索空间压到一维，再在这一维上做无导数搜索，用重投影一致性打分。招牌一搜的是 yaw 角，招牌二搜的是圆上的位置，骨架完全一样——在发布代码里更是字面意义上的同一个 `get_pts_cost`。把这个骨架记住，比记住任何一个具体公式有用。

顺带说一句为什么是*三分法*而不是二分法。二分法要的是"函数值符号变化"或者单调性，用来找*零点*；这里要找的是单峰函数的*极值*，符号帮不上忙。三分法在区间内取两个内点比较函数值：哪一侧的值更差，极值就不在那一侧，于是每轮把区间砍掉三分之一。它只需要能*算出函数值*，不需要导数——对于"投影一遍、算面积、比比值"这种没有解析导数的代价函数，正合适。

这里还有一个容易被忽略的后续。日志里有一条（README:417）记着：三分法模板封装后"改用 phi 优选法，提升效率"。phi 优选法就是*黄金分割搜索*，它和三分法解决同一个问题，快在哪里值得算一笔：三分法每轮要算*两个*新的函数值、区间缩到 $2\/3$，折合每次函数求值只缩到 $sqrt(2\/3) approx 0.82$；黄金分割法把内点取在黄金比位置，使得其中一个点在下一轮*可以复用*，于是每轮只需*一个*新函数值、区间缩到 $0.618$。在代价函数本身就要做投影和面积计算的场合，函数求值次数就是全部开销——把区间压到同样窄，三分法要花的求值次数是黄金分割法的约 2.4 倍，这一换省掉了一半以上的计算。*当你的搜索瓶颈在函数求值上时，该关心的不是每轮砍掉多少，而是每次求值砍掉多少*。

==== 十组实测数据，和一个反直觉的用法

作者在工程日志里留下了一份 10 组的实测对照表（README:503-514），把三分法和"直接用面积比计算角度"的老方法放在一起比。角度量在原文里叫 `l_to_zn`，真值是匀速旋转假设下手算出来的。这类量化对照在 RM 的开源项目里极其罕见，原表照录：

#block(breakable: false)[
#figure(
  table(
    columns: (auto, auto, auto),
    align: (center, center, center),
    stroke: (x, y) => (
      top: if y == 0 or y == 1 { 1pt } else { 0pt },
      bottom: if y == 10 { 1pt } else { 0pt },
    ),
    inset: 7pt,
    table.header([*实际 `l_to_zn`（匀速时手算）*], [*面积法测量值*], [*三分法测量值*]),
    [25.3], [21.3], [31],
    [29.3], [26], [35],
    [34.9], [32], [41],
    [40.5], [41.3], [44],
    [46.2], [46], [50],
    [50.5], [52.8], [52],
    [56.2], [61], [55],
    [61.8], [68], [61],
    [67.5], [73.7], [65],
    [71.7], [78.2], [70],
  ),
  caption: [两种角度估计方法的实测对照，单位为度，数据引自 rm.cv.fans 工程日志 `aimer/auto_aim/README.md:503-514`。读表规则：横着看第一行与最后一行——低角度处面积法偏*低*（21.3 对 25.3）、三分法偏*高*（31 对 25.3）；高角度处两者恰好调个个儿（71.7 度处面积法给 78.2、三分法给 70）。两列的误差符号相反，且各自在中段某处穿过零点。],
)
]

把这张表画成误差曲线，结构就藏不住了。

#figure(
  image("images/proj-cvfans-trisection.png", width: 100%),
  caption: [两个系统误差方向相反的角度估计器（脚本 `proj_cvfans_trisection.py`，三列原始数据逐字转录自上表，代价函数为 4 月 25 日那一版的面积比，其余量均由脚本算出）。左图：估计值对真值，灰色虚线是理想的 $y = x$，每个点向对角线引的细线段就是该点的误差本身，长度与方向一并可读。右图：带符号误差——品红（面积法）自下而上穿越零线，蓝（三分法）自上而下穿越，*两条曲线斜率相反、过零点也不同*（分别在约 47 度与 54 度），所以没有哪一个"更准"，只有各自更擅长的角度区间。绿色三角是两者的简单平均：它把一条 $plus.minus 6 degree$ 的、随角度摆动的偏差，压成了一条几乎水平的 $1.8 degree$ 常值偏置——扣掉这个常数后残差 RMS 只有 $0.5 degree$，而两个原始估计器各自的 RMS 是 $4.3 degree$ 与 $3.8 degree$。],
)

现在讲这份数据最值钱的地方，它藏在*怎么被用*里。原文（README:499）写得很清楚：三分法"与之前直接用面积比计算角度的方法*共同作为角度滤波器的样本*"。

*不是二选一，是两个都喂进滤波器。*

这一句把整件事的层次提上去了。如果你的目标是"选一个更好的估计器"，那张表能给的结论很有限——两者 RMS 相差不到半度，谁也没赢。但如果你的目标是"把角度估准"，那么*两个偏差符号相反的有偏估计器摆在一起，本身就是一种资源*：它们的误差不是独立同分布的噪声，而是方向相反的系统偏差，一起进滤波器时会互相抵消掉一大截。上图那条绿线就是这件事的量化结果——一条随角度摆动 $plus.minus 6 degree$ 的偏差，被压成一个几乎与角度无关的常数偏置，而常数偏置是最好对付的一类误差，标定一次减掉就行。

提炼成一句可迁移的话：*与其花力气去修一个估计器的系统误差，不如再找一个偏向相反的估计器*。这在工程上常常便宜得多——修系统误差要你搞清楚它的成因（可能是畸变、可能是灯条膨胀、可能是网络的非正态偏置），而找第二个视角只要求它"从另一个方向错"。多传感器融合、多假设估计的价值，很多时候不在于新增了多少信息，而在于新增的那份信息*错的方式不一样*。

要给这一段划三条边界。第一，作者本人对三分法的评价是克制的——原文（README:501）说它"存在一个系统误差，但不太大"，且"在 `l_to_zn` 小于 50 度时误差较明显"，别把它拔高成"三分法碾压面积法"。第二，上面那条"平均一下就好"是从这 10 个点里读出来的结构，作者原文只说了两者共同作为滤波器样本，*没有*说他用的是简单平均，也没有给出这 10 个点之外的验证。10 个点、单次实验、真值本身还是"匀速假设下手算"来的——这是一份有说服力的启发，不是一个已被证明的定律。

第三，也是最要紧的一条——这个"两法共喂"的安排只存在了四天。日志按日期倒序排列，上面引的 README:499 属于 *4 月 25 日* 条目；四天后的 *4 月 29 日*（README:483）作者写下"更新三分法求取角度，*删除面积法*，提升角度角速度测量准确度"——他删掉了其中一个估计器，并认为这样更准。再一天后的 4 月 30 日那份设计总述（README:460-468）里，进滤波器的角度已经只剩一个；发布代码也印证了这一点：`top_model.cpp:161-201` 的 `fit_single_z_to_v` 与 `fit_double_z_to_l` 都只有一路三分搜索，全库再找不到第二路面积法样本。所以*上面那条口诀是本书从这 10 个点里提炼的可迁移想法，不是该项目的最终选择——作者本人走了相反的路*。

代价也要如实记：作者自陈（README:375），含迭代法的单次预测在车上耗时已达 5～6 ms，其中三分法迭代约占 1.6 ms。为了一个角度估计花掉这个量级的时间，值不值，取决于你的帧率预算——这正是"极致算法"的定价。

=== 招牌三：延迟不是一个常数，是六个时间点

大多数队伍的自瞄里，延迟是一个数：一个标定出来的 $t_d$，加在预测时间上。rm.cv.fans 把它拆成了六个有名字的时间点（`docs/auto_aim/latency.md:7-14`）：`img`（相机曝光时间的中点）、`predict`（神经网络与预处理跑完、开始运动解算的时刻）、`send`（预测结束、准备发信号的时刻）、`control`（电控收到信号、电机开始动的时刻）、`fire`（这个信号对应的子弹射出的时刻）、`hit`（子弹命中的时刻）。

拆开的第一个收益是*可测性变得可讨论*。原文给每一段都标了性质：`send` 这个点被单独分出来，理由就写在表格里——"分离出这个时间点是因为这个时间点可以测量"；`img` 到 `predict` 到 `send` 这一段是本帧算法的真实耗时，可以逐帧测；而 `predict` 之后的几段只能预测。把"能测的"和"只能猜的"分开标注，是这份文档最专业的地方——一个混成常数的 $t_d$ 会让你永远分不清自己是在测量还是在拟合。

拆开的第二个收益更狠：*同样这六个点，可以按需求组合成两个不同的和*。

- *预测时刻*（代码里叫 prediction）取 `img` 加上 `img_to_predict`、`predict_to_send`、`send_to_control`、`fire_to_hit` 四段。注意里面*没有* `control_to_fire`。
- *命中时刻*取 `img` 加上全部五段延迟，`control_to_fire` 也在内。

第一个和少加一段，不是疏忽，是想清楚了的取舍。原文（`latency.md:34`）的理由是："不妨假设子弹始终在发射，就像发射水流一样"。展开讲——`control` 到 `fire` 之间确实有几十毫秒（传动要时间、子弹加速要时间），但视觉端并不知道自己这一帧的指令对应的是哪一颗子弹；而由于摩擦轮持续供弹，可以近似认为*任何时刻都有子弹在出膛*。既然如此，与其纠结"我这条指令的那颗子弹何时射出"，不如认定"电机在 `control` 时刻到位，那一刻出膛的子弹就该命中"——于是瞄准点该算到 `img` 加 `img_to_control` 加 `fire_to_hit`，`control_to_fire` 被*故意*丢掉。而当你要回答另一个问题——"这颗子弹什么时候落地"（比如为了复现弹道）——就必须把 `control_to_fire` 加回来。*同一组时间点，两个问题，两个不同的和*。

#figure(
  image("images/proj-cvfans-latency.png", width: 100%),
  caption: [六个时间点按真实比例画开，以及从它们构建出的两个不同的和（脚本 `proj_cvfans_latency.py`）。上图读图规则：绿色是*预测视界*（该往哪儿指），在 `control` 与 `fire` 之间断开成虚线——那正是被*故意*跳过的一段；紫色是*命中视界*（这颗子弹何时落地），五段全含。品红那段箭头朝*左*：配置里的信号传输延迟是 $-18$ ms，一个负值——它不可能是一段物理传输时间，实际承担的是整体校准偏置的角色。视觉段的 80 ms 是由作者实测的约 82 ms 均值减去两个配置段*反推*所得，其内部 `predict` 的位置无从得知，故画成虚线。下图读图规则：把每段延迟乘上 120 转/分舵轮的角速度（$720 degree$/s），换算成"若把这段直接忽略会产生多大瞄准偏角"——两段大头各自就压过品红虚线（$30 degree$，作者实测反陀螺命中率归零的门槛）：视觉段 $58 degree$、飞行段 $86 degree$，任一段被含糊掉，反陀螺命中率直接归零；另两段虽只有 $13 degree$ 与 $14 degree$，够不到这条线，却各相当于约两个 10 ms 滑移（灰色点划线 $7.2 degree$，作者说单是这 $7 degree$ 就"很可能打不中"）。四段没有一段可以当噪声抹掉。],
)

图里那条 $30 degree$ 的红线来自本项目最该被引用的一句话（README:319）：

#blockquote[
为什么尝试精准估计延迟？因为有的车转的太快了……对于 120 转的舵轮，每 10 ms 的误差就会导致目标装甲板已经转过去 7 度。反陀螺坐标推算本身的误差尚可接受，但加上这样的 7 度就很可能打不中了。更糟糕的情况是，如果误差达到 30 度以上，反陀螺的命中率会是 0，刚好每发都打在两装甲板之间。
]

这两个数值得自己验算一遍，因为它们是"延迟为什么要命"最凝练的表达。120 转/分即每秒 2 转，也就是 $720 degree$/s；10 ms 对应 $7.2 degree$，与原文的"7 度"吻合。反过来，$30 degree$ 的偏差只需要约 42 ms 的延迟误差就能攒出来——按本书 100 fps 的工作假设，*不过 4 个帧间隔*；对照「进阶篇」性能优化技巧一章，一个深度 5 的相机队列白白积压的就是约 40 ms，*单这一项就几乎攒满了命中率归零的门槛*。而"刚好每发都打在两装甲板之间"的几何理由是：相邻两板中心相差 $90 degree$，偏差累积到三分之一个板间距时，弹着点已经滑进两块板中间的空档。这句话的杀伤力在于它描述的不是"精度下降"，是*命中率归零*——一个本该连续退化的量在这里变成了断崖。

==== 视觉替电控擦屁股

这份文档里还有一段，是全书最好的"权衡教材"之一，值得原样讲清楚。

下位机的 PID 在跟踪*斜坡输入*时有稳态误差——这是控制理论里的标准结论：一型系统跟阶跃无差，跟斜坡则留下一个正比于输入斜率的常值稳态误差。而视觉端发给电控的 yaw 指令，在目标匀速转动时恰好就是一个斜坡。原文（`latency.md:22-26`）记录了现象和分析：稳态偏差可描述为 `yaw_v` 乘一个时间常数 `t0`，`t0` 与电机性质和 PID 参数相关。文档里给的例子是——角速度 $10 degree$/s 的目标，视觉的命令最终会停在 yaw $= 2$（理想情况应该收敛到 0）。按 `yaw_v` 乘 `t0` 反推，这个例子里的 `t0` 约合 0.2 s。

视觉端的应对是：把发出去的指令从 yaw 改成 yaw 加 `t0` 乘 `yaw_v`，`t0` 通过跟踪匀速目标暴力测出。效果是好的——原文说这确实能让稳态下视觉期望的 yaw 收敛到 0。

现在来评价它。从系统角度看，这是一个*前馈补偿*：视觉预先加上电控将要丢掉的那部分，把闭环的稳态误差在开环侧填平。手法本身干净、有理论依据。但它同时是一个赤裸裸的*跨层耦合*——原文自己都写了，这个稳态偏差"可能是当前的一个算法缺陷"，缺陷在电控侧，补丁打在视觉侧。代价至少有三条：其一，`t0` 是电控 PID 参数的函数，电控哪天调了增益，视觉这边的补偿就默默失效，而且*没有任何报错*；其二，`t0` 要逐车标定，换车重来；其三，也是最隐蔽的——从此以后视觉发出去的 yaw 不再是"我希望你转到的角度"，它的物理含义被污染了，下一个接手的人读这行代码会困惑很久。

那该不该这么做？在一个赛季只有几个月、电控和视觉是两拨人、比赛下周就开始的现实里，*该*。这是工程判断，不是技术妥协。但要做得诚实：把它记进文档（作者做到了）、给参数起个能自解释的名字、并且知道正确的修法是在电控侧加前馈或改积分环节。*所有跨层补丁都在借钱，能借，但要记账*——这是本节真正要留下的东西。「弹道解算」一章讲的补偿是在补*物理*（重力、空气阻力），本节这个补的是*另一个子系统的缺陷*，两者形似而神不同，别混为一谈。

=== 招牌四：让相机看着自己打出去的子弹

前三个招牌是把目标算准，第四个是把*自己*算准。

三家开源项目里，独此一份的是它的自主弹道校正。机制大致是这样（`aimer/auto_aim/predictor/aim/` 目录下 `aim_corrector`、`detect_bullet`、`do_reproj` 三组文件，共约 1000 行）：电控回传"最近发射的子弹 id"，视觉侧据此回溯查出这颗子弹对应的那一帧瞄准信息，用弹道模型模拟出它*理应*飞出的轨迹，再把这条轨迹投影成图像上的一个圆；与此同时，图像里去*真的把这颗子弹检测出来*。理想位置与实测位置之差，就是一个二维的校正量，反过来修正瞄准。`aim_corrector.hpp` 开头的注释把适用前提写得很清楚：它假设瞄准计算本身无误，发射误差仅来源于机械结构、电控或视觉补偿参数的错误——*这是一个校正参数的工具，不是一个兜住算法错误的工具*。

为什么这件事重要，要放到上一章的对照里才看得清。rm_vision 的设计取舍是把弹道解算、选板和开火整体推给下位机，视觉只管吐整车状态——好处是视觉侧极简，代价是*视觉永远无法验证自己*：你算出的提前量对不对，唯一的反馈是操作手说"好像打不中"。这里正好反过来：让相机看见自己打出去的子弹，等于给整条开环链路装上了一只眼睛，把"预测—发射—命中"从开环变成了闭环。

这也正是那句方法论金句的最佳注脚——没有观测，你就只能靠"效果"去猜；有了观测，才谈得上定位到底是哪一段出了问题。

=== 不用 ROS，那些自研件长什么样

四个招牌讲完，回头看承载它们的那副骨架。README 那句"手搓通信，参数，UI"对应三件东西，每一件都值得看一眼——因为它们回答了同一个问题：*不用 ROS，你到底要自己补上什么*。

*进程内消息分发：UltraMultiThread（UMT）。* 仓库里带着这个小库，`include/umt/` 下三个头文件加起来约 550 行，提供模板化的 `Publisher<T>` / `Subscriber<T>` 和一条按主题名索引的 `MessagePipe<T>`，另有超时、空、已停止三类异常。对照一下量级：ROS 2 要用一整套 DDS 中间件才能提供的发布订阅，这里 550 行就够了。差别不在实现水平，在*需求范围*——UMT 只做*进程内*通信，于是序列化、网络发现、跨机传输、QoS 协商这些最重的部分统统不需要，消息就是内存里的对象，直接传引用。「自瞄算法设计」一章讲 ROS 2 的进程内零拷贝时说过，跨进程的代价主要花在序列化上；UMT 干脆从一开始就不给自己制造这个问题。它用 pybind11 是为了让 Python 侧（调试脚本、网页后端）能接进同一条消息管道。

这里还有个细节很值得抄：`Subscriber` 的队列容量*默认为 1*。一个容量为 1 的订阅队列意味着"只保留最新的一条，旧的直接丢"——这正是「工业相机」一章讲取流与推理解耦时给的结论。对自瞄这种*只关心最新状态*的实时系统，积压一队旧帧毫无意义，只会让延迟越滚越大。把这条策略设成默认值而不是可选项，是一个把领域知识写进 API 的好例子。

*运行时热更新的参数表。* 参数存在 TOML 里，跑着的时候可以改、改完立刻生效。这件事在训练场上的价值极高：调一个滤波器方差、调一个补偿角，如果每次都要停程序、重编译、重启、重新对准目标，一下午调不了几轮；热更新把这个循环从分钟压到秒。而且参数文件里每一项都带 `@brief` 和 `@param` 注释，写明"调大会怎样"——这是给未来的自己和学弟写的说明书，成本极低、回报极高。

*Flask 网页调试界面。* 车上跑的是无显示器的计算设备，最自然的调试出口就是网页：浏览器连上去，看图像、看俯视图、看日志流。日志里能看到这套界面被反复迭代（README:178 把俯视图分离到单独的复选框，README:219 加了往网页输出日志的全局对象）。

把这三件放在一起，"兼容性一般，但是非常快"的另一半含义就清楚了：*不用 ROS 省下的不只是运行时开销，还有"每加一个功能都要按框架的方式来"的约束*；而代价是这三件家什你都得自己写、自己维护、自己踩坑——README:84 记着一个 Flask 的 bug 让程序无法用 Ctrl+C 终止，这类问题用 ROS 是不会遇到的，因为别人已经替你遇到过了。*框架的价值从来不是它能做什么，而是它替你挡掉了什么*。选型时该问的不是"哪个更好"，而是"我这支队伍有没有人愿意长期维护自研的那一份"。

=== 一份 500 多行的工程日志

这个项目最富矿的部分不是任何一段算法，是 `aimer/auto_aim/README.md` 这份 522 行的文件。它不是文档，是一份*工程日志*：按日期倒序记录了每一次改动、每一次上车测试、以及大量"试过、失败了、回退了"的记录。开源项目里，把失败留在档案里的极少——多数仓库的 README 只呈现最终的胜利姿态。

日志开篇（README:119）有一段作者的方法论自述，值得当作本章的题眼整段引用（着重号是作者原文的）：

#blockquote[
最好不要以 *效果* 为驱动力，应该以 *逻辑* 为驱动力。当发现 *效果* 存在问题时，应该溯源找到系统的哪个部分出了问题。效果是这样来的：原因 -> 逻辑 -> 代码 -> 效果。当不能定位系统的哪个部分出问题时，不要通过修改别的部分来达到效果，例如，不要把 距离 - 抬头补偿表格 写入代码的主分支，那最多作为一个放弃更新的实用分支。但也可以通过摧毁系统的若干个不合理部分来达到效果，有时也需要 *基于* 某个原因引入新的部分。当然不得不说有的时候硬补偿真香。
]

这段话该被每一个调参调到深夜的人读一遍。RM 视觉的调试有一个致命的诱惑：*效果是可以被凑出来的*。打不中就加个补偿角，落点偏低就查个距离补偿表，反正参数够多，总能在训练场上凑准。而凑出来的系统有一个共同特征——换个距离、换个朝向、换台车就全废，因为你补的是症状不是病因。最后那句"有时候硬补偿真香"的自嘲尤其真实：作者知道正道是什么，也承认自己走过捷径。这种诚实比任何一句正确的口号都有教育意义。

日志里紧接着就有一个"用错误 A 补偿错误 B"的活体标本（README:167）。相机与枪口之间存在安装角度偏差，参数表里有一个补偿角用来把相机坐标修正到枪口坐标——本意如此。但实际使用中，大家总是*通过观察落点偏差去调这个补偿角*。后果是：这个角被当成落点微调旋钮用了，导致整个系统的*重力方向被偏转*，抛物线解算全错。作者重新梳理坐标转换后发现，抛物线抬头补偿根本不需要 yaw 和 pitch，只要枪口坐标系下正确的 xyz 就够。

这个坑的结构值得记住：*一个参数一旦被用于它设计意图之外的目的，它就会在你看不见的地方破坏别的东西*。补偿角的物理含义是"两个坐标系之间的旋转"，你拿它调落点，就等于在悄悄旋转整个世界——包括重力矢量。而症状（落点偏）和病因（坐标系被转歪）之间隔着好几层，几乎不可能靠试参数试出来。这正是上面那段方法论说的"原因 → 逻辑 → 代码 → 效果"倒着走的下场。

日志里还有几条，每一条都能独立成课：

- *网络的细微误差如何放大成 10 厘米*（README:353）。神经网络的角点误差会导致单板拟合角度出现*非正态*的较大误差，车辆中心左右漂移 2～3 cm，最终预测的打击点漂移可达 10 cm。一条从像素噪声到弹着点的完整放大链——注意"非正态"这三个字，它正是下一条的病根。作者的处置是回退大量修改、假定车辆匀速旋转，仅在双板面积很接近时才用双板拟合去采样角度和半径。
- *130+ 帧率反而更差*（README:387）。这条最反直觉：高帧率可能导致几个滤波器过于信任短时间内的密集测量值，误以为角速度陡增或陡降；反而在低到 20 帧率时部分滤波器发挥稳定。作者给的根因是——角度和半径滤波器接受的数据*并不服从正态分布*。这一条正好戳中「卡尔曼滤波」一章的前提：卡尔曼的最优性建立在噪声高斯的假设上，假设一破，"多给数据"就未必是好事，密集的同向偏差会被滤波器当成真实的运动趋势。*更快的相机不一定带来更好的估计*，值得写在墙上。
- *PnP 在斜视角下的测距偏差可达 20～30 cm*（README:369）。作者发现舵轮半径一直测不准的根因在此：测半径高度依赖测距准确性，而位置估计又依赖半径——误差在两者之间循环放大。解法很务实：当两块装甲板像素面积相差较大时（意味着至少有一块处在很斜的视角下），*拒绝*用当前测量去更新半径。识别出"这个观测不可信"并主动丢弃它，比给它一个大方差更干脆。
- *角度在 $plus.minus 180 degree$ 处的突变会让滤波器发散*（README:190）。观测的 yaw / pitch 在正负 180 度处跳变，未处理时会让 EKF 内部作差的部分出现跃差，乘上一阶展开的系数后 xyz 大幅偏离。这与「装甲板的跟踪」一章讲的 yaw 解缠是同一个病、同一种药，两个独立项目都栽过——足见它有多普遍。
- *硬件才是真正的敌人*（README:312）。上车测试季节，作者写下的是："现在上车测试比较害怕的问题是相机线松了，光圈松了等。"不是算法不收敛，不是滤波器发散——是线松了。这句话该给每一个准备通宵改代码的人看。

最后是日志顶部那份"已发现明显缺陷"清单（README:4-11），六条，作者自己列的。其中第 3 条把前面几条串了起来：由于 PnP 测距的巨大方差，以及因光线等问题对侧向装甲板测距存在稳态误差，模型难以准确测量 3 m 外的车辆半径，导致打击 3 m 外的陀螺命中率*严重下滑*。第 2 条则更根本——装甲板朝向角算法存在误差，且*误差不是正态的*。

一个项目敢在 README 最顶部挂出自己的缺陷清单，这份坦诚本身就是技术水平的一部分。你读别人的开源项目时，*先找有没有这样一份清单*：有，说明作者知道自己的边界在哪；没有，你得自己去把边界撞出来。

=== 测距方差随距离的四次方增长

上面几条坑反复指向同一个源头：PnP 测距的方差。作者在 `docs/auto_aim/pnp.md` 里给了一个只有几行的推导，干净得值得复述。

假设识别算法导致的*像素浮动长度不变*（比如角点始终抖动 2 px）。设物距为 $u$、测量到的像素长度为 $p$，两者是反比关系 $u = K p^(-1)$。对 $p$ 求导得 $(dif u) / (dif p) = -K p^(-2)$，把 $p = K \/ u$ 代回，得

$ dif u = K^(-1) u^2 dif p $

也就是说，同样大小的像素抖动 $dif p$，换算成距离误差时被放大了 $u^2$ 倍。既然误差正比于 $u^2$，其平方——方差——就正比于四次方：

$ "Var"(u) prop u^4 $

这个结论必须和「相机模型」一章对上，而且*对得上*，只是要小心别把指数说串。那一章推的是 $abs((dif Z) \/ (dif w)) = Z^2 \/ (f_x W)$，结论写作"每个像素的测量误差换算成深度误差，随距离平方增长"。两处说的是同一件事：这里的常数 $K$ 就是那里的 $f_x W$（焦距乘物体物理宽度），两个式子结构完全一致。区别只在*用哪个矩来表述*——

- 说*误差*或*标准差* $sigma$，配的是*平方*：$sigma prop Z^2$；
- 说*方差* $sigma^2$，配的是*四次方*：$"Var" prop Z^4$。

因为 $"Var" = sigma^2$，两句话严格等价，是同一条定律的两种矩表述，彼此印证而非冲突。*把"方差"和"平方增长"拼在一起，或者把"误差"和"四次方"拼在一起，结论就直接错一个量级*——这是本节唯一需要你小心的地方。

代进「相机模型」一章的数字体会一下：$f_x = 1739$、小板宽 $W = 135$ mm，于是 $K = f_x W approx 235$（像素·米）。2 米处每像素误差对应 1.7 cm 深度误差，5 米处 10.6 cm。*距离翻一倍，深度误差变四倍，方差变十六倍*。这就是为什么作者的缺陷清单里"3 m 外测不准半径"和日志里"斜视角下偏差 20～30 cm"会同时出现——它们不是两个 bug，是同一条平方律在两个方向上的表现。

顺带说，这一节还示范了读开源项目的一份额外收获：*一个独立来源用不同的方法，验证了你书里早已推过的结论*。交大这份文档从"像素浮动固定"出发用方差写，本书从"宽度反解深度"出发用误差写，殊途同归。当两个互不相干的推导给出同一条定律，你对它的信心该显著提高。

=== 代价清单

把该记的账记完，这一章才算诚实。

*强耦合于特定电控实现。* 前馈里的 `t0`、参数表里的信号传输延迟与控制到开火延迟，全都是在特定的车、特定的电控代码、特定的 PID 参数下暴力测出来的。换一台车要重标，换一支队伍的电控可能整个前馈逻辑都不成立。README 那句"兼容性一般"不是谦辞。

*那个负数参数。* `assets/base.param.toml:41` 里的信号传输延迟被设成 $-18$ ms，注释标着"（测量值）"，可它是*负的*——信号不可能比发出去更早到达。合理的推断是：这个参数实际承担的是整体校准偏置，把模型里别处的系统偏差一并吸收了进来（参数表自己的说明也印证了这一点，它写的是"调大将使对运动靶的跟踪更超前"，这是个调参手感的描述，不是物理量的描述）。这不算错——工程上用一个可调偏置去兜住模型残差很常见——但它意味着*这个"延迟"已经不是延迟了*，别照着名字去理解它。这也是给读者的一个通用提醒：*看到一个物理量取了物理上不可能的值，说明它的名字在骗你*。

*不可独立编译。* 仓库的 issue 里有人反馈开源出来的代码缺文件、不自洽、无法独立编译。（此条未经本书亲自复现构建，仅作为已知风险转述。）配上无 LICENSE 这件事，结论很清楚：*这个仓库的价值在于读，不在于跑*。

*算法开销。* 单次预测 5～6 ms、其中三分法约 1.6 ms（README:375）。在 100+ 帧率的预算下，这是一笔需要认真算的账。

==== 那么该怎么读它

上一章给 rm_vision 的建议是"按调用链读代码，一个下午读得完"。这个仓库的读法*不一样*，而且恰恰因为你不能抄它的代码，读法反而更清晰：*先读文档和日志，最后才读代码*。

推荐顺序是：`README.md`（31 行，五分钟看完取舍与四个招牌的入口）→ `docs/auto_aim/latency.md`（延迟建模的完整推理，本章招牌三的全部依据）→ `docs/auto_aim/pnp.md`（几行推导，但结论管着整个测距误差观）→ `aimer/auto_aim/README.md`（那份 522 行的工程日志，倒序读，从最新的缺陷清单开始）。到这里你已经拿走了这个项目九成的价值，而且*一行代码都还没看*。

这不是偷懒，是这个仓库的性质决定的：它的稀缺资源是*判断*——为什么砍自由度、为什么两个估计器一起用、为什么故意丢掉一段延迟、为什么高帧率反而更差——而判断全都写在文档和日志里。代码只是这些判断的一种实现，何况它无法独立编译、也不允许你复制。真想看实现，README 顶部那份功能列表已经把每一条对应的文件名列好了，照着点进去即可。

顺带，这也是一条通用的读仓库经验：*当一个项目的文档质量明显高于平均水平时，先把文档吃透再碰代码*；反过来，当文档自相矛盾或者写着 TBD 时（上一章那个项目就是），才必须以代码为准。判断该信哪一边，本身就是读开源项目的基本功。

==== 本章小结

这一章拆的是一个把通用性卖掉、换极致延迟与极致精度的项目。开篇先讲了一件和代码无关的事：仓库里没有 LICENSE，全库找不到任何版权声明，法律默认是保留全部权利——公开不等于授权，你可以读、可以学思想、可以自己重写，但不能搬代码。这条纪律本身就是本章的第一个交付。

技术上有四个招牌。*降自由度求姿态*：把规则给定的装甲板倾角与 roll 当成硬约束，六维 PnP 塌缩成一维 yaw 搜索；它的影响力有跨项目的实证——sp_vision 里那个叫 `SJTU_cost` 的函数，以及它被注释掉的调用。*三分法角度估计*：靠泰勒斯定理把车心的可行域压成一个圆，再用无导数的三分法在单峰代价上收敛；而那 10 组实测数据最值钱的结论不是"谁更准"，是两个偏差符号相反的估计器*可以*被一起喂进滤波器——与其修一个估计器的系统误差，不如再找一个偏向相反的。要注意这是本书从数据里提炼的想法而非该项目的归宿：作者四天后就删掉了面积法，发布代码只剩一路三分搜索。*分段延迟建模*：六个命名时间点、两个不同的和、故意丢掉的那一段，以及"每 10 ms 误差对应 7 度、误差过 30 度命中率归零"这条让延迟变得具体的换算；其中给电控补稳态误差的那个前馈，是"跨层补丁能借但要记账"的标准教材。*自主弹道校正*：让相机看见自己打出去的子弹，把开环的火控链路闭上。

架构上，它示范了"不用 ROS"要自己补齐什么：约 550 行的进程内发布订阅（默认队列容量为 1，把"只留最新帧"这条领域知识写进了 API）、运行时热更新的参数表、Flask 网页调试界面——省下框架开销的同时，也把维护这三件家什的责任全揽了过来。

方法论上，那份 522 行的工程日志给的东西比算法更耐久：以逻辑而非效果为驱动力；一个参数被用于设计意图之外就会在别处闯祸；高帧率不必然带来好估计，因为噪声未必正态；以及最朴素的那句——上车前最怕的是相机线松了。而 $"Var" prop u^4$ 那个推导，则和「相机模型」一章的深度误差平方律互为印证，前提是你别把方差和标准差的指数说串。

前两章看完，你手上有了两个极端：一个最规范的框架，一个最极致的算法。前者告诉你系统该怎么搭才不会烂，后者告诉你单点能被逼到多深，而两者各自的代价也都摊开了——一个把火控推给下位机因而无法自证，一个把通用性卖掉因而换车即失效。下一章要拆的「同济自喵」是三者中唯一仍在活跃维护、也是唯一交出了赛场量化战绩的项目，它的位置恰好在这两个极端之间：既吸收了前者的工程规范，也吸收了后者的火控与延迟思想，再补上自己的轨迹规划。看完它，我们再回过头来把三家并排放进一张表里，给出"先读哪个、抄哪一段、别抄哪一段"的建议。

（本章所述项目为 julyfun 的 rm.cv.fans（LMTD）。*该仓库未附带任何开源许可证，著作权人保留全部权利*，故本章通篇不含其源码片段；文中引用的文字段落与实测表格出自其公开的 README 与 docs 文档，属合理引用，文件名与行号基于本书写作时的仓库状态，可能随上游更新而变动。文中的角速度换算、误差统计、常数反推等数值均由本书脚本按引用数据算出，非原文给出。）
