#import "/template/template.typ": *

=== 先找主线，再钻进代码

本章以 rm.cv.fans 的提交 `6abd1bf`（2025-07-20）为准，后文的文件名与行号也都对应这个版本。

为什么值得专门读这个仓库？它没有把反陀螺写成一个孤立算法，而是把姿态约束、车辆中心搜索、整链路延迟和发射反馈连成了一套火控思路；522 行倒序工程日志还保留了方案被提出、试车、修改和放弃的过程。对新手来说，这比只看一份“最终正确答案”更有价值：你能看到一个工程判断是怎样形成的，也能看到它在哪些条件下会失效。

仓库位于 julyfun 的个人账户下，根 README 使用上海交通大学 RoboMaster 战队标志并链接到战队主页。标题把项目称为 LMTD（Linear Modelled Top Detector），文档与代码的重点都是旋转目标建模和反陀螺火控。

作者在根 README 的第一句（README:17）就写明了项目取舍：

#blockquote[
手搓通信，参数，UI，数学和坐标变换，兼容性一般，但是非常快。
]

通信、参数系统、调试界面、数学工具和坐标变换都由项目自行实现，换来的是更直接的数据流和时序控制，也意味着适配、并发安全、调试工具和长期维护都要自己承担。这里的“非常快”适合先理解为作者的设计目标；仓库没有提供与 ROS 2 的同硬件、同负载对照，不能据此得出框架间的通用性能排名。

第一次读时不必沿目录逐个文件啃。先抓住四个问题：透视 n 点（Perspective-n-Point，PnP）解算的 yaw（偏航角）不稳时，怎样用规则先验少估几个自由度；同时看到两块装甲板时，怎样把车心从二维平面约束到一条圆弧；计算、控制和弹道延迟怎样按问题重新组合；最后，飞行中的弹丸怎样反过来校正补偿参数。后面的基础设施和工程日志，都是为这四条主线服务的。

涉及实现时，本章会给出文件位置、测量表和必要短句，不大段摘录源码：固定提交没有提供公开许可证，复用代码前需要另行向作者确认授权。记住这条边界就够了，下面继续看技术主线。

=== 第一条主线：PnP 不稳时，先问哪些量其实已知

先从反陀螺里最容易踩坑的 yaw 说起。PnP 会给出装甲板的完整 6 自由度位姿：三个平移、三个旋转；但“能解出六个数”不等于六个数同样可靠。「相机模型」一章已经讲过，装甲板近乎正对相机时，板面绕竖直轴偏转几度，四个角点的像素位置可能只发生很小变化，yaw 因而容易抖动。偏偏整车建模又很依赖这个角度。

rm.cv.fans 的办法不是继续增加 PnP 的后处理，而是先问：装甲板姿态中有没有来自规则和机械结构的已知量？根 README 把这项特性概括为“降低自由度计算装甲板 Rotation，相比 PnP 精度显著提升”；工程日志则把装甲板的 pitch（俯仰角）按当时规则固定为 15°。如果 roll（横滚角）也已知或可以忽略，旋转部分就只剩 yaw 需要搜索。

于是算法可以枚举候选 yaw，把已知 pitch、roll 和装甲板尺寸一起重投影到图像，在可行区间内寻找角点残差最小的位置。原本三个旋转自由度的估计，就变成了一维搜索。这个流程是把根 README、工程日志和后续同类实现串起来得到的，文档没有给出完整伪代码或对照实验，所以“精度显著提升”仍是作者报告，而不是本章复现结果。固定 15° 也只是当时规则下的参数，换赛季、车型或安装方式都要重新确认。

这里顺便约定引用方式：根 `README.md` 只有 31 行，后文超过 31 的 `README:N` 都指 522 行的 `aimer/auto_aim/README.md`。

降自由度为什么可能有用？因为它不再让图像不敏感的旋转方向跟着像素噪声自由漂动。代价也同样直接：硬约束会把先验误差原样带进结果。斜坡、车体侧倾、装甲板安装误差、特殊车型和规则变化，都可能让固定 pitch 或 roll 失效。验证时要分别看正视、斜视、坡面和不同车型上的姿态误差与重投影残差，不能仅凭“变量变少了”就宣布一定更准。

读到后面的 sp_vision 时还能看到一个有意思的对照：文件中保留了名为 `SJTU_cost` 的候选代价函数，但当前调用使用四角点欧氏距离，`SJTU_cost` 调用被注释。这只能说明候选实现还留在文件里，无法仅凭注释判断它为何停用；需要提交历史、作者说明或对照实验才能回答。读仓库时要养成这个习惯：文件里“写着”的路径和当前“跑着”的路径，必须分开确认。

=== 第二条主线：两块装甲板为什么能把车心约束到圆上

第一条主线用规则减少旋转自由度，第二条主线则用整车几何缩小车心的搜索范围。4 月 25 日的工程日志（README:499-501）记录了最初方案：同时看到两块装甲板时，先把车心限制在“以两块板中心连线为直径的圆”上；再对圆上的候选车心生成两块假想装甲板，以投影面积比和观测面积比的差作为代价。作者假设这项代价沿圆周参数近似单峰，因此用三分搜索找角度。

为什么会得到这个圆？假设四块装甲板中心分别位于车体矩形的四边中点，而且当前看见的是相邻两块板，那么从车心指向这两块板中心的方向互相垂直。根据泰勒斯定理，所有能让这条线段张成直角的点，都落在以线段为直径的圆上。矩形的长宽可以不同，所以前后半径和左右半径不必相等；真正不能少的是“相邻板”“矩形布局”以及投影到地面后仍满足直角关系。特殊车型、坡面姿态或装甲板误关联会破坏这些前提。

注意，圆约束只把车心从二维候选域缩到一维，并没有直接给出唯一答案。还要根据装甲板可见方向和投影一致性，在圆弧上选出最合适的位置。这个区分很重要：先验负责缩小搜索空间，图像观测负责在剩余空间里作选择。

接着看方案怎样演变。4 月 29 日日志写下“删除面积法”（README:483），4 月 30 日改为比较候选投影与观测投影的差（README:465）。当前版本的 `top_model.cpp:19-51` 用 `get_pts_cost` 逐边组合像素距离与斜率角差，`SingleCost`（`:115-134`）和 `DoubleCost`（`:136-160`）都调用它，不再使用面积比。保留下来的是“圆上的一维搜索”，被替换的是“怎样给候选点打分”。这比记住某个代价公式更值得借鉴：几何关系若仍成立，可以继续作为搜索约束；观测模型则应随着数据暴露的问题迭代。

这里为什么用三分搜索而不是二分法？二分法通常用来找零点，三分搜索解决的是给定区间内的单峰极值。每轮比较两个内点，就能排除代价更高的一侧；它不需要导数，却依赖“区间内近似单峰”这个条件。遮挡、角点误配和多个相近极小值都可能让条件失效。部署前最好把代表性样本的代价曲线画出来，检查搜索区间和极小值附近的形状，而不是只确认迭代最终停了下来。

5 月 1 日志又记录“改用 phi 优选法”（README:417），也就是黄金分割搜索。常规三分搜索每轮计算两个新点，把区间缩到 $2\/3$；黄金分割搜索初始化后可以复用一个内点，每轮只计算一个新点，区间缩到约 $0.618$。若两者使用相同停止尺度，并且只比较新增的函数求值次数，三分搜索约是黄金分割搜索的 2.4 倍。这个数字不是整段程序的加速比：它还假设区间单峰、正确复用内点，而且函数求值确实占主要开销。若投影代价很便宜，控制逻辑和其他处理时间会稀释这部分差异。

==== 十组记录能说明什么

工程日志保留了 10 组对照数据（README:503-514），正好可以练习怎样读一张早期实验表。它比较的是面积法与 4 月 25 日版本的三分搜索。第一列原名为“实际 `l_to_zn`（匀速时手算）”，也就是说，它是建立在匀速假设上的手算参考值，并非独立测量系统给出的基准真值。先把三列原始数值放在一起：

#block(breakable: false)[
#figure(
  table(
    columns: (auto, auto, auto),
    align: (center, center, center),
    stroke: (x, y) => (
      top: if y == 0 or y == 1 { 1pt } else { 0pt },
      bottom: if y == 10 { 1pt } else { 0pt },
    ),
    inset: 7pt,
    table.header([`l_to_zn` 手算参考值（匀速假设）], [*面积法估计*], [三分法估计]),
    [25.3], [21.3], [31],
    [29.3], [26], [35],
    [34.9], [32], [41],
    [40.5], [41.3], [44],
    [46.2], [46], [50],
    [50.5], [52.8], [52],
    [56.2], [61], [55],
    [61.8], [68], [61],
    [67.5], [73.7], [65],
    [71.7], [78.2], [70],
  ),
  caption: [两种角度估计与手算参考值的对照，单位为度，数据引自 `aimer/auto_aim/README.md:503-514`。],
)
]

仅看估计值不容易发现规律，把它们减去手算参考值后，残差结构会清楚很多：面积法从低估逐渐转为高估，三分搜索则从高估转为低估；面积法残差在中段多次过零，并不存在一个可以概括全部样本的“决定性过零点”。

#figure(
  image("images/proj-cvfans-trisection.png", width: 100%),
  caption: [10 个样本上的估计值与残差，三列输入逐字转录自上表，由脚本 `proj_cvfans_trisection.py` 绘制。绿色曲线是两种估计的算术平均。],
)

4 月 25 日的日志还说，两种估计会“共同作为角度滤波器的样本”，所以作者当时考虑的并不是简单二选一。不过，原文没有公开滤波器怎样给两路样本加权，也没有说使用算术平均。图中的绿色曲线只是本章做的事后演示：在同一批 10 个点上，简单平均的平均残差为 $1.825 degree$；再用这 10 点估计并扣除同一批数据上的常值偏置，样本内残差均方根（Root Mean Square，RMS）为 $0.510 degree$。作为对照，面积法和三分搜索相对手算参考值的 RMS 分别为 $4.3 degree$ 与 $3.8 degree$。

这组结果看起来很漂亮，但还不能拿来宣称融合精度。偏置就是用这 10 点拟合后又在这 10 点上评估的，既没有留出测试集，也没有独立真值；两路估计还共享角点和投影模型，误差很可能相关。若真要做融合，应先在标定集上估计偏置与残差相关性，再换场次或用留出轨迹检查它们是否仍然互补。只有样本外误差继续下降，这条思路才经得住验证。

后续日志给出了更直接的工程选择：4 月 29 日删除面积法，4 月 30 日的设计说明和当前实现都只保留投影代价搜索。于是这 10 个点最有价值的地方，不是选出一个“获胜算法”，而是留下了“先尝试两路输入，随后删除其中一路”的依据。README:375 还记录，当时车上单次预测耗时 5～6 ms，其中三分搜索约 1.6 ms。硬件型号、构建条件和统计分布没有一并给出，把方案迁到自己的目标机时仍要重新测量。

=== 第三条主线：延迟别急着合成一个常数

姿态和车心都算对了，瞄准仍可能落在装甲板后面，原因往往不是空间模型，而是时间没对齐。把整条链路压成一个常数 $t_d$ 用起来方便，却容易把实测间隔、配置补偿和模型假设揉在一起。rm.cv.fans 的做法值得借鉴：先在 `docs/auto_aim/latency.md:7-14` 给六个事件命名，再讨论哪些时间段应该相加。

`img` 是曝光中点；`predict` 是神经网络与预处理完成、开始运动和目标解算的时刻；`send` 是预测结束、准备发出信号的时刻；`control` 是电控收到信号并开始驱动电机的时刻；`fire` 是对应弹丸出膛的时刻；`hit` 则是弹丸到达目标的时刻。

这些名字不是为了把图画复杂，而是为了让每一段都能追溯来源。分开来看：

- *img → predict → send* 属于本帧计算路径，原则上可用同一个时钟来源逐帧测量。
- *send → control* 跨越视觉计算机和电控，需要两边共同观测或先建立时钟映射。
- *control → fire* 取决于电控与发射机构。
- *fire → hit* 由弹道模型和目标距离决定。

这些时间段最后当然可以求和，但最好连同各段的测量方法、分布和软硬件版本一起保存，而不是只留一个来历不明的总延迟。

接下来是容易读错的地方：同一组时间点，在项目里服务于两个不同问题。

- *预测云台应指向哪里*：代码中的 *prediction* 表示预测需要覆盖的时间范围，由下面四项相加得到：

  ```text
  img_to_predict
  + predict_to_send
  + send_to_control
  + fire_to_hit
  ```

  这项计算不含 `control_to_fire`；它回答的是，在项目假设下“云台此刻应指向哪里”。
- *为已发射弹丸重现轨迹*：此时需要估计实际 `fire` 与 `hit`，会用到包含 `control_to_fire` 的完整时间关系。

为什么第一条没有 `control_to_fire`？`latency.md:34-36` 使用了连续发射近似：系统不追踪“这条控制命令唯一对应哪一颗弹”，而是希望云台到达 `control` 时刻时，已经指向一颗此刻出膛的弹丸所需的位置。持续供弹时这样组织有道理；换成单发、低射频、明显的发射门控或必须逐发绑定的系统，就不能直接省略这一段。

#figure(
  image("images/proj-cvfans-latency.png", width: 100%),
  caption: [根据项目文档与配置绘制的延迟关系（脚本 `proj_cvfans_latency.py`）。它用于解释各时间项怎样组合，不是一次完整事件追踪。],
)

这张图有几个细节需要当场说清。日志记录的 `img → fire` 均值约为 82 ms；结合配置中的 `send_to_control = -18 ms` 和 `control_to_fire = 20 ms`，可以反推出 80 ms 的视觉侧合计项，但 `predict` 在这 80 ms 中的确切位置没有记录。`send_to_control` 为负也不表示信号在物理上“倒着传播”，它实际扮演了带符号时间校准项的角色，当前资料不足以继续拆出其中各类偏差。图中的 80 ms、18 ms、20 ms 和示例飞行时间都是区间或模型项，不是这些区间各自的测量误差；下半图另用 $delta t$ 表示时间误差，并以 $abs(omega delta t)$ 近似恒定转速下的角度偏差。

图中的 7° 与 30° 来自工程日志（README:319）：

#blockquote[
为什么尝试精准估计延迟？因为有的车转的太快了……对于 120 转的舵轮，每 10 ms 的误差就会导致目标装甲板已经转过去 7 度。反陀螺坐标推算本身的误差尚可接受，但加上这样的 7 度就很可能打不中了。更糟糕的情况是，如果误差达到 30 度以上，反陀螺的命中率会是 0，刚好每发都打在两装甲板之间。
]

角度换算可以直接复核：每分钟 120 转，也就是 120 rpm（revolutions per minute），等于 $720 degree$/s；10 ms 对应 $7.2 degree$，30° 则对应约 41.7 ms 的时间误差。前一个数字是单位换算；“30° 时命中率为 0”则是项目日志对当时测试情形的总结。由于日志没有同时给出样本量、射频、距离和命中判定，不能把 30° 当成所有车辆通用的开火阈值。

再用图像队列感受一下量级。若 100 fps 图像按先进先出（First In, First Out，FIFO）顺序处理，队列从最新帧向前积压 4 个帧间隔，最旧帧就落后约 40 ms；在恒定 120 rpm 的假设下，对应 $28.8 degree$ 的角位移。这个算例不是预测误差上限，因为实际结果还取决于时间戳、丢帧策略、转速变化以及预测器是否使用了正确的观测时刻。它真正提醒我们的是：高帧率并不能抵消排队，队列策略本身就是延迟模型的一部分。

==== 跨子系统的速度前馈

延迟文档还记录了一个上下位机联调现象：匀速目标下，视觉持续发送 yaw 命令，系统进入稳态后，命令仍保留一个本应回到零的偏差。项目用 `yaw_v * t0` 描述它，并指出 `t0` 会随电机和 PID 参数变化（`latency.md:22-28`）。文档示例中，目标角速度为 $10 degree$/s，稳态偏差约 2°，所以 $t_0 approx 0.2$ s。

熟悉控制理论的读者可能会想到一型线性闭环，也就是开环传递函数含一个积分环节的理想模型：在系统稳定、模型结构和输入定义都满足条件时，它确实可能表现为“阶跃输入零稳态误差、斜坡输入常值误差”。但当前文档没有给出下位机控制器结构、饱和、摩擦和离散实现，仅凭这一个现象还不能确认实际系统就是标准一型模型，更不能直接得出“加积分项”或“修改某个 PID 参数”一定能解决问题。

项目的处理是在视觉侧发送 `yaw + t0 * yaw_v`，也就是把目标角速度乘以一个标定时间，作为跨子系统的速度前馈。原文报告，它在当时的匀速实验中让视觉期望的 yaw 回到零。把这个办法用到另一台车之前，至少要记住两件事：它依赖匀速近似；电机、机械结构或控制参数一变，`t0` 就要重新标定。最好把参数单位、标定工况和电控版本写进配置管理，让通信两端校验版本，并在 PID 或机械结构变化后强制重标。补偿最终放在视觉侧还是电控侧，应通过上下位机联合实验估计动态参数，并结合阶跃、斜坡测试决定，不能只凭“这个模块看起来更顺手”来分配。

=== 第四条主线：把弹丸图像变成校正信号

前三条主线都在估计“应该往哪里打”，第四条开始观察“弹丸实际飞到了哪里”。当前版本在 `aimer/auto_aim/predictor/aim/` 下实现了 `aim_corrector`、`detect_bullet` 和 `do_reproj`。电控回传发射编号（id）后，视觉侧找到这个 id 对应的历史瞄准状态，用弹道模型重建预期弹丸轨迹并投影回图像，同时从图像中检测真实弹丸。两者在图像上的残差，随后用于调整补偿参数。

这里要把反馈闭环的范围看准。相机观察到的是飞行中的弹丸，不是弹丸是否命中装甲板，因此闭合的是“弹丸图像残差 → 补偿参数”这一环，而不是完整的命中反馈。目标检测、运动预测、弹道模型和发射 id 关联仍可能分别出错。`aim_corrector.hpp` 的注释也明确假定瞄准计算本身正确，把剩余发射误差归到机械、电控或视觉补偿参数。这个假设让校正问题变得可解，却也决定了它不能反过来证明整条“预测—发射—命中”链路都正确。

即便如此，这个设计仍很实用：原先只能从弹着效果间接猜测的偏差，现在变成了图像中可以保存和回放的残差。测试时应把发射 id 关联、弹丸误检、曝光与拖影、投影模型误差以及多发同时入镜分别拆开，并保留一组没有参与调参的射击序列。若参数只在调参用的射击上收敛，我们只能知道它解释了这些样本，还无法区分机械偏差与模型误差，更不知道下一场是否仍然有效。

=== 回头看基础设施：短路径需要自己保证什么

四条算法主线都依赖一套运行基础设施。图像和状态要在线程间流动，参数要在车上调整，结果还要能被人看见，这就对应了 README 里的“手搓通信，参数，UI”。读这部分时不要只数功能或代码行数，而要盯住三个问题：消息会不会积压或复制，参数更新是不是一致的，调试界面出故障时会不会拖住主程序。

*进程内消息分发：UltraMultiThread（UMT）。* 相关代码位于：

`UltraMultiThread/include/umt/`

这个目录下的三个头文件共 552 行，提供按名称索引的 `Publisher<T>`、`Subscriber<T>` 和 `MessagePipe<T>`，还定义了超时、空消息和发布停止等异常。它解决的是同一进程内的线程通信，不负责数据分发服务（Data Distribution Service，DDS）提供的跨进程发现、网络传输、可靠性和服务质量（Quality of Service，QoS）协商。两者范围不同，不能拿代码行数直接比较实现水平或性能。

真正影响图像链开销的是消息怎么传。`Publisher::push(const T&)` 会对每个订阅者调用 `fifo.push(obj)`，也就是给每条订阅队列各复制一个 `T`；`pop()` 再把队首对象移动给调用者，所以 UMT 不能笼统称为“零拷贝”。若 `T` 是 `std::shared_ptr` 或 `cv::Mat`，复制的通常是共享所有权或指向缓冲区的句柄，底层像素数据可能仍然共享；若 `T` 是自己拥有数据的普通容器，就会按它的复制构造行为复制内容。看到消息框架宣称“很轻”时，下一步应去查实际的 `T`，而不是只看 `push` 的函数名。

C++ 中带名称的 `Subscriber` 构造函数默认容量为 1：队列满后先丢最旧消息，再加入最新消息。对只关心最新画面的视觉链，这能主动限制陈旧帧积压；但 Python 绑定的默认 `fifo_size` 是 0，而源码把 0 解释为不限长度，同一接口在两种语言下因此有不同的默认行为。控制事件、发射确认和日志也未必适合容量 1，它们可能要求完整顺序。队列大小应跟着消息语义定，不能把图像链的策略原样套给所有主题。

*轮询式参数热更新。* 参数线程每隔 1 s 重新解析一次 TOML，再把读到的值写入共享的 `std::variant` 对象。这样调参不必重新编译或重启，参数文件里的 `@brief`、`@param` 注释也能记录方向和用途。不过，“支持热更新”只说明运行中能改，不代表修改会立即、成组且安全地生效：变更最晚要等下一轮解析，多项参数逐个写入时，算法线程可能看到新旧混合状态，配置中被删除的项目也不会自动从已创建的参数对象中消失。

当前代码里的写入 `*found = res` 和读取 `Param found = *ptr` 之间没有互斥，也没有一次切换整组配置。参数线程与算法线程若同时访问同一个 `variant`，就形成 C++ 数据竞争；这不只是“偶尔读到旧值”，而是未定义行为。另一个容易漏掉的坑是错误回退：名称不存在或类型不匹配时，接口记录错误后返回 `T{}`，于是 0、`false` 或空容器可能悄悄进入控制计算。更稳妥的流程是先完整解析配置，统一校验类型、范围和跨字段约束，构造一份发布后不再修改的新配置，再一次替换旧版本，并明确删除项、解析失败和回滚分别怎样处理。

*Flask 网页调试界面。* 车载计算机通常不接显示器，用网页集中展示图像、俯视图和日志很方便。工程日志既记录了界面功能的多次调整，也记录过一次 Flask 相关问题导致 Ctrl+C 无法正常结束程序（README:84）。这提醒我们，调试界面不是主算法之外“坏了也没关系”的附件，它同样参与进程生命周期。比较自研管线与 ROS 2 时，除了单条消息开销，还要把退出行为、参数一致性、观测工具、跨机通信需求和后续维护人力放在一起评估。

=== 工程日志该怎么看：先分清观察、解释和改动

`aimer/auto_aim/README.md` 有 522 行，按日期倒序记录算法改动、上车观察、回退和待办。这份日志最值得学的不是某个参数，而是它保留了判断发生的顺序：先看到什么现象，作者当时怎样解释，接着改了哪一层，后来又有没有撤回。日志不能代替可重复实验，但把日期与当前代码放在一起看，可以避免把一次短暂试验当成最终架构。

作者在 README:119 这样总结自己的调试思路（着重号来自原文）：

#blockquote[
最好不要以 *效果* 为驱动力，应该以 逻辑 为驱动力。当发现 效果 存在问题时，应该溯源找到系统的哪个部分出了问题。效果是这样来的：原因 -> 逻辑 -> 代码 -> 效果。当不能定位系统的哪个部分出问题时，不要通过修改别的部分来达到效果，例如，不要把 距离 - 抬头补偿表格 写入代码的主分支，那最多作为一个放弃更新的实用分支。但也可以通过摧毁系统的若干个不合理部分来达到效果，有时也需要 基于 某个原因引入新的部分。当然不得不说有的时候硬补偿真香。
]

这段话的重点是先追“原因—逻辑—代码—效果”，同时也承认赛期压力下有时会用硬补偿。它并不是拒绝所有经验参数，而是提醒你给参数分清身份：哪些描述物理关系，哪些只是特定工况下的临时校正；后者尤其要记录适用车型、版本和标定条件。

README:167 给了一个很典型的反例。相机到枪口的安装旋转本应只描述两个坐标系的关系，团队却曾根据落点偏差去调整这个角。问题在于，弹道解算中的重力方向也会被一起旋转，于是一个局部落点校正悄悄改变了整套坐标关系。作者后来重新梳理变换，指出抬头补偿真正需要的是枪口坐标系下正确的 xyz。遇到类似问题时，不要被“改这个参数确实有效”说服；先查它被哪些下游模块消费，以及每个消费者把它当成什么物理量。

按“观察—可能解释—下一步检查”的方式读，下面几条日志会更清楚：

- *角点误差的下游放大*（README:353）。作者记录，单板拟合角度的较大误差并不近似正态分布，会使车心左右漂移 2～3 cm，预测打击点可漂移约 10 cm；随后回退多项修改，并只在双板面积接近时采样角度和半径。这里能确认的是当时系统中的放大现象，2～3 cm 和 10 cm 不是其他网络、相机与目标条件下的误差上限。
- *130+ fps 时部分滤波器反而不稳定*（README:387）。日志观察到密集测量时角速度估计突升突降，而约 20 fps 时部分滤波器更稳定，并猜测与角度、半径误差不服从常见正态分布有关。高帧率本身并不能诊断原因：相邻帧误差强相关、时间步长 `dt` 处理不当、过程噪声和测量噪声没有随采样率重设，或者固定参数只适合某一帧率，都可能产生类似现象。可以对同一段带时间戳数据受控降采样，再检查创新序列，也就是“新测量与滤波预测之差”，并按 `dt` 重设协方差，逐项区分这些可能性。
- *斜视下的 PnP 测距偏差*（README:369、391）。项目在约 1.7 m 的一组斜视条件下记录了 20～30 cm 偏差；半径估计依赖距离，所以作者在双板像素面积差较大时拒绝更新半径。这是针对当时系统的防护策略，换镜头、标定、角点算法或距离后都要重新验证。
- *角度跨越 $plus.minus 180 degree$ 的跳变*（README:190）。若角度始终被限制在 $[-180 degree, 180 degree]$，再直接对这种包裹角作差，滤波器会把边界两侧原本接近的方向误认为相差近一整圈。处理方法与「装甲板跟踪」一章一致：进入滤波器前统一角度域并连续解缠，还要专门测试穿过边界的轨迹。
- *接线和光圈的赛场问题*（README:312）。原文记下了“相机线松了，光圈松了”等担忧。它对应的是一份很具体的部署清单：连接器固定、线缆应力释放、镜头锁紧和振动测试。日志没有说硬件一定比算法更重要，而是提醒你别让算法测试替代整机测试。

日志顶部还列了六项“已发现明显缺陷”（README:4-11），包括装甲板朝向误差呈非正态，以及在当时系统中，PnP 距离方差和侧向装甲板的稳定偏差使 3 m 外半径难以准确测量、远距离反陀螺命中率下降。读到这种清单，最有用的动作是把缺陷与当前代码中的保护条件、待办和测试对应起来；清单说明作者当时知道什么，但不能替代对当前版本的检查。

=== 一段值得亲手推的公式：远处测距为什么迅速变差

工程日志里多次提到远距离半径不稳，`docs/auto_aim/pnp.md` 则给出了一条能帮助判断量级的针孔模型推导。设物距为 $u$，目标在图像中的尺度为 $p$，把目标已知物理尺寸与焦距合并成常数 $K$，就有

$ u = K \/ p, quad (dif u) \/ (dif p) = -K \/ p^2 = -u^2 \/ K. $

对 $p$ 求导，可以看到同样大小的像素误差会怎样传到距离上。若像素尺度误差足够小，可以使用一阶误差传播：

$ "Var"(u) approx u^4 \/ K^2 "Var"(p). $

因此，如果 $"Var"(p)$ 在所比较的距离上近似不变，距离标准差会随 $u^2$ 增长，距离方差会随 $u^4$ 增长。读公式时别把前提丢掉：像素误差要足够小，才能使用一阶近似；物体尺寸和焦距组成的 $K$ 要正确；针孔投影关系要适用；像素误差方差还要近似不随距离变化。这条式子描述的是随机误差怎样传播，不会自动解释斜视、光晕、角点选错或模型失配造成的系统偏差。

代入「相机模型」一章使用的 $f_x = 1739$ 和小装甲板宽度 $W = 0.135$ m，可得 $K = f_x W approx 235$ px·m。用一阶导数换算，2 m 处 1 px 约对应 1.7 cm 的深度变化，5 m 处则约为 10.6 cm。距离翻倍后，每像素对应的深度变化约变为四倍；若像素方差不变，深度方差就约为原来的十六倍。这也是为什么远距离时，同样一两个像素的角点波动会显得格外严重。

不过，前文“斜视时出现 20～30 cm 偏差”不能直接归因于这条方差公式。公式讨论的是明确假设下的随机误差敏感度，日志数字则是整套系统的综合观测，里面还可能有角点系统偏置和姿态模型误差。两者都出现与距离相关的平方项，只能说明代数上彼此相容，尚不足以诊断真实误差由哪种机制主导。

=== 把思路迁到自己车上之前

读懂方案不等于可以原样搬走。真正动手前，建议先完成三项检查。

*把整车参数和版本绑在一起。* 前馈常数 `t0`、`send_to_control` 与 `control_to_fire` 都会受电机、机械结构、电控程序和标定流程影响。换车或改控制参数后，不要继续沿用旧值；配置文件应能追溯到对应的软硬件版本和测量记录，并重新检查稳态误差与延迟分布。

*先确认当前版本能在你的环境中完整构建。* 仓库 issue 中有人报告公开代码缺少文件、无法独立编译；本章没有搭建原始依赖环境复现，所以这仍是一条待核实线索。最小检查是按项目声明的工具链从干净环境构建，并把缺失依赖、生成文件和本地路径逐项记录下来。

*在目标机重测迭代开销。* 前文的 5～6 ms 与 1.6 ms 是工程日志留存的版本数字，没有同时给出硬件型号、构建条件和统计分布。更换处理器、代价函数或编译选项后，它们都可能变化。测试时至少记录目标机、编译器与优化选项、输入规模、预热方式、重复次数和延迟分位数。

==== 推荐阅读顺序

这个仓库适合按“设计说明 → 时间线 → 当前实现”来读。先看根 `README.md` 的 31 行，了解项目自述和目录入口；再看 `docs/auto_aim/latency.md`，理清六个事件和两种时间关系；接着用 `docs/auto_aim/pnp.md` 复核误差传播假设；最后读 522 行的 `aimer/auto_aim/README.md`，沿倒序日志寻找方案的提出、回退和“已抛弃”标记。每找到一条重要日志，再回到相应源文件确认当前版本实际调用哪条路径。

记住三类材料各自回答的问题：文档解释作者想怎样设计，代码说明这个提交实际实现了什么，测试才说明它在给定条件下表现怎样。面积法就是现成例子：4 月 25 日日志记录两路输入，4 月 29 日宣布删除，当前代码只剩投影代价。它们并不互相矛盾，只是属于不同时间点。遇到文档与代码不一致时，先查日期和版本，再判断哪一份材料能回答你眼前的问题。

==== 本章小结

rm.cv.fans 最值得带走的是一套逐步减少不确定量的工程方法：图像对某个姿态方向不敏感，就用规则与安装关系减少待估自由度；车心候选太多，就用整车几何把二维搜索约束到圆弧；时间链太乱，就先命名事件，再按控制或弹道问题选择区间；补偿难以标定，就把弹丸投影残差变成可保存的校正信号。每一步都先引入结构，再用观测选择或修正，而不是单靠增加优化器复杂度。

同样值得学习的是它留下了方案失败的过程。10 点表能提示两路残差可能互补，却没有证明融合在新数据上仍有效；高帧率下滤波不稳是观察，不等于非正态误差已经被确认为原因；距离方差的四次方关系来自带前提的一阶传播，也不能替斜视下的系统偏差作诊断。把观察、解释和验证分开，工程日志才会成为下一次实验的入口，而不是一串看起来很确定的结论。

最后别忽略支撑算法的基础设施。UMT 的队列容量、消息类型的复制行为、Python 与 C++ 的默认差异，都会改变延迟和积压；参数热更新缺少并发同步和整组切换，则可能直接影响控制计算。自研短路径确实能换来更直接的控制，但只有把这些接口行为、退出过程和配置版本一起管住，性能优势才有机会在实车上稳定复现。
