=== 从模块到系统

前面四章，你已经把自瞄的每一个器官做深做稳：工业相机稳定地吐出低曝光图像，识别器在一帧里框出装甲板，跟踪器用整车模型在时间维度上积累证据，能量机关那条线也自成一路。可这些还只是零件。把它们摆在一起，离一套"能上场"的自瞄系统，中间还隔着两个从没被正面回答的问题——本书前几篇一路把它们记在账上：

- *什么时候开火？* 「卡尔曼滤波」一章讲完整车建模，把"如何判定对手进入陀螺状态、开火时机怎么选"这两笔账明确挂到了本章。
- *打不中，是预测的锅还是弹道的锅？* 「弹道解算」一章验收固定靶之后，把"运动目标打不中时如何区分病根"留给了整链联调。

这两个问题看似无关，其实有一个统一的答案。本章先把散落的模块编排成一条完整数据流，再引入一套把"瞄准"和"开火"装进同一个框架的视角——*轨迹视角*，用它一次性回答上面两笔债，最后落到怎么把整套系统联调上场。算法原理与数学不在这里重推：整车 EKF 回指「卡尔曼滤波」，坐标链回指「坐标变换」，弹道回指「弹道解算」，本章只管把它们串起来、让它们协同、并在翻车时能定位到是哪一环。

=== 整体架构：一条数据流，两种编排哲学

一套自瞄系统的骨架，是一条七站流水线：

*相机 → 识别 → 跟踪 → 坐标变换 → 预测 → 弹道 → 通信。*

相机在惯性系一无所知的时刻曝光出一帧（「工业相机」一章），识别器给出装甲板的像素角点与图案类别（「装甲板识别」一章），PnP 把它解算成相机系下的位姿（「相机模型」一章），坐标链把位姿搬进惯性系（「坐标变换」一章），跟踪器用整车 EKF 估计出目标的运动状态（「装甲板的跟踪」一章），决策器沿模型把状态外推一个提前量、算出瞄准点，弹道解算把三维点变成云台角度与飞行时间（「弹道解算」一章），最后串口把指令送进下位机（「串口模块」一章）。

这条链每一站的内部都在前面讲过，本章关心的是*站与站之间*：数据以什么形式流动、哪些站在同一个进程里、谁先启动谁后启动、一站崩了另一站怎么办。这些"胶水"决定了系统能不能稳定跑满一场比赛——而它恰恰是各队差异最大的地方。社区里主要有两种编排哲学，值得摆在一起对照。

==== 编排哲学一：ROS 2 的组合节点

第一种是把每一站做成一个 ROS 2 节点，用 launch 文件编排。一套广泛使用的 ROS 2 自瞄框架（社区常称 rm_vision，MIT 许可证）就是这个路线的范本，它的编排里藏着几个"看似随意、其实全是账"的设计，逐一拆开：

*相机与识别塞进同一个容器。* 相机每秒吐出上百帧 1440×1080 的图像，如果相机节点和识别节点分属两个进程，每一帧都要走一次进程间拷贝——上百 MB/s 的无谓搬运。ROS 2 的*组合节点*（composable node）把两者放进同一个 `component_container`，开启 `use_intra_process_comms`，图像以指针在进程内零拷贝传递：

```python
ComposableNodeContainer(
    name='camera_detector_container',
    composable_node_descriptions=[
        camera_node,                              # 相机
        ComposableNode(                           # 识别，与相机同容器
            package='armor_detector',
            plugin='rm_auto_aim::ArmorDetectorNode',
            extra_arguments=[{'use_intra_process_comms': True}]),
    ],
    on_exit=Shutdown(),                           # 容器一挂，整个系统退出
)
```

*启动要错峰。* 三个动作不是同时拉起的：容器和 TF 发布器在 $t=0$ 起，串口驱动延迟 1.5 s，跟踪器延迟 2.0 s。

```python
delay_serial_node  = TimerAction(period=1.5, actions=[serial_driver_node])
delay_tracker_node = TimerAction(period=2.0, actions=[tracker_node])
```

这两个数字不是玄学。串口要等一会儿，是给相机枚举、下位机上电、USB 设备稳定留出时间——过早打开一个还没就绪的 `/dev/ttyACM0` 只会拿到一串异常；跟踪器排在最后，是因为它要把装甲板位姿从相机系变换到惯性系，依赖串口把云台姿态喂进 TF 树、依赖 TF 树先建立起来。启动顺序编码的是*依赖顺序*：谁的输入先就绪，谁就先起。

*TF 树分两层：静态链一次发完，动态链跟着串口走。* 这里容易想当然，值得拆开。`robot_state_publisher` 按 URDF 负责的是*静态*的那一段——`gimbal_link → camera_link → camera_optical_frame`——它发到 `/tf_static`，一次发完、此后不再更新；其中云台到相机的外参（`xyz=[0.10, 0, 0.05]`）写在 launch 参数里，是手眼标定的产物（「相机标定与手眼标定」一章）。而真正*高频承重*的 `odom → gimbal_link` 根本不由它发：那一段是串口驱动每收到一帧下位机姿态就广播一次，刷新率就是串口的收包率。

为什么必须是串口来发这一段？因为云台在飞速转动，相机曝光那一瞬间的云台姿态必须查得到——插值再准也架不住参考点太稀；而这个姿态只有下位机 IMU 知道，只从那根串口进来。顺带一个读 launch 文件的教训：`robot_state_publisher` 的参数里确实写着 `publish_frequency: 1000.0`，看着像"TF 以 1 kHz 刷新"，但这个参数只管*由 `joint_states` 驱动的活动关节*重复发布的节奏，而整份 launch 里没有任何 `joint_states` 发布者——所以那个 1000 实际上没有承载任何东西。*看到一个频率参数，先问清楚它到底在驱动哪条链。*

#figure(
  image("images/app-autoaim-architecture.png", width: 100%),
  caption: [ROS 2 组合节点编排下的自瞄数据流（脚本 `app_autoaim_architecture.py`）。读图规则：绿色虚线框内相机与识别共处一个容器、图像零拷贝流转（蓝色前向箭头）；跟踪器工作在 odom 惯性系；TF 树是*两层拼起来的*——灰框的 `robot_state_publisher` 只按 URDF 把静态链（`gimbal → camera → optical`）发到 `/tf_static`，动态的 `odom → gimbal_link` 由串口按收包率广播（橙色虚线），串口同时把云台指令下发给下位机、并回写识别器参数（紫色，敌方颜色/复位跟踪器）；底部时间轴是*承重的*——容器与 TF 在 $t=0$、串口 $+1.5$ s、跟踪器 $+2.0$ s，错峰启动编码的是模块间的依赖顺序，不是随手写的延时。],
)

值得点破的一层：这张图里，串口不只是"最后一站的出口"。它同时是三条反馈的枢纽——把下位机测得的云台四元数与实时弹速送回来供预测和弹道使用，把云台的实时变换灌进 TF 树，还把裁判系统读到的敌方颜色、复位信号回写给识别器和跟踪器。自瞄从来不是一条单向流水线，而是一个带多条回路的闭环，这些回路的编排属于"通信的软件模块实现层"，「串口模块」一章会落到代码。

==== 编排哲学二：无 ROS 的线程与队列

第二种哲学干脆不用 ROS。当季一支国赛队伍开源的自瞄框架（同济 SuperPower 战队 25 赛季，MIT 许可证）走的就是这条路：整个程序只有一个进程，用 `std::thread` 加自研的线程安全队列做数据流，把 ROS/DDS/TF 那一整套学习成本从新人肩上卸下来。它的核心取舍是一句话——*一个相机只能被一个进程打开*。步兵既要自瞄又要打符，两者都要读相机，做成两个独立程序就会互相抢相机。于是它把自瞄、打符拆成同一个框架下的一个个*功能组*，程序（`main`）只有一个：从相机取图，再根据下位机发来的档位信号（自瞄档还是打符档）选对应的功能组执行。不同兵种就是不同的 `main`。

这条路线的一些设计和 ROS 版形成鲜明对照：相机驱动用"守护线程 + 取流线程"双线程，守护线程 100 ms 轮询一次采集状态，一旦掉线就 `libusb_reset_device` 复位 USB 重来，比 ROS 的节点级重启更贴近硬件；队列只留最新一帧（长度 1），落实"宁要最新、不要完整"；还自带一个类似 rosbag 的录制器，把图像时间戳、视频、云台四元数一起录下来，做到"赛前问题排查、赛后问题重现"。为哨兵的"追击"功能，它保留了一个可选的 ROS 通信接口——ROS 从"框架底座"降级成了"插件"。

两种哲学没有对错，只有场景。把关键差异摆成一张表：

#figure(
table(
  columns: (auto, auto, auto),
  align: (left, left, left),
  stroke: (x, y) => (
    top: if y == 0 or y == 1 { 1pt } else { 0pt },
    bottom: if y == 6 { 1pt } else { 0pt },
  ),
  inset: 7pt,
  table.header([*维度*], [*ROS 2 组合节点*], [*无 ROS 线程队列*]),
  [进程与通信], [多节点，DDS＋进程内零拷贝], [单进程，`std::thread`＋线程安全队列],
  [相机复用], [每功能一套容器], [一相机一进程，"一 main 多功能组"切换],
  [时序编排], [launch 延时启动（串口 $+1.5$ s、跟踪 $+2.0$ s）], [主循环按档位信号切功能组],
  [掉线自愈], [节点级重启], [守护线程＋libusb 复位，取流线程独立],
  [录制回放], [rosbag], [自研录制器（时间戳＋视频＋四元数）],
  [上手成本], [需学 ROS／DDS／TF], [只需 C++，无框架前置知识],
),
caption: [两种自瞄编排哲学的对照。读表规则：左路用 ROS 的生态换来分布式与现成工具链，适合人手充足、多机协同（雷达、哨兵）的队伍；右路用一点手写线程换来完全可控和零学习成本，适合想让新人快速上手、把复杂度压在一个进程里的队伍。无论哪种，"零拷贝取图""掉线自愈""只留最新帧""可回放"这几件事都躲不掉——这些才是编排真正要解决的问题，ROS 与否只是手段。],
)

无论哪条路，有一条铁律两边一致：*感知与决策跑在小电脑上，控制跑在下位机上*。识别、估计、决策算法复杂，需要算力；控制回路要求硬实时和稳定，交给单片机。这条分界线同时也解耦了视觉组和电控组的职责——本章讲的一切到"发出云台指令"为止，指令怎么变成电机力矩，是下位机的事。

=== 决策器：自瞄真正的大脑

流水线搭好了，真正决定自瞄好坏的是倒数第二站——*决策器*。它接过跟踪器给的目标运动状态，回答两个问题：*云台指向哪里*（瞄准决策）、*要不要开火*（火控决策）。识别再准、跟踪再稳，决策器蠢，照样打不中。这一节先看传统做法为什么难，下一节给出一套更统一的视角。

==== 分段决策：为每种情况写一段代码

最直接的决策器是"分情况讨论"。对手的运动状态无非几种：静止、平移、低速小陀螺、高速小陀螺；不同兵种打法还不同——英雄射频低，只瞄旋转中心、靠时间误差决定开火；步兵平移时要持续跟随装甲板。于是代码里长出一棵大 `if-else` 树。以一段被后来者替换掉的经验决策器为例，它处理小陀螺的核心逻辑是：一侧的装甲板不断"进入"、另一侧不断"离开"，显然打正在进入的那块命中概率更高——

```cpp
// 经验分段决策（节选）：小陀螺时挑"正在进入"的装甲板
for (int i = 0; i < armor_num; i++) {
  if (std::abs(delta_angle_list[i]) > coming_angle) continue;   // 太偏，跳过
  if (ekf_x[7] > 0 && delta_angle_list[i] <  leaving_angle) return armor[i];  // ekf_x[7] 即 ω
  if (ekf_x[7] < 0 && delta_angle_list[i] > -leaving_angle) return armor[i];
}
```

低速时它又切到另一支：在可射击角内选最正对的板，还要加一个"锁定"防止在两块各呈 45° 的板之间反复横跳。开火判断则是另一套独立的容差逻辑：云台指令没有突变、且云台当前角与指令角之差小于容差，才允许射击。

这套东西能用，但它有两个说不出的苦：*实现繁琐*——每个兵种、每种运动状态都要一段专属代码和一组专属阈值（`coming_angle`、`leaving_angle`、容差、锁定滞回……），维护成本高、参数越加越多；*调参没有指北针*——自瞄控制器的输入既不是阶跃也不是斜坡，而更像一个周期和幅值随对手运动不断变化的三角波，传统控制指标套不上去，调参只能靠经验猜，甚至说不清"这台云台的控制效果上限在哪、被什么决定"。分段决策把一个连续的问题切成了一堆离散的特例，特例之间的接缝就是 bug 的温床。

==== 陀螺判定：到底怎么知道对手在转

分段决策的第一步，是判定对手是否进入了陀螺状态——这正是「卡尔曼滤波」一章挂下的第一笔账。有两种层次的做法。

*单板跟踪时代的做法*：不建整车模型，只为每块看得见的装甲板维护一个跟踪器。小陀螺最显著的特征是"连续有装甲板从一侧出现、在另一侧消失"，而且视野里同 ID 的装甲板最多同时两块，于是跟踪器数量会呈现 *…-2-1-2-1-…* 的固定周期变化；同时每块板的位姿以"侧向倾斜 → 正对 → 另一侧倾斜"的规律翻转。建一个专门的模块，用计数器统计这两种模式的出现次数，超过阈值就判定为陀螺。这套办法能用，但本质上是在从一串短命观测里*反推*旋转，脆弱且滞后。

*整车建模时代的做法*：判定几乎不要钱。「卡尔曼滤波」一章已经把一台车建成"中心平动 + 装甲板绕中心旋转"，自旋角速度 $omega$ 本身就是状态向量里的一个分量。判定陀螺，退化成读一个状态量再比个阈值：

```cpp
// 整车 EKF 状态里，索引 7 就是自旋角速度 ω
double omega = target.ekf_x()[7];
bool spinning = std::abs(omega) > decision_speed;   // 超过门限即认定小陀螺
```

索引 7 不是随手取的：「装甲板的跟踪」一章那张状态分量表里，下标 7 正是 $omega$（代码里的 `v_yaw`）。上一章对照的九维与十一维两套实现，前九维的排列完全一致，十一维版只是在末尾追加了 $l$、$h$ 两维——所以这个索引在两边都成立，不必改写。

这就是换状态空间的红利：单板时代要靠 2-1-2-1 数板子才能勉强推断的事，在整车状态空间里成了一次比较。工程上还会给不同兵种设不同门限，甚至对前哨站这种"转速由规则钉死（近年为 $0.8 pi approx 2.51$ rad/s，随赛季变动、以当季手册为准）"的目标做特判——一旦估计出的 $omega$ 收敛且超阈，直接把它钉到规则值，避免估计噪声干扰。判定之后怎么打，才是真问题，下一节整个交给它。

=== 轨迹视角下的自瞄理论

分段决策的困境，根子在于*没有一个统一的量来衡量"自瞄好不好"*。前述那套无 ROS 框架提出了一套"轨迹视角"来破局：它不再按情况分段，而是把瞄准和开火都归结到几条轨迹的*重合度*上，用一个规划器统一生成瞄准位置和开火决策。这套方法在 25 赛季国赛实战验证——搭载它的步兵最高命中率 39.6%，是本章的核心，下面完整讲透。

==== 命中率不够，还要看击杀时间

先破一个直觉：命中率不是衡量自瞄的终极指标。道理很简单——"不谈射频的命中率是没有意义的"：只要自瞄只在最有把握的一瞬间才开火，命中率可以刷得很高，但打死对面要很久。真正的金标准是*击杀时间*：打光对方一个单位的血量要多长时间。它便于测量（一把秒表加一台靶机，比赛回放也能估），且天然封住了"靠超高射频刷时间"的路（击打检测和射频都有上限）。

击杀时间由 DPS（每秒伤害，Damage Per Second）决定。设对方血量为 $H$，则

$ T_"kill" = H / "DPS", quad "DPS" = eta dot f dot D $

其中 $f$ 是射频、$D$ 是单发伤害，都基本是常数；唯一能被自瞄软件左右的是 $eta$——*单位时间内的射击窗口占比*，也就是"云台处在能打中的位置上"的时间比例。于是问题被压缩成一句话：*怎么让 $eta$ 尽可能大*。要回答它，得先把"能打中的位置"精确定义出来。

==== 三条轨迹：目标、云台、射击

在做两条简化假设的前提下——假设估计器完美（不计估计误差）、假设目标匀速（不计变速预测误差）——定义三条只关心 yaw 的轨迹（pitch 完全同理）：

- *目标轨迹* `yaw_target(t)`：由整车运动状态和匀速公式，算出任意时刻 $t$ 各装甲板的位置，选距云台最近的一块，它相对云台的方位角随时间的函数。
- *云台轨迹* `yaw_gimbal(t)`：云台实际指向的方位角随时间的函数。
- *射击轨迹* `yaw_shoot(t)`：子弹出膛后要飞行 $t_"fly"$ 才到达装甲板，所以此刻要瞄的，是目标 $t_"fly"$ 之后的位置。把目标轨迹整体提前一个飞行时间，就是射击轨迹：

$ "yaw"_"shoot" (t) = "yaw"_"target" (t + t_"fly") $

射击轨迹是这套理论的枢纽。它的物理含义是：*如果某一时刻云台角恰好落在射击轨迹上，此刻从枪管射出的子弹就会命中*。更进一步——如果云台轨迹与射击轨迹*完全重合*，那么任意时刻发射的子弹都会命中，"子弹发射如水流一样"，一发不空。这就把"瞄得准不准"从一个模糊的直觉，变成了两条曲线"贴得紧不紧"的几何问题。

==== 什么是好自瞄：重合度决定一切

有了三条轨迹，"好自瞄"就有了定义：*云台轨迹与射击轨迹的重合度越高，自瞄越好*。这条主线把前面所有线索一次收拢：

重合度高 → 云台处在射击轨迹上的时间比例（也就是射击窗口占比 $eta$）高 → DPS 高 → 击杀时间短。同一个重合度也框住了命中率的上限。把云台轨迹分成"重合射击轨迹段"和"偏离射击轨迹段"，二者之比就是理论命中率的天花板：

$ p_"hit" <= rho = t_"重合" / (t_"重合" + t_"偏离") $

这条不等式直接给出了本章欠下的第二笔账——*开火时机*的答案：*云台一旦偏离射击轨迹就停火*。偏离时开火，子弹必然落空，只会白白消耗弹量、拉低命中率；把"是否落在射击轨迹上"当作扳机的开关，就在不牺牲击杀时间的前提下守住了命中率。开火决策至此不再需要一堆分段阈值，它就是重合度这一个量的副产品。

那重合度又被什么限制？被*云台的最大加速度*。射击轨迹在装甲板切换处会突然跳变（下一节详述），云台要跟上跳变，得有足够的角加速度；而云台能产生的最大角加速度，是一道物理天花板：

$ alpha_max = tau_max / J $

$tau_max$ 是云台电机的最大扭矩（厂家给定），$J$ 是云台的转动惯量（对云台施加已知力矩、测其角加速度响应即可系统辨识出来）。这个式子就是转动版的牛顿第二定律 $tau = J alpha$。它揭示了一件残酷但清醒的事：*自瞄效果有一个由硬件决定的上限*，扭矩不够大、惯量不够小，再好的算法也追不上高速陀螺——这解释了为什么同样的代码，换一台云台效果就天差地别。理解了这一点，调参才第一次有了指北针：先辨识出 $alpha_max$，就知道这台云台"理论上能有多好"，剩下的是让算法逼近这个上限。

#figure(
  image("images/app-autoaim-trajectory.png", width: 100%),
  caption: [轨迹视角下的自瞄（脚本 `app_autoaim_trajectory.py`，用与规划器同构的"加速度受限最小二乘"复现）。场景是一台自旋的车，最近装甲板每转过 90° 就切换一次，于是射击轨迹（红色虚线）是锯齿状的；云台轨迹（粗蓝）是在 $alpha_max$ 约束下能跟随它的最优解。读图规则：*绿色带 = 云台落在射击轨迹上（$|"误差"| <$ 开火阈值）→ 开火；红色带 = 装甲板切换处云台跟不上、偏离 → 停火*。绿色带的时间占比就是重合度（此例约 52%），它同时是命中率的天花板、也决定 DPS。注意粗蓝线在每次跳变*之前*就提前拐弯下探——这就是下一节的"提前减速"：与其硬追一个追不上的跳变，不如早点松手平滑过渡。下方是 yaw 误差与开火阈值线，误差峰值出现在切换处，正好对应红色停火带。],
)

值得强调的是重合度和命中率的关系。图里的重合度约 52%，而这套方法国赛实测命中率约 30%–40%——命中率总是*低于*重合度，因为估计误差、机械抖动、弹速波动都会再吃掉一块。重合度是天花板，不是保证。所以"命中率 30% 到 40%"不是自瞄没调好，恰恰是一个健康自瞄的正常区间；拿"怎么还没到 100%"去要求它，是没理解这条不等式。

==== 轨迹规划器：提前减速让云台跟得上

射击轨迹的锯齿从哪来？*装甲板切换*。小陀螺时最近的装甲板每转过约 90° 就换一块，目标轨迹（进而射击轨迹）在切换点发生跳变，跳变点的瞬时速度和加速度根本没有定义。强行让控制器去跟一条这样的轨迹，结果不是超调就是滞后——这正是分段决策处理不好陀螺切换的根源。

轨迹规划器的对策叫*提前减速*：既然预见到未来某刻装甲板会切换、射击轨迹会跳，那就不要死追到最后一刻再猛拐，而是*提前*开始减速、平滑地向下一块装甲板过渡，让规划出来的轨迹的加速度始终不超过 $alpha_max$（图中粗蓝线在跳变前提前下探，就是这个策略）。这样牺牲了切换点附近一小段的重合（那一小段本来也追不上，注定要停火），换来了整条轨迹的可跟随性。实现上有两种方案：

- *隐式搜索*（国赛上场用的方案）：以规划轨迹的*加速度序列*为优化变量，构造代价函数（与射击轨迹的重合度尽量高）和约束（加速度不超 $alpha_max$），这是一个标准的二次规划（QP）。调用一个专为 MPC 加速优化的第三方库（TinyMPC）求解，实测耗时小于 1 ms。这里要澄清一个常见误解：*这个 MPC 只是当求解器用来找一条可行轨迹，不是闭环控制器*——它的输入里不含云台的实际状态。团队试过把 MPC 直接当闭环控制器、让下位机执行它算出的力矩，自身也小陀螺时效果反而更差，最终闭环控制还是留在下位机。
- *显式搜索*（仅仿真验证）：以过渡时间为变量，确定切换点前后的起点终点，用一段五次多项式生成过渡曲线，调长过渡时间直到加速度落进限制。它确定性高、不依赖第三方库，跟随段不参与优化因而与射击轨迹严格重合，高转速下更稳，只是没来得及上车。

两种方案共享同一个内核：*把"追不上的跳变"提前摊开成"加速度受限的平滑过渡"*。

==== 从理论到代码：规划器逐点映射

理论落到代码，是一段出人意料地短的程序。规划器在一个 1 秒、步长 `DT=0.01` 的时间窗上工作（`HORIZON=100` 步，中点 `HALF_HORIZON=50` 对应"现在"）。第一步，把射击轨迹采样成一条离散序列——沿匀速模型把目标逐点外推，每一点解算出应瞄的 yaw／pitch，再用*中心差分*算出对应的角速度：

```cpp
// 逐点采样射击轨迹：位置来自匀速外推，速度用中心差分
for (int i = 0; i < HORIZON; i++) {
  target.predict(DT);                                  // 目标沿模型推进一步
  auto yaw_pitch_next = aim(target, bullet_speed);     // 该时刻应瞄的 yaw/pitch
  auto yaw_vel = tools::limit_rad(yaw_pitch_next(0) - yaw_pitch_last(0)) / (2 * DT);
  //           ^^^ limit_rad：角度住在圆上，差分前先把跨 ±π 的假跳变归一化
  traj.col(i) << tools::limit_rad(yaw_pitch(0) - yaw0), yaw_vel, yaw_pitch(1), pitch_vel;
  yaw_pitch_last = yaw_pitch;  yaw_pitch = yaw_pitch_next;
}
```

那句 `limit_rad` 不是装饰——它正是「卡尔曼滤波」一章反复强调、又在「装甲板的跟踪」一章被 `orientationToYaw` 具体兑现过的角度归一化：$359 degree$ 和 $-1 degree$ 是同一个方向，差分前不把跨越 $plus.minus pi$ 的假跳变收回来，就会凭空算出一个巨大的假角速度，让整条轨迹在切换点炸掉。凡是把角度放进状态、观测或差分，这一步都不能省。

第二步，把这条采样好的射击轨迹当作参考，交给 QP 求最优的、加速度受限的云台轨迹。yaw 和 pitch 各建一个*双积分*模型——状态是（位置，速度），输入是加速度：

```cpp
Eigen::MatrixXd A{{1, DT}, {0, 1}};   // 位置 += 速度·DT
Eigen::MatrixXd B{{0}, {DT}};         // 速度 += 加速度·DT
// 输入约束：|加速度| ≤ max_yaw_acc，就是那道物理天花板 α_max
u_min = -max_yaw_acc;  u_max = +max_yaw_acc;
tiny_setup(&yaw_solver_, A, B, /*Q,R 权重*/ ..., HORIZON, 0);
```

$bold(A)$、$bold(B)$ 就是离散双积分：$x_(k+1) = bold(A) x_k + bold(B) u_k$ 展开正是"位置加速度乘时间、速度加加速度乘时间"。求解器吐出一条可行轨迹后，规划器取"现在"这一点的位置、速度、加速度，*三个量一起*发给下位机——不只发目标位置，还发速度和加速度作为*前馈*，让下位机的控制器提前知道云台该往哪个方向使多大劲，响应更快。这是"发一个 yaw 就完事"和"发 yaw／yaw 速度／yaw 加速度三元组"的区别，也是本章数据流里那条"三元组指令"的由来。

第三步，开火决策，就是本节反复说的那句话的代码化：

```cpp
// 在稍稍提前的一点（对应开火延迟 t_fire）比较射击轨迹与规划轨迹的偏差
plan.fire = std::hypot(
    traj(0, HALF_HORIZON + 2) - yaw_solver_->work->x(0, HALF_HORIZON + 2),    // yaw 偏差
    traj(2, HALF_HORIZON + 2) - pitch_solver_->work->x(0, HALF_HORIZON + 2))  // pitch 偏差
  < fire_thresh_;                                                              // ≈ 0.0035 rad
```

`traj` 是射击轨迹、`work->x` 是规划出的云台轨迹，`hypot` 是两者在 yaw–pitch 平面上的距离。距离小于阈值（约 0.0035 rad ≈ 0.2°）就是"云台落在射击轨迹上"，开火；否则停火。取 `HALF_HORIZON + 2` 而不是"现在"，是因为开火命令发出到子弹真正出膛（摩擦轮掉速）有个延迟 $t_"fire"$，要判断的是*子弹出膛那一刻*会不会命中，而不是现在。整套开火逻辑，就是这一个 `hypot` 比较——没有分段、没有滞回、没有一堆专属阈值。分段决策器几十行才勉强覆盖的火控，被压成了一行几何。

==== 预测偏移：把每一段延迟都算进提前量

射击轨迹提前于目标轨迹的时间叫*预测时间*，它不只是子弹飞行时间 $t_"fly"$。从相机曝光到云台真正到位，链路上每一环都有延迟，都要提前补上。一种清晰的社区做法是把整条链拆成六个可命名的时间点——曝光中点、预处理完成、指令发出、下位机开始动、子弹出膛、子弹命中——逐段核算哪些能测、哪些要估。落到代码里，图像处理延迟能直接从时间戳算出，其余（传输、通信）之和难以逐项精测，就并成*一个*调试参数，靠拍慢动作视频、比对击杀时间来标定，量级约 15 ms。

要留意这 15 ms 的*口径*：它不含云台响应，也不含拨弹空程——前者被前面那条双积分前馈轨迹本身吸收掉了（规划器算出来的本来就是云台跟得上的轨迹），后者作为出膛延迟 $t_"fire"$ 单列在开火判据里，就是上面那个 `HALF_HORIZON + 2`（20 ms）。所以它与「弹道解算」一章那个覆盖"曝光到出膛"整段、量级上百毫秒的常数 $t_d$，是同一种"难测就并成一个常数"的*思路*，但两者的口径不同、数值不可直接比较。*看到一个延迟常数，先问它包住了哪几段。*

这里还藏着一个只有实战才暴露的坑：某些下位机的 PID 跟一条斜坡指令时会留一个稳态偏差，正比于目标角速度——追一个匀速自旋的目标，视觉期望云台停在偏差为零处，实际却总差一个 `yaw_v · t0`（$t 0$ 是与电机和 PID 相关的时间常数）。有队伍据此在发给下位机的角度上加一项 `t0 · yaw_v` 前馈把它抵消。这类补偿要不要做、怎么标，取决于你的下位机控制律——但它提醒一件事：*视觉发出的"完美"提前量，会被下位机的动态特性再改写一次*，整链的账要一起算，不能只算到串口为止。注意：这一段补偿和上面的双积分前馈是两回事，前者修的是下位机稳态偏差，后者给的是轨迹动态前馈，可以并存。

=== 整链联调：预测的锅还是弹道的锅

现在兑现最后一笔账——「弹道解算」一章验收完固定靶后留下的问题：*运动目标打不中，怎么知道病根在预测还是在弹道？* 轨迹视角恰好给了一把手术刀。

先回顾弹道那一章定下的规矩：*先静后动*。固定靶把弹道模型和参数（$k$、$v_0$、pitch 补偿）钉死，静态偏差表的均值形状指认系统误差、标准差划清机械责任。做完这一步，你手里就有一个*已被证明可信的弹道模型*。这一步是整个联调的地基——地基没打，后面所有"动态打不中"的诊断都无从归因。

打固定靶准了，换成运动目标打不中，用轨迹视角分三层归因：

+ *匀速平移的目标打不中*。若命中点的水平残差随目标速度线性增大，病根几乎必在*延迟*没算够——某一段固定延迟被漏掉或低估，导致提前量偏小。诊断法很干脆（也是「弹道解算」一章教的黑箱标法）：命中点水平残差除以目标速度，就是还欠的那段 $t_d$。这是弹道／时延的锅，不是预测的锅——因为匀速平移下整车估计最简单，几乎不会错。

+ *小陀螺目标打不中，且规划轨迹与射击轨迹本就贴不紧*（重合度低）。这是*预测／规划*的锅。要么整车估计发散了——半径 $r$、角速度 $omega$ 跑飞，射击轨迹本身就是错的；要么云台加速度不够、跟不上切换，重合度被 $alpha_max$ 天花板压死。用调试曲线工具画出三条线一看便知：云台 yaw 的执行情况、开火决策、目标角速度。跟随一块匀速装甲板时稳态误差能压到小于 0.01 rad，是估计与规划都健康的证据；若这条线本身就飘，别怀疑弹道，先查估计器。

+ *重合度明明很高，子弹却还是擦着过*。这时轮到弹道和时延复查：飞行时间 $t_"fly"$ 解得对不对、$v_0$ 是不是用了裁判系统的实时滑动均值、pitch 补偿有没有随距离更新。重合度高说明"云台指对了地方、也在射击轨迹上"，剩下的偏差只能来自"从枪口到装甲板"这段物理——那是弹道的地盘。

这套归因的底层逻辑是*用重合度把两类锅切开*：重合度低，是"指错了地方"，查预测与规划；重合度高却打不中，是"指对了却没送到"，查弹道与时延。还有一条经验值得记住：PnP 的深度*误差*随物距平方增长（「相机模型」一章算过），折成*方差*就是四次方，"远了就飘"——所以远距离打不中，先怀疑观测方差把估计带偏，而不是一上来就改弹道参数。归因搞反了方向，调参就会陷进"改了 A 的参数去补 B 的错"的泥潭，越调越乱。

=== 让系统活下去：优先级、参数与容错

理论和主链之外，还有几件不起眼却决定"能不能跑满一场"的系统集成活儿。

==== 目标优先级：视野里不止一台车

赛场上常常同时看到好几台敌方车，只能打一个，选谁？基本策略是两条打分：*离准星近*（切换代价小、飞行时间短）和*兵种优先级*（英雄威胁大于步兵大于工程）。工程实现里通常给每个目标算一个综合分数，选最高的；哨兵这类需要全向感知的兵种还会把多个相机的目标汇总重排。切换目标要加滞回，避免在两个分数接近的目标间反复横跳——这和上一节锁定装甲板防抖是同一种工程直觉。敌我识别则靠裁判系统告知我方颜色，由串口回写给识别器（数据流图里那条"敌方颜色"回路）。

==== 参数管理：一个弹速引发的翻车

每台机器人的相机内参、手眼外参、云台零位、$alpha_max$、各种阈值都不同，全部收进*每台车一份的 YAML 配置*，程序启动时加载。这里有一个真实的、极易忽视的坑：*同一个默认弹速在不同模块里写了不同的值*。某开源框架里，规划器默认 22 m/s、瞄准器默认 23 m/s、打符默认 24 m/s——三处硬编码各写各的。平时裁判系统的实时弹速一到，覆盖掉默认值，看不出问题；一旦某场读不到实时弹速，三个模块就用着三个不同的假设各算各的，弹道悄悄错开。教训很直接：*凡是同一个物理量，全系统必须只有一个来源*，默认值也要统一，最好以裁判系统实时测速为唯一真值（「通信协议设定」一章讲过弹速字段）。这类"多处硬编码不一致"是集成阶段最隐蔽的一类 bug，代码评审时要专门盯。

==== 状态监控与容错：坏了要能自己爬起来

一场比赛几分钟不能重启，系统必须能容错自愈。几个必备件：*掉线自愈*——相机掉线自动复位重连、串口断开自动 `reopen`，前面两种架构都各有实现；*目标丢失容忍*——目标被遮挡几帧，跟踪器靠模型纯预测硬撑，但 $bold(P)$ 无界增长，必须设丢失时长上限，超时就废弃目标重新初始化（「卡尔曼滤波」一章异步更新一节的直接落地）；*发散检测*——半径跑出合理范围（如 $0.05 tilde 0.5$ m）、或卡方新息检验（NIS）连续超标，就判定估计发散、复位跟踪器，复位信号同样由串口回写触发。最后，别忘了前面提过的那个录制器：把时间戳、图像、云台四元数一起落盘，是"赛场上的诡异 bug"唯一可复现的抓手——离线重放一遍，问题往往当场现形。

=== 上场前：整系统怎么联调

把一切串起来，上场前的联调是一套有顺序的方法论，核心是*分层锁定、先离线后上车*，每一层只放行一类问题。

*先离线*。借助录制的视频和独立测试程序（成熟框架会给识别、跟踪、规划、通信、云台响应各配一个单独可跑的测试），在笔记本上把每个模块回归一遍——识别率、跟踪稳定性、规划器输出，全在上车之前测完。这正是「软件工程基础」强调的可测性在整系统尺度上的兑现：能离线复现的 bug，就别留到赛场上抓。

*再上车，按运动状态由简到难*：固定靶（锁死弹道）→ 匀速平移（标定延迟 $t_d$）→ 低速小陀螺（验证整车估计与规划）→ 高速小陀螺（逼近 $alpha_max$ 天花板）。每一层锁定一类问题，上一层不过关绝不进下一层——这样任何一层翻车，你都能笃定病根就在这一层新引入的环节里，而不是在一锅粥里大海捞针。这套顺序，本质上就是上一节"预测锅还是弹道锅"的归因逻辑，前移成了主动的验收步骤。

*怎么算联调成功？* 用击杀时间这个金标准量化，而不是"感觉打得挺准"。控制住变量（对方血量、距离、运动状态）掐秒表：作为参照，前述那套方法在 25 赛季国赛上的实测是——面对 2 m 处 300 血的步兵，7 rad/s 转速下约 8 s 击杀、14 rad/s 下约 10 s，跟随稳态误差小于 0.01 rad，发挥正常时命中率不低于 30%、最高 39.6%。这几个数字给了一把标尺：转速越高击杀越慢（重合度被 $alpha_max$ 压低，与理论一致），30%–40% 的命中率是优秀水平而非缺陷。拿它对照自己的系统，就知道差距在硬件天花板、在估计、还是在弹道——这，就是把一套自瞄真正调上场的完整闭环。

==== 本章小结

这一章把前四章的器官接成了一副能上场的身体。骨架是七站数据流，胶水是两种编排哲学——ROS 2 组合节点用生态换分布式，无 ROS 线程队列用手写线程换可控与零学习成本，但零拷贝取图、掉线自愈、只留最新帧、可回放这些账两边都要还。真正的大脑是决策器：传统分段决策为每种情况写一段代码、调参没有指北针；轨迹视角用一个"重合度"统一了瞄准与开火——定义目标／云台／射击三条轨迹，`yaw_shoot(t) = yaw_target(t + t_fly)`，好自瞄就是云台轨迹与射击轨迹重合度高，于是 DPS 高、击杀短，命中率以重合度为天花板，偏离射击轨迹即停火。重合度被 $alpha_max = tau_max \/ J$ 这道物理天花板限制，轨迹规划器用"提前减速"把追不上的跳变摊成加速度受限的平滑过渡，隐式搜索转成 QP 交给毫秒级求解器，落到代码不过是中心差分采样、双积分模型、一个 `hypot` 开火判据。本章还兑清了两笔旧账：陀螺判定在整车状态空间里退化成读一个 $omega$ 分量、比一个阈值；整链联调则用重合度把"预测的锅"和"弹道的锅"一刀切开——重合度低查估计与规划，重合度高却打不中查弹道与时延。至此，从一帧图像到一条会开火的云台指令，整条链闭合了。剩下"通信怎么在软件层落地"的最后一块，是「串口模块」一章的事。
