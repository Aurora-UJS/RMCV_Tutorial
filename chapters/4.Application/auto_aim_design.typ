=== 从模块到系统

前面四章分别讨论了相机、装甲板识别、跟踪和能量机关。把这些模块组成系统时，还要回答两个跨模块问题：

- *什么时候开火？* 「卡尔曼滤波」一章介绍了整车建模，但没有展开陀螺状态判定和开火时机选择，本章继续讨论这两个问题。
- *运动目标出现落点偏差时，怎样区分估计、规划、时间、控制和弹道问题？* 「弹道解算」一章完成固定靶检查后，把动态条件下的分层诊断留给本章。

本章先说明两种工程编排方式，再用*轨迹视角*统一描述瞄准参考、可实现的规划轨迹和开火窗口，最后给出分层联调方法。整车 EKF 回指「卡尔曼滤波」，坐标链回指「坐标变换」，弹道回指「弹道解算」；本章重点是模块接口、假设和验证边界。

=== 整体架构：一条数据流，两种编排方式

从功能上看，自瞄可以分成七个环节：

相机 → 识别与 PnP → 坐标变换 → 跟踪估计 → 预测与选板 → 弹道／规划 → 通信。

相机产生带时间戳的图像（「工业相机」一章），识别器给出装甲板像素角点与类别（「装甲板识别」一章），PnP 解算相机系位姿（「相机模型」一章），坐标链将其转换到跟踪坐标系（「坐标变换」一章），整车 EKF 再用这些观测估计运动状态（「装甲板的跟踪」一章）。后续环节预测目标、选择候选装甲板并解算弹道或可实现的云台参考，最后通过通信模块交给下位机。各项目对“预测、弹道、选板和开火”放在上位机还是下位机有不同选择；这条箭头表示功能依赖，不表示唯一的软件进程边界。

本章关心模块之间的数据格式、进程边界、启动依赖、故障传播和恢复策略。下面用 ROS 2 组合节点与单进程线程队列两个公开实现作对照；它们是具体案例，不代表社区只有这两种架构。

==== 方式一：ROS 2 的组合节点

第一种方式将功能拆成 ROS 2 节点，再用 launch 文件编排。rm_vision 主仓库根目录的 `LICENSE` 是 MIT；它的公开 launch 文件展示了组合容器、定时启动和 TF 发布等具体配置。launch 引用的功能包来自独立仓库，而且部分根许可证、源码文件头与 `package.xml` 标注并不一致，不能把主仓库的 MIT 标注直接套到所有子仓库；具体冲突列在本章末尾。

相机与识别放在同一个容器。高分辨率、高帧率图像的跨进程序列化和复制可能占用可观的内存带宽。ROS 2 的*组合节点*（composable node）允许相机与识别处于同一 `component_container`；两端开启 `use_intra_process_comms` 后，在消息类型、发布方式和订阅关系满足条件时，可减少序列化与复制。是否达到严格的零拷贝（不复制消息负载）仍要用实际消息所有权和性能测量确认。

#block(breakable: false)[
```python
ComposableNodeContainer(
    name='camera_detector_container',
    composable_node_descriptions=[
        camera_node,                              # 相机
        ComposableNode(                           # 识别，与相机同容器
            package='armor_detector',
            plugin='rm_auto_aim::ArmorDetectorNode',
            extra_arguments=[{'use_intra_process_comms': True}]),
    ],
    on_exit=Shutdown(),                           # 容器一挂，整个系统退出
)
```
]

*启动采用固定错峰。* 该版本让容器和 TF 发布器在 $t=0$ 启动，串口驱动延迟 1.5 s，跟踪器延迟 2.0 s。

```python
delay_serial_node  = TimerAction(period=1.5, actions=[serial_driver_node])
delay_tracker_node = TimerAction(period=2.0, actions=[tracker_node])
```

源码能直接确认这两个定时值和启动顺序，但没有说明选择 1.5 s、2.0 s 的实验依据。合理的工程解释是给硬件枚举、串口连接和 TF 数据留出准备时间；这仍是推断，不是源码证明。固定延时简单，却不能保证慢启动设备已经就绪，也会在快速启动时徒增等待。更完整的实现可使用设备存在、串口握手、首个 TF 可查询等健康条件触发下游模块。

TF 树同时包含固定与动态关系。`robot_state_publisher` 根据 URDF 发布固定关节关系，例如云台、相机与光学坐标系之间的外参；动态的 `odom → gimbal_link` 则由串口驱动根据接收的姿态更新。这是两个独立的 TF 广播源，不是串口先把动态姿态交给 `robot_state_publisher`。部署时要以实际 URDF 和广播器为准，并确认两部分在父子关系、时间戳和坐标语义上可以组成一棵一致的树。

在该实现中，下位机 IMU 姿态从串口进入，因此串口节点承担动态变换广播。跟踪器真正需要的是图像采样时刻附近的姿态，而不只是高发布频率；要检查 IMU 采样时间、传输时间、时间戳偏移和 TF 插值范围。launch 中还设置了 `publish_frequency: 1000.0`，但当前文件没有看到 `joint_states` 发布者；这个参数不能被解释为动态 `odom → gimbal_link` 已按 1 kHz 更新。读频率参数时，应同时找到它对应的数据生产者。

#figure(
  image("images/app-autoaim-architecture.png", width: 100%),
  caption: [rm_vision 的 ROS 2 编排示意（脚本 `app_autoaim_architecture.py`）。相机与识别位于同一组合容器，并启用进程内通信；跟踪器使用 odom 坐标系。`robot_state_publisher` 发布 URDF 中的固定链，串口节点独立广播随下位机姿态更新的 `odom → gimbal_link`，两路 TF 都进入跟踪器的变换缓冲。串口协议向下游发送整车目标状态，而不是云台角度或开火位；由此可以确定这些后续处理不在已核对的上位机仓库中，但本章没有下位机源码，不能继续断言其内部怎样划分选板、弹道与开火。底部时间轴记录源码中的 $t=0$、$+1.5$ s 和 $+2.0$ s 固定启动时刻，不代表设备就绪已被检测。],
)

图中的串口同时承担前向命令和反向状态：接收下位机的 roll、pitch、yaw，按固定时间偏移生成 TF 时间戳并广播动态变换；接收瞄准点坐标用于可视化；再根据颜色位和复位位分别更新识别参数、调用跟踪器复位服务。反向则把跟踪器给出的整车目标状态发给下位机。因此，功能链不是纯单向流水线；接口还要规定时间戳、有效期、复位语义和失联行为。「串口模块」一章会展开这些实现细节。

==== 方式二：无 ROS 的线程与队列

第二种方式不以 ROS 作为主框架。`sp_vision_25`（同济 SuperPower，根 `LICENSE` 为 MIT）将主要功能放在一个进程内，通过 `std::thread` 和线程安全队列传递数据。该项目把工业相机视为单进程独占资源，因此自瞄和能量机关不是两个同时打开相机的程序，而是同一主程序中的功能组，由下位机模式信号选择。相机能否多进程访问取决于驱动和采集方式；这里描述的是项目约束，不是所有相机的通用定律。

其相机驱动包含状态检查与取流线程：检查线程每 100 ms 查看状态，恢复路径中可调用 `libusb_reset_device`。相机队列容量为 1，但该版本使用 `ThreadSafeQueue` 的默认满队列策略：队列已有一帧时，新到帧会被拒绝，因而不能把它描述为“始终保留最新帧”。`standard_mpc.cpp` 中的目标队列才显式启用 `PopWhenFull=true`，满时先移除旧目标再写入新目标。若目标是限制图像数据年龄，相机队列也应明确采用覆盖旧帧的策略，并用时间戳检查实际效果。项目还提供录制器，保存视频、时间戳和云台四元数。哨兵程序可按构建依赖启用 ROS 2 导航接口。与之相对，前述 rm_vision launch 对容器和串口设置的是 `on_exit=Shutdown()`；该配置会结束整套 launch，而不是自动完成节点级重启。若需要进程拉起，还要另行配置 `respawn` 或外部服务管理器。

两种方式各有取舍。下面按当前源码中的具体实现进行对照：

#figure(
table(
  columns: (auto, auto, auto),
  align: (left, left, left),
  stroke: (x, y) => (
    top: if y == 0 or y == 1 { 1pt } else { 0pt },
    bottom: if y == 6 { 1pt } else { 0pt },
  ),
  inset: 7pt,
  table.header([*维度*], [ROS 2 组合节点], [无 ROS 线程队列]),
  [进程与通信], [多节点，DDS＋进程内通信], [单进程，`std::thread`＋线程安全队列],
  [相机复用], [相机与识别共享组合容器], [项目按“一进程一相机”组织，多功能组切换],
  [时序编排], [launch 延时启动（串口 $+1.5$ s、跟踪 $+2.0$ s）], [主循环按档位信号切功能组],
  [故障处理], [`on_exit=Shutdown()`；重启需另配], [状态检查线程＋USB 复位路径],
  [录制回放], [ROS 2 可使用 rosbag；该 launch 未启动录制], [自研录制器（时间戳＋视频＋四元数）],
  [主要依赖], [ROS 2／DDS／TF 与 launch], [C++ 线程、队列及项目自研工具],
),
caption: [两个公开项目的编排对照。ROS 2 提供节点发现、TF、launch 和 rosbag 工具，但需要管理消息语义、QoS（消息队列与可靠性策略）和进程故障；单进程方案减少了跨进程接口，同时要自行维护线程同步、队列、录制和恢复逻辑。表格同时区分了框架可用能力与当前 launch 已启用功能，不意味着任一路线天然更易学、更可靠或适合某类队伍。选型时应比较端到端延迟、故障隔离、调试工具和团队维护能力。],
)

两种架构都把高频电机控制留在下游控制器，但上位机下发的内容并不相同。rm_vision 的已核对协议发送整车目标状态，后续怎样选板、解算和开火不在本章掌握的下位机源码范围内；sp_vision 的规划器则可发送角度、角速度、角加速度和开火位。分界位置决定了延迟测量、故障回退和视觉/电控两组的调试接口，应在协议中明确，而不是把“决策一定在上位机”当作固定规则。

=== 决策器：瞄准点与开火条件

决策器接收目标运动状态，回答“云台指向哪里”和“当前是否允许开火”。它的输出质量受上游估计、时间对齐、弹道模型和下游控制共同限制。本节先介绍分段规则，再说明轨迹规划方案怎样减少运动状态之间的人工分支。

==== 分段决策：为每种情况写一段代码

一种直接实现是按静止、平移、低速自旋和高速自旋等状态分支，并为不同机器人配置不同策略。例如，低射频机器人可能优先选择稳定瞄准点，步兵在平移目标上持续跟随可见装甲板。`sp_vision_25` README 称旧的经验决策器后来由规划器替代；固定版本仍保留了这份源码，可以看到它原本想在自旋分支中依据角速度符号和装甲板相对角选择候选：

```cpp
// 旧分段决策的自旋分支（节选）：按旋转方向选择候选装甲板
for (int i = 0; i < armor_num; i++) {
  if (std::abs(delta_angle_list[i]) > coming_angle) continue;
  if (ekf_x[7] > 0 && delta_angle_list[i] < leaving_angle)
    return {true, armor_xyza_list[i]};
  if (ekf_x[7] < 0 && delta_angle_list[i] > -leaving_angle)
    return {true, armor_xyza_list[i]};
}
```

不过，这段代码不能作为“该版本已正确按角速度切换策略”的证据。`Target` 的 11 维状态注释明确给出下标 7 是角速度、下标 8 是装甲板半径；旧决策器进入非自旋分支时检查的却是 `abs(target.ekf_x()[8]) <= 2`。同一类又把有效半径限制在约 0.05～0.5 m，因此对有效的普通目标，这个条件与角速度无关且成立，自旋分支不会按注释意图被选中。上面的循环说明了设计意图，也暴露了直接写数字下标不容易发现错误的风险；修正时应改读下标 7，并为正反转和门限两侧建立分支测试。

按注释意图，非自旋分支会在可射击角内选择较正对的板，并用“锁定”避免在两块接近 45° 的板之间频繁切换。开火判断则是另一套独立的容差逻辑：相邻云台指令没有明显突变，且当前云台角接近上一条指令，才允许射击。

分段策略便于逐条理解，但随着兵种、运动状态和安全条件增加，会积累 `coming_angle`、`leaving_angle`、容差与滞回等参数。边界附近还可能频繁切换。它并非不能验证：可以为每个分支建立状态转移测试、参数扫描和回放数据；问题在于测试空间随分支组合增长，而且状态索引写错也可能让某一分支根本进不去。轨迹规划的价值之一，就是把部分离散分支改写成连续约束，但它仍保留阈值、模型和求解器参数。

==== 陀螺判定：到底怎么知道对手在转

分段决策的第一步，是判定对手是否进入陀螺状态。「卡尔曼滤波」一章留下的这个问题，可以从两个层次处理。

*单板跟踪的做法*：不建立整车状态，只为可见装甲板维护轨迹。目标自旋时，同 ID 装甲板可能交替出现和消失，数量与姿态呈周期变化；实现可以统计这些事件并在累计到阈值后判定自旋。遮挡、漏检、视角和装甲板数量都会破坏理想的 2-1-2-1 模式，因此需要用实际序列测量误报、漏报和判定延迟。

*整车建模的做法*：若状态中已经包含自旋角速度 $omega$，可以将其与门限比较：

```cpp
// 整车 EKF 状态里，索引 7 就是自旋角速度 ω
double omega = target.ekf_x()[7];
bool spinning = std::abs(omega) > decision_speed;   // 超过门限即认定小陀螺
```

在本书对照的 rm_vision 九维状态与 sp_vision 十一维状态中，下标 7 都是 $omega$（代码常写作 `v_yaw`），后者在末尾增加了两维。这个索引只对这两个已核对版本成立；设计公共接口时宜使用命名访问器，避免状态排列变化后静默读错分量。

读取状态量减少了额外模式计数，但不等于判定必然可靠：$omega$ 仍受初始化、关联错误和过程噪声影响。工程上可为进入与退出设置不同门限，并同时检查协方差、连续帧数与目标类型。某些赛季的固定转速目标可以使用规则给出的转速作为先验约束，例如公开代码曾使用 $0.8 pi approx 2.51$ rad/s；是否固定该值必须由当季手册和目标类别确认，且不能用先验掩盖明显不相容的观测。

=== 轨迹视角下的自瞄理论

`sp_vision_25` 的 README 用“轨迹视角”描述瞄准与开火：先生成理想射击轨迹，再在云台加速度约束下规划可实现的参考轨迹，并以两者的偏差决定开火窗口。项目 README 报告了 25 赛季比赛中的命中率和击杀时间，但本书没有原始日志，不能独立复现这些数字；本节将它们作为项目自述，重点分析方法和指标定义。

==== 命中率不够，还要看击杀时间

命中率必须与射频、伤害、热量和测试时长一起解释。只在把握较高的窗口开火，条件命中率可能提高，但单位时间命中数未必增加。击杀时间能够综合反映一组条件下的输出效率，不过它还受目标血量、单发伤害、裁判判定、冷却和发射机构状态影响，不能作为跨规则、跨机器人的唯一评价标准。

击杀时间由 DPS（每秒伤害，Damage Per Second）决定。设对方血量为 $H$，则

$ T_"kill" = H / "DPS", quad "DPS" = eta dot f dot D $

在一个简化模型中，令 $f$ 为允许的发射频率、$D$ 为单发有效伤害、$eta$ 为允许开火的时间占比，并暂时假设窗口内每发都有效，则有上述关系。真实系统还应乘以窗口内条件命中概率，并计入热量、弹量、发射间隔和裁判状态。软件可以影响 $eta$ 和条件命中概率，却不能把 $f$、$D$ 或外部约束视为恒定不变。下面先定义这个简化模型中的射击窗口。

==== 三条轨迹：目标、云台、射击

先采用两个明确的简化假设：估计状态等于真值，且目标按匀速模型运动。由此定义三条只关心 yaw 的轨迹；pitch 可以用相同接口处理，但其弹道和机械约束未必与 yaw 相同。

- *目标轨迹* `yaw_target(t)`：由整车运动状态预测各装甲板位置，再按当前实现的“水平距离最近”规则选择一块，其方位角随时间的函数。最近并不等于最适合射击，实际还可加入朝向、遮挡与目标优先级。
- *云台轨迹* `yaw_gimbal(t)`：云台实际指向的方位角随时间的函数。
- *射击轨迹* `yaw_shoot(t)`：子弹出膛后要飞行一段时间才到达装甲板。若在当前预测窗口内先把飞行时间 $t_"fly"$ 视为已知常数，那么此刻要瞄的是目标 $t_"fly"$ 之后的位置；把目标轨迹整体提前这段时间，就得到

$ "yaw"_"shoot" (t) = "yaw"_"target" (t + t_"fly") $

实际飞行时间会随距离、高度、弹速和所选弹道变化，严格计算时可能需要与未来目标位置迭代求解，不能总把它当成全程固定偏移。在上述理想假设下，如果某时刻云台的实际指向与射击轨迹足够接近，且弹道、枪口外参和出膛时刻也准确，弹丸才可能到达目标区域。轨迹差提供了清晰的角度指标，但它没有包含估计误差、机械跟随误差、弹速波动和目标有限尺寸。后文还要区分*规划轨迹*与*实际云台轨迹*：求解器输出前者，编码器或 IMU 才能测到后者。

==== 重合度：开火窗口的时间占比

给定角度阈值后，可以定义射击轨迹与另一条轨迹处于阈值内的时间占比 $rho$。若比较的是规划轨迹，$rho$ 反映规划器在其简化动力学下提供的开火窗口；若比较的是实测云台轨迹，才包含下位机跟随效果。较大的 $rho$ 通常意味着更长的可开火时间，但不能单独决定命中率或 DPS。

原项目 README 将 $rho$ 写成“理论命中率上限”，但这取决于命中率的分母。若系统按固定射频连续发射，且理想模型中只有阈值内的弹丸命中，则总发射命中比例可写成 $p_"hit,continuous" approx rho$。若系统只在窗口内发射，通常报告的条件命中率是“命中数 / 已发射数”，它可以高于 $rho$，理想情况下接近 1。此时 $rho$ 约束的是最大开火占空比：

$ rho = t_"窗口内" / (t_"窗口内" + t_"窗口外"), quad "DPS" approx rho f p_"hit|fire" D $

式中 $p_"hit|fire"$ 是开火条件成立后的命中概率。用轨迹偏差作开火门限可以减少明显偏离时的无效发射，但阈值内仍可能因其他误差脱靶，阈值外也不能仅凭这个简化模型断言必然落空。实际火控还要叠加目标置信度、数据年龄、云台状态、热量、弹量和安全条件。

射击轨迹在装甲板切换处会跳变，规划器需要考虑云台的速度、加速度和控制能力。忽略摩擦、重力、轴间耦合、电流限制和电机转速特性时，可用刚体模型估算角加速度：

$ alpha_max = tau_max / J $

$tau_max$ 是可用轴向扭矩，$J$ 是该轴等效转动惯量。厂家堵转扭矩不一定等于运行时可持续扭矩，$J$ 的辨识也要同时考虑摩擦、控制器和负载变化。因此 $tau_max/J$ 是参数量级的起点，不是对整机最大加速度的完整证明。更直接的做法是在安全范围内给定轨迹，测量速度、加速度、跟随误差和饱和状态，再为规划器设置带余量的约束。

#figure(
  image("images/app-autoaim-trajectory.png", width: 100%),
  caption: [加速度受限规划的合成示例（脚本 `app_autoaim_trajectory.py`）。目标中心固定、四块装甲板以 4 rad/s 自旋；按水平距离选择最近装甲板，并把飞行时间固定为 0.12 s 后，射击轨迹（红色虚线）在切换处跳变。粗蓝线由一个简化的加速度有界最小二乘问题得到，不是实测云台轨迹；它没有复现公开规划器滚动的 1 s 窗口、pitch 轨迹、实际云台状态和下位机动力学。绿色区只表示规划线与射击线的 yaw 偏差小于 0.0035 rad，采样占比在本算例中约为 52%；原代码用的是 yaw–pitch 合成误差，因此本图只是 yaw 方向的开火可用率示意，不是命中率上限。蓝线在切换前偏离当前射击线，展示了平滑过渡可能提前发生。],
)

图中的 52% 来自合成模型的时间采样，项目 README 报告的 30%～39.6% 则来自另一组比赛统计，两者分母、场景和数据来源不同，不能据此验证大小关系或因果机制。正确的对比方式是同一次试验同时记录规划可用率、实测云台可用率、开火次数、命中次数和击杀时间，再分析各级损失。

==== 轨迹规划器：用提前过渡处理切板跳变

对四块等间隔装甲板的简化车辆，按最近距离选板时，目标自旋约 90° 后会切换候选，目标轨迹和射击轨迹随之跳变。三块装甲板、半径不等、遮挡或其他选板规则会改变切换间隔。跳变点无法由有限加速度的云台严格跟随，直接作为连续控制参考通常会产生较大误差。

规划器可以利用未来窗口，在切换前逐步偏离当前参考，并平滑过渡到下一段，使规划加速度满足约束。这通常被称为*提前减速*。它牺牲切换附近的一部分开火窗口，以降低参考轨迹的不可实现程度；实际云台能否跟随仍要由闭环测试确认。公开 README 介绍了两种方案：

- *隐式搜索*（README 称为国赛采用方案）：以加速度序列为优化变量，在双积分模型、状态跟踪代价和输入边界下形成二次规划问题（quadratic programming, QP），并用 TinyMPC 求解。README 报告特定硬件上求解耗时小于 1 ms，本书未复现实测。该版本用射击轨迹起点初始化求解器，输入中没有当前云台实测状态，因此这里的 TinyMPC 主要是轨迹优化求解器，不构成完整闭环控制器。README 还记录了直接下发力矩的闭环尝试在自身自旋场景下不如原下位机方案，这是该项目测试范围内的比较。
- *显式搜索*（README 称仅做仿真）：以过渡时间为变量，用五次多项式连接切换前后的边界状态，并延长时间直到满足加速度限制。它减少第三方依赖，但仍需处理速度、位置边界、目标预测变化和实际跟随。项目自评其高转速表现更稳定，但未上车，因此不能把该比较当成部署结论。

两种方案都将不连续参考转化为满足给定动力学约束的连续规划问题，区别在优化变量、代价和验证程度。

==== 从理论到代码：规划器逐点映射

公开规划器使用 `DT=0.01` s、`HORIZON=100` 的 1 s 窗口，中点 `HALF_HORIZON=50` 对应当前时刻。它先把目标副本回推到窗口起点，再逐点预测、解算 yaw／pitch，并用中心差分估计参考角速度。把源码中的长表达式等价拆成中间变量后，流程如下：

```cpp
// 逐点采样射击轨迹：位置来自匀速外推，速度用中心差分
for (int i = 0; i < HORIZON; i++) {
  target.predict(DT);
  auto yaw_pitch_next = aim(target, bullet_speed);
  auto yaw_delta = tools::limit_rad(
      yaw_pitch_next(0) - yaw_pitch_last(0));
  auto yaw_vel = yaw_delta / (2 * DT);
  auto pitch_vel =
      (yaw_pitch_next(1) - yaw_pitch_last(1)) / (2 * DT);
  auto yaw = tools::limit_rad(yaw_pitch(0) - yaw0);
  traj.col(i) << yaw, yaw_vel, yaw_pitch(1), pitch_vel;
  yaw_pitch_last = yaw_pitch;
  yaw_pitch = yaw_pitch_next;
}
```

`limit_rad` 将差值归一化到同一角度分支，避免跨越 $plus.minus pi$ 时出现接近 $2 pi$ 的伪速度。它默认相邻采样的真实转角小于 $pi$；若时间间隔过大或目标转得过快，还需用最大角速度检查这一假设。这里处理的是两个已计算 yaw 之间的差值，不应与带内部历史状态的 `orientationToYaw` 混用。

第二步，把这条采样好的射击轨迹当作参考，交给 QP 求最优的、加速度受限的云台轨迹。yaw 和 pitch 各建一个*双积分*模型——状态是（位置，速度），输入是加速度：

#block(breakable: false)[
```cpp
Eigen::MatrixXd A{{1, DT}, {0, 1}};   // 位置 += 速度·DT
Eigen::MatrixXd B{{0}, {DT}};         // 速度 += 加速度·DT
// 输入约束：|加速度| ≤ max_yaw_acc，即加速度上限 α_max
u_min = -max_yaw_acc;  u_max = +max_yaw_acc;
tiny_setup(&yaw_solver_, A, B, /*Q,R 权重*/ ..., HORIZON, 0);
```
]

这里采用前向欧拉形式：位置按当前速度更新，速度按输入加速度更新；与包含 $0.5 Delta t^2$ 项的零阶保持离散化不同。`standard3.yaml` 的 yaw 权重为 `Q_yaw=[9e6, 0]`、`R_yaw=[1]`：位置误差被直接惩罚，参考角速度虽然已计算，却没有直接进入该配置的代价。规划速度仍会由位置序列和双积分约束共同形成，不能仅凭 `traj` 中有速度一行就说求解器在跟踪参考角速度。求解后，规划器取中点的位置、速度和输入加速度发给下位机，速度与加速度可作为前馈。是否改善响应取决于下位机如何使用这些字段、单位和时间基准是否一致，以及模型与实际机构的匹配程度。

第三步，根据规划轨迹与射击轨迹的偏差设置开火标志：

```cpp
// 把源码中的长表达式等价展开，2 表示未来两个采样点。
const int fire_index = HALF_HORIZON + 2;
auto yaw_plan = yaw_solver_->work->x(0, fire_index);
auto pitch_plan = pitch_solver_->work->x(0, fire_index);
auto yaw_error = traj(0, fire_index) - yaw_plan;
auto pitch_error = traj(2, fire_index) - pitch_plan;
plan.fire = std::hypot(yaw_error, pitch_error) < fire_thresh_;
```

`traj` 是射击轨迹，`work->x` 是规划轨迹，`hypot` 计算 yaw–pitch 偏差。标准 3 配置中的 `fire_thresh` 为 0.0035 rad，代码把 `shoot_offset` 硬编码为 2，即比较 20 ms 后的点。README 将这解释为开火到出膛的延迟补偿；成立的前提是该延迟稳定且时间步长不变。代码判断的是两条*计划曲线*的接近程度，并未读取实际云台角，所以还需要下位机跟随误差、安全状态和命令有效期共同决定是否真正发射。这个方案减少了按运动状态分支，但仍包含阈值、延迟步数和求解器参数。

==== 预测偏移：把每一段延迟都算进提前量

预测时间不只包含弹丸飞行时间 $t_"fly"$。可以把链路标成曝光中点、图像到达、处理完成、命令发出、下位机接收、执行器响应、弹丸出膛和到达目标等时间点，再明确哪些区间由轨迹规划处理、哪些作为常数补偿。图像处理延迟可由同一时钟域内的时间戳计算；传输与执行延迟需要时钟同步、事件回传或外部测量。`sp_vision_25` README 报告将若干剩余项合并为约 15 ms 的调试参数，这是该设备上的经验值，不应直接移植。

README 的口径把云台动态交给规划与下位机，并把开火延迟单独用于未来点判断；代码中的两步偏移对应 20 ms。规划轨迹满足简化加速度限制，并不证明实际云台响应已被完全补偿；开火延迟也可能随摩擦轮、拨弹机构和供电变化。它与「弹道解算」一章覆盖“曝光到出膛”整段的 $t_d$ 口径不同，不能直接比较。每个延迟参数都应附带起止事件、时钟域、均值、分位数和更新条件。

对某些闭环控制器，斜坡参考会产生近似与角速度成比例的跟随偏差，可用 `yaw_v dot t_0` 表示一阶近似。后文「rm.cv.fans 项目复盘」记录了一个把该项加到角度指令中的公开实现；那份文档中的 $t_0$ 只对应特定控制参数和匀速试验，不能直接移植。应通过正反向、多速度试验确认残差是否真呈线性，并检查补偿是否与已有速度/加速度前馈重复。这个例子也说明，系统验收必须包含实际云台反馈，不能只看串口前的参考量。

=== 整链联调：分层定位动态误差

运动目标出现落点偏差时，要区分观测、状态估计、预测、时间对齐、轨迹规划、云台控制和弹道。轨迹日志能缩小排查范围，但仅凭曲线形状通常不能确认唯一根因。

先用固定靶检查弹道模型和参数（$k$、$v_0$、pitch 补偿），并按距离、弹速和温度记录均值与离散程度。固定靶通过只验证已测试条件下的静态链路，不能证明运动条件下的时间、控制和气动模型都正确；它的作用是先排除一批明显的静态偏差。

打固定靶准了，换成运动目标打不中，用轨迹视角分三层归因：

+ *匀速平移目标*。若水平残差与速度近似线性且换向后符号随之反转，可用 $Delta t approx e_x / v_x$ 估计等效时间偏差。这一关联也可能由速度估计尺度、坐标轴、外参或控制滞后造成；需要改变速度、方向并做时间补偿干预，才能把它归因到某一段延迟。

+ *自旋目标且规划轨迹与射击轨迹偏差较大*。先检查目标状态 $r$、$omega$、装甲板切换和求解器状态，再检查规划约束是否过紧。这里比较的仍是两条计划曲线，不能说明实际云台是否跟随；必须叠加编码器/IMU 反馈。项目 README 报告过单板跟随稳态误差小于 0.01 rad，但本书没有其原始样本，不能把这个值当作普遍健康门限。

+ *规划偏差小但仍未命中*。继续比较实测云台与规划轨迹；若实际跟随也在阈值内，再检查图像/姿态时间对齐、枪口外参、飞行时间、弹速数据年龄、pitch 补偿和发射时刻。规划偏差小只说明求解器在自身模型内找到了接近参考的轨迹，不能把剩余误差全部归给弹道。

上述顺序形成“射击轨迹 → 规划轨迹 → 实测云台 → 实际落点”的证据链，每次只比较相邻接口。相机模型一章给出的局部算例表明，在固定焦距、像素噪声和几何条件下，单目深度灵敏度可能随距离平方增大，相应方差近似随四次方增长；这不是所有 PnP 场景的普遍定律。远距离误差应同时检查观测协方差、目标姿态、外参、弹道和时间，而不是用一个经验规律提前指定根因。

=== 系统运行：优先级、参数与容错

除主要算法链外，系统还要处理多目标选择、参数来源和故障恢复。

==== 目标优先级：视野里不止一台车

同时检测到多个目标时，可以综合角度切换代价、距离、目标类别、可见性、无敌状态和任务策略评分。诸如“英雄高于步兵高于工程”只是策略示例，不是通用正确顺序；规则、己方角色和比赛阶段都会改变权重。切换目标应设置滞回和最短保持时间，并记录选择原因，避免分数接近时来回切换。敌我颜色可由裁判系统经通信链提供，但还要处理消息过期、未知状态和配置冲突。

==== 参数管理：统一弹速来源

每台机器人的相机内参、手眼外参、云台零位、加速度约束和阈值通常存入对应配置。`sp_vision_25` 源码中，同一弹速缺省路径确实出现 22、23 和 24 m/s 三个值：规划器、普通瞄准器和能量机关瞄准器各自回退。实时弹速有效时差异可能被覆盖；数据缺失或越界时，各模块会采用不同模型。应为同一物理量建立单一配置入口，并在接口中携带测量时间与有效标志。裁判系统或测速器的读数也可能延迟、异常或带噪，不能无条件称为“唯一真值”；需要定义滤波、过期门限、回退值和告警策略，并让所有消费者共享同一决策结果。

==== 状态监控与容错

运行时至少要监控相机、通信、跟踪和命令输出。相机可在失败后重新枚举或复位，串口可关闭后重开，但重试次数、退避和安全输出必须明确。短时遮挡期间，跟踪器可只做预测；在本书的常速度模型和非零过程噪声下，协方差通常随丢失时间增大，因此应按时间和不确定度限制外推，超限后重新初始化。半径越界或归一化创新平方（NIS，用于检查观测与预测是否相容）连续超门限，表明状态与观测在当前噪声模型和门限下不相容；这可能来自滤波发散、错误关联、噪声配置或模型切换。复位是一种保护动作，不等于根因已确认。录制图像、原始时间戳、姿态、观测、状态和命令，可以复现许多软件路径，但 USB 供电、机械松动等物理问题仍需额外测量。

=== 上场前：整系统怎么联调

联调可以按“先离线、再闭环；先简单运动、再复杂运动”的顺序进行，并为每一步写明输入条件和通过标准。

*先离线*。用带时间戳的录制数据和模块测试分别检查识别、跟踪、规划与通信。离线回放适合验证确定性软件路径，但不能替代真实相机时序、USB 故障、云台动力学和发射机构测试；两类证据应分开记录。

再进行实体测试：固定靶检查静态弹道与外参；多方向匀速平移检查速度尺度和等效延迟；低速自旋检查关联、整车状态与选板；高速自旋检查规划约束、控制饱和和开火可用率。每一步不通过时，优先检查新引入的变量，但仍要允许旧模块在新距离、姿态或速度下暴露边界问题。只有干预或独立测量排除其他解释后，才能确认根因。

验收指标至少应包含：规划与实测轨迹偏差、开火可用率、条件命中率、单位时间命中数、击杀时间、恢复时间和无效命令数，并注明样本量、置信区间、距离、目标运动、弹速和规则版本。`sp_vision_25` README 自述：2 m、300 HP 的目标在 7 rad/s 下约 8 s、14 rad/s 下约 10 s；单板跟随稳态误差小于 0.01 rad；比赛最高命中率 39.6%，所谓“正常发挥”不低于 30%。这些数字缺少本书可审计的原始日志，只能作为该项目报告，不能作为优秀水平、因果关系或其他系统的验收线。

==== 本章小结

本章从功能链与软件边界两个层面组织自瞄系统。rm_vision 展示了 ROS 2 组合节点、TF 与固定启动时序，sp_vision 展示了单进程队列、硬件恢复路径和轨迹规划；两者对上位机/下位机职责的划分不同。分段决策便于直接编码，轨迹规划则把选定模型下的射击参考转成加速度受限的规划轨迹。`yaw_shoot(t) = yaw_target(t + t_fly)` 只在明确的预测与弹道假设下成立；规划轨迹与射击轨迹的阈值内时间占比表示开火可用率，不是条件命中率上限。实现还要核对角度分支、离散模型、实际云台反馈、数据年龄、弹速来源和开火延迟。联调时按“射击轨迹、规划轨迹、实测云台、实际落点”逐级比较，并用干预区分关联与根因。下一章继续说明这些状态与命令如何通过串口传递。

（本章核对的固定版本包括：rm_vision 主仓库 `2ead8d0`、rm_auto_aim `f244d0e`、rm_serial_driver `794f4ec`、rm_gimbal_description `0ccd8ce`，以及同济 SuperPower 的 `sp_vision_25` `58c6278`。文中的比赛性能值来自 `sp_vision_25` README 自述，本书未获得原始比赛日志；示意脚本只复现文中声明的简化模型。）

（许可证按文件核对后仍有未消解的冲突：rm_vision、rm_auto_aim 与 `sp_vision_25` 的根 `LICENSE` 是 MIT，但 rm_vision 的 bringup 包和 rm_auto_aim 的部分接口包在 `package.xml` 中仍写着 `TODO`，rm_auto_aim 的 detector／tracker 包则写 BSD；rm_serial_driver 的根 `LICENSE` 与源码文件头是 Apache-2.0，`package.xml` 却写 MIT；rm_gimbal_description 的根 `LICENSE` 是 Apache-2.0，`package.xml` 仍是 `TODO`。这些是固定快照中的文件级观察，本章不替上游判断哪个标注具有优先效力。复用或再分发前，应按实际文件清单审计并向维护者确认。）
