这一篇要把前三篇的积累落成一件具体的东西——*一套能上场的完整自瞄系统*。基础篇给了工程机制（Linux、C++、ROS、串口与 OpenCV），数学理论篇给了数学（相机模型、坐标变换、卡尔曼滤波、神经网络），实战技术篇给了支撑设施（通信协议、标定、时间戳对齐、弹道）；到这里零件和图纸都齐了，缺的是把它们装成一台真能在赛场上打中目标的机器。

六章顺着数据流一站一站走：相机把世界变成带时间戳的图像，识别器在图像里框出装甲板，跟踪器把一帧帧孤立的检出串成一台车的运动状态，能量机关另起一条识别线，自瞄算法设计把所有模块编排成系统、并回答"何时开火"，串口再把算好的目标送回电控、也把电控的姿态送回视觉。每一章都守同一条章魂——怎么做、怎么验、怎么翻车，各配一个能上手的验证动作；算法原理与数学只回指前三篇，不在这里重推。本篇插图全部由脚本生成、可复现，脚本统一放在仓库的 `examples/application/` 下，后文图注只写文件名。现在从第一站开始。

=== 为什么自瞄不用普通摄像头

自瞄这条链路，第一站是眼睛。眼睛看错了，后面再精妙的 PnP、滤波、弹道都是把错误一路放大。所以在写任何识别代码之前，先得把相机这一环立住——而立住的第一步，是想清楚一个看似无聊的问题：手边一枚免驱的 USB 摄像头（罗技那类 UVC 设备）为什么不能用？

答案不是"工业相机更贵更好"这种废话，而是一条第一性的观察：自瞄工作在一个极端的成像域里。基础篇在真实比赛画面上跑级联识别时已经点破——实车自瞄相机用 1～4 毫秒的低曝光拍摄，画面里除了自发光的灯条几乎全黑，整套判据正是为那个域设计的。普通摄像头恰恰把这个域给毁了：它的自动曝光会拼命把画面提亮到"人眼看着舒服"，于是灯条彻底过曝成一团纯白、背景杂光全被抬起来，红蓝分不开、数字看不清，你精心设计的低曝光判据当场失效。而 UVC 协议根本不给你关自动曝光的稳定接口——这才是致命的。

把两类相机摆在一起看，差距是系统性的：

#figure(
table(
  columns: (auto, auto, auto),
  align: (left, left, left),
  stroke: (x, y) => (
    top: if y == 0 or y == 1 { 1pt } else { 0pt },
    bottom: if y == 7 { 1pt } else { 0pt },
  ),
  inset: 7pt,
  table.header([*能力*], [免驱 USB 摄像头], [工业相机]),
  [曝光/增益], [自动，接口不稳、关不干净], [SDK 手动锁定，可精确设到微秒],
  [快门], [多为卷帘（果冻效应）], [全局快门（同一时刻采样）],
  [外触发], [无], [6-pin 光耦 IO，硬触发],
  [时间戳], [到达即打，含未知缓冲延迟], [可在曝光/取流瞬间打，或硬件同步],
  [镜头], [固定，视场不可选], [可换 M12/C 口，按兵种选焦距],
  [动态范围], [低，灯条易饱和截断], [高，亮灯条与暗数字同帧可见],
  [接口], [USB2/UVC 带宽有限], [USB3 / 千兆网口 GigE],
),
caption: [自瞄选相机看的不是"清不清晰"，而是这七项里能不能把成像牢牢攥在自己手里。读表规则：普通摄像头把"自动"当卖点，而自瞄要的恰恰是"手动"——每一项自动化都是一个你无法控制的变量，而识别算法的判据全都绑死在这些变量上。],
)

这一章就沿着这张表往下走：先把无法软件补救的物理层（快门、动态范围、镜头）在选型时定死，再谈能靠参数和代码调的那一半（曝光、增益、白平衡、去马赛克、取流），最后落到怎么把相机接进整个系统的时间轴，以及怎么验证它真的调对了。标定怎么做是「实战技术篇」相机标定与手眼标定一章的主场，针孔与畸变的数学在「数学理论篇」相机模型一章，曝光域背后的成像原理基础篇也讲过——本章只在需要处回指，不重讲。

=== 选型第一步：先把物理层定死

有些东西买错了，再多代码也救不回来。快门类型、动态范围、镜头焦距就是这一类——它们在下单那一刻就决定了，赛场上无从更改。

==== 快门：全局还是卷帘

CMOS 传感器读出一帧有两种方式。*卷帘快门*（rolling shutter）逐行曝光读出：第一行先曝光，然后第二行、第三行……直到最后一行，首尾之间差着几百微秒乃至毫秒。全局快门（global shutter）则让所有感光单元在同一时刻一起曝光，代价是每个像元都要配一个暂存电荷的寄存器，制造成本更高。

对静止场景两者没区别，但自瞄面对的全是高速运动的目标——平移的步兵、自旋的小陀螺、匀速转动的能量机关。卷帘快门读到最后一行时，目标已经动了，同一帧图像的上下部分其实不是同一时刻拍的，于是画面里的直线被拧成斜线、断层，这就是臭名昭著的*果冻效应*。一根本该竖直的灯条被拍成倾斜的平行四边形，倾角判据、PnP 解算全都跟着错。

所以结论没有余地：*自瞄相机必须是全局快门*。这不是"最好是"，而是"必须是"。果冻效应属于逐行采样的系统性几何畸变，落到时间轴上就是"同一帧内不同像素对应不同时刻"——它和多传感器之间的时间对齐是同一类问题，怎么把这类时序错位量化、补偿，「实战技术篇」时间戳对齐一章会专门算。选型阶段你只要认准一件事：卷帘相机在这个任务上直接出局。

==== 动态范围与靶面：为什么灯条会"发白"

几乎每个 RM 新人都问过同一个问题：为什么我的画面整体很暗，可灯条中间还是白花花一片，连红蓝都分不出来？这不是曝光没调好，是撞上了传感器的硬件天花板——*动态范围*（宽容度）不足。

动态范围是单个感光单元能记录的最暗到最亮的光强跨度，物理上由这个单元的*满阱容量*（能容纳多少电荷）决定。灯条是主动发光体，中心光强极高；当它产生的电荷超过满阱容量，输出就*截断*在最大值——像放大器削顶一样，再亮也是那个数。三个通道全部顶到 255，颜色信息就此丢失，纯白一片。这时候你压低曝光，只是让暗的地方更暗，早已饱和的灯条中心纹丝不动。

这个现象直接影响判据设计。传统级联要的是"恰当的低曝光"：灯条足够亮以便二值化找到它，但又*别过曝到纯白*——因为红蓝阵营的投票要靠灯条像素里 R、B 通道谁大，一旦饱和成 (255,255,255)，投票就瞎了，最坏的后果是把敌我认反、开枪打友军。同时两灯条之间那块透视矫正出来的数字小图还得看得清，分类器才认得出兵种。要在同一帧里既容下极亮的灯条、又保住暗处的数字，靠的就是高动态范围。至于摄影里的 HDR（多曝光融合），在对抗中不可用——它要连拍多张再对齐融合，运动目标一拍就糊，实时性也不允许。软件救不了动态范围，只能在选型时选够。

与之相关的两个硬件量也顺带定了成像下限。*靶面尺寸*是感光面积，"底大一级压死人"——面积越大进光越多、信噪比越高，暗环境成像越好。*像元尺寸*是单个像素的面积，同样靶面下像元越大感光越强、满阱越深（动态范围更好），但分辨率随之下降；这是一对此消彼长，选型就是在"看得清远处小灯条"（要分辨率）和"暗处不糊、不过曝"（要大像元、大动态范围）之间权衡。工业相机 SDK 通常还提供*像素合并*（Binning）：合并相邻像元换取亮度和帧率，代价是分辨率——这是一个可以留到调参阶段再动的旋钮。

==== 镜头：焦距就是视场

传感器决定了"每个像素能记录什么"，镜头决定了"这些像素对准哪一片空间"。工业相机的镜头一般是定焦、可调像距；在小孔成像近似下，焦距和像距等同，焦距这一个数就框定了视场角和视距的此消彼长——焦距长看得远但视野窄，焦距短视野广但看不远。广角镜头是后者的极端。

对自瞄，镜头选型的落脚点其实很具体：目标身上的灯条，在像面上要占足够多的像素，识别和 PnP 才有精度。拿真实内参估一下就有体感——社区一台 1440×1080、焦距约 1806 像素的海康相机（配 6 mm 镜头），一根 55 mm 长的灯条成像大小是"物长 ÷ 物距 × 焦距(像素)"：5 m 处约 20 px 高，7 m 处降到约 14 px。灯条越小，二值化的斑块越碎、角点越抖，PnP 深度误差越大。所以打得远的场合要换长焦，把远处目标"拉大"。

于是各兵种的典型配置就有了逻辑：

#figure(
table(
  columns: (auto, auto, auto),
  align: (left, center, left),
  stroke: (x, y) => (
    top: if y == 0 or y == 1 { 1pt } else { 0pt },
    bottom: if y == 3 { 1pt } else { 0pt },
  ),
  inset: 7pt,
  table.header([*用途*], [典型焦距], [理由]),
  [步兵近战], [广角 / 6 mm], [近距离对抗，视场要广，6 mm 兼顾长短],
  [步兵打符], [8～12 mm 长焦], [能量机关激活点约 7 m，长焦拉大远处靶心],
  [哨兵], [双相机（广角＋中长焦）], [广角"广撒网"粗定位，长焦精定位],
),
caption: [镜头焦距按打击距离选，本质是"让目标灯条在像面上占够像素"。读表规则：这些不是死数字——打符选 12 mm 而非 8 mm 的实际考量之一是长焦下目标更大、调参更从容；同一支队伍也可能给所有车都上双相机或变焦镜头，做各距离通吃。],
)

这里要厘清一个新人常混的概念：镜头给相机看的是*灯条*，不是装甲板那块塑料外壳。装甲模块的物理外形（大装甲约 235 mm 宽、小装甲约 140 mm）和视觉真正拿去做 PnP 的*灯条几何*（灯条间距、灯条长度那一组尺寸）是两回事——PnP 拟合的是两根灯条的角点，不是塑料板的外沿，所以这两组数字对不上是正常的，别拿外壳尺寸去解算。灯条几何的具体取值和 PnP 怎么解，是「装甲板识别」一章与「实战技术篇」相机标定与手眼标定一章的内容，这里只提醒：镜头这一环关心的是"灯条在像面上多大"，不是塑料板多大。焦距和视场的精确计算要用相机成像模型，那在「数学理论篇」相机模型一章；镜头带来的畸变要靠标定去除，那也在同一章——本章不重推。

=== 成像参数调优：曝光、增益、帧率的三角

物理层定死之后，剩下这一半是能靠 SDK 和代码调的。核心就三个量：曝光、增益、帧率，它们互相拉扯，构成一个绕不开的三角。

==== 曝光决定拖影

曝光时间是每帧的感光时长。它一手定亮度，也一手定*运动拖影*——而拖影这件事，值得掰开算清楚，因为它常被错误地归咎于帧率。

设目标以横向速度 $v$ 在距离 $d$ 处运动，它相对相机的角速度是 $omega = v \/ d$。这段角运动投影到像面上（焦距 $f$ 以像素计），像点每秒扫过 $omega f$ 个像素。在一次曝光的时间窗 $t_"exp"$ 内，目标的像就在传感器上涂抹了

$ "blur" = v/d dot f dot t_"exp" quad ["px"] $

个像素。这条式子藏着一个关键的、反直觉的结论：拖影只由曝光时间决定，和帧率无关。全局快门在曝光窗内让所有行一起积分，一帧内涂多长，取决于快门开了多久，而不是你每秒拍几张。帧率影响的是时间上的采样密度（跟踪够不够密），不是单帧糊不糊。想要灯条锐利，唯一的办法是压短曝光。

#figure(
  image("images/app-camera-motion-blur.png", width: 100%),
  caption: [拖影随曝光线性增长，与帧率无关（脚本 `camera_motion_blur.py`，焦距取真实内参 $f approx 1806$ px）。读图规则：绿色带是"灯条仍然锐利"的区（拖影 < 2 px 的经验阈值）；两条线是两个典型工况，竖虚线是社区某配置实际用的 2.5 ms 曝光。中距离横移（蓝，$v=3$ m/s、$d=5$ m）在 2.5 ms 处拖影 2.7 px 刚出锐利区，而近距离快速运动（红，$v=4$ m/s、$d=3$ m）达到 6.0 px 已明显拖尾——同一个曝光，越近越快的目标糊得越狠，这就是压曝光的动机。],
)

压短曝光的代价是画面变暗，于是三角的第二个顶点登场：*增益*。增益在电荷信号转成数字前把它放大（单位常是 dB，注意量级），能在低曝光下把亮度买回来，代价是噪声一起被放大、信噪比下降。所以曝光和增益是一对反向旋钮：曝光往下压去拖影，增益往上提补光，提到噪点开始淹没灯条边缘就到头了。实践中的次序是*先用曝光把拖影压到锐利区，再用增益补足亮度*——反过来先拉高增益，只会得到一张又糊又噪的图。至于低曝光下"只见灯条、背景全黑"这个域本身，以及它为什么让传统判据成立，是基础篇讲过的曝光域，这里不重复。

三角的第三个顶点*帧率*，受两头夹。上头是曝光：一帧的周期不可能短于它的曝光时间，所以曝光给出一个帧率上限 $1\/t_"exp"$——2.5 ms 曝光对应 400 fps 的上限。下头是接口带宽：一台 1440×1080 的相机，单帧 Bayer 原始数据约 1.5 MB，跑到 245 fps 就是约 380 MB/s，已经逼近 USB3 的可用带宽（约 350～400 MB/s）。两相对照就明白：在 2.5 ms 曝光下，曝光远不是瓶颈（400 fps 上限用不满），真正卡住你的是带宽和读出——这也是为什么相机手册标称的两百多帧是传感器满速上限，实战跑不到，常见工作点在 100～165 fps。带宽这笔账还顺带解释了下一节的一个选择：为什么图像要以 Bayer 而非 BGR 的格式传上来。

==== 增益、白平衡与 Bayer 去马赛克

先接着上面的带宽说去马赛克。传感器的每个像元其实只感受一种颜色——最常见的是 RGGB 的 *Bayer 阵列*，红、绿、蓝像元交错排布，绿色因人眼敏感而占一半。所以相机直出的原始数据是单通道的 Bayer 图（每像素 1 字节），把它变成三通道 BGR 图需要*去马赛克*（demosaic）：对每个像素，用邻域里其它颜色的像元插值补出缺失的两个通道。

这一步放在哪做，是一个带宽驱动的工程决策。如果相机内部就转成 BGR8 再传（每像素 3 字节），带宽直接三倍——上一节算过，245 fps 的 BGR8 要 1.1 GB/s，USB3 根本扛不住。所以主流做法是*相机传 Bayer、上位机去马赛克*：线上只走 1 字节/像素，落到 CPU 上再用一次 `cv::cvtColor` 展开成三通道。少数场合（带宽富余、想省 CPU）也可以让 SDK 直出 BGR8——它对 OpenCV 最友好，`cv::Mat` 拿来即用、零转换开销。两条路都对，取舍看你的带宽和算力哪个更紧。

去马赛克这一行代码有个极易踩的坑：Bayer 版式必须和传感器实际排列匹配。

```cpp
// 相机报的像素格式 -> OpenCV 的转换码，逐版式建一张查找表
static const std::unordered_map<PixelFormat, cv::ColorConversionCodes> kBayer = {
    {BAYER_RG8, cv::COLOR_BayerRG2RGB}, {BAYER_GR8, cv::COLOR_BayerGR2RGB},
    {BAYER_GB8, cv::COLOR_BayerGB2RGB}, {BAYER_BG8, cv::COLOR_BayerBG2RGB},
};
cv::cvtColor(raw, bgr, kBayer.at(frame.pixel_format));   // 一次插值展开成 BGR
```

社区的相机驱动几乎都有这么一张按像素格式分支的查找表，原因就在这里：RG / GR / GB / BG 四种版式差的只是起始那一格的相位，配错一格，整幅图就偏色，最恶劣的情况是红蓝互换——对自瞄这等于把敌我颜色投票整个反过来。更阴险的是 OpenCV 的 Bayer 命名约定和厂商传感器的版式约定常常错开一格：厂商按左上角起始的那两格命名，OpenCV 却取自第二行的第二、三列，光看名字对不上。上表刻意用 `…2RGB` 拼写，正是为了抵消这一格——厂商报 `BAYER_RG8`，就配 `COLOR_BayerRG2RGB`，它在 OpenCV 里和 `COLOR_BayerBG2BGR` 是同一个枚举值（都是 46），于是名字上"看着像 RGB"，实际吐出的恰恰是错开一格之后正确的 BGR 图。若照着同名的 `COLOR_BayerRG2BGR` 写，红蓝就正好互换。所以*别信文档，用一张已知红蓝装甲的图实测*：拍出来红显红、蓝显蓝，才算配对。

最后是*白平衡*：让本应是白色的物体在任何色温光源下都还原成白，本质是调 R、G、B 三通道的增益配比。多数工业相机可以设成连续自动白平衡，交给相机自己收敛即可；只有当场馆灯光让红蓝装甲明显偏色、影响颜色投票时，才需要手动锁定一组白平衡系数。

=== 外触发：把相机接进系统的时间轴

到这里相机能出清晰的图了，但自瞄是个多传感器系统：相机一两百帧、IMU 和电控上千赫兹、云台一直在转。要把一帧图像和"拍这帧时云台指向哪"对上，图像必须带一个可信的*时刻*。给图像盖时间戳有两条路。

第一条是*软件时间戳*：取流线程一拿到帧，立刻用一个稳定时钟（`std::chrono::steady_clock`，单调不回拨）打一个戳。简单、不用接线，代价是这个戳并非真正的曝光时刻——它含着传感器读出、USB 传输、驱动缓冲这一路的延迟和抖动。对单相机自瞄，配合后端用时间戳把 IMU 姿态插值到图像时刻（在 IMU 队列里按四元数 slerp 插值对齐），这条路在实战里跑得很稳。

第二条是*硬件外触发*：用相机侧面那个圆形 6-pin 数字 IO 口。它一般包含一对经光耦隔离的输入/输出（电气隔离保护）、电源与地、以及一路可配置 GPIO。把外部信号接到光耦输入，相机就在信号沿的确定时刻曝光，采样瞬间由硬件钉死、抖动极小。它有两个不可替代的场景：*多相机同步*（哨兵的广角＋长焦要同时曝光，才能把两视图对上）和*相机–IMU 硬同步*（用同一个触发源同时驱动相机曝光和 IMU 采样，从物理上消除对齐误差）。软触发和自由运行（free-run）则是另外两档：软触发由上位机发命令逐帧取，自由运行让相机按设定帧率自己连续吐帧——单相机自瞄多用自由运行加软件时间戳，够用且简单。

选哪条路是一个"复杂度换确定性"的权衡：接线的硬触发给你确定的采样时刻，软件时间戳给你零布线但有抖动的近似。无论哪条，图像时刻怎么和 IMU/电控对齐、延迟怎么补偿，都是「实战技术篇」时间戳对齐一章的正题，本章只负责把时间戳*正确地打上*——记住一条：时间戳要在曝光/取流的瞬间打，绝不能等图像处理完再打，否则你补偿的是一个被处理耗时污染过的假时刻。

=== 从 SDK 取流到 cv::Mat

选型和参数都想清楚了，最后是把它写成代码：怎么从厂商 SDK 拿到一帧，变成算法能吃的 `cv::Mat`。各家 SDK（海康、大恒 Galaxy、迈德威视）函数名不同，但取流的骨架惊人地一致。

==== 一条标准取流链

以海康 SDK 为例，从初始化到第一帧 `cv::Mat`，标准链路是这样一串（大恒、迈德威视只是把 `MV_CC_*` 换成 `GX*` 或 `Camera*`，步骤一一对应）：

```cpp
MV_CC_DEVICE_INFO_LIST list;
MV_CC_EnumDevices(MV_USB_DEVICE, &list);        // 1. 枚举设备
MV_CC_CreateHandle(&handle, list.pDeviceInfo[0]);
MV_CC_OpenDevice(handle);                       // 2. 按索引/序列号打开

// 3. 一次性配置：先关掉所有"自动"，再写死我们要的值
MV_CC_SetEnumValue(handle, "ExposureAuto", MV_EXPOSURE_AUTO_MODE_OFF);
MV_CC_SetEnumValue(handle, "GainAuto",     MV_GAIN_MODE_OFF);
MV_CC_SetFloatValue(handle, "ExposureTime", 2500.0);   // 微秒，即 2.5 ms
MV_CC_SetFloatValue(handle, "Gain", 8.0);
MV_CC_SetEnumValue(handle, "BalanceWhiteAuto", MV_BALANCEWHITE_AUTO_CONTINUOUS);
// PixelFormat 设成 BayerRG8；触发模式设自由运行或外触发

MV_CC_StartGrabbing(handle);                    // 4. 开始取流

while (running) {
    MV_FRAME_OUT raw{};                                     // 每轮独立，且必须清零
    if (MV_CC_GetImageBuffer(handle, &raw, 100) != MV_OK) {
        ++fail_count;                                       // 连续失败 N 次才升级为重开设备
        MV_CC_StopGrabbing(handle);
        MV_CC_StartGrabbing(handle);                        // 一级软重连，详见下节
        continue;                                           // 本轮没有帧，绝不能往下走
    }
    fail_count = 0;                                         // 成功即清零
    auto stamp = std::chrono::steady_clock::now();          // 5. 取流瞬间打戳
    cv::Mat bayer(raw.stFrameInfo.nHeight, raw.stFrameInfo.nWidth, CV_8UC1, raw.pBufAddr);
    cv::Mat bgr;
    cv::cvtColor(bayer, bgr, cv::COLOR_BayerRG2RGB);        // 6. 去马赛克 -> cv::Mat
    queue.push({bgr.clone(), stamp});
    MV_CC_FreeImageBuffer(handle, &raw);                    // 7. 必须归还缓冲
}
```

关键顺序不能乱。三处最常见的坑：*像素格式配错*（Bayer 版式或 RGB/BGR 弄反，见上一节）；接口次序错，比如在 `StartGrabbing` 之前就去 `GetImageBuffer`，取流还没开当然拿不到帧；触发模式设成了外触发却没接触发信号，相机静静等一个永不到来的沿，`GetImageBuffer` 一直超时——排障时先确认触发模式，是屡试不爽的第一刀。还有两个易漏点。其一，`GetImageBuffer` 拿到的缓冲是 SDK 借给你的，用完必须 `FreeImageBuffer` 归还，否则缓冲池很快耗尽、取流卡死。其二，取流失败的分支必须显式 `continue`（或跳出循环），绝不能让控制流直接落到下一行——那时 `MV_FRAME_OUT` 里的 `pBufAddr` 还是未初始化的野指针，拿它去包 `cv::Mat` 是标准的未定义行为，往往在开机第一次超时时就当场段错误。这也是上面把 `MV_FRAME_OUT` 声明在循环*内部*并显式清零的原因：让每一轮的失败无法污染下一轮。

#figure(
  image("images/app-camera-grab-pipeline.png", width: 100%),
  caption: [从 SDK 到 `cv::Mat` 的取流链，以及它的有界自愈重连（脚本 `camera_grab_pipeline.py`，骨架取自社区常用的大恒/海康开源 ROS2 驱动，MIT/Apache 协议）。读图规则：绿色是正常路径——一帧进、一张 `cv::Mat` 出，成功就把失败计数清零、回去取下一帧；红色是掉帧路径——一次取流失败通过"停止＋重启采集"就地自愈，只有连续失败 $N$ 次才升级为重开设备。左列的配置只做一次，右列的取流循环一直转。],
)

==== 掉线重连：一帧掉了不该拖垮整局

赛场上 USB 会被震松、相机会瞬断，取流失败是常态而非异常。健壮的驱动从不因为一次 `GetImageBuffer` 失败就崩掉，而是分级自愈，图里画的正是这套逻辑。

*第一级是软重连*：取流失败了，就地 `AcquisitionStop` 再 `AcquisitionStart`，失败计数加一；只要下一帧回来就把计数清零。这一级足以扛过绝大多数瞬时抖动，代价极小。第二级是硬重连：如果软重连不管用（USB 协议栈卡死这种，光停/开采集救不回来），就要把 USB 设备整个复位。社区一种成熟的无 ROS 实现用了两个线程配合——一个取流线程正常吐帧，失败时置一个"未在取流"的标志并退出；一个守护线程每 100 ms 轮询这个标志，一旦发现取流停了，就调 `libusb_reset_device` 把设备从 USB 层彻底复位（按 vid:pid 定位那台相机），再重启取流。这比软重连重，但能救回软重连救不了的僵死。第三级是放弃：连续失败超过阈值 $N$，说明不是抖动而是真断了（线掉了、相机烧了），此时该主动 shutdown 或重开设备，把故障暴露出来，而不是在坏状态里空转——让上层看见"相机没了"，比默默卡住安全得多。

这套分级的哲学和通信那条链一脉相承：宁要最新的一帧，不要为了一帧完整而阻塞整条链路。掉一帧就丢一帧、立刻自愈，跟「实战技术篇」通信协议设定一章里"坏帧直接丢弃、等下一帧"是同一个"UDP 式"的取舍。

==== 两种架构：ROS 节点 vs 一相机一进程

相机驱动怎么组织进整个系统，社区有两种成型的架构。整套系统该怎么编排、两种哲学各自的完整账本，是「自瞄算法设计」一章的正题；这里只看它们在*相机这一环*落地成什么样，因为取图的组织方式会一路影响到后面所有模块。

一种是*ROS2 节点式*。相机被写成一个 ROS2 组件节点，把图像用 `image_transport` 发布成 topic，曝光、增益等参数走 ROS 参数系统、支持运行时动态调。它在相机这一环的红利很具体：节点组合（composition）让相机节点和识别节点跑在同一进程里做进程内零拷贝传递，省掉每帧上百 MB/s 的序列化搬运；参数、录包、可视化工具都是现成的。

另一种是*无 ROS、一相机一进程*。相机就是一个普通 C++ 类，藏在一个抽象基类（如 `CameraBase`，`read(cv::Mat&, timestamp&)` 一个接口）后面，用工厂按配置文件挑具体型号（海康/迈德威视）的实现；类内部自己起线程（前面那对守护＋取流线程），用一个容量为 1 的线程安全队列只保留最新帧。取这条路的代价是线程、队列这些 ROS 白送的东西都得自己写，红利是掉线自愈可以做得很重——上一节那套 `libusb` 复位就是它的产物。

两种架构都上过场、都拿过成绩。但有一个设计是它俩共有的、也是最该学走的：队列容量取 1、只留最新帧。相机可能比识别快，若把帧堆进长队列，识别永远在处理几十毫秒前的旧图，云台就追着一个过去的目标打——这在实战里表现为"甩枪"。容量 1 的队列强制"取流快就丢旧帧"，让识别永远看最新的画面。这又是"宁要最新、不要完整"的同一条哲学，和上一节的分级重连完全同源。

==== 多相机与连接方式

工业相机连到运算平台，自瞄里最常见两种物理接口：*USB3*（即数据表上的 USB3.0/3.1，5 Gbps 量级）和*千兆网口 GigE*。USB3 布线简单、延迟低，是单相机的主流；GigE 走网线可拉得更远、天然支持一网多机，多相机或相机离主机较远时有优势。历史遗留的 CameraLink、IEEE1394 通用性差正被淘汰，一般用不到。

多相机（哨兵双目那类）在软件上就是把上面的取流类实例化多份、各跑各的线程；难点不在取流而在两件事：一是*同步*，要它们同时曝光就得靠上一节的硬触发共用一个触发源，否则两图不同时、几何对不上；二是*区分设备*，多台同型号相机枚举出来顺序不固定，必须靠序列号（SN）或 vid:pid 把每台钉到固定的逻辑角色上，不能靠"第 0 台"这种会漂移的索引——这和串口用 udev 规则固定设备名是同一类问题，串口模块一章会给出完整做法。

最后一句上车实操的经验：USB 线在相机一端一定要用带锁扣（螺孔固定）的接头，赛场震动下松动会导致取流异常，反复瞬断甚至可能损坏相机内部电路；确认机械结构不再改动后，接口处打热熔胶或用 3D 打印件固定，能省掉很多"时好时坏"的玄学故障。

=== 取图侧：取流帧率与推理帧率解耦

相机能稳定出图之后，还剩一个把它接到识别算法上的细节，它决定了整条链路的延迟表现。这也是「数学理论篇」深度学习和神经网络一章末尾留下的账——引擎选型和帧率调优——的取图那半边。

问题是这样的：相机可能跑 150 fps，而车载算力上跑一次神经网络推理也许只有 30～60 fps，两者对不齐。绝不能让相机把帧堆进队列等推理慢慢消化——那样每处理一帧，队列里都积着更旧的帧，延迟越滚越大。正确做法是把*取流线程和推理线程解耦*：取流线程只管以最新帧覆盖那个容量为 1 的队列，推理线程处理完一帧就去队列拿"此刻最新的一帧"，中间攒下的旧帧直接丢弃。宁可丢帧，也不让延迟累积——毕竟自瞄要的是当下目标在哪，不是三帧前在哪。成熟框架还会把推理做成异步流水（提交一帧的同时取上一帧的结果，吞吐模式榨满硬件），但闸门始终是"只留最新"。

这里还锁死了一件和跟踪、预测生死攸关的事：*时间戳的归属*。每一帧带的必须是它*被拍下那一刻*的时间戳（前面取流线程打的那个），而不是它被推理完成的时刻。因为跟踪器要用这个时间戳，把目标状态沿运动模型外推到"现在"甚至外推一个弹丸飞行时间去瞄准——时间戳错一点，外推的落点就偏一截。取流侧把时间戳正确地钉在帧上、把延迟压在最小，跟踪侧才有正确的时间基准可用。至于具体在某块板卡上选哪个推理引擎（OpenVINO / TensorRT）、怎么把帧率榨到极限，是装甲板识别一章推理落地的正题，这里只交代取图这半边的接口纪律。

=== 怎么确认相机调对了

前面每一步都可能悄悄埋雷，而相机的问题往往到识别阶段才以"识别率低"的面目暴露，回头排查代价很大。所以相机装好、参数设完，别急着跑识别，先过一遍这份验收清单——每一条都对应本章讲过的一个坑，都能当场看出来：

- *灯条不过曝*：拍一块敌方装甲，看灯条中心是否顶成纯白。用直方图看有没有一大堆像素堆在 255；再抽灯条像素看 R、B 通道是否还分得开——分不开就是曝光过高或动态范围不够，颜色投票会瞎，先降曝光。
- *无拖影*：让装甲板横向快速移动或开小陀螺，放大看灯条边缘是否锐利、有没有被拉成拖尾的平行四边形。对照本章拖影公式 $"blur" = (v \/ d) f t_"exp"$ 估一下，超过约 2 px 就压曝光、补增益。
- *颜色不反*：拿已知的红、蓝装甲各拍一张，确认出图里红显红、蓝显蓝。若红蓝互换，是 Bayer 版式或 BGR 顺序配错——这条不测，赛场上可能直接打友军。
- *帧率稳、不周期掉帧*：打印相邻帧时间戳的差值，看帧率是否稳定在工作点附近、有没有规律性的卡顿；掉帧就去看重连日志，区分是偶发抖动（软重连自愈）还是线路真有问题。
- *时间戳单调*：确认时间戳用的是单调时钟、严格递增、间隔均匀——一次回拨就能让下游的滤波器发散。
- *数字可读*：把两灯条之间的区域透视矫正出来，看数字小图在当前低曝光下是否还辨得出，分类器认不认得——认不出往往不是分类器的错，是曝光把数字压没了。

这份清单的价值在于*把问题拦在相机环*：以上六条任何一条不过，都别往下走，因为它引发的故障会在识别、跟踪、开火各处以完全不同的面目冒出来，届时你会去怀疑算法，而根子在这里。相机调对了，后面的识别才站在一个可信的地基上。

==== 本章小结

这一章把自瞄的眼睛立了起来。先想清楚为什么普通摄像头不行——自瞄工作在低曝光域，而它把成像的每一个变量都自动化交了出去。然后按"能不能软件补救"两分：物理层（全局快门、高动态范围、按兵种选的镜头焦距）在选型时定死，参数层（曝光压拖影、增益补亮度、帧率受曝光与带宽两头夹、白平衡与 Bayer 去马赛克）靠 SDK 和代码调。接着把相机接进系统的时间轴（软件时间戳还是 6-pin 硬触发），把 SDK 取流写成一条标准链落到 `cv::Mat`，配上分级自愈的掉线重连，对照了 ROS 节点式与一相机一进程两种架构——但它们共享同一条"只留最新帧"的哲学。最后把取图侧接到推理帧率上（解耦、丢旧帧、时间戳钉在拍摄瞬间），并留下一份能当场排雷的验收清单。

一句话收束：相机这一环，能力全在你把成像攥得多牢，而对错常常等到识别阶段才显形——所以在这里多花的每一分力气，都是给后面所有模块省下的排障时间。相机稳定地吐出带正确时间戳的清晰图像之后，下一章就该在这些图像里，把装甲板真正识别出来。
