本篇把前三篇介绍的知识串成一套自瞄系统。基础篇介绍 Linux、C++、ROS、串口与 OpenCV 等工程基础，数学理论篇介绍相机模型、坐标变换、卡尔曼滤波和神经网络，实战技术篇则讨论通信协议、标定、时间戳对齐与弹道。本篇关注这些模块如何组合、联调和验证。

六章大体按数据流展开：相机输出带时间戳的图像，识别器检测并分类装甲板，跟踪器根据连续检出估计车辆运动状态；能量机关使用另一套识别与预测流程，自瞄算法设计负责组合各模块并给出开火条件，串口模块则在视觉与电控之间传递目标、姿态等数据。每章都说明实现方法、验证手段和常见故障；需要的算法与数学原理回指前三篇，不在这里重复推导。本篇插图由 `examples/application/` 下的脚本生成，后文图注只写文件名。

=== 为什么自瞄通常选工业相机

图像是识别、PnP、跟踪和弹道解算的共同输入，因此相机的成像方式会限制后续算法。写识别代码之前，先要确认手边的免驱 USB 摄像头（例如常见的 UVC，即 USB Video Class 设备）能否稳定提供任务所需的曝光、快门、同步和镜头条件。

自瞄常用毫秒量级的短曝光来限制运动模糊并压低背景；1～4 毫秒可以作为部分场景的调参起点，但实际值要随镜头光圈、环境照度、传感器和目标速度调整。若相机持续启用自动曝光或自动增益，图像亮度会随场景变化：灯条可能饱和，背景杂光也可能被抬高，使依赖亮度和颜色阈值的判据漂移。UVC 规范包含曝光控制，部分消费级相机也能关闭自动曝光；问题在于不同型号、固件和驱动暴露的范围与步进并不一致。只有实测确认能够可靠锁定参数，才能把这类设备纳入方案。即使曝光可控，还要继续检查快门、触发、时间戳和镜头等能力。

下面按自瞄常用的检查项比较两类设备的常见能力：

#figure(
table(
  columns: (auto, auto, auto),
  align: (left, left, left),
  stroke: (x, y) => (
    top: if y == 0 or y == 1 { 1pt } else { 0pt },
    bottom: if y == 7 { 1pt } else { 0pt },
  ),
  inset: 7pt,
  table.header([*能力*], [免驱 USB 摄像头], [工业相机]),
  [曝光/增益], [常默认自动；手动范围取决于型号和驱动], [通常可由厂商 SDK 锁定；范围与步进查规格书],
  [快门], [常见卷帘快门], [可选全局或卷帘快门，需核对具体型号],
  [外触发], [通常没有专用触发 IO], [部分型号提供隔离 IO 和硬触发],
  [时间戳], [设备或驱动可能给出时间戳，但采样语义和缓冲延迟需核对], [部分型号提供设备时间戳或与触发信号同步],
  [镜头], [常为集成镜头，可调范围有限], [常见 M12/C 口，可按任务更换焦距],
  [动态范围], [可选传感器和输出格式较少], [型号选择较多，但仍须核对饱和与噪声表现],
  [接口], [USB/UVC，分辨率与帧率受设备和链路限制], [常见 USB3、GigE 等接口],
),
caption: [消费级 USB 摄像头与工业相机的常见能力对比。产品差异很大，表格用于列出选型检查项，不能代替具体型号的规格书和实测。自瞄更关注参数能否锁定、采样时刻能否确定，以及镜头是否适合目标距离。],
)

本章先讨论软件难以补救的硬件条件，包括快门类型、传感器动态范围和可用镜头，再讨论曝光、增益、白平衡、去马赛克与取流参数，最后说明如何把相机接入系统时间轴并验证配置。标定步骤见「实战技术篇」的相机标定与手眼标定一章，针孔模型与畸变见「数学理论篇」的相机模型一章，基础成像原理也沿用前文；这里仅在需要时回指。

=== 选型第一步：先确定硬件边界

快门类型和传感器动态范围主要由硬件决定，软件不能消除卷帘快门的逐行采样，也不能恢复已经饱和的像素。镜头若采用可换接口，可以在部署前更换，但焦距与光圈仍需按目标距离和照明条件选择。因此，先明确这些硬件边界，再进入参数调优。

==== 快门：全局与卷帘的取舍

CMOS 传感器常见卷帘与全局两种快门。*卷帘快门*（rolling shutter）让各行依次开始和结束曝光，因此首行与末行代表的时刻不同；具体行间隔和整帧扫描时间要查传感器手册或实测，不能由曝光时间直接推出。*全局快门*（global shutter）让所有像元在近似同一时间窗内曝光，再读出整帧。它通常需要更复杂的像元或存储结构，并可能在成本、噪声或填充因子上作取舍。

静止场景中，两者通常不会产生明显的运动几何差异；目标或相机运动时，卷帘快门可能把直线拍斜，或让物体不同高度对应不同姿态，这类现象常称为*果冻效应*。例如，灯条在扫描期间横向移动时，轮廓可能变成倾斜的平行四边形，从而影响倾角判据与 PnP 输入。可先用“像面速度 × 首末行时间差”估计最大像素偏移，再与角点误差容限比较。缩短曝光可以减轻单行内的运动模糊，却不会自动消除行与行之间的时间差。

对于高速移动、旋转目标以及相机自身振动明显的 RM 场景，全局快门通常是风险更低的默认选择，因为后续算法可以把一帧近似视为同一时刻。卷帘快门并非在所有条件下都不能使用：若目标较慢，或通过较小的感兴趣区域（ROI）、高速读出把首末行时间差压到误差预算内，实测也可能满足要求。选型时若没有这类测量证据，优先选择全局快门；若使用卷帘快门，则应把行时序纳入成像模型和验证。多设备时间对齐的方法见「实战技术篇」时间戳对齐一章，但它不能直接替代帧内卷帘畸变建模。

==== 动态范围与靶面：先区分饱和与噪声

画面整体偏暗而灯条中心发白，通常说明一个或多个颜色通道已经饱和，但仅凭画面不能断定传感器动态范围不足。先查看原始像素直方图，再分别降低模拟增益和曝光时间；必要时缩小光圈或加中性密度滤镜。若降低增益或曝光后峰值离开量化上限，说明至少一部分截断与当前参数有关。只降低增益能消除的是后级饱和；若像元在满阱阶段已经饱和，仍须减少曝光时间或入射光。若在任务允许的最低曝光和增益下仍然饱和，同时暗部又必须保留，才说明现有传感器与光学组合覆盖不了所需亮度范围。

传感器动态范围通常用满阱容量与读出噪声之比描述；相机最终可用并编码到图像中的范围，还取决于读出模式、模拟增益和模数转换器（ADC）位数。灯条产生的电荷若超过像元满阱容量，或后级增益使信号超过 ADC 范围，输出就会截断在最大码值。以 8 位图像为例，三个颜色通道都到 255 时，通道之间的比例已无法从该像素恢复。降低曝光会减少像元积累的电荷；降低模拟增益不会改变已经收集的电荷，只会减小送入后级和 ADC 的信号幅度。两者都可能让输出退出最大码值，但只有减少曝光或入射光才能避免满阱阶段的截断；代价是暗部输出也会降低，更容易受到噪声与量化误差影响。

这个权衡会直接影响判据设计。传统流程需要让灯条亮到足以分割，同时尽量避免参与颜色判断的像素饱和；否则基于 R、B 通道大小的阵营判定会失去信息。两根灯条之间的数字区域也要保留足够信噪比，透视矫正后的分类输入才稳定。因此应在代表性照明和距离下，同时检查灯条饱和比例、数字区域信噪比与分类结果。多帧 HDR（高动态范围）融合会引入对齐与延迟问题，通常不适合高速目标；某些传感器提供单帧多增益或其他 HDR 读出，但仍需实测运动伪影、帧率和延迟，不能只依据“HDR”标识选型。

还要区分*靶面尺寸*与*像元尺寸*。靶面尺寸表示有效感光区域，会与镜头像圈和焦距共同决定视场，本身并不保证更高信噪比。像元更大时，在照度、光圈和曝光相同的条件下通常能收集更多光子，并可能具有更大的满阱容量；实际噪声与动态范围仍取决于传感器工艺和读出电路。同样靶面尺寸下，增大像元通常意味着降低原生分辨率，因此要在远处目标的像素数、暗部信噪比和饱和余量之间权衡。若相机支持传感器侧*像素合并*（Binning），可用分辨率换取信噪比，有些型号还能提高帧率；纯软件缩图则不一定有相同收益，选型和调参时要确认实现方式。

==== 镜头：用焦距与靶面共同确定视场

工业相机常用定焦、手动对焦镜头。焦距是镜头属性，像距是镜头主面到成像面的距离，两者并不等同；对远距离目标，合焦时像距才近似等于焦距。视场角还取决于靶面尺寸：在小孔模型下，某一方向的视场角约为 $2 arctan(S / (2 f))$，其中 $S$ 是该方向的传感器尺寸，$f$ 是焦距。同一靶面上，长焦让目标占更多像素但缩窄视场，短焦扩大视场但让同一目标占更少像素。“看得远”不是镜头的硬截止距离，而是目标像素数、对焦、信噪比和算法阈值共同形成的可用距离。

镜头选型可以从目标在像面上的最小像素数反推。忽略目标倾斜时，投影高度近似为 $h_p = f_y H / Z$，其中 $H$ 是目标特征的实际高度，$Z$ 是距离，$f_y$ 是标定得到的纵向像素焦距。例如，对一套 1440×1080、$f_y = 1806$ px 的示例内参，一根 55 mm 长的灯条在 5 m 处约为 20 px，在 7 m 处约为 14 px。这个计算只给数量级；实际还受姿态、畸变、模糊和角点算法影响。可用离线数据测出检测率与角点误差随像素高度的变化，再据此选择能覆盖目标距离、同时保留足够视场的焦距。

下面给出几个候选起点，用来说明距离与视场的取舍：

#figure(
table(
  columns: (auto, auto, auto),
  align: (left, center, left),
  stroke: (x, y) => (
    top: if y == 0 or y == 1 { 1pt } else { 0pt },
    bottom: if y == 3 { 1pt } else { 0pt },
  ),
  inset: 7pt,
  table.header([*用途*], [候选起点], [理由]),
  [步兵近距离对抗], [广角 / 6 mm 起步], [优先保留近距离目标和快速转向时的视场],
  [较远距离的能量机关], [8～12 mm 作为候选], [增加目标像素数；须复核当前场地距离与视场],
  [覆盖较大距离范围], [广角＋中长焦双相机], [广角负责大范围候选，长焦提供更多目标像素],
),
caption: [按任务距离选择镜头的示例起点，不是通用配置。相同焦距在不同靶面上视场不同；下单前应代入实际传感器尺寸计算视场，并用目标像素数和实拍数据复核。],
)

还要区分装甲模块的整体外形与 PnP 所用特征点的物理坐标。传统灯条流程常以两根灯条的端点或中心关系构造 `objectPoints`，其他检测器也可能直接回归装甲角点；无论采用哪一种，三维模板都必须与图像特征的定义一致。尺寸应来自当前赛季规则或对目标的可靠测量，不应把外壳宽度直接代入灯条端点模型。具体检测与 PnP 接口见「装甲板检测与识别」和「实战技术篇」的相机标定与手眼标定两章；视场与畸变模型见「数学理论篇」的相机模型一章。

=== 成像参数调优：曝光、增益与帧率

硬件能力确定后，可以通过 SDK 调整曝光、增益与帧率。曝光影响运动模糊和进光量，增益影响输出幅度与噪声，帧率还受传感器读出和接口带宽限制；三者需要按同一组任务指标联合设置。

==== 用曝光时间约束单帧运动模糊

曝光时间是每帧的积分时长，既影响收集到的光子数，也决定目标在一次积分期间能移动多远。帧率描述相邻帧的采样间隔；在曝光时间不变时，提高帧率不会直接缩短单帧内的运动轨迹。

在小角度、距离变化可忽略的近似下，设目标相对相机的横向速度为 $v$，距离为 $d$，则角速度约为 $omega = v \/ d$。若像素焦距为 $f$，一次曝光时间为 $t_"exp"$，预测的像面位移为

$ "blur" = v/d dot f dot t_"exp" quad ["px"] $

像素。这个一阶模型说明：在相对运动与光学参数固定时，单帧运动模糊近似随曝光时间线性变化，而帧率主要影响跟踪的时间采样密度。实际模糊还会受到相机旋转、目标姿态变化、离焦、振动和卷帘扫描影响。缩短曝光是常用手段，也可以通过减小相对运动、改善安装或使用与曝光同步的照明来降低模糊；最终应在实拍图像上测量边缘或角点误差。

#figure(
  image("images/app-camera-motion-blur.png", width: 100%),
  caption: [一阶模型预测的拖影与曝光时间关系（脚本 `camera_motion_blur.py`，示例取 $f approx 1806$ px）。绿色区域采用 2 px 作为演示阈值，并非通用合格线；应根据实际角点算法确定容限。按公式，2.5 ms 曝光下，$v=3$ m/s、$d=5$ m 的位移约为 2.7 px，$v=4$ m/s、$d=3$ m 时约为 6.0 px。这些数值是模型计算，不包含振动、离焦和卷帘效应。],
)

缩短曝光会减少收集到的光子数，此时可适量提高模拟*增益*。增益在量化前放大信号，也会放大光子散粒噪声和增益级之前的读出噪声，并减少 ADC 端的高亮信号余量；它不能补回没有采集到的光子。适当的模拟增益可以让信号相对增益级之后的电路噪声和量化误差更大，却不能改善增益之前已经确定的信噪比。因此，“增益必然降低信噪比”和“增益可以补光”都不准确。实用次序是：先按运动模糊和饱和上限确定曝光，再使用满足数字区域可见性的最低增益，并同时检查噪声、饱和比例和识别指标。若仍不满足，应回到照明、光圈或传感器选择，而不是继续提高数字亮度。低曝光域与传统判据的关系见基础篇。

*帧率*同时受曝光、传感器读出、ROI、像素格式、接口与主机处理能力限制。若不考虑流水重叠和读出开销，$1\/t_"exp"$ 只是曝光给出的粗略上界，例如 2.5 ms 对应 400 fps；具体相机还要按手册中的时序模型计算。带宽也可先估算：1440×1080 的 Bayer8 图像每帧约 1.56 MB，245 fps 的有效载荷约 381 MB/s，尚未计入协议开销。这个量级可能接近某些 USB3 相机、主控与线缆组合的持续能力，但不能据此给所有设备设定统一帧率。应读取 SDK 报告的实际帧率，并检查丢帧计数和主机负载。该估算也说明了下一节为何常在线上传 Bayer 数据，而不是体积约三倍的 BGR8。

==== 增益、白平衡与 Bayer 去马赛克

彩色传感器常在每个像元上覆盖一种颜色滤光片，RGGB 是常见的 *Bayer 阵列*。因此 Bayer 原始帧每个采样点只包含一种颜色；Bayer8 是每像素 1 字节，Bayer10/12 等格式的存储量还取决于是否打包。要得到三通道 BGR 图，需要*去马赛克*（demosaic），利用邻域采样估计每个像素缺失的两个颜色通道。

去马赛克放在相机、厂商 SDK 还是应用程序中，会影响链路带宽和主机开销。若相机在线上传 BGR8，有效载荷约为 Bayer8 的三倍；以上一节的分辨率和帧率估算，约为 1.14 GB/s，超过常见 USB3 Vision 链路的持续有效吞吐。因而常见方案是在线上传 Bayer8，再在主机上用 `cv::cvtColor` 或厂商转换函数生成 BGR。若 SDK “输出 BGR”，还要确认转换发生在相机内还是主机库中；只有前者可能增加传输量，后者仍占用主机算力。应以相机支持的像素格式、实测吞吐和 CPU/GPU 余量作选择。

Bayer 版式必须与传感器实际排列匹配，因此需要显式维护转换表：

```cpp
// 这里假设厂商枚举描述左上角的 Bayer 相位；目标内存顺序是 BGR。
// OpenCV 的 Bayer 名称容易误读，须用当前版本和已知色块验证这张表。
static const std::unordered_map<PixelFormat, cv::ColorConversionCodes> kBayer = {
    {BAYER_RG8, cv::COLOR_BayerBG2BGR}, {BAYER_GR8, cv::COLOR_BayerGB2BGR},
    {BAYER_GB8, cv::COLOR_BayerGR2BGR}, {BAYER_BG8, cv::COLOR_BayerRG2BGR},
};
cv::cvtColor(raw, bgr, kBayer.at(frame.pixel_format));
```

RG、GR、GB、BG 表示不同的 Bayer 起始相位，映射错误会造成整体偏色或红蓝交换。厂商枚举与 OpenCV 转换码的命名不一定能按同名直接配对；上表假定厂商的 `BAYER_RG8` 表示左上角为 R 的 RGGB 数据，并显式选择输出 BGR 内存顺序的转换码。不要把这张表未经检查地复制到另一家 SDK：先核对像素格式定义，再用不饱和的已知红、绿、蓝色块采集原始帧，分别检查输出通道均值。该测试还应固定 OpenCV 版本，以免升级后未察觉转换行为变化。

最后是*白平衡*，本质是调整 R、G、B 三通道的增益比例，使中性物体在给定光源下接近中性。连续自动白平衡会随画面内容改变通道增益，在背景很暗、发光目标占比变化的场景中可能使颜色阈值漂移。可先用中性目标让自动白平衡收敛，再记录并锁定系数；随后在预期场馆照明范围内复测红蓝分类。若照明条件跨度较大，可按场地保存多组配置或改用对增益变化更稳健的颜色特征，而不是默认连续自动模式一定适用。

=== 触发与时间戳：把相机接入系统时间轴

自瞄需要把图像与云台姿态、IMU 和电控数据对齐。各设备频率不同并不是主要困难；关键是明确图像时间戳代表曝光开始、曝光中点、曝光结束还是主机收帧时刻，并把各时间戳转换到同一时钟域。可用方案不止一种，应根据设备能力给出时间误差预算。

如果相机提供设备时间戳或曝光事件时间戳，应先确认其语义、计数频率和回绕规则，再标定设备时钟到系统时钟的映射。这通常比主机收帧时间更接近实际曝光。若只能使用*软件时间戳*，可以在取流线程拿到帧后立即读取 `std::chrono::steady_clock`。这个时间表示应用线程取得 SDK 帧缓冲，包含传感器读出、传输、驱动和缓冲延迟；只有测量其固定偏移与抖动后，才能估计曝光时刻并据此插值 IMU 姿态。若 IMU 使用单片机时钟，还需先完成两种时钟之间的同步，不能直接比较数值。

*硬件外触发*可让一台或多台相机响应同一电气边沿，适合需要限制相机间曝光偏差的系统。接口形态、允许电压、是否光耦隔离以及引脚定义均随型号变化，必须按相机手册接线。触发边沿到曝光开始仍有设备规定的延迟与抖动；共享触发源只能减少采样时刻的不确定性，不能自动消除相机与 IMU 的时钟偏移、曝光窗口差异或信号传播延迟。应记录触发边沿的时间，并用示波器、闪光 LED 等手段测量实际曝光响应。

软触发由主机命令启动单帧，延迟通常受软件调度和链路影响；自由运行（free-run）让相机按配置连续采集。单相机是否可以使用自由运行加设备或主机时间戳，要由姿态角速度、总延迟和抖动预算决定，不能仅凭系统规模判断“够用”。多设备时间同步、时钟映射与延迟补偿见「实战技术篇」时间戳对齐一章。无论采用哪种模式，都应在帧进入处理链时保留原始采样时间语义，而不是在处理完成后用当前时间覆盖它。


=== 从 SDK 取流到 cv::Mat

完成选型与参数设置后，还要把厂商 SDK 返回的帧转换为算法使用的 `cv::Mat`。海康、大恒 Galaxy、迈德威视等 SDK 的类型、错误码和资源生命周期不同，但通常都包含枚举设备、打开、配置、开始采集、取得帧和归还缓冲等步骤。下面的代码用于说明控制流，实际项目必须以所安装 SDK 版本的头文件和示例为准。

==== 一条标准取流链

以下用海康 SDK 风格的接口展示取流链。错误码名称和参数节点可能随 SDK 版本变化；大恒、迈德威视等 SDK 需要按各自文档重新实现，不能只替换函数名前缀。

```cpp
auto check = [](int code, const char* operation) {
    if (code != MV_OK) throw std::runtime_error(operation);
};

MV_CC_DEVICE_INFO_LIST list{};
check(MV_CC_EnumDevices(MV_USB_DEVICE, &list), "enumerate cameras");
if (list.nDeviceNum == 0) throw std::runtime_error("no camera found");

// 项目辅助函数遍历设备信息，并按配置的唯一序列号选择相机。
MV_CC_DEVICE_INFO* device = select_by_serial(list, configured_serial);
if (device == nullptr) throw std::runtime_error("configured camera not found");

void* handle = nullptr;
check(MV_CC_CreateHandle(&handle, device), "create camera handle");
check(MV_CC_OpenDevice(handle), "open camera");

// 参数值只是示例；曝光、增益和白平衡应来自本机验收结果。
check(MV_CC_SetEnumValue(handle, "ExposureAuto", MV_EXPOSURE_AUTO_MODE_OFF), "lock exposure");
check(MV_CC_SetEnumValue(handle, "GainAuto", MV_GAIN_MODE_OFF), "lock gain");
check(MV_CC_SetFloatValue(handle, "ExposureTime", 2500.0), "set exposure");
check(MV_CC_SetFloatValue(handle, "Gain", 8.0), "set gain");
check(MV_CC_SetEnumValue(handle, "BalanceWhiteAuto", MV_BALANCEWHITE_AUTO_OFF), "lock white balance");
// 还须显式设置并回读 PixelFormat、触发模式和白平衡通道系数。

check(MV_CC_StartGrabbing(handle), "start grabbing");

constexpr int kSoftRestartAfter = 3; // 示例值；应由帧率与容许中断时长推导
int fail_count = 0;
while (running) {
    MV_FRAME_OUT raw{};
    const int result = MV_CC_GetImageBuffer(handle, &raw, 100);
    if (result != MV_OK) {
        log_sdk_error(result);
        // 这两个项目辅助函数须按当前 SDK 的错误码和触发模式实现。
        if (is_expected_trigger_wait(result)) continue;
        if (!is_recoverable_stream_error(result)) {
            report_camera_fault(result);
            break;
        }
        if (++fail_count >= kSoftRestartAfter) {
            check(MV_CC_StopGrabbing(handle), "stop before restart");
            check(MV_CC_StartGrabbing(handle), "restart grabbing");
            fail_count = 0;
        }
        continue;
    }
    fail_count = 0;
    const auto received_at = std::chrono::steady_clock::now(); // 应用取得缓冲的时刻，不是曝光时刻
    // 以下包装假定已回读为紧密排列的 BayerRG8；其他格式须按 SDK 描述转换。
    cv::Mat bayer(raw.stFrameInfo.nHeight, raw.stFrameInfo.nWidth, CV_8UC1, raw.pBufAddr);
    cv::Mat bgr;
    cv::cvtColor(bayer, bgr, cv::COLOR_BayerBG2BGR); // 假设输入是左上角为 R 的 RGGB
    check(MV_CC_FreeImageBuffer(handle, &raw), "release frame buffer");
    queue.push({std::move(bgr), received_at});
}

MV_CC_StopGrabbing(handle);
MV_CC_CloseDevice(handle);
MV_CC_DestroyHandle(handle);
```

排障时先核对四项：实际像素格式与每行占用的字节数（行步长）是否符合包装假设，BGR/RGB 顺序是否匹配，是否在开始采集后才取帧，以及触发模式是否与实际接线一致。示例中的 `select_by_serial`、错误分类和故障上报是项目辅助函数，不是厂商 API；实现时须把当前 SDK 的错误码映射到“等待触发、可恢复流错误、不可恢复错误”等类别。外触发模式下没有触发沿时，取帧超时可能是预期行为，不应计入掉线阈值。

`GetImageBuffer` 成功后，`bayer` 只是借用 SDK 内存的 `cv::Mat` 头，必须在 `FreeImageBuffer` 之前完成读取；`cvtColor` 写出的 `bgr` 拥有独立内存，因此示例先归还 SDK 缓冲，再把 `bgr` 移入队列，无需额外 `clone`。若直接把原始帧交给消费者或尝试零拷贝，就不能在消费者结束前归还缓冲。生产代码还应使用作用域守卫，确保格式检查、转换或其他操作抛异常时也能归还缓冲；设备句柄与 SDK 全局状态同样宜由 RAII（随作用域自动清理）对象管理。失败分支必须 `continue` 或退出，不能继续包装帧。示例中的 `MV_FRAME_OUT raw{}` 会把指针初始化为空，而不是“野指针”；若把结构体放在循环外且不清零，才可能误用上一次成功留下的地址。所有配置调用也应检查返回值并回读关键参数，避免节点名不支持时静默沿用默认值。

#figure(
  image("images/app-camera-grab-pipeline.png", width: 100%),
  caption: [从 SDK 缓冲到 `cv::Mat` 的示意流程（脚本 `camera_grab_pipeline.py`）。绿色表示把借用的 SDK 缓冲转换为自有图像、归还缓冲并清零连续失败计数；红色表示先分类错误，等待触发不计入故障，只有可恢复流错误达到阈值后才升级恢复。阈值和恢复层级需要结合 SDK 错误码与实测故障设置，图中流程不是所有相机的统一实现。],
)

==== 取流异常恢复：按错误类型分级

设备全生命周期内可能出现取帧超时、传输错误、连接断开或参数失效，驱动需要在不中断进程的前提下报告并尝试恢复。不过，外触发尚未到来、超时时间设得过短和真正掉线可能返回不同错误；恢复动作之前应先分类 SDK 错误码，并记录连续失败次数、持续时间和最近一次成功帧时刻。

一种可实现的分级策略是：单次可恢复超时只丢弃本轮；在给定时间窗内连续失败达到阈值后，先停止并重启采集；仍失败时关闭句柄，按序列号重新枚举设备、打开并重新应用全部参数。阈值 $N$ 应由帧率和允许的中断时间推导，例如“连续丢失 50 ms”比固定帧数更容易跨帧率复用。`libusb_reset_device` 只能作为特定设备上验证过的附加手段：它可能需要额外权限，也可能影响同一 USB 拓扑上的其他设备；USB 的 `vid:pid`（厂商与产品标识）不能区分多台同型号相机。恢复反复失败时，驱动应进入明确的故障状态、按退避间隔重试并通知上层停止使用旧图像，而不是把故障原因直接判定为线缆或相机损坏。

恢复成功后还要回读曝光、增益、像素格式和触发模式，并检查新时间戳没有沿用断线前的错误偏移。无法取得的帧不能由旧帧冒充；上层应根据帧龄进入降级或禁止输出状态。丢弃无效帧、记录错误并等待后续有效数据的原则，与「实战技术篇」通信协议设定一章的坏帧处理相似，但相机重连还多了设备状态恢复这一步。

==== 两种常见组织方式：ROS 2 节点与独立进程

相机驱动可以作为 ROS 2 节点，也可以放进不依赖 ROS 的独立进程。它们不是仅有的两种方案，但足以说明接口、线程和故障隔离方面的主要取舍。整套系统的编排见「自瞄算法设计」一章，这里只讨论图像入口。

在 *ROS 2 节点式*方案中，相机组件可通过 `image_transport` 发布图像，并用参数系统管理曝光、增益等配置。只有驱动实现参数回调和合法性检查后，这些参数才能安全地运行时更新。节点组合（composition）允许相机与识别组件位于同一进程；若消息类型、发布订阅方式和所用 ROS 2 版本满足进程内通信条件，可以避免中间件序列化并减少拷贝，但“组合”本身不保证零拷贝。从厂商 SDK 缓冲去马赛克到 `cv::Mat`，再填入 `sensor_msgs::msg::Image`，通常仍有各自的内存所有权边界。若要继续减少拷贝，需要核对类型适配、借用消息、ROS 中间件（RMW）实现和订阅者是否共同支持，并保证 SDK 缓冲在最后一个使用者结束前不被归还。ROS 2 的录包、可视化和生命周期工具则便于联调与状态管理。

在*独立进程*方案中，可以用抽象基类统一接口，例如 `CameraBase::read(cv::Mat&, timestamp&)`，再由工厂根据配置选择具体厂商实现。进程内需自行实现线程、队列、参数更新、监控和退出顺序；好处是依赖较少，进程故障边界也更清楚。掉线恢复的能力取决于驱动实现，与是否使用 ROS 2 没有必然关系。

对于允许丢帧、目标是降低在线控制延迟的识别链，可以使用容量为 1 的覆盖式队列：生产者写入新帧时替换尚未处理的旧帧，消费者每次取得当时最新的帧。这样不会因推理较慢而让排队延迟持续增长，同时要统计覆盖次数和帧龄，便于发现算力不足。队列中的图像必须拥有像素内存，或持有能延长 SDK 缓冲生命周期的对象；只传递指向已归还缓冲的 `cv::Mat` 头会产生悬空引用。该策略不适用于必须逐帧处理的录制、标定或某些时序算法；这些任务应使用独立队列或降低输入帧率，而不是静默丢帧。

==== 多相机与连接方式

RM 视觉常见 *USB 3.x* 与 *GigE Vision* 工业相机。5 Gbit/s 档 USB 链路布线较直接，但可用吞吐、稳定长度和延迟取决于相机、主控、控制器、线缆与转接链。千兆以太网支持更长的标准网线和交换网络，便于多机布置，但多相机共享链路时必须核算总带宽、包开销与交换机缓存，并配置包长和包间隔。Camera Link 等其他接口也可用；选择应以平台接口、距离、带宽、触发与驱动支持为准，而不是仅凭接口名称判断延迟。

多相机软件可以复用同一取流类，但还要处理共享带宽、CPU 亲和性、时间同步、内外参以及设备身份。若两幅图要做立体匹配或同一时刻的几何融合，需要把曝光偏差限制在误差预算内；共同硬触发是一种方法，部分 GigE 设备也支持基于精确时间协议（PTP）的设备时钟同步与计划采集，实际精度都要测量。若各相机独立承担不同任务，则未必需要同时曝光。多台同型号设备的枚举顺序可能变化，应优先按唯一序列号绑定逻辑角色；`vid:pid` 只能标识型号，不能区分同型号个体。设备没有可靠序列号时，可记录物理 USB 端口路径等稳定属性。串口侧的 udev 命名方法见串口模块一章。

机械固定也应纳入验收。优先使用相机规定的锁紧接头，并在机架侧做应力释放，避免线缆重量和振动直接作用于插座；同时核对线长、屏蔽、弯折半径和额定速率。若使用打印夹具或可移除胶固定，应确认不遮挡散热、不向接口施加偏载，并保留检修方式。连接松动与取流异常可以通过轻摇线缆时的错误计数和链路日志关联验证，不应把所有间歇掉帧都归因于接头。

=== 取图侧：取流帧率与推理帧率解耦

相机稳定出图后，还要处理采集速率与识别吞吐不一致的问题。这里关注取图侧的排队延迟；推理引擎与模型优化见后续识别章节。

例如，相机输出 150 fps，而一次推理只能达到 30～60 fps；这些数字只是用来说明生产者快于消费者。对于容许跳帧、追求低延迟的在线识别，应把*取流线程与推理线程解耦*，使用覆盖式最新帧槽或有界队列，并在推理空闲时取得当前最新帧。这样会丢弃中间帧，但不会让队列无限增长。异步推理可以提高吞吐，却也可能因批处理或过多在途请求增加延迟，因此要限制队列和在途数量，并同时记录输入帧龄、处理耗时、覆盖数与输出频率。需要完整帧序列的录制或时序算法应走另一条队列。

每帧还应保留不同阶段的时间：`sample_time` 表示曝光中点或经标定估计的采样时刻，`received_time` 表示应用线程取得 SDK 帧缓冲的时刻，`processed_time` 表示推理完成。前面示例在取流线程读取的是 `received_time`，不能未经延迟标定就改名为拍摄时刻。跟踪器应使用语义明确、已映射到共同时间域的 `sample_time` 推进运动模型，并可用后两者计算链路延迟。时间误差造成的角度误差一阶近似为角速度乘时间误差，可据此设定同步精度要求。OpenVINO、TensorRT 等推理后端的选择见「装甲板检测与识别」一章；本节只约定取图接口和时间字段。

=== 如何验收相机配置

相机、镜头和参数装配完成后，应先单独记录成像与时序指标，再把识别器接入。下列检查给出方法，具体合格阈值要由目标距离、速度、算法容差和控制误差预算预先确定：

- *饱和与暗部信噪比*：在预期最近、最远距离和照明条件下，分别统计灯条 ROI 各通道达到最大码值的比例，以及数字 ROI 的均值、噪声和对比度。逐级降低曝光或增益，观察饱和与暗部指标如何变化；仅凭 R、B 分不开不能区分饱和、白平衡和 Bayer 映射问题。
- *运动模糊与卷帘形变*：按预期最大相对角速度移动目标或转动相机，测量灯条边缘宽度、角点重复性和轮廓倾斜。公式 $"blur" = (v \/ d) f t_"exp"$ 只作一阶预测，2 px 也只是前图的演示阈值；应用应使用识别与 PnP 能容忍的实测上限。
- *颜色通道*：在像素未饱和时拍摄已知红、绿、蓝色块和两种装甲灯光，检查通道均值与分类输出。红蓝完全互换时优先检查 Bayer 相位和 BGR/RGB 顺序；偏色还可能来自白平衡、光源频谱或颜色矩阵。
- *帧率与丢帧*：同时记录设备帧号、设备时间戳、主机收帧时间、SDK 错误码和队列覆盖数，统计间隔分布及丢帧率，而不只看平均 fps。异常发生后再用日志区分触发缺失、链路、主机负载或设备重连。
- *时间戳语义与同步*：验证设备时间戳在回绕和重连后的处理，检查映射后的 `sample_time` 单调且与触发或闪光事件相符；再测量它相对 IMU 时钟的偏移和抖动。单调性通过并不等于曝光时刻准确。
- *数字分类输入*：保存不同距离、姿态和照明下的透视矫正图，统计分类器指标和拒识样本。结果不佳时分别检查曝光、对焦、运动模糊、角点裁剪与模型，不能预先归因于曝光或分类器。
- *配置恢复*：冷启动并模拟一次受控断连，回读曝光、增益、白平衡、像素格式和触发模式，确认恢复后配置、时间字段和设备角色均符合预期。

这些检查用于在联调前隔离相机侧问题。某项未达到预先定义的阈值时，应先保存原始帧、配置和时序日志，判断它是否足以影响下游指标，再修改对应环节并复测。清单通过只说明相机在已测试距离、速度、照明和链路条件下满足这些阈值，不证明识别、跟踪或整车系统已经正确。

==== 本章小结

本章从任务条件出发比较消费级 USB 摄像头与工业相机，并把选型拆成快门时序、传感器动态范围、靶面和镜头。参数部分用运动模糊与带宽估算说明如何联合设置曝光、增益和帧率，也讨论了 Bayer 去马赛克与白平衡锁定。随后区分触发方式、设备时间戳和主机收帧时间，给出从 SDK 缓冲转换到 `cv::Mat` 的控制流，以及按错误类型分级恢复的方法。

ROS 2 组件和独立进程都可以承载相机驱动；覆盖式最新帧队列适用于容许丢帧的低延迟链路，但录制和逐帧算法需要独立策略。完成本章清单后，应保留原始帧、配置、帧号及各阶段时间戳，作为后续识别与时序问题的对照证据。下一章将在这些已记录输入上实现装甲板检测与识别。
