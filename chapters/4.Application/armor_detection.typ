=== 从"能识别"到"能上场"：识别器的工程账

上一章的相机已经稳定地吐出带正确时间戳的低曝光图像——画面近乎全黑，只有灯条自发光。现在轮到在这些图像里把装甲板真正找出来。

好消息是，你并非从零开始。基础篇 OpenCV 一节的结尾，你已经在真实比赛画面里把装甲板找了出来：灰度二值化、找灯条候选、几何配对、MLP 分类，四级级联一气呵成。那套代码是*教学最小实现*——它把每一级的判据都讲透了，跑通了从一帧图像到一块装甲板的完整逻辑。但把它直接搬上车，会在赛场上死得很惨。

差距不在算法，在工程。教学实现假设了太多"恰好"：恰好这一帧曝光正常，恰好目标不残血，恰好画面里只有一台车，恰好参数不用改。真实赛场没有一个"恰好"成立：曝光在移动对抗里不断漂移，灯条被打灭一半还在闪，视野里三四台车的装甲板同时晃动，换个场馆连灯带亮度都不一样。一个能上场的识别器，要在这些全都不成立的时候依然稳定输出，还要跑得够快、调得动参、测得出对错、接得上相机和跟踪器。这一章就是把那套四级级联，补齐成一套*上场级识别器*——补的不是新算法，是模块化、参数化、鲁棒性、可测性和接口。

本章拿两套开源实现当参照，全章统一以*ROS2 那套*和*无 ROS 那套*相称。前者是社区里流传最广的 ROS2 识别器，把识别器拆成 `Detector`、`NumberClassifier`、`PnPSolver` 三个模块，参数全部走 ROS 参数系统，是"模块化 + 配置化"的教科书范本。后者是一支国赛队伍 25 赛季的完整框架，反其道而行——*不依赖 ROS*，用普通 C++ 类加 `std::thread` 搭起整套系统（相机只能被一个程序打开，一台步兵却要同时跑自瞄和打符，于是"一个 main、多个功能组"成了自然的选择）。同一个识别任务，两种工程答案——对照着看，比只看一种学得多。两套的出处与许可证在本章末尾一并交代。

=== 把识别器拆成模块

教学实现里，二值化、找灯条、配对、分类挤在一个函数里顺次调用。上场级的第一步，是把它们切成职责单一、可以独立替换和测试的模块。识别器的顶层调用因此干净得像一份目录：

```cpp
std::vector<Armor> Detector::detect(const cv::Mat & input) {
  binary_img = preprocessImage(input);      // 预处理：灰度 + 二值化
  lights_ = findLights(input, binary_img);  // 找灯条候选（几何 + 颜色投票）
  armors_ = matchLights(lights_);           // 配对成装甲板候选
  if (!armors_.empty()) {
    classifier->extractNumbers(input, armors_);  // 透视矫正取数字图案
    classifier->classify(armors_);               // MLP 分类，严出
  }
  return armors_;
}
```

每一级的*判据原理*在基础篇已经讲透，这里不重复——本章关心的是这种拆法买来了什么。三条好处：其一，*可替换*。分类器是独立的 `NumberClassifier` 类，想把它从 MLP 换成别的网络，只动这一个类；想把整条传统级联换成神经网络，`detect` 的接口不变，下游全无感知（这正是下一节两条路线能共存的前提）。其二，*可测*。`PnPSolver` 只依赖相机内参和四个像素点，不依赖任何图像，可以脱离相机单独喂点验证；识别链路可以离线喂录像回归。其三，*边界清晰*。装甲板从"两根灯条的像素几何"变成"相机系下的三维位姿"这一步，被单独关进 `PnPSolver`——它调用 `cv::solvePnP` 的 `IPPE` 方法（四点共面时的解析解，几何原理见理论篇「相机模型」一章），把 `Detector` 交出的四个角点解算成位姿。识别器只管在像素里找点，PnP 只管把点变成位姿，各守各的。

模块拆好，识别器怎么接进系统？两套参照给了两种答案。ROS2 那套把识别器包成一个节点：订阅相机的 `/image_raw` 和 `/camera_info`，识别加解算后，把装甲板位姿发布到 `/detector/armors`，跟踪器订阅这个话题——模块间靠话题名和消息类型解耦，谁也不认识谁。无 ROS 那套把识别器就当一个普通类，在主循环里被相机线程喂图、被下位机线程喂云台姿态，返回的装甲板直接传给预测器——模块间靠函数调用和线程安全队列解耦。不管哪种，识别器的*对外契约*是同一句话：*进去一帧图像加相机内参，出来一组带位姿的装甲板*。上游是相机（上一章），下游是跟踪器（下一章），这一章就是打磨中间这个盒子。

=== 参数活在配置里，不在代码里

教学实现里，`0.1`、`0.4`、`160` 这些数字是硬编码的字面量。上场级识别器的铁律是：*凡是要调的参数，都不许写死在代码里*。原因很实在——每台车的相机曝光不同，每个场馆的灯光不同，红蓝方切换要改识别颜色，这些都得在不重新编译的情况下现场改。

ROS2 那套把每个参数都声明成 ROS 参数，代码里只写默认值：

```cpp
int binary_thres = declare_parameter("binary_thres", 160);
Detector::LightParams l = {
  .min_ratio = declare_parameter("light.min_ratio", 0.1),
  .max_ratio = declare_parameter("light.max_ratio", 0.4),
  .max_angle = declare_parameter("light.max_angle", 40.0)};
// armor.min_light_ratio 默认 0.7，center_distance 小板 0.8~3.2 / 大板 3.2~5.5……
double classifier_threshold = declare_parameter("classifier_threshold", 0.7);
```

真正上车时，一个 YAML 文件覆盖这些默认值。有意思的是对照实车 YAML 和代码默认值——它们不一样：

#figure(
  table(
    columns: (auto, auto, auto, auto),
    align: (left, center, center, left),
    inset: 7pt,
    stroke: (x, y) => (
      top: if y == 0 or y == 1 { 1pt } else { 0pt },
      bottom: if y == 6 { 1pt } else { 0pt },
    ),
    table.header([*参数*], [*代码默认*], [*实车 YAML*], [*管的事*]),
    [`binary_thres`], [160], [*80*], [二值化阈值：多亮才算发光体],
    [`light.min_ratio`], [0.1], [0.1], [灯条最小宽长比],
    [`armor.min_light_ratio`], [0.7], [*0.8*], [配对两灯条的最小等长程度],
    [`classifier_threshold`], [0.7], [*0.8*], [分类器置信度门槛],
    [`detect_color`], [RED], [0 (RED)], [打哪种颜色的灯条],
    [`ignore_classes`], [negative], [negative], [哪些类别识别到也丢弃],
  ),
  caption: [识别器参数：代码默认值 vs 一支实车队伍的现场 YAML（加粗为被覆盖的三项）。读表规则：默认值是"社区通用的安全起点"，YAML 是"这支队伍在自己的相机和曝光下重整定的结果"——二值化阈值从 160 压到 80、配对与分类门槛都从 0.7 提到 0.8，是一次典型的"宽进变严、门槛抬高"整定。参数没有绝对正确值，只有"配这套成像条件"的值。],
)

`binary_thres` 从 160 到 80 的差距最值得深究，因为它直接暴露了"判据永远和成像条件绑定"这条铁律。把二值化阈值从低到高扫一遍，看看在一帧画面上能筛出多少候选：

#figure(
  image("images/app-armor-binary-sweep.png", width: 100%),
  caption: [二值化阈值扫描（脚本 `armor_binary_thres_sweep.py`，跑在一帧*普通曝光*的观赛视频上，与基础篇同源）。读图规则：灰色虚线是二值化后的原始白斑总数，蓝线是通过几何判据的灯条候选，橙线是通过配对判据的装甲板对。阈值太低（左侧），背景纹理、反光、大屏统统变成白斑涌进来（阈值 80 时 104 个原始白斑，160 时只剩 21 个）；阈值太高（右侧 220 以上），连真灯条都被切断、候选数崩塌。两条竖线标出代码默认 160 与某队实车 YAML 的 80——注意它们分处曲线两侧。],
)

为什么实车敢把阈值压到 80，而通用默认要保守到 160？答案藏在图注那四个字：*普通曝光*。这张图跑在观赛视频上，画面里地板、大屏、观众席都有正常亮度，低阈值自然把它们全放进来。而实车自瞄相机用 1～4 毫秒的极低曝光拍摄，画面里除了自发光的灯条几乎一片漆黑——在那个成像域里，背景根本亮不过 80，阈值压低不但不会引入杂斑，反而能把残血变暗的灯条也保住。*同一个阈值，在两个成像域里是两种含义*：160 是"防背景泛滥"的通用保险丝，80 是"极低曝光下尽量多保灯条"的实车选择。所以别问"哪个阈值对"，要问"这个阈值配的是哪种曝光"。这也正是参数必须可配置的根本原因：识别算法的每一个数字，都是对一种特定成像条件的假设，换了条件就得跟着换。改参数的手段值得说准，因为它直接决定你在赛场边能多快拧完一轮。ROS 那套：节点每帧重读 `binary_thres`、`detect_color`、`classifier_threshold` 这三个参数，所以它们能用 `ros2 param set` 在*运行时*改、下一帧立刻生效，一边看画面一边拧就是靠这条路；而灯条长宽比、配对中心距这些几何判据参数是构造检测器时一次性固化的，改了必须重启节点。要注意 YAML 只在节点启动时读入一次——运行中改文件是不会生效的，别对着没反应的画面怀疑人生。无 ROS 那套参数同样写在 YAML 里、启动时读入，改完重启进程即可，不用重新编译。

=== 传统级联还是神经网络：两条路线的取舍

到这里，识别器内部还是一条传统的几何级联。但基础篇结尾那句"不少强队已改用神经网络直接回归灯条角点"，在应用篇必须兑现成工程决策：传统和 CNN 两条路线，什么时候用哪个？

先说清楚各自的账，*原理不重讲*（CNN、YOLO、四点关键点回归、ONNX 的来龙去脉都在理论篇「深度学习和神经网络」一章，包括为什么关键点网络能把整条链路压进一个函数）。这里只算工程账：

*传统级联*便宜一档——整条级联在 CPU 上是亚毫秒到毫秒级（约 1～3 ms，随分辨率和候选数量浮动），且*只吃 CPU*，不占 GPU/NPU 预算，逻辑透明到每一步都能打印中间结果。真正省下的与其说是那几毫秒，不如说是一整块加速器和它的部署链。它的软肋在基础篇已经眼见为实：二值化、找轮廓、拟合、配对，每一环都是一个阈值，每个阈值都是一个失效面。曝光变了、灯条残了、数字发光笔画混进来了，都可能在某一环翻车。

*CNN 四点路线*把整条链路换成一个网络：一帧图进去，直接吐出每块装甲板的四个角点和类别。它对半灭灯条、部分遮挡、曝光波动的鲁棒性来自数据——训练集里见过的糟糕情况，它就扛得住。代价一样明白：上万张四点标注的数据、训练算力，以及推理端实打实的算力预算。据公开分享的实战数据，量级大致如此：改造 YOLO 系的四点网络，装甲板 5 米内稳定检出、7 米仍可用，数字 6 米内稳定，单帧推理约 5.5～6 毫秒。这类数字随相机、镜头焦距和板卡而变，只能当量级参考，不能当验收指标——你的车必须自己测。速度这一维上，无 ROS 那套三个赛季的演进最能说明问题：23 赛季纯 Python 实现导航时只有 60 FPS，24 赛季用 C++ 重写做到约 100 FPS，25 赛季换上四点网络识别——路线之争的背后，一直是帧率和鲁棒性的权衡。

有意思的是，成熟框架的答案往往不是"二选一"，而是"都要"。无 ROS 那套 25 赛季以四点网络为主力，却仍保留了一套完整的传统检测器——一来当对照和后备，二来它还有个巧用：先用网络框出装甲板大致位置，再在那个小 ROI 里用传统的二值化找轮廓，把角点精修到灯条边缘。网络负责"在哪"，传统负责"精确到哪个像素"，各取所长。

不管走哪条路，*两条路线在同一个接口上汇合*：

#figure(
  image("images/app-armor-detector-pipeline.png", width: 100%),
  caption: [上场级识别器的两条工程路线与统一接口（示意图，脚本 `armor_detector_pipeline.py`）。读图规则：无论走上方的传统级联（CPU、亚毫秒～毫秒级）还是下方的 CNN 四点回归（NPU/核显、约 5～6 毫秒），识别器最终都交出同一样东西——装甲板的*四个像素角点*，进 PnP 解出相机系位姿，送给下一章的跟踪器。顶栏强调参数活在配置文件里、随成像域重整定；底栏是贯穿两条路线的鲁棒性工程。正因为出口是同一个"四点 + 位姿"契约，换路线才不惊动下游。],
)

这张接口图是本章的骨架：识别器对下游的承诺，从头到尾是"四个点 + 一个位姿"。传统级联从灯条角点拿这四个点，四点网络直接回归这四个点——出口相同，所以两条路线可以随时替换、甚至并存，跟踪器一无所知。

顺带厘清一个新手常混的尺寸问题，它恰好卡在识别和 PnP 的接口上。装甲板有三套"尺寸"：塑料模块的*物理外形*（大装甲约 235 mm 宽、小装甲约 140 mm 宽，随赛季制作规范可能微调，以当季手册为准）；PnP 拟合用的*灯条几何*（社区基线小板取灯条间距 135 mm、大板 225 mm，灯条长约 55 mm）；以及分类器看的*数字图案*。PnP 要的四个点是两根灯条的角点，不是塑料板的外沿，所以喂给 `solvePnP` 的三维模型点用的是 135/225，而不是外形的 140/235。把外形尺寸填进 PnP，解算出的距离会系统性偏大——这是一个隐蔽但要命的接口错误。（不同队伍的取值略有出入，无 ROS 那套灯条长取 56 mm、大板取 230 mm，都在测量误差和标注习惯的范围内，不必较真到毫米。）

=== 数字分类器：训练、迭代与部署

分类器是级联的"严出"最后一关，也是识别器里唯一需要*训练*和*部署*的部件。理论篇「深度学习和神经网络」一章已经把 `mlp.onnx` 这个真实上场的分类器从里到外拆过一遍——三层全连接、8 万参数、为什么要有个 negative（负样本）类、ONNX 怎么当训练和推理之间的交接单。那一章末尾留了半句话：具体到某块板卡怎么选引擎、怎么调帧率，是应用篇的事。这一节把它补上，讲*数据怎么来、模型怎么迭代、引擎怎么上车*这半边。

先说数据。分类器要认的是透视矫正、二值化之后的干净图案（预处理替网络扛下了所有不变性，原理见理论篇），所以数据采集几乎是免费的：把识别器的几何级联在录像上跑一遍，它透视矫正出的每一张 20×28 小图，就是一个待标注样本。真正的巧思在*数据飞轮*——让识别器自己生产训练数据。无 ROS 那套的检测器里藏着两条这样的逻辑：识别到一个图案但置信度不够高时，把它存下来；识别出的装甲板类型和图案自相矛盾时（比如几何判成小装甲板、图案却认成只可能是大装甲板的"1 号"），也把它存下来。

```cpp
// 置信度不够高的疑似图案，自动存盘用于分类器迭代
if (name_ok && !confidence_ok) save(armor);
```

这些被存下来的，正是分类器*当前最没把握*的样本——上场跑一天，攒下的全是最该补进训练集的困难样本和现成的负样本（几何级联放行的误检，天生就是 negative 类的教材）。标注、重训、导出新的 ONNX，下一版分类器就在这些薄弱点上变强。*流水线自己生产自己的疫苗*，这是传统调阈值给不了的迭代能力。

再说部署。训练在云端用 PyTorch，上场在车上只跑前向——中间靠 ONNX 交货，这套分工理论篇讲过。应用篇要决定的是"用哪个引擎、怎么榨帧率"。两条路线的选择正相反：那个 8 万参数的 MLP 小到可以用 OpenCV 的 DNN 模块一行 `readNetFromONNX` 加载、CPU 上微秒级跑完，零额外依赖，是传统级联的天然搭档。而整图四点网络这种大模型，就得请专门的推理引擎。选哪个不是口味问题，原则只有一条：*引擎跟着算力平台走*。车上是 Intel NUC 这类 x86 小主机（CPU＋核显），就用 *OpenVINO*——无 ROS 那套正是这么跑的；车上是 NVIDIA *Jetson*（Orin NX/Nano 这类带 GPU 的嵌入式板卡），就用 *TensorRT*，它能把 ONNX 编译成针对这块卡深度优化的 engine 文件。这正是 ONNX 当交接单的价值：模型只导出一份，换了板卡只换接手的引擎。有一个坑要先知道：TensorRT 的 engine 文件和具体的 GPU 型号、驱动、TensorRT 版本*绑定*，必须在目标板卡上现编现用，不能像 ONNX 那样拷贝分发——所以刷机、换卡之后要记得重新编译，别指望备份的 engine 还能加载。

下面这两个部署旋钮，在两个引擎上同样成立，是把帧率榨出来的关键：

- *延迟模式 vs 吞吐模式*。OpenVINO 给推理请求设"性能提示"：单路自瞄要的是每一帧尽快出结果，用 `LATENCY`（低延迟）；如果想让识别和后续处理流水线并行、把 GPU 喂满，就用 `THROUGHPUT`（高吞吐）配*异步推理*——一帧图 `start_async` 塞进去立刻返回、不阻塞，稍后再 `wait` 取结果，取流、推理、后处理三段解耦跑。选错模式，帧率能差一截。TensorRT 上是同一件事换了名字：用 CUDA 流跑 `enqueueV3` 异步提交、事件同步取结果，单路自瞄照样把批大小压到 1 换最低延迟。
- *量化*。把 float32 权重压成 int8，模型缩到四分之一、速度翻倍，精度只掉一点点——在算力按瓦计的车载平台上，这往往不是可选项而是必需项。两个引擎都支持，也都要一小批有代表性的*校准数据*来定各层的量化尺度（OpenVINO 走 NNCF/POT，TensorRT 走 int8 calibrator）：校准集要取自实车成像域，拿网图校准出来的尺度在赛场上是不作数的。四点网络的部署基本都会走 int8。

一句话收束这条链路：Python 里炼丹，ONNX 里交货，C++ 里上场，而上场那一端的延迟/吞吐/量化，是把帧率从"跑得动"调到"跟得上"的最后一公里。

=== 鲁棒性工程：赛场不讲道理

前面几节让识别器*跑得对、调得动、接得上*，这一节让它*在赛场的恶劣条件下不崩*。鲁棒性不是一个开关，是散落在流水线各处的一堆补丁，每一个都对应一种具体的翻车方式。

*低曝光域是设计前提，不是意外*。整个识别器是为 1～4 毫秒的极低曝光设计的——在那个域里画面近乎全黑，只有灯条自发光。这不是相机没调好，而是主动选择：极低曝光天然滤掉了绝大部分背景干扰，把"找发光体"这个难题简化成"在黑幕上找亮斑"。前面二值化阈值那一课的另一面正是这个——判据和曝光是一套绑定的设计，识别器的鲁棒性有一大半是相机曝光策略给的（相机那一章的主场）。

*半血和熄灭的灯条*。装甲板被命中后灯条会闪烁、变暗，甚至半边熄灭。传统级联对此很脆——灯条一暗，二值化就可能丢掉它，或者只剩半根、宽长比失真被判据踢出。这是 CNN 四点路线的主场：训练集里喂够残血、半灭的样本，网络照样能回归出角点。识别器的颜色枚举里专门有个"熄灭"状态，就是承认"灯条不总是亮着的"。

*多目标管理与去重*。视野里同时有好几台车、十几根灯条时，两两配对会产生重叠和冲突的装甲板候选，必须去重。两套参照给了两种去重法，都值得学：ROS2 那套在配对时加一条否决规则——两根灯条之间如果夹着第三根灯条，这对作废（基础篇见过它误伤真装甲板的例子，这正是单帧几何的两难）。无 ROS 那套更细，等装甲板都配出来后再消解冲突：两块装甲板*共用一根灯条*时保留 ROI 更小的（更可能是正确的紧致配对），两块装甲板*相连*时保留置信度更高的。

```cpp
// 两块候选装甲板共用一根灯条：保留 ROI 更小的那块
if (armor1->left.id == armor2->left.id || armor1->right.id == armor2->right.id)
  (area1 < area2 ? armor2 : armor1)->duplicated = true;
```

*误识别处理*。识别错目标比识别不到更危险——认错车会让你对着工程车倾泻弹丸。第一道闸是 negative 类：softmax 里没有弃权票，垃圾输入必须有个类别接住，所以专设一个"什么都不是"的类，配合 `ignore_classes` 把它连同其它不想打的类别直接丢弃（为什么非要这个类，见理论篇 softmax 一节）。第二道闸是*类型-图案一致性*：几何判出的大小装甲板类型，和分类器认出的图案必须自洽——

```cpp
// 大装甲板不可能是"2 号/前哨站/哨兵"，小装甲板不可能是"1 号/基地"，矛盾即丢弃
if (armor.type == ArmorType::LARGE)
  mismatch = armor.number == "outpost" || armor.number == "2" || armor.number == "guard";
else if (armor.type == ArmorType::SMALL)
  mismatch = armor.number == "1" || armor.number == "base";
```

几何和分类是两个独立的证据源，让它们互相校验，能挡下不少单靠一方拦不住的误检。（一个工程提醒：不同实现对"哪些兵种是大装甲板"的口径未必完全一致，实车前务必核对当季手册，别照抄某一版硬编码。）

*最后一道翻车：交出去的位姿本身就是错的*。识别器对下游承诺的是"位姿"，但单帧 PnP 解出的*朝向角（yaw）在装甲板正对相机时是病态的*——四个共面点这时给不出可靠的旋转信息，yaw 会跳变（这类病态性「相机模型」一章分析过）。位置还准，yaw 却不可信，直接喂给跟踪器会污染整车朝向的估计。工程上两种补法：无 ROS 那套在识别端就做一次修正——以云台当前朝向为中心、在正负 70 度范围内按 1 度步长搜 140 个候选 yaw，取重投影误差最小的那个当输出（平衡步兵除外，因为它的装甲板俯仰假设不成立）；另一种更普遍的做法是识别端不硬解，把单帧的位姿连同它的不确定性交给跟踪器，用多帧融合把 yaw 磨出来——那是下一章的活。不管哪种，要记住的是：*识别器交出的不只是"在哪"，还有一个可信度参差的"朝哪"，而 yaw 这一维天生最不可信*。

=== 怎么确认识别器做对了

写完识别器，怎么知道它到底行不行？"跑起来看着挺准"不是验收标准。这一节讲两件事：怎么让识别器把自己的判断暴露出来（调试可视化），以及怎么把"准不准"变成能报出来的数字（识别率与误检率）。

*先让识别器自己说话*。上场级识别器都带一套调试输出：把二值化图、透视矫正出的数字图、画好灯条和装甲板框的结果图，连同每根灯条的宽长比/倾角、每次配对的距离/角度、单帧处理延迟，全部发出来（ROS 那套发成调试话题，用 rqt 或 rviz 看；无 ROS 那套直接开 `imshow` 窗口）。这些不是锦上添花——当识别器"莫名其妙"漏掉一块板，你要能立刻看出是二值化没保住灯条、还是配对判据把它踢了、还是分类器置信度不够。*可视化的价值，是把"它错了"变成"它在第几级、因为哪个判据错了"*。

*再把准确度变成数字*。这里必须先厘清一个常被混淆的边界：识别器的验收指标是*识别率*和*误检率*，不是*命中率*。命中率（打出去的弹丸命中的比例，后文那个 39.6% 就是它）是整个自瞄系统的指标，掺进了跟踪、预测、弹道、开火决策和云台响应，是「自瞄算法设计」一章的验收项。识别器只对"这一帧里的装甲板，找全了没有、找错了没有"负责：

- *识别率（召回）*＝ 正确检出的装甲板数 ÷ 画面里真实存在的装甲板数。漏检拉低它。
- *误检率*＝ 每帧误报的假装甲板数（或误报帧占比）。把背景当成装甲板、认错兵种、错误配对，都算误检。

怎么测？靠*离线回放带标注的录像*。这就是无 ROS 那套"类 rosbag 录制"的用处——它把图像、时间戳、云台四元数一起录下来，赛后能原样重放，"赛前排查、赛后重现"。录一段有代表性的对抗视频（覆盖远近、残血、多目标、曝光变化），逐帧标出真实装甲板，再让识别器在同一段视频上跑，逐帧比对，识别率和误检率就都出来了；改一版参数或换一版模型，在同一段视频上重跑，改进是涨是跌一目了然。有了这套离线回归，绝大多数识别 bug 在上车之前就该死掉了——这正是基础篇「软件工程基础」一章讲的可测性，在识别器上的落地。

值得盯的翻车模式有三类，测的时候重点看：*漏检*（曝光漂移、目标太远太小、残血灯条丢失）拉低识别率；*误配对与背景误检*（杂光配成装甲板）抬高误检率；最隐蔽的是*认错车*——装甲板找对了、位姿也准，兵种或颜色却判错，数字上可能只是一次误检，后果却是把炮口对准了错误目标。测识别率时，别只统计"检出没检出"，要连"检出的兵种和颜色对不对"一起统计。

==== 本章小结

这一章把基础篇那套教学级的四级级联，补齐成了一套能上场的识别器。补的不是算法：算法原理（级联判据、PnP、CNN）分别在基础篇和理论篇讲过，本章只调用不重推。补的是工程——把识别器拆成可替换、可测的模块（`Detector`/`Classifier`/`PnPSolver`），把该调的参数从代码里挪进配置文件并讲清"参数配的是成像域"，在传统级联和 CNN 四点两条路线间按帧率和鲁棒性权衡取舍（但让它们汇进同一个"四点 + 位姿"接口），用数据飞轮迭代分类器、按板卡选定推理引擎（x86 走 OpenVINO、Jetson 走 TensorRT）再用延迟/吞吐/量化把它部署上车，再用去重、一致性校验、negative 类、yaw 修正一层层堵住赛场的翻车方式，最后靠离线回放把识别率和误检率变成能报的数字。

识别器交出的是一组带位姿的装甲板——但那还只是*一帧*的答案，而且带着一个天生不可信的 yaw。单帧的假候选骗得过一帧，骗不过连续几十帧；单帧解不出的 yaw，多帧融合能磨出来。把一帧帧的识别结果在时间维度上串成对一台车稳定的估计与预测，是下一章「装甲板的跟踪」的事。

（本章的 ROS2 参照取自社区广泛使用的 rm_auto_aim `armor_detector`，无 ROS 参照取自同济 SuperPower 战队 2025 赛季公开框架，两者均为 MIT 许可证；文中代码为讲解目的重写，参数取值属"某赛季某队"经验值，规则类数字随赛季变动、以当季官方手册为准。）
