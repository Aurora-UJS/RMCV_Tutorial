=== 从单帧示例到实车识别器

上一章已经让相机稳定输出带有正确时间戳的低曝光图像：画面大部分区域很暗，自发光的灯条相对突出。接下来要在这些图像中找出装甲板，并把结果交给后续的跟踪器。

这里并不是从零开始。基础篇 OpenCV 一节已经用真实比赛画面走通了灰度二值化、灯条候选筛选、几何配对和 MLP 分类这四级流程。那套代码是*教学最小实现*：重点是说明每一级判据，并展示从一帧图像得到装甲板候选的完整过程。若直接把它搬到实车上，曝光变化、灯条损坏、多目标和参数差异会很快暴露出它没有处理的边界情况。

差距主要在工程条件。教学实现默认曝光合适、灯条完整、目标数量较少，而且参数已经调好；实车却会遇到曝光变化、灯条变暗或局部熄灭、多个目标同时进入视野，以及场馆照明和相机配置变化。识别器不仅要尽量维持稳定输出，还要满足延迟预算，允许现场调参，能够离线评测，并与相机和跟踪器通过明确接口连接。本章沿用前面的四级流程，看看怎样拆分模块、把参数移出代码、处理实车异常输入，并留下便于离线测试的接口，使它接近一套可部署的识别器。

本章以两套开源实现作参照，下文分别称为*ROS2 实现*和*无 ROS 实现*。前者把识别器拆成 `Detector`、`NumberClassifier`、`PnPSolver` 三个模块，并通过 ROS 参数系统管理配置，适合观察模块化与配置化怎样落到代码中。后者来自一支队伍公开的 2025 赛季框架：它不依赖 ROS，而是用普通 C++ 类和 `std::thread` 组织系统。该项目由主程序打开相机并向各功能组分发图像，使自瞄和能量机关识别无须各自打开同一设备。两种实现面对的是同一识别任务，对照它们可以看清框架选择如何影响接口、参数和线程组织。出处统一列在本章末尾。

=== 把识别器拆成模块

教学实现把二值化、灯条筛选、配对和分类集中在一个函数中顺次调用。工程化时可以按职责拆分，使各部分能够独立替换和测试。拆分后，顶层调用只保留流程本身：

```cpp
std::vector<Armor> Detector::detect(const cv::Mat & input) {
  binary_img = preprocessImage(input);      // 预处理：灰度 + 二值化
  lights_ = findLights(input, binary_img);  // 找灯条候选（几何 + 颜色投票）
  armors_ = matchLights(lights_);           // 配对成装甲板候选
  if (!armors_.empty()) {
    classifier->extractNumbers(input, armors_);  // 透视矫正取数字图案
    classifier->classify(armors_);               // MLP 分类并过滤低置信度结果
  }
  return armors_;
}
```

每一级的*判据原理*已在基础篇说明，这里关注拆分后的工程收益。

第一，*可替换*。分类器是独立的 `NumberClassifier` 类，将 MLP 换成其他模型时主要修改这一模块。只要检测阶段（源码中的 `detect`）的输出契约保持不变，也可以把整条传统级联换成神经网络，而不必同步改动跟踪器。

第二，*可测试*。`PnPSolver` 只依赖相机内参、畸变参数和四个像素点，可以脱离实时相机，用预先构造的数据单独验证；完整识别链路则可以离线回放录像。

第三，*边界明确*。`Detector` 在像素坐标中找点，位姿解算器再把四个点转换为相机坐标系下的位姿。参照实现调用 `cv::solvePnP` 的 `SOLVEPNP_IPPE` 方法，它专门处理共面目标，相关几何见理论篇「相机模型」一章。这样，像素检测与位姿解算可以分别修改和检查。

模块拆分后，还要确定它与系统其余部分怎样连接。ROS2 实现把识别器封装为节点：订阅相机的 `/image_raw` 和 `/camera_info`，完成识别与解算后，将装甲板类别与位姿发布到 `/detector/armors`，跟踪器再订阅该话题。模块通过话题名和消息类型解耦。无 ROS 实现把识别器作为普通 C++ 类，由相机线程传入图像、下位机通信线程传入云台姿态，再通过函数调用或线程安全队列把结果交给预测器。两种结构的*对外契约*相同：输入一帧图像及其相机参数，输出一组带类别与位姿的装甲板。上一章负责稳定取图，下一章负责连续跟踪，本章讨论的正是两者之间的识别模块。

=== 参数活在配置里，不在代码里

教学实现把 `0.1`、`0.4`、`160` 等数值直接写在代码中。进入实车部署后，凡是需要随相机、场馆或比赛配置调整的参数，都应移入配置系统。不同车辆的曝光和镜头不同，场馆照明也会变化，红蓝方切换还要改变识别颜色；这些调整不应要求重新编译程序。

ROS2 实现把可配置项声明为 ROS 参数，代码中保留默认值：

```cpp
int binary_thres = declare_parameter("binary_thres", 160);
Detector::LightParams l = {
  .min_ratio = declare_parameter("light.min_ratio", 0.1),
  .max_ratio = declare_parameter("light.max_ratio", 0.4),
  .max_angle = declare_parameter("light.max_angle", 40.0)};
// armor.min_light_ratio 默认 0.7，center_distance 小板 0.8~3.2 / 大板 3.2~5.5……
double classifier_threshold = declare_parameter("classifier_threshold", 0.7);
```

部署时，YAML 文件可以覆盖代码中的默认值。下面将一份实车配置与源码默认值并列，差异正好说明参数需要随成像条件重新整定：

#figure(
  table(
    columns: (auto, auto, auto, auto),
    align: (left, center, center, left),
    inset: 7pt,
    stroke: (x, y) => (
      top: if y == 0 or y == 1 { 1pt } else { 0pt },
      bottom: if y == 6 { 1pt } else { 0pt },
    ),
    table.header([*参数*], [代码默认], [实车 YAML], [管的事]),
    [`binary_thres`], [160], [*80*], [二值化阈值：多亮才算发光体],
    [`light.min_ratio`], [0.1], [0.1], [灯条最小宽长比],
    [`armor.min_light_ratio`], [0.7], [*0.8*], [配对两灯条的最小等长程度],
    [`classifier_threshold`], [0.7], [*0.8*], [分类器置信度门槛],
    [`detect_color`], [RED], [0 (RED)], [检测哪一方颜色的灯条],
    [`ignore_classes`], [negative], [negative], [哪些类别识别到也丢弃],
  ),
  caption: [识别器参数：代码默认值与一份实车 YAML（加粗项被配置文件覆盖）。默认值只是源码提供的起点；YAML 记录的是该队伍针对自己的相机、曝光与数据重新整定的结果。这里二值化阈值由 160 降为 80，以保留更多较暗区域；灯条等长程度和分类置信度门槛则由 0.7 提到 0.8，用更严格的后续判据过滤候选。其他成像条件未必适用同一组数值。],
)

`binary_thres` 从 160 变为 80，最能说明判据与成像条件之间的关系。先在同一帧图像上从低到高扫描阈值，观察原始白斑、灯条候选和装甲板配对数量怎样变化：

#figure(
  image("images/app-armor-binary-sweep.png", width: 100%),
  caption: [二值化阈值扫描（脚本 `armor_binary_thres_sweep.py`，使用与基础篇同源的一帧普通曝光观赛视频）。灰色虚线是至少含 5 个轮廓点的白斑数量，蓝线是通过几何判据的灯条候选数，橙线是通过简化配对判据的装甲板候选对数。对这帧图像，阈值 80 得到 104 个白斑，阈值 160 得到 21 个；阈值超过 220 后，灯条候选也消失。两条竖线仅标示源码默认值 160 与一份实车 YAML 中的 80，不能据此断定哪一个适合其他图像。],
)

为单独观察二值化阈值的影响，脚本只执行灰度分割和基础几何判据，没有加入颜色投票、第三灯条否决、数字分类等后续步骤。因此，图中的候选数量不是完整识别器的最终输出，也不能直接换算为召回率或误检率。它能说明的是：阈值改变后，有多少轮廓能继续进入后续判据。

为什么普通曝光视频在阈值 80 时出现大量背景白斑，而实车配置仍可能采用 80？差别在成像域。这张图来自普通曝光视频，地板、屏幕和观众席都保留了明显亮度，低阈值会把许多背景区域纳入候选。实车自瞄相机常把曝光压到约 1～4 毫秒，使大部分背景变暗，而自发光灯条仍较突出；在这样的图像上，较低阈值有机会保留受损或亮度下降的灯条，是否同时引入过多杂斑则要由实车数据确认。因此，阈值 160 和 80 分别描述两套成像条件下的选择，不能脱离图像直接比较优劣。

参数怎样生效也需要说明。在本章参照的 ROS2 实现中，`binary_thres`、`detect_color` 和 `classifier_threshold` 会在处理图像时读取，因此可用 `ros2 param set` 运行时修改；灯条长宽比、配对中心距等几何参数在构造检测器时传入，修改后需要重新创建检测器或重启节点。YAML 通常只在节点启动时加载，运行中只改文件不会自动更新已运行节点。无 ROS 实现同样在启动时读取 YAML，修改配置后重启进程即可，无须重新编译。

亮度阈值与颜色判定是两步，不能混为一个参数。ROS2 参照实现先把 RGB 图像转成灰度图，用 `binary_thres` 找轮廓；轮廓通过几何筛选后，再累计轮廓内部的红、蓝通道，决定灯条颜色。过曝区域的两个通道可能同时接近饱和，颜色信息往往主要来自尚未饱和的边缘，因此调低灰度阈值不等于颜色判定一定更可靠。还要核对输入编码：该节点显式请求 `rgb8`，数组下标 0 表示红、2 表示蓝；若另一条链路交来 OpenCV 常见的 BGR 图像而仍沿用相同下标，红蓝含义会交换。阈值扫描只检查了前一阶段，颜色混淆率需要在带颜色标签的实车图像上另行统计。

=== 传统级联还是神经网络：两条路线的取舍

到这里，识别器内部仍是一条传统几何级联。基础篇还提到过用神经网络直接回归灯条角点；在应用层面，需要进一步比较传统方法与 CNN 四点回归在延迟、算力、数据，以及面对异常输入时的稳定性上各有什么取舍。

CNN、YOLO、四点关键点回归和 ONNX 的原理已在理论篇「深度学习和神经网络」一章介绍，本节只讨论部署条件。

*传统级联*可以只在 CPU 上运行，不占用 GPU/NPU 预算，也便于输出每一级的中间结果。参照实现报告的处理时间约为 1～3 ms，但实际延迟会随处理器、分辨率、候选数量和实现细节变化，应在目标设备上测量。它的主要限制也很明确：二值化、轮廓拟合和几何配对都依赖人工判据；曝光变化、灯条局部缺失或数字区域的亮斑，都可能使某一级漏检或误检。

*CNN 四点路线*用一个网络直接输出每块装甲板的四个角点和类别。若训练数据覆盖了灯条局部熄灭、遮挡和曝光变化，网络可能比固定阈值级联更好地适应这些情况；这种能力不保证延伸到训练分布之外。相应成本包括四点标注数据、训练资源和推理端算力。一份公开分享在其特定相机、镜头和板卡上自述：改造后的 YOLO 系四点网络在 5 m 内检出较稳定、7 m 仍有可用结果，数字分类在 6 m 内较稳定，单帧推理约 5.5～6 ms。本书没有保留这份分享的原始数据、输入分辨率、样本量和统计口径，“较稳定”“可用”也没有量化定义，5.5～6 ms 究竟是单次值、均值还是其他统计量同样不明。因此，这组数字只能作为未独立复现的项目自述，不能直接用作其他车辆的验收标准。无 ROS 实现公开的演进记录还给出过 2023 赛季 Python 版本约 60 FPS、2024 赛季 C++ 版本约 100 FPS，2025 赛季再换为四点网络；这组记录说明语言实现、模型和硬件需要一起评估，不能只比较算法名称。

两条路线也可以组合。无 ROS 实现的 2025 赛季版本以四点网络为主，同时保留传统检测器用于对照和备用；它还可以先由网络确定装甲板的大致区域，再在较小的 ROI 中二值化并提取轮廓，以灯条边缘细化角点。这里网络负责缩小搜索范围，传统图像处理负责局部几何细化。是否确实提高精度，仍应通过带标注数据比较细化前后的角点误差和 PnP 误差。

不管走哪条路，两条路线在同一个接口上汇合：

配套绘图脚本是 `armor_detector_pipeline.py`。

#figure(
  image("images/app-armor-detector-pipeline.png", width: 100%),
  caption: [传统级联与 CNN 四点回归共用的接口。上方路线可只用 CPU，下方通常借助核显、GPU 或 NPU；延迟只表示参照实现的量级，不代表其他硬件。两路都先输出类别和有序四点，再由 PnP 求相机坐标系下的位姿。交给跟踪器的是类别、类型与位姿，而不是待解算的像素点。],
)

这张图包含两层接口。检测前端与 PnP 之间传递"类别 + 按约定排序的四个像素点"；PnP 完成后，对外发布的装甲板消息再包含类别、类型和位姿。传统级联从灯条几何得到四点、由 MLP 得到类别，四点网络则同时回归关键点和类别。四点不是无序集合，顺序必须与 PnP 的三维模型逐点对应。例如，ROS2 参照实现依次传入左下、左上、右上、右下四个灯条端点；无 ROS 参照实现先将网络输出整理为左上、右上、右下、左下，再使用同序的三维点。两种顺序都可以成立，却不能把一套二维顺序与另一套三维顺序混用。只要类别语义、点序、坐标系、单位和消息类型都写入接口契约，两条路线便可以替换或并存，跟踪器无须依赖内部实现。

这里还要区分三套容易混淆的尺寸：塑料模块的*物理外形*（大装甲约 235 mm 宽、小装甲约 140 mm 宽，具体值随赛季制作规范变化，应查当季手册）；PnP 三维模型使用的*灯条几何*（社区基线实现中，小板宽取 135 mm、大板宽取 225 mm，灯条长约 55 mm）；以及分类器实际看到的*数字图案*。参照实现传给 PnP 的像素点位于两根灯条的几何位置，并非塑料板外沿，因此三维模型也应使用与这些像素点对应的 135/225 mm 尺寸。若像素点取自灯条而三维点却按塑料外沿建立，模型尺度不一致，会给距离带来系统性尺度偏差。不同实现的取点方式和实测尺寸可能不同，例如无 ROS 实现取灯条长 56 mm、大板宽 230 mm；关键不是照抄某个数字，而是保证二维点与三维模型描述同一组物理位置。

=== 数字分类器：训练、迭代与部署

分类器是传统级联中最后一道过滤，也是其中需要训练模型并部署推理环境的部件。理论篇「深度学习和神经网络」一章已经分析过实际使用的 `mlp.onnx`：三层全连接、约 8 万个参数、negative（负样本）类的作用，以及 ONNX 怎样连接训练框架与推理端。本节继续讨论样本来源、模型迭代和车载推理引擎。

先看数据。分类器接收的是经过透视矫正和二值化的图案，预处理已经减少了位置、尺度和亮度变化，但并不能消除所有成像差异。可以让几何级联离线处理录像，把每张 20×28 小图保存为待标注样本，从而降低采集成本。无 ROS 实现还会主动保存两类需要复查的数据：分类结果名称有效但置信度不足的图案，以及装甲板几何类型与分类结果不一致的图案。例如，几何判为小装甲板，分类结果却是规则中只应出现在大装甲板上的"1 号"。

```cpp
// 置信度不够高的疑似图案，自动存盘用于分类器迭代
if (name_ok && !confidence_ok) save(armor);
```

这些样本集中在当前模型置信度较低或不同判据互相矛盾的区域，经过人工复核后，适合补充为困难正样本或 negative 类样本。将它们标注、重新训练并导出新的 ONNX，可以有针对性地改进已观察到的薄弱场景。不过，自动保存只是在筛选待标注数据；它看不到模型置信度很高却判断错误的样本，也不会自动补齐从未进入相机视野的距离、视角和照明条件。因此还要定期随机抽样实车录像，检查样本标签与类别平衡，并用与训练、调参数据分开的固定验证集确认新版模型是否退化。

再看部署。模型可以在工作站上用 PyTorch 训练，车载程序只执行前向推理，ONNX 作为两端之间的模型交换格式。约 8 万参数的 MLP 可以直接由 OpenCV DNN 的 `readNetFromONNX` 加载，并在 CPU 上运行；具体延迟仍取决于处理器、线程和构建选项。整图四点网络计算量更大，通常会使用与硬件匹配的推理后端。Intel x86 主机及其核显可考虑 OpenVINO，无 ROS 参照实现采用的正是这一路线；NVIDIA Jetson 等平台常用 TensorRT，把 ONNX 编译为针对目标环境优化的 engine。TensorRT engine 的兼容性受 GPU 架构、TensorRT 版本和相关软件栈影响，通常应在目标设备及目标环境中构建和验证。刷机、升级运行库或更换板卡后，不应默认旧 engine 仍可加载。

无论使用哪种推理后端，都需要明确延迟、吞吐和数值精度之间的取舍：

- *延迟模式与吞吐模式*。OpenVINO 可以为推理请求设置性能提示：单路自瞄通常更关注单帧完成时间，可从 `LATENCY` 开始；若取流、推理和后处理能够流水并行，则可评估 `THROUGHPUT` 与异步推理。`start_async` 提交后不会等待结果，程序随后再调用 `wait` 或相应接口同步。TensorRT 中可用 CUDA 流和 `enqueueV3` 异步提交。批大小 1 常用于降低单路等待时间，但最终选择应同时测量端到端延迟、抖动和持续吞吐，而不能只看引擎报告的推理时间。
- *量化*。INT8 权重的单个元素占用通常是 FP32 的四分之一，但整个模型占用、内存带宽和速度提升不会固定为四倍或两倍，精度损失也可能因模型与数据而显著不同。OpenVINO 与 TensorRT 都支持 INT8 路径。训练后量化通常需要有代表性的校准数据确定量化尺度；量化感知训练则在训练中模拟量化误差。相关数据应覆盖实车相机的曝光、距离、颜色和受损灯条等场景。是否采用 INT8，要比较量化前后的角点误差、分类指标和目标硬件延迟后再决定。

因此，训练框架负责得到模型，ONNX 负责交换模型，C++ 推理端负责在目标硬件上执行；延迟模式、异步流水和量化则需要结合完整识别链路测量，而不是只根据模型文件或单次基准决定。

=== 实车异常输入：逐项补上处理

前面几节解决了模块、配置、接口和部署问题，本节继续处理实际比赛中反复出现的异常输入。要让识别器在这些条件下仍能稳定工作，要靠曝光策略、候选去重、类别校验和位姿质量控制等多处机制配合；每一处处理都应对应一个可以复现和测试的失效场景。

低曝光域是这类识别器的设计前提。约 1～4 毫秒的曝光会压低许多非发光背景，使灯条与背景的亮度差更明显，从而简化阈值分割；它同时也可能让未自发光的结构和较暗灯条丢失细节。因此，曝光时间、增益、二值化阈值和灯条损坏场景应作为一组共同整定，而不能只修改检测器参数。相机配置的具体方法见上一章。

*变暗、闪烁和局部熄灭的灯条*。装甲板被命中后，灯条可能闪烁、亮度降低或局部熄灭。传统级联可能在二值化阶段丢失较暗区域，也可能因轮廓长度和宽长比变化而拒绝候选。CNN 四点模型若在训练集中包含足够多此类样本，有机会在部分灯条缺失时仍给出角点，但效果需要在独立录像上验证。参照识别器的颜色枚举还保留了"熄灭"状态，提醒后续逻辑不要默认灯条始终完整发光。

*多目标候选去重*。视野中同时出现多台车和多根灯条时，两两配对会生成重叠或相互冲突的装甲板候选，需要额外消解。ROS2 实现在配对阶段加入否决条件：若两根灯条之间还存在第三根灯条，则拒绝这组配对。基础篇已经展示过该规则也可能误删真实装甲板，因此它是一项需要结合数据评估的启发式。无 ROS 实现在配对后处理冲突：两块候选共用一根灯条时保留 ROI 较小者；两块候选相连时保留分类置信度较高者。前者假设正确配对通常更紧凑，后者借助分类结果排序；两种假设都应在多目标录像中统计误删率。

```cpp
// 两块候选装甲板共用一根灯条：保留 ROI 更小的那块
if (armor1->left.id == armor2->left.id || armor1->right.id == armor2->right.id)
  (area1 < area2 ? armor2 : armor1)->duplicated = true;
```

*误识别处理*。漏检会造成短时无目标，错误类别却可能使系统选择不应攻击的对象，因此需要单独控制。第一种机制是 negative 类：softmax 总会把输入分到某个类别，参照实现专门提供"不属于任何有效数字"的负样本类别，并通过 `ignore_classes` 丢弃它以及当前策略不处理的类别（原理见理论篇 softmax 一节）。其他实现也可以结合置信度拒绝或开放集方法，但都需要为非目标输入设计明确的拒绝路径。第二种机制是*类型与图案一致性检查*：几何判出的大小装甲板类型，应与分类器识别出的图案符合当季规则。

```cpp
// 该实现对应赛季的映射：类型与类别不一致时丢弃
if (armor.type == ArmorType::LARGE)
  mismatch = armor.number == "outpost" || armor.number == "2" || armor.number == "guard";
else if (armor.type == ArmorType::SMALL)
  mismatch = armor.number == "1" || armor.number == "base";
```

几何类型和数字分类来自不同判据，交叉检查可以过滤一部分只在单一路径上看似合理的候选。规则会随赛季变化，不同代码版本的类别映射也可能不同；部署前必须以当季手册和本队实际装甲板为准更新测试，不能直接沿用旧版硬编码。

最后还要检查位姿本身的可信度。装甲板的四个点近似共面；当目标接近正对相机、像素噪声相对明显时，单帧 PnP 对朝向角（yaw）的约束会变弱，可能出现跳变，相关几何见理论篇「相机模型」一章。位置和朝向的误差并不会固定地一准一差，应分别通过重投影误差、距离和视角分桶评测。无 ROS 实现在识别端加入了一个特定修正：以云台当前朝向附近的正负 70 度为范围，按 1 度步长搜索约 140 个候选 yaw，选择重投影误差较小者；平衡步兵因装甲板俯仰假设不同而跳过该步骤。另一种做法是保留单帧位姿及其不确定性，在跟踪器中利用多帧观测估计整车朝向。无论采用哪条路径，都不应把单帧 yaw 当作与位置同等可靠的无误差量测。

=== 怎么确认识别器做对了

识别器能够运行，只能说明链路接通，不能说明检测质量达到要求。验证需要同时回答两件事：每个候选在哪一级被保留或拒绝，以及在固定数据集上漏检和误检各有多少。

*先检查中间结果*。参照实现会输出二值化图、透视矫正后的数字图、标出灯条和装甲板的结果图，以及灯条宽长比与倾角、配对距离与角度、单帧处理时间等调试量。ROS2 实现将它们发布为调试话题，可用 rqt 或 rviz 查看；无 ROS 实现可直接使用 `imshow`。出现漏检时，逐级观察这些结果，才能区分二值化未保留灯条、几何判据拒绝候选和分类置信度不足等不同原因。

*再报告可复现的指标*。识别器评测与整机命中率不是同一层问题。命中率还受到跟踪、运动预测、弹道、开火决策和云台响应影响，是「自瞄算法设计」一章的整机指标。识别器首先应报告这一帧中装甲板是否找全、类别是否正确，以及额外产生了多少错误候选：

- *召回率*＝正确检出的真实装甲板数 ÷ 标注中的真实装甲板总数，主要反映漏检。
- *误检指标*可选每帧误检数（FPPI）或含误检帧的比例。两者定义不同，报告时必须写明采用哪一种；背景误检、错误配对和类别错误还应分别统计。

评测可以通过*离线回放带标注的录像*完成。无 ROS 实现提供了类似 rosbag 的录制机制，把图像、时间戳和云台四元数一起保存，便于赛后按相同输入重放。数据集应覆盖不同距离、灯条受损、多目标和曝光变化，并逐帧标注真实装甲板、类别与颜色。参数或模型更新后，在同一测试集上重新运行并比较各项指标；训练或调参使用的数据应与最终测试集分开，以免得到过于乐观的结果。这正是基础篇「软件工程基础」所讲回归测试在识别器中的应用。

评测时至少分开观察三类错误：曝光变化、目标过小或灯条受损造成的*漏检*；反光和错误灯条配对造成的*背景误检*；装甲板位置正确但兵种或颜色错误的*类别错误*。最后一类错误会影响目标选择，不能只用"是否检出装甲板"这一项掩盖。按距离、视角、曝光和损坏程度分桶统计，通常比只给一个全局平均值更有助于定位问题。

==== 本章小结

本章把基础篇的四级教学流程扩展为一套可部署的识别模块。检测、数字分类和位姿求解的职责已经拆开，各部分可以分别替换与测试。

这里涉及三个组件：`Detector`、`NumberClassifier` 和 `PnPSolver`。可调参数移入配置文件，并明确与曝光和相机条件共同整定。传统级联与 CNN 四点回归对算力和数据的要求不同，面对异常输入时也各有适用条件，但都先形成"类别 + 有序四点"，再经 PnP 得到对外发布的"类别 + 位姿"。

分类器通过困难样本迭代，推理后端按目标硬件选择，并通过端到端测量决定异步模式与量化。低曝光、候选去重、negative 类、类型一致性和 yaw 质量控制分别处理具体失效场景，最终再用独立标注录像报告召回率和明确口径的误检指标。

识别器输出的仍是*单帧*结果，其中既可能包含短暂误检，yaw 的不确定性也会随视角和图像质量变化。下一章将把连续帧的装甲板观测关联起来，利用运动模型估计整车状态，并说明多帧信息能改善什么、又不能保证什么。

（本章的 ROS 2 参照取自 rm_auto_aim `armor_detector`，无 ROS 参照取自同济 SuperPower 战队 2025 赛季公开框架。文中代码为讲解目的重写，参数取值属“某赛季某队”经验值；规则类数字随赛季变动，以当季官方手册为准。）
