=== 从单帧识别到连续状态估计

上一章的识别器回答单帧问题：画面中哪些区域是装甲板，它属于哪个类别，在相机坐标系下位于哪里。自瞄还需要处理时间：图像采集、计算、云台响应和弹丸飞行都会引入延迟，因此运动目标的瞄准点通常不能只取当前量测位置。跟踪器把连续帧关联起来，估计速度和转动状态，为后续预测提供输入。

跟踪的作用也不只在预测。基础篇 OpenCV 一节已经展示过单帧识别的限制：场馆灯带、地面反光、受损后闪烁的灯条、局部熄灭的装甲板和其他车灯都可能产生候选；不同兵种的装甲板外形又很相似，类别错误不一定能从几何上直接发现。连续帧提供了额外的时间一致性约束，可以拒绝一部分短暂候选，并在少量漏检期间保留已有轨迹。本章将说明这种能力怎样落实为状态模型、数据关联、状态机和验证方法。

有些歧义来自信息本身，而不是某个阈值没有调好：单帧中的细长亮斑可能是灯条，也可能是反光；相邻目标的候选还可能在空间上接近。跟踪把上一时刻的位置、速度和整车几何引入当前判断，使不符合预测的候选更难通过关联门。它能降低短时误检和漏检的影响，却不能保证排除持续反光、错误类别或与模型一致的假目标，因此识别质量和离线评测仍然不可替代。

=== track-by-detection：识别负责找，跟踪负责串

本章参照的实现采用 track-by-detection（先检测后跟踪）：识别器逐帧给出装甲板观测，跟踪器再把这些离散观测关联为带有身份和运动状态的轨迹。识别器主要处理像素，跟踪器主要处理观测与状态，两者可以分别测试和替换。逐帧检测还便于在目标重新出现时恢复，但会重复执行计算量较大的识别步骤；是否需要 ROI、降频或其他优化，应根据目标硬件的端到端延迟决定。

其中的核心问题是*数据关联*（data association）：当前帧的某块装甲板，是否属于上一帧正在跟踪的目标。关联正确时，滤波器获得同一目标的连续量测；关联错误时，其他车辆的观测会把状态估计带偏。SORT 等多目标跟踪方法也围绕这一问题展开：先用运动模型预测轨迹在当前帧的位置，再与检测结果匹配。本章的参照实现一次只维护一个主跟踪目标，因此关联由多对多指派简化为从候选中选择与预测最接近的一块装甲板。短暂遮挡、旋转换板和目标切换仍需单独处理。

关联需要先由运动模型给出预测位置。小陀螺运动尤其说明了模型选择的重要性：若只把当前可见装甲板当作匀速平动点，换板时量测位置和朝向会发生明显跳变，模型很快失配。理论篇「卡尔曼滤波」一章已经推导过整车模型：估计车体中心的平动与整车自转，再由几何关系得到各装甲板位置。本章把该模型对应到实际跟踪代码。

=== 状态里装的不是一块板，是整台车

理论篇把一台车近似为"中心平动 + 装甲板绕中心匀速旋转"，状态包含车体中心的位置与速度、当前装甲板朝向、自转角速度和旋转半径。实际参照实现还把装甲板高度及其变化率纳入状态，并在滤波器外维护另一组半径与高度差，以处理常规四装甲板机器人两组安装尺寸可能不同的情况。其状态共九维：

$ bold(x) = (x_c, v_x, y_c, v_y, z_a, v_z, theta, omega, r)^top $

其中 $(x_c, y_c)$ 与 $(v_x, v_y)$ 是车体中心在惯性系下的水平位置与速度，$z_a$ 是*当前这块可见板*的高度、$v_z$ 是它的变化率，$theta$ 是当前板的朝向角（即代码里的 `yaw`），$omega$ 是整车自转角速度（即 `v_yaw`），$r$ 是当前板的旋转半径。

#figure(
  table(
    columns: (auto, auto, auto),
    align: (center, center, left),
    stroke: (x, y) => (
      top: if y == 0 or y == 1 { 1pt } else { 0pt },
      bottom: if y == 9 { 1pt } else { 0pt },
    ),
    inset: 7pt,
    table.header([*下标*], [分量], [含义]),
    [0], [$x_c$], [车体中心水平坐标 x（惯性系）],
    [1], [$v_x$], [中心 x 方向速度],
    [2], [$y_c$], [车体中心水平坐标 y],
    [3], [$v_y$], [中心 y 方向速度],
    [4], [$z_a$], [当前可见板的高度],
    [5], [$v_z$], [高度变化率],
    [6], [$theta$], [当前可见板的朝向角（`yaw`，连续量）],
    [7], [$omega$], [整车自转角速度（`v_yaw`）],
    [8], [$r$], [当前可见板的旋转半径],
  ),
  caption: [九维整车状态。位置与速度*交错*排列：0/1 是 $x_c$ 与 $v_x$，2/3 是 $y_c$ 与 $v_y$，其后依次为高度/变化率、朝向/角速度，最后一维为半径。下标顺序决定状态转移和观测函数怎样访问每个分量，不能按另一份文档的排列直接套用。车体中心由整车共享，$z_a\/theta\/r$ 描述当前可见装甲板，换板时需要更新。],
)

这里有两个需要与代码逐项核对的细节。第一是排列顺序：*位置和速度交错存放*，即 $(x_c, v_x, y_c, v_y, dots)$，而不是先放全部位置、再放全部速度。这样，每个"位置—速度"对都对应一个 $2 times 2$ 匀速子块，$mat(1, Delta t; 0, 1)$ 在状态转移矩阵 $bold(F)$ 的对角线上出现四次，分别用于水平两轴、高度和朝向；$r$ 单独保持不变。该开源实现的 README 仍保留另一种九维排列和更早的六维模型描述，与当前代码并不一致。涉及维数、下标和参数名时，应以实际版本的状态初始化、转移函数和观测函数相互核对，项目分析篇还会给出逐行对照。

第二是观测方程。图像端经过 PnP 和坐标变换后，得到当前可见装甲板在惯性坐标系中的位置 $(x_a, y_a, z_a)$ 与朝向 $theta$。状态描述整车，观测描述单块装甲板，两者通过几何关系连接：

$ x_a = x_c - r cos theta, quad y_a = y_c - r sin theta $

式中的 $r cos theta$ 和 $r sin theta$ 使观测函数成为非线性函数，因此 EKF 更新需要相应雅可比，推导见理论篇。本章只说明角度定义的差异：理论篇用 $theta$ 表示从中心指向装甲板的方向，而参照代码把 PnP 位姿转换得到的装甲板 `yaw` 直接放入状态，因此公式中的符号与理论篇不同。检查实现时不要只比较正负号，而要验证初始化与观测是否互为逆关系：初始化用 $x_c = x_a + r cos theta$、$y_c = y_a + r sin theta$，观测则用 $x_a = x_c - r cos theta$、$y_a = y_c - r sin theta$。$z_a$ 与 $theta$ 作为观测向量中的分量直接参与更新。

=== 状态机：识别、跟踪、短暂丢失、彻底丢失

有了状态空间，还要决定何时确认新目标、何时容忍暂时漏检，以及何时清空轨迹。单帧候选不宜立即对外声明为稳定目标，已经确认的目标也不应因一次漏检立刻丢弃。参照实现用四状态机和两个计数门限完成这层调度。

#figure(
  image("images/app-tracking-state-machine.png", width: 92%),
  caption: [跟踪器的四状态生命周期（脚本 `tracking_state_machine.py`）。`DETECTING` 要求匹配计数超过 `tracking_thres` 后才进入 `TRACKING`，用于确认新目标；`TEMP_LOST` 在漏检计数超过 `lost_thres` 前保留轨迹并继续预测，用于容忍短暂缺测。参照节点只在 `TRACKING` 与 `TEMP_LOST` 两态发布 `tracking = true`。这些状态只描述跟踪器输出是否有效，不保证下游云台采用何种控制策略。],
)

#figure(
  table(
    columns: (auto, auto, auto, auto),
    align: (left, left, center, left),
    stroke: (x, y) => (
      top: if y == 0 or y == 1 { 1pt } else { 0pt },
      bottom: if y == 6 { 1pt } else { 0pt },
    ),
    inset: 7pt,
    table.header([*当前态*], [触发事件], [转到], [工程含义]),
    [`LOST`], [首次检出（选离光心最近的板初始化）], [`DETECTING`], [初始化，但暂不发布有效跟踪],
    [`DETECTING`], [匹配计数 > `tracking_thres`（源码默认 5）], [`TRACKING`], [确认连续目标],
    [`DETECTING`], [一次未匹配], [`LOST`], [取消尚未确认的轨迹],
    [`TRACKING`], [一次未匹配], [`TEMP_LOST`], [保留状态并继续预测],
    [`TEMP_LOST`], [重新匹配], [`TRACKING`], [恢复观测更新],
    [`TEMP_LOST`], [漏检计数 > `lost_thres`], [`LOST`], [超时后清空轨迹],
  ),
  caption: [状态转移表。`DETECTING` 提供确认延迟，减少单帧候选立即成为有效轨迹的情况；`TEMP_LOST` 提供删除延迟，允许短暂缺测。`tracking_thres` 控制确认所需的匹配次数，`lost_thres` 控制允许的漏检次数，后者还由时间门限与帧间隔换算。],
)

实现状态机时，`detect_count_` 与 `lost_count_` 都应在构造或初始化轨迹时明确置零。这里核对的公开版本没有在构造函数中显式初始化这两个整型成员；直接照抄会读取未初始化值，在 C++ 中这会触发未定义行为，状态转移也就不再具有表中语义。图和表描述的是计数器正确初始化后的预期流程，而不是对该处源码初始化行为的认可。

参照实现没有把 `TEMP_LOST` 到 `LOST` 的门限固定为某个帧数，而是计算 `lost_thres = int(lost_time_thres / dt)`，其中 `dt` 是当前帧间隔；转移条件是 `lost_count > lost_thres`。源码默认 `lost_time_thres` 为 0.3 s，而仓库中的一份部署 YAML 覆盖为 1.0 s，实际容忍时间应以运行配置为准。以稳定帧率和 0.3 s 为例，125 FPS 时 `lost_thres` 约为 37，并在第 38 次连续未匹配时转入 `LOST`；200 FPS 时门限约为 60，并在第 61 次未匹配时转入。若把门限固定为 30，代码会在第 31 次未匹配时转移，从第一次未匹配到转移的名义时间约为 0.24 s 和 0.15 s。按当前 `dt` 换算只适合帧率相对稳定的情况；若帧间隔波动明显，直接累计时间戳差并与秒级门限比较会更准确。

`TEMP_LOST` 期间只执行 EKF 预测，不执行量测更新。状态沿运动模型外推，未观测方向上的不确定性通常会随时间增大；节点仍可发布预测状态，但下游应知道此时没有新观测。目标重新出现后，滤波器再按当前协方差计算增益并恢复更新，收敛速度取决于丢失时长、模型误差以及 $bold(Q)$、$bold(R)$ 的设置，不能保证一两帧恢复。`lost_time_thres` 限制纯预测持续时间，避免长期缺测后仍把外推状态当作可靠目标。

=== 一次 update：预测、关联、匹配门与换板

跟踪器的 `update()` 可以概括为四步：先预测当前状态，再从同编号候选中做关联，检查位置与朝向门限，最后处理可能的旋转换板。

```cpp
Eigen::VectorXd x_pred = ekf.predict();               // 先把状态推进到本帧
Eigen::Vector3d pos_pred = armorPosFromState(x_pred); // 由整车状态算出"当前板"的预测位置

double min_pos_diff = DBL_MAX, yaw_diff = DBL_MAX, selected_yaw = 0.0;
int same_id_count = 0;
for (const auto & a : armors) {
  if (a.number != tracked_id) continue;               // 只考虑与当前 tracked_id 同号的候选
  same_id_count++;
  double d = (pos_pred - toVec(a.pose.position)).norm();
  double yaw = unwrapYawNear(a.pose.orientation, x_pred(6));  // 纯计算，不修改历史量测
  if (d < min_pos_diff) {
    min_pos_diff = d;
    yaw_diff = std::abs(yaw - x_pred(6));
    selected_yaw = yaw;
    matched_armor = a;
  }
}

if (min_pos_diff < max_match_distance && yaw_diff < max_match_yaw_diff) {
  last_yaw_ = selected_yaw;                           // 关联确定后才提交角度历史
  Eigen::Vector4d z(px, py, pz, selected_yaw);
  target_state = ekf.update(z);
} else if (same_id_count == 1 && yaw_diff > max_match_yaw_diff) {
  handleArmorJump(matched_armor, selected_yaw);
}
```

正常更新使用一道*双阈值匹配门*：候选与预测的位置差小于 `max_match_distance`，朝向差小于 `max_match_yaw_diff` 时，才把量测交给 `ekf.update`。源码默认值分别约为 0.15 m 和 1.0 rad，但本项目分析篇核对的一份部署 YAML 将位置门限覆盖为 0.5 m，因此应区分源码默认值与实际运行参数。关联只在与 `tracked_id` 相同的 `number` 候选中进行；同一台车的装甲板通常共享数字，这能缩小搜索范围，但分类抖动或多个候选同号时仍可能错配。

没有候选通过正常匹配门时，代码还可能执行换板处理。这里有一处容易忽略的控制流：当前公开版本在调用 `handleArmorJump` 后没有把局部变量 `matched` 置为 `true`。因此，换板函数虽然已改写状态，本帧在后续状态机中仍按"未匹配"计数：`TRACKING` 会进入 `TEMP_LOST`，`DETECTING` 会回到 `LOST`。若设计意图是把已接受的换板当作本帧有效关联，就需要明确修改 `matched` 的语义，并用从两种前置状态进入换板分支的测试核对转移结果。

==== 先处理 yaw 的周期边界

匹配门和观测更新都要使用装甲板 `yaw`。从四元数提取的欧拉 yaw 通常落在 $[-pi, pi]$；连续旋转跨过区间边界时，数值会从接近 $+pi$ 跳到接近 $-pi$。若滤波器把它当作普通实数直接相减，就可能得到接近 $2 pi$ 的角度差，而真实转角其实很小。这种错误新息会显著扰动状态，影响程度取决于滤波增益和门限，不能简单断言一次更新必然发散。

参照实现通过 `shortest_angular_distance` 将回绕观测接到连续角度上。为了把“计算候选角度”和“提交历史量测”分开，可以先写成无副作用的辅助函数：

```cpp
double unwrapYawNear(const Quaternion & q, double reference_yaw) {
  double raw_yaw = getYawFromQuaternion(q);  // 落在 [-pi, pi]
  return reference_yaw + shortest_angular_distance(reference_yaw, raw_yaw);
}
```

`shortest_angular_distance(a, b)` 返回从 $a$ 到 $b$ 的带符号最短转角，范围在 $[-pi, pi]$。以预测的连续角度为参考，就能把每个回绕候选映射到离预测最近的等价角度；关联确定后，再把选中值写入 `last_yaw_`。这里有一个必要前提：预测与真实观测之间的角度误差绝对值必须小于 $pi$，而且关联不能换成其他目标；否则仅凭一个回绕角无法判断实际相差了几圈。另一种等价思路是保留回绕角，并始终将角度新息归一化到 $[-pi, pi]$。无论采用哪种表示，只要角度进入状态、观测或差分，就必须显式处理其周期性。

上游公开版本的 `orientationToYaw` 把上述计算和 `last_yaw_ = yaw` 合在一个有状态函数中。若它只对最终选中的观测调用一次，语义清楚；但该版本还在遍历同号候选、尚未确定关联对象时调用它。画面中有多个同号候选时，中间候选便可能提前改写 `last_yaw_`，使结果依赖遍历顺序。上面的伪代码因此刻意使用纯计算函数，并在关联或换板分支确定后只提交一次历史角度。若保留有状态写法，也应为多候选和跨越 $plus.minus pi$ 的组合建立回归测试。

#figure(
  image("images/app-tracking-yaw-unwrap.png", width: 100%),
  caption: [yaw 回绕与解缠示例（脚本 `tracking_yaw_unwrap.py`）。仿真目标以每秒两转匀速旋转。上图红线是限制在 $[-pi, pi]$ 的原始 yaw，蓝线是用 `shortest_angular_distance` 累加得到的连续角度。下图画的是相邻采样的角度增量，而不是完整 EKF 新息：原始 yaw 在边界处产生接近 $-2pi$ 的数值跳变，解缠后的增量保持为 $omega\/"fps"$。该图验证周期边界机制，不代表任意滤波参数下都必然发散或收敛。],
)

==== handleArmorJump：处理旋转换板

车辆旋转时，原先可见的装甲板会离开视野，相邻的同号装甲板随之出现。参照代码在正常双阈值匹配失败后，若画面中恰有一块同号装甲板且 `yaw_diff` 超过门限，便调用 `handleArmorJump`。需要注意，代码的这个分支本身没有要求 `min_pos_diff < max_match_distance`；它先按可能换板处理，再用位置差检查是否需要重置中心。因此，"单个同号候选 + 较大 yaw 差"是一项启发式条件，并不是换板已经被独立证实。

```cpp
void handleArmorJump(const Armor & new_armor, double yaw) {
  last_yaw_ = yaw;                          // 该候选已由换板分支选定，此时再提交
  target_state(6) = yaw;                    // 朝向角切到新板
  // 常规四板车才有"两组半径 / 两组高度"，换板时交替
  if (armors_num == NORMAL_4) {
    dz = target_state(4) - new_armor.pose.position.z;
    target_state(4) = new_armor.pose.position.z;  // 高度切到新板
    std::swap(target_state(8), another_r);        // 半径在两组间交换
  }
  // 若换板后推算位置与实测相差过大，则按当前观测重置中心
  if ((measuredPos - inferPos(target_state)).norm() > max_match_distance) {
    resetCenter(target_state, new_armor);
  }
  ekf.setState(target_state);
}
```

正常换板路径先更新当前装甲板的朝向 $theta$；对常规四装甲板机器人，还更新高度 $z_a$、记录两组高度差 `dz`，并用 `std::swap` 在状态半径 $r$ 与外部保存的 `another_r` 之间切换。车体中心和自转角速度 $omega$ 在这一步保持连续，这是整车模型相对逐板平动模型的主要优势。随后，代码比较状态推算的装甲板位置与当前量测；若差值超过 `max_match_distance`，则按当前观测重置中心及相关速度，所以不能笼统地说换板永远不影响中心。高速旋转下能否稳定估计 $omega$ 还受到帧率、可见时长、模糊、关联准确率和噪声设置限制，应通过已知转速或外部测量验证。

==== 半径限幅：约束弱可观状态

$r$ 只通过 $r cos theta$ 和 $r sin theta$ 间接进入装甲板位置观测，并与未知的车体中心 $(x_c, y_c)$ 耦合。当一段观测中的 $theta$ 变化很小，中心平移与半径变化可以产生相近的位置效果，滤波器便难以仅凭这些量测区分二者。这里的弱可观并不是因为 $theta approx 0$ 时对 $r$ 的导数趋近于零；事实上 $partial x_a / partial r = -cos theta approx -1$。问题在于有限视角下不同状态分量之间的相关和可辨识性。参照跟踪器因此在每帧更新后限制半径范围：

```cpp
if      (target_state(8) < 0.12) target_state(8) = 0.12;  // 旋转半径下限
else if (target_state(8) > 0.4)  target_state(8) = 0.4;   // 上限
```

$0.12 tilde.op 0.4$ m 是该实现依据其目标车型采用的经验范围，并不是由比赛规则保证的通用合法区间；机器人结构或状态定义变化后需要重新测量。硬限幅能防止半径数值继续漂离可接受范围，但也可能掩盖模型失配，并使协方差不再准确描述被截断后的状态分布。工程上应同时记录限幅触发次数：偶发触发可作为容错，频繁触发则需要检查 PnP、换板关联、$bold(Q)$、$bold(R)$ 和半径范围本身。

==== 过程噪声矩阵中的一处实现偏差

参照实现的过程噪声矩阵 $bold(Q)$ 中有一处值得核对的偏差。若 `yaw` 与角速度采用标准的分段白噪声模型，该 $2 times 2$ 子块的三项应使用同一个 yaw 噪声强度 `s2q_yaw`：

```cpp
// yaw 过程噪声块（分段白噪声，秩一形式）：三项都应乘 yaw 的强度
double q_yaw   = pow(t,4)/4 * s2q_yaw;
double q_yaw_w = pow(t,3)/2 * s2q_yaw;   // ← 上游此处误写成了 s2q_xyz
double q_w     = pow(t,2)   * s2q_yaw;
```

上游代码的交叉项 `q_yaw_w` 使用了位置噪声强度 `s2q_xyz`，而两个对角项使用 `s2q_yaw`。以源码默认值 20 和 100 为例，交叉项是标准形式的五分之一；当 $t != 0$ 时，该 $2 times 2$ 子块仍为正定矩阵，但不再是同一白噪声驱动得到的秩一形式。一般地，其行列式为 $t^6 ("s2q_yaw"^2 - "s2q_xyz"^2) / 4$：两个参数非负时，`s2q_xyz < s2q_yaw` 得到正定子块，两者相等时为半正定，前者更大时则成为不定矩阵。$t = 0$ 时该子块为零矩阵。即使默认矩阵合法，也不能据此保证滤波器不会在其他条件下发散。修正乘子后仍应结合实车数据重新整定过程噪声，并检查 $bold(Q)$ 的对称性与特征值，而不是只改一处变量名便认为验证完成。

=== 目标选择与切换

画面中可能同时出现多个候选，系统需要决定初始目标，并在必要时切换。

*初始化*时，参照开源实现选择离*光心最近*的装甲板，识别器已经为每个候选计算 `distance_to_image_center`。这个判据倾向于选择当前云台指向附近的目标，适合作为简单默认值；它并不直接保证 PnP 更准确，也没有考虑距离、兵种优先级或遮挡风险。

*跟踪中*，参照实现仍在同号候选中选择离预测位置最近的一块，并使用双阈值门限制正常更新。更高层的目标切换需要在稳定性与战术优先级之间取舍：若每帧都重新选择离光心最近者，两个候选接近时容易频繁切换；若从不切换，又可能忽略更高优先级目标。一种常见设计是给兵种设置优先级，并要求候选优先级明显更高、持续若干帧或当前轨迹已稳定后再切换。具体权重与迟滞条件属于策略配置，必须结合当季规则和实战数据确定。

无 ROS 框架的 2025 赛季实现还增加了 `switching` 状态，用于多相机全向感知。车身广角相机发现更高优先级目标后，系统先进入 `switching`，引导云台转向该方向；等主相机也检测到目标，再建立正式轨迹。这个中间态把"副相机发现"与"主相机确认"分开，避免把不同相机下精度和时序不同的观测直接当成同一种关联结果。

目标选定后，稳定的 $omega$（`v_yaw`）估计可作为判断目标是否持续自转的重要输入。至于多高转速算小陀螺、瞄准车体中心还是某块装甲板、何时开火，还需要结合弹道、云台约束和战术规则，由「自瞄算法设计」一章继续讨论。本章只负责说明状态怎样估计及其可信度怎样检查。

=== 另一种实现：球坐标观测与自适应噪声

同一个整车跟踪问题可以有不同实现。将前面的 ROS2 九维模型与无 ROS 框架并列，可以比较状态维度、观测坐标、噪声模型和对时方式分别带来的复杂度与适用条件。

*状态维度不同。* 无 ROS 框架采用十一维状态 $(x, v_x, y, v_y, z, v_z, theta, omega, r, l, h)$，把九维版在滤波器外通过 `another_r` 和 `dz` 维护的两组半径、高度差写入状态，其中 $l = r_2 - r_1$、$h = z_2 - z_1$。纳入状态后可以统一传播这些量及其协方差，但维数增加、可观性和噪声整定也更复杂；放在滤波器外则结构简单，却不能完整表达另一组参数的不确定性。选择取决于量测是否足以约束新增状态以及调试成本。

*观测坐标不同。* 九维版使用装甲板直角坐标 $(x_a, y_a, z_a, theta)$；无 ROS 版使用方位角、俯仰角、距离和装甲板朝向（yaw–pitch–distance–angle）。单目位姿解算的距离方向通常比角度方向更不确定，而且误差会随距离和视角变化。球坐标将角度与距离显式分开，使对角或分块的 $bold(R)$ 更容易表达"角度较准、距离较差"这一近似。直角坐标并非不能表达同样的不确定性：通过坐标变换雅可比可以得到带有非对角项的完整协方差；只是若仍使用简单对角 $bold(R)$，方向性容易被忽略。相关误差传播见理论篇「相机模型」和「卡尔曼滤波」。

*量测噪声自适应。* 无 ROS 实现让 $bold(R)$ 随观测条件变化：距离量测方差随装甲板朝向与水平视线方向的夹角调整，装甲板朝向量测方差随距离调整。前者只是完整入射角的一项水平近似。其目的不是保证这两个函数精确描述真实误差，而是避免在远距离或大斜视角下仍使用固定置信度。函数形式和系数需要用带参考真值或重复观测的数据标定，并按距离与视角分桶检查新息统计。

*对时方式不同。* 跟踪通常在惯性坐标系中进行，否则云台转动会使静止目标在相机坐标系中产生表观运动。把相机观测变换到惯性系时，需要与图像采集时刻对应的云台姿态。ROS2 实现使用带时间戳的 `tf2` 坐标树，`message_filter` 在相应时刻的变换可用后再触发回调。无 ROS 实现由取流线程使用 `steady_clock` 记录帧时间，再从 IMU 姿态队列中做四元数球面插值（slerp）。若时间戳记录的是取帧返回而非传感器曝光时刻，还要测量并补偿采集延迟；软件插值本身不能消除时间戳来源误差。两条路线分别依赖框架管理和自建队列，验证时都应通过已知转动测试测量对时残差。工业相机章节已讨论取流与重连，这里只关注时间戳和姿态对齐。

以上比较用于说明方法选择，不是两个仓库的逐行导读；完整代码路径留到「项目分析」篇。实现自己的跟踪器时，需要明确回答四个问题：哪些物理量进入状态、观测使用哪个坐标系、$bold(R)$ 是否随条件变化，以及时间戳与姿态由谁对齐。每项选择都应配套可复现的验证，而不是只依据代码结构判断优劣。

=== 怎么验证跟踪结果

跟踪器能够持续输出状态，只能说明程序和状态机仍在运行，不能说明状态接近真实目标。验证应从可视化、关联量、统计一致性和外部参考四个层面进行，并明确每项检查能够证明的范围。

一、*把整车状态投影出来*。由中心、半径和朝向可以推算全部装甲板的位置，在三维可视化中画出板面、中心和速度，再与当前检测叠加。明显偏移、漂移或换板时不连续，能够暴露坐标系、半径交换或关联问题。需要注意，这是一项模型内部一致性检查，不是独立真值：显示的装甲板与滤波状态来自同一模型，视觉上贴合也不能证明中心、速度和角速度都准确。

二、*记录关联差值和门限事件*。参照实现发布 `position_diff` 与 `yaw_diff`，它们正是正常匹配门使用的两个量。可以统计其分布、接近门限的比例、`handleArmorJump` 触发率以及进入 `TEMP_LOST` 的持续时间。频繁接近门限说明预测与观测不一致，可能来自远距离或灯条受损时的位姿波动，也可能来自同号候选错配；这些量能定位异常发生在关联阶段，但不能单独区分具体原因。

三、*在定义正确的前提下使用 NIS*。标准归一化新息平方使用更新前的新息 $bold(nu) = bold(z) - bold(h)(hat(bold(x))^-)$ 与新息协方差 $bold(S) = bold(H) bold(P)^- bold(H)^top + bold(R)$：

$ "NIS" = bold(nu)^top bold(S)^(-1) bold(nu) $

在线性、高斯且噪声模型一致的理想条件下，四维观测的 NIS 服从自由度为 4 的卡方分布；EKF 中这是近似关系。上侧 95% 分位为约 9.49，表示理想情况下约 95% 的样本不超过该值，而不是所有值都应靠近 9.49。还可以使用上下两侧分位检查 $bold(Q)$、$bold(R)$ 是否过大或过小，并在滑动窗口中统计超限率。

比较阈值前必须核对代码计算的究竟是不是上述统计量。另一套无 ROS 实现中出现过约 0.711 的阈值，但它使用更新后的状态和协方差计算后验残差统计量，并非标准 NIS；因此不能直接拿 0.711 与 $chi^2_4$ 的上侧 95% 分位比较，也不能把标准 NIS 的结论原样套到该实现。项目分析篇会逐行说明这一区别。即便标准 NIS 分布符合预期，也只支持滤波器在测试数据上的统计一致性，不等于轨迹具有足够小的绝对误差。

四、*统计物理约束的触发率*。九维实现将半径限制在 $0.12 tilde.op 0.4$ m；无 ROS 实现要求 $r$ 与 $r + l$ 位于 $0.05 tilde.op 0.5$ m，否则重新起跟；对前哨站等规则给定转速的目标，还可能把 $omega$ 限制在约 $2.51$ rad/s（$0.8pi$，规则数值随赛季变化，应查当季手册）。这些范围本身不能证明状态正确。若约束频繁触发，至少说明当前数据、模型、参数或边界不匹配，需要结合触发前后的观测和关联日志进一步定位。

下面列出几类常见问题及其优先检查项：

- *跨越 $plus.minus pi$ 时 yaw 跳变*：检查 `unwrapYawNear`／`orientationToYaw` 的参考值、最短角距和相邻观测转角是否满足小于 $pi$ 的前提。
- *半径剧烈变化或 $omega$ 频繁变号*：检查 `handleArmorJump` 触发条件、半径交换、候选关联和量测 yaw，而不要先假定一定是滤波公式错误。
- *同号目标错配*：结合候选列表、分类置信度和位置门限回放，区分识别类别抖动与关联策略问题。
- *帧率变化时预测误差增大*：确认每次预测使用实际 $Delta t$，并检查时间戳是否单调、是否对应同一时钟域；超时判断也应使用实际经过时间。
- *云台转动时静止目标出现系统运动*：检查相机到惯性坐标系的变换方向、时间戳对齐和单位，而不仅是确认代码调用了坐标变换函数。

可以先做两项最小测试。第一项让相机、云台和目标保持静止，记录估计中心、$omega$、关联差值和协方差；状态均值应避免系统漂移，$omega$ 应围绕零波动。协方差收敛只表示滤波器内部置信度变化，必须同时比较重复测量或外部位置参考。第二项让目标按可测转速旋转，最好使用编码器、转台指令或人工标定视频提供独立参考，再比较 $omega$ 误差、换板连续性、丢失率和重投影偏差。两项测试是基础核对，不覆盖真实对抗中的加减速、遮挡、多目标和曝光变化；这些场景仍需单独录像回归。

==== 本章小结

本章用 track-by-detection 将逐帧装甲板观测转换为连续整车状态，核心问题是数据关联。参照实现采用九维状态，位置与速度交错存放，当前装甲板的高度、朝向和半径在换板时更新；四状态机用确认延迟和删除延迟控制轨迹生命周期，并把丢失门限从秒换算为帧数。每次 `update` 依次完成预测、关联、双阈值匹配和可能的换板处理。最短角距用于处理 yaw 的周期边界，候选计算与历史量测提交还应避免互相污染；`handleArmorJump` 在正常路径下保持中心和自转状态连续，半径限幅则约束弱可观状态，但这些机制都有适用前提和失败边界。

与无 ROS 实现对照后，可以看到状态维度、观测坐标系、自适应量测噪声和对时方式的不同选择。验证不能只看程序是否持续输出：还要检查整车投影、关联差值、标准定义下的 NIS、物理约束触发率，并使用静止与已知转速数据提供外部参考。任何单项检查都不能独立证明跟踪准确，真实遮挡、多目标和曝光变化仍需录像回归。

装甲板识别与跟踪至此形成了一条完整的数据链：识别器提供类别和单帧位姿，跟踪器估计连续位置、速度和自转状态。下一章转向运动形式由规则限定的能量机关，比较它与装甲板在检测和建模上的差异。小陀螺判定、提前量和开火时机等系统级决策，则留到「自瞄算法设计」一章统一处理。

（本章的整车跟踪器以社区广泛使用的 rm_auto_aim `armor_tracker` 为主线蓝本，无 ROS 的对照实现取自同济 SuperPower 战队 2025 赛季公开框架，两者均为 MIT 许可证；文中所有代码为讲解目的重写，硬编码的参数取值属"某赛季某队"经验值，规则类数字随赛季变动、以当季官方手册为准。）
