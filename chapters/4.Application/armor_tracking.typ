=== 单帧的两难：从"这一帧是什么"到"它下一刻在哪"

上一章的识别器，回答的是一个"当下"的问题：这一帧画面里，哪些像素是装甲板，它是哪台车、在相机系的哪个位置。可自瞄要的从来不是"它现在在哪"——子弹飞到目标需要时间，你必须瞄准它*将要在*的地方。识别器对未来一无所知，这道鸿沟得靠跟踪来填。

但跟踪要补的还不止预测这一件事。基础篇 OpenCV 一节在真实比赛画面上跑那套四级级联时，已经把单帧识别的软肋摊开给你看过了：即使在实车的低曝光域里，场馆灯带、地面反光、残血闪烁的灯条、被打灭一半的装甲板、乃至队友的车灯，都在持续制造假候选；而各兵种装甲板外形几乎一样，"认错车"比"认错板"更隐蔽。那一节的结论是一句伏笔——"一条规则挡住一类错误，也误伤一个正确答案，单帧几何规则的两难在这里一览无余，实战系统靠连续帧跟踪来兜底"。这一章就是来兑现这句伏笔的：把兜底的那个 tracker 从零讲成一套能上场的工程。

单帧的信息量是有上限的。一个细长的亮斑到底是灯条还是反光，一块装甲板到底属于哪台车，在一帧里原理上就可能分不开——这不是参数没调好，是信息本身不够。跟踪的第一性原理正是：把证据在时间维度上累积起来。一个假候选很难在连续几十帧里都自圆其说——它不会沿着一条平滑的运动轨迹走，也凑不出一台车该有的四块板的几何。于是同一件识别做不到的事，交给"这个东西上一帧在哪、按什么规律动"这条额外约束，就有了着落。识别负责"宽进"，跟踪负责在时间上"严出"。

=== track-by-detection：识别负责找，跟踪负责串

现代自瞄的跟踪，几乎都走 track-by-detection（先检测后跟踪）这条范式：每一帧先让识别器独立地找出所有装甲板，跟踪器再把这些"孤立的检出"跨帧串成一条有身份、有运动状态的轨迹。识别器不需要知道上一帧发生了什么，跟踪器也不重新去抠像素——两者职责清爽地分开。这背后是一个很实在的工程理由：识别（尤其是神经网络那条路线）是整个流水线里最贵的一环，让它每帧从头做、不背历史包袱，反而最容易做快、做稳。

范式定了，跟踪器的核心难题就浮出来了——*数据关联*（data association）：这一帧检出的这块板，和我上一帧正在跟的那个目标，是不是同一个东西？关联对了，滤波器吃到一条干净的量测；关联错了（把 A 车的板喂给了 B 车的滤波器），比丢帧还糟，估计会被硬生生带偏。这就是社区里 SORT 一类多目标跟踪方法的主线：用运动模型预测每条轨迹"这一帧应该出现在哪"，再拿预测去和检出做匹配。RM 的自瞄把问题收窄了一层——同一时刻通常只锁一台车，是*单目标*跟踪，关联从"多对多指派"简化成"在候选里挑一块最像我预测的"。但简化不等于消失：目标被短暂遮挡、开着陀螺换板、旁边冒出更该打的车，每一个都是关联要处理的实况。

关联要"拿预测去比对"，就绕不开一个运动模型——你得先说得清"这个目标下一刻大概在哪"，才谈得上判断哪块检出对得上。模型选得好不好，直接决定跟踪成不成立。而 RM 里最刁钻的运动，恰恰是小陀螺：逐块板去跟，看到的是一串只活零点几秒、速度方向不停翻转的短命目标，任何平动模型都会被甩开。理论篇「卡尔曼滤波」一章已经把出路推导清楚了——不跟板，跟整台车。这一章就把那套数学，落成跟踪器里真正跑的代码。

=== 状态里装的不是一块板，是整台车

理论篇给出的整车模型，把一台车抽象成"中心平动 + 装甲板绕中心匀速旋转"，状态取车体中心的位置速度、当前板的朝向角、自转角速度和旋转半径。那是骨架。实车的跟踪器在骨架上多长了两根肋骨：竖直方向（装甲板高度）也进了状态，而且——理论篇结尾特意留的那句"相邻两组装甲板的安装半径与高度可以不同，工程实现会在状态里维护两个半径交替使用"——在这里正式兑现。社区最广泛使用的那套开源跟踪器，状态定义是九维：

$ bold(x) = (x_c, v_x, y_c, v_y, z_a, v_z, theta, omega, r)^top $

其中 $(x_c, y_c)$ 与 $(v_x, v_y)$ 是车体中心在惯性系下的水平位置与速度，$z_a$ 是*当前这块可见板*的高度、$v_z$ 是它的变化率，$theta$ 是当前板的朝向角（即代码里的 `yaw`），$omega$ 是整车自转角速度（即 `v_yaw`），$r$ 是当前板的旋转半径。

#figure(
  table(
    columns: (auto, auto, auto),
    align: (center, center, left),
    stroke: (x, y) => (
      top: if y == 0 or y == 1 { 1pt } else { 0pt },
      bottom: if y == 9 { 1pt } else { 0pt },
    ),
    inset: 7pt,
    table.header([*下标*], [分量], [含义]),
    [0], [$x_c$], [车体中心水平坐标 x（惯性系）],
    [1], [$v_x$], [中心 x 方向速度],
    [2], [$y_c$], [车体中心水平坐标 y],
    [3], [$v_y$], [中心 y 方向速度],
    [4], [$z_a$], [当前可见板的高度],
    [5], [$v_z$], [高度变化率],
    [6], [$theta$], [当前可见板的朝向角（`yaw`，连续量）],
    [7], [$omega$], [整车自转角速度（`v_yaw`）],
    [8], [$r$], [当前可见板的旋转半径],
  ),
  caption: [九维整车状态。读表规则：位置与速度*交错*排列（0/1 是 x 与 $v_x$，2/3 是 y 与 $v_y$……），不是"位置全在前、速度全在后"——这个顺序直接决定了状态转移矩阵长什么样，抄错一位滤波器就散。中心是整台车共享的，$z_a\/theta\/r$ 却是"当前这块板"的属性，换板时要跟着切换。],
)

这里藏着两个容易被文档带偏的坑，值得点破。其一是排列顺序：*位置和速度是交错的*，$(x_c, v_x, y_c, v_y, dots)$，而不是把四个位置写在前、四个速度堆在后。为什么要交错？因为这样每个"位置-速度"对就是一个独立的 $2 times 2$ 匀速子块，状态转移矩阵 $bold(F)$ 变成清爽的分块对角，$mat(1, Delta t; 0, 1)$ 沿对角线排四份（水平两轴、高度、朝向角各一份），$r$ 独占最后一维不动。理论篇那个 $mat(1, Delta t; 0, 1)$ 在这里原样上岗四次。顺带提醒一句：那套开源实现的 README 里，状态被写成"位置全体在前、速度全体在后"的另一种排列，还配着一个更早的六维匀速模型的描述——那是项目早期的版本，代码早已换成这里的九维整车模型。读跟踪器永远以代码里的状态向量和转移矩阵为准，文档会过时，`target_state << ...` 那一行不会骗你。

其二是观测方程。图像端能测到的，只有*当前那块可见板*的位姿——PnP 给出它在惯性系下的位置 $(x_a, y_a, z_a)$ 和朝向 $theta$。状态是整车，观测是单板，两者靠几何关系连起来：

$ x_a = x_c - r cos theta, quad y_a = y_c - r sin theta $

这两式是非线性的（$r cos theta$），正是理论篇里 EKF 雅可比要上场的地方——具体推导那一章讲透了，本章不重推。这里只提一个约定上的细节，免得你拿它和理论篇逐字母对照时犯迷糊：理论篇把 $theta$ 定成"中心指向装甲板"的方向，写作 $x_a = x_c + r cos theta$；这套代码里的 `yaw` 是 PnP 解出的装甲板自身朝向，正方向约定相反，于是同一个几何关系写成了减号。符号来自约定，内部自洽即可，不必和理论篇强行对齐——你只要认准：初始化时怎么由板算中心（$x_c = x_a + r cos theta$），观测里就怎么由中心算板（$x_a = x_c - r cos theta$），一正一反闭合就对了。$z_a$ 与 $theta$ 则被直接观测。

=== 状态机：识别、跟踪、短暂丢失、彻底丢失

有了状态空间，还差一层"什么时候该信、什么时候该放"的调度逻辑。跟踪器不是一上来就死咬目标：闪现一帧的假候选不值得起跟，遮挡两帧的真目标也不该立刻放弃。这层迟滞逻辑，就是一个四状态的状态机。

#figure(
  image("images/app-tracking-state-machine.png", width: 92%),
  caption: [跟踪器的四状态生命周期（脚本 `tracking_state_machine.py`）。读图规则：`DETECTING` 是给"出生"加的低通——必须连续匹配上超过 `tracking_thres`（默认约 5）帧才转正为 `TRACKING`，一帧闪光进不来；`TEMP_LOST` 是给"死亡"加的低通——目标短暂丢失时先扛住、EKF 照常预测，撑过 `lost_thres` 帧还没回来才判真丢。底栏是下游最关心的一件事：只有 `TRACKING` 和 `TEMP_LOST` 两态才对外发布 `tracking = true`、云台才跟随，所以短暂丢失期间云台不会松、也不会抽。],
)

#figure(
  table(
    columns: (auto, auto, auto, auto),
    align: (left, left, center, left),
    stroke: (x, y) => (
      top: if y == 0 or y == 1 { 1pt } else { 0pt },
      bottom: if y == 6 { 1pt } else { 0pt },
    ),
    inset: 7pt,
    table.header([*当前态*], [触发事件], [转到], [工程含义]),
    [`LOST`], [首次检出（选离光心最近的板初始化）], [`DETECTING`], [起跟，但先别信],
    [`DETECTING`], [连续匹配 > `tracking_thres`（≈5）], [`TRACKING`], [确认是真目标（生的低通）],
    [`DETECTING`], [一次没匹配上], [`LOST`], [闪一下不算数，打回重来],
    [`TRACKING`], [一次没匹配上], [`TEMP_LOST`], [先扛住，EKF 继续预测],
    [`TEMP_LOST`], [重新匹配上], [`TRACKING`], [找回来了],
    [`TEMP_LOST`], [连续丢失 > `lost_thres`], [`LOST`], [判定真丢，废弃重来],
  ),
  caption: [状态转移表。读表规则：竖着看第三列，`DETECTING` 与 `TEMP_LOST` 是两道对称的迟滞门——一道拦住假目标不让它冒充，一道护住真目标别被一两帧遮挡打断；两道门的阈值（`tracking_thres`／`lost_thres`）就是这套状态机全部可调的旋钮。],
)

这里有一个实车上极易踩、也极能体现工程素养的细节：`TEMP_LOST` 熬到 `LOST` 的门限 `lost_thres`，不是一个写死的帧数，而是按时间算的——`lost_thres = lost_time_thres / dt`，`lost_time_thres` 是一个约 0.3 秒的时间常数，`dt` 是当前帧间隔。为什么非得这么绕？因为帧率会变：相机跑 125 fps 时 0.3 秒是约 37 帧，跑 200 fps 时就是 60 帧。如果把门限硬编成"丢 30 帧就放弃"，换一台更快的相机，容忍时间就悄悄缩水到 0.15 秒，目标稍微被挡一下就被放掉——*帧数不是物理量，时间才是*。凡是"多少帧算超时"的判断，都该先除以 `dt` 折算成时间，这是滤波和跟踪代码里反复出现的同一课。

`TEMP_LOST` 期间"EKF 照常预测"这件事，理论篇「卡尔曼滤波」的异步更新一节已经把机理讲透：预测和修正本就是两个独立步骤，没有量测时只跑预测——状态沿模型外推，协方差 $bold(P)$ 如实增长，滤波器"知道自己越来越不确定"。跟踪器要做的只是把这个能力接进状态机：短暂丢失不清空滤波器、不停发布，等目标回来，变大的 $bold(P)$ 让增益自动加大，一两帧就把估计拉回来。当然不能硬推太久——`lost_thres` 就是那根"推到这儿还没回来就认栽"的红线。

=== 一次 update 的内部：预测、关联、匹配门、跳变

把镜头推到每一帧真正发生的事。跟踪器的 `update()` 是一条固定的四步流水：先预测，再关联，验门，处理跳变。

```cpp
// 一次 update：预测在前，关联在后
Eigen::VectorXd x_pred = ekf.predict();               // 先把状态推进到本帧
Eigen::Vector3d pos_pred = armorPosFromState(x_pred); // 由整车状态算出"当前板"的预测位置

// 在同 number 的候选里，挑离预测位置最近的一块
double min_pos_diff = DBL_MAX, yaw_diff = DBL_MAX;
int same_id_count = 0;
for (const auto & a : armors) {
  if (a.number != tracked_id) continue;               // 只认同一台车（同数字）
  same_id_count++;
  double d = (pos_pred - toVec(a.pose.position)).norm();
  if (d < min_pos_diff) {
    min_pos_diff = d;
    yaw_diff = std::abs(orientationToYaw(a.pose.orientation) - x_pred(6));
    matched_armor = a;
  }
}

if (min_pos_diff < max_match_distance && yaw_diff < max_match_yaw_diff) {
  // 门内：一次正常的观测更新
  double yaw = orientationToYaw(matched_armor.pose.orientation);
  Eigen::Vector4d z(px, py, pz, yaw);
  target_state = ekf.update(z);
} else if (same_id_count == 1 && yaw_diff > max_match_yaw_diff) {
  // 位置对得上、yaw 却跳了一大截，且只剩这一块同号板 —— 陀螺把新板转过来了
  handleArmorJump(matched_armor);
}                                                     // 否则本帧不更新，仅靠 predict 外推
```

关联用的是一道*双阈值匹配门*：候选与预测的位置差要小于 `max_match_distance`（默认约 0.15 m），朝向差要小于 `max_match_yaw_diff`（默认约 1.0 rad），两条都过才算真匹配、才喂给 `ekf.update`。位置管"是不是同一处"，朝向管"是不是同一块板的姿态"。只在*同一个 `number`* 的候选里挑——数字由上一章的分类器给出，同一台车的四块板共享同一个数字，这是关联能锁定"同一台车"的根基。没有任何候选进门，本帧就干脆不更新，只留预测的外推值：这正是 `TEMP_LOST` 敢于"扛住"的底气。

==== 先解缠，yaw 才能进滤波器

匹配门和观测更新都要用到装甲板的 `yaw`，但 PnP 直接给的 `yaw` 有个致命的性质：它落在 $[-pi, pi]$ 区间里，会回绕。一台开陀螺的车，每转一圈这块板的朝向就跨一次 $plus.minus pi$ 的接缝，PnP 输出会从 $+pi$ 猛跳到 $-pi$。如果把这个原始值直接喂给滤波器，残差 $bold(z) - bold(h)(bold(x))$ 会在接缝处凭空出现一个约 $2 pi$（即 $360 degree$）的假跳变——而真实的帧间转动只有几度。一次这样的错误修正，就足以把滤波器打散。

解药是 `orientationToYaw`：把回绕的观测*累加*成一个连续量。

```cpp
double orientationToYaw(const Quaternion & q) {
  double yaw = getYawFromQuaternion(q);            // PnP 给的，落在 [-pi, pi]
  // 用"最短角距"累加：把 [-pi,pi] 的锯齿接回 (-inf, +inf) 的连续量
  yaw = last_yaw_ + shortest_angular_distance(last_yaw_, yaw);
  last_yaw_ = yaw;
  return yaw;
}
```

`shortest_angular_distance(a, b)` 返回从 $a$ 到 $b$ 的*带符号最短转角*（永远落在 $plus.minus pi$ 内），把它累加到上一次的连续 `yaw` 上，就等于说"这一帧相对上一帧只转了这么一点点"，接缝被悄无声息地缝合。这一步和理论篇结尾"残差的角度分量要归一化回 $plus.minus pi$"是同一枚硬币的两面：凡是把角度放进状态或观测，就必须处理它住在圆上这件事，否则迟早在某个接缝上翻车。

#figure(
  image("images/app-tracking-yaw-unwrap.png", width: 100%),
  caption: [为什么 yaw 要先解缠再进滤波器（脚本 `tracking_yaw_unwrap.py`）。仿真一台匀速自转的车（每秒两转），上图：细红线是 PnP 直接给的 `yaw`，落在 $[-pi, pi]$、每转一圈在接缝处竖直跌落一次；粗蓝线是 `shortest_angular_distance` 累加还原出的连续 `yaw`，喂给 EKF 的是它。下图：两条路各自交给滤波器的帧间残差——喂原始值（红）在每个接缝处诈出一个约 $-2pi$ 的假残差，一次就能让 EKF 发散；喂解缠值（蓝）残差稳稳停在 $omega\/"fps"$ 的真实小步长上。读图规则：接缝处红蓝的落差，就是这一步防住的灾难。],
)

==== handleArmorJump：陀螺把新板转过来了

`update` 里那个 `else if` 分支，处理的是陀螺跟踪最核心的一幕。车转到某个角度，原先正对相机的板转走了、旁边一块*同号*的板转进视野——检出的位置和预测大致对得上（都在车周围那一圈），但 `yaw` 突然差了一大截（相邻两块板差约 $90 degree$），而且此刻画面里只有这一块同号板。这个组合就是"陀螺换板"的签名，交给 `handleArmorJump`：

```cpp
void handleArmorJump(const Armor & new_armor) {
  double yaw = orientationToYaw(new_armor.pose.orientation);
  target_state(6) = yaw;                    // 朝向角切到新板
  // 常规四板车才有"两组半径 / 两组高度"，换板时交替
  if (armors_num == NORMAL_4) {
    dz = target_state(4) - new_armor.pose.position.z;
    target_state(4) = new_armor.pose.position.z;  // 高度切到新板
    std::swap(target_state(8), another_r);        // 半径在两组间交换
  }
  // 若换板后推出的板位置与实测差太远，认定 EKF 已发散，重置中心
  if ((measuredPos - inferPos(target_state)).norm() > max_match_distance) {
    resetCenter(target_state, new_armor);
  }
  ekf.setState(target_state);
}
```

关键是它*只切当前板的属性*（朝向 $theta$、高度 $z_a$、半径 $r$），不碰车体中心的位置速度、也不碰自转角速度 $omega$。这是整车建模最漂亮的回报：换板对中心的估计毫无扰动——中心一直在那儿匀速平动；而 $omega$ 是从每一段"单块板可见期间朝向的平滑爬升"里估出来的，换板只是把 $theta$ 挪个初值、接着爬，$omega$ 的估计连续不断。所以陀螺再快，只要每块板能被稳定跟上几帧，整车的自转角速度就能稳稳量出来。那句"两组半径交替"（`std::swap` 半径、`dz` 记高度差）也在这里落地——常规四板车前后两对板的安装半径和高度往往不等，换到另一对就把维护着的 `another_r` 换进状态。最后那道"推出的板位置和实测差太远就重置中心"，是给发散兜的底：万一滤波器已经跑飞，与其信一个错得离谱的中心，不如就着当前这块板重新落一个初值。

==== 半径限幅：给弱可观状态上一道护栏

$r$ 是状态里最"软"的一维。它只透过 $r cos theta$、$r sin theta$ 间接被观测，正对相机时（$theta approx 0$）它对 $x_a$ 的影响被 $cos theta approx 1$ 摊平、可观性很弱，量测噪声一冲，估计就容易往不合物理的方向漂。于是跟踪器在每帧更新后给它硬性限幅：

```cpp
if      (target_state(8) < 0.12) target_state(8) = 0.12;  // 旋转半径下限
else if (target_state(8) > 0.4)  target_state(8) = 0.4;   // 上限
```

$0.12 tilde.op 0.4$ 米这个区间，是真实机器人装甲板到旋转中心距离的物理范围——比这更小、更大都不是一台合法的车。*把物理先验写成一道限幅，是对付弱可观状态发散最省事也最有效的护栏*。这不是"教科书卡尔曼"里的操作，却是实车能不能扛住一整场比赛的分野——理论保证的是"噪声高斯、模型精确"时的最优，而赛场上两个前提都不成立，限幅就是替滤波器把住不越界的那只手。

==== 一处值得当练习的勘误

读这套跟踪器的过程噪声矩阵 $bold(Q)$ 时，能撞见一个真实的手误，很适合当作"读代码挑错"的练习。$bold(Q)$ 的 `yaw` 块本该是标准的分段白噪声形式——三项都乘同一个 yaw 噪声强度 `s2q_yaw`：

```cpp
// yaw 过程噪声块（分段白噪声，秩一形式）：三项都应乘 yaw 的强度
double q_yaw   = pow(t,4)/4 * s2q_yaw;
double q_yaw_w = pow(t,3)/2 * s2q_yaw;   // ← 上游此处误写成了 s2q_xyz
double q_w     = pow(t,2)   * s2q_yaw;
```

上游代码里，中间那个交叉项 `q_yaw_w` 的乘子被错写成了位置噪声强度 `s2q_xyz`（默认 20）而非 `s2q_yaw`（默认 100），对角两项则用对了。后果要*如实*地估量、别夸大：这让 yaw 角与角速度之间的过程噪声互协方差偏小了 5 倍，矩阵仍是合法的（半）正定协方差（可以验算：对角不变、非对角变小，行列式反而由 0 变正，秩从 1 升到 2），滤波器不会因此发散；何况这几个 $sigma^2$ 本就是手调的经验旋钮，真要修，正确值也得重新整定。所以它*不是一个会让你输比赛的缺陷*，而是一处"不再是分段白噪声该有的秩一形式"的形式瑕疵——认得出它、说得清它为何无伤大雅，比逮住它更见功力。这也顺带示范了读开源跟踪器的正确姿势：以代码语义为准去理解意图，遇到手误就当练习，别照抄那一行，也别把噪声当缺陷。

=== 目标选择与切换：先跟谁，什么时候换

画面里往往不止一台敌车，跟踪器得决定"先咬谁、什么时候换"。

*起跟*时，那套开源实现用的是最朴素的判据——选离*光心最近*的板初始化（识别器早已给每块板算好了到图像中心的距离 `distance_to_image_center`）。离光心近，通常意味着离云台指向近、姿态解算更准，也更可能是操作手正瞄着的那台。这是个合理的默认，谈不上最优。

*跟踪中*的关联，前面讲过：在同号候选里挑离预测位置最近的一块，用双阈值门守住。真正微妙的是*换目标*——什么时候该放下手里这台去打另一台？纯按"离光心最近"每帧重选会导致目标乱跳，稳定性崩掉；完全不换又会漏掉更该打的高价值目标。成熟的做法是给每台车一个*优先级*（英雄 > 步兵这类兵种权重），只有当画面里出现优先级*更高*的目标、且当前目标已经跟稳（滤波器收敛）时才切换，把"稳定咬住"和"及时改打"这对矛盾调和开。

另一支强队 2025 赛季公开的无 ROS 框架，在这件事上多走了一步值得一提：它的状态机在四态之外多了一个 `switching` 态，专门服务*全向感知*——除了主相机，车身还挂着几颗广角相机扫视四周，当某颗副相机瞥见一个优先级更高的目标，跟踪器先进 `switching`、把云台往那个方向带，等主相机也捕到同一目标再正式起跟。这就把"换目标"从一个瞬时决定，摊成了一段有中间态的平滑过渡。

无论哪种实现，目标一旦选定、$omega$（`v_yaw`）估得又稳又大，你手里就攥着判断对手是否在开陀螺的关键量了。但*怎么据此定开火时机*——转速多高才算陀螺、枪口该指车心还是提前量、哪一刻扣扳机——是系统集成层的决策，理论篇「卡尔曼滤波」结尾把这件事明确指向了「自瞄算法设计」一章，本章到"把整车状态估准、把陀螺看清"为止，不越界替开火决策做主。

=== 另一种工程选择：球坐标观测与自适应噪声

同一个整车跟踪问题，工程上不止一种落法。把前面那套基于 ROS 的九维实现，和刚提到的那套无 ROS 框架并排看，能学到的东西比任何单一实现都多——它们在几个关键设计点上的分岔，恰恰是跟踪器工程里最值得琢磨的取舍。

*状态更细。* 那套无 ROS 框架用了十一维状态 $(x, v_x, y, v_y, z, v_z, theta, omega, r, l, h)$，把九维版里靠 `another_r`/`dz` 在滤波器外面"手动交替"的两组半径高度，直接写进状态：$l = r_2 - r_1$、$h = z_2 - z_1$，让滤波器自己去估这两组的差。孰优孰劣没有定论——放进状态更统一、更"卡尔曼"，放在外面更简单、更好控——但它把"常规车有两组不等的半径高度"这条物理，从补丁升格成了一等公民。

观测在球坐标，不在直角坐标。 这是最见功底的一处分岔。九维版的观测是装甲板的直角坐标 $(x_a, y_a, z_a, theta)$；无 ROS 版的观测则是*方位角、俯仰角、距离、板朝向*（yaw–pitch–distance–angle）这组球坐标量。为什么值得费这个劲把观测搬到球坐标？因为*量测噪声的形状本来就是球坐标的*：单目 PnP 在横向（方位/俯仰）上很准，在*视线方向（距离）上却差得多*，而且这个深度误差随距离迅速放大——「相机模型」一章已经把"远了就飘"算成了定量的结论（深度误差随距离平方增长）。把观测放在球坐标，噪声协方差 $bold(R)$ 就能沿着"方位准、距离糙"的天然轴去描述，而不必像直角坐标那样，让一个本该是细长纺锤形的不确定性去硬凑一个歪斜的椭球。理论篇「卡尔曼滤波」讲 $bold(R)$ 时那条"噪声诞生在相机系、深度方向方差远大于横向"的注记，在这套球坐标观测里被贯彻得最彻底。

*噪声自适应。* 顺着上一点，它的 $bold(R)$ 不是常数：距离量测的方差随观察入射角变大（斜着看 PnP 测距更不可信），板朝向量测的方差随距离变大（远了 yaw 更糊）。哪个量测此刻更可信，就让滤波器多信它几分——这正是理论篇"$bold(R)$ 该随距离增大"那句话的工程兑现，只是做得更细。

*对时的哲学不同。* 跟踪必须在惯性系里做（「坐标变换」一章论证过，云台在转，相机系里静止的目标都在"运动"），而把相机系的观测搬进惯性系，要用到*那一帧曝光时刻*的云台姿态——这就是对时问题。基于 ROS 的实现把这件事交给框架：`tf2` 维护带时间戳的坐标树，`message_filter` 自动等到对应时刻的变换齐了才触发回调。无 ROS 的实现则自己扛：取流线程用 `steady_clock` 给每帧打时间戳，再拿这个戳去 IMU 的姿态队列里做四元数球面插值（slerp），对齐到曝光那一刻。前者省心、吃整个 ROS 生态，后者更重、但把每一环的时序攥在自己手里、也不依赖 ROS——两条路没有对错，是"用生态"与"自己造"在自瞄上的一次典型分野——「工业相机」一章已经在取图这一环见过它的另一面（守护线程加 `libusb` 复位的掉线自愈），这里只是同一套哲学在对时上的延伸。

需要说清楚边界：以上是*借两套实现对照着讲方法*，不是给谁做逐行导读——整仓的拆解是「项目分析」篇的活。这里要你带走的是那几个可迁移的判断：状态放几维、观测搁哪个坐标系、$bold(R)$ 要不要自适应、对时靠框架还是自造——每一个都是你写自己的跟踪器时躲不开的选择题。

=== 怎么确认跟对了：跟踪稳定性怎么看

跟踪器最坑的地方是：它*看起来*几乎总在工作——只要不崩，它永远吐得出一个目标状态。所以"没报错"离"跟对了"隔着十万八千里，你必须主动去验。下面几件事，从眼睛到数字，层层递进。

一、把估计画出来，让四块板去贴真车。 整车模型给了一个天然的自检：既然状态里有中心、半径、朝向，就能把*全部四块板*的位置重投影回三维、在可视化里画成方块，再叠上位置球和速度箭头。跟对了，这四个方块会稳稳套住画面里真实装甲板的位置、随车一起转；跟歪了，方块会偏、会飘、会和真板错开。这是最直观、最该第一时间打开的一关——整车模型自带真值旁证，别浪费。

二、盯关联残差，别只盯输出。 那套开源实现专门发布了一个调试话题，把每帧的位置差 `position_diff` 和朝向差 `yaw_diff`（就是匹配门里那两个量）暴露出来。跟得稳时这两条线应当小而平；一旦 `position_diff` 频繁顶到 `max_match_distance` 边上、或 `yaw_diff` 反复触发 `handleArmorJump`，说明关联在打摆子——可能是识别在半血/远距离下位姿不稳，也可能是把两台同号车认混了。残差是滤波器的体温计，输出正常不代表体温正常。

三、用卡方一致性检验（NIS）让滤波器自证。 这是把"感觉跟得挺好"变成"有数"的关键一招。每次更新，残差 $bold(y) = bold(z) - bold(h)(bold(x))$ 配着它自己的协方差 $bold(S)$，可以算一个归一化新息平方 $"NIS" = bold(y)^top bold(S)^(-1) bold(y)$。若滤波器的 $bold(Q)$、$bold(R)$ 定得和现实相符，NIS 应服从自由度等于*观测维数*的卡方分布——这套跟踪器观测是四维，于是 NIS 该落在 $chi^2(4)$ 附近：其 $95%$ 分位约 $9.49$、$99%$ 分位约 $13.28$。实现里常见的做法是开一个滑动窗口统计 NIS 超限的比例，超限太多就判定滤波器与现实脱节、干脆重新起跟。这里埋着一个*极易写错的坑*，正好当反面教材：判超限用的阈值，必须是"观测维数对应的卡方临界值"——四维就该用约 $9.49$。见过实现把这个阈值误设成 $0.7$ 量级的，那等于要求 NIS 落在分布最内层 $5%$ 才算"正常"，结果把 $95%$ 本来一致的更新全误判成失败——阈值选小了，一致性检验就退化成了随机报警。理解 NIS 该和谁比、比多少，比会调用它更重要。

四、发散护栏是否在合理区间。 前面的半径限幅（$0.12 tilde.op 0.4$ m）、无 ROS 版里"半径 $r$ 与 $r + l$ 都须落在 $0.05 tilde.op 0.5$ m 否则判发散"、以及前哨站那类已知转速目标把 $omega$ 钳在约 $2.51$ rad/s（$0.8pi$，前哨达稳态后的官方转速，*随赛季以当季手册为准*）——这些护栏被频繁触发本身就是信号：不是护栏太紧，就是上游位姿在往外喷野值。定期看它们的触发率，是给跟踪器做体检。

最后把常见的翻车模式列成一张检查单，多数上场事故逃不出这几条：

- *yaw 没解缠*：陀螺一转过整圈就发散——先查 `orientationToYaw` 的累加对不对。
- *半径乱跳 / $omega$ 忽正忽负*：`handleArmorJump` 的触发条件或半径交换写反了，把正常换板误当发散、或反之。
- *认错车*：两台同号（或半血导致数字分类抖动）的车关联串了线，估计被拽向两者之间——这是关联而非滤波的锅，回到识别端和优先级逻辑去查。
- *$Delta t$ 写死*：帧率一波动预测就错，是实车最常见的滤波 bug，理论篇已经点名过；`lost_thres` 那种"按帧数写死超时"是它的同类变种。
- *没在惯性系里滤波*：直接拿相机系坐标喂滤波器，云台一转静止目标都在"动"——这是坐标系的锅，「坐标变换」一章讲过，务必先转到惯性系再跟踪。

一个可以马上执行的验收动作：*录两段、看两张图*。第一段让目标和机器人都固定，跑跟踪器，看估计的车体中心方差是否收敛、$omega$ 是否稳稳趴在零附近——这验的是静态下滤波器不自己抖。第二段录一段真实小陀螺（每秒一两转量级），看 $omega$ 能否很快收敛到一个稳定值、四块板的重投影能否全程贴住真车——这验的是动态下整车模型立不立得住。两张图一出，"跟得好不好"就从口头感觉变成了白纸黑字。

==== 本章小结

这一章把识别器交出的一帧帧孤立检出，串成了一台车连续、可预测的运动状态。主线是 track-by-detection：识别宽进、跟踪在时间上严出，核心难题是数据关联。落到工程，是九维整车状态（位置速度交错、当前板属性随换板切换）、四状态迟滞机（`DETECTING` 拦假、`TEMP_LOST` 护真、超时按时间而非帧数折算）、以及每帧 `update` 里"预测–关联–验门–处理跳变"的四步流水；`orientationToYaw` 把回绕的 yaw 解缠成连续量，`handleArmorJump` 让陀螺换板不扰动中心与自转估计，半径限幅给弱可观状态兜底。对照另一套无 ROS 实现，你看到了状态维度、观测坐标系、噪声自适应、对时方式这四道可迁移的选择题。而验证一以贯之地遵循应用篇的章魂：四板重投影自检、关联残差、NIS 卡方一致性、发散护栏触发率，加上"录两段看两张图"的验收动作。

至此，装甲板这条线——识别与跟踪——都已成型，系统攥住了"目标是谁、正往哪儿动、下一刻在哪"。但赛场上还有另一类目标不按人的意志机动，而是照着规则手册匀速或变速地转：能量机关。下一章先岔到这条平行的识别线上，看它怎么把"打一个未知的运动"翻转成"辨识一个已知的模型"；至于把枪口指向未来那一点、选准时机开火——陀螺怎么判、提前量怎么给、扳机哪一刻扣——要等两条识别线都齐了，交给「自瞄算法设计」一章收口。

（本章的整车跟踪器以社区广泛使用的 rm_auto_aim `armor_tracker` 为主线蓝本，无 ROS 的对照实现取自同济 SuperPower 战队 2025 赛季公开框架，两者均为 MIT 许可证；文中所有代码为讲解目的重写，硬编码的参数取值属"某赛季某队"经验值，规则类数字随赛季变动、以当季官方手册为准。）
