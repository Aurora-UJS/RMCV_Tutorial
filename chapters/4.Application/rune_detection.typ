=== 能量机关：一道会动的靶

前两章把装甲板这条线走完了：识别找出板，跟踪估出整车状态。现在岔到平行的另一条识别线上。装甲板线和能量机关线，看着都是"识别 + 预测"，骨子里却是两个相反的问题。打装甲板时，你*不知道*对手下一刻怎么动——加速、变向、开陀螺全凭他，滤波器的活儿是从带噪的位置序列里把速度*估*出来（「卡尔曼滤波」一章的主场）。打能量机关时正相反：它怎么转，规则手册白纸黑字写死了——小符匀速、大符按一条正弦规律变速。你不再是"估计一个未知的运动"，而是"辨识一个*已知模型*的参数，再把击发时机掐准"。

这一句话的转向，决定了本章和前几章的分工：识别部分（找扇叶、找圆心、定靶心）和装甲板那套形态学、拟合、PnP 是同一个工具箱，回指即可；真正的新东西在预测——一个*参数已知、参数值未知*的系统辨识问题，以及把辨识结果、各段时延、弹道解算串成一条能真打中会动目标的完整链路。

==== 机制：五片扇叶、R 标、与激活顺序

先把靶看清楚。能量机关是一个竖直安装的大风车：*五片*扇叶均布在中心四周，相邻两片相差 $360 degree \/ 5 = 72 degree$；正中心是一个发光的机甲大师标志——带小闪电的 *R 标*，它就是整个风车的旋转中心。每片扇叶末端是一块待击打的*装甲板*（靶面分成若干环、由内向外计环数），扇叶中段是一条*箭头形的流水灯*。

关键在于扇叶的两种状态，视觉全靠它区分：

- *待激活*扇叶——中段箭头灯*流动*，末端是一个*闭合的矩形边框*，两侧无灯。
- *已激活*扇叶——两侧灯条*亮起*，中段箭头*停止流动*。

比赛里你要按顺序逐片激活：任一时刻只有一片是"待击打目标"，打中它、它转为已激活，下一片随机亮起成为新目标，直到五片打完。规则手册把靶的尺寸（最大直径、环数）、激活的时限（一段时间内没打完当前扇叶会回落重来）、以及命中增益的算法都规定得很细——*这些数字随赛季变动，落到代码里一律以当季手册为准，本章只用它们的量级*。近年一个稳定的框架是：一局的前若干分钟是小符、之后是大符；小符与大符未激活时都*匀速*旋转，$pi\/3$ 弧度每秒（官方口径用 rad/s，即每分钟 10 转），大符激活后才进入正弦变速。

#figure(
  image("images/app-rune-geometry.png", width: 88%),
  caption: [能量机关的解剖图（脚本 `rune_geometry.py`）。五片扇叶以 72° 均布在中心 R 标周围，颜色编码状态：红＝当前待击打目标、橙＝已激活、灰＝未亮。读图规则：视觉的任务是从五片里挑出*唯一那片*待击打扇叶，再在半径 $r$ 处定位它的装甲板中心——一切预测都建立在"圆心 R 找准、半径 $r$ 稳定"之上。右侧曲箭头标出旋转方向：当季规则每局开赛前随机定向、局内不变（红蓝共轴，两面手性相反），但双方看到的是相反的两个面、赛季间约定也会变，所以*别按颜色写死*，上场后用叉乘现测最稳（下文）。左上标注的是纯传统流水线的看家技巧：只要认出一片扇叶，绕 R 转 $plus.minus 72 degree$ 就能推出其余四片的位置。],
)

关于*旋转方向*要多说一句，因为这里最容易埋雷。当季规则的口径是*每局开赛前随机确定绝对转向、该局之内保持不变*。手册同时交代红蓝两面共轴同转——红方看到顺时针转时，蓝方相应地看到逆时针，那是同一根轴的两个相反面，*不是*按阵营指派的绝对方向。这两句合起来才自洽：若转向真按颜色钉死，共轴就已完全确定物理方向，"开赛前随机"便无从谈起。所以*同一个物理转向在你我眼里手性相反*，光凭"我是红方"这一条推不出屏幕上该顺还是该逆。加上手册对方向的约定各赛季有别，工程上仍推荐*上场现测*（下文的叉乘定向，几行就够），而不是按颜色硬编码：现测最省心，也不会因为规则改版或换了半场就整套预测朝反方向甩枪。

搭建和调试能量机关的实体靶成本很高，社区里有基于 UE5 光追引擎的开源仿真器，可以合成带标注的打符图像与视频（大符小符、动态模糊、命中反馈都能切换）——拿来做机制讲解、生成训练数据、以及后面"离线回放验预测"的素材都很合适，是没有实体靶时的好替代。

=== 识别流水线：从一帧到"该打哪片扇叶"

识别的终点很明确：输出待击打装甲板的四个角点（供 PnP 解算）和圆心 R 的位置（供圆/转动建模）。前处理与装甲板识别同源——灰度、二值、形态学，基础篇四级级联那套"成像条件决定判据"的道理照样成立，这里只讲能量机关*特有*的三步。

==== 传统形态学：把"两种状态"变成"数子轮廓"

两种扇叶状态的形态差异非常显著，比灰度梯度好用得多。二值化之后有两条路：

- *闭操作连通法*——用一个足够大的结构元素做闭运算，把待激活扇叶中段的流水箭头和末端矩形框*连成一个整体*，得到一个"锤子"状轮廓；已激活扇叶因两侧灯条也亮，连通后面积明显更大。按面积和长宽比一筛，待击打的那片就浮出来了（闭操作后记得用同一结构元素做一次开操作，避免轮廓被撑大）。
- *漫水填充法*——先开运算把各部分分割开，再用 `cv::floodFill()` 把只有单层轮廓的区域整片染同一色，一举抹掉流动箭头、R 标和背景杂斑。漫水要遍历像素、更慢，但后处理干净。

真正优雅的判据是*轮廓层级*：`findContours` 带 `RETR_TREE` 时会给出 hierarchy，已激活扇叶内部有*三个*子轮廓（两侧灯条 + 中段），待激活扇叶只有*一个*子轮廓——数一数子轮廓就能分开两态，比模板匹配、SVM 那类"额外操作"干净得多：

```cpp
// 用轮廓层级区分两态：只有单个子轮廓的父轮廓 = 待击打扇叶
for (int i = 0; i < (int)contours.size(); ++i) {
    if (hierarchy[i][3] != -1) continue;          // 只看最外层父轮廓
    int child = hierarchy[i][2], n = 0;           // [2]=第一个子轮廓
    for (; child != -1; child = hierarchy[child][0]) ++n;  // [0]=同级下一个
    if (n == 1) target = cv::minAreaRect(inner_of(i));     // 单子轮廓→待击打
}
```

这一步能否成立，全押在前处理把"流水箭头 + 矩形框"恰好连成一体、又不把 R 标粘进去。帧率太低时流水灯和中心 R 会糊在一起，识别直接作废——所以打符对相机帧率和曝光的要求，比打装甲板更苛刻。

==== 找 R 标：圆心的两条独立来源

圆心 R 是后面一切转动建模的锚点，越准越好，好在它有两条互相独立的来源，可以加权融合：

*其一，直接检出。* R 标是一团近似圆形的亮斑，用"圆形度" $(4 pi A) \/ P^2$（面积与周长的无量纲比，越接近 1 越圆）加上先验面积范围一筛就能定位——这是「传统视觉算法」一章圆形判据的直接应用，不重讲。8 mm 镜头下 R 偏糊、12 mm 下清晰，糊的时候检出会飘。

*其二，几何外推。* 扇叶末端装甲板宽度、扇叶长度、到圆心的距离，规则图纸上都有——识别出装甲板和扇叶，按比例就能反推圆心。有一个纯传统流水线里极好用的技巧值得单独点出：*只要认准一片扇叶，绕 R 转 $plus.minus 72 degree$ 就能生成其余四片的位置假设*，再用当前帧的候选框和这些假设做 IoU 匹配，就能把五片扇叶的身份一次锁定。整条链路的命门是"R 不跟丢"——社区一个纯传统的开源实现（Python，MIT）就把 R 的跨帧关联做成了核心：用上一帧的 R 框和当前所有候选框算 CIOU、取最大者，稳稳咬住中心，其余"骚操作"全建立在此之上。

两个来源加权，比谁单干都稳。

==== 靶心几何定位与"在哪个空间里建圆"

拿到待击打扇叶后，末端装甲板的中心就是要瞄的靶心，它在以 R 为心、半径 $r$ 的圆上。这里有一个已经在理论篇裁决过的决策：*圆到底在像平面上拟合，还是在三维空间里拟合？* 「传统视觉算法」一章那道综合演练题给了答案——打符时机器人停在约 7 m 的固定激活点，风车近似正对相机，单目 PnP 在这个距离上解出的深度误差常达分米级，拿这种第三维去拟合空间圆，噪声比信号还大，所以经典做法是*降维到像平面*每帧重拟合。这套论证、以及短弧导致圆心不可观、差分放大噪声的账，理论篇都算过，此处只调用不重推。

值得补一笔的是近年的演进。随着四点关键点检测网络把角点定位做准、平面位姿用 IPPE 这类专门解法（针对共面点，比通用 PnP 稳）解算，一些队伍已经*敢在三维惯性系里直接建模*了——这和理论篇"深度太吵、宁可降维"的结论并不矛盾：三维路线是*量测足够好之后*才敢开的路，而且它靠两件事把深度噪声摁住——一是把圆心距离当作缓变状态、不逐帧猛信（给它一个偏大的测量噪声，让时间维度去平滑），二是同一帧用 R 标位置和扇叶位置做*两段观测更新*，信息更足。这类三维实现（如同济 SuperPower 的开源框架 sp_vision，MIT）把待击打装甲板固定在一个"能量机关系"里：以圆心 R 为原点、转轴为 $x$ 轴，装甲板落在 $(0, 0, 0.7 upright("m"))$——即沿*半径方向*（与转轴垂直的转动平面内）外移约 0.7 m（赛季值，以手册为准），PnP 与 EKF 都在惯性系里跑，彻底绕开了像平面每帧重拟合的麻烦。

==== CNN 一路：旋转框与角点回归

深度学习这条路和装甲板识别几乎一致：按扇叶发光特征标数据、训练、部署。两种常见做法——

- *旋转矩形检测框*：普通检测框四个参数只能框水平矩形，扇叶是斜的，得多回归一个角度 `angle`。角度定义有多种约定（OpenCV 定义、长边定义等），选不好会在 $plus.minus 90 degree$ 边界处回归突变，这属于旋转检测的通用坑，不展开。先检出整片扇叶得到 ROI，再在 ROI 内用传统方法抠末端装甲板角点，往往比直接回归角点更准。
- *角点回归*：直接回归待击打扇叶的关键点。这里有个反直觉的提醒——*不要只回归末端装甲板那四个角点*，因为已激活和待激活扇叶的角点特征差别很小（区别只在内侧角点旁有没有灯条），场地杂光一干扰就漏检误检；应当选特征更丰富、更特殊的点作回归目标。

现代的融合做法很值得学：用一个轻量检测网络（如 YOLO11 的关键点分支，导出到 CPU 上跑 OpenVINO、量化到 int8）一次性回归扇叶的六个关键点，*再用传统法补一个高精度 R 标*——取其中沿扇叶中心线的第 5、6 两个点做直线外插（外插系数约 1.4），得到 R 的粗位置，画面里有多片扇叶时把各自的外插结果求平均；再在其周围划圆形 mask，对 mask 内的轮廓按"长宽比接近方 + 离外推点近"打分选出真正的 R。这就把网络的鲁棒检出和传统法的亚像素精度接在了一起：网络负责*不漏*，传统负责*定准*。

=== 旋转预测：小符是直线，大符是正弦

识别输出的是一串带时间戳的角度 $theta_k$（装甲板相对圆心的方向角）。预测要回答的是：再过 $Delta t$（各段时延加弹丸飞行时间），这个角度会变成多少。小符和大符是两个难度截然不同的子问题。

==== 先量角速度：帧差、定向、低通

无论小符大符，第一步都是从角度序列量出角速度。最直接的是*帧差*：$hat(omega) = (theta_k - theta_(k-1)) \/ Delta t$。但「传统视觉算法」一章已经替我们算过一笔要命的账——*差分是噪声放大器*：半径 150 px 的圆上 2 px 的点噪声折合约 0.013 rad 的角度噪声，以 100 fps 做差分会放大成约 1.9 rad/s 的角速度噪声，比小符 1.05 rad/s 的信号本身还大。这笔账宣判了"裸差分出角速度"的死刑，所以量角速度必须配三件套：

- *帧差*得到相邻两帧的角度增量；
- *叉乘定向*——用"前一帧装甲板、圆心、当前帧装甲板"三点的叉积符号判旋转方向（右手定则，以前一帧为起点），这就是上一节反复强调"方向要现测"的落地手段，几行就够；
- *低通滤波*压掉高频抖动——滑动平均、指数加权这类经典低通都能用，一个实用的选择是*按变化量定权的指数加权平均*（当前帧和上一帧差得越大、权重给得越小），既跟得上又不放大毛刺。

==== 小符：匀速就是一条直线

小符匀速，$theta(t) = a t + b$，预测简单到几乎不需要"拟合"。对角度序列做间隔为 $Delta t$ 的差分，$Delta theta = a Delta t$ 是个常数，*对常数求最小二乘就是求平均*——攒一小段差分取均值就得到每帧转过的角度。更妙的是它的线性可叠加性：要预测未来第 $n$ 帧（对应飞行时间 $n Delta t$）的提前量，直接把单帧增量乘 $n$ 即可，$Delta theta(t, n Delta t) = n dot Delta theta(t, Delta t)$。所以小符预测的前置时间几乎只有一帧，收敛极快。真正要留神的仍是那个匀速值到底是不是标称的 $pi\/3$——手册没给误差范围，若你的量测够准，不妨自己拟合出实际匀速值，别盲信标称。

==== 大符：正弦转速的系统辨识

大符激活后，转速按一条正弦规律变化（某赛季取值，以当季手册为准）：

$ "spd"(t) = a sin(omega t) + b, quad b = 2.090 - a $

其中 $a in [0.780, 1.045]$、$omega in [1.884, 2.000]$（单位 rad/s），每次进入可激活状态所有参数重置、$t$ 归零、$a$ 与 $omega$ 在区间内随机取值。相位随机、方向随机，你要在场上*现辨识* $a$、$omega$、初相和偏置这几个量。

先从这条公式榨出一个能省事的结构性事实：因为 $b = 2.090 - a$，峰值 $a + b equiv 2.090$ 对*任何*合法的 $a$ 都恒定——大符转得再花，最快也就 2.090 rad/s，波谷则是 $2.090 - 2a$（$a$ 取上限 1.045 时波谷压到接近 0，风车几乎瞬停）。周期 $2 pi \/ omega in [3.14, 3.34]$ s。这些边界对*校验*辨识结果极有用：拟合出的参数一旦越界，多半是辨识发散了，直接丢弃重来。

怎么辨识？这是一个*参数已知形式、参数值未知*的正弦拟合问题，本质是非线性最小二乘，基础篇 Ceres 一节的非线性优化器与「传统视觉算法」一章的采样一致性（RANSAC）都能上，各有取舍，此处只点名不重推：

#figure(
  table(
    columns: (auto, 1fr, auto),
    align: (left, left, left),
    stroke: (x, y) => (
      top: if y == 0 or y == 1 { 1pt } else { 0pt },
      bottom: if y == 4 { 1pt } else { 0pt },
    ),
    inset: 7pt,
    table.header([*方法*], [*思路一句话*], [*代价*]),
    [DFT 频谱], [采够一个周期做傅里叶变换，最大响应峰即旋转频率], [必须先攒满一整周期],
    [非线性最小二乘], [Ceres 直接优化四参数，有解析初值时收敛快], [CPU 占用高，抢识别线程],
    [PROSAC / RANSAC], [随机采样一致拟合，对离群点鲁棒], [点要够散、够多],
    [自适应梯度下降], [当单层感知机在线学习，可上 momentum/Adam], [学习率、窗长要细调],
  ),
  caption: [四类正弦辨识方法的取舍。读表规则：第三列才是选型的决定因素——冷启动要多久、抢不抢识别线程的 CPU，比"哪个拟合得更准"更早决定你能不能在激活时限内打完。它们背后的工具，有的前面几篇备过（非线性最小二乘、采样一致性拟合），有的（如 DFT 频谱）是信号处理的常识，这里只是把它们搬到"辨识大符转速"这个具体优化问题上——识别一个已知形式的模型参数，关键永远是*合理建代价函数、给好初值*。],
)

工程上有两个特别漂亮的技巧，值得展开。

*技巧一：把差分本身当作待拟合信号。* 大符的角度—时间是 $theta(t) = A cos(omega t + C) + D t + E$（正弦的积分再叠个匀速项）。对它做间隔 $Delta t$ 的差分，用和差化积一化简，*差分曲线自己还是一条正弦*：$Delta theta(t) = A' sin(omega t + C') + D'$。妙处在于——如果把这个 $Delta t$ 直接取成*弹丸飞行时间*，那么 $Delta theta(t)$ *就是*你要打的提前量本身！于是"预测提前量"被直接转化成"拟合这条差分曲线"，一步到位。社区一个纯传统实现（MIT）实测这种一阶差分加拟合的方法，任意预测时间差都能把误差压在 $plus.minus 5 degree$ 以内。

*技巧二：固定频率把非线性拟合降成线性。* 正弦拟合难在它对 $omega$、初相非线性。但注意一个恒等式：

$ A sin(omega t + phi) + C = A_1 sin(omega t) + A_2 cos(omega t) + C $

一旦*把 $omega$ 当成已知固定值*，右边对未知量 $(A_1, A_2, C)$ 就是*线性*的——普通最小二乘（SVD）一发解出，再由 $A = sqrt(A_1^2 + A_2^2)$、$phi = op("atan2")(A_2, A_1)$ 还原幅值和相位。而 $omega$ 只在 $[1.884, 2.000]$ 这个窄区间里，随机采几十个候选值、每个都线性求解、取内点最多者，就是一个专为正弦定制的 RANSAC。前述三维实现正是这么做的：一个 EKF 逐帧跟踪瞬时状态（把正弦规律直接积分进状态转移方程），把 EKF 输出的*瞬时角速度*喂给这个 RANSAC 正弦拟合器，得到鲁棒的 $A$、$omega$、$phi$、$C$，再外推预测。这是一种很值得记住的分工：*EKF 管逐帧的快变几何，正弦拟合器管缓变的参数辨识*，两个时间尺度各司其职。

```cpp
// 固定 omega -> 正弦拟合退化为线性最小二乘；在窄频带里随机搜 omega 即成 RANSAC
for (double w : sample_omega(1.884, 2.000)) {   // 频带内随机候选
    Eigen::MatrixXd X(n, 3);                     // 列: sin(w t), cos(w t), 1
    for (int i = 0; i < n; ++i)
        X.row(i) << std::sin(w*t[i]), std::cos(w*t[i]), 1.0;
    Eigen::Vector3d p = X.bdcSvd(ComputeThinU|ComputeThinV).solve(y);  // 线性解
    double A = std::hypot(p[0], p[1]), phi = std::atan2(p[1], p[0]);
    keep_if_most_inliers(A, w, phi, /*C=*/p[2]);
}
```

#figure(
  image("images/app-rune-predict.png", width: 100%),
  caption: [大符转速辨识与它的验收（脚本 `rune_predict.py`）。上图：逐帧帧差得到的瞬时角速度是一片"噪声风暴"（灰点，噪声可比信号还大），但在一整段窗口上*拟合正弦*（粗蓝，用固定 $omega$ 的线性最小二乘）就能把 $(a, omega)$ 稳稳还原——图里拟合出 $a=0.90, omega=1.98$，真值 $a=0.90, omega=1.95$。红色虚线标出峰值恒为 2.09 rad/s 这一结构事实。下图：验收只认唯一那件事——预测的提前角有没有落在装甲板范围内。绿色带是 $plus.minus 13.5 degree$（半个装甲板弧长折算的中心角），误差整段稳在 $plus.minus 5 degree$ 以内、远在带内，判定命中。读图规则：别看瞬时速度多吵，只要拟合的正弦对、且预测误差不越绿带，就打得中。],
)

=== 完整打符链：识别 → 预测 → 时延 → 击发

前面识别给出圆心与靶心、预测给出转速模型，现在把它们和时延、弹道拼成一条真能打中会动目标的链路——这兑现了「传统视觉算法」一章末尾留下的"完整打符链路在应用篇"那笔债。链路是这样闭合的：

+ *识别*当前帧的待击打装甲板与圆心；
+ *辨识/更新*转速模型（小符匀速值、大符正弦四参数）；
+ *累加时延*得到总提前时间 $Delta t$——这是打符和打装甲板最不同的一环。$Delta t$ 不是单一的弹丸飞行时间，而是*一串时延之和*：识别耗时、通信与电控响应、以及弹丸飞行时间。17 mm 弹速上限近年规则定在 25 m/s（随赛季变动、以当季手册为准），实战为不超速要压出余量、常工作在 20～23 m/s（各家框架的默认值也落在这一带，见「自瞄算法设计」一章那笔"一个弹速引发的翻车"）；取 20 m/s 打 7 m 外的激活点，光飞行就约 0.35 s，加上前段处理，总提前量常在 0.4 s 量级。装甲板打的是近距、飞行时间短；能量机关打的是 7 m 远、飞行时间长，*时延估不准，提前量就错*，这是打符最大的误差来源之一；
+ *预测*目标在 $t + Delta t$ 时刻的角度与三维位置；
+ *弹道解算*把三维位置换成云台的俯仰角——这一步的数学（含空气阻力的抬枪补偿、迭代求解）是「实战技术篇」弹道解算一章的正题，只回指；
+ *择机击发*——不是每帧都开火，而是等云台真正对准、且预测可信时才打。

弹道和预测在这里*互相耦合*：飞行时间取决于目标距离，而目标距离又取决于你预测它转到哪——先有鸡还是先有蛋？工程解法是*定点迭代*：先用当前位置估一个飞行时间 $t_0$，按 $t_0$ 预测目标新位置、再解一次弹道得到 $t_1$；若两次飞行时间之差已经很小（比如 $< 0.01$ s），说明迭代收敛，可信；差得还大就说明目标在快速转、这一帧的解不稳，*宁可不打*。开火本身还有两道闸：*换扇叶的瞬间抑制开火*（目标刚从一片跳到另一片、云台还没跟上，这时开火必空），以及*两发之间留固定间隔*（打符靶转得慢、也不需要连发，间隔常设在 0.7 s 量级）。

还有一条来自规则的硬约束必须记进容差里：手册允许能量机关的*实际*转速与目标正弦函数存在最多约 500 ms 的时间偏差。这意味着你的预测*原理上就不可能零误差*——不过要把这 500 ms 拆开看：其中*恒定*的那一部分只是个未知的时间原点，会被在线辨识的初相 $phi$ 一并吸收掉，真正追不平的是它*时变的残余*。即便如此，残余也不容小觑（下一节会把它折算成角度算一笔账），所以整条链路必须留够容差，把"打不中一两发"当常态，靠多打几发把平均命中率做上去，而不是苛求每发必中。

=== 怎么确认预测做对了

打符的验证比装甲板更需要一把明确的尺子，因为"云台在动、靶也在动"，肉眼很难判断一发脱靶到底是预测锅还是弹道锅。给出可量化的验收方法：

*一把物理尺子：提前角误差 vs 半个装甲板。* 预测对不对，最终只归结为一件事——$Delta t$ 之后弹丸落点还在不在装甲板上。把它写成角度判据：

$ |theta_"pred" - theta_"true"(t + Delta t)| < "阈值", quad "阈值" = 0.5 dot L \/ R $

其中 $L$ 是装甲板在像面上的弧长、$R$ 是像面半径，阈值就是"半个装甲板弧长折算到圆心的张角"。按典型尺寸这个阈值约 $13.5 degree$（本章预测图的绿带就是它）。落在带内就算命中，带宽给了你一个天然的容错量。但这里要拿它算一笔账，别把容错量想得太宽裕：*时间偏差要乘转速才是角度*。大符的平均转速就是偏置 $b = 2.090 - a in [1.045, 1.310]$ rad/s，整段 500 ms 若原样不抵消，折算成 $0.5 times 1.045 tilde.op 1.310 approx 0.52 tilde.op 0.66$ rad，即 $30 degree tilde.op 37.5 degree$（若恰好落在峰值段 2.090 rad/s，可达 $60 degree$）——是 $plus.minus 13.5 degree$ 判据带的 2.2～2.8 倍，*根本兜不住*。所以能被这条带宽兜住的从来不是整段 500 ms，而是上一节说的那个"辨识吸收掉恒定部分之后的残余时间误差"；反算一下，要让折算角度不越 $13.5 degree$，残余必须小于 $0.2356\/1.310 approx 0.18$ s 到 $0.2356\/1.045 approx 0.23$ s，即*约 0.2 s 量级*。这才是对辨识精度的真实要求。

*一个不费额外硬件的自检：回放对齐。* 录一段打符视频（连时间戳一起存，最好也存下同步的云台姿态——现代框架里常有类似 rosbag 的录制工具，把图像、时间戳、四元数一起落盘），离线跑预测器，做两件事：其一，*用 0.2 s 之前的那一帧去预测"现在"*，把预测点画到当前帧上，若它和当前真实靶心重合，就直接证明了这段时延的预测精度；其二，统计整段的提前角误差分布，看均值、看最大值、看有多大比例落在 $plus.minus 13.5 degree$ 带内。前面提到的 UE5 仿真器在这里也用得上——它能吐出带*真值标注*的打符序列，让你把预测误差和绝对真值直接对比，而不用靠人眼判断重合。

*两个必须报告的工程指标。* 一是*命中率*（带内比例），这是最终 KPI；二是*首次可预测时延*——大符要靠攒一段数据才能辨识出正弦参数，从开始打符到吐出第一个可信预测常需 3 s 左右（DFT 甚至要攒满一整周期），这段"预测冷启动"时间直接决定你能不能在激活时限内打完，是选辨识方法时绕不开的权衡。小符则几乎没有这段冷启动。

一句话收束验证的哲学：*打符的预测永远有下限误差（规则允许的时间偏差里辨识吸收不掉的残余 + 量测噪声），验收不是追求零误差，而是证明误差稳定地小于半个装甲板*——图里那条不越绿带的误差曲线，就是"做对了"的全部证据。

==== 本章小结

能量机关这条识别线，把前几篇的工具重新组合成了一个*相反*的问题：不是估计未知运动，而是辨识已知模型的参数、再掐准时机。识别部分复用装甲板那套形态学与几何（两态靠数子轮廓分开、圆心靠 R 标检出与 $72 degree$ 外推双源融合、靶心在像平面或世界系里定位），预测部分则是一道干净的系统辨识题——小符是求平均的直线，大符是那条峰值恒为 2.09 rad/s 的正弦，用固定频率降成线性、或把差分本身当提前量来拟合，都是把非线性问题拆成能算的形状。最后，识别、预测、时延、弹道、择机被串成一条会互相耦合的链路，用"提前角是否落在半个装甲板内"这把物理尺子来验收。

至此，两条识别线（装甲板与能量机关）都已落地。把相机、识别、跟踪、坐标变换、预测、弹道、通信编排成一套完整、能上场、抗得住实战波动的自瞄系统，是下一章「自瞄算法设计」的任务——那里也会兑现"如何判定小陀螺、如何选开火时机"的伏笔。
