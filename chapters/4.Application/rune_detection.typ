=== 能量机关的检测与预测

前两章讨论了装甲板的单帧识别和连续跟踪。本章转向另一类目标：能量机关。两条流程都包含“识别 + 预测”，但运动先验不同。装甲板的运动主要由对手机器人决定，需要从带噪观测中估计速度和转动状态；能量机关在特定赛季采用规则给定的转速模型，视觉系统要估计的是转向、相位以及模型参数。后者更接近一个*模型形式已知、参数待估计*的系统辨识问题。

因此，本章的识别部分继续使用形态学、几何拟合和 PnP 等工具，重点放在能量机关特有的扇叶状态与圆心定位；预测部分则说明怎样把参数辨识、处理延迟、弹丸飞行时间和开火条件接起来。文中的尺寸、速度区间和状态定义来自特定赛季及公开实现，只用于说明方法，部署前必须逐项对照当季规则和本队硬件。

==== 机制：五片扇叶、R 标、与激活顺序

先明确本章算例采用的几何。五片扇叶均布在中心周围，相邻两片相差 $360 degree \/ 5 = 72 degree$；中心的 *R 标*用于定位旋转中心，每片扇叶末端包含击打区域，中段带有可用于区分状态的灯光结构。具体外形、尺寸和亮灯逻辑可能随赛季调整，不能把本节描述直接写成跨赛季常量。

本章算例用灯光结构区分两种扇叶状态：

- *待激活*扇叶——中段箭头灯*流动*，末端是一个*闭合的矩形边框*，两侧无灯。
- *已激活*扇叶——两侧灯条*亮起*，中段箭头*停止流动*。

在本章对应的规则模型中，系统需要识别当前待击打扇叶；它被激活后，再处理下一目标。激活时限、命中收益和小符/大符出现时段属于赛季规则，不在程序里长期写死。后文用到的小符匀速 $pi\/3$ rad/s（每分钟 10 转）以及大符正弦变速参数，也应视为一组带版本的算例，而不是永久不变的设备属性。

#figure(
  image("images/app-rune-geometry.png", width: 88%),
  caption: [能量机关几何示意（脚本 `rune_geometry.py`）。五片扇叶以 72° 间隔分布在 R 标周围，红、橙、灰分别表示本算例中的待击打、已激活和未亮状态。识别器先确定目标扇叶，再估计圆心与半径 $r$；这些量的误差会直接进入转角估计。若当季规则允许不同转向，程序应根据连续观测估计图像中的旋转方向，而不是仅按阵营颜色推断。已知圆心和一片扇叶后，也可以旋转 $plus.minus 72 degree$ 产生其余位置假设，但仍需与实际观测匹配。],
)

旋转方向还要区分“物理转向”和“图像中的顺、逆时针”。从旋转轴两侧观察同一运动，图像手性相反；相机安装方向或图像翻转也会改变符号。因此，即使规则规定了物理方向，也应先把观察面、相机坐标约定和图像预处理写清楚。更稳妥的工程做法是用连续观测计算方向，并用规则范围作一致性检查，而不是让颜色直接决定角速度符号。

实体靶搭建成本较高时，可以使用仿真器生成带标注的图像和视频，用于接口联调、训练数据补充与离线回放。不过，渲染图像通常不能复现真实曝光、灯光频闪、镜头污损和机械偏差；仿真结果只能验证其建模范围内的算法行为，最终仍需用实拍数据和实体靶复核。

=== 识别流水线：从图像到目标扇叶

识别器需要输出待击打区域的有序角点（供 PnP 解算）和圆心 R 的位置（供转动建模），必要时还要输出状态置信度。灰度化、二值化和形态学等前处理与装甲板识别相通，本节只讨论能量机关特有的判据。

==== 传统形态学：利用子轮廓区分状态

若曝光和阈值设置能保留灯光结构，可以利用两种状态的形态差异。二值化之后有两条常见处理路线：

- *闭操作连通法*——选择与当前目标像素尺度相匹配的结构元素做闭运算，将待激活扇叶中段的灯光与末端矩形框连接成较完整的轮廓；已激活扇叶还可能包含两侧灯条，连通后的面积和形状会不同。后续开运算可去除小噪点，但不能保证恢复闭运算前的边界，核尺寸和迭代次数必须在不同距离、曝光和运动模糊条件下验证。
- *漫水填充法*——先用形态学操作分离区域，再以 `cv::floodFill()` 填充满足条件的连通域，借此抑制内部纹理和部分背景杂点。它是否更慢、结果是否更干净取决于图像尺寸、实现方式和场景，不能脱离实测下结论。

若二值化后的拓扑关系稳定，可以直接使用*轮廓层级*。`findContours` 配合 `RETR_TREE` 会返回 hierarchy；在本节所假设的灯光结构中，已激活扇叶可能形成三个子轮廓（两侧灯条与中段），待激活扇叶形成一个。下面的代码展示基本计数方法：

```cpp
// 本赛季、本二值化配置下：单个子轮廓作为待击打候选。
for (int i = 0; i < (int)contours.size(); ++i) {
    if (hierarchy[i][3] != -1) continue;          // 只看最外层父轮廓
    int child = hierarchy[i][2], n = 0;           // [2]=第一个子轮廓
    for (; child != -1; child = hierarchy[child][0]) ++n;  // [0]=同级下一个
    if (n == 1) target = cv::minAreaRect(inner_of(i));     // 单子轮廓→待击打
}
```

这个判据依赖前处理保持预期的拓扑：既要连接属于同一扇叶的部分，又不能把 R 标或相邻亮斑粘进来。曝光过长会造成运动模糊，灯光扫描与相机采样还可能产生闪烁或混叠；这些现象都会改变子轮廓数量。调参时应按距离、转速和曝光分组统计，而不能只在少量清晰帧上确认。

==== 找 R 标：圆心的两种估计方式

圆心 R 的误差会影响半径、转角和提前量。它通常可由直接检测和扇叶几何外推两种方式估计；两者来自同一幅图像，误差可能相关，不能默认它们是独立观测。

*其一，直接检出。* 当 R 标在二值图中形成近似圆形的亮斑时，可用“圆形度” $(4 pi A) \/ P^2$（面积与周长的无量纲比，越接近 1 越圆）和面积范围筛选候选，这是「传统视觉算法」一章圆形判据的直接应用。某些公开调试记录比较过 8 mm 与 12 mm 镜头，但清晰度还取决于工作距离、对焦、像元尺寸和曝光，不能仅由焦距判断。

*其二，几何外推。* 规则图纸给出了击打区域、扇叶和圆心之间的几何关系；识别出相应特征后，可以按比例外推圆心。已知一片扇叶和圆心时，绕圆心旋转 $plus.minus 72 degree$ 可生成其余位置假设，再用交并比（intersection over union，IoU）比较位置假设与当前候选框的重叠程度。标准的完全交并比（Complete IoU，CIoU）还会考虑中心距离和宽高比一致性。本章引用材料把一项候选度量命名为 `CIOU`，却没有给出这个函数的定义；这个名称不足以说明它与标准 CIoU 采用同一公式，复用时还要沿源码确认各项计算。无论采用哪种度量，跨帧匹配都不保证在遮挡、误检或快速运动时一定跟对，因此还要设置距离门限、丢失状态与重新初始化条件。

若要融合两个圆心估计，应先在验证集上测量各自的偏差、方差和失效条件，再确定权重；简单平均不一定优于其中一个稳定来源。

==== 靶心几何定位与"在哪个空间里建圆"

拿到目标扇叶后，可以在像平面或三维坐标系中描述其圆周运动。像平面方法避免把单目深度噪声直接送入空间圆拟合，特别适用于相机位置较固定、能量机关近似正对相机的场景；但透视变化明显时，像平面轨迹并不是严格的圆。三维方法能统一处理相机姿态和目标平面变化，却依赖角点、标定、PnP 与坐标变换的精度。理论篇已经讨论过短弧圆拟合和差分放大噪声的问题，这里不再重推。选哪一种应由实拍重投影误差和预测误差决定，不能只凭“二维更稳”或“三维更完整”判断。

公开框架 sp_vision 给出了三维路线的一个具体实现：对共面角点调用 IPPE，建立以 R 为原点的目标坐标系，并按该赛季几何将扇叶点写成 $(0, 0, 0.7 upright("m"))$。其状态估计先用 R 的方位、俯仰、距离与扇叶转角更新，再用扇叶中心的方位、俯仰和距离做第二次更新；测量噪声矩阵在各自单位下把方位、俯仰的对角项设为 0.01，把距离项设为 0.5。不同量纲的数值不能直接解释为两类误差的倍数，只能说明该实现给观测分量配置了不同权重。这些是该版本源码中的设计，不足以单独证明 IPPE 或三维建模在所有相机和距离上更准。该调用路径也没有检查 `solvePnP` 的布尔返回值；复用时还需核对点序、目标坐标轴、0.7 m 规则值和噪声设置，并用正深度、重投影误差与跨帧连续性检查共面位姿。

==== CNN 一路：旋转框与角点回归

深度学习流程同样需要标注、训练、验证和部署，输出形式常见以下两种：

- *旋转矩形检测框*：在中心、宽高之外回归角度。OpenCV、长边角度等约定在周期和边界上不同；标注、损失函数、后处理和 `minAreaRect` 的约定必须一致，否则会在角度边界附近跳变。也可以先检测扇叶 ROI，再用传统方法提取局部角点，但是否优于端到端角点回归需要在同一数据集上比较。
- *角点回归*：直接回归扇叶关键点。只标末端四角时，网络可能缺少区分激活状态所需的灯光结构；加入扇叶中段或中心线上的关键点，可以让几何外推和状态分类共享更多特征。具体点集仍应通过遮挡、杂光和运动模糊样本验证。

sp_vision 的这份源码快照把网络关键点和几何外推接在了一起：YOLO11 关键点模型输出六个扇叶关键点，并在 CPU 上通过 OpenVINO 推理。

这份快照中的 OpenVINO IR 由两个同名文件配合加载：

- `yolo11_buff_int8.xml`：描述网络结构，也是多份配置传给 `read_model()` 的加载入口。
- `yolo11_buff_int8.bin`：保存配套权重。

代码再沿中心线上的两个点按约 1.4 的系数外推 R 粗位置。得到多片扇叶候选时，`get_r_center()` 会平均各自的外推结果，并在附近的圆形 mask 内按候选框长宽比和到粗位置的距离选择 R。

不过，该快照的多候选解析函数注释仍描述 15 行输出和五个关键点，截取上界也仍是 15；而循环与类常量已经按六个关键点读取：`rowRange(NUM_POINTS, 15)` 只截出 9 个数，后续循环却会读取 12 个。因而，多扇叶平均只是后续函数已经写出的处理逻辑，不能据此认定整条多候选路径已经可用。这里的 1.4、mask 半径和评分公式也都是实现参数，不具备跨相机通用性；模型文件名不能说明量化方法、精度损失或实际速度，传统后处理同样不自动等于亚像素定位。这些部分都需要用修正后的代码和标注数据验证。

=== 旋转预测：匀速模型与正弦变速模型

识别器输出带时间戳的角度 $theta_k$，即目标扇叶相对圆心的方向角。预测器要计算经过总提前时间 $Delta t$ 后的角度；$Delta t$ 包含从图像采样到控制生效的延迟以及弹丸飞行时间。本节分别讨论匀速和正弦变速模型。

==== 先量角速度：帧差、定向、低通

最直接的角速度估计是帧差：$hat(omega) = (theta_k - theta_(k-1)) \/ Delta t$。不过差分会放大量测噪声。理论篇的算例取半径 150 px、位置噪声标准差 2 px 和 100 fps；单帧角度噪声约为 0.013 rad，若相邻两帧噪声独立，差分角速度的标准差约为 $sqrt(2) times 0.013 \/ 0.01 approx 1.9$ rad/s，与 1.05 rad/s 的匀速信号同量级。实际处理至少要考虑以下三点：

- 先将角度展开到连续分支，避免跨过 $plus.minus pi$ 时出现接近 $2 pi$ 的伪差分；
- 用相邻半径向量的 `atan2(cross, dot)` 同时得到有符号增量。只取叉积符号在增量接近 0 或接近 $pi$ 时不稳定，还应设置有效半径和最大步长门限；
- 对角度或角速度做滑动窗口、指数滤波或状态估计。滤波会引入相位延迟，窗口和权重需按最终提前角误差选择，不能只追求曲线平滑。

==== 小符：匀速就是一条直线

匀速模型写成 $theta(t) = a t + b$。若采样间隔固定，角度增量 $Delta theta = a Delta t$ 的期望为常数，可以对一段展开后的角度做线性回归，也可以对若干帧差取加权平均。预测 $n Delta t$ 后的增量为 $n dot Delta theta$。窗口越长，随机噪声通常越小，但对转速变化和目标切换的响应越慢；因此冷启动并非必然只有一帧。标称的 $pi\/3$ rad/s 可作为先验或越界检查，实际是否采用固定值，应根据当季允许偏差和实测数据决定。

==== 大符：正弦转速的系统辨识

大符激活后，转速按一条正弦规律变化（某赛季取值，以当季手册为准）：

$ "spd"(t) = a sin(omega t) + b, quad b = 2.090 - a $

本章算例取 $a in [0.780, 1.045]$、$omega in [1.884, 2.000]$，角速度单位为 rad/s。即使规则给出这些区间，系统开始观测时的时间原点通常未知，等价于还要估计初相；图像中的转向符号也要按上一节的坐标约定确定。因此待估参数至少包括幅值、频率、相位和偏置，具体哪些可固定取决于当季规则。

由 $b = 2.090 - a$ 可直接得到：该模型的峰值恒为 2.090 rad/s，谷值为 $2.090 - 2a$，周期 $2 pi \/ omega$ 约为 3.14～3.34 s。这些边界适合做模型一致性检查。参数越界说明当前拟合与这组规则模型不相容，原因可能是误检、时间戳错误、尚未收敛或规则配置不匹配；处理方式可以是拒绝本次预测、扩大数据窗或重新初始化，而不是把越界一概归为同一种“发散”。

这是一个正弦参数拟合问题。可以使用离散傅里叶变换（discrete Fourier transform，DFT）频谱、非线性最小二乘、采样一致性方法或在线梯度法；它们需要的数据窗口、面对异常值时的稳定性、初始化方式和计算量各不相同。普通 DFT 直接作用于等间隔采样序列；若帧间隔明显波动，应先依据时间戳重采样到共同时间轴，或改用适合非均匀采样的方法。

随机抽样一致性（RANSAC）反复从样本中随机抽取最小集合，再按内点数或残差选择候选模型。渐进采样一致性（PROSAC）还要求样本能按可解释的先验质量排序：它先从高质量子集采样，再逐步扩大候选池。没有这种排序时，普通随机采样不应仅因反复迭代就称为 PROSAC。

#figure(
  table(
    columns: (1.1fr, 1.45fr, 2.2fr),
    align: (left, left, left),
    stroke: (x, y) => (
      top: if y == 0 or y == 1 { 1pt } else { 0pt },
      bottom: if y == 4 { 1pt } else { 0pt },
    ),
    inset: 7pt,
    table.header([*方法*], [思路一句话], [代价]),
    [DFT 频谱], [从等间隔窗口的频谱峰估计频率，再细化参数], [分辨率受窗口长度、采样率和谱泄漏影响],
    [非线性最小二乘], [联合优化幅值、频率、相位和偏置], [依赖初值与鲁棒损失，需限制迭代耗时],
    [PROSAC / RANSAC], [按质量渐进采样／普通随机采样，再选候选模型], [前者依赖可靠排序；两者都依赖阈值、次数和内点分布],
    [在线梯度法], [随新观测更新参数，可使用动量或 Adam], [学习率、尺度与收敛判据需要验证],
  ),
  caption: [四类正弦辨识方法的主要取舍。除了拟合误差，还应记录首次产生可信预测所需的窗口长度、单次更新时间、异常值比例和失效后的恢复时间。表中的代价是一般性质，不代表任一方法在当前硬件上必然更快或更准。],
)

上面的模型一次要处理多个参数。下面看两个常用的拆分办法，让每一步只解决更少的未知量。

*技巧一：拟合固定时间间隔的角度增量。*正弦角速度积分后可写为 $theta(t) = A cos(omega t + C) + D t + E$。对它取固定间隔 $tau$ 的差，所得 $theta(t+tau)-theta(t)$ 仍可写成“正弦项 + 常数项”。若 $tau$ 选为某个固定提前时间，拟合后的差分模型可以直接输出该时间间隔内的角度增量。要注意，训练样本中的未来角度要等到 $t+tau$ 才能得到，因此这只是改变拟合目标，并没有消除数据窗口和因果延迟；总提前时间变化时也要重新计算或建立含 $tau$ 的模型。公开实现给出的误差只能说明其数据与配置，不能推广为任意时间间隔都满足同一误差界。

*技巧二：固定频率后求线性参数。*正弦模型对 $omega$ 和初相是非线性的，但有恒等式：

$ A sin(omega t + phi) + C = A_1 sin(omega t) + A_2 cos(omega t) + C $

固定一个候选 $omega$ 后，模型对 $(A_1, A_2, C)$ 是线性的，可以用 SVD 最小二乘求解，再由 $A = sqrt(A_1^2 + A_2^2)$、$phi = op("atan2")(A_2, A_1)$ 还原幅值和相位。在窄频带内枚举或随机采样 $omega$，并按残差或内点数选取模型，就把多参数非线性优化转化为一维频率搜索加线性求解。sp_vision 的对应版本使用 EKF 更新瞬时几何和角速度，再将落在 $[0, 2.1)$ rad/s 的角速度状态送入 RANSAC 正弦拟合器；其实现每轮随机选择三个样本和一个频率候选，共运行配置的迭代次数。最佳候选只按内点数保留，没有再用全部内点重估；`best_result_` 也不会在每次 `fit()` 开头重置，数据窗口或运动状态改变后需要另外设计重置条件。该结构提供了可参考的职责划分，但上述样本门限、阈值、窗口和收敛质量仍需独立评估。

```cpp
// 固定 omega 后，剩余三个参数可用线性最小二乘求解。
for (double w : sample_omega(1.884, 2.000)) {   // 频带内随机候选
    Eigen::MatrixXd X(n, 3);                     // 列: sin(w t), cos(w t), 1
    for (int i = 0; i < n; ++i)
        X.row(i) << std::sin(w*t[i]), std::cos(w*t[i]), 1.0;
    Eigen::Vector3d p = X.bdcSvd(ComputeThinU|ComputeThinV).solve(y);  // 线性解
    double A = std::hypot(p[0], p[1]), phi = std::atan2(p[1], p[0]);
    keep_if_most_inliers(A, w, phi, /*C=*/p[2]);
}
```

#figure(
  image("images/app-rune-predict.png", width: 100%),
  caption: [正弦转速拟合的合成算例（脚本 `rune_predict.py`）。上图在真值曲线上加入固定随机种子的高斯噪声，并在给定频带内扫描 $omega$、对其余参数做线性最小二乘；最终拟合得到 $a=0.90, omega=1.98$，生成真值为 $a=0.90, omega=1.95$。下图从 3 s 起滚动拟合，并计算固定 0.40 s 提前时间下的角度误差；绿色区域是按示例像面半径和击打区域宽度的小角近似得到的 $plus.minus 13.5 degree$ 参考带。曲线落在带内只验证这组合成噪声和模型下的角度预测，不等同于真实弹丸命中。],
)

=== 完整打符链：识别 → 预测 → 时延 → 击发

识别器给出圆心与目标扇叶，预测器给出运动模型，系统还要把它们与时间和弹道接口连接起来：

+ *识别*当前帧的待击打区域与圆心；
+ *辨识/更新*转速模型（小符匀速值、大符正弦四参数）；
+ *计算总提前时间* $Delta t$。它至少包含图像时间戳到当前控制时刻的延迟、控制与执行延迟以及弹丸飞行时间，且各项可能重叠，不能不加区分地重复相加。作为量级示例，若水平距离取 7 m、弹速取 20 m/s，忽略阻力和竖直运动时飞行时间约为 0.35 s；再加入处理和执行延迟，总提前时间可能接近 0.4 s。这里的距离和弹速不是当前赛季结论，实际值应来自规则、测速和端到端时间测量；
+ *预测*目标在 $t + Delta t$ 时刻的角度与三维位置；
+ *弹道解算*把三维位置换成云台的俯仰角——含空气阻力的抬枪补偿和迭代求解参见「弹道解算」一章，这里不再重复；
+ *根据条件击发*，同时检查预测有效性、云台误差、目标切换状态、发射间隔和电控反馈。

弹道飞行时间取决于预测后的目标位置，而预测位置又依赖飞行时间，因此二者需要联合求解。一个简单做法是定点迭代：先由当前位置求 $t_0$，按 $t_0$ 预测目标并重新解算得到 $t_1$，继续迭代到时间或角度残差小于阈值，或达到迭代上限。sp_vision 的公开版本只做一次修正，并在 $abs(t_1-t_0) <= 0.01$ s 时接受；这个判据只能说明两次估计局部一致，不能证明模型正确或一般意义上的收敛。实现还应处理弹道无解、非有限数和目标超出可达范围。目标扇叶发生切换时，可以暂时抑制开火；固定发射间隔（该框架通过配置项给出）也应结合实际装填、热量和当季规则设置，而不是照搬某个数值。

复用这一击发路径前还要修正两个 C++ 未定义行为：该快照的普通瞄准函数与模型预测控制（model predictive control，MPC）瞄准函数都在 `get_send_angle()` 赋值前读取局部变量 `yaw`、`pitch`，成员 `switch_fanblade_` 也没有初始化，并可能在弹道解算失败后被读取。它们不影响上面对算法步骤的说明，却意味着这份源码不能直接作为击发逻辑已经可靠运行的证据。

若当季规则允许实际转速曲线相对标称模型存在时间偏差，必须把该条款的对象、时间范围和测量方式纳入误差预算。本书没有保留此前 500 ms 数字对应的赛季、手册版本、条款位置和正负口径，因此不能把它称为规则上限。下面只把 0.5 s 设为*残余时间偏差的量级假设*，用来演示如何换算角度影响。恒定时间偏移可等价为相位参数的一部分，随时间变化的残差则不能被一个固定相位完全吸收。它会与识别噪声、时间戳误差和弹道误差共同影响结果，因此开火阈值应由分项测量确定。

=== 怎么确认预测做对了

云台和目标同时运动时，仅凭肉眼难以区分角度预测、时间对齐、弹道和控制误差。验证应从可重复的离线角度指标开始，再逐步扩展到端到端实弹结果。

一个便于离线计算的参考量，是提前角误差与目标角宽的比较。在像面目标宽度 $L$ 相对圆心到目标中心的半径 $R$ 较小时，可用

$ |theta_"pred" - theta_"true"(t + Delta t)| < "阈值", quad "阈值" = 0.5 dot L \/ R $

其中 $L$ 和 $R$ 必须位于同一成像空间，$0.5 L\/R$ 是小角度近似。若把 $L$ 定义为圆上的弦长，精确半角为 $arcsin(L\/(2R))$；实际矩形经过透视后，还应直接由标注角点和圆心计算可用角宽。按图中示例 $L=300$ px、$R=636.42$ px，简式给出 $13.50 degree$，弦长模型给出 $13.63 degree$。提前角落在这个参考带内，并不直接证明弹丸命中：还没有计入扇叶平面透视、击打区域形状、相机与枪口外参、弹道和控制误差。

这个参考带可以用来估算时间误差的量级。算例中的平均项为：

$ b = 2.090 - a in [1.045, 1.310] "rad/s" $

若假设的 0.5 s 偏差完全残留，按常速近似会产生约 $30 degree tilde.op 37.5 degree$ 的角度误差；若处在 2.090 rad/s 的峰值附近，则约为 $60 degree$。反过来，只考虑这一项并要求角度小于 $13.5 degree$，残余时间需约小于 0.18～0.23 s。这个计算只说明上述条件算例的量级，不是规则限值或完整的辨识精度要求，因为实际角速度随时间变化，其他误差也要占用同一容差。

一种低成本检查是回放对齐。录制图像原始时间戳、同步云台姿态、检测结果和预测输出；离线用 $tau$ 之前的观测预测当前目标，再与当前帧标注比较。应统计有符号误差、绝对误差分位数、越界比例，并按转速区间、方向、曝光和遮挡分组。若使用仿真真值，还要把结果限定在仿真器的成像与运动模型内。这样的回放验证的是观测到预测这一段；只有加入控制反馈、枪口状态和实际落点，才能评估完整击发链路。

至少应分别报告两类指标：一类是离线角度指标，包括误差分位数、参考带内比例和异常样本；另一类是可用性指标，包括首次可信预测所需时间、连续丢帧后的恢复时间和单帧计算延迟。本章脚本为了展示滚动拟合，从 3 s 开始计算误差，这不是“所有算法都需要 3 s”的测量结论。端到端测试还应报告实际落点或命中结果，并注明样本量、距离、弹速、规则版本和失败分类。

验证的目标不是让一条合成曲线看起来平滑，而是说明每一级误差在规定条件下占用多少容差，并用独立数据检查总误差。图中的绿色带只是一项角度参考，不能替代真实系统的端到端验收。

==== 本章小结

能量机关识别复用了形态学、轮廓、关键点、PnP 和状态估计，但它的运动预测更依赖规则模型。传统方法可利用子轮廓与五等分几何，网络方法可输出扇叶关键点并结合 R 标后处理；像平面和三维建模各有适用条件。匀速模型可由窗口回归估计，正弦模型则可通过固定频率后求线性参数来降低优化难度。完整系统还必须统一图像时间戳、执行延迟、弹道飞行时间、目标切换和开火条件，并将离线角度验证与端到端落点验证分开。

至此，装甲板和能量机关两条识别流程都有了明确的输入、输出和验证边界。下一章「自瞄算法设计」讨论怎样组织相机、识别、跟踪、坐标变换、预测、弹道和通信模块，并说明小陀螺判定与开火时机放在哪一层处理。

（本章三维检测与预测参照同济 SuperPower 战队公开的 `sp_vision_25`，文中代码与示意脚本为讲解目的重写。传统检测与 Unreal Engine 5（UE5）仿真器部分根据社区公开方案整理，但没有绑定到可复现的仓库版本，因此不引用其性能数字作为结论。）
