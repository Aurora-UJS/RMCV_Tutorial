#import "/template/template.typ": *

=== 找到瓶颈之后：怎样排优化顺序

「基础篇」软件工程基础一章给出了性能工作的基本循环：测量 $arrow.r$ 分析 $arrow.r$ 优化 $arrow.r$ 验证；计算机系统基础一章介绍了 `perf`、火焰图和缓存指标，还用一次自瞄掉帧定位说明怎样回答“时间花在哪里”。

定位瓶颈后仍然要做取舍。编译选项、线程数、CPU 亲和性、实时调度、模型精度和 Jetson 功耗模式都可能改变结果，但收益、风险和适用范围并不相同；有些只需改一行配置，有些需要重新采集数据和验证整机行为。

本章讨论怎样用现有测量排定顺序：先确认哪一项确实限制当前系统，再比较可获得的收益、实施成本和回归风险。

本章不重复 `perf`、火焰图、构建类型、取流解耦和推理引擎的基础用法。相关机制分别见「计算机系统基础」「CMake 与构建系统」「工业相机」「装甲板检测与识别」和「自瞄算法设计」；这里补充的是端到端预算、局部测量与部署代价。

本章依次完成三件事：把稳定延迟与逐帧变化分开；用生成代码和局部记录判断编译、线程与通信选项；最后把候选项整理成一份需要在目标机器上复核的实施顺序。

#note("本章本地记录所用环境")[
g++ 13.3.0 ／ OpenCV 4.6.0（以 TBB 作为线程并行后端）／ ROS 2 Jazzy（`rmw_fastrtps_cpp`）／ Intel i7-13790F（16 核 24 线程，支持 x86 向量指令扩展 AVX2、不支持 AVX-512）／ Linux 7.0.0-28。TBB 使 OpenCV 的部分操作能够拆到多个线程执行；AVX2 与 AVX-512 则让适合向量化的运算有机会用一条 CPU 指令同时处理多个数据元素。下文的本地数字只描述这台桌面 x86 主机、相应程序和当次负载；其中编译基准可以重跑，OpenCV 与 ROS 2 两组数据则只保留了汇总值。NUC、Jetson、其他 OpenCV 后端及不同 ROS 2 RMW 都需要重新测量。引用数字时应同时保留环境、输入、样本量和统计量；原记录缺少其中某项时，也应一并说明。
]

=== 延迟预算：把稳定偏差与逐帧变化分开

==== 常数补偿不能覆盖逐帧变化

「实战技术篇」弹道解算一章列过一张“从曝光到命中”的时间预算表：曝光参考到图像可用、识别与预测、上下位机通信、云台跟随、供弹到出膛和弹丸飞行。在运动模型与时间戳定义成立时，其中一部分稳定偏差可以合并进补偿量 $t_d$。

补偿不会让链路本身变快，也不能修正没有被模型描述的变化。常数延迟仍会影响控制响应和可用时间窗；如果延迟随帧变化，只使用一个 $t_d$ 还会留下时序误差。

因此，同一张预算表在优化阶段要多回答三个问题：这一段的分布是什么，视觉侧能改变多少，修改后会引入什么代价。

设实际延迟为 $L = mu + delta t$，补偿量采用均值 $mu$。在目标横向速度近似恒定、其他模型误差暂不计入时，残余位置误差的一阶近似为

$ e approx v delta t $

若 $delta t$ 的标准差为 $sigma_t$，则位置误差的标准差约为 $abs(v) sigma_t$。这是用于分配时间预算的简化关系，不是完整命中模型：目标加速度、装甲板旋转、云台控制和弹道误差仍要单独处理。

例如，装甲板以半径 $r = 0.25$ m、角速度 $omega = 10$ rad/s 旋转时，切向速度为 $v = r omega = 2.5$ m/s。若暂以 67.5 mm 作为示意容差，残余时间误差需小于约 27 ms；$omega = 24$ rad/s、$v = 6$ m/s 时则约为 11 ms。67.5 mm 只是把 135 mm 参考跨度折半得到的几何示例，不能替代实际开火判据。

于是，延迟均值与分布尾部需要分开记录。一个均值较小但尾部很长的环节，可能比更慢却稳定的环节更难用常数补偿；反过来，稳定延迟也可能因为压缩了控制和开火时间窗而值得降低。

排序不能只看均值，也不能只看一个“抖动”数字。先检查超预算帧来自哪里，再结合均值、尾部分位数、可控程度和任务时限决定先后。

==== 一张用于定位的预算表

下面把全链路展开。表中数字来自前文量级、本章留存的局部记录和外部公开测量；没有可靠数字的环节则明确留空。它们不是同一台机器人上的联合实测，有些行还采用了不同的阶段划分，不能直接相加成一条已验证的总延迟。

#figure(
table(
  columns: (auto, auto, auto, auto),
  align: (left, center, left, left),
  stroke: (x, y) => (
    top: if y == 0 or y == 1 { 1pt } else { 0pt },
    bottom: if y == 10 { 1pt } else { 0pt },
  ),
  inset: 6pt,
  table.header([*环节*], [耗时量级或范围], [逐帧变化], [能不能压、代价是什么]),
  [曝光参考到图像可用（前文量级）], [约 1～20 ms], [受曝光、读出、传输与调度影响], [先统一时间戳事件，再在目标相机和驱动上拆分测量；自动曝光还会改变曝光时长],
  [排队等待], [由前方帧数、消费间隔和溢出策略决定], [负载失衡时可很大], [最新帧语义可用容量 1 与覆盖旧帧限制积压；录像和完整序列任务不能照搬],
  [预处理（本章留存记录）], [0.188～0.838 ms], [五档线程设置的汇总值], [同时看墙上时间与聚合 CPU 时间；原记录没有保留样本分布],
  [推理（外部测量）], [FP16 平均迭代 17.44 ms], [该文未给逐帧分布], [同文另报 58.82 FPS；先在目标模型、精度与功耗模式下复测],
  [PnP 与滤波], [本章未单独测量], [前文只给识别、PnP 与预测的合计量级], [先用自身剖析确认占比，再决定是否排在更大瓶颈之后],
  [节点间传递（本章留存记录）], [0.8～675.6 µs], [随消息大小与通信路径变化], [进程内路径可降低本地传递成本；外部订阅会增加另一条发送路径],
  [打包与串口（前文量级）], [约 2～15 ms], [取决于帧长、速率与周期], [按实际链路计算线时并测量排队；提速需要上下位机共同验证],
  [云台跟随（前文量级）], [数毫秒～数十 ms], [取决于控制器与机构], [通常是动态响应而非纯延迟，仍应纳入端到端时间预算],
  [供弹到出膛（前文量级）], [数十～数百 ms], [随机构相位、连发状态和卡滞变化], [由机械、电控与开火判据共同处理，并用发射事件测量],
  [弹丸飞行（前文量级）], [100～600 ms], [随距离和弹速变化], [属于预测时域中的物理飞行时间，按弹道模型计算],
),
  caption: [端到端时间预算的定位版本。OpenCV 与节点间传递两行只保留了汇总结果，没有配套采样程序、原始样本和完整统计口径。外部推理数据来自 arXiv:2502.15737 在 Jetson Orin NX、YOLOv8n 640×640、TensorRT FP16 下报告的平均迭代 17.44 ms；同文表格另报 58.82 FPS，二者接近但并非完全互为倒数，也都不是本书自瞄模型的逐帧延迟分布。各行用于提示应测哪些区间，不能直接相加。],
)

排队等待值得优先排查，因为它可能来自配置而不是算法，但队列深度本身不能换算出等待上界。普通先进先出队列中，一帧要等待前方各帧的实际处理时间，还要加上当前处理的剩余时间和调度延迟；队列满时采用阻塞生产、丢新帧还是覆盖旧帧，也会产生不同结果。若 100 fps 的输入缓冲始终只保留最新 5 帧，最旧与最新时间戳的跨度约为 4 个采集周期，即 40 ms；这只是该覆盖策略下的帧龄跨度，不是普通 FIFO 的通用等待公式。对只关心最新观测的识别链，容量 1 并覆盖旧帧可以避免处理多帧旧数据，但消费者被唤醒前仍有等待，调度和复制也不会消失。录像、逐帧计数或依赖完整序列的算法不能照搬这一策略。

#figure(
  image("images/adv-perf-latency-budget.png", width: 100%),
  caption: [时间预算的两个视图（脚本 `adv_latency_budget.py`）。左图只画有可追溯数值端点的行；预处理和节点间传递是缺少原始样本的留存范围，FP16 是外部论文报告的单个平均值，均不能把横线宽度解释为逐帧抖动。排队、云台跟随和供弹的数值取决于策略或工况，因此保留在表中而不人为补画区间。右图按一阶关系 $abs(e) approx abs(v delta t)$ 展示残余时间误差如何转成横向位置误差；27 ms 与 11 ms 是在 67.5 mm 示意容差下推导出的交点，不是实测命中分布。],
)

==== 抖动怎么量，又怎么边跑边量

「基础篇」软件工程基础一章已经说明：持续时间使用单调时钟，平均值之外还要看高分位数与超预算帧占比。下面关注怎样持续采集，并限制测量本身的开销。

均值与标准差不需要保存全部样本，可以用 Welford 算法在线更新；它们适合描述主体分布。若更关心慢帧尾部，可以同时报告

$ J_99 = p_99 - p_50 $

即 99 分位与中位数之差。$J_99$ 是尾部展开程度，不等于标准差 $sigma_t$，也不能在没有分布假设时直接代入前面的公式。最大值仍可记录，但应连同样本量和测试时长解释。

热路径上的埋点应遵守几条原则，避免测量过程成为新的瓶颈：

- *热路径不直接输出日志*。`printf`、`std::cout` 和日志后端可能加锁或阻塞；热路径只写入内存，低优先级线程再汇总输出。
- *预分配记录空间*。固定长度环形缓冲区可以避免 `vector` 扩容；多线程写入时还要避免所有线程争用同一个原子计数器。
- *按范围选择表示*。`uint32_t` 微秒值约 71.6 分钟后溢出，记录单段耗时通常足够；更长时间戳应使用 64 位类型，并明确溢出处理。
- *为尾部分位数选择合适结构*。固定桶宽直方图能以常数内存近似分位数，但要设置下溢、上溢桶并说明 0.5 ms 等量化误差；也可以使用经过验证的在线分位数算法。

Linux 在所支持的架构和时钟类型上通常通过 vDSO 提供 `clock_gettime`，避免每次进入内核；但具体开销仍受平台、时钟源和调用方式影响。部署前可用紧循环测量一次打点成本，并把启用与关闭埋点的端到端结果作对照。只要测量开销远小于目标预算，就可以保留足够密的阶段时间戳，而不必依赖估算。

=== 编译器：生成代码和运行时间要一起看

==== `-march=native` 到底有没有用

构建类型选 `Release` 的意义已经在「基础篇」CMake 与构建系统一章说明。这里继续追问：在 `-O2` 或 `-O3` 之后，针对目标 CPU 生成代码是否还能缩短当前热点？

`-march=native` 会根据编译机启用编译器识别到的指令集和处理器特征，并可能改变向量宽度、指令选择与调度。在 x86-64 上，较宽的 AVX2 向量有机会让一次指令处理更多像素，但向量宽度翻倍不等于程序必然快一倍：访存、循环结构、降频和额外的数据重排都可能限制收益。

配套程序把二值化、3×3 卷积和归约组成一段 1440×1080 的固定负载。本章环境的一次记录为：`-O0` 11.45 ms、`-O2` 1.49 ms、`-O3` 0.36 ms、`-O3 -march=native` 0.40 ms，加入 LTO 后仍约 0.40 ms。这里能直接得到的观察只有两点：优化等级对这段程序影响很大，而这次测试没有显示 `-march=native` 比 `-O3` 更快。它不代表其他视觉热点也没有收益。

计时说明结果，生成代码则帮助解释编译器做了什么。可以先用反汇编作一次粗略普查：

```bash
# 统计反汇编中提到相应寄存器的行数；这不是严格的指令数
objdump -d bench | grep -c '%ymm'   # 256 bit 向量寄存器
objdump -d bench | grep -c '%xmm'   # 128 bit 向量寄存器
```

这里统计的是已链接可执行文件，所以启动代码、`main` 和内联进来的函数都会计入，也不能说明这些指令是否位于热点。更有针对性的办法是查看编译器的向量化报告：

```bash
g++ -O3 -march=native -fopt-info-vec -c adv_compile_flags_bench.cpp
```

在本章这份源码与工具链上，报告显示三个循环的主向量版本在 `-O3` 下使用 16 字节向量，在 `-O3 -march=native` 下使用 32 字节向量。卷积函数接收两个指针，编译器无法在编译时确认它们是否指向重叠区域，因此生成了两套循环：运行时确认不重叠时走较宽的向量版本，否则走更保守的回退版本。这说明更宽的向量代码确实生成了，却仍不能证明它带来收益。反过来，即使反汇编里没有 `ymm`，`-march=native` 也可能通过 FMA、BMI、标量指令、调度或 `-mtune` 产生变化。因此，`ymm` 的行数只适合回答“目标文件中是否出现这类寄存器”，不能单独证实或证伪性能收益。

为什么本次计时没有更快？二值化每帧约读 1.55 MB、写 1.55 MB，而算术工作很少；观测结果与缓存或访存吞吐限制相符。不过，这仍是候选解释。没有硬件计数器、不同数据规模或热点隔离测试，就不能排除指令端口、频率变化和测量噪声等因素。本章不把其中任何一种写成已确认的原因。

反汇编和向量化报告的优点是：对同一份二进制或同一编译器、源码与选项，它们不会随运行时负载变化。计时则会受频率、温度和调度影响。书中固定记录将每档程序钉在 CPU 2，先在同一进程中预热 20 次，再连续测量 300 个迭代；这些不是 300 次独立启动。图中给出各段中位数与每帧总耗时的 p99，但仓库没有保留这次记录的原始样本，因而无法事后改用其他分位数算法重算。配套的 `adv_compile_flags_bench.py` 默认只重绘固定汇总值；传入 `--measure` 才会在当前机器重新编译、普查并生成一组新的中位数与 p99。这样既能复现书中图片，也不会把一次有噪声的重跑悄悄当成原始数据。

#figure(
  image("images/adv-compile-flags.png", width: 100%),
  caption: [同一段流水线在五组编译选项下的固定测试记录（脚本 `adv_compile_flags_bench.py`；i7-13790F、g++ 13.3、1440×1080、CPU 2、每档预热 20 次后连续测量 300 个迭代）。左图堆叠三段耗时的中位数，并用短横线标出每帧总耗时的 p99；右图以 `-O2` 为基准比较后续选项。本次记录中，`-O3` 明显快于 `-O2`，而 `-march=native` 与 LTO 没有继续缩短总时间。这个结果只适用于所列环境和负载；生成了 256 bit 向量代码是另一项观察，不能据此把未提速的原因确定为访存瓶颈。],
)

部署代价同样需要验证。`-march=native` 生成的二进制面向编译机；若目标机缺少某条实际执行到的指令，程序会收到 `SIGILL`，但不一定在启动时立即发生。编译机与机器人不一致时，应选择团队明确支持的目标基线，并在最低配置的目标机上运行测试。

`-march=x86-64-v3` 是一种可移植范围更明确的 x86-64 目标，但它要求的不只是 AVX2，还包括 AVX、FMA、BMI1/2 等一组特性。因此，`lscpu | grep avx2` 不能完成兼容性检查。可以用部署工具链的 CPU 特性检测，并在实际目标机执行包含热点路径的测试；ARM 平台则需要选择相应的架构选项，不能套用 x86-64 的分级。

==== LTO 与 PGO

*LTO*（Link Time Optimization，链接期优化）让优化器跨翻译单元分析代码，因而可能增加内联、删除不可达代码或传播常量。收益取决于热点是否受翻译单元边界限制，也取决于链接器和依赖库是否参与 LTO；代价包括更高的链接时间与内存占用，以及更复杂的调试和构建兼容性。本章合成负载的热循环都在同一文件，实测没有显示额外收益，但这个结果不能推广到完整工程。

*PGO*（Profile-Guided Optimization，按剖面引导的优化）通常分两次构建：先用 `-fprofile-generate` 生成插桩程序，让具有代表性的输入覆盖实际路径，再用 `-fprofile-use` 重编。剖面可影响分支布局、内联和代码排布；即便数值循环占主要时间，外围控制流也可能受益。主要风险是剖面不具代表性或已经过期，所以采样录像、敌方类型、功能开关和部署配置变化后都要检查覆盖率并重测。

本书没有在完整 RM 系统上测量 LTO 与 PGO，因此不提供通用收益比例。只有剖析显示编译和代码布局仍限制热点时，才值得把它们加入候选，并用同一组输入比较端到端分布。

=== 线程数：局部耗时与整机负载要一起看

==== OpenCV 线程数不是越多越好

OpenCV 的部分图像操作会通过并行后端拆分任务，具体后端和默认线程数由构建方式与运行环境决定。本章机器的构建信息显示后端为 TBB，`cv::getNumThreads()` 返回 24，与逻辑 CPU 数相同。这个返回值描述本机后续并行区域可使用的线程数，不表示每次调用都会同时占满 24 个核。

仓库保留了一次对 `cvtColor`、`GaussianBlur` 和 `threshold` 组成的 1280×1024 预处理所做的线程扫描。绘图脚本只有五组汇总值，没有采样程序、原始样本、样本量、统计量定义，也没有说明“聚合 CPU 时间”的具体计时接口。因此，下表适合演示怎样同时比较墙上时间与 CPU 占用，不能作为可独立复现的 OpenCV 基准：

#figure(
table(
  columns: (auto, auto, auto, auto, auto),
  align: (center, center, center, center, center),
  stroke: (x, y) => (
    top: if y == 0 or y == 1 { 1pt } else { 0pt },
    bottom: if y == 5 { 1pt } else { 0pt },
  ),
  inset: 7pt,
  table.header([*线程数*], [墙上时间], [加速比], [CPU 时间], [并行效率]),
  [1], [0.838 ms], [1.00×], [0.911 ms], [100%],
  [2], [0.422 ms], [1.99×], [0.872 ms], [99%],
  [4], [0.238 ms], [3.52×], [1.060 ms], [88%],
  [8], [0.249 ms], [3.37×], [2.040 ms], [42%],
  [24（记录中的默认值）], [0.188 ms], [4.46×], [3.570 ms], [19%],
),
caption: [OpenCV 线程数扫描的留存汇总值。从 4 线程增加到 24 线程，墙上时间由 0.238 ms 降至 0.188 ms，而记录中的聚合 CPU 时间由 1.060 ms 增至 3.570 ms；4 与 8 线程的墙上时间接近，后者的 CPU 时间约为前者 1.9 倍。缺少采样程序与统计口径意味着这些差值不能独立复现；记录也没有覆盖其他 ROS 2 节点，因此不能单凭此表断定整机抖动已经恶化。],
)

单看留存的局部墙上时间，24 线程是五档中最快的一档；把其中记录的聚合 CPU 时间也纳入后，4 线程附近成为值得继续测试的候选。更多线程会占用更多调度和计算资源，因此可能压缩跟踪、串口或导航节点的余量，但是否造成整机退化必须在这些节点同时运行时测量，不能由局部曲线直接推出。

可以在程序启动、尚未进入并行区域时设置一个候选值：

```cpp
cv::setNumThreads(4);   // 留存记录中的候选点；先在目标机器重新扫描
```

在这组留存数字中，4 线程的墙上时间相对单线程缩短约 3.5 倍，聚合 CPU 时间则增加约 16%，所以它可以作为本机重新采样的起点，而不是已经验证的推荐值。实际部署应至少扫描单节点和整机联合负载两种场景，同时比较吞吐、p99、超预算帧与其他节点的时限。Jetson、同构 x86 和不同 OpenCV 并行后端的拐点都可能不同。`cv::setNumThreads` 不会直接限制应用自行创建的 `std::thread` 或独立线程池；但在 TBB 后端中，同一进程内其他 TBB 并行代码可能与 OpenCV 共享调度资源或并发上限，所以仍要结合整套线程结构验证。

#figure(
  image("images/adv-perf-opencv-threads.png", width: 100%),
  caption: [OpenCV 线程扫描留存值的墙上时间、聚合 CPU 时间和按单线程墙上时间推导的并行效率（脚本 `adv_opencv_threads.py`，数据同上表）。灰色虚线标出记录中的 24 线程默认值，蓝色竖线标出供重新采样的 4 线程候选值。阴影只表示两种时间指标之间的数值差，不等同于“浪费”，因为并行工作本来就会累计多个线程的 CPU 时间。],
)

==== 进程内通信，以及录包增加的路径

「RoboMaster应用篇」自瞄算法设计一章介绍了 ROS 2 进程内通信的动机与配置：把相机和识别组件放进同一进程，并启用 `use_intra_process_comms`。这里补充固定环境下的量级，以及外部订阅者出现后会增加什么工作。

在同进程、同执行器、`sensor_msgs/Image`、`SensorDataQoS` 深度 1，并以 `unique_ptr` 发布的固定测试中，曾留下如下记录。当前仓库没有附生成这组数字的基准程序，也没有保留样本量和统计口径，因此它只能提示可能的量级，不能作为可独立复现的基准：

#block(breakable: false)[
  #figure(
  table(
    columns: (auto, auto, auto, auto),
    align: (center, center, center, center),
    stroke: (x, y) => (
      top: if y == 0 or y == 1 { 1pt } else { 0pt },
      bottom: if y == 4 { 1pt } else { 0pt },
    ),
    inset: 7pt,
    table.header([*消息大小*], [intra 关], [intra 开], [提速]),
    [0.23 MB], [14.1 µs], [0.8 µs], [18×],
    [0.92 MB], [68.5 µs], [0.8 µs], [86×],
    [3.93 MB], [378.8 µs], [1.6 µs], [237×],
    [6.91 MB], [675.6 µs], [2.3 µs], [294×],
  ),
  caption: [ROS 2 进程内通信的一次留存记录。关闭时，记录值随消息大小增长；开启时，四档负载均为微秒量级。曲线与减少序列化及载荷拷贝的预期一致，但缺少基准源码、样本量和统计口径，不能独立复现，也不能由曲线形状证明具体的数据移动次数。结果还取决于 ROS 2 版本、RMW、发布类型、订阅者数量、QoS 与执行器。],
  )
]

若 `rviz2` 或 `ros2 bag record` 从另一个进程订阅同一话题，发布端还要为进程外路径准备并发送消息。不过，本地订阅者仍可继续使用进程内路径；外部订阅不会让本地快路径整体失效。新增路径是否造成掉帧，取决于消息大小、发布频率、RMW、存储设备、压缩方式和剩余算力，需要在实际录包配置下复测。

可视化与录包因此都应纳入端到端预算。比赛时可以关闭没有诊断价值的高带宽可视化；录包则要在可复现性与资源开销之间取舍。常见控制手段包括只记录必要话题、降低图像频率或分辨率、采用合适的压缩与存储，并确认这些改动不会丢失故障分析所需的信息。下一章会从赛场故障复盘的角度继续讨论这项取舍。

=== 调度只能处理一部分时序抖动

==== 先区分正确性问题与时序问题

「基础篇」计算机系统基础一章说明过：CPU 亲和性和实时优先级不能修复竞态。这里讨论它们对时序分布的影响，两者并不矛盾。

- 竞态是*正确性*问题。`++counter` 的读、改、写之间只要仍可交错，错误就仍可能发生。改变调度最多影响复现概率，必须用原子操作、锁或重新设计所有权来建立同步关系。
- 时序抖动是*性能*问题。若测量显示长尾来自线程迁移或同核可运行任务的竞争，亲和性、cpuset 或调度策略可能减少这部分延迟；它们不能消除锁竞争、IRQ、缺页、内存带宽、I/O 阻塞和算法自身的变化。

因此，先用调度延迟、上下文切换和线程运行状态确认长尾与调度有关，再决定是否调整策略。不能因为某个优化降低了 p99，就反过来宣称所有抖动都由调度造成。

==== 亲和性、隔离与实时优先级

下面几种工具解决的问题不同，通常从可撤销、可观测的改动开始。

*CPU 亲和性*。`taskset -c 4,5 ./auto_aim` 限制进程及其继承该掩码的线程可在哪些 CPU 上运行。它不是独占：其他任务仍可被调度到这些 CPU。减少迁移可能提高缓存局部性，但迁移也不等于原有 L1/L2 数据“全部作废”；实际收益要用迁移次数和延迟分布验证。若只想约束少数关键线程，还要按线程设置并检查掩码。

*cpuset、IRQ 与核隔离*。cpuset 是 cgroup 提供的 CPU 集合，可以把应用和普通任务分开；启动参数 `isolcpus` 能减少普通调度负载均衡，却不会把内核活动全部迁走。硬件中断由 IRQ affinity 决定目标 CPU，RCU 回调是内核延后执行的清理工作，不可绑定的 workqueue 则是可在多个 CPU 上运行的内核后台任务。可以先从 `/proc/interrupts` 查看关键 CPU 上仍有哪些中断，再用线程级跟踪确认长尾时运行的是应用线程、内核线程还是后台工作；RCU 与 workqueue 的迁移接口随内核配置而异，应查当前内核文档后再改。隔离会减少系统其他任务可用的算力，配置前后都要测整机吞吐和长尾。

*实时调度策略*。`SCHED_FIFO` 和 `SCHED_RR` 是逐线程策略，实时线程在就绪时可抢占普通线程。它们只适合执行时间有上界、不会长时间持锁或阻塞关键资源的线程。优先级必须根据线程依赖关系安排，没有可跨项目照搬的 80 或 99；设错可能造成优先级反转、普通任务饥饿，甚至让终端和看门狗无法及时运行。应先在可恢复环境中测试，保留非实时监控与降级路径，并用 `chrt -p` 或线程级工具确认实际策略。

*PREEMPT_RT* 进一步缩短内核中不可抢占区间，但效果取决于内核版本、硬件、负载与驱动。本章没有在目标机器人上做 PREEMPT_RT 对照测试，因此不引用其他平台的最坏延迟数字作为预期收益。只有测量表明剩余长尾主要发生在内核调度路径，并且相机、GPU 与串口驱动都能在目标内核上稳定工作时，才适合评估这一选项。

=== 分配与拷贝：`cv::Mat` 真正贵的地方

「基础篇」软件工程基础一章介绍过：`cv::Mat` 的普通复制只复制带引用计数的矩阵头，通常不会复制全部像素；`clone()` 则会复制图像数据，其成本随数据量增长。本节继续讨论别名和外部缓冲区所有权。

第一件是*浅拷贝带来的别名*。`cv::Mat roi = frame(rect);` 得到的是同一块内存的视图，修改 `roi` 也会修改 `frame`。若下游需要未标注的原图，就应在绘图前分离数据，或对需要独立所有权的部分显式 `clone()`；若只读共享，浅拷贝正是合适的做法。

第二件是*外部缓冲区的所有权*。相机 SDK 常把循环缓冲区借给调用者，并要求通过 `FreeImageBuffer` 一类接口及时归还。用 `cv::Mat` 包装这段地址，并不会让 OpenCV 的引用计数接管 SDK 的生命周期。如果先归还再异步消费，数据可能已被下一帧覆盖；如果一直不归还，SDK 缓冲池可能耗尽。

是否必须复制，不由“有没有跨线程”决定，而由所有权决定：若 SDK 只提供短期借用，入队前需要复制到应用拥有的缓冲区；若接口允许延长所有权，或能把释放回调随对象一同转交，就可以安全地把拥有型缓冲传给消费者。无论哪种方案，都应在队列类型中体现生命周期，并测试停止、丢帧和异常路径是否最终归还缓冲。

减少分配时，可以先复用尺寸和类型稳定的输出 `Mat`。OpenCV 函数在输出缓冲满足要求时通常会复用已有存储；尺寸、类型变化或某些实现细节仍可能触发重新分配。把灰度图、二值图和梯度图等中间结果放进具有清晰线程所有权的工作区，通常比直接引入通用内存池更容易验证。若多个线程共享同一工作区，则还要避免并发写入。

=== Jetson：确认功耗模式与数据路径

Jetson 的可用 CPU/GPU 频率、在线核心数和功耗上限受 SKU、JetPack 版本与当前 `nvpmodel` 配置影响。不要根据“出厂默认”猜测性能档；先在目标板上查询当前模式，再记录温度、频率、功耗和是否发生热降频。

`nvpmodel` 用于查询或切换预定义功耗模式；`jetson_clocks` 可在当前模式允许的范围内请求较高且稳定的时钟。不同平台支持的模式和具体行为并不完全相同，应以该 SKU 的 NVIDIA 文档和命令输出为准。较高模式可能增加功耗与发热，散热不足时仍会降频，因此设置前后都要在持续负载下复测，而不是只看短时峰值。

Jetson 的 CPU 与集成 GPU 共享物理内存，但这不等于主机到设备的数据处理自动消失。普通内存、CUDA 统一内存、页锁定映射、NvMM 与推理引擎输入缓冲的访问方式不同，还可能发生页迁移、格式转换、缓存同步或显式复制。GPU 直接读取映射内存也会占用共享内存带宽。应沿实际的相机缓冲、预处理和 TensorRT 输入路径打点；若相机 SDK 能交付兼容的拥有型缓冲，再验证同步和生命周期后，才可以确认省掉了哪一次复制。

最后补充一组外部量化数据。一篇公开论文（arXiv:2502.15737）在 Jetson Orin NX、YOLOv8n、640×640 与特定 TensorRT 配置下，将 5000 张图像的孤立推理均值记为模型推理速度。正文报告 FP16 平均迭代时间为 17.44 ms，表格则分别给出：

$ "FP32" 52.19 "FPS" quad arrow.r quad "FP16" 58.82 "FPS" quad arrow.r quad "INT8" 65.83 "FPS" $

同一表中，FP32、FP16、INT8 的 mAP50-95 分别为 0.842、0.844、0.719；FP16 相对 FP32 的帧率约增加 12.7%，INT8 约增加 26.1%。58.82 FPS 的倒数约为 17.00 ms，与正文的 17.44 ms 接近但并不相同，因此本章保留论文各自报告的统计量，不把 FPS 倒数改写成逐帧延迟。这些是该论文 UAV 数据集和孤立推理设置下的观察，不是 RoboMaster 装甲板数据，也没有给出本书整条视觉链的端到端收益。FP16 可作为目标平台上的优先候选，但仍要检查模型输出和数值稳定性；INT8 还依赖有代表性的校准集，应在自有验证集、不同距离与光照下复测精度，再决定帧率收益是否值得。

=== 实施顺序：从已测瓶颈出发

没有一份跨机器通用的性能优化排名。下面按照“先排除构建与排队问题，再调整局部资源，最后考虑部署特化”的顺序列出候选；只有左侧条件成立时，才进入相应一行。

#figure(
table(
  columns: (auto, auto, auto, auto),
  align: (left, left, left, left),
  stroke: (x, y) => (
    top: if y == 0 or y == 1 { 1pt } else { 0pt },
    bottom: if y == 9 { 1pt } else { 0pt },
  ),
  inset: 6pt,
  table.header([*候选手段*], [何时考虑], [重点复测], [主要边界]),
  [确认优化构建], [当前构建尚未启用优化], [端到端输出与耗时], [断言、日志和数值行为可能变化],
  [容量 1 并覆盖旧帧], [任务只需要最新观测且存在积压], [帧龄、丢帧率、跟踪连续性], [录像、计数和完整序列任务不适用],
  [扫描 OpenCV 线程数], [预处理占用明显或 CPU 余量不足], [联合负载的 p99 与各节点时限], [本章的 4 线程只是本机候选],
  [启用进程内通信], [大消息在同进程组件间传递], [消息所有权、外部订阅与录包], [收益依赖 ROS 2、RMW、QoS 和订阅结构],
  [CPU 亲和性或 cpuset], [迁移与调度竞争对应长尾], [迁移数、IRQ、整机吞吐和 p99], [亲和性不等于独占],
  [目标架构编译选项], [生成代码报告与计时都显示潜力], [最低配置目标机的热点与完整测试], [`native` 二进制可能不兼容其他机器],
  [FP16 推理], [推理是瓶颈且平台支持], [自有数据精度、端到端延迟和功耗], [外部论文的 12.7% 不能直接套用],
  [实时调度或 PREEMPT_RT], [证据指向调度或内核长尾], [最坏执行时间、饥饿、驱动和恢复路径], [配置错误可能影响整机可用性],
  [LTO、PGO 或 INT8], [前述项目完成后仍有明确瓶颈], [代表性负载、构建复现与任务精度], [剖面会过期；INT8 依赖校准集],
),
caption: [性能优化候选项及其进入条件。表格给出检查顺序而不是保证收益的排行榜：每项都应由当前剖析结果触发，并在相同输入、构建和目标机器上做修改前后对照。若某项没有改善目标指标，或破坏正确性与任务精度，就应撤回，而不是因为它通常被称作“优化”而保留。],
)

实施时一次只改变一个主要变量，并保存输入、二进制、配置、温度和统计口径。局部耗时变短不等于整机时限改善，平均值降低也不等于长尾收敛；同样，单次没有观察到收益，只能说明该测试范围内没有显示收益。把这些边界写进记录，下一次换机器或换模型时才知道哪些结论需要重测。

==== 本章小结

本章把性能工作分成三个层次。第一，延迟均值和逐帧变化要分开记录；$e approx v delta t$ 只是恒速条件下的预算关系，67.5 mm、27 ms 和 11 ms 都是示意推导，不是开火判据。第二，生成代码、局部计时和整机联合负载回答的是不同问题：向量寄存器计数不能证明收益，4 线程也只是本机候选。第三，亲和性、实时调度、FP16 与 Jetson 数据路径都有明确适用条件，只有目标机上的对照测试才能决定是否保留。

下一章「实战经验与常见问题」转向赛场运行与复盘：怎样保留足够的日志和录包，怎样识别线缆、光照、内存和配置造成的失效，以及怎样把这些经验整理成可执行的检查项。
