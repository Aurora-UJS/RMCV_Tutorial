#import "/template/template.typ": *

=== 工具都会用了，帧率还是不够

「基础篇」软件工程基础一章给过性能工作的形状：测量 $arrow.r$ 分析 $arrow.r$ 优化 $arrow.r$ 验证；「基础篇」计算机系统基础一章给过工具，还带着一次真实的自瞄掉帧定位——`perf` 抓出 `cv::resize` 吃掉 31% 的 CPU，每帧多花 11 ms。那两章合起来，已经足够让你回答"时间花在哪"。

可赛前一周最常见的处境不是"不知道时间花在哪"，而是*知道了，然后呢*。火焰图摊在屏幕上，最宽的那几根柱子你一根也砍不掉：推理是模型本身要的、PnP 就一毫秒、串口在等下位机。剩下的候选项倒是一大堆——换编译选项、钉核、提优先级、量化模型、改线程数、上 Jetson 的高功耗档——每一项都要花掉一到三天，而你只剩一周。

本章回答的就是这个问题：*同样的工程时间，钱该花在哪一档*。

先划清边界，免得和前面几篇打架。`perf` 与火焰图怎么用、缓存未命中的账怎么算，是「基础篇」计算机系统基础一章的主场，本章一概不重讲；进程内零拷贝的动机与配置在「RoboMaster应用篇」自瞄算法设计一章，取流与推理的解耦在同篇工业相机一章，推理引擎选型与 int8 量化在同篇装甲板识别一章，构建类型与 `ccache` 在「基础篇」CMake 与构建系统一章——这些本章只补*数字与代价*，不重复它们已经讲清的机制。

本章要新建的是三样东西：一张为*压缩*服务的端到端延迟预算表、一套判断某个优化值不值得做的方法（尤其是编译选项那一档，答案比你想的反直觉），以及一份按性价比排好序的清单。

#note("本章所有一手数据的测试环境")[
g++ 13.3.0 ／ OpenCV 4.6.0 ／ ROS 2 Jazzy（`rmw_fastrtps_cpp`）／ Intel i7-13790F（16 核 24 线程，支持 AVX2、无 AVX-512）／ Linux 内核 7.0.0-28。这是一台*桌面级* x86 主机，与车上常见的 NUC 同族，但与 Jetson 这类同构 ARM 平台不同——凡是涉及核数与调度的结论，换到 Jetson 都要重测。引用本章数字时请连环境一起引用。
]

=== 延迟预算：同一批数字，相反的目的

==== 补偿吸收均值，压缩对付抖动

「实战技术篇」弹道解算一章已经列过一张"从曝光到命中"的延迟预算表：相机曝光、传输与识别、PnP 与解算、上下位机通信、云台响应、拨弹空程、弹丸飞行，七行。那张表的结论是——*前四行加起来不过几十毫秒且基本恒定，逐项精确测量的收益有限，粗测后并成常数 $t_d$ 即可*。

这个结论没错，但它只在那一章的语境里成立。因为那一章要做的是*补偿*：把延迟当成已知量，让预测多外推 $t_d$，落点就回来了。对补偿而言，一个恒定的延迟几乎是无害的——它只是个加法常数。

本章的目的正相反：同样这几十毫秒，是你被雇来*削掉*的东西。于是同一批数字必须重新拆开，逐项问三个新问题：谁能削、削多少、代价是什么。

更关键的是第三个差别，它决定了本章的表和弹道那张表要按不同的方式排序：

*补偿只能吸收均值，吸收不了抖动。*

这句话值得算一笔账。设某个环节的延迟是随机变量，均值 $mu$、标准差 $sigma$。你把 $mu$ 写进 $t_d$，预测多外推 $mu$，均值部分被吃干净；剩下的 $sigma$ 无处可去——你不知道这一帧究竟晚了多少，只能任由它变成落点误差。目标横向速度为 $v$ 时，残余落点误差就是

$ e = v dot sigma $

代进真实数字：小陀螺的装甲板在半径 $r = 0.25$ m、角速度 $omega = 10$ rad/s 时，切向速度 $v = r omega = 2.5$ m/s。要让残余误差压在半块小装甲板宽度（约 67.5 mm，按 135 mm 的灯条间距折半）之内，需要 $sigma < 67.5 \/ 2500 approx 27$ ms。若对手转到 $omega = 24$ rad/s（$v = 6$ m/s），门槛立刻收紧到 $sigma < 11$ ms。

所以结论是：*一个均值 8 ms、抖动 $plus.minus 6$ ms 的环节，比一个恒定 20 ms 的环节难对付得多*。前者你压不掉、也补不掉；后者一行代码就并进常数了。在弹道那一章里可以忽略的方差，在本章是头等公民。

这也给了你一条排序准则：*先削抖动大的，再削均值大的*。均值大的环节至少还有补偿兜底，抖动大的环节没有任何东西兜底。

==== 一张为压缩服务的预算表

按这个准则把全链路摊开。表里的量级有四个来源，各自标注：引自弹道那一章的（那是全书已经取过的量级）、本章环境实测的、按物理关系推导的、以及引自外部公开测量的。

#figure(
table(
  columns: (auto, auto, auto, auto),
  align: (left, center, left, left),
  stroke: (x, y) => (
    top: if y == 0 or y == 1 { 1pt } else { 0pt },
    bottom: if y == 11 { 1pt } else { 0pt },
  ),
  inset: 6pt,
  table.header([*环节*], [*均值量级*], [*抖动*], [*能不能压、代价是什么*]),
  [相机曝光], [1～4 ms], [固定曝光时近乎为零], [压不动也不用压；但*别开自动曝光*——那等于给延迟加一个随场景变化的随机量],
  [传输与驱动交付], [毫秒量级], [小，受内核调度影响], [压不动；抖动可用调度手段摁住],
  [排队等待（推导）], [$0$ 到 $D-1$ 帧], [*大*，取决于队列深度 $D$], [*最该先查的一项*：容量取 1、丢旧帧就直接归零（工业相机一章）],
  [预处理（实测）], [0.24～0.84 ms], [中，与其他节点抢核], [显式设线程数；见本章线程一节],
  [推理（外部）], [约 17 ms（FP16）], [小，计算量固定], [FP16 几乎白拿；INT8 再快 26% 但精度有代价],
  [PnP 与滤波], [约 1 ms], [小], [不值得动——它在火焰图上根本不显眼],
  [节点间传递（实测）], [1.6 µs～379 µs], [小，但录包时会突变], [进程内零拷贝已是标配；陷阱见本章],
  [打包与串口], [3～15 ms], [*大*], [提波特率、提下位机收包频率；跨组协作，成本在沟通],
  [云台响应], [数十 ms], [看控制器整定], [不归视觉，但必须计入——它是全链路第二大头],
  [拨弹空程], [约百 ms], [*大*，随机构状态波动], [机械与电控的活；视觉侧只能在开火判据里留裕度],
  [弹丸飞行], [100～600 ms], [由距离决定，可解算], [不是延迟，是被建模的量（弹道解算一章）],
),
caption: [端到端延迟预算表，*为压缩服务*的版本。读表规则：它与「实战技术篇」弹道解算一章那张为*补偿*服务的表用的是同一批量级，但排序依据完全不同——那张表按"能不能并进常数 $t_d$"排，本张表按"抖动大不大"排，因为补偿只吸收均值、吸收不了抖动。看表时先扫第三列的三个"大"：排队等待、串口、拨弹空程。排队等待是唯一一个既大又完全免费可消除的，永远先查它；串口那一档要跨组沟通，成本在人不在码；拨弹空程视觉侧碰不到，只能在开火判据里留裕度。环节名后的括号标了该行量级的来源：引用来自弹道那一章、实测来自本章环境、推导给出了公式、外部则来自本章 Jetson 一节引的那份公开测量（arXiv:2502.15737，Jetson Orin NX ／ YOLOv8n 640×640 ／ TensorRT，58.82 FPS 折合约 17 ms），与弹道那一章无关。],
)

表里最该被反复念的是第三行。*排队等待是整张表里唯一一个既能贡献几十毫秒、又能被一行配置彻底消除的环节*。一个深度 5 的队列，在相机快于推理时会稳定攒满，贡献约 4 个帧间隔——100 fps 下就是 40 ms，比整条视觉流水线的计算时间加起来还多。而这 40 ms 不产生任何价值：你处理的是 40 ms 前的画面，云台追着一个过去的目标打。容量取 1、丢旧帧的做法在「RoboMaster应用篇」工业相机一章讲过机制，这里补上它在预算表里的分量：*它往往是全链路性价比最高的一次修改，而且是免费的*。

#figure(
  image("images/adv-perf-latency-budget.png", width: 100%),
  caption: [延迟预算的两个面（脚本 `adv_latency_budget.py`）。读图规则：左图是各环节的量级区间（对数横轴），颜色标明归属——蓝色是视觉侧能动的、灰色是电控与机械的、品红是被建模的物理量；柱子的*长度*就是它的不确定度，越长越难当常数补掉。右图把抖动折算成落点误差 $e = v sigma$：三条斜线是三种目标横向速度，水平虚线是半块小装甲板（67.5 mm）的容忍线，交点给出各速度下的抖动预算——2.5 m/s 时 27 ms，6 m/s 时收紧到 11 ms。两图合起来读：*左图告诉你哪一段长，右图告诉你多长才算超标*。注意右图是按 $e = v sigma$ 推导出的关系曲线，不是实测落点散布。],
)

==== 抖动怎么量，又怎么边跑边量

「基础篇」软件工程基础一章已经交代过：用 `steady_clock` 而不是 `system_clock`，看 99 分位与超预算帧占比而不是平均值。本章接着往下走一层——*怎么在整场比赛跑的过程中持续采集，而采集本身不把性能吃掉*。

先说度量。工程上把抖动量成 $sigma$ 并不方便（要存全部样本才能算），更实用的代理量是

$ "jitter" approx p_99 - p_50 $

即 99 分位减中位数。它只关心分布右侧那条尾巴——而尾巴正是让你丢帧的那部分——且不像最大值那样对单次偶发的极端值敏感。

再说采集。热路径上的埋点有几条硬纪律，每一条都对应一种把测量变成新瓶颈的方式：

- *不要在热路径上做 I/O*。`printf` 和 `std::cout` 内部有锁，写到管道或终端时还可能阻塞——你为了测延迟而引入了新的、更大的延迟。日志留给低优先级线程，热路径只往内存里写。
- *不要在热路径上分配*。埋点缓冲必须预分配：一个固定长度的数组加一个原子写指针，写满就绕回覆盖。每帧新 `push_back` 一个样本，早晚会撞上一次扩容重分配，那一次就是你 p99 上的一根尖刺。
- *存最小的东西*。微秒数存成 `uint32_t` 就够——它能表示约 71 分钟的微秒，远超一局比赛，而只占 `double` 一半的空间、缓存友好得多。
- *在线聚合，别事后排序*。要 p99 不必存全部样本：定一组固定边界的直方图桶（比如 0～50 ms 分成 100 个 0.5 ms 的桶），每帧只做一次除法加一次自增，$O(1)$ 时间、常数内存，随时能从桶里读出任意分位数。

至于打点本身贵不贵：Linux 上 `clock_gettime` 走 vDSO，不进内核，开销在纳秒到几十纳秒量级。也就是说，*一帧里打二十个点，总开销仍在微秒以下*，相对 10 ms 的帧预算完全可以忽略。这条很重要——它意味着你不必吝啬埋点，可以把预算表里每一段都真正量出来，而不是靠估。

=== 编译器这一档：别假设，去数一数

==== `-march=native` 到底有没有用

构建类型选 `Release` 这件事，「基础篇」CMake 与构建系统一章已经算过账，不重复。这里问的是再往前一步：`-O0` 到 `-O2` 是一个量级的差距（本章环境下约 7 倍，这种大幅差异对机器负载不敏感，可以当量级放心用），那么 `-O2` 之后还有没有肉？

最常被推荐的是 `-march=native`：告诉编译器"按本机 CPU 的全部指令集生成代码"。x86-64 的基线只保证 SSE2（128 bit 向量），而现代 CPU 普遍有 AVX2（256 bit）。理论上向量宽度翻倍，同样的循环一次能处理两倍的像素。于是就有了一个流传很广的建议：加上 `-march=native`，白拿一倍。

本书的两组测试给出了两个完全相反的结果，同一台机器、同一个 g++：

- 一组负载（二值化加阈值，1280×1024）加上 `-march=native` 后拿到约 2 倍的提速；
- 另一组负载（二值化、3×3 卷积、归约三段合成，1440×1080）*纹丝不动，甚至略慢*。

表面上这是数据打架。真相比任一组数据都值钱，而找到真相的方法不是把计时再跑一百遍——是去看编译产物：

```bash
# 数一数编译出来的目标文件里，各宽度的向量指令各有多少条
objdump -d bench.o | grep -c '%ymm'   # AVX2，256 bit
objdump -d bench.o | grep -c '%xmm'   # SSE，128 bit
```

结果一目了然，但两组的答案不一样。第一组负载：不加 `-march=native` 时 `ymm` 计数为 *0*，加上之后变成 *148*——编译器确实把循环用 256 bit 指令重新发了一遍，128 bit 到 256 bit 的宽度比正好对应观察到的约 2 倍。第二组负载：`ymm` 从 *0* 涨到 *110*、`xmm` 从 218 降到 161——AVX2 同样生成了，工作也确实迁到了更宽的寄存器上。可墙上时间纹丝不动。

数寄存器是个聚合数字，会把标准库内联进来的代码一并计入，所以再换编译器自己的报告问一遍：

```bash
g++ -O3 -march=native -fopt-info-vec -c adv_compile_flags_bench.cpp
# adv_compile_flags_bench.cpp:21:21: loop vectorized using 32 byte vectors  (threshold)
# adv_compile_flags_bench.cpp:31:23: loop vectorized using 32 byte vectors  (conv3x3)
# adv_compile_flags_bench.cpp:41:21: loop vectorized using 32 byte vectors  (reduce)
# 去掉 -march=native，同样这三行变成 16 byte vectors
```

三段循环被逐个点名，全部从 16 字节升到 32 字节。也就是说，第二组*拿到了 AVX2，却没有拿到提速*——这比"没生成所以不快"要有意思得多。

*`ymm` 数为 0 能证伪收益——没发出更宽的指令，就一定不会更快；但 `ymm` 大于 0 不保证收益——指令发出来了，还要看这个循环到底卡在哪。前半句一条命令就能数出来，后半句得算一笔带宽的账。*

那为什么发了 AVX2 却不快？先排除两个听起来很顺、但经不起查的解释。第一个是"归约拆不开"——`-fopt-info-vec` 明写着 `reduce_op`（第 41 行）被 32 字节向量化了，编译器自己把累加拆成多路再合并。第二个是"指针可能重叠（aliasing）挡住了向量化"——`conv3x3`（第 31 行）确实有这个问题，但编译器做了运行期版本化绕过去（`loop versioned for vectorization because of possible aliasing`）；更要命的是，*拿到 2 倍提速的第一组同样触发了这条版本化*。两组行为一致，拿它解释两组的差异根本不成立。

说得通的是访存。threshold 这一段每帧读 1.55 MB、写 1.55 MB，算术上只有一次比较——`-O3` 下它跑 0.044～0.058 ms，折合 *53～71 GB/s*，而三个缓冲区合计 6.2 MB 整个落在 33 MB 的 L3 里。这个数字已经贴着单核从 L3 取数的上限：循环在等数据，不在等 ALU。把向量宽度从 128 bit 加到 256 bit，只是让同样多的字节用更少的指令搬完，瓶颈那一端一点没松。

至于 conv3x3 与 reduce 为什么还各慢了约 7% 与 10%，反汇编里有条线索：native 版本多出 24 条 `vpmovzxbw`（字节展宽成字）与 14 条 `vextracti128`（跨 128 bit 通道取数）——在字节数据上用 256 bit 向量，要额外付展宽与跨通道的钱。但*这只是线索，不是结论*：要坐实它，得用 perf 的硬件计数器分清究竟是访存停顿还是端口争用，本书没有测，所以这里不给机制。宁可停在"观察到了、没测出来"，也别为一个现象编一个听起来说得通的因果——那正是本节要教你避开的毛病。

这条方法论比具体结论更重要：*反汇编普查是确定性的*。它不受 CPU 负载、频率调节、调度抢占的影响，同一份源码同一组选项，数出来永远是同一个数；`-fopt-info-vec` 同理，而且比数寄存器更准——数 `ymm` 只能告诉你"有没有"，它能告诉你"哪一行、多宽"。而计时不是：这组数据第一次采集时机器上并行跑着别的任务，同一档的中位数在 0.41～0.64 ms 之间乱跳，足以把两档之间的真实差距完全淹掉。后来在空闲机器上钉核重跑（`taskset -c 2`，每档 300 次），噪声才干净下来，得到本节引用的这组数：`-O0` 11.44 ／ `-O2` 1.49 ／ `-O3` 0.38 ／ `-O3 -march=native` 0.40 ms。*当计时不可靠时，先去找一个确定性的观测量，再回头把计时做干净*，这是性能工作里非常值钱的一招。

配套的 `adv_compile_flags_bench.cpp` 与 `adv_compile_flags_bench.py` 实现了这套普查：同一份源码用多组选项各编译一次，逐档反汇编计数再计时。你可以在自己的机器上跑它，但请先确认机器是空闲的。

#figure(
  image("images/adv-compile-flags.png", width: 100%),
  caption: [同一段流水线在五组编译选项下的耗时（脚本 `adv_compile_flags_bench.py`；13 代 i7 ／ g++ 13.3 ／ 1440×1080 ／ `taskset` 钉核 ／ 每档 300 次取中位数。*换机器、换编译器、换负载，数字都会变，会变的是数字不是方法*）。读图规则：左图按三段工作拆开看时间去哪了——`-O0` 到 `-O2` 那一步是数量级的（$11.4 arrow.r 1.5$ ms），`-O2` 到 `-O3` 又快了四倍，主要来自蓝色那段（二值化，访存密集）被向量化。右图才是工程上真正要问的问题：*`-O2` 之后还剩多少*——因为没人会拿 `-O0` 上场，拿它当基准只会得到一个虚高的加速比。右图里 `-march=native` 那根柱子几乎贴着零线，正是本节的论点：它确实发出了 256 bit 指令，但这个负载卡在访存带宽上，宽指令无处施展。],
)

最后是代价，这一档不是白拿的：`-march=native` 编出来的二进制*绑定编译它的那台机器*。在带 AVX-512 的开发机上编好，拷到只有 AVX2 的车载 NUC 上运行，会直接 `SIGILL`（非法指令）崩溃——而且是启动就崩，不是偶发。所以要么在车上现编，要么老实写明确的目标（如 `-march=x86-64-v3`）而不是 `native`。

顺带回收基础篇的一笔账：「基础篇」CMake 与构建系统一章给的那份顶层 `CMakeLists.txt` 模板，几个优化档都写了 `-march=native`——那是"在哪台机器上编就在哪台上跑"的写法。若你的构建机和车不是同一台，先在车上跑一句 `lscpu | grep avx2` 确认，有 AVX2 就把它换成 `-march=x86-64-v3`（该档等价于 AVX2 加 FMA 加 BMI2，不含 AVX-512）；若车上是 Celeron／Pentium 一类没有 AVX2 的低功耗 U，这一档同样会崩，老实退回默认基线。

==== LTO 与 PGO

再往后两档收益递减得很快，但知道它们存在、以及各自的代价，能帮你判断什么时候不值得碰。

*LTO*（Link Time Optimization，链接期优化）让优化器跨翻译单元工作。平时编译器只能看见一个 `.cpp`，跨文件的函数调用它既不敢内联也不敢消除；开了 `-flto`，这些边界被打通。所以 LTO 的收益取决于*你的热路径是否被文件边界挡住了内联*：热循环全在一个文件里的，几乎没有收益；把小函数散在十几个 `.cpp` 里的，收益会明显一些。代价是链接时间显著变长，且个别情况下会暴露原本被翻译单元边界掩盖的未定义行为。它是个位数百分点量级的活，别指望 `-march` 那种跳变。

*PGO*（Profile-Guided Optimization，按剖面引导的优化）更进一步：先用 `-fprofile-generate` 编一个插桩版本，拿真实数据跑一遍，收集分支走向与函数热度，再用 `-fprofile-use` 重编。编译器于是知道哪个分支才是常走的，据此排布代码、决定内联。它的适用面很窄——收益主要来自分支密集的代码，而视觉流水线的热点大多是数值循环，分支本来就少。而且它有个维护上的陷阱：*剖面会过期*。采样时跑的是去年的录像，今年换了检测器，剖面指向的热点全变了，编译器会照着一份错误的地图做优化。

本书没有在 RM 负载上复现过 LTO 与 PGO 的收益数字，所以这里只给机制与代价，不给数字。你若要用，用本章开头那套方法自己量——测量之前的任何数字都是别人机器上的故事。

=== 线程数：局部最优害死全局

==== OpenCV 默认占满你的整台机器

这一节是本章最实用的一条，而且大概率你现在的代码里就有这个问题。

OpenCV 的多数图像操作内部是并行的，后端通常是 TBB。问题在于*默认线程数*：本章环境下 `cv::getNumThreads()` 返回 *24*——全部逻辑核。也就是说，你只是调了一句 `cv::GaussianBlur`，OpenCV 就把整台机器的 CPU 全要走了。

把线程数扫一遍（`cvtColor` 加 `GaussianBlur` 加 `threshold`，1280×1024）：

#figure(
table(
  columns: (auto, auto, auto, auto, auto),
  align: (center, center, center, center, center),
  stroke: (x, y) => (
    top: if y == 0 or y == 1 { 1pt } else { 0pt },
    bottom: if y == 5 { 1pt } else { 0pt },
  ),
  inset: 7pt,
  table.header([*线程数*], [*墙上时间*], [*加速比*], [*CPU 时间*], [*并行效率*]),
  [1], [0.838 ms], [1.00×], [0.911 ms], [100%],
  [2], [0.422 ms], [1.99×], [0.872 ms], [99%],
  [4], [0.238 ms], [3.52×], [1.060 ms], [88%],
  [8], [0.249 ms], [3.37×], [2.040 ms], [42%],
  [24（默认）], [0.188 ms], [4.46×], [3.570 ms], [19%],
),
caption: [OpenCV 线程数扫描（本章环境，一次典型预处理）。读表规则：只看第二、三列会得出"线程越多越好"的错误结论——必须把*第四列的 CPU 时间*一起看。从 4 线程走到 24 线程，墙上时间只快了 21%，CPU 时间却涨到 3.4 倍：多烧掉的那两毫秒 CPU 不是凭空来的，是从跟踪、串口、导航那些节点嘴里抢的。还有一处值得注意：从 4 线程到 8 线程，墙上时间*不再变快*（0.238 对 0.249，差异在本机轮次噪声内），CPU 时间却从 1.06 涨到 2.04 ms——多烧一倍算力，什么也没换到。这一档还能不能更快，取决于这 8 个线程被调度到哪几个核上，本书未做核绑定验证。],
)

这张表是"局部最优害死全局"的标准教材。单独给预处理计时，24 线程确实最快，你会心满意足地把它留在默认值；可整个系统跑起来，跟踪器、串口线程、哨兵上还有导航栈，全在同一台机器上，它们被这一句 `GaussianBlur` 挤到没有核可用，于是*别的环节的抖动全面恶化*——而这恰恰是上一节说的、补偿吸收不了的那部分。

工程结论很直接：

```cpp
cv::setNumThreads(4);   // 在 main() 开头设一次，按机器核数与并发节点数取 2~4
```

选 4 的理由就在表里：并行效率还有 88%，墙上时间已经拿到 3.5 倍，而 CPU 时间只有单线程的 1.16 倍——剩下的核留给别人。这是一次*主动放弃局部最优、换取全局稳定*的交易，而它只有一行。

两个限定必须说清楚：本机是 P/E 核异构的桌面 U，Jetson 那类同构 ARM 平台的曲线形状会不同（拐点位置会不同），核数也少得多，得自己扫一遍；另外这一句只管 OpenCV 自己的并行，你手写的线程不受它约束。

#figure(
  image("images/adv-perf-opencv-threads.png", width: 100%),
  caption: [OpenCV 线程数的两条曲线（脚本 `adv_opencv_threads.py`，数据即上表，本章环境）。读图规则：*两条曲线必须一起看*——蓝色的墙上时间从 4 线程往后已经基本躺平（0.238 降到 0.188 ms），品红的 CPU 时间却从 1.06 一路涨到 3.57 ms，两者之间那块阴影就是被白白烧掉、本该留给其他节点的算力。灰色虚线标出 24 线程这个*默认值*的位置，蓝色竖线标出推荐的 4 线程：右移一格换来 21% 的墙上时间，代价是 3.4 倍的 CPU。8 线程那一档与 4 线程基本持平，起伏在噪声内——别对着这个量级的起伏讲故事，本章开头刚说过，计时不可靠时就别拿它下结论。],
)

==== 进程内零拷贝的账，以及录包时它会消失

「RoboMaster应用篇」自瞄算法设计一章已经讲透了 ROS 2 进程内通信的动机与配置——为什么要把相机和识别塞进同一个容器、`use_intra_process_comms` 怎么开、只组合不开开关为什么不生效。这里只补两样那一章没有的东西：*它到底值多少*，以及*它什么时候会失效*。

同进程同执行器、`sensor_msgs/Image`、`SensorDataQoS` 深度 1、发布 `unique_ptr`，四档负载各测一遍：

#figure(
table(
  columns: (auto, auto, auto, auto),
  align: (center, center, center, center),
  stroke: (x, y) => (
    top: if y == 0 or y == 1 { 1pt } else { 0pt },
    bottom: if y == 4 { 1pt } else { 0pt },
  ),
  inset: 7pt,
  table.header([*消息大小*], [*intra 关*], [*intra 开*], [*提速*]),
  [0.23 MB], [14.1 µs], [0.8 µs], [18×],
  [0.92 MB], [68.5 µs], [0.8 µs], [86×],
  [3.93 MB], [378.8 µs], [1.6 µs], [237×],
  [6.91 MB], [675.6 µs], [2.3 µs], [294×],
),
caption: [ROS 2 进程内通信的量化收益（本章环境）。读表规则：*别只看最后一列的倍数，看前两列的形状*——关闭时开销随字节数近似线性增长（收敛到约 97 µs/MB，反推约 10 GB/s，正是序列化加共享内存拷贝几趟内存的量级），开启时基本是常数（只移交一个指针，与负载无关）。曲线形状本身就证明了机制，这比倍数可信得多。另需注意：关闭那一列走的是 Fast DDS 的共享内存传输而非网络，已经是进程间通信里较快的一档，不是最坏情况。],
)

第二件事更值钱，因为它解释了一个很常见的怪现象：*调试时好好的，一录包就掉帧*。

进程内零拷贝成立的前提是——这个话题的订阅者*全部*在同一个进程里。一旦有外部节点订阅了同一个话题（`rviz2` 开着看图、`ros2 bag record` 在录），进程外的那条路径就被激活了：消息必须被序列化、拷贝、发出去，那 379 µs 又回来了，而且是每帧都回来。零拷贝的收益部分消失，取流线程被拖慢，队列开始积压，帧率就掉了。

所以：*录包和可视化是有代价的，而且这个代价恰恰落在你最精心优化过的那条路径上*。可视化比赛时该关的都关掉。录包是个例外——下一章会给出理由：赛场故障若没有录包，赛后基本无法复现，也就基本无法修复。所以对录包，正确的动作不是关掉它，而是*把它当成一项常驻开销纳入预算表重新测一遍*（降分辨率、降帧率、编码压缩三选一），别拿"调试时没开录包"的帧率去承诺比赛表现。

=== 把抖动交给调度器

==== 一句"救不了你"的反转

「基础篇」计算机系统基础一章讲竞态条件时有一句斩钉截铁的话：*"所以钉核、提实时优先级、把临界区做短，都救不了你。"*

那句话是对的，而本节要做的事看起来正好相反，所以必须先把两者的区别说透，否则你会以为其中一处写错了。

区别在于*问题的性质*：

- 竞态是*正确性*问题。`++counter` 的读-改-写三步之间存在一个窗口，只要窗口存在，交错就可能发生。钉核不能消灭这个窗口（同核上调度器照样能在两步之间切走），提优先级也不能（更高优先级的线程一样会抢占你）——你只是降低了它发生的*概率*，而一个概率降低了的 bug 仍然是 bug，而且更难复现。唯一的出路是让三步不可分割。
- 抖动是*时序*问题。它的成因本来就是调度：你的取流线程被浏览器、被日志进程、被另一个视觉节点抢走了 CPU，于是这一帧晚了 8 ms。这里调度手段不是在降低概率，而是在*直接消除成因*——把核留给你、把优先级提到抢不走，那一次抢占就真的不再发生。

一句话记住这个反转：*调度手段治不了"该不该发生"，但治得了"什么时候发生"*。竞态属于前者，抖动属于后者。

==== 亲和性、隔离与实时优先级

三件工具，由轻到重：

*CPU 亲和性*。`taskset -c 4,5 ./auto_aim` 把进程钉在指定的核上。收益不只是"独占"——线程在核之间迁移时，它辛苦预热的 L1/L2 缓存全部作废，得从头再来一遍（缓存的账「基础篇」计算机系统基础一章算过）。把取流与推理线程钉在固定核上，等于把这笔迁移成本永久删掉。代价几乎为零，是这三件里最先该试的。

*核隔离*。`isolcpus=6,7`（写进内核启动参数）告诉调度器：这两个核不参与普通任务的负载均衡，除非有人明确 `taskset` 过来。这样被钉上去的线程不仅优先，而且是*独占*——系统里其他所有进程都不会被派到这两个核上。代价是这两个核对系统的其余部分永久消失了，机器的总吞吐会下降。适合把最怕抖动的那一两个线程（取流、串口收发）单独供养起来。

*实时优先级*。`chrt -f 80 ./auto_aim` 把进程放进 `SCHED_FIFO` 实时调度类。实时类里的线程只要就绪就会抢占所有普通线程，且不会因为跑得久而被降权。对串口接收线程这种"必须在字节到达后立刻被唤醒"的角色，效果最直接。

实时优先级有个真会把机器锁死的坑：一个 `SCHED_FIFO` 线程若进入死循环且不主动让出，同核上的普通线程*永远*得不到调度——包括你的终端。所以先在能物理断电或有另一路 SSH 的环境里试，别在赛前一晚直接上车；也别把优先级设到 99，那一档留给内核线程。

最后一档是换内核：*PREEMPT_RT* 补丁把内核中大量不可抢占的临界区改造成可抢占的。一份同行评审的公开测量（OSPERT 2013，MPI-SWS，16 核 Xeon，`cyclictest` 加重 I/O 负载）给出的量级是：最坏调度延迟从 80～200 ms 降到 50 µs 以下——注意这是*最坏值*，也就是分布最右端那条尾巴，正是让你偶尔丢一帧的那部分。这是几个数量级的改善，但代价也实在：要自己编内核、可能与某些驱动（尤其是 GPU 与相机 SDK 的内核模块）冲突，且平均吞吐会略降。对绝大多数队伍，前三件工具的收益已经够用；只有当你已经把队列、线程数、编译选项都做完、抖动仍然卡在开火判据上时，才值得投入这一档。

=== 分配与拷贝：`cv::Mat` 真正贵的地方

「基础篇」软件工程基础一章已经纠正过一个常见误解：`cv::Mat` 是引用计数的*浅*拷贝，按值传递并不贵，贵的是你随手写下的 `clone()`。本节接着这句话往下讲两件它没展开的事。

第一件是*浅拷贝的另一面：别名*。`cv::Mat roi = frame(rect);` 得到的不是新图，是同一块内存的一个视图。往 `roi` 里写就是往 `frame` 里写。这在调试可视化里最容易翻车：你在 ROI 上画个框想看看检测对不对，结果原图被永久涂改，下游的分类器拿到的是带框的图。要一份独立数据必须显式 `clone()`——这才是 `clone()` 存在的正当理由，而不是随手乱用的对象。

第二件更隐蔽，而且是 RM 特有的：*入队之前必须真拷贝*。相机 SDK 交给你的那块内存，通常是驱动内部的一个循环缓冲区，用完要还给 SDK（`FreeImageBuffer` 一类接口）。如果你用 `cv::Mat` 把这块内存包一层就塞进帧队列，`cv::Mat` 的引用计数管不到 SDK 那侧的归还逻辑：要么你还了缓冲、队列里的 `Mat` 指向一块正在被下一帧覆写的内存（画面撕裂，且极难复现），要么你不还，SDK 的缓冲池被耗尽、取流直接停摆。规则很简单——*跨线程传递图像时拷贝一次*，这一次拷贝是必要的，别为了省它去和别人的生命周期赌。

至于减少分配，思路是"预分配加复用"，而不是上一套复杂的内存池：OpenCV 的多数函数接受输出参数，若传进去的 `Mat` 尺寸与类型已经对上，它就直接复用那块内存、不再分配。所以把每帧都要用的中间缓冲（灰度图、二值图、梯度图）提成类成员而不是函数里的局部变量，一帧一次的堆分配就消失了。这比引入内存池简单得多，也解决了绝大部分问题——真正需要内存池的场景（高频、变长、生命周期交错的小对象）在自瞄流水线里并不多见。

=== Jetson：先确认板子没在省电

如果车上是 Jetson，有一件事必须在做任何算法优化之前确认，因为它能白白吃掉一半算力：*板子出厂默认往往不在最高性能档*。

两条命令：`nvpmodel` 管功耗模式，不同模式限制在线的 CPU 核数与频率上限；`jetson_clocks` 把 CPU/GPU/内存的频率锁死在当前模式允许的上限，绕过动态调频。前者决定天花板，后者决定你是否一直贴着天花板跑。二者的代价一致且真实：功耗与发热常态化，散热跟不上时反而会触发降频。所以锁频之后要复测，别假设它一定更快。

平台层面还有一处结构性红利：Jetson 的 CPU 与 GPU *共享同一块物理内存*。在独显机器上必须做的主机到设备拷贝（H2D），在这里原则上可以省掉——用统一内存或页锁定内存让 CPU 写入的图像被 GPU 直接读取。对每帧几 MB 的图像流水线，这是一笔实打实的省；相机 SDK 若支持直接把帧写进这类缓冲，取流到推理之间就一次拷贝都不用了。

最后把「RoboMaster应用篇」装甲板识别一章讲过的 int8 量化补上*代价的数字*。一份公开测量（Jetson Orin NX，YOLOv8n，640×640，TensorRT；arXiv:2502.15737）给出：

$ "FP32" 52.19 "FPS" quad arrow.r quad "FP16" 58.82 "FPS" quad arrow.r quad "INT8" 65.83 "FPS" $

INT8 相对 FP32 提速约 26%，听起来划算；但同一份测量里 mAP50-95 从 *0.842 掉到 0.719*，而 FP16 几乎无损。把两组数字放在一起，结论就清楚了：*FP16 是白拿的，INT8 不是*。在装甲板这种"认错了就打友军"的任务上，用十几个百分点的精度换 26% 的帧率，多半是笔亏本买卖——除非你已经确认帧率就是当前唯一的瓶颈，且量化后在自己的实车数据上复测过精度。别拿公开基准的掉点幅度替代自己的复测：掉多少强烈依赖校准集与模型结构。

=== 性价比排序：同样的工时花在哪

把全章收成一张按性价比排序的清单。排序依据是"收益除以工程时间"，并把风险单列——有些优化很便宜，但会在赛场上以别的形式把钱还回来。

#figure(
table(
  columns: (auto, auto, auto, auto),
  align: (left, left, center, left),
  stroke: (x, y) => (
    top: if y == 0 or y == 1 { 1pt } else { 0pt },
    bottom: if y == 9 { 1pt } else { 0pt },
  ),
  inset: 6pt,
  table.header([*手段*], [*收益*], [*工时*], [*风险与代价*]),
  [确认 `Release` 构建], [数倍], [分钟], [无。先查这个，别在 `Debug` 上优化算法],
  [队列容量取 1、丢旧帧], [削掉几十 ms 排队], [分钟], [无。全链路性价比最高的一项],
  [`cv::setNumThreads(2~4)`], [省下约 70% CPU], [一行], [单段墙上时间略升，需自己扫一遍],
  [进程内零拷贝], [每帧几百 µs], [launch 一行], [录包与可视化会让收益部分消失],
  [CPU 亲和性 `taskset`], [削抖动尾巴], [小时], [几乎无；应先于隔离与实时优先级试],
  [FP16 推理], [约 12% 帧率], [小时], [几乎无损，Jetson 上应默认开],
  [`-march=native`], [0 或约 2×], [小时], [*先数 `ymm`，再确认不是访存受限*；二进制绑定机器，换机 `SIGILL`],
  [`SCHED_FIFO` 与核隔离], [削最坏延迟], [天], [配错会锁死机器；先在可断电环境试],
  [INT8 量化 / LTO / PGO], [26% ／ 个位数 ／ 未知], [天], [INT8 精度有真实代价；PGO 剖面会过期],
),
caption: [优化手段的性价比排序（依据本章数据与代价分析）。读表规则：*从上往下做，做到帧率够了就停*。前四行是分钟到一行代码的量级，几乎不该跳过；中间三行需要一次实测才能判断值不值；最后两行是"前面都做完了还不够"时才碰的。第二列的收益量级各有来源——数倍与几十毫秒是结构性的，70% 与几百微秒是本章实测，2× 与 26% 引自本章对应小节并附了条件。特别注意 `-march=native` 那一行：它是全表唯一一个*收益可能为零*的手段，做之前先用反汇编数一数，再算一笔带宽的账。],
)

排序之外还有一条元规则，比表里任何一行都重要：*每一项做完都要用同一组输入、同一台机器、同一个构建类型复测一遍*。基础篇强调过的"验证"那一步，在本章尤其容易被跳过，因为这些优化听起来都"显然有用"。而本章至少给了两个反例：`-march=native` 在一半的负载上收益为零，24 线程比 4 线程"更快"却让全系统更差。*显然有用的东西，一半时候没用*。

==== 本章小结

性能优化的难点从来不是不会用工具，而是候选项太多、工时太少，本章给的是一张花钱的地图。延迟预算表按*抖动*而非均值排序，因为补偿只吸收均值、吸收不了抖动，残余落点误差就是 $v sigma$——2.5 m/s 的目标只留 27 ms 的抖动预算，6 m/s 时只剩 11 ms；排队等待是表里唯一一个又大又免费的项，永远先查它。埋点要预分配、存 `uint32_t`、在线聚合成直方图，而 `clock_gettime` 走 vDSO 便宜到可以随便打。编译选项那一档的真正教训不是某个 flag 好用，而是*别假设、去数一数*——但也别只数一半：`ymm` 为 0 能证伪收益，`ymm` 大于 0 却不保证收益，第二组负载数出 110 条 `ymm` 反而慢了约 8%。OpenCV 默认 24 线程是"局部最优害死全局"的活标本，一行 `setNumThreads(4)` 换回七成 CPU。调度手段治不了竞态但治得了抖动，亲和性先试、实时优先级最后试。`cv::Mat` 真正的坑是别名和跨线程入队，不是按值传参。Jetson 上先确认功耗模式，FP16 白拿、INT8 要拿十几个百分点的精度去换。

到这里，这台车已经跑得够快、抖得够小了。可赛场从不按你的预算表出牌——线会松、灯会晃、内存会在第三局漏爆。下一章「实战经验与坑点总结」把全书散落的坑收成一份索引，再补上一层这本书还没系统讲过的东西：按"上场会怎么死"归并的赛场级失效模式。那也是进阶篇的最后一章。
