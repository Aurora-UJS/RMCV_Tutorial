#import "/template/template.typ": *

「性能优化技巧」一章讨论了如何用测量找到瓶颈，并把赛场运行与复盘留给本章。这也是进阶篇的收尾：前文分别讨论了相机、识别、跟踪、弹道、通信和部署，本章改从整机运行的角度，检查这些环节组合后会出现哪些失效。

前面各章已经详细解释过弹速多处配置、PnP 尺寸定义、TensorRT engine 兼容性、udev 匹配和其他具体问题。本章先给出一张跨章节索引，后文不再重复每个模块的实现细节，而是关注实验室单项测试很难覆盖的组合问题：车体震动会改变镜头状态，长时间运行会累积资源问题，场馆灯光和网络不由队伍控制，视觉与电控还会通过延迟、时间戳和补偿项相互影响。

正文按光照、硬件、时间、系统和控制耦合五类失效模式展开，并单列一节说明端到端延迟的实车测量。多数条目依次给出*现象*、*可能机制*、*现场处置*和*赛前验证*。现象只是第一条线索：供电、信号质量和驱动错误都可能表现为相机掉线，降低增益后抖动减轻也不能单独证明延迟就是原因。因此，每项应急动作都尽量可逆，每项原因判断都要留下能区分其他解释的证据。

最后一节把这些经验整理成五分钟内可执行的排查顺序：先保存现状，再根据症状选择信息量最大的检查，一次只改一个主要变量，并在不能完成根因定位时转入已验证的降级模式。

=== 全书相关问题索引

下表适合在赛前作为跳转清单。右列只是检查提示，完整的机制、代码与验证方法仍以中列指向的章节为准。

#figure(
table(
  columns: (auto, auto, auto),
  align: (left, left, left),
  stroke: (x, y) => (
    top: if y == 1 or y == 0 { 1pt } else { 0pt },
    bottom: if y == 14 { 1pt } else { 0pt },
  ),
  inset: 7pt,
  table.header([*问题*], [*对应章节*], [检查提示]),
  [同一个弹速在三处硬编码], [「自瞄算法设计」], [同一物理量应有明确的权威来源与时效],
  [PnP 装甲板尺寸采用 135 mm 还是 140 mm], [「装甲板识别」], [先统一接口两端对“宽度”的定义],
  [TensorRT engine 与环境兼容性], [「装甲板识别」], [只在已验证兼容的 GPU、TensorRT 与构建环境间复用],
  [Bayer 版式配错导致红蓝反色], [「工业相机」], [赛前用实际相机与目标验证颜色链路],
  [跟踪阈值只按帧数配置], [「装甲板的跟踪」], [帧率变化会改变实际容忍时间],
  [$Delta t$ 固定为常数], [「装甲板的跟踪」], [丢帧或抖动时预测步长会与实际时间脱节],
  [udev 规则的 `ATTR` / `ATTRS` 层级写错], [「串口模块」], [用 `udevadm info` 确认属性来自当前设备还是父设备],
  [手眼欠约束也会给你一个数], [「相机标定与手眼标定」], [标定收敛不等于标定正确],
  [`publish_frequency` 未接入实际行为], [「自瞄算法设计」], [配置项要有读取点、作用路径与验证方法],
  [教学案例中 `cv::resize` 样本占比约 31%], [「基础篇」计算机系统基础], [用 perf 和分阶段计时定位当前负载，不照搬比例],
  [灯带、大屏、反光与车灯形成干扰候选], [「基础篇」C++ 语言基础], [把干扰当作必须覆盖的输入，不只测干净背景],
  [内存泄漏与文件描述符泄漏], [「基础篇」计算机系统基础], [高速率泄漏可能很快耗尽资源并引发分配或 I/O 失败],
  [相机上车验收清单], [「工业相机」], [过曝、拖影、反色、掉帧、时间戳单调],
  [串口上车自检清单], [「串口模块」], [波特率、帧对齐、重连、CRC 校验、时间偏移],
),
caption: [全书相关问题索引。读表时先在左列找现象或风险，再回到中列章节核对条件与验证步骤；右列不代替诊断结论。],
)

=== 光照：保持已验证的成像工作域

「工业相机」已经说明低曝光如何压低背景并减少拖影，「基础篇」C++ 语言基础则列出了灯带、大屏、反光和车灯等干扰来源。这里只补两个赛场特有的操作问题：场馆光源可能在帧间变化，临场为了“看清画面”而改动曝光，又可能把系统推离原来验证过的工作域。

==== 帧间亮度起伏：光源纹波与曝光窗

*现象*。固定相机和场景后，背景或地面反光的亮度仍在数帧间周期变化。亮度高的帧可能让更多背景像素越过固定阈值，也可能让灯条与邻近反光连成一块，从而改变轮廓几何。若训练场和赛场采用不同照明驱动，问题还可能只在其中一处出现。

*可能机制*。采用交流输入、且整流后平滑不足的灯具驱动，常产生电网频率两倍的亮度纹波：50 Hz 供电下常见 100 Hz，60 Hz 供电下常见 120 Hz。但 LED 驱动也可能使用其他开关或 PWM 频率，不能只凭现象预设为 100 Hz。当曝光窗明显短于纹波周期时，每帧积分到的相位不同；帧率又与纹波不同步时，可见亮度会以拍频缓慢变化。滚动快门还可能把它表现为横向亮暗带。短曝光有利于减少拖影，同时也减少了对一个纹波周期的平均；两者需要在实际相机和场馆照明下一起验证。

*现场处置*。先固定相机、关闭自动曝光与自动增益，对一块不含运动目标的感兴趣区域（ROI）连续记录均值或中位数，并同时保存时间戳。稳定周期只说明记录到的图像亮度按一定节拍变化；把相机转向另一光源、改变帧率后观察拍频是否随之改变，才能进一步区分光源纹波与算法自身抖动。确认后，优先恢复已验证的曝光、增益和阈值配置。若必须临时改阈值，要同时检查真灯条召回，不能只看假候选是否减少。跟踪器可以容忍短暂丢失，但容忍量应以时间表达或按当前帧率换算，不宜只按固定帧数临时增加。

*赛前验证*。对于近似正弦的稳定纹波，曝光覆盖整数个周期可以减小帧间差异；但 10 ms 或更长曝光可能带来拖影、饱和和额外延迟，并非通用解法。实际项目通常优先保持已标定的低曝光工作域，再检查相机是否提供防闪烁、外触发或帧率同步功能。这些功能的效果依赖具体快门和灯光驱动，仍要用赛场同类光源录像，比较帧间亮度分布、候选数和跟踪中断率。

==== 不要按显示效果临场调亮画面

*现象*。上一场采用同一程序时识别稳定，下一场却同时出现召回降低、干扰候选增多和云台输出异常。若代码与模型未变，应立即核对相机参数和实际图像统计，而不是继续调算法阈值。

*可能机制*。为了让调试画面更适合人眼观察，有人在相机 GUI 中提高了曝光或增益。曝光和增益不只改变显示亮度：它们会改变阈值前后的背景分布、灯条饱和与轮廓膨胀、红蓝通道分离度，也可能让神经网络输入偏离训练集。灯条宽长比本身是几何量，但分割边界随饱和与阈值改变后，程序实际测到的比值也会变。因此，临场改曝光会同时改变多条处理路径，不是一个单纯的界面设置。

*现场处置*。先导出或拍照保存当前参数，再恢复上一份已通过实拍的车辆配置。曝光、增益若只存在相机内部或 GUI 中，`git diff` 无法看到这项改动；部署脚本应在启动时显式写入并读回核对关键参数。恢复后用同一个目标和位置检查原图、二值图、候选数与识别结果，避免只凭画面看起来变暗就认为已恢复。

*赛前验证*。把曝光、增益、像素格式、白平衡以及与它们共同验证的阈值和模型版本写入每台车的配置档案。训练和比赛前都从这份档案启动，并把读回值打入日志。人眼适合查看的图像可以在调试界面中单独做显示拉伸，不要为了显示而改变识别链实际接收的像素。

=== 硬件：软件正常时也要检查机械与链路

串口断连与重连、相机取流恢复已经分别在「串口模块」和「工业相机」中讨论。本节关注另一组上车后常见的问题：镜头状态变化未必触发软件错误，相机掉线也未必只由驱动造成。日志能说明程序观察到了什么，却不能替代对镜头、接插件、供电和触发链路的检查。

==== 镜头机械松动：一次不报错的内参失效

*现象*。识别和跟踪仍有输出，日志也没有明显异常，但落点出现随距离变化的系统偏差；重新检查镜头状态或重新标定后偏差减小，经过震动或拆装又出现。

*可能机制*。对焦环在震动中转动会移动焦平面，并可能改变有效焦距和畸变；变化幅度取决于镜头结构。简化针孔模型中，其他条件不变时深度估计与焦距近似成比例，因此焦距误差会以相同方向进入测距，但“焦距变化 1% 就必然造成距离误差 1%”不能直接推广到带畸变、目标姿态变化的完整 PnP。光圈环转动则会改变进光量、景深，并可能改变暗角；分割边界与远近目标清晰度也会随之变化。软件仍在使用标定时保存的 $f_x$、$f_y$ 和畸变参数，所以这类硬件状态变化通常不会自行报错。

*现场处置*。先保存一张原图和当前镜头状态，再检查对焦环、光圈环与跨过环和镜筒的见证线是否对齐。只有标记位置确实对应现有标定文件时，才能把环恢复到该位置并继续使用这份内参；若位置关系不明，旧标定文件不能修复未知的镜头状态。时间允许时，至少用固定目标在近、远两个距离检查清晰度、重投影和测距，再决定采用已验证的备用镜头配置还是降级运行。

*赛前验证*。优先使用镜头自带锁环或可靠的机械限位，并在环与镜筒之间画见证线。确需固定剂时，应按镜头和结构件材料选择经团队验证、可维护的方案，避免让胶进入光学组件，也不要把所有镜头一律用热熔胶或螺纹锁固剂封死。内参文件应同时记录相机编号、镜头编号、分辨率、对焦与光圈位置，使“恢复标记”与“加载标定”成为同一个操作。

==== 间歇断流：供电与信号都要检查

*现象*。相机间歇掉线，重连后恢复；更换线缆、接口或供电方式后发生频率可能改变。应用日志通常只能看到取流失败或设备重连，单凭这些现象还不能判断是欠压。

*可能机制*。USB 端口供电能力、线缆压降、无源集线器分配以及车载电源瞬态跌落，都可能使相机端电压越过设备工作边界。但信号完整性、接头接触、电磁干扰（EMI）、相机固件、主控控制器和驱动错误也能产生相似的断流或重枚举。本书没有对某一台车完成同步电压测量和故障复现，因此供电不足应作为待验证候选，而不是由“随机掉线”直接得到的诊断。

*现场处置*。保存应用日志和内核日志，记录掉线时是否发生 USB reset、disconnect 或协议错误。随后一次只替换一个变量：缩短并固定线缆、绕过集线器、换已验证接口，或改用规格匹配的独立供电集线器。若条件允许，在相机端而不是只在电源输出端测量负载变化时的电压；把掉线与大功率机构动作对齐，也比笼统地说“负载大”更有区分力。

*赛前验证*。记录重连次数、连续取流失败、USB 错误、实际帧率和丢帧计数，使偶发故障能在连续长时间运行测试（也叫浸泡测试）中被发现。可观测性不能代替修复，但能为多轮、一次只替换一个变量的 A/B 对照提供故障计数。最终还应在底盘、云台和发射机构按比赛负载运行时验证整条链路，而不只在桌面电源下测试相机。

==== 驱动失败策略：进程内恢复还是外部重启

*现象*。视觉进程退出，日志末尾可能有相机错误；也可能进程还在，但取流线程已经停止。`RCLCPP_FATAL` 只是日志级别，本身不会自动关闭进程，是否退出要结合后续的 `shutdown()`、异常抛出或返回路径判断。

*可能机制*。有些驱动在连续失败达到阈值后退出，把恢复交给 systemd、容器或其他进程守护；另一些驱动会在进程内关闭设备、重新枚举并恢复取流。两种架构都可以成立。外部重启必须恢复配置、发布就绪状态并重新建立依赖连接；进程内恢复则要正确释放 SDK、缓冲区和设备句柄。真正的风险是驱动选择了前一种策略，而部署没有守护，或者守护把尚未就绪的进程误判为可用。

*现场处置*。同时检查进程、取流帧率和日志时间线。进程已退出且部署未自动恢复时，先按既定启动流程拉起；进程仍在但无图时，不要仅凭一条 FATAL 日志反复启动第二份实例，应先停止旧实例或执行驱动提供的恢复流程。

*赛前验证*。明确恢复责任并写进部署文档：要么验证设备级重连，要么验证外部守护。systemd 中，异常退出后重启通常可用 `Restart=on-failure`；确实要求正常退出后也拉起时再考虑 `Restart=always`，并配置启动间隔和频率限制，避免永久故障造成重启风暴。还要让节点在相机参数读回、首帧到达和下游连接恢复后再报告就绪。在安全测试环境中注入拔线、断电、设备占用和 SDK 错误，分别检查恢复时间与状态是否完整。

==== 设备节点与外触发边界

STM32 原生 USB CDC 设备常枚举为 `/dev/ttyACM*`，CH340、CP2102 等 USB 转串芯片常枚举为 `/dev/ttyUSB*`，但这只是常见模式，不应把节点前缀当作设备身份。照抄只匹配 `ttyACM` 的 udev 规则时，转串设备自然不会生成预期符号链接。写规则前先用 `udevadm info --attribute-walk --name=/dev/实际节点` 查看设备链，确定稳定的厂商、产品、序列号或接口属性，并分清属性属于当前设备还是父设备；完整规则仍见「串口模块」。

外触发还要核对相机的数据手册和实际计数器。曝光、读出与触发周期发生重叠时，不同型号可能忽略触发、排队等待、允许流水重叠，或者只增加丢触发计数，并非所有相机都会“直接吞掉脉冲”。因此，除了让曝光时间满足型号规定，还应同时观察触发计数、成功帧数、时间戳间隔和丢帧统计，在目标帧率下验证曝光与读出余量。

==== 待验证假设：散热与降频

“运行一段时间后因温度升高而降频，继而导致掉帧”在机制上成立，但本书没有找到包含温度、实际频率、负载和分阶段耗时的可复现实车记录，因此不能把“越跑越慢”直接归因于散热。功耗模式改变、内存或文件描述符泄漏、队列累积、录包写入和输入场景变化也可能形成相似曲线。

验证时应在同一时间轴记录 CPU/GPU 温度、实际时钟、功耗模式、各阶段耗时、队列深度和丢帧数。温度与延迟一起上升只能说明相关；在负载近似不变时改善散热，或固定散热条件后切换已验证的功耗模式，才更能区分热限制与其他原因。即使暂时不能定位，也可先设置温度和帧率告警，避免把尚未验证的解释写成结论。

=== 时间：先分清时钟域，再处理跳变与漂移

「时间戳对齐」已经说明图像与姿态错配会造成多大误差，「工业相机」的验收清单则要求时间戳单调。本节进一步回答两个现场问题：相邻帧的 $Delta t$ 为什么会异常，不同设备的时间戳又为什么不能直接相减。

*现象*。跟踪器偶尔丢失仍在视野内的目标，预测位置突然跳变，或者协方差、新息与云台指令在同一帧出现异常。另一类现象更缓慢：刚启动时对齐尚可，运行一段时间后运动目标偏差逐渐变化。它们都与时间错误相容，但误检、队列积压、模型失配和机械变化也可能产生相似输出，必须结合时间戳和队列日志判断。

*可能机制一：同一时钟发生不连续变化*。滤波器用相邻采集时刻之差构造 $Delta t$。`CLOCK_REALTIME` 或 `std::chrono::system_clock` 表示可与日历时间对应的系统时钟；时间同步服务如何调整它取决于实现、偏差大小和配置，可能缓慢校频，也可能步进。ROS time 在仿真、暂停或 bag 回放时同样可能停住或跳变。若这些时间戳直接进入预测，$Delta t <= 0$ 会让状态转移 $bold(F)(Delta t)$ 按非预期方向传播，过大的 $Delta t$ 则会把一次丢帧或时钟跳变当成很长的正常运动。

协方差未必能替你发现负 $Delta t$。以「卡尔曼滤波」中的分段白噪声模型为例，$bold(Q) = sigma_a^2 bold(Gamma) bold(Gamma)^top$ 对任意实数 $Delta t$ 都是半正定的；负号会改变部分交叉项，却不会让这个外积自动失效。因此，时间戳逆序时滤波计算仍可能给出外观正常的矩阵。这个结论只针对上述外积构造，其他离散化不一定保留半正定性；无论矩阵是否报警，负采样间隔都不应作为正常预测输入。另一方面，过大的 $Delta t$ 会增大过程噪声和预测协方差，进而增大新息协方差；使用归一化新息门限时，这可能削弱对某些异常量测的拒绝能力，但不等于所有量测都会通过。

*可能机制二：时钟域之间缺少映射*。相机硬件时钟、电控 MCU 计时器、主机单调时钟和系统时钟各有原点与频率。只标定一次时间偏移（offset）而不估计频率差时，两端时间会逐渐漂移。反过来，把整条链都替换成各自的单调时钟也不能解决跨设备相减：每个单调时钟内部适合计算间隔，它们之间仍需要同步，或显式估计时间偏移与频率偏差（skew）。随时间变化的偏差应优先检查这一映射，同时保留温度、机械和状态估计等其他候选。

*现场处置*。先保存异常前后的原始时间戳、时钟来源、帧序号、接收时刻和队列深度，再确认是单个源逆序、长时间无数据，还是两个时钟域的映射漂移。局部预测遇到不符合采样节拍的 $Delta t$ 时，应按项目预先定义的策略丢弃该次预测、重新初始化或进入降级模式。多机系统不要为了排除一次跳时就直接关闭时间同步；这可能避免一次系统时钟调整，却会引入持续的跨机漂移。应检查同步服务状态和策略，并在安全条件下重新估计映射。

*赛前验证*。为每个时间戳写明“事件是什么、在哪个时钟域、精度和同步方式是什么”。曝光或触发时刻应尽量保留采集源时间戳，不要用晚到的回调接收时刻替代。单机内部的耗时、超时和相邻间隔使用 `std::chrono::steady_clock` 或 ROS 2 的 steady clock；跨进程、跨主机或跨设备融合则要建立可验证的时钟映射，并监控 offset 与 skew。最后依据实际帧率和最大可接受丢帧设置 $Delta t$ 范围，注入暂停、逆序、长间隔以及同步重启，确认滤波器不会把时间异常当成正常运动。

=== 系统：超过单局时长的累积问题

这一组问题的共同点是积累时间可能长于一局比赛。以七分钟一局为例（具体时长以当季规则为准），低速资源泄漏、录包增长和网络参与者变化未必在一次功能测试中出现，却可能在连续训练、候场与比赛之间累积。测试时长因此要覆盖计划连续运行时间，并包含反复重连、目标切换和启停等真实事件。

==== 低速资源泄漏

「基础篇」计算机系统基础已经计算过高速内存泄漏：若每帧遗失一张完整图像的所有权，资源会很快耗尽，并可能触发分配失败或内存不足处理。现场更难发现的是低频泄漏，例如每次重连遗失一块缓冲区、每次保存失败少关闭一个文件描述符，或者目标切换后仍保留旧对象。它们在一局内的增长可能不明显，却会降低后续几局的余量。

*现场处置*。已知存在低速泄漏且暂时无法修复时，可以把经过验证的计划重启作为隔离措施，但它不是修复。重启流程必须安全停止执行器与录包、清理设备占用，重新写入配置，并在相机、串口和下游节点就绪后才允许自动控制。若这些条件没有验证，“每局重启”本身也可能制造新故障。

*赛前验证*。做覆盖连续运行窗口并留有余量的浸泡测试，例如连续四局以上，同时周期性触发重连、目标切换和录制。记录常驻内存（RSS）或按共享页分摊后的内存（PSS）、堆使用量、GPU/厂商 SDK 缓冲区、线程数和打开的文件描述符。缓存预热时内存可以先增长后稳定，文件描述符也可能随日志轮转小幅波动；判断重点是预热后是否仍有与事件次数或时间稳定相关、在重复测试中可复现的净增长，而不是要求每条曲线从启动起完全水平。

==== 磁盘写满

*现象*。日志或录包写入先后失败，应用出现 `ENOSPC`、数据库关闭或临时文件创建错误。磁盘空间耗尽本身通常返回 `ENOSPC`，不会自动把文件系统重新挂载为只读；若同时出现只读挂载，还应检查内核日志中的 I/O 错误、文件系统损坏和设备故障，不能把两者合并成同一个原因。

*可能机制*。录包需要先计算持续码率。假设图像是紧密排列、每通道 8 bit、三通道且不压缩，1440×1080 一帧占 $1440 times 1080 times 3 approx 4.45$ MiB；100 fps 约为 445 MiB/s，连续七分钟约 180 GiB。实际值还受像素格式、行跨度、消息头、索引、其他话题和文件系统开销影响。无损压缩保留像素，但压缩率取决于画面内容并会增加计算负载；有损压缩还能进一步降低码率，代价是可能丢失诊断所需的细节。这个例子说明该配置下原图全量录制通常难以持续，并不表示所有原始图像录制都不可行。

*现场处置*。用 `df -h` 检查容量、用 `df -i` 检查文件系统索引节点（inode）数量，并查看录包进程与内核错误。空间告急时先停止新增录制，再按团队的数据保留规则处理旧文件；若故障证据尚未备份，不应不加区分地删除。为了保证基本功能，可以切换到已验证的低码率记录方案，但要明确这会减少赛后证据。

*赛前验证*。为录包设置总容量、单文件大小或时间窗口，并在写满前停止或按明确策略循环覆盖；同时监控剩余空间、写入吞吐和丢消息数。把数据放在与根文件系统隔离且有配额的分区，可以限制 `ENOSPC` 的影响范围，但应用仍需正确处理写入失败。最后按一天的比赛与调试安排计算保留量，并实际录制足够时长，确认编码负载和磁盘持续写速不会反过来拖慢视觉链路。

==== 网络与 DDS 发现负载

*现象*。节点只在共享网络中启动缓慢、话题匹配不稳定或额外占用 CPU/带宽，并能发现不属于本车的参与者。这些现象也可能来自无线丢包、防火墙或服务质量（QoS）策略不匹配，需要用 DDS/ROS 2 诊断信息区分。

*可能机制*。许多 ROS 2 DDS 配置使用 UDP 多播进行初始发现。同一 `ROS_DOMAIN_ID`、网络可达且发现配置兼容的参与者会交换元数据；参与者和端点增加时，发现与匹配开销随 DDS 实现、拓扑和端点数量增长，不能一律写成参与者数量的平方。共享无线网络中的多播传输效率较低时，这部分开销会更明显。

*现场处置*。如果本车所有必需节点确实都在同一主机，且不需要远程监控或跨机传感器，可以断开共享网络，或在重启相关进程后使用 `ROS_LOCALHOST_ONLY=1`。这个环境变量会改变通信范围，不能在仍有必要远端参与者时直接启用，也不能假设对已经运行的进程即时生效。

*赛前验证*。为车队规划 `ROS_DOMAIN_ID` 并写入启动环境，避免无意使用默认域。确需跨机通信时，根据实际使用的 ROS 中间件（RMW/DDS 实现）选择静态对端（peer）、发现服务器或受控多播等方案，并验证相应配置；这些能力和语法并不在所有实现间通用。还要在与赛场接近的网络中检查发现时间、端点数量、带宽和断网后的行为，而不是只验证节点能互相 `ping`。

==== 区分图传、自瞄成像与裁判数据

操作手图传与板载工业相机通常是两条直接链路：前者经过赛事规定的图传系统，后者多通过 USB 或以太网接入车载运算平台。因而图传画面花屏并不等于工业相机没有出图，自瞄识别失败也不应从调整图传天线开始排查。具体频段、设备数量和使用限制会随赛季变化，应以当季规则和设备文档为准。

不过，“两条图像链路分开”不等于故障完全独立。它们可能共享电源、运算资源、结构空间或裁判系统状态，网络配置错误也可能同时影响远程调试和数据接收。现场应分别观察图传状态、工业相机帧率、视觉输出以及裁判数据新鲜度，再根据系统连接图确定共享点。

公开公告曾记录无线干扰与后续链路调整；本书未独立复核该案例，引用它只用于说明无线输入可能中断。程序应为每类裁判数据定义更新时间、有效期和过期策略，不能统一“保留上一次的值”。例如，敌我颜色在本场已经可靠确定时可以保留最后一次有效值并标记陈旧；弹速可在明确范围内退回该车唯一的已标定配置；热量、射击状态等动态安全数据过期后，则可能需要禁止相关自动发射或采用保守限制。最终策略必须同时符合当季规则和本车安全边界。

=== 端到端延迟：先明确测量的起点与终点

上一章「性能优化技巧」用分段预算回答“工程时间可能花在哪”。实车还需要另一种测量：把一次曝光与由它产生的控制、发射结果对应起来。各函数耗时相加不能自动得到端到端延迟，因为队列等待、线程调度、串口传输、电控周期和机构动作可能跨越多个进程与设备。

一种可行思路是利用裁判系统返回的射击数据，把报告事件匹配回触发该次指令的图像帧。这里必须区分*物理发射时刻*与*射击数据到达处理端的时刻*。如果协议只提供报告到达事件，而没有可映射到同一时钟域的物理时间戳，那么“到达时刻减曝光时刻”得到的是一个报告代理量：它包含期望测量的感知到发射链路，也叠加了裁判端生成、传输和处理端接收延迟。只有在事件与帧正确匹配、两个时间戳已映射到同一时钟域，并且附加报告延迟的定义已知时，才能进一步解释这个差值。

rm.cv.fans 的公开材料自述了 35～110 ms、均值约 82 ms 的实车结果。按其工程日志的描述，视觉端在 `camera.read` 前把来自 STM32 的时钟值与图像绑定，电控决定发射后保存该帧的时钟值，再在裁判系统更新射击数据时取差。后续日志又说明，当时电控状态包固定每 50 ms 发送，并因无法实时取得可靠的电控时间戳而放弃了这套联合监听方案。前一个记录仍能说明项目当时观察到的数值范围，后一个记录则表明图像所绑时钟值的新鲜度、曝光事件位置和报告路径都没有完成独立标定。

本书没有拿到原始样本，也没有在本地复现这套匹配过程，因此把三个数字称为*项目报告的延迟代理范围*，不把它们移植成其他车辆的参数，更不把它们视为上图理想测量方案的验证结果。即使暂时不能剥离报告链路，在匹配方法和附加路径保持同一配置的前提下，重复取得的代理量仍可用于比较改动前后的变化，并提示逐段计时遗漏了哪些环节；要作为弹道补偿的绝对延迟，还需把图像、发射和报告事件映射到定义明确的时刻，并校准附加路径。

#figure(
  image("images/adv-field-latency-loop.png", width: 100%),
  caption: [射击报告回填方法示意（脚本 `adv_field_latency_loop.py`）。上排区分视觉进程内可插桩的灰色环节与需要跨设备观测的品红环节，矩形宽度不代表耗时；离膛后的裁判检测与报告返回也是代理量的一部分。箭头表示在完成帧—报告匹配和时钟映射后构造差值，不表示已经取得物理离膛时刻。下排只复述项目报告的 35～110 ms 范围与 82 ms 均值；没有原始样本时，范围不能当作概率分布。],
)

只有当起点确为所配图像的曝光时刻、终点确为同一颗弹丸的报告到达时刻、报告由离膛后的事件产生、两个时间戳的映射正确，且报告附加路径的耗时非负时，这个差值才是相应“曝光到离膛”物理链路延迟的条件性上界。若起点只是读取图像前后取得的主机时间、连发时错配了弹丸、时钟仍有偏移，或者报告字段的语义不是预想的事件，上界关系都不成立。实施时应记录图像序号、控制序号、发射计数、原始时间戳和接收时间，并用已知节拍或外部高速相机抽查对应关系。

82 ms 也不是“放进 $t_d$ 就能精确消除”的常数。采用均值、中位数还是与状态相关的延迟模型，取决于希望最小化哪种误差（损失函数）以及样本分布；任何固定值都只能消除相应模型下的中心偏差。35～110 ms 目前只是报告范围，没有样本密度、分位数和标准差，因而不能由 75 ms 的跨度推出“多少比例的射击受到抖动影响”。不过，可以把这个范围代入简化运动模型，检查延迟变化在所选工况下是否足以占用显著误差预算。

#figure(
  image("images/adv-field-jitter-residual.png", width: 100%),
  caption: [报告范围上的简化残差包络（脚本 `adv_field_jitter_residual.py`）。曲线是推导值，不是落点实测：假设装甲板以恒定角速度绕半径 $r = 0.25$ m 的圆运动，其他误差为零，并选 $t_0 = 82$ ms 作为标称补偿，以小角度切向近似得到 $e approx omega r |t - t_0|$。横线只是小装甲板半宽 6.75 cm 的几何参照，不是命中判据。在这些假设下，14 rad/s 曲线低于参照线的延迟区间约宽 39 ms；报告范围中高于参照线的横轴长度接近一半，但这不等于近一半样本或近一半射击越界，因为原始分布未知。],
)

这张图能支持的结论是：在所选半径、恒定角速度、固定补偿和报告范围内，一阶模型给出的位移量级可能超过 6.75 cm，因此延迟变化值得进入误差预算。它不能证明延迟是实际失准的主导项，也不能证明某个点必然脱靶或滤波器调节无效；实际命中还取决于瞄准点、装甲板尺寸与姿态、相机测量、完整运动模型、弹道、机构、控制与开火策略，事件代理误差也尚未比较。下一步应在本车采集可匹配的样本，报告分位数或标准差，并把延迟与落点共同记录。若数据确实显示少量很慢的样本形成长尾，再按上一章的顺序处理队列积压、线程竞争和调度配置。

=== 控制耦合：单端测试覆盖不到闭环接口

视觉输出、电控跟随和机械结构分别通过测试，并不代表组合后的闭环稳定。接口时序、姿态来源、坐标变换和补偿定义只有在整机运动时才同时出现。本节给出延迟和补偿重叠两条检查路径，但不把它们当作振荡或运动偏差的唯一原因。

==== 云台振荡：延迟是候选因素之一

*现象*。自瞄接管并锁定运动目标后，云台出现持续高频抖动或幅度增长的往复运动；手动模式或静止目标下不明显。还应记录振荡频率、幅值、是否触发限幅以及机械声音，因为结构共振、摩擦、饱和、反馈噪声、符号错误和控制器参数都可能给出相似现象。

#block(breakable: false)[
*可能机制*。视觉测量、角度指令、电机驱动和云台运动构成闭环。理想纯延迟 $tau$ 在频率 $f$ 处增加的相位滞后为：

$
phi = 360 degree dot f dot tau
$
]

这个公式只计算*额外相位*，不直接给出稳定性。闭环稳定性需要结合完整的开环传递函数或实测频率响应；常见检查之一，是看增益穿越频率（开环增益等于 1 的频率）处还剩多少相位裕度。控制器、采样保持、电机、结构模态和滤波都包含在这条开环路径中。延迟越大、所考察的频率越高，额外相位滞后越多，因此它是值得测量的风险项，但观察到振荡还不能据此确认根因。

还要分清两个 $tau$。上一节的代理量面向感知—发射链，可能包含串口、电控、机构与射击报告路径；云台位置回路在“云台运动改变测量”处闭合，拨弹与离膛不在这条反馈路径中。上一节报告的 82 ms 既未被本书复现，也不是云台回路延迟，更不能在匹配与报告路径未知时保证是它的上界。回路延迟应使用同步到同一时钟域的指令时间戳与编码器响应，或经标定的高速相机运动时刻单独测量；必须定义超过噪声的运动起始阈值，并报告采样周期和不确定度。

下表保留几个数值算例，作用是帮助估算纯延迟项的量级：

#figure(
table(
  columns: (auto, auto, auto, auto),
  align: (left, center, center, center),
  stroke: (x, y) => (
    top: if y == 1 or y == 0 { 1pt } else { 0pt },
    bottom: if y == 3 { 1pt } else { 0pt },
  ),
  inset: 8pt,
  [*闭环回路延迟 $tau$*], [*$f = 2$ Hz*], [*$f = 5$ Hz*], [*$f = 10$ Hz*],
  [20 ms], [$14.4 degree$], [$36.0 degree$], [$72.0 degree$],
  [50 ms], [$36.0 degree$], [$90.0 degree$], [$180.0 degree$],
  [82 ms（上一节的项目报告代理均值，仅作算例）], [$59.0 degree$], [$147.6 degree$], [$295.2 degree$],
),
caption: [纯延迟的额外相位滞后，按 $phi = 360 degree f tau$ 计算，全部是推导值。读表时应使用本车测得的云台回路延迟和实际关注频率，并与完整开环模型或频响测量结合。单个格子超过 $60 degree$ 或 $180 degree$ 都不能独立证明相位裕度耗尽或系统必然自激；表中 82 ms 只展示误用上一节项目报告代理均值会得到怎样的数值，不应代入本车控制器。],
)

另一个候选因素是姿态源不一致。视觉坐标变换与电控反馈若使用不同 IMU，却没有标定零偏、比例、安装坐标，或没有把读数映射到同一时刻和坐标系，两路差异会进入控制接口。两块 IMU 本身并非错误：经过标定和融合后可以用于冗余或覆盖不同动态范围；未经定义地混用才会带来问题。

一份公开战队记录自述，快速运动时的振荡在同步时间戳并改用同一陀螺仪后消失。本书未独立复现，而且两项改动同时发生，无法仅凭该记录区分通信时序、姿态源或其他随改动变化的因素。它能说明这些检查在该项目中有效，不能把“延迟未补偿和双 IMU”推广为所有云台振荡的已确认根因。

*现场处置*。先切换到赛前验证过的低增益配置；若振荡仍存在，检查机械松动、结构共振、输出饱和、反馈噪声、时间戳和坐标符号。没有验证过的“降低视觉发送频率”或“临时加一级低通”可能增加采样与滤波延迟，不能当作通用稳定手段。无法在安全范围内恢复时，应进入已经演练过的手动或降级模式。

*赛前验证*。分别测量通信/采样延迟和电机响应，在安全条件下测量开环频率响应，或向闭环内注入小幅测试信号并测量环路传递，获得实际穿越频率和裕量。闭环阶跃实验可以检查响应速度、超调、饱和与起动时延，但不依赖模型时不能单独给出相位裕度。延迟补偿、前馈或预测控制都依赖具体对象与模型，应在目标控制器上验证，不能只按相位表添加。若有多路 IMU，则为每路保存标定、坐标变换、时钟映射和健康状态；若只需一路，统一姿态源可以减少接口复杂度，但仍要验证其失效策略。

==== 补偿重叠与职责不清

*现象*。静态目标偏差较小，目标运动越快，超前或滞后偏差越明显；视觉端和电控端各自的单元测试都能通过。这个速度相关特征会把范围缩小到预测、时序、坐标和执行器动态等路径，却不能单独证明发生了重复补偿。

*可能机制*。同一物理项若在接口两端重复加入，确实会造成过量补偿。例如手眼标定已经包含相机到枪口的固定旋转，电控零位又加入同一固定角；或者两端都按相同定义补偿同一段延迟。不过，视觉预测目标未来位置与电控为减小斜坡跟踪误差（目标角度持续匀速变化时的跟随偏差）所用的前馈，可能分别描述目标运动和执行器动态，并不因都含“提前”就必然重复。应展开两端公式，比较各项对应的事件、时间区间、坐标系和符号。

*现场处置*。在安全测试台上记录每个补偿项的单独输出，随后一次禁用一项做 A/B 对照。偏差减小只说明该项或它的交互值得继续检查；参数数值错误、单位错误或模型不适用仍与结果相容，不能立即宣布找到了根因。比赛现场若有经过验证的无预测配置，或关闭部分功能的降级配置，可回退到该配置，而不是临时把未知参数全部归零。

*赛前验证*。接口文档应逐项写清延迟、枪口偏角、重力下坠、弹速修正和控制前馈的物理定义、负责人、输入输出、单位、符号、时钟域及验收实验。随后用静止目标、匀速目标和方向相反的运动轨迹做整机测试，确认各项在换向时表现符合定义。这样既能发现真正的重复补偿，也能保留视觉预测与控制前馈各自必要的作用。

=== 五分钟：从恢复能力开始现场排查

赛场只给出症状和倒计时，通常不足以完成根因定位。五分钟流程有两个更现实的目标：在不引入额外风险的前提下恢复一个已验证的可用状态，并保存足够证据，让赛后能继续区分原因。若故障涉及失控运动、意外发射或人员安全，应先停用相关执行器，不能为了赶时间跳过安全边界。

现场诊断的核心不是“多试几个参数”，而是选择结果能够改变下一步判断的检查。盲目同时调整阈值、增益和补偿会混淆效果，也难以回滚；但恢复一整套已验证配置，或依据明确假设只改变一个变量，可以是正确动作。每次检查前都应说清：不同结果会让哪些候选原因更可能或更不可能，又有哪些原因无法由这一步区分。

具体可以按下面的顺序执行。

第一步，*保存现状并核对变化*。记录时间、故障表现、部署版本、配置文件、相机读回参数、进程状态和日志末尾；资源允许时保留原图、录包、内核日志与裁判数据。随后比较上一场可用状态与当前状态，包括代码、构建产物、模型、参数、固件、线缆和镜头。`git diff` 与 `git log` 只能解释受版本控制的源码变化，不能证明车上实际运行的二进制或相机内部参数相同，因此部署清单和产物校验值同样重要。

若变化范围明确，可以切回赛前验证过的部署包；不要在没有保存现状时执行会覆盖配置和证据的操作。这里的“回退”是恢复整套已知状态，不是随意从仓库取出某个旧文件。恢复后仍要完成启动自检和最小功能检查，历史上可用不等于此刻硬件一定可用。

第二步，*在链路边界观察输入和输出*。感知到执行可以分为相机图像、识别、位姿与预测、通信、电控和机构。选择中间边界检查消息内容、序号和时间戳，能快速缩小范围；但“识别节点有输出”只证明它产生了某种输出，不证明此前图像、类别和位姿都正确。每次切分都要同时检查存在性与合理性。

第三步，根据最先看到的症状选择信息量较大的检查。下表给的是起点和证据范围，不是原因对照表：

#figure(
table(
  columns: (auto, auto, auto),
  align: (left, left, left),
  stroke: (x, y) => (
    top: if y == 1 or y == 0 { 1pt } else { 0pt },
    bottom: if y == 8 { 1pt } else { 0pt },
  ),
  inset: 7pt,
  table.header([*现场症状*], [优先检查的边界], [检查结果能说明什么]),
  [完全没反应], [进程、就绪状态与执行器使能], [区分进程退出、进程存活但未就绪，以及输出被安全状态禁止],
  [识别不到目标], [相机帧与识别输入], [帧率、原图和参数异常把范围移向取流或成像；图像正常后仍需查预处理与识别],
  [识别有输出但云台不动], [视觉发送与电控接收], [对比序号、时间戳和计数，定位消息在哪一侧停止或变旧],
  [云台运动方向错误], [坐标系、符号与模式], [在限功率安全测试中验证各轴方向，区分映射错误与模式/执行器错误],
  [静止尚可、运动时偏], [运动相关预测路径], [切到已验证的无预测配置后偏差减小，只能把范围缩到时序、速度估计或补偿等路径],
  [近处尚可、远处偏], [光学、几何、弹道与机构], [检查镜头标记和多距离测距/重投影，不能由距离趋势单独确定内参或弹速],
  [运行一段时间后飘或丢], [时间、队列、资源与估计状态], [对齐 $Delta t$、队列深度、资源曲线和新息，寻找共同开始变化的量],
  [云台抖动或振荡], [闭环与机械状态], [切换已验证低增益配置并查饱和、噪声和结构；减轻只说明闭环参与，不证明延迟],
),
caption: [赛场症状排查表。中列给出优先观察点，右列限定该检查最多能支持的结论。优先选择快速、可逆且不破坏证据的动作是现场原则，不是“一分钟以上一律不做”的硬规则；安全检查和恢复已知部署需要更久时，应按实际风险完成。],
)

第四步，*一次只改变一个主要变量并记录结果*。写下修改前后的值、操作时间、观察窗口和恢复方法。若必须恢复整套已知配置，那是一个明确的部署级变量；不要一边回退版本，一边换镜头和调控制参数，然后把恢复归因于其中任意一项。实验无效后按记录恢复，避免让多次临时修改累积成新的未知状态。

第五步，*选择已验证的可用模式*。现场无法完成原因区分时，可以关闭故障功能、降低自动化等级或切换手动，但前提是这些模式在赛前演练过，并且仍满足安全与比赛规则。模块隔离、健康状态、未就绪时禁止自动控制的门控，以及降级入口，都应在设计阶段准备；临场才发明的降级路径通常缺少验证。

最后把证据带回实验室。录包很有价值，但不是唯一来源：版本与配置清单、应用和内核日志、崩溃转储（core dump）、相机原图、时间同步状态、电压或逻辑分析仪记录都可能成为区分原因的关键。比赛结束后尽快复制并标注发生时刻、车辆和操作，确认文件完整后再清理设备。即使没有完整录包，也应保留现有证据，并明确哪些输入缺失导致哪条结论无法验证。

==== 本章小结

本章把前文的模块知识放回整车环境：光照参数要保持在已验证工作域，镜头、供电和触发需要独立证据，时间间隔与跨设备时钟映射不能混为一谈，资源和磁盘测试必须覆盖连续运行窗口，控制振荡则要在完整闭环中比较延迟、机械、传感器和控制器等候选原因。

射击报告回填提供了一种观察跨设备链路的思路，但在没有物理事件时间戳、可靠匹配和时钟映射时，得到的是包含报告路径的代理量。项目自述的 35～110 ms 范围可以用于简化误差预算，不能替代本车样本，也不能单独证明命中率由延迟主导。贯穿这些案例的做法是保存现状、写清测量定义、用可区分的检查缩小范围，并准备经过验证的恢复和降级路径。

下一篇「项目分析」将分别阅读 rm_vision、rm.cv.fans 与 sp_vision，考察这些公开项目如何组织数据流、时序与工程边界。它们提供的是具体实现和取舍，不保证覆盖本章列出的每一种失效模式；读者仍需结合自己的硬件、规则和实测结果判断哪些方法可以迁移。
