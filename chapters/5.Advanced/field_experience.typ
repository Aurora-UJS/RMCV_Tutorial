#import "/template/template.typ": *

「串口模块」一章交棒给这一篇时数了三件事，前两件——换兵种、榨性能——已经办完了。这是第三件，也是进阶篇的最后一章：把这套系统在赛场上会以哪些方式死掉，一条条归档。

先说清楚这一章*不是*什么。它不是把前面散落的坑再讲一遍。走到这里，你已经在十几章里交过学费了：弹速在三个模块里各写各的、PnP 尺寸填错 5 mm、engine 文件拷到另一台机器就跑不起来、udev 规则里少写一个 S。那些坑各自长在它们该长的地方，本章只给它们建一份索引，不重复。

本章真正要补的是另一层：*赛场级失效模式*。前四篇讲的每一个模块，都是在实验室里被单独验证过的；而赛场是把它们全部塞进一台会被撞、会发热、会掉线、会跑满七分钟的机器里，再拉到一个灯光、电磁、网络都不由你控制的环境中去。在那里死掉的方式，往往不属于任何一个模块——它长在模块之间，长在时间轴上，长在你和电控的接口上。这些没法按"章"归档，只能按*怎么死*归档。

所以本章按五种死法分节：光照、硬件、时间、系统、控制耦合。每一条给四段：*症状*（你在场上会先看到什么）、*根因*（为什么会这样）、*现场应急*（五分钟内能做什么）、*赛前预防*（怎么让它根本不发生）。四段里最要紧的是第一段和第三段——根因和预防是工程师的语言，症状和应急才是赛场的语言，而赛场上你手里只有症状。

最后一节讲的不是坑，是方法：在"五分钟后必须让车上场"的压力下，怎么把故障定位这件事做得像工程而不是像抽签。

=== 索引：已经交过学费的地方

动手之前先过一遍这张表。它把全书散落的坑集中到一处，方便你在赛前当清单用；每一条的完整来龙去脉都在对应的那一章，本章不复述。

#figure(
table(
  columns: (auto, auto, auto),
  align: (left, left, left),
  stroke: (x, y) => (
    top: if y == 1 or y == 0 { 1pt } else { 0pt },
    bottom: if y == 14 { 1pt } else { 0pt },
  ),
  inset: 7pt,
  table.header([*坑*], [*在哪一章交的学费*], [*一句话*]),
  [同一个弹速在三处硬编码], [「自瞄算法设计」], [同一物理量必须只有一个来源],
  [PnP 装甲板尺寸 135 与 140 之争], [「装甲板识别」], [接口两端对"宽度"的定义要写死],
  [engine 文件绑定 GPU 与驱动], [「装甲板识别」], [不能拷贝分发，须在目标机现场构建],
  [红蓝反色导致打友军], [「工业相机」], [Bayer 版式配错，赛前必须实拍验证],
  [跟踪阈值按帧数写死], [「装甲板的跟踪」], [帧率一变语义就变，应按时间折算],
  [$Delta t$ 写死成常数], [「装甲板的跟踪」], [丢帧时预测步长与真实时间脱节],
  [udev 规则里是 `ATTRS` 不是 `ATTR`], [「串口模块」], [少一个 S，规则静默不生效],
  [手眼欠约束也会给你一个数], [「相机标定与手眼标定」], [标定收敛不等于标定正确],
  [`publish_frequency` 是个假参数], [「自瞄算法设计」], [配置项不承载行为，改了没反应],
  [`cv::resize` 悄悄吃掉 31% CPU], [「基础篇」计算机系统基础], [掉帧要用 perf 定位，不要猜],
  [灯带、大屏、反光、队友车灯都是假候选], [「基础篇」C++ 语言基础], [误识别是常态不是意外],
  [内存泄漏与文件描述符泄漏], [「基础篇」计算机系统基础], [快性泄漏几十秒就被 OOM killer 杀掉],
  [相机上车验收清单], [「工业相机」], [过曝、拖影、反色、掉帧、时间戳单调],
  [串口上车自检清单], [「串口模块」], [波特率、帧对齐、重连、CRC、offset],
),
caption: [全书坑点索引。读表规则：这张表只是路标，不是结论——右列那句话是提醒你"这条坑存在"，真正能让你避开它的推理在中列指向的那一章里。赛前把左列当勾选项逐条过一遍，比读完本章其余部分更能救你的比赛。],
)

=== 光照：识别器活在一条很窄的光带上

「工业相机」一章已经把低曝光域的成因讲透了：自瞄工作在 1～4 ms 的极低曝光下，画面除了自发光的灯条几乎全黑，整套判据都是为那个域设计的；把曝光调亮，背景杂光被一并抬起来，判据当场失效。「基础篇」C++ 语言基础一章也已经把假候选的来源列全了——场馆灯带、大屏、地面反光、残血闪烁的灯条、队友的车灯。这两件事本章不重讲。

要补的是光照在*赛场*这个特定场合下的两条新账。

==== 帧间亮度起伏：曝光越短，越受电网摆布

*症状*。识别时好时坏，看不出规律；把画面逐帧看，会发现*画面整体亮度*（背景、地面反光、装甲板周围的杂光）在几帧的尺度上周期性起伏，而自发光的灯条本身纹丝不动。亮的那几帧背景涌过固定的二值化阈值，假候选成片冒出来，真灯条还可能和紧挨着的反光糊成一团、宽长比失真被判据踢掉，跟踪器于是频繁地丢目标又重捕。静止调试、离线跑录像都可能复现不出来，因为换个场地灯就好了。

*根因*。交流供电的照明——包括不少驱动电源做得不讲究的 LED——亮度随整流后的纹波起伏，频率是电网频率的两倍，50 Hz 电网对应 100 Hz、周期 10 ms。曝光时间远短于这个周期时，每一帧只截取了纹波的某一段相位：2.5 ms 的曝光窗只覆盖 10 ms 周期的四分之一，落在波峰的那帧和落在波谷的那帧，环境照度可以差出可观的一截。而相机帧率与 100 Hz 一般不成整数比，相位于是逐帧缓慢漂移，表现为亮度的低频拍频。这是曝光时间短所付出的代价之一：曝光越短，对光源纹波越敏感——和「工业相机」一章里"曝光越短拖影越小"的收益，是同一个参数的两面。

*现场应急*。先确认是不是这个问题：把相机对着场地灯拍十几秒，逐帧算一次画面平均亮度并打印，若它以固定周期起伏，就是纹波。确认之后，赛场上*不要去动曝光*（下一条会讲为什么），而应该往上调二值化阈值、把背景的波峰挡在门槛外面，或者把跟踪器的丢失容忍帧数放宽几帧，让波峰那几帧被跟踪器的低通吸收掉——「装甲板的跟踪」一章那个四状态生命周期正是为这类短暂丢失设计的。

*赛前预防*。理论上把曝光设成纹波周期的整数倍（10 ms、20 ms）可以把纹波积分掉，但自瞄用不起 10 ms 的曝光——拖影会直接毁掉灯条形状，这条路在 RM 里堵死。真正可行的只有一条：*让灯条亮到不在乎环境纹波*。灯条是自发光的，它的像素值只取决于自身亮度与曝光时间，纹波影响的是背景与反光。所以只要曝光足够低、灯条与背景的对比度足够大，纹波就被压在噪声里。换句话说，这个问题的解法和低曝光域本身的设计动机是同一个——这也是为什么在正确调过的车上你几乎见不到它，一旦见到，通常说明曝光已经被人调高了。

==== 赛场上最容易做错的一个动作：随手把画面调亮

*症状*。上一场还好好的，这一场识别率断崖式下跌，同时开始出现莫名其妙的假目标，云台偶尔朝观众席或者场地边缘甩过去。

*根因*。有人在调试时觉得"画面太黑什么都看不见"，把曝光或增益往上加了一点。这个动作的破坏力被严重低估了：曝光不是一个可以独立微调的显示参数，它是整套识别判据的*设计前提*。二值化阈值、灯条的宽长比范围、颜色投票的红蓝分离度、神经网络训练集的成像分布，全部是在某一组曝光增益下标定或训练出来的。曝光一动，这四样东西同时失配——阈值偏低导致背景涌入，灯条中心过曝导致红蓝通道饱和到分不开（「工业相机」一章讲过这笔动态范围的账），网络看到的图像也不再属于它的训练分布。

*现场应急*。改回去。这就是为什么每一次参数改动都必须可回滚，也是为什么曝光增益要写在配置文件里而不是在某个 GUI 里手调——文件里的改动 `git diff` 一眼可见，GUI 里的改动没人知道发生过。如果不确定被改成了什么，直接从上一场能正常比赛的那个提交把配置文件签出来。

*赛前预防*。把曝光和增益当作*一组绑定的标定结果*而不是两个可调参数，和阈值、宽长比范围一起放进同一台车的 YAML，并在文件里写一行注释说明它们是配套的。同时给所有人立一条规矩：*赛场上不允许凭肉眼调曝光*。人眼觉得舒服的亮度和识别器需要的亮度是两个东西，前者永远比后者亮得多。

=== 硬件：真正的敌人是一根松了的线

写自瞄的人容易高估算法的地位。一位长期维护开源自瞄的作者在他的工程日志里写过一句很朴素的话，大意是上车测试时他最害怕的问题是相机线松了、光圈松了——不是滤波器发散，不是网络误检，是螺丝。这句话值得当作本节的题记。

串口的断连与重连、相机的三级自愈重连，「串口模块」和「工业相机」两章已经写透了，这里不重复。本节补的是几种*不会报错*的硬件失效，它们的共同特征是：系统一切正常，日志干干净净，只有落点是错的。

==== 镜头机械松动：一次不报错的内参失效

*症状*。识别正常、跟踪正常、滤波器不发散、日志无异常，但打不中；而且偏差随距离系统性地变化——近处还凑合，远处越来越离谱。重新标定一次就好了，过几天又不行。

*根因*。镜头的对焦环或光圈环在震动下慢慢转动。对焦环转动改变了实际焦距，而 $f_x$、$f_y$ 是标定时冻结进内参矩阵的常数；PnP 解出的距离与焦距近似成正比，焦距变了 1%，测距就系统性地偏 1%，弹道解算拿到错误的距离，抬枪量随之全错。光圈环转动改变进光量，等效于有人替你改了曝光——上一节那条坑于是被机械地触发一次。这类失效不报错，因为软件层面每一个环节都在正常工作，错的是软件对硬件的一个静默假设：*内参在标定之后不再改变*。

*现场应急*。摸一下镜头，确认对焦环与光圈环还在原位；若已经动了，最快的办法不是重新标定（赛场上没时间），而是回滚到上一次的标定文件并把环转回标记位置。所以标定完成后必须在镜头上做记号——一道油漆笔的线，比任何软件都管用。

*赛前预防*。标定完成后用螺纹胶或热熔胶把对焦环、光圈环锁死，并在环与镜筒之间画一道对齐线，赛前目视检查一秒钟就能确认。这条和「工业相机」一章末尾那句"USB 线在相机一端要用带锁扣的接头、结构定型后打胶固定"是同一类工程纪律：*凡是能松的，最终都会松*。

==== 供电裕量不足：随机的、不可复现的断流

*症状*。相机随机掉线，重连后又好，掉线时刻与负载没有明显关系；换一根线、换一个 USB 口，现象会变但不消失。日志里只有取流失败与重连记录。

*根因*。USB 供电有电流上限，工业相机的取流功耗并不小，再叠上线缆压降、被动集线器的分流、以及车载电源在底盘电机大电流时的电压跌落，相机的供电就可能在某些瞬间掉到欠压阈值以下，触发一次 USB 重枚举。它最难查的地方在于*随机*：欠压是多个因素叠加到某个瞬间才发生的，实验室里桌面供电稳定，很难复现。（这一条是机制上的推断与工程惯例，本书没有做过定量的电流测量，因此不给具体数字。）

*现场应急*。缩短链路：去掉集线器、换短线、把相机直插运算平台，避开与大功率设备共用同一路供电。若必须用集线器，用带独立供电的那种。

*赛前预防*。让驱动的重连做得足够重（「工业相机」一章的三级自愈），并且——这一点很关键——*让重连事件被记录、被计数、被看见*。掉线一次自愈成功，系统表现正常，你不会知道；但如果日志里每分钟有三次重连，那就是一颗定时炸弹。把重连计数打进状态输出，是把"随机的、不可复现的"问题变成"可观测的"问题的唯一办法。

==== 驱动的致命默认：失败 N 次就退出

*症状*。视觉进程整个消失，没有段错误，日志最后一行是一条 FATAL 级别的相机错误。

*根因*。社区里流传很广的一份海康相机 ROS2 驱动，在连续取流失败若干次之后直接打一条 `RCLCPP_FATAL` 然后 `shutdown()`——它没有设备级重连这一档。也就是说，一次 USB3 瞬断就能让整个视觉节点退出，而不是像「工业相机」一章那套三级自愈那样就地爬起来。这不一定是设计缺陷：在有进程守护的部署里，"快速失败 + 外部拉起"是一种合理的架构选择，把重连的复杂度交给 systemd 或容器的重启策略。危险的是*你不知道自己选了哪一种*——直接把这份驱动拿来用、又没有配任何守护，那么第一次瞬断就是这场比赛的终点。

同样的失效，两种设计哲学：一种在进程内自愈，一种在进程外重启。两种都能上场，但它们对部署环境的要求完全不同。读第三方驱动时，*先去找它的失败路径写了什么*，比读它的成功路径更重要。

*现场应急*。确认进程真的退出了（`ps` 一下），手工拉起。

*赛前预防*。二选一并且写进部署文档：要么改驱动加设备级重连，要么给视觉进程配 systemd 的 `Restart=always`。另外做一次破坏性演习——车跑着的时候直接拔相机线，看系统是不是能在几秒内自己回来。这类演习和「实战技术篇」通信协议设定一章里"注入坏帧、中途复位"是同一个思路：*在你控制得了的时候，把故障主动制造出来*。

==== 认错设备节点类型

RM 官方的 C 板走的是 STM32 原生 USB CDC，在 Linux 下枚举为 `/dev/ttyACM*`；而 CH340、CP2102 这类 USB 转串芯片枚举为 `/dev/ttyUSB*`。网上流传较广的两份 udev 教程用的都是 `ttyACM`，如果你的板子其实是转串芯片，照抄规则就永远不生效——而且是*静默*不生效，规则文件躺在那里，符号链接就是不出现。udev 的完整写法在「串口模块」一章，这里只提这一条：*写规则之前先看清楚你的设备到底是哪一类*。

顺带一条相关的硬件时序坑：用外触发时，如果曝光时间长于触发周期，相机在还没处理完上一帧时收到的触发脉冲会被直接吞掉，表现为帧率恰好掉成触发频率的几分之一。外触发的机制在「工业相机」一章，这里补的是它的这个边界条件——*曝光时间必须留在触发周期之内*。

==== 一处诚实的空白：散热降频

"车跑久了发热降频导致掉帧"这件事在社区里被反复提起，听起来也完全合理：车载运算平台散热受限，持续满载时 CPU 或 GPU 触发热保护降频，帧率随之下滑。但写这一章时我们没能找到一份可归因、可复现的一手案例——没有温度曲线、没有前后帧率对照、没有能指认到具体平台和具体负载的记录。所以这里不给根因、不给数字，只留下这个空白，并给出一条可执行的建议：*把平台温度和当前频率打进你的状态日志*。有了这两条曲线，下次它真的发生时，你会是第一个能拿出证据的人；在此之前，它只是一个传说。

宁可留白，也不要给读者一个编出来的因果。这条纪律本身比这一节的内容更重要。

=== 时间：跳变、回拨与不同步

「时间戳对齐」一章已经把配对错位讲成了一笔算得出来的账，也给出了三代对齐方案和验证方法；「工业相机」一章的验收清单里有一行"时间戳单调"。本节补的是那行清单没有展开的部分：时间*为什么*会不单调，以及不单调之后系统具体怎么死。

*症状*。系统跑着跑着，跟踪器毫无征兆地丢掉一个明明还在视野正中的目标；或者协方差瞬间爆炸、目标位置飞出场地；或者更隐蔽——某一帧的预测量突然巨大，云台猛地抽一下再回来。日志里找不到任何错误。

*根因*。绝大多数滤波器的预测步都要用一个 $Delta t$，而 $Delta t$ 来自两帧时间戳相减。如果时间戳取自系统的墙上时钟（`CLOCK_REALTIME`、`std::chrono::system_clock`），它随时可能被校时服务步进或回拨：车一连上赛场网络，chrony 或 systemd-timesyncd 就可能把时钟往回拨几百毫秒甚至几秒。回拨之后有两种死法。$Delta t < 0$：状态转移 $bold(F)(Delta t)$ 按负时间外推，等于让目标沿运动方向倒着走一步——而量测流仍在向前走，于是下一次修正拿"现在"的观测去校一个"过去"的状态，新息凭空变大，估计当场被污染。这里要破除一个流传很广的误解：*不要指望靠 $bold(P)$ 失去正定性来发现这件事*。「卡尔曼滤波」一章给的分段白噪声 $bold(Q) = sigma_a^2 bold(Gamma) bold(Gamma)^top$ 是一个外积，对 $Delta t$ 的正负号不敏感——$Delta t^4$ 是偶次幂根本不变号，翻转的只有交叉项 $Delta t^3\/2$，而外积对任意实数 $Delta t$ 恒为半正定；$bold(F)$ 又恒可逆，于是 $bold(P) = bold(F) bold(P) bold(F)^top + bold(Q)$ 一路保持正定，没有任何数值指标会报警。「项目分析」篇同济自喵那一章里，MPC 正是靠这个性质，故意用负 $d t$ 把状态往回推半个窗口来播种求解器初值。（只有换成 1 kHz 小步长常用的那种连续白噪声离散化 $bold(Q)$，负 $Delta t$ 才真会让 $bold(Q)$ 非半正定。）时间倒流因此是一种*静默*故障，只能在预测步入口显式判 $Delta t <= 0$ 挡掉。$Delta t$ 巨大（时钟往前跳）：$bold(Q)$ 按高次幂膨胀，$bold(P)$ 一步涨到天上，新息检验于是全部通过，滤波器退化成一个只会跟随测量的低通器——这时候恰好来一个误检，它会毫不犹豫地跟过去。

还有一种更隐蔽的不同步：视觉端和电控端各用各的时钟，两边的时钟频率有微小差异，于是标定好的 offset 会随比赛时间缓慢漂移。开局能打，打到后半场越来越偏——这类"随时间单调恶化"的症状，几乎总是时钟问题。

*现场应急*。断网。这一条听起来粗暴，但在赛场上是最快的：断掉网络就断掉了校时服务改动时钟的途径。如果你怀疑是漂移，重新跑一次「时间戳对齐」一章那个甩云台标 offset 的流程，几分钟就能做完。

*赛前预防*。三条，按重要性排序。第一，*全链路只用单调时钟*：C++ 用 `std::chrono::steady_clock`，ROS 2 里用 `rclcpp::Clock(RCL_STEADY_TIME)` 而不是默认的系统时钟。单调时钟不会被校时服务改动，这是它存在的全部理由。第二，*在预测步入口对 $Delta t$ 设防*：小于等于零就跳过这一帧的预测，大于某个上限（比如 0.5 s）就判定为时间异常、复位跟踪器而不是硬着头皮外推——这和「自瞄算法设计」一章讲的发散检测是同一类兜底。第三，*在车上禁用自动校时*，或者至少在比赛期间停掉它。

这三条的共同点是：它们都不解决"时间为什么会跳"这个问题，而是让系统在时间跳了之后仍然*可控地*失败。这是本章反复出现的一个模式——赛场上你没有能力消除所有异常，只有能力决定异常发生时系统以什么姿势倒下。

=== 系统：跑到第三局才爆的那一类

这一族的共同特征是*时间常数比一局比赛长*。单局比赛七分钟（随赛季变动，以当季手册为准），任何在七分钟内就会暴露的问题，你在训练赛里早就发现了；真正阴险的是那些需要几十分钟才积累到临界点的问题——它们会挑在你打进淘汰赛、连着跑了三四局之后发作。

==== 慢性泄漏

「基础篇」计算机系统基础一章已经算过一笔快性泄漏的账：每帧漏掉一整张图像，几十秒内内存就被吃光、OOM killer 上门。那种泄漏你在实验室的第一次运行里就会撞到，不需要本章操心。

赛场上真正危险的是慢一千倍的那一档：不是每帧漏，而是每次目标切换漏一点、每次重连漏一点、每次存图漏一个描述符（描述符泄漏那笔账同一章也算过）。这类泄漏在一局比赛里只涨几十兆，你盯着 `htop` 看七分钟看不出任何异常；连打四局不重启，它就到了。

*现场应急*。这一条的应急措施朴素得有点丢人，但它是对的：*每局之间重启视觉进程*。这不是逃避问题，而是承认"进程寿命远短于泄漏的时间常数"是一个可以利用的事实——可靠性工程里管这叫计划内重启。代价只是重启那几秒，收益是把一整类慢性病挡在赛场之外。

*赛前预防*。做一次*浸泡测试*：让整套系统连续跑满至少四局的时长（半小时以上），期间持续记录常驻内存与打开的文件描述符数，画出曲线。判据很简单——曲线必须收敛到一条水平线，只要它还在稳定爬升，无论斜率多小，都是泄漏。定位工具在「基础篇」计算机系统基础一章讲过，这里补的只是这个"跑够时长再看曲线形状"的赛场化验收方法。

==== 磁盘写满

*症状*。跑到某一局，日志停止更新、录包中断，接着各种依赖写文件的组件开始出奇怪的错误，严重时文件系统被挂载成只读，整台车变砖。

*根因*。录包。「自瞄算法设计」一章说过，把时间戳、图像、云台四元数一起落盘是赛场诡异 bug 唯一的复现抓手，所以录包该录——但要先算清楚它的码率。1440×1080 的三通道图一张就是 $1440 times 1080 times 3 approx 4.45$ MiB，100 fps 的裸录相当于每秒往磁盘写 445 MiB；按七分钟一局折算，一局约 180 GiB。这个数字不是吓唬人，它说明*裸录原始图像在 RM 场景下根本不可行*，必须降分辨率、降帧率、或者编码压缩三选一。而如果你没算过这笔账就开了录包，磁盘会在你毫无察觉的时候被填满。

*现场应急*。`df -h` 一眼看穿。清掉旧录包，或者干脆把录包关掉——比赛能跑完比留下证据重要。

*赛前预防*。三件事：给录包设置容量上限与滚动删除策略（写满就覆盖最旧的）；把可用磁盘空间做成一个被监控的状态量，低于阈值时报警；以及在赛前算一遍上面那笔码率账，确认你的配置能在一天的比赛里活下来。顺带一句，把录包放在与系统盘不同的分区上，可以让"录包写满"退化成一个局部故障，而不是让整个系统只读。

==== 网络与 DDS 发现风暴

*症状*。只在赛场或调试网络里出现：节点启动变得很慢，话题时通时断，CPU 里凭空多出可观的一块开销，列一下话题会看到一堆不属于你的名字。

*根因*。ROS 2 默认的发现机制走 UDP 多播，同一个子网、同一个 `ROS_DOMAIN_ID` 下的所有参与者会互相发现并两两匹配端点。赛区的调试网络里可能同时挂着好几支队伍的好几台车和一堆笔记本——如果大家都用默认 domain，发现流量与端点匹配的规模大致随参与者数量的平方增长，无线链路又会把多播放大，于是形成发现风暴。这不是谁的 bug，是默认配置在一个它没有被设计来面对的环境里的正常表现。

*现场应急*。把车从网络上摘下来。车上的所有节点本来就跑在同一台机器上，令 `ROS_LOCALHOST_ONLY=1` 就够了；需要远程看数据时再单独接。

*赛前预防*。给自己的车定一个不与人重复的 `ROS_DOMAIN_ID` 并写进启动脚本；能不上网就不上网；确实需要跨机通信时，用 DDS 的配置文件把发现改成静态 peer 列表，别让它去发现全世界。这条和「基础篇」Linux & Ubuntu 操作一章讲多机器人网络时规划 ROS 2 域 ID 是同一件事，只是那里是为了不互相干扰，这里是为了不被别人拖垮。

==== 图传与工业相机是两套系统

赛场的无线环境还有另一层，网上的文章常常把它讲混，值得点破：*操作手看到的图传画面和自瞄用的板载工业相机，是两套完全独立的系统*。图传是一条独立的无线链路，官方对它有明确的使用限制，比如要避开 5.8 GHz 频段的干扰、同一场地内的套数有上限；而自瞄的相机通过 USB 或网线直连车载运算平台，图像从来不上无线。

这个区分在故障定位时有直接价值。图传花屏、卡顿、断流，是无线链路的问题，*与自瞄一点关系都没有*——自瞄该打中还是能打中；反过来，自瞄识别不到目标，去调图传的天线是白费力气。赛场上时间紧、人手乱，最容易发生的就是把这两件事混在一起排查。

无线链路本身的脆弱是有先例的：官方公告记录过裁判系统无线链路被干扰导致比赛中断的情况，后续也补充了通道冗余。（这一条我们引自公开公告，未做独立复核。）它对视觉的影响是间接但真实的：裁判系统的数据——敌我颜色、实时弹速、血量、发弹信息——都要经过链路才能到达你的车。所以，*凡是依赖裁判系统数据的逻辑，都必须能在数据断流时降级运行*：颜色断了就保持上一次的值而不是切回默认，弹速断了就用配置里那个唯一的标定值，而不是让三个模块各用各的默认（这正是索引表里那条弹速坑的赛场版本）。

=== 端到端延迟：怎么在真车上把它量出来

上一章「性能优化技巧」把全链路摊成过一张延迟预算表，逐段给了量级——但那张表是*按环节估的*，它回答的是"工程时间该花在哪一段"。有一个问题它没有回答、也回答不了：*你这台车，此刻，端到端究竟是多少毫秒*。这个问题比它看起来难：每个模块的耗时你都能用计时器打出来，但把它们加起来不等于端到端延迟——中间还有队列等待、线程调度、串口发送、电控响应、拨弹机构的空程，这些要么不在你的进程里，要么根本没有代码可以插桩。

有一个很漂亮的办法可以把整条链闭合起来：*用裁判系统的发弹事件回填时间戳*。裁判系统会上报实时射击数据，也就是"这一刻真的有一颗弹丸射出去了"。视觉端收到这个事件时，回头去查是哪一帧图像触发的这次开火指令，用发弹事件的时刻减去那一帧的曝光时间戳，得到的就是*从光进入镜头到弹丸离膛*的全程延迟。一个自研自瞄项目用这个方法在实车上测出来的分布是 35～110 ms，均值约 82 ms（*该项目的自述数据，本书未做独立复现*；它依赖于对方的车、对方的电控，换一台车数值必然不同，要当量级用而不是当常数抄）。

这个方法的价值不在那三个数字，而在它的结构。逐段插桩测的是"我的代码跑了多久"，闭环测的是"这个系统从看见到开火用了多久"——后者才是弹道解算里那个提前量真正需要的东西，而且它自动把所有你插不进桩的环节（串口排队、电控的控制周期、拨弹盘转过去的那段机械时间）一并包含进来。你不需要知道每一段是多少，就能拿到那个唯一会影响命中的总量。

#figure(
  image("images/adv-field-latency-loop.png", width: 100%),
  caption: [用裁判系统发弹事件回填时间戳的闭环延迟测量（脚本 `adv_field_latency_loop.py`）。读图规则：上排是链路，灰色的几段是你能在自己进程里插桩计时的部分，品红的几段插不进桩——闭环测量的全部价值就在于它把这几段也量了进去。下排的横条是引用的实测分布 35～110 ms（均值 82 ms），条的*宽度*而不是它的位置才是本章关心的量：均值可以被弹道解算当成常数补掉，宽度不能。],
)

有两条限制必须说清楚，否则这个数字会被用错。第一，发弹事件本身经由裁判系统链路传回，这条链路自己有延迟，所以测出来的值系统性地偏大一点；把它当成上界来用是安全的，当成精确值来用不安全。第二，能这么测的前提是你能把发弹事件对应回具体某一帧——如果开火指令是连发的，就得靠计数或者别的方式做归属，归属错了整个测量就没有意义。

真正要拿走的一条是关于*分布形状*的：均值 82 ms，而范围横跨 35～110 ms——*抖动的幅度和均值本身是同一个量级*。这件事的后果，上一章已经把账算过了：补偿只能吸收均值、吸收不了抖动，残余落点误差等于目标横向速度乘以延迟的标准差。「弹道解算」一章把前几段延迟并成一个常数 $t_d$，吸收的正是这里的 82 ms，那是对的；而剩下那几十毫秒的宽度，没有任何一个环节接得住。

所以这份实测分布真正的用处，是给上一章那条抽象的判据填上真实数字：把 35～110 ms 代进去，看它在小陀螺工况下具体值多少距离。

#figure(
  image("images/adv-field-jitter-residual.png", width: 100%),
  caption: [补掉均值之后，延迟抖动残下来的落点偏差——把上一章「性能优化技巧」的抖动判据套到本节这份实测分布上（脚本 `adv_field_jitter_residual.py`）。⚠ 曲线本身是*推导值*不是实测：横轴取自上图引用的 35～110 ms 实测分布，纵轴按 $e = omega r |t - t_0|$ 计算，装甲板半径 $r$ 取 0.25 m，$t_0 = 82$ ms 处偏差为零即"均值已被补掉"。读图规则：与小装甲板半宽 6.75 cm 那条横线比——3 rad/s 的慢速自旋整条曲线都在线下，抖动无害；7 rad/s 时分布的低端已经出界；14 rad/s 时线内只剩中间 39 ms 的一段——实测 75 ms 的分布宽度里有近一半落在线外，也就是说*高速小陀螺下命中与否主要由延迟抖动决定，而不是由你的滤波器精度决定*。],
)

这张图解释了一件让很多队伍困惑的事：为什么把跟踪算法调得更准，对高速小陀螺的命中率几乎没有帮助。因为在那个工况下，误差预算里最大的一项根本不在算法里，而在链路的抖动里。想提高命中率，该做的是压缩延迟的*方差*，而不是继续调滤波器。具体怎么压，上一章「性能优化技巧」已经按性价比排好了序——先查队列积压、再设 OpenCV 线程数、然后才是调度亲和性；这里只是给那张排序表补上它缺的最后一样东西：*一个真车上量出来的、可以拿去验收的总数*。

=== 控制耦合：视觉与电控互相补偿

这一族是最难查的，因为故障不在任何一端。视觉单独测是对的，电控单独测也是对的，合起来就不对——而每一方都能拿出证据证明自己没错。

==== 云台自激振荡

*症状*。云台锁上目标之后开始高频抖动，越靠近目标抖得越厉害；快速甩枪时尤其明显，有时会发展成越来越大的往复摆动直到失控。手动遥控云台一切正常，静止调试也一切正常，只有自瞄接管、目标又在动的时候才出现。

*根因*。这是一个闭环控制系统被纯延迟推到不稳定的经典案例。视觉测量目标、算出角度、发给电控、电控驱动云台、云台转动改变视觉看到的画面——这是一个闭环。闭环里每多一段延迟 $tau$，在频率 $f$ 处就多一份相位滞后：

$
phi = 360 degree dot f dot tau
$

这个滞后随频率线性增长，而增益并不同步下降。控制系统设计时留的相位裕度通常在 $30 degree$ 到 $60 degree$ 之间，一旦延迟吃掉的相位超过裕度，原本稳定的闭环就在穿越频率附近自激。

代之前要先分清两个 $tau$。上一节测的是*端到端*延迟——从曝光到弹丸离膛，它之所以有用，正是因为它把拨弹与离膛也算了进去，那是弹道提前量需要的量。但这里要的是*伺服回路*延迟：闭环在"云台转动改变画面"处闭合，拨弹与离膛发生在回路之外，是开火分支的下游，不参与位置反馈。把端到端值直接代进相位公式，会把火控段的时间当成回路滞后，系统性高估相位损失——方向上偏保守（读者会去降增益），但结论是错的，代价是白丢跟随带宽。回路 $tau$ 要单独测：给电控发一个角度阶跃，用相机（或电机编码器）看云台起动时刻，减去指令下发时刻。*端到端延迟 $eq.not$ 伺服回路延迟*，这是两个不同的量；本书别处那条"同一个物理量全系统只有一个来源"的规矩，反过来同样成立：不同的物理量不能共用一个实测值。

心里有了这个区分，再来看纯延迟到底吃掉多少相位：

#figure(
table(
  columns: (auto, auto, auto, auto),
  align: (left, center, center, center),
  stroke: (x, y) => (
    top: if y == 1 or y == 0 { 1pt } else { 0pt },
    bottom: if y == 3 { 1pt } else { 0pt },
  ),
  inset: 8pt,
  [*闭环回路延迟 $tau$*], [*$f = 2$ Hz*], [*$f = 5$ Hz*], [*$f = 10$ Hz*],
  [20 ms], [$14.4 degree$], [$36.0 degree$], [$72.0 degree$],
  [50 ms], [$36.0 degree$], [$90.0 degree$], [$180.0 degree$],
  [82 ms（端到端实测均值，含拨弹与离膛，是云台回路 $tau$ 的*上界*）], [$59.0 degree$], [$147.6 degree$], [$295.2 degree$],
),
caption: [纯延迟在不同频率处吃掉的相位裕度，按 $phi = 360 degree f tau$ 计算（推导值，非实测；表中 82 ms 一行的延迟取自上一节引用的实测均值）。读表规则：拿你的云台闭环穿越频率去查对应的列——超过 $60 degree$ 的格子意味着典型设计裕度已被吃光，超过 $180 degree$ 的格子意味着反馈已经彻底变号、只要增益还大于 1 就必然自激。它同时解释了另一个现象：*同一套参数，云台调得越"快"（穿越频率越高）越容易振荡*，因为右边的列涨得比左边快。注意 82 ms 这一行是*上界*：它按上一节的定义包含拨弹与离膛，而那两段在云台位置回路之外——真正代进公式的应是端到端值减掉这两段。],
)

延迟只是根因的一半，另一半更隐蔽：*两端用了不同的姿态源*。视觉端算目标位置时要用云台姿态做坐标变换，如果它用的是自己那块 IMU 的读数，而电控闭环用的是另一块 IMU，那么两块陀螺仪的零偏、噪声、安装误差之差，就作为一个未建模的扰动注入了闭环。这比延迟更难查，因为它连一个可以代进公式的参数都没有。

这两条在一份公开的战队工程记录里被同时点名。那份记录写道，他们的云台在电机快速运动时会震荡，根因是视觉发出的指令到电控执行之间的通信延迟没有被补偿；而在*视觉和电控同步时间戳、并使用同一块陀螺仪*之后，振荡就消失了。（这是该队的*自述结论，本书未做独立复现*——但它指认的两个根因，与上面那张相位表的推算方向一致，可以互为旁证。）

这句话是全书那条"时间戳对齐"的线在赛场上的最终兑现。「时间戳对齐」一章花了一整章讲怎么让图像和姿态配到同一个时刻，讲的时候它看起来像一个精度问题——对不齐，目标就偏几厘米。现在你看到它的另一副面孔：对不齐还是一个*稳定性*问题。精度问题让你打不准，稳定性问题让你的云台在场上抽风到没法比赛。同一件事，两种量级完全不同的后果。

*现场应急*。三个动作，按代价从小到大：降低电控端的控制增益（最快，代价是跟随变慢）；降低视觉的指令下发频率或对输出做一级低通（等于牺牲带宽换裕度）；实在不行切手动，让操作手打完这一局。三个动作的共同逻辑都是*降低穿越频率*——把工作点从表格的右列挪回左列。

*赛前预防*。按根因的两半各做一件事。延迟这一半：把视觉到电控的回路延迟测出来（不是上一节那个端到端值——用上面说的角度阶跃法单独测），并在电控端做前馈补偿，而不是假装它不存在。姿态源这一半：*全车只用一块陀螺仪*，视觉需要姿态就从电控要，别自己再装一块。这条听起来像是在削弱系统，实际上是在消除一个没有任何收益的自由度——两块 IMU 不会让你更准，只会让你多一个对不上的量。

==== 两端各补一次的正反馈

*症状*。上车之后越打越偏，而且偏差随目标运动速度增大而增大；两端的工程师各自单独测试都是对的。

*根因*。同一个物理效应在视觉端和电控端各被补偿了一次。最典型的是延迟：视觉端算提前量时加了一个 $t_d$，电控端为了跟上斜坡输入又自己加了一份前馈，两份加起来提前量翻倍。其次是零位：视觉端的手眼标定里已经包含了相机相对枪管的安装偏角，电控端又在云台零位里补了一次同样的角度。这类问题的可怕之处在于，*单独测试时每一端都是对的*——视觉端拿理想的电控模型测，电控端拿理想的视觉输入测，只有合在一起才错。

「自瞄算法设计」一章立过一条规矩："同一个物理量，全系统必须只有一个来源"。这里是那条规矩的另一半，也是更容易被违反的一半：*同一个补偿项，全系统必须只有一个归属*。参数只有一个来源，是防止两边读到不同的值；补偿只有一个归属，是防止两边都做了同一件事。

*现场应急*。把可疑的补偿项在一端归零，看偏差是变小还是变大。变小就找到了。这个动作两分钟能做完，比任何推理都快。

*赛前预防*。在接口文档里逐条写明每个补偿项由哪一端负责——延迟补偿在谁那里、枪口偏角在谁那里、重力下坠在谁那里、弹速修正在谁那里。这份文档不需要长，一页表格足够，但它必须是两端共同签署的。RM 的视觉与电控通常是两个人甚至两个组，接口上的默契从来不会自动产生。

=== 五分钟：赛场故障定位的方法论

前面所有内容都建立在一个前提上：你知道故障是什么。赛场上你不知道。你只有一个症状、一个下场倒计时、和一堆等着你的队友。

一位开源自瞄作者在工程日志里写过一句话，值得当作这一节的引子：不要以*效果*为驱动力，应该以*逻辑*为驱动力——原因、逻辑、代码、效果，按这个顺序走。以效果为驱动，是"我改一下这个参数看看会不会好"；以逻辑为驱动，是"这个症状只可能由 A、B、C 三个原因产生，我先做一个实验把范围砍掉一半"。这句话完整的展开在「项目分析」篇的 rm.cv.fans 一章，这里只借它开个头，因为它恰好是赛场压力下最容易被丢掉的东西：人一急，就开始改参数。

改参数为什么是最差的策略？因为它同时违反了三条：它不缩小搜索空间（参数空间是连续的、无穷的）；它不产生信息（改完没好，你并没有排除任何东西）；它破坏可回滚性（改了三个参数之后，你已经回不到上一场那个能跑的状态了）。相比之下，一个好的诊断动作应该*把可能性砍掉一半*，哪怕它本身不修复任何东西。

具体到操作，赛场五分钟按这个顺序走。

*第一步，先问"上一场还好好的，中间改了什么"*。绝大多数赛场故障不是天降的，是人为的。`git diff` 与 `git log` 是赛场上最快的定位工具，比任何调试器都快。如果上一场是好的、这一场坏了，而中间有提交，那么在你想清楚之前，*先回滚*——回滚不是放弃定位，是把车先救回可用状态，把定位挪到有时间的时候做。这也是为什么每场比赛前都该给当前状态打一个 tag：你要保证自己随时能回到一个已知能跑的点。

*第二步，做一次二分，把链路砍成两半*。视觉到命中是一条长链：看得见吗（相机出图）→ 认得对吗（识别输出）→ 算得准吗（位姿与预测）→ 送得出去吗（串口与电控）。不要从头挨着查，从中间切一刀：看识别节点有没有在输出目标。有输出，前半段是好的，问题在后半段；没输出，问题在前半段。一刀砍掉一半，两三刀就到底了。

*第三步，用症状去查第一刀该切在哪*。不同的症状对应不同的最优切点，下面这张表是把前面几节的失效模式按"你先看到什么"重排的结果——它不是诊断结论，是告诉你第一个实验该做什么。

#figure(
table(
  columns: (auto, auto, auto),
  align: (left, left, left),
  stroke: (x, y) => (
    top: if y == 1 or y == 0 { 1pt } else { 0pt },
    bottom: if y == 8 { 1pt } else { 0pt },
  ),
  inset: 7pt,
  table.header([*现场症状*], [*第一刀切在哪*], [*一分钟内能做的判据*]),
  [完全没反应], [进程还活着吗], [看进程在不在、日志最后一行是不是 FATAL],
  [识别不到目标], [相机还是识别], [看取流帧率与一张原图：画面对不对、亮度对不对],
  [识别到了但云台不动], [串口方向], [看视觉侧发送计数与电控侧接收计数在不在同步涨],
  [云台动但方向反], [坐标系符号], [把目标放在画面四角，看云台往哪边转],
  [静止能打、动起来偏], [时间与延迟], [关掉预测只发当前角，偏差消失即时间问题],
  [近处能打、远处偏], [内参与弹道], [摸镜头对焦环；换一个固定弹速常数试],
  [打一会儿就飘或丢], [滤波器], [看发散计数与新息检验，看 $Delta t$ 有无异常值],
  [云台抖动或振荡], [控制耦合], [降视觉下发频率或电控增益，抖动减轻即延迟问题],
),
caption: [赛场症状分诊表。读表规则：中列不是故障原因，是*第一个该做的实验*——它的作用是把可能性砍掉一半，而不是修好问题。右列的动作都控制在一分钟内、且都可逆，这是赛场诊断动作的两条硬标准：*不可逆的动作不做，超过一分钟的动作不做*。真正的根因定位留到赛后用录包复现。],
)

*第四步，一次只动一个变量，并且记下来*。这条在实验室是常识，在赛场上会被压力冲垮——人在紧张时会同时改三个地方然后祈祷。一次改一个，改了之后写在纸上或者提交里。如果你改到第五个还没好，前四个必须都已经改回去了，否则你现在面对的是一个全新的、没人见过的系统。

*第五步，接受"能上场"就是这五分钟的目标*。根因定位是赛后的事，赛场上的目标只有一个：让车能跑完这一局。回滚到能跑的版本、关掉出问题的功能降级运行（比如关掉打符只留自瞄）、甚至改手操——这些都不是失败，是正确的工程决策。而让它成为可能的前提，是你的系统本来就支持降级：功能之间不要硬耦合，一个模块挂了不该带走整个进程。

*最后，把证据带回去*。录包是唯一能把赛场问题带回实验室的东西。「自瞄算法设计」一章讲过录制器的价值，这里补一句赛场化的用法：*故障发生的那一局，比赛一结束立刻把包拷出来并标注清楚*。赛场的机器会被反复重启、清盘、改配置，你以为还在的东西经常已经没了。一个没有录包的赛场故障，赛后基本无法复现，也就基本无法修复——它会在下一场比赛里原样再来一次。

==== 本章小结

这一章把赛场上的死法归了档。开头那张索引表把全书散落的坑收拢到一处，供你赛前当清单用；正文按五种失效模式展开——光照上补的是电网纹波与"随手调亮"这两条只在赛场出现的账，硬件上补的是镜头机械松动、供电裕量、驱动的致命默认与设备节点认错这几种*不报错*的失效，时间上把"时间戳单调"那行清单展开成时钟回拨如何让滤波器具体地死，系统上归了那些时间常数长于一局比赛的慢性病，控制耦合上则借一份公开的战队记录，把云台自激振荡的根因钉在了通信延迟未补偿与双陀螺仪上。中间那节讲的是怎么用裁判系统发弹事件把端到端延迟闭环量出来——那个 82 ms 的均值不重要，重要的是 35 到 110 ms 这个宽度：均值可以被补偿吸收，宽度不能，而在高速小陀螺下它就是命中率的主导项。最后一节讲的是方法：以逻辑为驱动而不是以效果为驱动，先查改了什么、再做二分、按症状选第一刀、一次一个变量、目标是能上场、证据要带回去。

进阶篇到这里收束，该回头看看这一篇把系统推到了哪里。「雷达站视觉方案」把单目自瞄那套经验横向铺开成了全场感知；「哨兵决策视觉」把"看得见"往前推了一步，推到"自己决定打谁"；「性能优化技巧」纵向把这套系统压到延迟与抖动的极限；而本章把它在赛场上会怎么死一条条归了档。四章合起来只做一件事：把一台在实验室里能跑的车，变成一台在灯光、电磁、网络都不由你控制的地方还能跑完七分钟的车。

进阶篇讲到这里，教的一直是*把东西做出来*。下一篇「项目分析」换个方向：把 rm_vision、rm.cv.fans、同济自喵三套真上过场的开源项目摊开拆一遍——看别人在岔路口上做了什么判断，又为这些判断付了什么账。你会在那三份代码里读到本章这些死法的现成防御，比如滤波器承认自己跑飞之后怎么爬起来；也会看到开头那张索引表里的坑原样长在别人的仓库里——同一个默认弹速在一个项目里散着三份，和你可能刚踩过的是同一个。
