#import "/template/template.typ": *

=== 从导轨哨兵到地面哨兵

2022 赛季手册的“初始位置”表中，步兵、英雄和工程位于启动区，哨兵则单列为*哨兵轨道*。它沿导轨运动，底盘位置近似只需用一个沿轨道的量描述。到 2026 赛季，手册 4.2.1 节将哨兵的初始位置写为 Starting Zone，哨兵已经按可在场地中自主移动的地面机器人来设计。

这个变化扩展了视觉系统的下游：目标检测与跟踪仍然服务于云台，自身定位和场景感知还要为底盘提供地图、代价地图与导航目标。哨兵需要同时回答“我在哪里”“应该去哪里”“路径能否安全通过”以及“移动时如何维持瞄准”。因此，本章讨论的重点不是再讲一套识别算法，而是感知、导航与比赛决策如何衔接。

广角与长焦相机的选型、多相机硬件同步、用序列号固定同型号相机的逻辑角色，以及多路目标的重排、滞回和跟踪状态切换，已经在「工业相机」「装甲板的跟踪」「自瞄算法设计」中介绍。本章从这些模块的输出继续：决策层怎样组合自身位姿、敌方位置、导航目标和攻击优先级，并在指令冲突时完成仲裁。

规则还限制了哨兵可获得的队外视野信息。上一章「雷达站视觉方案」已经按 2026 手册的通信表说明：在机器人间通信一栏中，雷达与哨兵的接收对象彼此受限，而它们都可以向己方机器人发送数据。这里从哨兵端补充两个接口边界。

第一，手册 3.7 节说明，雷达获取战场信息后可以发送给“己方机器人或裁判系统选手端”。雷达情报可以直接发给英雄和步兵，不要求由哨兵转发，因此哨兵不应被设计成全队唯一的情报中继。第二，在机器人间通信这一层，哨兵接收的外部机器人情报来自雷达；云台手还可以通过裁判系统客户端转发小地图标记，但这是人工触发的另一条通道。决策层最终会面对两类数据：自身相机与激光雷达给出的局部观测，以及雷达给出的更大范围观测。两者的覆盖、时延和误差来源不同，不能只用一句“全局数据优先”或“本车数据优先”处理。

手册还允许云台手通过裁判系统客户端转发小地图标记、远程兑换弹丸与血量、确认复活和切换姿态模式。按本章采用的 2026 规则，全自动哨兵每次接受这类人工操作会消耗 50 金币，半自动哨兵则不收取这项费用。因此，全自动哨兵可以保留人工介入接口，但决策逻辑不宜把它当作高频、无成本的常规输入。

=== 地图从哪来：离线预建图还是上场 SLAM

导航的第一个问题不是"怎么走"，是"我在哪"。而"我在哪"要有个参照物，那就是地图。

2026 手册在赛前流程附录中注明：This season does not have a separate Sentry Mapping section，即本赛季没有单设的哨兵建图环节。这不等于禁止使用先验地图，但意味着规则没有保证一段专门用于正式场地采图的时间。地图来源因而需要在赛前确定，常见路线有两种：

- *离线预建图 + 上场重定位*。赛前通过允许的练习或场地资料构建点云并保存为 `.pcd`；比赛时将当前激光扫描与先验点云配准，估计地图中的位姿。坐标系可以跨局保持一致，防守点、增益点和通道入口等位姿也可以写入配置。代价是先验地图必须可获得，并且要处理实际场地与地图不一致、临时障碍物干扰和错误重定位。
- *在线建图或仅使用局部里程计*。开机后建立局部地图，`odom` 原点通常取启动位姿，不依赖预先采集的点云。它更容易适应局部变化，但固定路点会依赖每局启动位姿；如果摆放或初始朝向有误，所有相对路点都会带上同一偏差。仅靠里程计还会累积漂移，因此不能把“无需先验”理解成“无需校准”。

工程上可以让两条路线共享节点和启动文件，只通过配置切换。有一份 Apache-2.0 许可的公开哨兵导航栈使用布尔参数 `slam`：为真时禁用重定位节点、发布静态的 `map → odom` 变换，并在退出时保存点云；为假时加载先验点云，由配准节点估计 `map → odom`。这种结构减少了赛前建图与赛时导航之间的配置分叉，但切换后仍要分别验证 TF 树、初始位姿和失败恢复，不能只确认 launch 成功启动。

该项目 README 还记录了一项团队观察：在大场景中，带先验点云的里程计效果不如不带先验点云的方案，更容易漂移。本书没有复现这一现象，也没有足够证据确定机制。先验地图自身的累积误差、场地变化、初值错误或配准参数都可能产生类似结果，需在固定数据集上逐项关闭或替换先验约束，通过对照结果加以区分。这里能够确定的只有：加入先验不会自动改善定位；先验与当前观测不一致时，融合方法必须能够拒绝或降低异常约束的权重。这与「卡尔曼滤波」一章强调的噪声建模边界相通，但不能据此断定该项目的问题来自 $bold(Q)$ 或 $bold(R)$。

定位会直接限制视觉输出的用途。PnP 首先给出相机坐标系中的目标位置；若要把它发送给队友，或与雷达的场地坐标关联，还需要完整的相机到场地变换。定位误差会叠加到目标坐标上，即使 PnP 本身很准，也不能抵消地图位姿误差。上一章提到的 $0.8$ m 是雷达标记的规则判定阈值，不是雷达测量精度；它只能说明一米量级的系统偏差足以越过该判定边界，不能反推出雷达通常有多准。

=== 自身运动补偿：自旋参考系与导航指令

定位和规划接通以后，还要处理云台与底盘相对转动带来的参考系问题。

哨兵的云台和底盘通常独立转动。云台偏航角由搜索或跟踪任务决定，底盘朝向则由导航和反自瞄策略决定。Nav2 中的 `robot_base_frame` 用来描述机器人本体，控制链也必须明确 `cmd_vel` 的表达坐标系。许多通用导航配置默认这个坐标系固定在车体上；如果把它改成快速转动的云台系，却没有同步修改控制链的坐标约定，速度指令就可能被错误解释。

在前述公开导航栈中，重定位与点云处理节点的 `robot_base_frame` 配成了云台偏航系 `gimbal_yaw`。这不是由激光雷达安装位置直接决定的：仓库描述的 mid360 安装在底盘上，里程计首先给出 `odom → base_footprint`，点云处理节点另行发布以 `gimbal_yaw` 为 child 的 odometry 供下游使用。该栈还在速度出口叠加固定底盘自旋角速度，参数值为 $3.14$ rad/s。由这些代码和配置可以确认它有一套专门的速度变换约定；仅凭参数文件仍不能推断所有哨兵都应把 `robot_base_frame` 设为云台系。

继续推导前，需要先区分两个同名的 `odom`：

#note("同名不同义：`odom` 在自瞄栈和导航栈里不是一个东西")[
自瞄栈中，串口节点接收下位机欧拉角后广播 `odom → gimbal_link`。所核对的 `rm_serial_driver.cpp` 只设置了旋转，平移保持为零（该文件采用 Apache-2.0 许可证）。因此，这里的 `odom` 用于表达云台姿态参考：它没有底盘在场地中的平移信息，偏航零点也取决于系统建立参考姿态的方式。「项目分析」篇的「rm_vision」一章还会从框架角度说明这条变换。

导航栈中的 `odom` 则来自 LIO（激光惯性里程计），包含连续的平移与旋转，但会随时间漂移；重定位模块再估计 `map` 与 `odom` 之间的变换。它用于回答底盘相对于局部或全局地图的位置。

tf2 通过字符串标识坐标系，不会区分同名坐标系来自哪个软件包。若两个栈未经适配就合并，零平移的 `odom → gimbal_link` 会把云台放在导航 `odom` 的原点，而不是随底盘平移；只依赖相对姿态的功能可能仍能运行，但变换到 `map` 的目标位置会缺少底盘位移。处理方式不只是改名：应先给姿态参考系使用明确名称（如 `gimbal_odom`），再建立 `map → odom → base_footprint → gimbal_link → camera` 这类物理含义一致、时间戳一致的 TF 链，并用已知底盘位移检查相机原点是否随车移动。

这说明坐标系命名属于跨包接口，而不是包内实现细节。合并工作空间时应同时审查名称、父子关系、物理含义和时间来源。
]

参考系不一致的后果可以用一个简化模型说明。假设控制器在时刻 $t$ 得到世界系目标方向 $hat(bold(d))$，并按偏航角为 $psi(t)$ 的云台系表达后发送

$ bold(R)(-psi(t)) hat(bold(d)) $

若下位机却把这两个线速度分量当作偏航角为 $chi(t)$ 的底盘系指令执行，实际世界系方向为

$ hat(bold(d))_"actual" = bold(R)(chi(t) - psi(t)) hat(bold(d)) $

只有在简化假设 $chi(t) = 0$ 下，它才退化成原式中的 $bold(R)(-psi(t)) hat(bold(d))$。因此，误差角一般是两个执行约定之间的偏航差，而不是无条件等于云台偏航角。下面的图采用 $chi(t)=0$、云台匀速扫描和理想速度执行，目的是展示坐标系误配本身的量级；它不是该开源车辆的复现。项目 README 对“云台自旋时机器人无法正常运动”的描述属于团队自述，本书没有实车或录包验证。

#figure(
  image("images/adv-sentry-spinnav.png", width: 100%),
  caption: [坐标系误配的简化仿真（脚本 `adv_sentry_spinnav.py`，推导值、非实测）：控制周期 50 ms、速度 1.5 m/s、云台偏航速率 2 rad/s，每周期重新指向目标。仿真假设底盘执行系偏航 $chi=0$，不代表前述开源哨兵的实际控制链。左图比较期望直线、坐标一致时的轨迹和把云台系指令误作底盘系执行时的轨迹；右图给出到目标的距离。脚本以 5 cm 为到点阈值；在这组假设下，坐标一致的模型约 3.3 s 进入阈值，误配模型 6 s 内最近仍距目标约 4.2 m。图只能验证该简化模型的几何结果，不能判断实车控制器的失效原因。],
)

上述公开实现用 `fake_vel_transform` 包建立虚拟坐标系 `gimbal_yaw_fake`：它与真实 `gimbal_yaw` 原点重合，但用反向旋转抵消当前偏航，使规划侧看到的 yaw 保持固定。节点发布的核心变换如下：

```cpp
// 发布 robot_base_frame → fake_robot_base_frame：只有一个绕 z 的反向旋转
tf2::Quaternion q;
q.setRPY(0, 0, -current_robot_base_angle_);   // 抵消掉当前云台偏航角
t.transform.rotation = tf2::toMsg(q);
```

参数文件将 `bt_navigator`、局部与全局代价地图及 `behavior_server` 的本体坐标系设为 `gimbal_yaw_fake`，里程计、重定位与传感器处理仍使用真实的 `gimbal_yaw`。速度出口再将虚拟系中的指令转换回执行侧约定的坐标系：

```cpp
geometry_msgs::msg::Twist aft_tf_vel;
aft_tf_vel.angular.z = twist->angular.z + spin_speed_;                       // 叠加固定自旋
aft_tf_vel.linear.x  =  twist->linear.x * cos(yaw_diff) + twist->linear.y * sin(yaw_diff);
aft_tf_vel.linear.y  = -twist->linear.x * sin(yaw_diff) + twist->linear.y * cos(yaw_diff);
```

（以上两段引自 SMBU 北极熊战队开源的 `pb2025_sentry_nav`，Apache-2.0 许可证。）

第一行把独立配置的底盘自旋角速度叠加到导航角速度上；后两行执行平面旋转。这样做把感知与里程计使用的真实坐标系、规划使用的固定偏航虚拟系，以及硬件执行坐标系分隔开，无需修改 Nav2 源码。不过，“叠加两个角速度”只表示接口分离，并不自动保证二者互不影响：输出饱和、底盘功率限制和安全仲裁仍需在下游处理。

还要区分最终数值与中间计算坐标系。在理想化约定下，虚拟系中的世界速度 $bold(v)$ 经出口旋转后为 $bold(R)(-psi) bold(v)$；若控制器直接在真实 `gimbal_yaw` 中正确表达同一世界速度，输出也可能是 $bold(R)(-psi) bold(v)$。所以这个包的收益不一定体现在最终两个线速度分量不同，而可能体现在控制器、代价地图和速度平滑器都工作在变化更慢的坐标系中。这个等价关系依赖两条链使用相同的角度、时间戳和坐标约定，实装时应逐项核对。

速度平滑器给出了一个可计算的例子。该 launch 将它放在控制器与变换节点之间，配置为 20 Hz 开环限幅，加速度上限 $4.5$ m/s²。世界系恒定速度在角速度为 $omega$ 的旋转坐标系中，其方向变化率量级为 $|bold(v)| omega$。取配置中的速度上限 $2.5$ m/s，并另作云台偏航速率 $2$ rad/s 的假设，可得 $5.0$ m/s²，超过该限幅。于是旋转系中的速度分量可能被削幅；固定偏航系会把这次旋转放到平滑器之后。这个计算只说明给定参数下存在触发限幅的可能，未验证实际轨迹或控制器内部状态，低速时也可能不触发。

因此，评估虚拟坐标系方案时，应同时检查最终指令和每个有状态中间节点所使用的坐标系，而不能只比较速度出口。

==== `Twist` 没有时间戳时怎样对齐姿态

把 `cmd_vel` 从虚拟系转回执行系时，需要与这条指令对应时刻的云台偏航角 $psi$。理想接口应让速度和姿态都带采样或生成时间，再按「时间戳对齐」一章的方法查取变换；ROS 2 的 `message_filters` 提供了 `ExactTime` 和 `ApproximateTime` 等同步策略。

该项目基于 ROS 2 Humble，当时控制器发布的 `cmd_vel` 使用 `geometry_msgs/Twist`，消息没有 header 和时间戳；较新的 Nav2 接口使用 `TwistStamped`。相关接口演进可参见 navigation2 issue #1594。对于无时间戳的 `Twist`，同步器无法直接确定它应与哪一帧姿态对应。

这份实现使用 `local_plan` 的时间戳作为代理。控制器还会发布带时间戳的 `nav_msgs/Path`，代码假设 `local_plan` 与 `cmd_vel` 来自同一次控制计算，再用 `ApproximateTime` 同步 `local_plan` 和 `odom`；最新的 `cmd_vel` 被缓存到同步回调中使用。

```cpp
// In Navigation2 Humble release, the velocity is published by the controller without timestamped.
// We consider the velocity is published at the same time as local_plan.
sync_ = std::make_unique<message_filters::Synchronizer<SyncPolicy>>(
  SyncPolicy(100), odom_sub_filter_, local_plan_sub_filter_);
```

注释中的 “we consider” 明确表示这是实现假设，而不是消息接口提供的保证。控制器若改变发布顺序、一个控制周期发布多条路径或速度，缓存值就可能与代理时间戳不匹配。应通过录包比较两条话题的序号、发布时间和回调顺序，并在升级 Nav2 后重新验证；有条件时直接改为带时间戳的速度接口。

这个例子说明，时间戳是融合接口的一部分。自行设计队内协议时，凡是要参与状态估计或坐标变换的消息，都应定义时间基准、时间戳语义、分辨率和回绕方式；字段大小取决于协议周期、比赛时长和时钟范围，不能统一按“四个字节足够”处理。

==== 五种公开实现的坐标处理方式

在本章核对的几份公开哨兵导航仓库中，坐标处理最终都涉及平面旋转，但实现位置不同：有的构造虚拟坐标系，有的只在输出端转换，也有的直接在 `map` 系优化。比较它们时，除了旋转公式，还要看 $psi$ 的来源、时间戳和速度消息的坐标约定。

#figure(
table(
  columns: (auto, auto, auto),
  align: (left, left, left),
  stroke: (x, y) => (
    top: if y == 0 or y == 1 { 1pt } else { 0pt },
    bottom: if y == 5 { 1pt } else { 0pt },
  ),
  inset: 7pt,
  table.header([*做法*], [坐标处理], [$psi$ 的时间一致性]),
  [造子帧 + 输出端转回],
  [在真实云台系下挂一个 yaw 恒为零的虚拟子帧，规划器用虚拟系，速度出口乘 $bold(R)(-psi)$],
  [借 `local_plan` 的时间戳，用 `ApproximateTime` 间接对齐姿态；依赖代理时间戳假设],

  [把 TF 旋转设为单位阵],
  [发布 `odom` 到底盘系的 TF 时保留里程计平移、将旋转设为单位四元数，使该系只平移不旋转],
  [TF 使用发布时的 `now()`，不是里程计消息的采样时刻],

  [只在输出端做一次旋转],
  [不造帧，收到 `cmd_vel` 后查一次底盘系与云台 yaw 系的相对角，就地转换],
  [查询使用 `TimePointZero`，得到缓冲区最新变换，不保证对应指令时刻],

  [对齐局部路径而非世界],
  [取局部路径上约四分之一处那个位姿的朝向，减去底盘朝向当作待补偿角],
  [路径与 TF 未同步，路径本身还含规划延迟],

  [不增加虚拟帧],
  [MPC 直接在 `map` 系里优化轨迹，只在把结果写进 `cmd_vel` 前用同一拍的状态量转一次],
  [$psi$ 与速度来自同一次求解状态；仍需保证执行端使用同一约定],
),
caption: [五份公开实现中的坐标处理（结构与时间戳用法据仓库源码核对，运行效果未复现）。前四种都显式处理两个参考系之间的角度，最后一种在 `map` 系中求解后只在输出边界转换。它们在理想二维模型下可以写成相近的旋转形式，但因路径定义、时间戳和执行坐标系不同，不能据此认定行为完全等价。],
)

时间错配的量级可以直接计算。SMBU 参数文件中的 `controller_frequency` 为 $20.0$ Hz，因此一个控制周期 $Delta t=50$ ms。这里需要的 $omega$ 是*速度参考系自身*的偏航速率，不是 `init_spin_speed` 或 `PublishSpinSpeed` 所设置的底盘自旋角速度。下文取 $omega=3$ rad/s 作为假设，并假设所用 $psi$ 恰好落后一个控制周期；这两个条件都不是该仓库的测量结果。

若节点使用旧角度 $psi'$ 发送 $bold(R)(-psi') bold(v)$，执行侧按当前角度 $psi$ 还原到世界系，则

$ bold(v)_"actual" = bold(R)(psi) bold(R)(-psi') bold(v) = bold(R)(delta) bold(v), quad delta = psi - psi' = omega Delta t $

在匀速转动假设下，速度误差等效为期望速度转过 $delta=omega Delta t$。代入上述假设：

$ delta = 3 times 0.05 = 0.15 "rad" approx 8.6 degree $

按配置中的速度上限 $|bold(v)|=2.5$ m/s，误差模长为 $2|bold(v)|sin(delta \/ 2)=0.37$ m/s，约占指令的 $15%$。平行分量为 $|bold(v)|(cos delta-1)=-0.03$ m/s，垂直分量为 $|bold(v)|sin delta=0.37$ m/s，所以小角度下主要表现为横向误差。若其余假设不变并把 $omega$ 增至 $6$ rad/s，误差为 $0.75$ m/s，约占 $30%$。这是参数化推导，不代表实车必然落后整整一个周期；实际误差需用消息时间戳和录包数据计算。

#figure(
  image("images/adv-sentry-yaw-staleness.png", width: 100%),
  caption: [偏航角时间错配的二维推导（脚本 `adv_sentry_yaw_staleness.py`，非实测）：速度上限 $2.5$ m/s 与控制周期 $50$ ms 取自公开参数文件，参考系偏航速率 $3$ 和 $6$ rad/s 为假设值，与底盘自旋参数不是同一量。左图展示 $Delta t=50$ ms、$omega=3$ rad/s 时的期望速度、执行速度和误差分量；右图展示误差随时间差增长的结果。在假设成立时，50 ms 对应的两档误差约为 $0.37$ 和 $0.75$ m/s。该图隔离了坐标时间错配，不包含控制器、轮胎和执行器动态。],
)

因此，`lookupTransform(..., TimePointZero)` 是否可接受不能凭接口方便程度判断。应先测量它与指令时刻的实际时间差，再代入 $delta=omega Delta t$ 评估；上面的 $15%$ 只对应一组明确假设。借用 `local_plan` 时间戳是在旧接口约束下减少时间差的办法，同时引入了代理时间戳假设。

另一份 GPL-3.0 项目的实现还显示，平面旋转后将 $x$、$y$ 同时乘以 $0.12$。仅从源码无法确认这个系数表示标定增益、限幅折算还是临时调参；同一函数还对 $y$ 分量额外取反，可能对应底盘轴约定，也可能是符号补偿。这里不摘录其源码，也不推断作者意图。工程上应把纯坐标变换、单位换算和增益标定拆成不同步骤，并用单位向量测试检查旋转是否保持模长、轴方向是否符合接口文档。

==== 何时需要云台—底盘解耦

公开配置中既有以 `base_link` 为 `robot_base_frame` 的方案，也有使用云台系或虚拟系的方案。仓库来自真实赛季，但本书没有据此验证它们的赛场运行效果。选型应从本车接口开始，而不是照抄队名或参数：底盘是否持续自旋，导航是否控制该旋转，控制器是否支持全向底盘，`cmd_vel` 由哪个坐标系解释，以及中间节点是否依赖稳定朝向。

若底盘不自旋，或所有导航组件都能在底盘系中正确处理旋转，`base_link` 往往已经足够。若存在不受导航控制的持续自旋，并且控制器或平滑器假设本体坐标系变化较慢，则需要显式解耦，虚拟系只是其中一种实现。新增坐标系也会增加 TF 连通性和时间对齐的验证成本，因此判断标准应是接口假设是否冲突，而不是“会自旋就一定要采用某个公开包”。

==== 仿真与实车的限幅差异

一份 MIT 许可的公开工程同时提供了仿真和实车 Nav2 参数。对所核对版本做文本比较时，差异集中在速度与加速度限幅；这只是仓库配置的观察，不代表这些参数已经在本书环境中复现。

#figure(
table(
  columns: (auto, auto, auto, auto),
  align: (left, center, center, center),
  stroke: (x, y) => (
    top: if y == 0 or y == 1 { 1pt } else { 0pt },
    bottom: if y == 5 { 1pt } else { 0pt },
  ),
  inset: 7pt,
  table.header([*参数*], [仿真], [实车], [倍数]),
  [`max_vel_x`（m/s）], [$2.5$], [$0.5$], [$5 times$],
  [`max_speed_xy`（m/s）], [$8.0$], [$0.6$], [$13 times$],
  [`max_vel_theta`（rad/s）], [$12.0$], [$5.0$], [$2.4 times$],
  [`acc_lim_x`（m/s²）], [$5.0$], [$2.0$], [$2.5 times$],
  [`acc_lim_theta`（rad/s²）], [$15.0$], [$0.5$], [$30 times$],
),
caption: [SCNU-RM-Sentry 中的仿真与实车参数（MIT 许可证，运行效果未复现）。表中“倍数”为仿真值除以实车值：所列线速度和线加速度相差约 $2.5 tilde.op 13$ 倍，角加速度相差 $30$ 倍。参数差异说明该项目没有把仿真限幅直接用于实车；仅凭配置文件不能确定调整由电机能力、轮胎附着、功率规则、安全裕量还是控制器整定中的哪一项造成。],
)

这组数据适合用来提醒读者：仿真参数只能作为实车整定的初值，不能据此规定“上车先缩小多少倍”。应先测量实际速度、加速度、功率与跟踪误差，再逐步放宽限幅。该实车配置还将角加速度与角减速度分别设为 $0.5$ 和 $5.0$，而线速度轴的不对称方向相反：`acc_lim_x` / `acc_lim_y` 为 $2.0$，`decel_lim_x` / `decel_lim_y` 的绝对值为 $0.5$。因此，不能把这组数概括成统一的“急停优先”；它至少反映了各速度轴被分别配置。仿真配置中的对应限幅则成对对称。

还要注意 DWB 中 `acc_lim` 与 `decel_lim` 的实现语义。所核对版本的 `dwb_plugins::projectVelocity()`（BSD-3-Clause）按 `v0 < target` 选择两个限幅，它比较的是带符号速度轴上的数值变化，并不直接判断车辆在物理意义上“踩油门”还是“刹车”。例如，从正角速度降到零与从零变为负角速度可能落入同一个方向的限幅分支。若需求是限制 $|omega|$ 的增减，应针对正转、反转、过零和急停分别做测试，不能只凭参数名判断。

因此，实车整定至少要覆盖两个符号方向和过零过程；加速、减速与方向反转不是一个参数就能概括的行为。

=== 打谁：优先级从视野内升到全场

「自瞄算法设计」讨论了视野内多目标选择，例如准星距离、兵种优先级和切换滞回。哨兵还会接收自身视野之外的信息，因此需要在战术层决定关注哪个区域、追踪哪个目标，以及当前是否适合交战。

一份 Apache-2.0 许可的公开决策库使用了下面的两项打分：

$ s_i = (1 - d_i / d_max) dot w_d + (1 - h_i / h_max) dot w_h $

$d_i$ 是本车到目标的平面距离，$h_i$ 是目标剩余血量，$w_d$、$w_h$ 是权重。距离更近或血量更低都会提高分数；这通常与较短切换距离和优先处理低血量目标的策略一致，但分数本身没有验证命中概率或实际收益。位置或血量缺失时，代码给出 $-0.5$，使未知目标排在有效目标之后。

$d_max$ 和 $h_max$ 取当前目标集合中的最大值，而非常量。这使 $s_i$ 主要表示集合内的相对次序：目标加入、消失或更新后，其他目标的分数也可能变化。固定阈值若要表达绝对战术条件，就不应直接建立在这组动态归一化分数上；若阈值本来就表示“相对当前候选是否足够好”，则需把这种语义写清楚。此外，空集合、$d_max=0$ 和 $h_max=0$ 都要有显式分支，避免除零。

代码还有两个接口细节。其一，$w_d$ 与 $w_h$ 虽然是函数参数，仓库中唯一调用点却都传入字面量 $0.5$；这说明接口支持传参，不说明运行时已经支持配置。其二，函数不取 `argmax`，而是发布完整分数向量，由下游视觉模块完成最终选择。这样的分层让决策侧提供较慢的全局偏好，视觉侧仍可结合当前可见性和新观测；是否优于直接指定目标，还需结合消息频率、失效策略和实车测试判断。

阅读赛季代码时还要把当前行为与可能的历史意图区分开。该函数末尾无条件执行 `result[5] = 0`，其上一行 `if (!enemyOutpostDown)` 已被注释；当前行为可以确认，注释前的设计意图和注释原因则无法从静态代码确定。另一份决策库在 `if (!on_slope)` 内又判断 `if (on_slope && ...)`，在没有并发修改该变量的前提下，内层分支不可达。对这类代码，应写单元测试验证当前行为，再向维护者确认是否需要恢复条件，不能用注释推测替代事实。

==== 目标打分还需要哪些输入

在本章核对的公开实现中，这一例的打分维度最完整，但仍只有距离和血量。公开仓库不是随机样本，策略权重也可能因赛季整理、保密或配置缺失而不完整。因此，下面只把它当作设计起点，不据此概括参赛队伍的普遍做法。

为自己的哨兵设计打分时，可以依次考虑以下输入：

- *兵种与任务价值*：该目标失去战斗能力对当前局面的影响。
- *剩余血量*：低血量目标可能更快被处理，但它会与兵种价值冲突。
- *距离与预计命中率*：距离只是一项代理变量，还应结合遮挡、姿态和弹道边界。
- *当前威胁*：受击装甲板 ID、血量变化和目标朝向可提供线索，但不能单独证明是哪台敌车造成伤害。
- *攻击可行性*：视线、云台俯仰范围、枪口热量和安全射界不满足时，通常应先过滤候选，而不是仅少加一点分。
- *队友任务与边际收益*：避免多人重复追击同一目标，也避免抢占补给或通道。

这些量有不同单位和可信度。将它们相加前，应先定义目标函数，例如单位时间内完成防守任务的期望收益，再说明每个归一化和权重在近似什么。权重调试还需要记录候选集、最终选择与结果，否则很难区分“评分公式不合理”和“输入状态已经过期”。

=== 雷达情报怎样进入哨兵决策

按本章采用的 2026 通信表，哨兵与雷达在机器人间通信的接收对象上彼此对应。通道存在并不表示消息可以直接参与控制；至少还要处理坐标系、时间戳、目标身份和数据失效。

*第一步是统一坐标系。* 雷达通常发布场地坐标，哨兵导航使用自己的 `map`。若先验点云已经在场地坐标中建立，可以将两者的刚体变换作为标定结果保存；若 `map` 从每局启动位姿建立，则每局都要重新估计或初始化该变换。固定启动位姿只能在摆放误差满足需求时作为近似，不能替代校验。消息中还应携带 `frame_id`，接收端按目标时间戳查 TF，并在变换缺失或过期时拒绝数据。

*第二步是测量时延与误差。* $0.8$ m 是雷达标记的规则判定阈值，不能当作雷达消息的精度上限或典型误差；通信频率限制也不能直接推出“百毫秒量级”的端到端时延。应在发送端记录观测时间，在哨兵端记录接收时间和当前时间，分别评估观测年龄、链路延迟、坐标残差和目标身份错误。未经这些测量，较稳妥的默认做法是把雷达信息用于区域级导航提示或相机搜索先验，不直接作为开火坐标。

一种便于逐步验证的分层方式是：

- 雷达信息先影响区域、路径或云台搜索方向，并设置最大允许年龄；
- 自身相机确认目标身份与装甲板位置后，才进入高频跟踪和开火判据；
- 雷达目标可以作为跟踪器的初始化先验，但要带较大的不确定度，不能覆盖更新鲜的本车量测；
- 两者冲突时保留两个假设并等待新观测，超时后分别撤销导航提示或火控目标，而不是让一方永久覆盖另一方。

反向链路也可以让哨兵向雷达报告本车看到的目标。固定雷达会受高地和掩体遮挡，移动哨兵的观察位置不同，二者视野可能互补；“可能”很重要，因为两边也可能同时被同一障碍遮挡。若要合并目标，至少要统一机器人 ID、坐标系、观测时间和置信度，并避免同一观测经哨兵—雷达回传后被重复计数。雷达标记带来的规则收益使这条链路值得实现，但收益不替代数据关联验证。

以上是根据规则接口和常见融合边界提出的设计建议，本书没有验证雷达—哨兵链路的端到端延迟、精度或赛场收益。

公开仓库只能证明其中可见的实现方式。没有检索到某种融合方案，不足以说明参赛队伍没有采用它；本章对仓库的描述均限于所核对样本。

=== 决策层与导航层的边界

哨兵决策层回答“当前选择哪个任务”：去增益点、支援、回基地、防守或撤退补给。导航层则负责在给定目标后规划并执行安全路径。

Nav2 的 `bt_navigator` 本身使用 BehaviorTree.CPP，但“使用行为树”不等于比赛策略必须写进同一棵树。本章核对的公开仓库呈现了下面几种事实：

- 一份 Apache-2.0 许可的导航栈中，所核对的十个 `.cpp` / `.hpp` 文件没有出现 `referee`、`game_status` 或 `decision`；至少这部分导航代码不读取比赛状态。
- 两份修改过 Nav2 默认行为树的仓库主要调整重试、重规划和恢复动作，树中使用的是 `ComputePathToPose`、`FollowPath`、`RecoveryNode`、`RateController` 等导航节点，没有发现比赛决策节点。
- 另一份实现将比赛策略放在独立的 `rm_decision_cpp` 包中，与导航节点分进程运行。

这些样本支持一种清晰的接口划分：比赛决策发布任务或目标，导航行为树处理规划失败、重试、恢复和到点判定。两层可以在不同进程，也可以在同一进程，只要接口、更新频率和抢占语义明确。将“基地血量低于阈值”与“规划失败后清理代价地图”直接混在同一层，会增加测试和恢复难度；但公开样本不足以证明这种组织在所有项目中都错误。

也有实现把 Lua 虚拟机嵌入导航 component。当前源码在 `lua_tick_count++ == 10` 时执行决策脚本；计数器从 0 开始，因此实际是每 11 次 component 更新执行一次，而不是直观理解的每 10 次。它减少了跨进程消息传递，并允许修改脚本；相应地，脚本阻塞、异常和内存问题会与导航共享故障域。上述内容只描述所核对仓库，不代表社区分布。

公开实现还展示了多种策略载体。它们不是按成熟度从低到高排列，而是在修改成本、类型安全、时序表达和运行隔离之间作不同取舍：

#figure(
table(
  columns: (auto, auto, auto, auto),
  align: (left, left, left, left),
  stroke: (x, y) => (
    top: if y == 0 or y == 1 { 1pt } else { 0pt },
    bottom: if y == 7 { 1pt } else { 0pt },
  ),
  inset: 6pt,
  table.header([*范式*], [载体], [改策略的成本], [代价]),
  [时间轴脚本], [按开局后的时间依次发布目标点], [改常数并重启或重载], [若没有状态门控，受中断后容易与时间轴脱节],
  [随机游走], [到点后在给定区域重采目标], [改采样范围], [实现简单，但需单独处理不可达点、禁区与超时],
  [手写行为树], [自研树框架与黑板，节点为 C++ 类], [改 C++ 并重新编译], [类型检查较强，树框架和工具链由团队维护],
  [XML 行为树], [BehaviorTree.CPP；结构写在 XML，条件可用标准脚本节点], [改 XML 并重载；可用 Groot 查看], [键名、字符串和跨节点约定需额外验证],
  [插件化 XML 行为树], [条件与动作节点编成动态库，按名字加载], [改 XML；新增能力需编译插件但不改主程序], [需要维护 ABI、端口类型、插件发现和版本兼容],
  [加权规则表], [评分函数配合 JSON 权重与阈值], [改配置并重载], [排序直接，复杂时序与抢占需另建机制],
  [嵌入式脚本状态机], [节点内嵌脚本运行时，状态机写在脚本中], [改脚本文件], [可热改，但需运行时校验、超时和异常隔离],
),
caption: [公开仓库中可见的决策载体。选型时可重点比较修改后是否需要编译、错误在加载期还是运行期暴露、能否表达抢占与超时，以及故障是否会影响导航。赛场上快速改配置有价值，但未经校验的热更新同样可能把错误直接带入运行。],
)

相较于集中在一个大函数中的分支，XML 行为树可以把策略结构与节点实现分开，也便于用图形工具检查执行路径。它并不会自动提高正确性：动作和条件仍需实现、注册和测试，黑板键、抢占、节点 `halt()` 与超时语义还会增加新的接口。

BehaviorTree.CPP v4 的 `ScriptCondition` 是上游提供的标准节点，定义在 `include/behaviortree_cpp/actions/script_condition.h`。因此，在 XML 属性中写条件表达式不表示项目修改了行为树库；是否存在分叉仍要比较实际依赖源码和版本。

==== 一个十五行的主树

SMBU 北极熊战队公开的哨兵行为树包采用 Apache-2.0 许可证，其中 RMUL 主树只有十五行：

```xml
  <BehaviorTree ID="rmul_2025">
    <KeepRunningUntilFailure>
      <ForceSuccess>
        <ReactiveSequence>
          <SubTree ID="check_game_start"/>
          <PublishSpinSpeed spin_speed="7.0"
                            duration="0.0"
                            topic_name="cmd_spin"/>
          <SubTree ID="check_robot_status"/>
          <PubNav2Goal goal="4.65;-3.5;0"
                       topic_name="goal_pose"/>
        </ReactiveSequence>
      </ForceSuccess>
    </KeepRunningUntilFailure>
  </BehaviorTree>
```

（引自 `pb2025_sentry_behavior` 的 `rmul_2025.xml`，Apache-2.0 许可证。）

按该版本节点语义，`ReactiveSequence` 每个 tick 从第一个子节点重新检查：先检查比赛开始，再发布自旋速度，随后检查机器人状态，最后发布导航目标。某一步返回失败时，后续节点在该 tick 不执行。外层 `ForceSuccess` 将内部失败转换为成功，`KeepRunningUntilFailure` 因而持续重复这段序列。需要注意，重复发布是否会产生额外副作用（即动作是否幂等）、失败后是否撤销旧导航目标，仍由具体节点实现决定，单看 XML 不能确认。

`check_game_start` 子树先判断比赛状态；未开始时，它发布零自旋和零平移指令，再返回失败，使主树本 tick 停在这里。`check_robot_status` 在状态不满足条件时检查车辆是否位于补给区，否则发布补给点目标。这里的“停车”只表示上层发布零速度，实际制动效果还取决于速度仲裁、通信超时和底盘控制器。

这个例子展示了组合方式：比赛状态门控、自旋、机器人状态门控和导航目标由 XML 排序，节点不必彼此直接调用。新增“低血量回防”时可以增加子树，但仍需定义它相对补给、旧目标取消和自旋命令的优先级。

该包包含五个条件节点、七个动作节点以及三个控制或装饰节点，共十五个插件：输入覆盖比赛状态、机器人状态、受击、目标与 RFID，动作覆盖导航、自旋、平移、云台指令和攻击位姿计算。每个插件编译为动态库并按名称加载。新增能力仍需编译、安装和版本匹配，只是不必修改主程序源码。

==== 根黑板中的 `@` 键

BehaviorTree.CPP 的节点通过黑板传递数据，XML 中通常写作 `{key}`。子树具有自己的黑板作用域，这有利于复用，但跨层数据需要端口重映射；任何一层的键名或映射错误都会使下游拿不到预期值。

v4 可以在键名前加 `@` 访问根黑板。例如 `{@referee_gameStatus}` 在不同子树中引用同一个根键。该项目用下面的模板函数订阅话题并更新根黑板：

```cpp
template <typename T>
void SentryBehaviorServer::subscribe(
  const std::string & topic, const std::string & bb_key, const rclcpp::QoS & qos)
{
  auto sub = node()->create_subscription<T>(
    topic, qos,
    [this, bb_key](const typename T::SharedPtr msg) { globalBlackboard()->set(bb_key, *msg); });
  subscriptions_.push_back(sub);
}
```

（同上，Apache-2.0 许可证。）

同一个模板覆盖比赛状态、机器人血量、事件数据、RFID、自身状态、增益状态、地面机器人位置、自瞄目标和全局代价地图等输入。新增一种已知消息类型时，只需在构造处增加订阅并约定根键。

这种写法把输入话题与黑板键集中成一张映射表，减少了逐层端口传递。代价是子树隐式依赖全局键，复用性和静态可见性会下降；消息值也没有自动附带“是否收到过”和“是否过期”的判断。实际项目可给每个全局状态同时保存时间戳或新鲜度标志，并在加载时检查必需键。另一种 `Topics2Blackboard` 节点会显式列出端口，配置更长，但依赖更容易从树顶看见，两者应按可维护性选择。

==== 另一种风格：把时间轴写进树里

另一个未提供仓库级许可证的项目包含一棵标记为 2025 赛季的 149 行行为树。本节只描述结构，不摘录源码。主树首先判断补给：自身血量低于 90 或 17 mm 弹丸余量低于 14 时去补给，直到血量高于 399 且弹量高于 99 才离开；随后是基地血量低于 2001 时的回防分支；主战逻辑再以剩余时间 270 秒为界切换两套动作子树。

这些阈值不能脱离赛季直接复制。以本章引用的 2026 手册为例，400 对应全自动哨兵的血量上限，因此“高于 399”表示回满；100 不是弹量上限：哨兵初始弹量为 300，远程兑换和补给区定额存在 100 发这一档，手册 5.3.2 节还规定了每次至少 10 发的场内兑换。由此只能推测“高于 99”可能用于等待一批补给，不能确认 2025 树的作者意图。时间分段则清楚地表明，同一条件在比赛不同阶段会进入不同动作子树；具体战术含义还要结合目标点和比赛录像验证。

前后两套动作都使用 `IsInPosition` 装饰器。它接收四边形顶点，并在*机器人当前位置*位于四边形内时 tick 下方的“导航到攻击位姿”动作。这更准确地说是动作的地理激活区，而不是对目标点的严格地理围栏：仅从结构看，它没有验证计算出的攻击目标是否位于四边形内；机器人越界后是否立即停止，还取决于装饰器每 tick 的返回值和子节点 `halt()` 实现。

两组顶点的轴对齐包围盒约为 $8.1 times 8.5$ m 和 $11.8 times 15.6$ m；后者包围盒面积约为前者的 $2.7$ 倍，但这不是四边形的精确面积。三个方向的边界向外扩展，其中最大变化约 $6.6$ m，$x$ 下界则内收约 $0.4$ m。这说明后半段的激活区域更大，并不单独证明机器人实际追击距离或策略收益增加。

这一结构的可复用之处在于：时间窗、地理区域、频率限制和冷却等约束可以写成装饰器，再用于多个动作。与此同时，区域坐标应注明参考系，位置消息要有时间戳，边界点要覆盖红蓝方镜像，装饰器还应测试越界、恰好落边和 TF 暂不可用时的行为。

==== 声明式配置仍需要输入校验

将策略结构移到 XML 后，修改判据不必重新编译主程序，但部分错误也从编译期移到了加载期或运行期。前述无许可证仓库中有两个例子，本节仍只描述现象。

第一，五棵标记为 2024 赛季的树将 17 mm 弹丸余量写入拼成 `sllowance` 的黑板键，所核对的这些 XML 中没有节点读取该键。BehaviorTree.CPP 允许写入无人读取的键，因此这不会自然报错。2025 树中键名被改正，弹量也进入补给判据；这只能说明两个版本文件的差异，不能证明整个 2024 赛季部署时都未使用该数据。

第二，一个导航目标的坐标分量写成了 `-5.6.0`。解析使用 `std::stod`，它读取最长合法前缀，在第二个小数点处停止并返回 $-5.6$；若调用者不检查停止位置，这个格式错误就会被接受。

调用 `std::stod` 时应接收第二个参数 `pos`，在忽略约定允许的尾随空白后检查是否消费完整字符串；也可以使用提供错误码与终止指针的解析接口。`5.6abc` 这类输入同样会暴露前缀解析问题。小数点和区域设置还要统一，不能让配置文件依赖运行机器的 locale。

较稳妥的做法是让 C++ 节点声明具体端口类型，使 XML 只提供阈值和映射，并在加载后遍历黑板依赖、检查未知端口和未消费键。比赛状态、机器人状态和 RFID 等使用独立条件节点，也更容易为缺消息、旧消息和非法值编写测试。是否能在加载期发现拼错的名称取决于节点注册与校验实现，不能只依赖框架默认行为。

==== 仓库内置 BehaviorTree.CPP 的版本差异

同一仓库将 BehaviorTree.CPP 4.5.1 的依赖源码直接放入工作空间（常称为 vendor）。与上游同版本比较后，`src/controls/reactive_fallback.cpp` 中的静态开关初值不同：

```text
仓库内置版本：            ReactiveFallback::throw_if_multiple_running = false
上游 4.5.1 同一个文件：   ReactiveFallback::throw_if_multiple_running = true
```

在上游 4.5.1 的相关执行路径中，当前 RUNNING 子节点与上次不同时，这个检查可以抛出 `LogicError`；分叉版默认关闭该检查。

该项目顶层 `ReactiveFallback` 下包含补给、基地告急和分阶段作战分支，其中有重试循环与 `Sleep` 等跨 tick 保持 RUNNING 的节点。静态结构表明运行分支可能随比赛状态变化而切换，与该检查处理的场景相符；本书没有运行这棵树来确认具体触发序列。

因此，把 XML 移到未修改的 4.5.1 时，在发生相应 RUNNING 分支切换的路径上可能抛异常；不是“复制后立即必然失败”。上游从 4.6.0 起也将该默认值改为 `false`。这说明复现行为树需要记录依赖版本和补丁，而不能只保存 XML。所核对的内置依赖相对上游 4.5.1 只改动了 `reactive_fallback.cpp`，但其中共有三处差异。

第二处差异位于 RUNNING 分支的 halt 循环：上游遍历所有兄弟并跳过当前节点，分叉版只处理当前 RUNNING 节点之后的兄弟。按所核对源码，前面的 FAILURE 节点因而可能保持 FAILURE，而不是被复位为 IDLE；`_skipIf`、`_while` 等前置条件是否重新求值又与节点状态有关，所以这一差异可能改变后续 tick 行为。第三处差异缩短了 `LogicError` 文本，删除了关于 `EnableException(false)` 的提示。即使在干净上游中关闭异常，也未复现前述状态复位改动；需要针对分支切换和前置条件写逐 tick 回归测试。

复现这类项目时，应将仓库内置依赖与标称上游版本做差异比较，并把补丁纳入版本记录。若设计依赖修改控制节点语义，需要明确记录原因并测试上游升级；也可以评估普通 `Fallback`、互斥前置条件或显式抢占器，但这些替代方案同样要验证 halt、恢复和并发状态，不能仅凭结构认定哪一种必然正确。

==== 三个赛季代码中的决策细节

下面三个公开实现片段分别涉及终局策略、共享区域和不可直接观测的弹量。它们可以作为设计问题的例子，不应脱离赛季规则直接复制。

*第一条：终局占点优先。* 一份决策脚本通常在低血量时撤退，但在“剩余时间小于阈值且位于增益点”时优先进入防御状态，使撤退分支不再执行。同一仓库的两份配置分别将阈值设为 $10$ s 和 $280$ s；在总时长 $420$ s 的规则下，触发窗口差异很大。静态代码可以确认这是一项可配置的策略切换，不能确认两组值是否都用于正式比赛，也不能证明增益点价值随时间单调变化。实装时应记录触发原因，并给终局覆盖逻辑设置清楚的安全例外。

*第二条：限制补给区停留时间。* `rmcs-navigation`（BSD-2-Clause，Copyright 2025 Alliance Algorithm Team）的黑板中设置了 $6$ s 的“补给间隔”，注释说明该限制用于避免机器人意外阻塞其他机器人进出。

这条规则把共享空间的占用纳入单车策略。在本章核对的样本中，它是明确提及队友通行的一例，但样本不足以说明其他项目没有协同逻辑。自己的血量、弹量和目标之外，决策层还可以维护补给区占用、队友任务、火力重复分配和路线冲突。固定 $6$ s 仍需与当季补给规则和实际通行时间核对，并设置“补给尚未到账”“出口被占用”等异常分支。

*第三条：估计弹仓余量。* 裁判系统的 `projectile_allowance` 表示剩余发射配额，不等同于弹仓内的实弹数。一份 GPL-3.0 决策库累计配额增加量，再与当前配额比较以估计已发射数量，最后用注释中明确标为“假设”的弹仓容量估计余量。

这是用可观测增量估计不可直接观测状态的一种方法，但“配额增加”“实弹装入”和“发射成功”不一定严格同步。漏包、配额重置、补给与射击在两个消息间同时发生、卡弹或人工装填都可能破坏账本。更可靠的实现应定义状态重置条件，保留原始配额与估计值，并利用确认空仓、补给完成或弹丸传感器等独立观测校正。没有这类观测时，它只是开环估计，不能标成真实弹量。

==== 复用代码前检查许可证

前述手写行为树框架的源码头部包含 `Copyright (C) 2019 RoboMaster`，并采用 GNU General Public License v3.0（GPL-3.0）；其代码来源与 DJI RoboRTS 项目有关。技术结构可以学习，实际复用还要满足许可证条件。

GPL-3.0 是 copyleft 许可证。复制 GPL 代码并分发由此形成的衍生作品时，通常需要按 GPL 提供相应源代码和许可；动态链接、进程边界、仅内部使用等具体情形的义务不能用“传染性”三个字准确概括。若团队不能接受相应发布义务，应避免复制，改用具有兼容许可的实现或自行实现，并在有法律风险时咨询专业意见。许可证判断要阅读全文和文件头，不能只看第一行。

本节描述的另一个仓库存在不同问题：决策包的 `package.xml` 写有 `<license>Apache-2.0</license>`，但仓库根目录和该包目录中没有找到适用于作者新增代码的 LICENSE 正文或清晰文件头。包内一份启动脚本保留了 Intel 的 Apache 版权头，它只说明该文件的来源，不能替其他新增文件授权。

仓库中的第三方依赖子树分别带有自己的许可证：BehaviorTree.CPP 为 MIT，FAST_LIO 为 GPL-2.0，Livox 驱动为 MIT；另一个接口包放置了带未填写版权占位符的 Apache-2.0 模板。这些文件说明各子树的许可情况，不会自动为相邻的原创决策包补上授权。反过来，顶层许可证也不能覆盖第三方子树原有且可能不同的条款。

`package.xml` 的 `license` 标签是包元数据，不能替代实际适用的许可文本或文件头。它可以帮助索引和打包工具检查声明，但复用者仍需确认谁对目标代码有权授权、许可文本适用于哪些路径，以及版权与声明保留义务。

实用检查顺序是：先确定要复用的具体文件；再从该目录向上查找 LICENSE、NOTICE 和文件头；识别 vendored 代码及其独立许可证；最后核对复制、修改、链接和分发方式是否满足条款。若新增代码没有明确授权，不应仅凭仓库公开、README 或 GitHub 标签复制到自己的项目。

发布自己的代码时也应在仓库根目录放置适用的 LICENSE，并为第三方代码保留原始声明。公开可访问和允许复用是两件事；许可证写清楚，读者才能知道可以怎样使用、修改和分发。

=== 从规则算例确定运算顺序

上一章通过手册中的 `i.e.` 从句确认雷达标记使用平面二维距离。哨兵姿态系统也有一个类似算例，它明确了不同冷却增益的选择方式与运算顺序。

2026 手册定义了进攻、防御和机动三种姿态模式。这里先使用一条规则：防御模式将热量冷却率乘以 $1 \/ 3$。

手册算例中的全自动哨兵处于防御模式，基础冷却率为 30/s，同时获得大符的三倍冷却效果和堡垒增益点的 $+75$/s。手册先在两种增益中取较大者，并明确写出 `i.e., +75 per second`；冷却率先算为 $30+75=105$/s，再乘防御模式的 $1 \/ 3$，得到 $35$/s。

“取二者中较大的一个”排除了同时叠加两项增益，因此下面两种计算不符合该算例：

- *叠加*：$30 times 3 + 75 = 165$；
- *先加后乘*：$(30 + 75) times 3 = 315$。

剩下的问题是怎样比较“三倍”和“$+75$/s”这两个不同写法。以基础值 30/s 计算，三倍效果对应总值 90/s，也就是增量 $+60$/s；堡垒增益对应总值 105/s。`i.e., +75 per second` 明确选择了后者，因此中间值是 105/s，而不是 90/s。

由于两种增益作用于同一基础冷却率，比较增量（$60$ 与 $75$）和比较总值（$90$ 与 $105$）会得到相同选择。赛季规则或增益基数变化后未必仍然等价，因此代码应实现手册规定的顺序，而不是只保存最终常数。

按每发 17 mm 弹丸增加 10 点热量计算，冷却率除以 10 给出*忽略热量上限瞬态、发射机构和其他限频条件时*的长期平均射速上界。105/s 与 35/s 分别对应 10.5 发/s 和 3.5 发/s。若错误地先乘 $1 \/ 3$ 再加 75，会算出 85/s，即 8.5 发/s，约为正确结果的 $2.4$ 倍。这里比较的是规则模型中的平均上界，不是实测射速。

规则常量应注明赛季、手册版本和条款位置；手册提供的算例则适合直接写成回归测试。对于这里的模型，测试输入应保留“基础 30、三倍增益、$+75$ 增益、防御模式”四项，并断言中间值 105 和最终值 35。这样赛季更新时能判断是规则有意变化还是实现顺序被改错。

（本节所有规则数值均取自 2026 赛季手册，随赛季变动，以当季手册为准。）

==== 姿态模式：带冷却时间的调度

先按 2026 手册中三种姿态的*默认效果*计算长期平均射速上界。以下数值均为规则推导、非实测，不包含强化或衰减后的效果，并沿用“基础冷却率 30/s、每发增加 10 点热量、无其他限频”的假设：

#figure(
table(
  columns: (auto, auto, auto, auto, auto),
  align: (left, center, center, center, left),
  stroke: (x, y) => (
    top: if y == 0 or y == 1 { 1pt } else { 0pt },
    bottom: if y == 3 { 1pt } else { 0pt },
  ),
  inset: 7pt,
  table.header([*模式*], [冷却率], [可持续射速], [底盘功率], [生存性]),
  [进攻], [$30 times 3 = 90$/s], [$9$ 发/s], [$times 0.5$（50 W）], [易伤 $+25%$],
  [防御], [$30 \/ 3 = 10$/s], [$1$ 发/s], [$times 0.5$（50 W）], [防御增益 $50%$],
  [机动], [$30 \/ 3 = 10$/s], [$1$ 发/s], [$times 1.5$（150 W）], [易伤 $+25%$],
),
caption: [三种姿态模式默认效果的规则推导（2026 手册，非实测）。底盘功率按全自动哨兵基准 100 W 换算，表中既不采用强化效果，也不采用持续三分钟后的衰减效果。射速列是仅由冷却率得到的长期平均上界：进攻模式为 9 发/s，防御和机动模式均为 1 发/s；它不包含热量余量带来的短时连发。],
)

手册还定义了三种强化姿态，每种在一局内最多累计 15 s，三者分别计时：强化进攻使当前热量保持为 0，发射不再增加热量；强化防御将防御增益提高到 $99%$；强化机动将底盘功率上限提高到 200 W。它们都是短时状态，不能用上表的长期默认射速替代；调度器还要分别记录三种强化姿态的剩余累计时间。

由表中规则可以得到三项设计约束：

- *进攻模式提高长期平均火力*。按上述简化模型，9 发/s 是另两种模式的九倍；机动模式仍可利用已有热量余量短时发射，不能表述为“不能打”。
- *火力与底盘功率存在权衡*。进攻模式的底盘功率为 50 W，机动模式为 150 W；这会限制同时高速机动和持续输出的能力，但不表示开火与移动在逻辑上完全互斥。
- *防御模式提供防御增益，同时降低冷却率和底盘功率*。它是否适合基地防守、拖延或撤退，应结合当前血量、热量、路径和敌方火力判断，不能仅由模式名称决定。

切换还有 5 s 冷却，同一模式持续超过三分钟后效果会衰减。因此，模式切换不适合跟随逐帧检测结果直接抖动；决策层至少要保存当前模式、最近切换时刻、累计持续时间、三种强化姿态各自的剩余时间和待切换原因，并设置滞回。持续使用一个模式是否需要主动切换，仍取决于衰减后的净收益，不能只凭“三分钟”得出必须轮换的结论。

更合适的抽象是把姿态视为带冷却与持续时间状态的调度资源。逐帧火控、秒级导航目标和更慢的姿态调度可以共享状态，但应分别限频并通过明确事件交互。前述 270 s 分段树说明公开实现确实会把比赛时间写入策略；它与姿态规则是否存在直接因果关系，本书没有证据确认。

=== 双云台：两路感知与一个底盘

单云台的瞬时视场有限，有队伍为哨兵配置两个独立云台：每套都有相机、yaw/pitch 机构和发射机构，共用一个底盘。双云台扩大了同时覆盖方向，也增加了 TF、时间同步和目标仲裁问题。

（本节参考的两个仓库根目录都没有 LICENSE。双云台工作空间中的 `rm_auto_aim`、`rm_vision` 和 `ros2_mindvision_camera` 子包保留 MIT 许可，`rm_gimbal_description` 与 `rm_serial_driver` 保留 Apache-2.0 许可；下面的 URDF 结构来自已许可的 `rm_gimbal_description`。队伍新增的仲裁包未找到 LICENSE，`package.xml` 仍是 `TODO: License declaration`，因此本节只描述公开可见的行为，不摘录其源码。两个从上游改名而来的相机包未保留原 LICENSE 文件；这不会消除上游权利和原许可证义务。具体复用仍按上一节的方法逐文件核对。）

*坐标系结构。* 所核对 URDF 在底盘 `odom` 下连接“大云台” `gimbal`，再向左右分出 `odom_link_first` 和 `odom_link_second` 两套小云台底座；横向偏移为 $plus.minus 0.123$ m，纵向偏移为 $-0.140$ m。每个分支继续连接小云台、相机和光学坐标系。它沿用了单云台的 TF 链，只是增加平行分支。另一支队伍的公开固件则分为主控、左云台和右云台三个 STM32 工程，说明双云台也可以按控制板拆分；这是另一个项目的结构观察，不能用于验证前述 URDF 的实机连接。

*两路目标仲裁。* 两台相机分别运行检测并发布装甲板，仲裁节点选择一路转发给单一跟踪器。所观察到的逻辑使用计数器维持互斥：先检测到目标的一路获得使用权并将计数器置为 80；计数器有效期间，另一路不向下游发布；当前一路连续丢失目标、计数器归零后，另一路才可切入。

这段结构有两个明确边界：

第一，它没有把两路三维观测变换到共同坐标系进行关联，因此不是跨相机去重或融合。在占用窗口内，跟踪器只收到当前一路，另一路观测被抑制。这样可以避免两个异步来源交替驱动单跟踪器，但也放弃了重叠视场中的互补信息。是否为作者有意简化，只能由文档或维护者确认。

第二，80 的单位是帧，不是时间。按文档给出的约 $60 tilde.op 70$ FPS，它对应约 $1.1 tilde.op 1.3$ s；若帧率降到 30 FPS，则变成约 $2.7$ s。更稳定的接口应记录最后一次有效检测的采样时间，并用持续时间判断切换，同时明确丢帧、消息暂停和时钟跳变时的处理。

项目文档称，两路图像的时间戳差异较大，精确同步和近似同步未达到预期，因此采用上述互斥方法。这是团队自述，本书未复现。时间差可能来自采集不同步、时间戳打在处理完成时、两路处理延迟不同或队列积压；仅凭“同步器效果不好”不能确认时间戳本身不可信。排查时应先同时记录硬件采集时间、ROS header 时间、到达时间和处理完成时间，再决定是校正时钟、补偿固定延迟，还是保留异步跟踪。

本章核对的公开样本都采用多路独立检测再在后端选择或仲裁，没有发现先拼接全景图再检测的实现。另一支队伍公开的是一台工业相机负责自瞄、四台 USB 相机负责更大范围感知的分离结构。全景拼接需要重叠视场、相机间外参和额外配准计算；独立检测则需要后端身份关联与时间管理。样本只说明两类方案的取舍，不能据此认定拼接在车载平台上一定不划算。

=== 反自瞄：运动模型的适用边界

前面的跟踪章节主要讨论怎样预测目标。本节从防守方考虑：主动改变运动状态会怎样影响使用固定运动模型的预测器。

卡尔曼滤波器必须指定状态转移模型。本书介绍的示例以及本章核对的若干公开实现采用匀速（CV）平动模型：状态包含位置与速度，未建模加速度通过过程噪声 $bold(Q)$ 吸收；旋转装甲目标还可能假设中心匀速移动、装甲板匀速转动。目标发生模型未覆盖的变速时，预测误差可能增大，具体幅度取决于过程噪声、量测频率、估计延迟和替代模型，不能只由加速度大小决定。

火控通常根据估计状态预测弹丸飞行时间 $t_"fly"$ 后的位置。若预测器仍沿用反向前的速度，而目标在这段时间内反向，匀速外推会产生系统偏差；更完整的模型、更新鲜的量测或降低提前量都可能改变结果。

用一维理想模型估算量级。取 17 mm 弹丸速度 25 m/s（本章引用的 2026 手册上限）和距离 5 m，先忽略空气阻力与高低差，得到名义飞行时间 $t_"fly"=0.20$ s。若目标横向速度从 $+u$ 瞬时变为 $-u$，而预测器在整个窗口仍使用 $+u$，两种外推位置之差为

$ Delta approx 2 u t_"fly" = 0.40 "m" $

取 $u=1$ m/s 得 $Delta=0.40$ m。为了给图提供尺度，脚本使用 PnP 模型中 135 mm 灯条中心距的一半，即 $0.0675$ m，作为参考阈值；$0.40$ m 约为它的 $5.9$ 倍。灯条中心距不是装甲板的完整物理命中边界，实际命中还取决于弹道、姿态、散布、时延和目标几何，因此这个比值不能推出“必然脱靶”。瞬时反向和预测器保持旧速度也都是理想化假设。

#figure(
  image("images/adv-sentry-antiaim.png", width: 100%),
  caption: [变速运动与匀速预测器的仿真（脚本 `adv_sentry_antiaim.py`，非实测）：目标以 1 m/s 横移，每 1.0 s 反向；预测器为 100 Hz 的二维状态 CV 卡尔曼滤波器，量测噪声标准差 2 cm；名义距离 5 m、弹速 25 m/s。左图剔除滤波器最初 0.5 s 的初始化过程后，比较 $t_"fly"$ 后的真实横向位置与预测位置，并统计误差超过 $plus.minus 67.5$ mm 参考阈值的时间；本次固定随机种子运行中占比约 $44%$。右图让不同反向周期复用同一量测噪声序列，模型内最大值约在 0.4 s、为 $90%$，3.0 s 时约为 $11%$。这不是命中率曲线：阈值不是完整靶面，仿真还假设速度瞬时反向；灰色区域尤其超出多数实车的加速度能力。],
)

图中的 $44%$ 只是“预测误差超过参考阈值”的时间比例，不能解释为把命中时间从 $100%$ 降到 $56%$，也不能据此计算击杀时间增加 $1.8$ 倍。「自瞄算法设计」中的 $eta$ 只有在开火机会、阈值与实际命中事件一一对应且其他条件不变时，才能与 DPS 建立那样的简化关系；这里没有验证这些前提。仿真的可靠结论限于：在给定 CV 模型和参数下，误差随反向周期呈非单调变化，因而“越快反向越好”并不成立。

一份公开决策库包含专门的反自瞄行为树节点。一个可配置周期被分为四段：两段纯横移，两段带 $30 degree$ 斜向分量；每段内部又使用慢—快—慢三档速度。按指令积分，一轮的净位移接近零。方向和速度的分段变化会增加固定 CV 或 CA 模型的失配，但实际轨迹受加速度限幅、轮胎滑移和下层闭环影响，不能由指令直接认定会回到原点或使某类跟踪器失效。

另一份实现比较相邻裁判系统消息中的血量；血量下降或前后相机任一路检测到目标时，设置底盘自旋标志，并在触发消失约 10 s 后停止。代码还在斜坡状态下禁用该动作。静态逻辑表明作者考虑了坡面边界，但本书没有验证“坡上自旋会翻车”的具体阈值。实装时还应处理血量消息乱序、复活重置、检测抖动，以及斜坡状态缺失时采用何种安全默认值。

机动策略还需同时评估以下代价：

- *功率与热量*。按 2026 规则，机动模式将 100 W 基准底盘功率提高到 150 W，同时把冷却率降至 $1 \/ 3$。高速机动与长期平均射速因此存在可量化权衡。
- *自身估计与控制*。急加减速会提高里程计、结构振动、图像模糊和运动补偿的负担；若还伴随底盘转动，TF 时间错配也会放大。影响量级需用本车录包和图像测量，不能假设会原样等于对手的预测误差。
- *指令仲裁*。导航与反自瞄若同时发布 `cmd_vel`，应通过唯一的速度选择器（mux）或控制器定义优先级、抢占条件、持续时间和超时。将底盘自旋放在独立话题后叠加可以分开接口，但仍要处理合成后的速度、功率和安全限幅。

反自瞄的核心不是寻找一条固定“最优”轨迹，而是承认对手模型、己方动力学和规则资源会共同变化。适合本车的周期只能在安全加速度范围内测试，并以对手预测误差、己方命中率、功率和定位质量共同评价。

==== 本章小结

2026 规则把哨兵置于 Starting Zone，也没有安排单独的建图环节。系统设计因而要提前确定地图来源，并让自身定位、视觉目标和雷达坐标落到同一场地参考系。雷达标记阈值不能反推测量精度，外部情报在进入火控前还要验证时间戳、身份和不确定度。

导航接口的重点是坐标含义一致。自瞄姿态参考与导航里程计不能共用含义不同的 `odom` 名称；旋转参考系中的速度转换还要使用与指令时刻对应的偏航角。虚拟固定偏航系可以降低控制器与平滑器面对的坐标变化，但不是所有自旋底盘的唯一方案。以 $omega=3$ rad/s、$Delta t=50$ ms、$|bold(v)|=2.5$ m/s 为假设，偏航时间错配对应约 $0.37$ m/s 的速度误差；实车数值必须由录包中的实际时间差计算。

比赛决策与导航恢复属于不同层次。无论使用 XML 行为树、插件、规则表还是脚本状态机，都要明确目标抢占、输入过期、故障隔离和配置校验。动态归一化分数适合候选排序，不宜未经说明就与绝对阈值比较；仓库内置依赖也要与上游版本一起记录。复用公开代码时按具体文件核对许可证，`package.xml` 的声明不能替代实际授权文本。

姿态模式把火力、底盘功率、防御效果、5 s 切换冷却和三分钟衰减放进同一个调度问题。双云台则增加两条 TF 与检测链，80 帧互斥只能完成来源选择，不能替代跨相机数据关联。反自瞄仿真进一步说明，固定 CV 模型对变速运动存在边界，但“误差超过 67.5 mm 参考阈值”不是命中率；评价机动策略还要同时测量己方火控、定位、功率和底盘安全。

下一章「性能优化技巧」继续讨论同一系统在固定算力上的运行效率：先测量时间花在哪里，再决定优化算法、内存、并行还是数据链路。

（本章规则数值已按 RoboMaster 2026 大学生超级对抗赛官方英文手册 V2.1.0（2026-07-17）复核，随赛季与版本变化，以当季正式手册为准。源码或配置摘录已就地标注来源与许可：SMBU 北极熊战队的 `pb2025_sentry_nav`、`pb2025_sentry_behavior` 为 Apache-2.0，CodeAlanqian 的 `SCNU-RM-Sentry` 为 MIT，Alliance Algorithm Team 的 `rmcs-navigation` 为 BSD-2-Clause，仓库内置的 BehaviorTree.CPP 子树为 MIT。GPL 项目及未找到明确许可证的原创包只描述可见结构，不摘录源码。本章的坐标误差、姿态射速和反自瞄结果均为规则计算或简化仿真，不是实车测量；团队 README 中的效果描述也未由本书复现。）
