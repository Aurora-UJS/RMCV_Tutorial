#import "/template/template.typ": *

=== 哨兵从导轨上下来了

翻开 2022 赛季的手册，各兵种"初始位置"那一栏里，步兵、英雄、工程都写着"启动区"，只有哨兵单独写着*哨兵轨道*——它是一台跑在环形导轨上的车，位置被一维参数化，"往哪走"这个问题在机械上就已经被回答掉了。再翻 2026 赛季手册，哨兵的初始位置和其他地面机器人一样，是 Starting Zone（4.2.1 节）。轨道没了。

这一条规则改动，就是本章存在的全部理由。哨兵不再是一台"只能前后挪的炮塔"，而是一台*自由移动的地面机器人*：它要自己知道现在在场地的哪里、要自己决定去哪、要自己规划一条不撞墙不掉高地的路、还要在移动的同时把云台稳住去打人。于是导航栈——定位、建图、代价地图、局部规划——从一个"哨兵不需要的东西"变成了哨兵视觉的核心。这是全书唯一一次，视觉的下游不是云台，而是底盘。

先把边界划清楚，免得你以为本章会重讲。哨兵在*感知层*的那些事——广角加长焦的相机选型、多相机怎么硬件同步、多台同型号相机怎么靠 SN 钉住逻辑角色、跟踪器为副相机瞥见高优先级目标而多出来的 `switching` 态、多路目标汇总重排与切换滞回——「工业相机」「装甲板的跟踪」「自瞄算法设计」三章已经讲完了，本章一个字都不重复。本章从那条线的*出口*接上：当一台车同时握着"我在哪""敌人在哪""我该去哪""我该打谁"这四个量时，它们怎么互相打架，以及怎么调停。

还有一条规则约束要先摆上来，因为它决定了哨兵在队伍里的位置。上一章「雷达站视觉方案」已经把手册通信表那两行逐字拆过一遍：受限的只有*接收*侧——雷达只能收哨兵、哨兵只能收雷达，而两者的发送侧都是己方全体，其余兵种的通信栏写的是不带条件的 Allowed。那一章是从雷达那一侧读的，这里从哨兵这一侧接住，补两件在雷达那侧看不出来的事。

其一，手册 3.7 节对雷达的正文描述写明，雷达自主获取战场信息后可以发给"己方机器人或裁判系统选手端"——所以雷达情报本来就能直接播给英雄、步兵，*不必经哨兵转发*，哨兵不是全队的情报中转站。其二，把辖域倒过来看接收侧：在*机器人间通信*这一层，哨兵能拿到的机外情报只有雷达一路——雷达站在高处，看得见你视野之外的半个场地。场外确实还有第二条通道，但它不在通信表里：云台手可以从裁判系统客户端给哨兵转发小地图标记（见下一段）。这条通道是*人工的、低频的、对全自动哨兵还按次收费*，它能在关键时刻捅你一下，撑不起一份持续更新的全局态势。所以真正给哨兵持续供数的机外信源，仍然只有雷达这一路。你的决策层因此有两个来源完全不同的世界模型——一个是自己相机加激光雷达构建的、精确但视野受限的局部模型，一个是雷达喂过来的、粗糙但全场覆盖的全局模型。怎么把它们融合、冲突时信谁，是哨兵决策层独有的题目。

顺带一个容易被忽略的接口：手册允许云台手用裁判系统客户端干预哨兵，包括转发小地图标记、远程兑换弹丸与血量、确认复活、切换姿态模式。但对*全自动*哨兵，云台手每执行一次这类操作要花 50 金币（半自动哨兵免费）。也就是说，人工介入在游戏里是明码标价的——你的决策层不能把"打不过就喊云台手"当成常规兜底，那是要付钱的。

=== 地图从哪来：离线预建图还是上场 SLAM

导航的第一个问题不是"怎么走"，是"我在哪"。而"我在哪"要有个参照物，那就是地图。

2026 手册在赛前流程附录里写了一句很关键的话：*This season does not have a separate Sentry Mapping section*——本赛季没有单设哨兵建图环节。这句话的分量得掂一掂：它意味着你*没有一个官方保证的、在正式比赛场地上从容跑一圈采点云的时间窗*。于是"地图从哪来"成了一道真实的设计选择题，两条路各有各的代价：

- *离线预建图 + 上场重定位*。赛前找机会（练习赛、检录间隙、或者干脆用官方公布的场地模型）录一份场地点云存成 `.pcd`，比赛时哨兵开机，把当前激光扫描配准到这份先验点云上，解出自己在地图里的位姿。好处是坐标系*跨局稳定*——你可以把"基地前的防守点""增益点""狗洞口"这些关键位姿硬写进配置，每一局都指向同一个物理位置。代价是你得先有那份点云，而且实际场地与预建图一旦有出入（挡板位置微调、场上多了几台车），配准就可能把你钉错地方。
- *上场纯里程计建图*。开机即建图，`odom` 系原点就是开机位置，不需要任何先验。好处是零准备、不怕场地变动。代价是所有目标点只能相对开机位置来表达，而开机位置每局都要靠人摆——摆偏了半米、转偏了 10 度，整套预设路点全跟着偏。

一个成熟的做法是*把这两条路做成同一套代码的一个开关*。有一支队伍开源的哨兵导航栈（Apache-2.0 许可证）就是这么干的：一个布尔参数 `slam`，为真时禁用重定位节点、直接发一个静态的 `map → odom` 变换，并在退出时自动把建好的点云存成 `.pcd`；为假时加载指定的先验点云，交给配准节点在线求解 `map → odom`。同一套 launch，改一个字，切换整条定位路线——赛前建图和赛时导航共用一份代码，是很值得学的工程结构。

这套代码里还留了一条极其诚实的记录，比任何教程都值钱。它的 README 在"下载先验点云"那一步下面挂了一句备注：*当前带先验点云的里程计在大场景下效果并不好，比不带先验点云更容易飘*（这是该项目的*自述观察，本书未做复现*，下面对成因的解释也只是推断）。这句话反直觉到值得停下来想一想——加了先验信息，结果反而更差？可能的机理是：配准把当前扫描往先验点云上"拽"，一旦先验点云与实际场地有系统性偏差（比如建图时本身就有累积漂移，或者场地被改动过），这个"拽"就成了一个持续注入的错误约束，反倒污染了本来自洽的里程计。*先验的价值取决于先验的正确性；一份有偏的先验比没有先验更坏*——这条在卡尔曼滤波里叫做"错误的 $bold(Q)$、$bold(R)$ 比保守的更危险"，「卡尔曼滤波」一章讲调参时说过同一件事，这里是它在定位上的一次现身。

定位这件事为什么归到"视觉"这本书里讲？因为它直接决定视觉输出的可用性。哨兵识别到一台敌方步兵，PnP 给的是相机系坐标，一路变换到 `odom` 系没问题（「坐标变换」一章）；但你要把这个坐标*发给队友*、或者拿去和雷达发来的全局坐标做关联，就必须再变换到*场地系*——而 `map → odom` 这一段正是定位给的。定位飘 1 米，你报给队友的敌人位置就偏 1 米，不管你的 PnP 多准。上一章算过雷达标记的判据是平面距离小于 0.8 米，同一把尺子量过来：*一米量级的定位漂移，足以让你所有对外发布的坐标全部失效*。

=== 自身运动补偿：小陀螺把导航栈搅乱了

现在哨兵能定位、能规划了，把云台装上去，麻烦就来了。

问题的根子在于*哨兵的云台和底盘是解耦的*。云台要么在扫描搜索（绕一圈找人），要么在跟踪目标（跟着敌人转），总之它的朝向由视觉说了算，跟底盘想往哪走毫无关系。而 Nav2 这类导航框架有一个叫 `robot_base_frame` 的参数，规划器和控制器输出的速度指令 `cmd_vel`，就是表达在这个坐标系里的——它默认假设这个系"钉在车上、朝向就是车头方向"。

真实配置里这个坑长什么样？前面那套开源哨兵导航栈的参数文件里，重定位与点云处理这两个节点用的 `robot_base_frame` 是 `gimbal_yaw`——*云台的 yaw 系*。为什么不用底盘系？先排掉一个想当然的答案：*不是因为传感器装在云台上*。那台车的 mid360 恰恰是倾斜侧放在*底盘*上的，里程计也先落在底盘系 `base_footprint`；点云处理节点在发出 `odom → base_footprint` 这份 TF 之外，还额外发了一份 child 为 `gimbal_yaw` 的 odometry，专门喂给下游。真正的约束在管线的另一头：*这条链最终吐出去的速度指令 `cmd_vel`，其参考系被定义为云台 yaw 系*。原因也不难想——底盘正在小陀螺（参数文件里给的固定自旋速度是 $3.14$ rad/s，每秒半圈），它自己的朝向根本没资格当速度指令的方向基准；云台朝向才是这台车上唯一还算稳定的参照。于是 `robot_base_frame` 只能取云台系，导航栈的"车头方向"也就被绑到了一个每秒转好几弧度的东西上。

在往下推之前，必须先拆掉一颗埋在这一节正中央的雷。上面这段话里出现了 `odom` 这个词，而它在哨兵这台车上*同时有两个互不相干的含义*：

#note("同名不同义：`odom` 在自瞄栈和导航栈里不是一个东西")[
自瞄那一侧，串口节点收到下位机的欧拉角之后，广播一份 `odom → gimbal_link` 的 TF——但它*只写 rotation，不写 translation*（`rm_serial_driver.cpp` 里那段构造 `TransformStamped` 的代码只设了 `t.transform.rotation`，平移分量保持默认的全零，Apache-2.0 许可证）。也就是说，rm_vision 系的 `odom` 是一个*原点永远与云台重合、只有姿态没有位置*的参考系：俯仰与横滚由 IMU 的重力方向定住，偏航的零点则是*上电那一刻云台碰巧朝的方向*。它回答的问题是"云台此刻相对开机姿态转了多少"，完全不回答"车在场地的哪里"。「项目分析」篇「rm_vision」一章会从框架那一侧再讲一遍这条链，并说明这个偏航零点的随意性还会怎样影响量测噪声的建模。

导航那一侧，`odom` 是 LIO（激光惯性里程计）的输出，是一个*会平移的、连续但会漂的世界系*，`map → odom` 那一段由重定位补上。它回答的问题恰恰是"车在哪"。

两个栈把同一个字符串写进了 `frame_id`。tf2 里坐标系*就是*字符串——同名即同一个节点，它没有任何办法知道你指的是哪个 `odom`。所以两个栈一旦合进同一个 TF 树，`gimbal_link` 就被挂到了导航那个*会平移的* `odom` 底下：姿态还是对的，但整个云台连同它下面的相机系，位置被钉在了导航坐标原点（开机点）上，不会跟着车走。你的自瞄照样能打——因为火控只用相对姿态；可你一旦想把目标位置变换到 `map` 系报给队友或做全局决策，拿到的就是"以开机点为原点算出来的敌人位置"，*一个看起来完全合理、量纲也对、只是错了整整一个车身位移的坐标*。这类错误不会崩溃、不会报警，只会让你的全局态势图一直是歪的。对策只有一个，而且必须在合并工作空间的第一天就做：*给其中一个改名*（`gimbal_odom` 之类），别指望以后靠调试发现。

一般规律：*坐标系名是全局命名空间里的裸字符串，两个独立开发的栈撞名是迟早的事*。「坐标变换」一章讲 TF 树时说过"坐标系的名字就是它的身份"，这里是那句话的账单。
]

后果可以严格推出来。先设想一台*把 `cmd_vel` 当底盘系执行*的车——这是很常见的一类下位机约定，也是理解这件事最干净的模型（下面会说清楚，前面那台开源哨兵*不是*这样的车）。设控制器在时刻 $t$ 算出一个世界系下指向目标的单位方向 $hat(bold(d))$，它按约定把这个方向表达进 base 系再发出去，也就是发出 $bold(R)(psi(t))^(-1) hat(bold(d))$，其中 $psi(t)$ 是 base 系（这里就是云台 yaw）在世界系里的偏航角。可底盘拿到这三个数是按*底盘自己的朝向*去执行的，而底盘的偏航角与 $psi$ 无关。于是实际走出来的世界系方向不是 $hat(bold(d))$，而是

$ hat(bold(d))_"actual" = bold(R)(-psi(t)) hat(bold(d)) $

*实际行进方向与期望方向之间，差了整整一个云台偏航角。* 云台扫描一圈，这个误差就扫过 $360 degree$；对时间取平均，朝向目标的那个前进分量被抵消掉绝大部分——车绕着圈打转，哪也去不了。这个模型值得你自己推一遍，因为"*参考系转了，同一份 `cmd_vel` 的物理含义就变了*"是一条通用的教训。作者对症状的描述也是这个味道："当云台处于自旋扫描时，局部路径规划器会将机器人的方向视为与当前路径规划方向一致，导致机器人无法正常运动。"（这句是该项目 README 的自述，本书未做复现。）

#figure(
  image("images/adv-sentry-spinnav.png", width: 100%),
  caption: [云台自旋如何把导航指令整个拧歪（脚本 `adv_sentry_spinnav.py`，*仿真推导值、非实测*：控制周期 50 ms、速度 1.5 m/s、云台自旋 2 rad/s，每周期重新朝目标点规划一次）。⚠ 这里仿真的是上面那台*把 `cmd_vel` 当底盘系执行*的假想车，不是前面那台开源哨兵——后者的失效方式见下一节。读图规则：左图看轨迹——灰色虚线是期望的直线，粗蓝是补偿后的实际轨迹（与期望重合、3.3 s 到达），品红是*不补偿*时的实际轨迹，因为每一步的行进方向都被云台偏航角旋转掉，它绕成了几个圈。右图看"离目标还有多远"随时间变化：蓝线单调下降到零，品红线在 $4.2 tilde.op 5.7$ m 之间来回震荡，*离目标最近时也还有 4.2 m，6 秒结束时反而比出发时远了 0.4 m*——它沿路径跑了 9 m，相对出发点的净位移只有 0.9 m。这不是控制器没调好，是坐标系用错了——*同一份 `cmd_vel`，解释它的参考系转了，指令的物理含义就变了*。],
)

对策朴素得让人意外：*既然导航栈要一个不转的参考系，那就造一个给它*。上述开源实现里有一个只有一百多行的功能包 `fake_vel_transform`，做的就是这件事——它凭空造出一个虚拟坐标系 `gimbal_yaw_fake`，原点与真实的 `gimbal_yaw` 重合，但 yaw 恒定朝向世界系的正前方，然后把这个变换发到 tf2：

```cpp
// 发布 robot_base_frame → fake_robot_base_frame：只有一个绕 z 的反向旋转
tf2::Quaternion q;
q.setRPY(0, 0, -current_robot_base_angle_);   // 抵消掉当前云台偏航角
t.transform.rotation = tf2::toMsg(q);
```

然后把整个 Nav2 栈的 `robot_base_frame` 全部改成这个虚拟系——参数文件里 `bt_navigator`、局部与全局代价地图、`behavior_server` 用的都是 `gimbal_yaw_fake`，而里程计、重定位、传感器处理这一层仍然用真实的 `gimbal_yaw`。导航栈输出的 `cmd_vel` 于是活在虚拟系里，再由这个节点转回真实系发给底盘：

```cpp
geometry_msgs::msg::Twist aft_tf_vel;
aft_tf_vel.angular.z = twist->angular.z + spin_speed_;                       // 叠加固定自旋
aft_tf_vel.linear.x  =  twist->linear.x * cos(yaw_diff) + twist->linear.y * sin(yaw_diff);
aft_tf_vel.linear.y  = -twist->linear.x * sin(yaw_diff) + twist->linear.y * cos(yaw_diff);
```

（以上两段引自 SMBU 北极熊战队开源的 `pb2025_sentry_nav`，Apache-2.0 许可证。）

三行代码，两件事。第一行的 `+ spin_speed_` 是把"底盘小陀螺"这个独立的角速度指令直接叠加到导航给的角速度上——*小陀螺与导航从此互不干涉*，导航只管平移，自旋是另一路话题喂进来的常数。后两行就是一个平面旋转矩阵 $bold(R)(-psi)$，把虚拟系的速度搬回真实系。整套方案的架构价值可以概括成一句话：*感知与定位活在真实系，规划与控制活在虚拟系，中间夹一个速度变换节点做翻译*。这是一个非常干净的解耦——你不必去改 Nav2 一行源码，只需要在它和硬件之间插一层。

但这里有一个反直觉的地方，不点破你会把这个包的作用理解错。虚拟系的 yaw 在世界系里恒为零（它就是把真实云台角 $psi$ 反向抵消出来的），所以 Nav2 在这个系下算出的速度*就是*世界系速度 $bold(v)$；节点再乘上 $bold(R)(-psi)$ 发出去，线上那三个数是 $bold(R)(-psi) bold(v)$。而假如不装这个包、直接让 Nav2 拿真实的 `gimbal_yaw` 当 base，控制器按同样的约定发出的*也是* $bold(R)(-psi) bold(v)$。两者逐位相同——*一次坐标系改名加一次反向补偿，对最终发给底盘的数值来说是恒等变换*。

所以这个包买到的东西不在线上，而在管线中间：改名之后，*上游每一级节点都改在一个不转的系里做计算*。最容易验证的是速度平滑器——launch 文件把它夹在控制器与本节点之间（控制器发 `cmd_vel_controller`，平滑器吃进去、吐出 `cmd_vel_nav2_result`，才轮到本节点），它按 20 Hz 开环限幅，加速度上限 $4.5$ m/s²。一个世界系恒定的速度，从一个以 $omega$ 旋转的系里看是一个转着的矢量，变化率是 $|bold(v)| omega$：按参数文件里 $2.5$ m/s 的速度上限，配一个 $2$ rad/s 量级的云台扫描速度算（*速度上限取自参数文件，这个扫描速度是假设值，与后文那两个自旋参数不是同一个量*），$2.5 times 2 = 5.0$ m/s² 已经顶穿了 $4.5$，平滑器会把指令削幅并拖出相位滞后；换成虚拟系，进平滑器的指令近乎恒定，那个旋转是在平滑器*之后*才补上的。（低速时不触顶，所以这是一条随速度增大而加重的失效，不是全域失效；控制器内部的积分状态大概率有同样的毛病，但该控制器源码不在本书参考集内，*未经验证*。）

一般规律值得记住：*在一条管线中间插入"改名 + 反向补偿"，管线的输出不变，变的是中间每一级所处的参考系*。收益在哪，要顺着管线一级一级找，不能停在最后那根线上。

==== 一个消息类型缺时间戳，怎么把人逼到墙角

这个包里还藏着一个极好的工程故事，正好接住「时间戳对齐」一章的主线。

要把 `cmd_vel` 从虚拟系转回真实系，你需要"发出这条速度指令的那一刻"的云台偏航角 $psi$。也就是说，你必须把 `cmd_vel` 和 `odom`（云台姿态的来源）*按时间戳对齐*——这是「时间戳对齐」一章从头到尾在讲的事。ROS 2 也确实给了现成工具：`message_filters` 的 `ExactTime` / `ApproximateTime` 同步策略。

问题是：ROS 2 Humble 版本的 Nav2 控制器发出的 `cmd_vel` 是 `geometry_msgs/Twist` 类型，而这个类型*没有 header，也就没有时间戳*。（后续版本才改用 `TwistStamped`，社区为此讨论了很久，见 navigation2 的 issue #1594。）没有时间戳，同步器就无从下手。

作者的应对是一记漂亮的侧手翻：控制器在发 `cmd_vel` 的同时还会发一条 `local_plan`（局部路径），而 `nav_msgs/Path` *是有时间戳的*。既然两者出自同一次控制器计算，就把 `local_plan` 的时间戳*当作* `cmd_vel` 的时间戳，拿它去和 `odom` 做 `ApproximateTime` 同步——`cmd_vel` 本身只是被缓存下来，等同步回调触发时再取出来用。

```cpp
// In Navigation2 Humble release, the velocity is published by the controller without timestamped.
// We consider the velocity is published at the same time as local_plan.
sync_ = std::make_unique<message_filters::Synchronizer<SyncPolicy>>(
  SyncPolicy(100), odom_sub_filter_, local_plan_sub_filter_);
```

这段代码值得你盯着看一会儿。它不优雅，作者自己在注释里也写得很坦白（"we consider…"，我们*认为*它们是同时发出的）——这是一个假设，不是一个事实，一旦控制器的实现改了发布顺序，这个假设就会静默地失效。但在给定约束下，它是能想到的最好的办法。

要带走的教训不是"Humble 的 Nav2 有个缺陷"，而是更普遍的一条：*一个消息类型上有没有时间戳，不是可有可无的细节，它决定了下游能不能做时序融合*。「时间戳对齐」一章反复强调"时间戳要在数据产生的瞬间打上"，这里是同一条原则的反面教材——上游没打，下游就只能去别处偷一个，并为这次偷窃承担一个说不清何时会破的假设。你自己设计队内通信协议时（「通信协议设定」一章），每一个会参与融合的消息都留一个时间戳字段，成本是四个字节，省下的是这种被逼到墙角的时刻。

==== 一题五解：五个队伍撞上同一道数学题

把视野放宽一点，这道题不是某一家的私事。翻开几份公开的哨兵导航仓库，只要底盘会自旋、而规划器又要一个不转的参考系，写出来的东西必然长得像同一个东西——*核心全是那个平面旋转 $bold(R)(-psi) bold(v)$*，差别只在两处：$psi$ 从哪儿取，以及*这个 $psi$ 有多新鲜*。

#figure(
table(
  columns: (auto, auto, auto),
  align: (left, left, left),
  stroke: (x, y) => (
    top: if y == 0 or y == 1 { 1pt } else { 0pt },
    bottom: if y == 5 { 1pt } else { 0pt },
  ),
  inset: 7pt,
  table.header([*做法*], [*怎么造出那个"不转的系"*], [*$psi$ 的时间一致性*]),
  [造子帧 + 输出端转回],
  [在真实云台系下挂一个 yaw 恒为零的虚拟子帧，规划器用虚拟系，速度出口乘 $bold(R)(-psi)$],
  [用 `ApproximateTime` 把姿态与控制器输出对齐后再取 $psi$ ✅],

  [把 TF 的旋转直接摁成单位阵],
  [发 `odom` 到底盘系的 TF 时，平移照抄里程计、*旋转硬写成单位四元数*，于是这个系只平移不旋转],
  [TF 的时间戳用发布瞬间的 `now()` 重打，而非里程计消息自带的采样时刻 ⚠],

  [只在输出端做一次旋转],
  [不造帧，收到 `cmd_vel` 后查一次底盘系与云台 yaw 系的相对角，就地转换],
  [查询用 `TimePointZero`，拿到的是*缓冲区里最新的一帧*，不是指令对应的那一刻 ⚠],

  [对齐局部路径而非世界],
  [取局部路径上约四分之一处那个位姿的朝向，减去底盘朝向当作待补偿角],
  [路径与 TF 各来各的，无同步；且路径本身有规划延迟 ⚠],

  [根本不造帧],
  [MPC 直接在 `map` 系里优化轨迹，只在把结果写进 `cmd_vel` 前用同一拍的状态量转一次],
  [$psi$ 与被转换的速度出自同一次求解，天然自洽 ✅],
),
caption: [同一道题的五种解法（做法与时间一致性均*据各仓库源码核对*，行为未复现）。读表规则：第二列的花样很多，但它们数学上*完全等价*——都是 $bold(R)(-psi) bold(v)$；真正把五家分成两档的是第三列。前两列的差异是口味，第三列的差异是精度。最后一行值得单独看：它*没有解这道题，而是让这道题不成立*——不引入第二个参考系，就不存在两个参考系之间的时间错配。],
)

第三列的两个 ⚠ 不是吹毛求疵，是可以算出数来的。控制周期取 SMBU 那份参数文件里的 `controller_frequency` $= 20.0$ Hz，即 $Delta t = 50$ ms。还需要一个量：*速度参考系自己转多快*。这里要当心一个容易张冠李戴的地方——该功能包的参数 `init_spin_speed` 与行为树里的 `PublishSpinSpeed`，按其 README 的原话都是"控制*底盘*固定旋转速度"，会被叠加到输出的角速度上；而我们要的是 `robot_base_frame`（默认就是 `gimbal_link`）*本身*的偏航角速率——README 写明这个参考系"变化剧烈"的场景是"*云台处于自旋扫描时*"。两者是两个量，不能互相代入。下面取一个量级相当的扫描速率 $omega = 3$ rad/s 作*假设值*（非参数文件读数），并假设你用的 $psi$ 陈旧了整整一个控制周期。

关键在于*陈旧的 yaw 会怎样体现在车的实际运动上*。节点发出去的是 $bold(R)(-psi') bold(v)$（$psi'$ 是那个过期的角），底盘拿到之后按*自己当前*的朝向 $psi$ 执行，等效于再乘一个 $bold(R)(psi)$。两者一合：

$ bold(v)_"actual" = bold(R)(psi) bold(R)(-psi') bold(v) = bold(R)(delta) bold(v), quad delta = psi - psi' = omega Delta t $

*车实际走出来的速度，就是你想要的速度整个转过一个角 $delta$，而 $delta$ 恰好等于"yaw 有多陈旧"乘以"云台转多快"*。中间那些坐标系怎么起名、谁转谁，全被约掉了——只剩下这一个角。代入数字（以下为*按公开参数推导，非实测*）：

$ delta = 3 times 0.05 = 0.15 "rad" approx 8.6 degree $

按速度平滑器给的上限 $|bold(v)| = 2.5$ m/s 算，误差矢量的模是 $2 |bold(v)| sin(delta \/ 2) = 0.37$ m/s，占指令的 $15%$。更要命的是*方向*：把误差拆到平行与垂直于期望速度的两个方向上，平行分量是 $|bold(v)|(cos delta - 1) = -0.03$ m/s，垂直分量是 $|bold(v)| sin delta = 0.37$ m/s——*误差几乎整个垂直于你想去的方向*。这不是"走慢了一点"，这是"一直在往旁边溜"。而且这笔账对扫描速率近乎线性：小角度下 $delta$ 与 $omega$ 成正比、误差又与 $delta$ 近似成正比，所以扫描速率翻一倍（$6$ rad/s），误差就涨到 $0.75$ m/s、$30%$。*扫得越快，越需要对齐时间戳*——而"扫得快"恰恰是搜索目标时你最想要的。

#figure(
  image("images/adv-sentry-yaw-staleness.png", width: 100%),
  caption: [陈旧的 yaw 如何变成横向速度误差（脚本 `adv_sentry_yaw_staleness.py`，*推导值、非实测*：速度上限 $2.5$ m/s 与控制周期 $50$ ms 取自公开参数文件，两档扫描速率 $3$ 与 $6$ rad/s 是*假设值*——它是速度参考系自身的偏航角速率，与参数文件里那个*底盘*自旋速度不是同一个量）。读图规则：左图是世界系里的两个矢量——粗蓝是你想要的速度，品红是实际走出来的，两者只差一个旋转 $delta = omega Delta t$；灰箭头是误差矢量，它*几乎与蓝箭头垂直*（垂直分量 $0.374$、平行分量仅 $-0.028$），所以这类误差表现为"莫名其妙的侧滑"而不是"跑慢了"。右图看误差怎么随 yaw 的陈旧程度长起来：横轴是 yaw 落后了多少毫秒，灰色竖虚线标出一个控制周期（$50$ ms），两条曲线在这里分别读到 $0.37$ 与 $0.75$ m/s。*这张图里没有一个量与控制器调得好不好有关*——把 PID 调到天上去也不改变任何一条曲线，因为误差来自参考系的时间错配，不来自控制律。],
)

所以「时间戳对齐」一章那句"要拿数据产生那一刻的时间戳去查变换"，在这里有了一个具体的价钱：*随手写 `lookupTransform(..., TimePointZero)` 的代价，是把指令速度的 $16%$ 泼向侧面*。这也是上一节那个"借 `local_plan` 的时间戳"的丑陋写法真正的动机——作者宁可背一个说不清何时会破的假设，也不肯接受这 $16%$。

顺带一个从同一批代码里挖出来的反面教材（该项目为 GPL-3.0，这里*只复述现象、不摘录源码*）。上表第四行那份实现，做完平面旋转之后又把 $x$、$y$ 两个分量*同时乘上了一个 $0.12$*——一个来路不明的魔数，硬编码在一个名叫"坐标变换"的函数里。坐标变换是纯几何，它的输出模长必须等于输入模长；一旦里面出现了一个不等于 1 的标量，这段代码就同时在做两件事：*转换参考系*和*标定增益*。后果是你以后调速度上限时会发现怎么调都不对，因为真正的限幅藏在一个名字叫"变换"的函数里。同一处还有个更细的东西：$y$ 分量除了旋转之外还被多取了一次反，而且是用一对嵌套的负号写出来的——它可能是在修正底盘的 $y$ 轴方向约定，也可能是在补一个符号错误，*光看这份源码分不出来*（未验证）。这两件事合起来正好是一条经验：*让每个函数只有一个物理职责*。变换就只做变换，增益标定放到它该在的地方去，否则将来没人（包括你自己）说得清那个 $0.12$ 是什么。

==== 那到底要不要做云台底盘解耦

看完五种解法，一个自然的问题是：这套东西是不是每台哨兵都得来一遍？

答案写在那些仓库的参数文件里，而且分得很干净：横着扫一遍公开的导航参数，`robot_base_frame` 填 `base_link` 的有，填云台系或某个虚拟系的也有。*两种都在真实赛季里跑过*。

判据不是水平高低，是一个条件判断：*你的底盘会不会自旋*。

不自旋的车，底盘朝向就是一个变化缓慢、由导航自己控制的量，`base_link` 天然就是那个"不转的参考系"，整套解耦纯属自找麻烦。会自旋的车，底盘朝向以每秒好几弧度的速度在变，而且*那个变化不受导航控制*——它是决策层为了防弹另开的一路指令。这时候不解耦，规划器就会在一个疯转的系里做优化，前面推的那个 $bold(R)(delta)$ 会全程挂在你的速度指令上。

所以这一节真正该带走的不是"要造虚拟系"，而是先回答一个更前面的问题：*你的车上，有没有一个方向是导航说了不算的？* 有，就必须解耦；没有，就别引入第二个参考系——每多一个坐标系，你就多欠一笔时间对齐的债，上面那张图已经把利率标出来了。

==== 仿真里调好的参数，实车上要全部推倒

还有一件事在仿真里学不到，但一份 MIT 许可证的公开工程把它量化得非常干净：同一支队伍的两份 Nav2 参数文件，一份用于仿真、一份用于实车，`diff` 出来*只有一个块不同，就是速度与加速度限幅*，其余一字不差。

#figure(
table(
  columns: (auto, auto, auto, auto),
  align: (left, center, center, center),
  stroke: (x, y) => (
    top: if y == 0 or y == 1 { 1pt } else { 0pt },
    bottom: if y == 5 { 1pt } else { 0pt },
  ),
  inset: 7pt,
  table.header([*参数*], [*仿真*], [*实车*], [*倍数*]),
  [`max_vel_x`（m/s）], [$2.5$], [$0.5$], [$5 times$],
  [`max_speed_xy`（m/s）], [$8.0$], [$0.6$], [$13 times$],
  [`max_vel_theta`（rad/s）], [$12.0$], [$5.0$], [$2.4 times$],
  [`acc_lim_x`（m/s²）], [$5.0$], [$2.0$], [$2.5 times$],
  [`acc_lim_theta`（rad/s²）], [$15.0$], [$0.5$], [$30 times$],
),
caption: [同一套代码的仿真参数与实车参数（数值*引自 CodeAlanqian 开源的 SCNU-RM-Sentry，MIT 许可证*，本书未复现其运行效果）。读表规则：横着看每一行都是"仿真敢给、实车不敢给"，但*竖着看那一列倍数才是重点*——线速度只砍到五分之一到十三分之一，角加速度却砍了 $30$ 倍。差距最大的恰恰是角加速度，因为仿真里*没有电机扭矩上限、没有轮胎打滑、没有裁判系统的底盘功率限制*，而这三样东西全都最先卡在"想让车瞬间转起来"这件事上。],
)

$30$ 倍这个数值得记住，它是"sim-to-real gap"最便宜的一个量化例证。你在 Gazebo 里调出来的那套漂亮的局部规划参数，*上车之后第一件事就是把加速度限幅砍一到两个数量级*，然后重新调整个控制器——因为限幅一变，控制器的整定就全变了。顺带一个可以自己去数的小细节：这份实车参数里，角加速度上限是 $0.5$、角减速度上限却是 $5.0$，差了十倍。但*先别把它读成"急停优先"*——同一个参数块里紧挨着的几行，线速度轴的不对称是*反过来的*：`acc_lim_x` / `acc_lim_y` 给到 $2.0$，`decel_lim_x` / `decel_lim_y` 只给 $0.5$。两条轴的不对称方向相反，就说明它不是一条统一的安全原则，而是分轴各自整定的结果——作为对照，同一支队伍的仿真参数里六个限幅两两对称（$5.0$ 对 $-5.0$、$15.0$ 对 $-15.0$），这份不对称是上车之后才出现的。

这里还藏着一个很容易犯的误读，值得顺手纠正：DWB 里的 `acc_lim` 与 `decel_lim` *并不是"油门"和"刹车"*，而是带符号速度轴上的*上升率*与*下降率*。源码里的判据（`projectVelocity()`，Nav2 `dwb_plugins`，BSD-3-Clause）只看 `v0 < target` 是否成立：往轴的正方向走就用 `acc_lim`，往负方向走就用 `decel_lim`，与车在物理上"正在加速还是正在减速"无关。所以 `decel_lim_theta = -5.0` 只在 $omega > 0$ 那半边才等于"停得快"；车往另一个方向自旋时，这个 $5.0$ 限的恰恰是*起旋*，而 $0.5$ 反倒成了刹车限幅。一条真正的"急停优先"策略要约束的是 $|omega|$ 的变化率，而这对参数在结构上就表达不了它。

所以这段能安全带走的只有一条，而它也确实值得带走：*加速与减速本来就是两个独立的约束*，Nav2 把它们拆成两个参数不是啰嗦。

=== 打谁：优先级从视野内升到全场

「自瞄算法设计」一章讲过视野里同时出现几台车时怎么选——离准星近、兵种优先级高、切换加滞回。那是*一台步兵在自己视野内*的选择。哨兵的题目不一样：它握着全场态势（自己的 + 雷达喂来的），要在*战术层面*决定去打谁、甚至要不要打。

一支队伍开源的决策库（Apache-2.0 许可证）给出了一个很有代表性的打分函数。它对每一个已知的敌方目标算两项权重再相加：

$ s_i = (1 - d_i / d_max) dot w_d + (1 - h_i / h_max) dot w_h $

$d_i$ 是自己到该目标的平面距离，$h_i$ 是该目标的剩余血量，$w_d$、$w_h$ 是两项的权重比。语义很直白：*越近越该打*（切换代价小、飞行时间短、命中率高），*越残血越该打*（补刀比起手划算，能立刻减少场上的敌方输出）。查不到位置或血量的目标兜底给 $-0.5$，等于把"我不知道它在哪"惩罚成负分，自然排到最后。

这个式子里有一处细节值得点破，因为它决定了这个分数的*性质*。$d_max$ 和 $h_max$ 不是常量，而是*当前这一批已知目标里的最大值*——代码是先遍历一遍求最大，再拿它去归一化的。这意味着 $s_i$ 是一个*相对分*，不是绝对分：场上少了一台车、或者最远那台走出雷达视野，$d_max$ 一变，*所有目标的分数会集体跳变*。工程上的后果是，你不能拿这个分数去和一个固定阈值比较（"分数高于 0.7 才追击"这种写法会在目标集合变化时随机触发），只能用它做*排序*。这是相对归一化与绝对归一化的差别，很容易在代码评审里被放过去。

还有两处细节，合起来能说明这类打分函数在系统里到底扮演什么角色。第一，$w_d$ 与 $w_h$ 在函数签名里是*两个参数*，看起来是留给调参的——但全仓库唯一的那个调用点，把它们写成了两个字面量 $0.5$。*接口是可配置的，实际是写死的*，这是很常见的一种"半成品可配置性"，读代码时要分清"能配"和"配了"。第二，这个函数*不取 argmax*：它把整个分数向量原样打包成一条消息发给下游，让视觉层自己去用。也就是说，决策层在这里给的不是"打 3 号"，而是"这几台车的优先级大致是这样"——*把最终选择权留在离观测最近的那一层*。这个分工其实很讲究：决策层握着全场态势但精度粗、更新慢，视觉层只看得见眼前但每帧都新；让粗的那层排优先级、细的那层做取舍，比让决策层越俎代庖直接指定目标要稳健得多。

顺带一课，关于怎么读开源代码。这个函数的末尾有一行 `result[5] = 0`（把某个固定索引的目标分数清零），而它上面那行 `if (!enemyOutpostDown)` 被注释掉了——也就是说，原本设计成"敌方前哨站没倒时才屏蔽"的条件判断，现在变成了*无条件清零*。这大概率是调试时随手注释掉忘了恢复的遗留代码，不是设计意图。类似的痕迹在赛季代码里到处都是：另一支队伍的哨兵决策库里，有一处 `if (!on_slope)` 分支内部又嵌了一个 `if (on_slope && ...)`，内层条件永远为假、整段是死代码。*读开源项目时，被注释掉的条件、永远走不到的分支、和代码打架的注释，都在告诉你这段逻辑最后一次被认真思考是什么时候*——把它们当设计意图去理解，只会把自己绕进去。

==== 留给你的设计题：一个好的打分函数该考虑哪些维度

上面这份是我们读到的公开实现里最完整的一个，而它也只有距离和血量两项。这里要说一句方法论上的话，免得你把结论推歪：*打分权重恰恰是竞技策略里最不会被开源的那部分*——赛季结束才放出来的仓库，通常只保留了跑得通的骨架，真正调过的那套权重未必在里面。所以从"公开代码里看到的都很朴素"推不出"大家都做得很朴素"，我们能说的只是"能读到的这几份是这样"。

有意思的是把它当一道设计题来做。假设你要给自己的哨兵写这个函数，值得进入打分的维度至少有这么几类，每一类背后都是一个不同的问题：

- *兵种价值*。打掉英雄和打掉工程的收益差着量级。这一项回答"打死它值多少"。
- *剩余血量*。补刀比起手划算——但要小心，这一项和上一项会打架：一台满血英雄和一台残血工程，谁优先？
- *距离*。近的目标飞行时间短、命中率高、切换代价小。这一项回答的其实是"我打得中的概率有多大"，而不是"该不该打"。
- *它是不是正在打我*。裁判系统的受击装甲板 ID 和血量变化都能提供这个信号。这一项回答"不打它我会损失多少"——前面那几项都是收益侧，这是*代价侧*，缺了它的打分函数是残缺的。
- *我打不打得到*。目标在掩体后面、或者在我云台的俯仰范围之外，分数再高也没有意义。这一项本质是一个*可行性掩码*，应该乘在最前面而不是加在总分里——不可行就该是零，不是"减一点分"。
- *打死它的边际收益*。一台已经被队友锁定的车，你再去打它，收益是重复的；这一项要的是全队层面的分工，而不是每台车各自贪心。

真正的难点不在列维度，在*量纲*：兵种价值是个离散标签，血量是 $0$ 到 $400$，距离是米，"正在打我"是布尔量。把它们加到一起之前，你必须先把每一项映射到一个共同的尺度上，而这个映射本身就是策略——它决定了"近一米"值多少"血量少 $50$"。所以这个函数不该是拍脑袋写出来的加权和，*它应该是从一个明确的目标函数（比如"最大化单位时间内对敌方战力的削减"）推下来的近似*。你未必推得动，但知道自己在近似什么，比不知道强很多。

=== 雷达喂过来的那份情报，该怎么用

本章开头说过，雷达与哨兵的接收侧被规则互相钉死：哨兵只能收雷达，雷达只能收哨兵——这是全队唯一一条被规则*强制成对*的链路，不用白不用。但"能收到"离"能用上"还差着几道工序，值得拆开。

*第一道坎是坐标系。* 雷达报出来的是敌方机器人在*场地系*里的坐标（它标定的时候就是往场地或场地点云上配的，上一章讲过四条标定路线）。而你的哨兵活在自己的 `map` 系里——如果走的是离线预建图那条路，这个 `map` 系是建图时的产物；如果走的是上场纯里程计那条路，原点干脆就是开机那一刻车停的位置。两者之间差一个刚体变换，而这个变换*没有人会自动给你*。所以整条情报链的第一个工程动作是：把雷达的场地系和哨兵的地图系对齐。离线预建图那条路在这里天然占优——如果你的点云本来就是照着场地建的，把 `map` 原点定义在场地的某个已知点上，这个变换就是一个写死的常量；纯里程计那条路则必须在每局开始时做一次对齐（比如约定哨兵从 Starting Zone 的固定位姿启动，或者上场后用一次配准把 `map` 对到场地模型上）。*"离线预建图 vs 上场 SLAM"这道选择题，在这里第二次收到账单*。

*第二道坎是精度与时延，它决定了这份情报能干什么、不能干什么。* 上一章算过雷达标记的判据：平面距离小于 0.8 m 才算"准确"。也就是说，*雷达情报的精度上限就是米级*，而且它还要走裁判系统的机器人间交互数据链路（「通信协议设定」一章讲过这条链路的形态），带宽与频率都有限制，到你手上时已经不新鲜了。米级精度、百毫秒量级的时延——拿去做导航决策绰绰有余（"敌人从右路来了，去左边高地卡位"这种判断，误差半米无所谓），拿去做火控则完全不可用（PnP 在 5 m 处的横向精度是厘米级，差着两个数量级）。

于是融合规则可以写得非常干净，而且是从精度量级直接推出来的，不需要拍脑袋：

- *雷达情报驱动导航目标*——去哪儿、防哪边、要不要支援、什么时候撤，这一层用雷达的全局视野。
- *自身相机驱动云台*——瞄哪块板、什么时候开火，这一层只信自己的观测，雷达数据一个字都不进跟踪器。
- 两者冲突时（雷达说这里有车、自己的相机什么也没看见），*相信自己的相机做火控、相信雷达做走位*——它们回答的本来就不是同一个问题。

反过来，哨兵发给雷达的东西同样有价值，而且价值来自一个几何事实：雷达架在场地一角的高台上，视线会被中央高地、掩体、梯形高地挡住，*它有确定的盲区*；而哨兵在场地里跑，它的盲区和雷达的盲区不重合。哨兵把自己看到的敌方位置回传，等于给雷达补了一双在场地里游动的眼睛——而雷达的标记进度直接换算成己方全队的双倍易伤机会。*两章要合起来读的理由，就在这里*：一条"我帮你看见、你帮我决策"的闭环，而规则不给两边任何别的选项。

需要提醒一句诚实的边界：以上是从规则约束与精度量级*推导*出来的设计原则，*我们没有在真实赛场上验证过这条链路的端到端效果*。

还要提醒一句关于"证据"的话，因为它比这条链路本身更值钱。你可能会想去开源仓库里翻一翻，看看别人是怎么接这条链路的——这件事该做，但结论要收着下：上一节讲打分权重时说过的那条理由，在这里原样适用，而且值得单独给它一个名字——*证据的缺失不是缺失的证据*。你在开源语料里搜不到某种实现，只能说明*你搜不到*，推不出"没人做"。本书凡是给出统计口径的地方，都会写清楚"我们读到的这几份是这样"，而不写"社区如何如何"——样本不是随机抽的，它是被战术保密筛过的。

规则这边的事实是清楚的：这条通道存在。怎么用它，是留给你的工程题。

=== 决策层：先搞清楚它住在哪个包里

哨兵的决策层要回答的是"此刻该做什么"：去增益点、去支援、回基地、原地防守、还是撤退补给。

在比较写法之前，先解决一个更基础、也更容易想错的问题：*这段逻辑应该放在哪儿？*

如果你用过 Nav2，一个非常自然的猜想是"放进 `bt_navigator` 的行为树里"——它本来就是一棵 BehaviorTree.CPP 的树，本来就在按 tick 跑，把"血量低于阈值就去补给"挂上去看起来天经地义。这个猜想很流行，而且是错的。去公开仓库里核一遍就知道：

- 有一份 Apache-2.0 的哨兵导航栈，全仓十个 `.cpp`/`.hpp` 文件里，`referee`、`game_status`、`decision` 这几个词*一次都没出现*。整个导航栈根本不知道自己在打比赛。
- 两家改过 `bt_navigator` 默认树的仓库，改的都是重试次数、重规划频率、卡死后倒车这类东西；树里出现的节点全部是 Nav2 原生的（`ComputePathToPose`、`FollowPath`、`RecoveryNode`、`RateController`…），*没有一个是比赛决策节点*。
- 而那个把决策写得最细的仓库，名字里明明白白写着"导航"，决策却单独住在一个叫 `rm_decision_cpp` 的包里，和导航是两个进程。

*决策不在 Nav2 的行为树里，但它常常就在同一个"导航仓库"里另起一个包。* 这个区分很值钱，因为它对应着一条清楚的架构边界：`bt_navigator` 那棵树管的是*"怎么把车安全送到一个已经给定的点"*——规划失败怎么办、卡住了怎么退、代价地图脏了怎么清；而决策那棵树管的是*"该去哪个点"*。前者是导航的内部事务，跟裁判系统一个字关系都没有；后者才是比赛。把两者混在一棵树里，你会得到一棵既要处理"路径规划重试三次"又要处理"基地血量低于 2001"的树，两个完全不同时间尺度的东西挤在同一层——这是哨兵决策代码最常见的结构性错误，本章后面「姿态模式：一个带冷却时间的调度问题」一节会用规则算出来的数字把这几个时间尺度分开摆一遍。

例外当然有。有一家把一个 Lua 虚拟机直接嵌进了导航 component 里，决策脚本每十个控制周期 tick 一次，共享同一个进程。这是另一种自洽的选择：它换来的是极低的通信开销和脚本热改的便利，付出的是决策一旦跑飞会拖着导航一起。（以上是对这几个公开仓库的*事实陈述*，不是对社区做法的统计。）

搞清楚住在哪之后，再看怎么写。把公开能读到的实现摊开排一排，你会发现它们不是几种互不相干的"流派"，而是一条从"完全不看战场"到"运行时可插拔"的连续光谱：

#figure(
table(
  columns: (auto, auto, auto, auto),
  align: (left, left, left, left),
  stroke: (x, y) => (
    top: if y == 0 or y == 1 { 1pt } else { 0pt },
    bottom: if y == 7 { 1pt } else { 0pt },
  ),
  inset: 6pt,
  table.header([*范式*], [*载体*], [*改策略的成本*], [*代价*]),
  [时间轴脚本], [一段脚本，按开局后的秒数依次发目标点], [改常数、重跑], [完全不看战场状态，被打断就全乱],
  [随机游走], [到点即在一个矩形里随机重采下一个点], [改采样范围], [谈不上策略，但胜在永远不会卡死],
  [手写行为树], [自研树框架加黑板，节点是 C++ 类], [改 C++ 并重编译工作空间], [全部自己维护，但完全可控],
  [XML 行为树], [BehaviorTree.CPP，逻辑写在 XML，条件用内置脚本节点写成表达式], [改 XML，不重编译；可用 Groot 图形化编辑], [判据散在树里，拼错的键、写坏的字面量编译器一个都看不见],
  [插件化 XML 行为树], [同上，且每个条件与动作节点单独编成一个动态库，运行时按名字加载], [改 XML；新增一种能力只要加一个插件，主程序不动], [前期要搭插件框架与类型约定，投入不小],
  [加权规则表], [权重打分加 JSON 配置], [改 JSON 里的权重与阈值], [没有树结构，复杂时序逻辑难表达],
  [嵌入式脚本状态机], [把一个脚本语言虚拟机嵌进节点，状态机写在脚本里], [改脚本文件], [脚本层不受 C++ 类型系统保护，错了只能在运行时发现],
),
caption: [公开实现里能读到的决策层写法，从上到下大致是一条成熟度光谱。读表规则：第三列"改策略的成本"才是选型的真正依据——*赛场上你只有几分钟改一条策略*，能改配置文件不重编译的方案在那个时刻是压倒性优势；第四列是你为这份便利付的账。上下两端都不是稻草人：*时间轴脚本和随机游走都是真实存在的实现*，把它们放进来是为了让你看清"成熟"到底成熟在哪一维——不是代码更漂亮，是*策略与代码的耦合被解开了几层*。中段那三行的差别也在这一维上：手写树要重编译，XML 树只要改文件，插件化树连新增能力都不用碰主程序。],
)

行为树相对于一棵大 `if-else` 树，真正的收益不在"更优雅"，而在两件很实际的事：*逻辑与代码分离*——策略活在 XML 里，赛场上改一条判据不用重新编译整个工作空间；*可视化*——一棵树能被图形化工具画出来，新人看得懂、赛前也能对着屏幕逐条 review。代价是你得为每个动作和条件写一个 C++ 节点并注册进工厂，前期投入不小。

这里要顺手纠正一个流传很广的误会。BehaviorTree.CPP v4 里那个可以在 XML 属性里直接写表达式的 `ScriptCondition`，*是官方库自带的原生节点*，不是哪支队伍魔改出来的——它就住在上游的 `include/behaviortree_cpp/actions/script_condition.h` 里，版权头写着库作者的名字。所以"用了脚本条件节点"不构成一种独立的范式，它只是 XML 行为树的标准用法之一。（真正被魔改过的地方在后面，而且是个坑。）

==== 主讲样本：十五行的主树

先看一份可以整段摊开的：SMBU 北极熊战队开源的哨兵行为树包（Apache-2.0 许可证）。它的 RMUL 主树只有十五行：

```xml
  <BehaviorTree ID="rmul_2025">
    <KeepRunningUntilFailure>
      <ForceSuccess>
        <ReactiveSequence>
          <SubTree ID="check_game_start"/>
          <PublishSpinSpeed spin_speed="7.0"
                            duration="0.0"
                            topic_name="cmd_spin"/>
          <SubTree ID="check_robot_status"/>
          <PubNav2Goal goal="4.65;-3.5;0"
                       topic_name="goal_pose"/>
        </ReactiveSequence>
      </ForceSuccess>
    </KeepRunningUntilFailure>
  </BehaviorTree>
```

（引自 `pb2025_sentry_behavior` 的 `rmul_2025.xml`，Apache-2.0 许可证。）

值得逐行读一遍，因为它把行为树的组合性用得很干净。`ReactiveSequence` 意味着*每一个 tick 都从头重新检查*所有前置条件：先确认比赛真的开始了，再把小陀螺速度发出去，再确认自己的血量弹量热量都还行，最后才发导航目标点。任何一步失败，后面的都不执行——于是"比赛没开始就不许动""状态不对就先去补给"这两条策略，不是靠 `if` 写出来的，是靠*节点顺序*表达出来的。外面套的 `ForceSuccess` 加 `KeepRunningUntilFailure` 是一个惯用法：把内部的失败吞掉并无限重跑，让这棵树变成一个永不退出的主循环。

两个 `SubTree` 各自也很短。`check_game_start` 是一个 `Fallback`：先问"比赛进行中吗"，是就直接成功；否则走进另一个分支，把自旋速度和平移速度全部发成零，然后*强制返回失败*——也就是说，比赛没开始时它不只是"不动作"，而是主动把车刹停，再让主树的这一 tick 到此为止。`check_robot_status` 同理：状态不 OK 时先看自己是不是已经在补给区，不在就发一个补给点的导航目标。

*这就是行为树相对 `if-else` 的真正卖点*：这四个能力（比赛状态门控、自旋、状态门控、去点）彼此不知道对方存在，是 XML 的结构把它们组装成了一套策略。想把"低血量回防"插进去，你加一个子树，不改任何一个已有节点。

再看它的节点库：条件节点五个（比赛状态、机器人状态、是否被击打、是否发现敌人、是否踩在某个 RFID 增益点上），动作节点七个（发导航目标、发目标并等待结果、发自旋速度、发平移速度、发云台绝对角、发云台角速度、算一个攻击位姿），控制与装饰节点三个，一共十五个。*每一个都编成一个独立的动态库*，运行时按名字从插件目录里加载。这就是上表最后那一档"插件化"的含义：新增一种能力等于新增一个 `.so`，主程序连重新编译都不需要。

==== 全局黑板：那个 `@` 是干什么的

上面 XML 里有一处写法值得单独拎出来，因为它是 BehaviorTree.CPP v4 的一个新特性，很多人不知道它存在，于是绕了很大的远路。

行为树的节点之间靠*黑板*（blackboard）传数据，XML 里写成 `{key}`。问题在于：*每棵子树有自己独立的黑板*。这是刻意的设计——子树要能被复用，就不能假设外面的黑板上有什么。代价是，一个所有人都要读的全局量（比如裁判系统发来的比赛状态），必须靠端口重映射一层一层往下传，树有多深就传多少层，中间任何一层写错一个名字，数据就悄悄断在那儿。

v4 给了一个出口：*键名前面加一个 `@`，就直接指向根黑板*。上面那份实现里，比赛状态的引用写成 `{@referee_gameStatus}`，无论这个节点埋在多深的子树里，读到的都是同一份数据。而喂数据那一侧只有这么一段：

```cpp
template <typename T>
void SentryBehaviorServer::subscribe(
  const std::string & topic, const std::string & bb_key, const rclcpp::QoS & qos)
{
  auto sub = node()->create_subscription<T>(
    topic, qos,
    [this, bb_key](const typename T::SharedPtr msg) { globalBlackboard()->set(bb_key, *msg); });
  subscriptions_.push_back(sub);
}
```

（同上，Apache-2.0 许可证。）

一个模板函数，把"订阅某话题、把收到的消息原样写进根黑板的某个键"这件事抽象掉；构造函数里于是每种输入只占一行：比赛状态、全场机器人血量、事件数据、RFID 状态、自身状态、增益状态、地面机器人位置，再加上自瞄的装甲板与跟踪目标、导航的全局代价地图。*十来行覆盖全部输入*，此后 XML 想用哪个就写哪个 `@` 键。

这个结构值得学的地方在于*它把"数据进入决策层"这件事变成了一张表*。你新增一路输入，不需要动树、不需要动节点、不需要动接口，只加一行订阅。对照一下另一种常见写法——写一个巨大的 `Topics2Blackboard` 节点，把所有输入作为它的输出端口一个个列出来，然后在树的最顶端 tick 它一次——那种写法每加一路输入都要同时改节点的 C++ 代码和每一棵 XML 树的属性列表，还得保证名字对得上。

==== 另一种风格：把时间轴写进树里

再看一棵风格完全不同的树，也是 2025 赛季实际上场的（这个仓库*没有许可证*，所以下面只描述结构，一行源码都不摘——为什么这很重要，本节末尾会讲）。它有 149 行，主干是这样分段的：先是一个补给判据——*自身血量低于 90 或 17 mm 弹丸余量低于 14* 就去补给点，直到*血量高于 399 且弹量高于 99* 才离开（两个阈值都写成"比某个整数小 1"，但那个整数的身份不同：400 是哨兵的血量上限，所以"高于 399"就是回满血才走；100 却不是弹量上限——2026 手册给哨兵的初始弹量就有 300 发且可以继续兑换，100 也不是唯一的补给粒度，而是哨兵最常吃到的那一档：远程兑换一次最少 100 发，占领己方补给区则每分钟定额发 100 发；而手册 5.3.2 节还给了更小的一档——站在补给区、基地或前哨站增益点上直接兑换，17 mm 一次最少 10 发，且哨兵可以经裁判系统串口自己完成。所以"高于 99"更像"等一笔补给到账"，不是"装满"。*一个要的是满，另一个要的只是够用*——抄阈值之前先分清它贴着的整数是上限还是步长，并回当季手册核对：这棵树是 2025 赛季的，本书引的是 2026 手册）；然后是一个基地告急的抢救分支（己方基地血量低于 2001 时无条件回防）；最后是主战逻辑，*以剩余时间 270 秒为界分成前后两套打法*，同一套"发现敌人"的条件在前期和后期挂到不同的动作子树上。这套写法的信息量在于：*它把比赛的时间轴显式编码进了策略*——前期偏向出击占点扩大优势，后期偏向收缩保基地。一棵大 `if-else` 也能写出这个，但你没法在赛前十分钟把它指给队长看。

而这棵树里最漂亮的一笔，是它表达"越到后期越激进"的方式。前后两套打法里各挂着一个同名的*装饰器节点*，叫 `IsInPosition`：它接收四个点组成的四边形，只有当机器人当前位置落在这个四边形内时才 tick 它的子节点。挂在下面的是"导航到刚算出来的攻击位姿"。于是它的作用是一道*地理围栏*——攻击点可以随便算，但只要算出来的位置把车带出围栏，这条指令就不执行。

关键在于前后两套用的围栏不一样：前期那个约 $8.1 times 8.5$ m，后期那个约 $11.8 times 15.6$ m，*后者的面积是前者的 2.7 倍*——四条边里三条向外推（最远的一条推出 6.6 m），只有 $x$ 的下界反而收进了 0.4 m。同一条策略"看见敌人就去打"，在比赛后半段被允许追出更远。

停下来体会一下这个写法的分量。"随着比赛推进逐步放开活动范围"这句战术描述，如果写成 `if-else`，你需要一个时间判断、一套坐标常量、一段边界检查，还得记得在两个分支里都写对；而这里*它只是同一个装饰器的两次实例化，参数不同*。这就是行为树组合性最好的一则广告：*把"什么时候允许"做成一个可以套在任何动作外面的壳，而不是揉进每个动作的内部*。你的树里任何一个动作想加地理约束，套上去就行。这类可复用的约束壳（时间窗、地理围栏、频率限制、冷却）是行为树真正值钱的地方，比"用了行为树"这个事实本身值钱得多。

==== 声明式配置的代价：编译器帮不了你了

把策略从 C++ 挪进 XML，买到的是"改一条判据不用重编译"。这笔交易有代价，而且代价很具体：*你同时把编译器的静态检查也挪出去了*。上面那个无许可证仓库里有两个现成的例子（只描述现象，不摘源码）。

第一个，2024 赛季的五棵树，都把裁判系统报来的 17 mm 弹丸余量写进了一个*拼错的黑板键*——正确的词是 allowance，树里写成了 sllowance。而整棵树里*没有任何一个判据去读这个拼错的键*。也就是说，这一路输入被完整地搬进了黑板，然后躺在那儿谁也不看。BehaviorTree.CPP 不会报错：黑板是一个键值容器，写一个没人读的键完全合法。到了 2025 赛季，键名改对了，弹量也真正进入了补给判据——*这个赛季差，本身就是那条数据一整年没被用上的证据*。如果这是 C++，一个拼错的成员名活不过第一次编译。

第二个更值得警惕，因为它*没有出错*。有一个导航目标点的坐标字符串里，某个分量写成了 `-5.6.0`——两个小数点。你可能以为它会抛异常，但解析走的是 `std::stod`，而 `std::stod` 的语义是*取最长的合法前缀*：它读到 `-5.6`，在第二个小数点处停下，返回 $-5.6$，然后若无其事地继续。而 $-5.6$ 恰好就是作者想要的那个数。

*一个本该被拒绝的输入，被静默接受了，而且碰巧结果是对的。* 这比崩溃危险得多——崩溃会逼你去看，静默通过不会。同一个函数下次吃到 `5.6abc` 照样返回 $5.6$，吃到 `5,6` 返回 $5$。凡是从字符串解析数值的地方，都该检查*解析停在了哪里*（`std::stod` 的第二个参数会告诉你消耗了多少字符），把"还有没读完的字符"当成错误。这条规矩在读配置文件、解析协议、处理用户输入的地方全都成立。

对策不是退回 C++，而是*把判据的类型化做在节点里，让 XML 只填数值*。前面那份主讲样本就是这么干的：比赛状态、机器人状态、RFID 状态各有一个 C++ 条件节点，端口类型是具体的消息类型，XML 里只写阈值。拼错端口名会在树加载时报错，而不是安静地少一路数据。*声明式配置的安全性，取决于你给它的那套节点定义得有多严*。

==== 可复现性陷阱：那棵树跑不了原版的 BehaviorTree.CPP

同一个仓库里还藏着一个坑，而且是那种"照着抄然后彻底卡住"的坑。

它把 BehaviorTree.CPP 整个 vendor 进了自己的工作空间，版本 4.5.1。翻到 `src/controls/reactive_fallback.cpp` 第 18 行，那里有一个静态开关的初值：

```text
仓库里的 vendored 版本：  ReactiveFallback::throw_if_multiple_running = false
上游 4.5.1 同一个文件：   ReactiveFallback::throw_if_multiple_running = true
```

这个开关管的是什么？`ReactiveFallback` 每个 tick 都会从头重新遍历所有子节点，按语义*只允许有一个子节点处于 RUNNING*——如果这一 tick 返回 RUNNING 的子节点和上一次不是同一个，上游会抛一个 `LogicError`。而这个仓库把它默认关掉了。

为什么必须关？因为它那棵树的顶层就是一个 `ReactiveFallback`，下面并列挂着补给、基地告急、前期作战、后期作战好几个分支，每个分支里都有会跨 tick 保持 RUNNING 的东西（等待补给到账的重试循环、`Sleep`）。比赛状态一变，RUNNING 的分支就从一个换成另一个——正好命中那个异常。

于是这条经验就摆在这儿了：*你把这棵 XML 拷到一个 4.5.x 的干净 BehaviorTree.CPP 工程里，它会直接抛异常*——上游自 4.6.0 起把这个默认值也改成了 `false`，所以在更新的版本上反而不抛。这本身就是个提醒：这类"默认值"是会随版本漂移的，判断行为要认准你手上那一版的源码，而不是记忆里的某一版。而你翻遍 XML 也找不到原因，因为原因不在 XML 里，在被 vendor 进去、改过的库源码里——而且不止一行：整个 vendored 树相对上游 4.5.1 只有 `reactive_fallback.cpp` 一个源文件被动过，但这一个文件里有三处改动。

另外两处也值得顺手看一眼，因为它们说明"关掉一个断言"并不是全部。一处是 RUNNING 分支里的 halt 循环：上游遍历*全部*兄弟节点、跳过自己再逐个 `haltChild`，分叉版改成只停排在 RUNNING 之后的那些。效果要说准——`haltChild` 只在子节点为 RUNNING 时才真正调 halt，而排在前面的兄弟这一 tick 不可能是 RUNNING（返回 FAILURE 的直接 `break` 进了下一轮，返回 SKIPPED 的已经被就地复位），所以没有任何清理回调被漏掉；差别只在状态复位：上游顺手把那些 FAILURE 的兄弟复位成 IDLE，分叉版让它们停在 FAILURE。而 BehaviorTree.CPP 的 `_skipIf`、`_while` 这类前置条件*只在节点状态为 IDLE 或 SKIPPED 时才求值*——于是在分叉版里，只要后面有分支正在 RUNNING，前面那些已失败分支上挂的前置条件就悄悄不再被求值了。另一处更小但更损：那条 `LogicError` 的提示文字被注释掉了一半，恰好删掉了"可以用 `EnableException(false)` 关掉"这句指路的话。三处合起来看，结论比"改了个开关"重得多：*即便你照抄到干净的 BT.CPP、再自己把异常关掉，行为仍然和原仓库不一样，而这回没有异常提醒你*。

这件事有两层教训。对读者是：*读开源项目要以代码为准，而"代码"包括它 vendor 进来的第三方库*——README 说"基于 BehaviorTree.CPP"，不等于那是你 `apt install` 装得到的 BehaviorTree.CPP。判断一个 vendored 依赖有没有被改，最省事的办法是把它和上游同版本 `diff` 一遍，几秒钟的事。对写代码的人是：*如果你必须改第三方库才能让自己的设计跑起来，那多半是设计有问题，而不是库有问题*。这里的正解其实是把顶层换成 `Fallback` 或者给分支加互斥前置条件，让"同时只有一个分支活着"成为结构上的事实，而不是靠关掉一个断言来回避。*断言响了，先怀疑自己*。

==== 三条从赛季代码里挖出来的战术细节

最后是三个具体的小设计。它们都不难，但都体现出"有人真的想过这件事"，值得你在写自己的决策层时借去用。

*第一条：死战不退。* 一份哨兵决策脚本里，正常情况下血量低于阈值就撤退；但它在最外层多加了一个分支——当比赛剩余时间少于某个阈值、*且自己正踩在增益点上*时，无条件切进防御状态，撤退分支根本走不到。这条逻辑背后是一个清晰的判断：*增益点的价值随比赛推进单调上升，而"活下来"的价值随剩余时间单调下降*。比赛还剩三分钟时保命是对的，还剩十秒时保命毫无意义——那十秒里占着增益点带来的收益，比一台完好的哨兵在终场哨响后的价值高。顺带一个耐人寻味的细节：这个时间阈值写在配置文件里，同一个仓库的两份配置分别给了 $10$ 秒和 $280$ 秒。七分钟的比赛总时长是 $420$ 秒，所以这两个值差得非常远：一个是"最后十秒拼了"，另一个是"开场两分多钟之后就再也不退"——*同一份代码，两种截然不同的性格*。这不是 bug，这是策略旋钮，而把策略旋钮做成配置项正是本节一直在讲的事。

*第二条：别堵门。* 另一份决策脚本 `rmcs-navigation`（BSD-2-Clause，Copyright 2025 Alliance Algorithm Team）的黑板里有一条规则常量，叫"补给间隔"，值是 $6$ 秒，注释写着：返回补给区只补充对应的秒数，*防止因意外而阻塞其他机器人进出*。

这一行值得多看两眼。它说的是：哨兵去补给区，不是补满就走，而是*只待固定的一小段时间就主动让开*——因为补给区是全队共用的通道，你的哨兵在那儿多赖十秒，可能就是英雄没能及时补上弹。在我们读到的这几份实现里，这是唯一一处*把队友的通行需求写进自己策略*的地方。RoboMaster 是团队赛，而本章前面讲到的那些判据——打分函数、行为树的各种条件节点——清一色是单机视角的：我的血量、我的弹量、我看见的敌人。这不是在说别人做得不好（协同策略恰恰是最不会开源的东西），而是想让你注意到一个真实存在、也很容易被忽略的设计维度：*你的哨兵决策层里，有没有任何一个变量是在描述队友的？* 补给区的占用、火力的重复分配、撤退路线会不会和工程撞车——这些都是单机状态空间里表达不出来的东西。

*第三条：弹量账本。* 裁判系统给的 `projectile_allowance` 不是"你弹仓里还有几发"，而是"你还被允许发几发"——它是一个*配额*，补给会增加它，发射会减少它。可你真正想知道的是弹仓里的实弹数，这两个量在补给之后会分道扬镳。一份决策库（GPL-3.0，只讲思路）的做法是自己记一本账：累计获得过多少配额、当前还剩多少配额，两者相减就是*已经打出去的弹数*；再拿一个假定的弹仓容量减去它，得到真实余量。代码注释里明写了那个容量是"假设"的。

这个思路的价值不在这个具体公式，在于它示范了*怎么从可观测量反推不可观测量*：你要的状态传感器不直接给，但它的*增量*和某个可观测量的增量存在已知关系，那就积分回去。这正是「卡尔曼滤波」一章里"用可观测的量约束不可观测的状态"那套思想的一个最朴素的版本——没有噪声模型、没有协方差，就是一本手工账。当然它也继承了所有开环积分的毛病：中途丢一包数据，账就永远错下去，而且*没有任何东西会告诉你账错了*。要做得更好，就该给这本账找一个偶尔能校准的观测（比如某个能确认弹仓已空的事件），这又回到了同一章讲过的"开环积分必须有观测来收敛"。

==== 一条必须先说的许可证红线

上面那份手写行为树框架的源码头部写着 `Copyright (C) 2019 RoboMaster`，正文是 GNU 通用公共许可证第三版（GPL-3.0）——它是 DJI 当年 RoboRTS 项目的血脉，被一支队伍继承下来继续用。做开源考古的时候看见这行版权头，还挺有意思的。

但它同时是一个必须说清楚的法律问题。*GPL 是有传染性的*：把一段 GPL-3.0 的代码抄进你的战队仓库，整个衍生作品在分发时都被要求以同样的协议开源。对一支"赛季结束才开源"或者"干脆不开源"的队伍，这是实打实的约束——不是道德建议，是许可证条款。本书引用这类项目的片段用于讲解属于合理引用，但*你把它抄进自己的代码库是另一回事*。判断方法很简单：动手抄之前，先把那个仓库的 LICENSE 文件从头到尾读一遍。顺带提醒，判断许可证要看全文不能只看首行——有的项目 LICENSE 首行是版权人姓名或者干脆是空行，正文才是标准模板，只 `grep` 第一行会误判。

还有一个更隐蔽、也更容易让人栽跟头的坑，正好出在本节反复引用的那个无许可证仓库上。它的决策包里 `package.xml` 明明白白写着 `<license>Apache-2.0</license>`——你如果只看这一行，会以为可以放心用。但翻遍*这个包*，没有 LICENSE 正文，也没有一行属于作者本人的版权声明（包里唯一出现的 `Copyright` 还是一份从 Nav2 抄来的启动脚本顶上、属于 Intel 的那行 Apache 头——*别人文件里的版权声明当然授权不了你的包*）。仓库根目录同样没有 LICENSE。

有意思的是，这个仓库里其实*躺着好几份 LICENSE 正文*：vendor 进来的 BehaviorTree.CPP 带着 MIT（版权人写得清清楚楚），FAST_LIO 带着 GPL-2.0 全文，Livox 驱动带着 MIT，还有一个接口包放了 Apache-2.0 的完整模板、可版权人那一栏还停在 `Copyright [yyyy] [name of copyright owner]` 的占位符上没人填。*没有一份覆盖这个决策包*。所以这里恰恰是"许可证按包看不按仓库看"的现场：你要是拿"仓库里有没有 LICENSE"当判据，这个仓库会让你一路判错——它有十来份 LICENSE，而你真正想抄的那个包一份都没沾上。

这里必须把话说清楚：*`package.xml` 的 `license` 标签不构成许可授予*。它是 ROS 生态里的一个*元数据*字段，用途是让打包工具和索引站知道这个包"声称"是什么协议；`ros2 pkg create` 甚至会给你填一个默认值进去，很多包里那一行从建包那天起就没人动过。真正构成授权的是许可证正文加上明确的版权人——著作权法默认*保留全部权利*，要放开权利必须有人明确地放开。

所以判定流程应该是：先找 LICENSE 正文（不是 `package.xml`，不是 README 里的一句话，不是 GitHub 侧边栏的标签），读全文，确认版权人；再看这个 LICENSE 覆盖到哪一层目录——*顶层的 LICENSE 不会自动覆盖子目录里 vendor 进来的第三方代码*，那些子树各有各的协议，本章前面那份被改过的 BehaviorTree.CPP 就带着它自己的 MIT 声明。这三步任何一步不过，结论就是"只能读，不能抄"。

顺便说一句，这条规矩对你自己的仓库同样成立，而且更重要：*你不写 LICENSE，别人就不能合法使用你的代码*——哪怕你本意就是开源分享。星标数不等于可用性；一个被很多人收藏、却没有许可证正文的仓库，收获的是关注度，不是可复用性。发布代码时花三十秒选一个协议放进 LICENSE 文件，是对所有想学你东西的人最基本的尊重。

=== 规则怎么落进代码：一个 i.e. 决定的判据

上一章讲雷达标记进度时，我们靠手册里一个 `i.e.` 从句把判据 $d$ 钉死为平面二维距离，省掉了整条高度精度的工程链。哨兵这边有一个同样级别的例子，而且更适合当教材，因为它直接决定你火控代码里的一个数。

2026 手册给了哨兵一个"姿态系统"：三种模式（进攻、防御、机动）各有增益与代价，完整对照见本节后面「姿态模式：一个带冷却时间的调度问题」那张表。这里只需要用到其中一条——*防御模式把热量冷却降到原来的 $1 \/ 3$*。

关键在手册给的那个热量冷却算例。原文的场景是：一台全自动哨兵处于防御模式，基础冷却 30/秒；它激活大符拿到*三倍*冷却增益，同时占据堡垒增益点拿到*每秒 $+75$* 的冷却增益。然后手册写道——先取二者中较大的那个（`i.e., +75 per second`），此时冷却率为 $30 + 75 = 105$；再乘防御模式的 $1 \/ 3$ 衰减因子，得到实际冷却率 $105 times 1 \/ 3 = 35$。

"取二者中较大的那个"这句话，主句其实已经替你做掉了一件事：*它排掉了"两个增益一起用"*。所以下面这两种读法从一开始就不成立——

- *叠加*：$30 times 3 + 75 = 165$；
- *先加后乘*：$(30 + 75) times 3 = 315$。

真正需要那个 `i.e.` 来救的是剩下的问题：*"较大"到底在比什么？* "二者"一个是*倍数*（三倍），一个是*每秒增量*（$+75$），量纲不一样，没法直接比大小。凭直觉觉得"翻三倍比加 75 厉害"，你就会取倍数那一边，得到 $30 times 3 = 90$。括号里那句 `i.e., +75 per second` 就是来指定比较对象的：先把倍数换算成每秒增量（基础 30，三倍相当于 $+60$/秒），再和堡垒点的 $+75$/秒比，取 $+75$，于是 $30 + 75 = 105$。

这里还有一个值得留意的巧合：$30 times 3 equiv 30 + 60$，所以"比每秒增量"（$+60$ 与 $+75$）和"比算完的结果"（$90$ 与 $105$）在这条规则下*恒等、永远同解*——这正是手册敢只用一句括号打发过去的原因。但它同解的前提是两种增益作用在同一个基数 30 上，换一版规则未必还成立。

而这个数进到代码里是有物理后果的。17 mm 弹丸每发使枪口热量增加 10，所以*可持续射速就是冷却率除以 10*。冷却 105 对应每秒 10.5 发，冷却 35 对应每秒 3.5 发——差三倍。更要命的是运算顺序：手册明写模式衰减是在各类冷却增益算完*之后*再乘上去的。如果你把顺序写反，先乘 $1 \/ 3$ 再加 75，会得到 $30 times 1 \/ 3 + 75 = 85$，对应每秒 8.5 发。同样两条规则、同样两个数字，*乘法与加法的先后顺序，让你的哨兵可持续输出差了 2.4 倍*。

这就是本节想让你带走的：*规则文本里的从句辖域和运算顺序，会一路走进你代码里的一个判据、一个阈值、一条 DPS 曲线*。工程上的对策有两条，都很朴素——第一，凡是从手册抄来的常量，在代码里写明出处（哪一版手册、哪一节），赛季一换好按图索骥；第二，手册给了算例的地方，*把算例写成单元测试*。上面那个 $30 → 105 → 35$ 就是一个现成的测试用例，你的热量模型跑不出 35，说明你读错了规则，而不是等到赛场上枪口过热锁定才发现。

（本节所有规则数值均取自 2026 赛季手册，*随赛季变动，以当季手册为准*。）

==== 姿态模式：一个带冷却时间的调度问题

把同一套算法再往前推一步，姿态模式这件事本身就是决策层的一道正经题目，而且它的结构和你熟悉的任何"状态机切换"都不一样。

先按手册的算法把三种模式的可持续射速推出来（*以下为按规则推导，非实测*）。全自动哨兵基础冷却 30/秒，按前面算过的那条换算——冷却率除以 10 就是可持续射速：

#figure(
table(
  columns: (auto, auto, auto, auto, auto),
  align: (left, center, center, center, left),
  stroke: (x, y) => (
    top: if y == 0 or y == 1 { 1pt } else { 0pt },
    bottom: if y == 3 { 1pt } else { 0pt },
  ),
  inset: 7pt,
  table.header([*模式*], [*冷却率*], [*可持续射速*], [*底盘功率*], [*生存性*]),
  [进攻], [$30 times 3 = 90$/s], [$9$ 发/s], [$times 0.5$（50 W）], [易伤 $+25%$],
  [防御], [$30 \/ 3 = 10$/s], [$1$ 发/s], [$times 0.5$（50 W）], [防御增益 $50%$],
  [机动], [$30 \/ 3 = 10$/s], [$1$ 发/s], [$times 1.5$（150 W）], [易伤 $+25%$],
),
caption: [三种姿态模式的推导对照（数值按 2026 手册规则推算，*非实测*；底盘功率以全自动哨兵基准 100 W 计，各模式效果超过三分钟会衰减，此处取未衰减值）。读表规则：横着看是每种模式的完整代价，*竖着看才是决策层要的东西*——可持续射速那一列只有进攻模式是能打的，另两种模式下你每秒只能发一发；而底盘功率那一列，唯一给你机动性的模式恰恰是射速最低的两种之一。],
)

这张表一摆出来，几条设计结论就是白纸黑字的了：

- *持续输出只在进攻模式下存在*。9 发/秒和 1 发/秒差着九倍，这不是"略有不同"，这是"能打"和"不能打"的区别。你的决策层如果一直待在默认的机动模式（手册规定开局默认就是机动模式），等于全程只有九分之一的持续火力。
- *开火与走位是互斥的*。进攻模式把底盘功率砍到 50 W，正好是你做反自瞄机动最需要功率的时候；机动模式给你 150 W，却把射速砍到 1 发/秒。上一节讲的反自瞄机动，代价在这里以规则条文的形式明码标价。
- *防御模式是唯一提高生存性的选项，代价是几乎放弃输出*。它适合的场景很窄——基地告急、需要用血量硬拖时间。

再看两条时间约束，它们决定了这道题的*形态*：切换有 5 秒冷却，同一模式待超过三分钟效果衰减。5 秒冷却意味着模式*不能做成对每帧感知结果的反应*——你的行为树不能"看见敌人就切进攻、敌人一走就切机动"，那样冷却根本不够用，而且切换途中你两边好处都拿不到。三分钟衰减则意味着*躺在一个模式里不动是会被罚的*：七分钟的比赛，你必须主动规划模式的使用节奏。

所以正确的抽象是：*模式是一个带冷却时间和使用时长惩罚的资源，要在比赛时间轴上调度，而不是在感知回路里反应*。这恰好和前面那棵真实赛用树"以剩余时间 270 秒为界分成前后两套打法"的写法对上了——赛季代码里那种看起来很粗糙的"按时间分段"，背后是规则逼出来的结构。决策层里*不是所有东西都该跑在同一个时间尺度上*：火控在毫秒级、导航目标在秒级、姿态模式在十秒到分钟级。把它们混在一棵树的同一层里轮询，是哨兵决策代码最常见的结构性错误。

=== 双云台：两个头，一个身子

哨兵要防的是 $360 degree$，而一个云台只能看一个方向。于是有队伍走到了极端：*给一台哨兵装两个独立云台*，各自带一台相机、各自一套 yaw/pitch、各自一根枪管，共用一个底盘。

（关于引用尺度，先说清楚一件事——*许可证是按包看的，不是按仓库看的*。本节参考的两个仓库*根目录*都没有 LICENSE，但这不代表里面什么都不能引：那个双云台工作空间里带 LICENSE 的包，全是从上游 rm_vision 系 vendor 进来的（`rm_auto_aim`、`rm_vision`、`ros2_mindvision_camera` 是 MIT，`rm_gimbal_description`、`rm_serial_driver` 是 Apache-2.0）。下面要讲的坐标系正出自 `rm_gimbal_description`，该包带 Apache-2.0，署名引用本就允许。而真正没有授权的是该队新写的仲裁包：它没有 LICENSE 文件，`package.xml` 的许可证字段还停留在 `ros2 pkg create` 生成的占位符 `TODO: License declaration`——*占位符不是授权*。所以对这个包，本节只描述其公开可见的结构与思想，一行源码都不摘。

顺带三课，都是从这个仓库里现挖的。第一，*在 GitHub 上公开不等于授权*：没有许可证时著作权人保留全部权利，你既没拿到复制权也没拿到修改权。第二，*vendor 目录里的 LICENSE 不覆盖你新加的包*——你把上游连 LICENSE 一起拷进工作空间，那份许可证只管它自己那个目录，你隔壁新建的包依然是零授权状态，得自己补。第三反过来也成立：这个仓库里那两个海康相机包是从上游复制改名而来的（README 连原文的拼写错误都留着），vendor 时把 LICENSE 文件弄丢了——*丢掉 LICENSE 并不会让上游代码变成无主物*，它依然受上游许可证约束，而删掉版权声明本身通常就已经违反了那份许可证的署名保留条款。判断方法还是那一条：把每一层目录的 LICENSE 都翻出来逐份读全文，别只看仓库根，也别只看首行。）

*坐标系怎么搭。* 从 URDF 上看得很清楚，TF 链是一棵两分叉的树：底盘 `odom` 下面挂一个"大云台" `gimbal`，大云台下面左右各挂一套小云台底座（`odom_link_first` / `odom_link_second`，横向偏移 $plus.minus 0.123$ m、纵向下沉 $0.140$ m），每个底座下面再挂小云台系、相机系、相机光学系。整棵树的结构和「坐标变换」一章讲的单云台链条完全同构，只是从一条链变成了两条并列的链——*这正是 TF 树这套抽象的价值：加一个头，不需要重新发明坐标变换，只需要多挂一个分支*。固件那一侧的证据也对得上：另一支队伍的双云台哨兵，STM32 工程是三个独立的——主控一个、左云台一个、右云台一个。软硬两侧的结构决策是一致的。

*两路目标怎么仲裁。* 这里是最容易想当然的地方，所以要如实写。两个相机各跑一套完整的识别流程，各自发布自己看到的装甲板；中间有一个仲裁节点，从两路里挑一路转发给下游的单一跟踪器。它的逻辑是*计数器实现的时间片互斥*：哪个相机先检到目标，就用哪个相机的数据，同时给它一个计数器置 80；另一路在此期间*完全不发布*；只有当占用方连续 80 帧都没检到东西，计数器归零，另一路才被放行。

这里有两件事必须说清楚，因为很容易被高估：

第一，*这不是基于三维位置的跨相机去重*。它没有把两个相机的观测投到同一个世界系里比对是不是同一台车，它只是在做"谁先抢到谁用"的排他。后果是：如果同一台敌车同时出现在两个相机视野里，跟踪器*永远只会收到其中一路的观测*——另一路的信息被整个丢掉，而不是被融合。这是一个刻意的简化，不是一个失败的融合。

第二，那个 80 是*帧数*不是时间。开发者自己在文档里换算过：实际帧率六七十，所以 80 帧大约是一秒多。这正是「装甲板的跟踪」一章点名过的坑——*按帧数写死的超时，会随帧率变化而变化*。相机换个分辨率、算力紧张掉到 30 帧，这个"一秒多"就悄悄变成了"两秒多"，切换会变迟钝而没有任何报错。同一个坑在跟踪器的 `lost_thres` 上出现过，在这里又出现了一次。

最诚实的一笔在这个仓库的文档里：开发者写道，因为两路图像的时间戳差距较大（相机延迟加上各自的处理延迟），ROS 2 自带的精确同步和模糊同步*都没能很好地解决问题*，所以才退而采用这个"比较笨的方法"。这句话把「时间戳对齐」一章的主线又兑现了一次——*现成的同步工具解决的是"时间戳可信时怎么对齐"，解决不了"时间戳本身就不可信"*。当两条链路的延迟不对等且未知，同步器只会把你对齐到一个错误的时刻上，还不如干脆不融合。

顺带纠正一个常见的想象。听到"全向感知"，很多人脑子里浮现的是一张缝合出来的全景图；但*我们读到的这几份公开实现里，没有一个是这么做的*，全都是多路独立检测流加一个后端仲裁。另一支队伍的哨兵是一台工业相机（自瞄）加四台 USB 相机（全向感知）的组合，两套系统物理上就是分离的。全景拼接需要精确的相机间外参、重叠视场、以及实时的图像配准开销，在一台要跑导航加两路推理的车载算力上不划算——*工程上更常见的胜利方式是绕开问题，而不是解决问题*。

=== 反自瞄：第一次站到攻方的对面

到这里为止，这本书的每一章都在教你怎么*预测别人*。这一节反过来：怎么让别人预测不了你。

先回到「卡尔曼滤波」一章的地基。任何一个卡尔曼滤波器都建立在一个运动模型上，而本书一路讲下来的那套跟踪器、以及我们读到的这几份公开实现，用的都是*匀速模型*（CV）——状态里有位置和速度，状态转移假设速度在两帧之间不变，加速度被当成过程噪声 $bold(Q)$ 吸收掉。整车模型也是同一套哲学：中心匀速平动、装甲板匀速自旋。这个假设在对手做匀速运动时非常好用，而它的失效模式也就写在脸上：*对手的加速度越大、越不规则，模型误差越大*。

再叠上「自瞄算法设计」一章的结论：瞄准点不是目标的当前位置，而是它 $t_"fly"$ 之后的位置。这个提前量是靠估计出来的速度外推的。于是攻击链上出现了一个可以被针对的杠杆——*如果我在对方的 $t_"fly"$ 窗口内改变速度，他的提前量就一定是错的，错多少由我决定*。

量化一下就很清楚。设子弹速度 25 m/s（2026 手册给 17 mm 的初速上限，*随赛季变动、以当季手册为准*）、交战距离 5 m，飞行时间 $t_"fly" = 0.20$ s。如果我以 $u = 1$ m/s 横移，然后突然反向，对方基于反向前的速度外推，落点误差是

$ Delta approx 2 u t_"fly" = 0.40 "m" $

而小装甲板的靶面按 PnP 所用的 135 mm 灯条间距算，半宽 $0.0675$ m（全书统一取这个偏保守的口径，「装甲板识别」一章辨析过 135 与 140 的分别）。*误差是靶子半宽的将近六倍*——这一发必然落空，而且是"打在两块装甲板中间"的那种落空。真实情况还要更糟一点（对攻方而言）：对方的滤波器不会瞬间反应，它要花几个时间常数才把速度估计扭过来，在这段时间里误差一直挂在上面。

#figure(
  image("images/adv-sentry-antiaim.png", width: 100%),
  caption: [反自瞄机动为什么有效（脚本 `adv_sentry_antiaim.py`，*仿真推导值、非实测*：横移速度 1 m/s、每 1.0 s 反向一次，敌方为 100 Hz、横向量测噪声 2 cm 的匀速模型卡尔曼滤波器，5 m 交战距离、25 m/s 弹速故 $t_"fly" = 0.20$ s）。读图规则：左上灰色粗线是*目标 $t_"fly"$ 之后的真实位置*、蓝细线是敌方滤波器给出的瞄准点，两者贴合即命中；左下是二者之差，品红带是小装甲板半宽 $plus.minus 67.5$ mm——*误差落在带内才可能命中，落在带外必然脱靶*，灰色竖条标出脱靶区间。每次反向后误差冲到 $2 u t_"fly" = 0.40$ m、约为靶半宽的 5.9 倍，随后随滤波器重新收敛而衰减，本例脱靶时间占 $44%$。右图是设计旋钮，*但读图规则是非单调的*：脱靶占比在反向周期约 $0.4$ s 处取到峰值 $89%$，再快反而回落（$0.2$ s 只有 $79%$），$3$ s 周期则只剩约 $12%$。回落的机理值得记住——反向一旦快过对方滤波器的带宽，它干脆估出接近零的速度、瞄准点贴住平均位置不动，而与此同时你自己的摆幅 $u dot t_"leg"$ 也在缩小，两头一夹，误差反而回落。*所以存在一个最优反向周期，由 $t_"fly"$ 与对方滤波器带宽共同决定*，这比"越快越好"有用得多。⚠ 灰色区是*乐观上界*——仿真假设速度可瞬时反向，真实底盘受加速度限制走不出那么陡的折线，峰值恰好落在这个区间里，所以 $0.4$ s 只是本仿真的指示值，不是可以照抄的调参目标。],
)

图里那个 $44%$ 值得单独说一句，因为它决定了你对这套战术该抱多大期望。*反自瞄机动不会让你免疫，它只是把对方的射击窗口砍掉一块*。「自瞄算法设计」一章把"云台落在射击轨迹上的时间占比"记作 $eta$，并推出 DPS 正比于 $eta$、击杀时间反比于 $eta$。反自瞄干的事就是从防守方去压对方的 $eta$：这个例子里把对方的可命中时间从 $100%$ 压到 $56%$，等价于把他打死你所需的时间拉长约 $1.8$ 倍。同一个 $eta$，攻方想抬高它、守方想压低它——*这一章和那一章讲的是同一个量的两端*。

工程上怎么实现？一支队伍的决策库里有一个专门的"反自瞄"行为树节点，思路是这样的：以一个可配置的周期为一轮，把一轮切成四段，每段给一个方向不同的平移速度——两段纯横移、两段带 $30 degree$ 斜向分量；并且在每一段*内部*再把速度切成"慢—快—慢"三档。整轮走完位移基本回到原点。这个设计有三层用心：*方向在段边界跳变*，破坏速度估计；*段内还有速度档位跳变*，让对方即便用匀加速模型（CA）也跟不住；*整轮闭合*，所以哨兵不会因为做机动而漂离自己的防守位。

另一条更省事的路子是*受伤即自旋*。另一支队伍的哨兵决策黑板里，每收到一包裁判系统数据就把当前血量和上一包比一次，一旦发现掉血（或者前后相机任一路检测到敌人），立刻置起小陀螺标志位交给底盘执行；持续到脱离触发条件约 10 秒后停止。这里有个细节值得学：它显式排除了*车在斜坡上*的情况——在坡上小陀螺容易翻，规则给的机动能力不等于物理上安全的机动能力。把"这个动作在什么条件下不该做"编码进代码，和「装甲板识别」一章讲的"平衡步兵显式跳过 yaw 重投影搜索"（因为它的装甲板俯仰假设不成立）是同一种素养：*把假设的适用边界写进代码，而不是写进某个人的脑子里*。

最后是代价，这一节不能只讲收益。机动不是免费的：

- *功率*。全自动哨兵底盘功率上限 100 W，机动模式能提到 $1.5$ 倍——但同一个模式把热量冷却降到原来的 $1 \/ 3$。也就是说，*你为躲子弹买的机动性，是拿自己的可持续射速换的*。上一节那笔账在这里付款。
- *算力与耦合*。底盘在做不规则机动，云台要在这之上把瞄准稳住，「坐标变换」一章那条 `odom → gimbal` 链的角速度会显著变大，对时间戳对齐精度的要求也水涨船高——*你给对方制造的预测困难，有一部分会原样返还给你自己的自瞄*。
- *决策冲突*。反自瞄机动会直接往 `cmd_vel` 上写速度，而导航也在往 `cmd_vel` 上写。谁优先、怎么抢占、抢占多久，必须在决策层显式仲裁，否则两个模块会打起来，表现为车在原地抽搐。前面那个"底盘自旋通过独立话题叠加、不与导航争 `cmd_vel`"的设计，正是为了避开这类冲突。

攻防对抗这个视角一旦打开，很多事情会重新排序：你的自瞄有多好，取决于对手有多配合；而对手配不配合，是他的一个设计选择。这是 RoboMaster 视觉里少有的、真正带博弈性质的一层。

==== 本章小结

哨兵之所以要单开一章，是因为一条规则改动：2026 赛季它的初始位置是 Starting Zone 而不再是 2022 那样的哨兵轨道——它从导轨兵种变成了自由移动的地面机器人，导航栈因此从"不需要"变成了视觉的核心下游。本章沿着这条主线走了一遍：*定位*上，本赛季没有官方赛前建图环节，"离线预建图加重定位"与"上场纯里程计建图"成了一道真实的选择题，成熟做法是把两条路做成同一份代码的一个布尔开关；而一份有偏的先验点云可能比没有先验更坏，这是社区留下的诚实记录。*运动补偿*上，云台自旋会让导航栈的参考系整个转起来——对一台把 `cmd_vel` 当底盘系执行的车，实际行进方向会被拧掉一整个偏航角；对策是造一个 yaw 恒定的虚拟系给规划器用、输出速度再转回真实系，感知活在真实系、规划活在虚拟系、中间夹一层翻译。这里有一处必须自己算过才不会讲错：改名加反向补偿对*线上那三个数*是恒等变换，它买到的是让上游每一级（控制器、速度平滑器）都在一个不转的系里计算；这个包里 `cmd_vel` 因为没有时间戳而被迫去借 `local_plan` 的时间戳，是"消息类型缺时间戳会怎样把人逼到墙角"的最好教材。同一道题在五份公开实现里有五种解法，数学核心全是 $bold(R)(-psi) bold(v)$，把它们分档的只有 $psi$ 有多新鲜——陈旧一个控制周期，实际速度就整个转过 $omega Delta t$，按公开参数推导是 $9 degree$、$0.39$ m/s、占指令 $16%$，*且几乎整个垂直于你想去的方向*；要不要做这套解耦则取决于一个条件判断，*底盘会不会自旋*，而不是水平高低。还有两个坐标系名的坑：自瞄栈那个只有姿态、原点钉在云台上的 `odom`，和导航栈那个会平移的 `odom`，同名不同义，合进一个 TF 树会让全局态势图整体偏掉一个车身位移。*决策*上，先要搞清楚它不住在 Nav2 的行为树里、却常常住在同一个导航仓库的另一个包里；决策写法是一条从时间轴脚本到插件化行为树的光谱，真正的分界线是"赛场上改一条策略要不要重编译"；优先级打分把距离与残血两项加权，但归一化用的是当前批次的最大值，所以它只能排序不能比阈值，而一个完整的打分函数还该包含兵种价值、是否正在攻击我、打不打得到、边际收益这些维度——那是留给你的设计题。把策略挪进 XML 的代价是编译器不再替你检查：拼错的黑板键、被 `std::stod` 静默接受的 `-5.6.0`，都是活生生的例子；而一棵依赖被改过的 vendored 库才能跑的树（同一个文件里三处改动，其中一处还悄悄改掉了前置条件的求值时机），是"读开源要以代码为准、包括它 vendor 进来的第三方库"的最好注脚。许可证上有两个必须记住的陷阱：*顶层 LICENSE 不覆盖子树*，*`package.xml` 的 license 标签不构成许可授予*；而那份 GPL-3.0 的 RoboRTS 血脉行为树，抄进战队仓库会带上传染性。*情报*上，雷达与哨兵这条接收侧被互相钉死、规则唯一强制成对的链路，用起来的门槛是把雷达的场地系和哨兵的地图系对齐（建图那道选择题在这里第二次收到账单），而米级精度与百毫秒时延直接推出了融合规则——雷达情报驱动导航目标，自身相机驱动云台，两者冲突时各管各的问题。*规则*上，热量冷却算例里那个 `i.e., +75 per second` 排掉了三种错误读法，加乘顺序写反会让可持续射速差 2.4 倍——把手册的算例写成单元测试，是最便宜的保险；顺着同一套规则往下推，三种姿态模式里只有进攻模式能打（9 发/秒对 1 发/秒），而它恰恰把底盘功率砍半，*开火与走位在规则层面就是互斥的*；5 秒冷却与三分钟衰减则说明模式是一个要在时间轴上调度的资源，不是感知回路里的反应——决策层的三个时间尺度（火控毫秒、导航秒、姿态十秒到分钟）不该混在一棵树的同一层里轮询。*架构*上，双云台是两条并列的 TF 链加一个计数器时间片仲裁，它不是三维跨相机去重、那个 80 也是帧数不是时间；我们读到的这几份"全向感知"实现，做的都是多路独立检测流加后端仲裁，不是图像拼接。仿真与实车的参数差距也量化过：同一套代码两份参数，只差速度与加速度限幅一块，其中角加速度差了 $30$ 倍。最后是全书第一次站到攻方对面：匀速模型的假设写在脸上，$2 u t_"fly"$ 的落点误差能轻松做到装甲板半宽的近六倍，而你为机动付出的代价是热量冷却、时间戳精度和决策层的仲裁复杂度。雷达与哨兵两章走完，"换个兵种要重做哪些假设"这条线就收束了。下一章「性能优化技巧」把镜头转向另一个方向：同一套系统、同一块板子，怎么跑得更快——当工具都会用了、帧率还是不够时，工程时间该花在哪一档。

（本章规则条款取自 RoboMaster 2026 大学生超级对抗赛官方手册，条款与数值随赛季变动，以当季手册为准。摘录了源码或配置数值的仓库均为宽松许可，正文已就地署名：`pb2025_sentry_nav` 与 `pb2025_sentry_behavior`（Apache-2.0，SMBU 北极熊战队）、`SCNU-RM-Sentry`（MIT，CodeAlanqian）、`rmcs-navigation`（BSD-2-Clause，Copyright 2025 Alliance Algorithm Team，正文"别堵门"那条补给间隔注释）；正文比对的那一行 `ReactiveFallback` 开关出自被 vendor 进去的 BehaviorTree.CPP，该子树自带 MIT 声明——这本身就是「许可证按包看不按仓库看」那条规矩的现成用例。其余被点评到的实现一律*只描述思想与结构、不摘录任何源码*，原因分两类：一类是 GPL-3.0（坐标变换里那个 $0.12$ 魔数、手写行为树框架、弹量账本），传染性见正文；另一类是仓库没有 LICENSE 正文（那棵 149 行的赛用树与它的地理围栏装饰器、反自瞄机动节点、双云台仲裁包、"死战不退"脚本），著作权人保留全部权利，*星标数与 `package.xml` 的标签都不构成授权*。本章的量化结论——陈旧 yaw 的 $bold(R)(delta)$ 误差、三种姿态模式的可持续射速、反自瞄的落点偏差与脱靶占比——均为按公开参数或规则*推导*所得，非实测；各队自述的效果与观察一律未复现。）
