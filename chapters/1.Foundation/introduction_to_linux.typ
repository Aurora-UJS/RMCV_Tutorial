
#import "/template/template.typ": blockquote

=== Linux 的前世今生
// 从 Unix 到 Linux 的历史脉络
// - Unix 的诞生（1969，贝尔实验室，Ken Thompson & Dennis Ritchie）
// - Unix 的设计取向：单一职责、组合、文本流
// - Unix 的分裂与商业化
// - Richard Stallman 与 GNU 计划（1983）
// - 自由软件运动与 GPL 协议
// - Linus Torvalds 与 Linux 内核（1991）
// - "I'm doing a (free) operating system (just a hobby)"
// - Linux 内核 + GNU 工具 = 完整操作系统
// - 从个人项目到多方参与的长期协作
// === Linux 的前世今生

在开始学习 Linux 的具体操作之前，先了解它从哪里来，会更容易理解后面的目录、命令行和开源软件。Linux 延续了 Unix 的许多设计原则，也与 GNU 计划和自由软件运动密切相关，并在全球开发者的协作下逐步扩展到服务器、移动设备和嵌入式系统。这段历史既包含技术演进，也涉及软件如何开发、授权和分享。

==== Unix：一切的起源

故事要从 1969 年说起。在美国新泽西州的贝尔实验室，Ken Thompson 在一台闲置的 PDP-7 小型机上开始实现一个新的操作系统；Dennis Ritchie 等人随后参与开发，这个系统后来被命名为 Unix。

在此之前，贝尔实验室参与了名为 Multics 的大型分时操作系统项目；由于项目进展和投入回报未达到预期，贝尔实验室最终退出。Thompson 仍希望在较小的机器上获得方便的交互式计算环境，于是逐步实现了文件系统、Shell 和其他工具。Brian Kernighan 后来提议用 Unics 称呼这个系统，即 “Uniplexed Information and Computing Service”，在名称上与 Multics 构成双关；写法之后变为 Unix。把它简单解释成对 Multics “过度复杂”的讽刺，会遗漏这段命名历史的细节。

Unix 在发展过程中形成了一组影响广泛的设计取向：让程序专注于明确的任务；让程序能够通过文本流等简单接口组合；尽早做出可运行的实现，再持续改进。后人常用“Unix 哲学”概括这些取向，它不是一份所有 Unix 程序都严格遵守的规范。后文在 Linux 终端中用管道符 `|` 连接多个命令，就是“让工具能够组合”的一个具体例子。

20 世纪 70 年代初，Dennis Ritchie 主导设计了 C 语言；到 1973 年，Unix 的大部分代码已用 C 重写。与主要使用特定机器汇编语言的系统相比，这显著提高了 Unix 的可移植性，使它更容易迁移到不同类型的计算机上，也为后来的广泛传播创造了条件。

贝尔实验室隶属于 AT&T 公司。当时的反垄断和解协议限制了 AT&T 在电话业务之外的商业活动，Unix 因而以较低成本向大学和研究机构提供源代码许可，并逐渐在学术界传播。加州大学伯克利分校的学生和研究人员在 Unix 的基础上进行了大量改进和扩展，形成了著名的 BSD（Berkeley Software Distribution）版本。BSD 对 Unix 网络技术影响很大，其中的 TCP/IP 实现也推动了这套协议在研究机构和工作站上的普及。

1984 年 AT&T 完成拆分后，Unix 的商业化程度提高，授权方式和费用也发生了变化。与此同时，各个公司和机构开发的 Unix 变体逐渐分化，兼容性成为问题。这个时期出现了许多商业 Unix 系统，例如 Sun 的 Solaris、IBM 的 AIX 和 HP 的 HP-UX；围绕接口标准、授权和知识产权的分歧，也改变了早期以高校交流为主的生态。

==== GNU 与自由软件运动

在 Unix 软件日益商业化的同时，MIT 人工智能实验室的程序员 Richard Stallman 开始推动一套不同的软件开发与分发方式；资料中也常按他的姓名首字母写作 RMS。

Stallman 主张，软件用户应当拥有运行、研究、修改和分发软件的自由。他后来经常讲到实验室打印机的经历：旧设备出现问题时，研究人员曾能修改程序；换用无法取得源代码的新设备后，他们不能再自行加入卡纸通知等功能。这个例子说明了闭源许可如何限制用户修改软件，也是理解他后来发起自由软件运动的背景之一。

1983 年，Stallman 发起了 GNU 计划。GNU 是“GNU's Not Unix”的递归缩写：展开后的名称中再次包含 GNU 自身。这个计划的目标是创建一个自由的、与 Unix 兼容的操作系统。这里的“自由”指用户可以运行程序、研究程序如何工作（因此需要取得源代码）、分发程序副本，以及修改并发布改进版本；它描述的是使用和分享软件的权利，不等同于价格为零。

1985 年，Stallman 创立了自由软件基金会（Free Software Foundation，FSF）；GNU 项目在 1989 年发布了 GPL（GNU General Public License）第一版。GPL 采用 copyleft 机制：分发受 GPL 约束的程序或相应衍生作品时，需要按适用版本的条款提供源代码并保留相应自由。具体义务取决于作品如何组合与分发，本节先理解其“修改和再分发仍保持开放”的基本目标。

在接下来的几年里，GNU 计划开发了大量软件工具：GCC 编译器、GDB 调试器、Emacs 编辑器、GNU Make、Bash Shell，以及 GNU coreutils 等命令行工具。它们至今仍是许多 Linux 发行版用户空间的重要组成部分，RoboMaster 开发者也经常会用到其中的一部分。例如，用 GNU `g++` 编译代码、用 GNU Make 构建项目或用 Bash 编写脚本，使用的都是 GNU 软件；具体系统也可能改用 Clang、Ninja 或其他实现。

然而，到了 1990 年代初，GNU 计划仍然缺少一个可广泛使用的内核。GNU Hurd 选择了基于 Mach 的多服务器架构，开发进度没有赶上其他 GNU 工具；它后来仍在开发，但没有成为主流生产系统。内核负责进程调度、内存管理和硬件访问，是让这些用户空间工具组成完整操作系统所缺少的关键部分。

==== Linux 内核的诞生

1991 年 8 月 25 日，芬兰赫尔辛基大学的一名 21 岁学生在 Usenet 新闻组发布了一条消息：

#blockquote[
  Hello everybody out there using minix -

  I'm doing a (free) operating system (just a hobby, won't be big and professional like gnu) for 386(486) AT clones.
]

这个学生的名字是 Linus Torvalds，他正在开发的这个“业余爱好项目”，就是后来的 Linux 内核。

Torvalds 当时希望在自己的 386 电脑上获得更适合个人使用和实验的类 Unix 环境。他使用 Minix 学习操作系统，也编写了终端仿真、磁盘访问等代码；这些工作逐步发展成了一个独立内核。GNU 计划和 Linux 内核的起点与目标并不相同，不必把二者简化为“理想主义”和“实用主义”的对立。

Torvalds 将源代码发布在网上，邀请其他人参与改进。来自不同地区的程序员通过互联网协作，报告 bug、提交补丁并添加功能；在当时，这种公开、分布式且快速扩大的开发方式并不常见。Torvalds 后来将 Linux 内核改用 GPL 许可证发布，使它能够与 GNU 工具共同组成可自由使用和修改的系统。

Linux 内核与 GNU 工具以及其他系统组件结合后，形成了可以实际使用的自由操作系统。对于大量采用 GNU 用户空间的发行版，GNU 项目倡议使用“GNU/Linux”这一名称，以同时体现 GNU 与 Linux 内核的贡献；日常交流中，人们通常简称为“Linux”。这个称呼并不适用于所有基于 Linux 内核的系统，例如 Android 的主要用户空间就不是 GNU 系统。

Linux 后来的广泛应用通常与多项条件联系在一起：它采用可动态加载模块的单内核架构，并持续适配新的处理器与设备；GPLv2 允许取得、修改和再分发内核源代码，同时对再分发设置相应条件；互联网降低了跨地区提交补丁和讨论问题的成本；服务器、嵌入式设备等场景又带来了持续需求。架构或许可证本身并不会自动带来性能、稳定性和活跃社区，这些结果还依赖长期的工程维护、测试、治理与参与者投入。

==== 从爱好项目到广泛应用

三十多年后的今天，Linux 已经应用于服务器、超级计算机、移动设备和嵌入式系统等多个领域：

Linux 广泛用于网站、云服务和数据中心的服务器。使用搜索引擎、在线视频或消息服务时，请求背后的部分服务很可能运行在 Linux 上，但具体平台取决于服务提供者的基础设施。

近年的 TOP500 超级计算机榜单中，入榜系统几乎都采用基于 Linux 的操作系统。科学计算、气象预测和人工智能训练等任务，也是 Linux 高性能计算生态的重要应用场景。

Android 系统基于 Linux 内核，这意味着全球数十亿部智能手机都在运行 Linux 的变体。

Linux 也广泛用于嵌入式设备，例如路由器、智能电视、汽车信息娱乐系统、树莓派和 NVIDIA Jetson。RoboMaster 机器人上常用的 Jetson 系列计算平台，其 JetPack 软件栈以 Ubuntu 为基础。

如今，Linux 内核是规模较大的开源协作项目之一，贡献者既有个人开发者，也来自 Google、Microsoft、Intel、Red Hat、华为等公司。Microsoft 还在 Windows 中提供了 WSL（Windows Subsystem for Linux），让用户可以在 Windows 环境中运行 Linux 发行版和工具。

Linux 从个人项目发展为由个人、企业和组织共同维护的基础软件，展示了一种长期、分布式协作的实现方式。理解这段历史，能够解释后文几个看似分散的概念为何会同时出现：目录和管道延续了 Unix 接口，许多用户空间工具来自 GNU，不同组件又通过各自的许可证和项目规则协作。

RoboMaster 开发常用的 ROS、OpenCV、GCC、CMake 和 Git 都是独立发展的开源项目，并不都只运行在 Linux 上。Ubuntu 把其中许多工具和依赖集成到同一套软件仓库与开发环境中。了解内核、发行版、工具和开源项目之间的区别，有助于继续阅读它们的文档、许可证、问题记录与贡献说明，而不是把所有组件笼统地称作“Linux”。


=== 为什么 RoboMaster 开发选择 Linux
// 为什么许多机器人项目使用 Linux
// - ROS 2 对 Ubuntu LTS 的一级支持
// - 开源生态：OpenCV、PCL、Eigen 等库
// - 低延迟机制与系统控制接口
// - 与树莓派、Jetson 等嵌入式平台衔接
// - 部署、仿真与持续集成环境
// - 开发工具链完善：GCC、GDB、CMake
// - 免费且可定制
// - 可迁移到其他开发场景的基础技能
// === 为什么 RoboMaster 开发选择 Linux

在 RoboMaster 社区中，许多队伍选择在 Linux 上运行视觉系统、导航算法和仿真环境。常见原因包括 ROS 的平台支持、开源库的安装方式、硬件接口、部署平台和开发工具。理解这些具体条件，也能帮助你判断后续学习内容与机器人开发有什么关系。

==== ROS 的平台支持

机器人操作系统 ROS（Robot Operating System）是机器人软件开发中广泛使用的开源框架。学术研究、工业项目和 RoboMaster 队伍都在使用 ROS 或 ROS 2，以实现节点通信、坐标变换和传感器数据处理等功能。

ROS 最初主要面向 Linux 开发。ROS 2 已支持 Windows 和 macOS，但各发行版对不同平台的支持等级、二进制包覆盖和第三方软件包测试情况并不相同；许多机器人项目仍以 Ubuntu 作为主要开发环境。选择对应 ROS 发行版的 Ubuntu 一级支持平台，通常能直接使用官方安装文档和二进制包，也更容易复用社区中的配置与问题记录。

ROS 常用工具包括 ROS 1 的 catkin、roslaunch，以及 ROS 2 常用的 colcon、rosdep 和 `ros2 launch`。它们都能自然地融入 Linux 命令行环境，用几条可记录、可复现的命令完成工作空间创建、依赖安装和软件包构建。

==== 机器人开源库生态

机器人开发会使用大量开源库：OpenCV 处理图像，Eigen 进行矩阵运算，PCL 处理点云，Ceres 解决优化问题，g2o 构建图优化。这些库大多支持多个平台，而机器人项目的上游文档、持续集成和依赖安装示例经常优先覆盖 Linux。

以 OpenCV 为例，在 Ubuntu 上可以通过包管理器安装发行版提供的版本，也可以从源码编译并选择 CUDA、OpenCL、TBB 等功能。Windows 同样可以使用预编译包、源码构建或 vcpkg 等工具，但编译器版本、路径和运行时库的配置方式与 Linux 不同。队伍统一平台和安装方法，可以减少成员之间的环境差异。

Linux 的包管理系统也便于集中管理依赖。例如，下面这条命令会安装 Ubuntu 仓库中的 OpenCV 开发包及其声明的依赖：

```bash
sudo apt install libopencv-dev
```

在 ROS 2 中，`rosdep install` 可以根据软件包清单解析系统依赖。两种工具都不能解决所有第三方版本和源码依赖问题，但能覆盖许多常见配置。

==== 实时性与系统控制

机器人软件通常包含有时限要求的任务：传感器数据要及时处理，控制指令也要在允许的延迟内发送。RoboMaster 视觉程序未必属于严格意义上的硬实时系统，但偶发的延迟尖峰仍可能使目标信息过期，影响控制效果。

Linux 提供 PREEMPT_RT、`SCHED_FIFO`、`SCHED_RR`、内存锁定和 CPU 亲和性等低延迟机制。PREEMPT_RT 可以减少内核中的不可抢占区间并改善调度延迟，但是否满足硬实时约束，仍要在具体内核、驱动、硬件和负载下测量最坏情况；提高实时线程优先级也需要相应权限，并要避免它阻塞系统中的其他任务。

Linux 通过设备文件和内核子系统提供串口、GPIO、I2C、SPI 等硬件接口；硬件仍然需要匹配的内核驱动，只是许多常见驱动已经随发行版提供。排查性能和系统调用问题时，还可以使用 perf、strace、ltrace 等工具观察程序行为。

==== 与嵌入式平台的衔接

RoboMaster 机器人上运行代码的硬件不一定是普通 PC。树莓派、NVIDIA Jetson 和许多工控机都可以运行 Linux：Jetson 使用以 Ubuntu 为基础的 JetPack 软件栈，Raspberry Pi OS 基于 Debian，x86 工控机也常安装 Ubuntu 等发行版。

在同一个操作系统家族中开发，可以减少脚本、包管理和目录布局方面的差异。Ubuntu 笔记本上的源代码通常能够沿用到 Jetson，但 x86-64 与 ARM64 的处理器架构、JetPack 提供的 CUDA 版本、相机驱动和其他硬件依赖仍需分别适配与验证。相比从 Windows 部署到 Linux，这样至少能复用较多构建和配置流程。

通过 Docker 容器或虚拟机，还可以在开发机上复现目标环境的一部分用户空间依赖。容器不能自动消除处理器架构、内核、驱动和真实设备之间的差异，但可以固定编译器和库版本，让部署前的构建与软件测试更容易重复。

==== 仿真与持续集成

仿真环境常用于机器人算法开发。Gazebo、Isaac Sim、Webots 等仿真器可以在暂时无法使用实机时复现一部分场景，也便于重复运行测试和构造边界条件。仿真结果仍受模型、传感器噪声、物理引擎和时序设置影响，不能替代真实硬件上的驱动、性能与控制验证。

Gazebo、Isaac Sim 和 Webots 都提供 Linux 版本，ROS 集成文档也常以 Ubuntu 为例。具体的图形驱动、GPU 加速和版本兼容性仍要根据仿真器的支持矩阵确认；选择社区常用的平台，通常更容易复用现有配置。

持续集成（CI）可以在代码变更后自动构建和测试。GitHub Actions、GitLab CI 等平台都提供现成的 Linux 运行环境，也可以选择 Windows、macOS 或自托管执行器。项目若以 Linux 为部署目标，就可以在相近的 Linux 环境中尽早发现构建和依赖问题。

==== 开发工具链的成熟

C++ 是机器人开发的主要语言之一。Linux 上常用 GCC 或 Clang，也能使用 CMake、Ninja、GDB、LLDB 等构建和调试工具。它们同样支持其他平台，但 Linux 发行版能够通过包管理器统一安装，许多机器人项目也直接提供相应的构建说明。

GDB 是 Linux C++ 开发中常用的命令行调试器，也可以与多种 IDE 集成。Valgrind 可检查部分内存问题，AddressSanitizer 和 ThreadSanitizer 分别帮助发现内存错误与数据竞争。它们各有适用范围和额外开销，但在排查难以复现的崩溃、泄漏或并发问题时很有价值。

性能分析工具也是 Linux 的强项。perf 可以进行 CPU 性能分析，gperftools 可以进行堆内存分析，FlameGraph 可以生成直观的火焰图。这些工具帮助你找到程序的性能瓶颈，在资源受限的嵌入式平台上尤为重要。

==== 免费、开源与可定制

对于学生团队来说，成本是一个现实因素。Ubuntu 等 Linux 发行版通常不按安装设备收取系统授权费，可以部署在多台开发机和机器人平台上。开源软件仍有各自的许可证条款，尤其在修改和再分发软件时需要遵守；Windows 的授权方式则取决于 OEM 预装、学校许可或单独购买等具体渠道。

开源使查看和修改源代码成为可能。在系统接口的文档不足时，源代码可以作为进一步分析行为的依据；有能力维护相应改动时，也可以裁剪组件，调整启动流程和资源占用。这种可定制性在资源受限的嵌入式平台上尤其有用，但读到源代码并不自动等于已经定位问题，修改后的系统也需要自行测试和维护。

Linux 还有规模较大的用户和开发者社区。遇到问题时，可以在发行版论坛、Stack Overflow、项目文档和 GitHub Issues 中查找相似案例。搜索结果的版本、硬件和前提不一定与本机相同，因此仍要核对适用条件，而不能只复制命令。

==== 可迁移的 Linux 技能

学习 Linux 的用途也不限于 RoboMaster。机器人开发、后端服务、嵌入式系统和云计算都经常使用 Linux；Android 使用 Linux 内核，许多服务器与 IoT 设备也采用 Linux。不同岗位需要的深度并不相同，但理解命令行、文件权限、进程和网络通常具有迁移价值。

在 RoboMaster 的开发过程中学习 Linux，可以把命令与具体任务联系起来：编译工程、连接车载计算机、检查进程和读取日志。相比只记命令表，这种以实际问题为线索的练习更容易建立完整认识；相关经验在后续课程、项目或求职中也可能继续使用。

综合来看，Linux 的 ROS 支持、依赖管理、硬件接口和部署环境都与 RoboMaster 的常见需求较为契合。接下来的章节会从安装、终端和文件系统开始，再逐步进入构建、调试与远程开发。初学时不需要一次记住所有命令，先理解常见操作的目的，再通过持续使用逐渐熟悉即可。


=== Linux 与 Windows 的差异
// 两个平台的常见默认设计
// - 开源 vs 闭源
// - 文件系统差异：目录树 vs 盘符
// - 路径分隔符：/ vs \
// - 大小写敏感 vs 不敏感
// - 可执行文件：无扩展名要求 vs .exe
// - 软件安装：包管理器 vs 安装向导
// - 命令行文化 vs 图形界面文化
// - 权限模型差异
// - 系统配置：文本文件 vs 注册表
// - 文件式接口与文件描述符
// - 常见的适应期困惑与解决
// === Linux 与 Windows 的差异

从 Windows 转向 Linux 的开发者往往会经历一段适应期。两个系统都能运行程序、管理文件，但目录结构、软件安装、权限和配置方式存在明显差异。本节对比与后续开发最相关的部分，帮助你理解 Linux 为什么采用这些操作方式。两边都有例外，下面描述的是常见默认行为，不是对所有版本和软件的绝对划分。

==== 开发与授权模式

Linux 和 Windows 的一个重要区别在于开发与授权模式。Windows 是微软的商业产品，大部分系统源代码不公开；用户主要依靠公开文档、诊断工具、技术支持和微软发布的更新。集中维护有利于提供统一支持和兼容性策略，但普通用户无法像修改开源项目那样直接审查、重编译并分发 Windows 系统组件。

Linux 内核和许多 Linux 软件采用开源许可证，允许用户在许可证范围内查看、修改和分发源代码。公开代码让安全研究者和开发者能够审计实现、提交补丁，也为理解系统行为提供了额外证据；它并不自动保证软件安全，也不保证每个问题都有人及时修复。项目方向则由维护者、贡献者、基金会和参与公司共同影响，不同项目的治理方式并不相同。

对于开发者来说，开源还提供了学习实现的机会。本书使用的 GCC、GDB、CMake 和许多算法库都能查看源代码；遇到文档没有覆盖的细节时，可以从接口声明、测试和实现中继续追踪，但仍要注意所读代码对应的具体版本。

==== 文件系统：目录树与盘符

打开 Windows 的文件资源管理器，你会看到 C:、D:、E: 这样的盘符。盘符通常分配给卷，也可以映射到网络共享；一块物理磁盘可以包含多个卷，某些分区则不会分配盘符。常见文件路径以盘符开头，如 `C:\Users\Username\Documents\file.txt`。盘符是一种访问入口，并不与物理磁盘一一对应；可移动设备在不同机器上取得的盘符也可能变化。

Linux 采用了完全不同的设计：单一的目录树。整个文件系统从根目录 `/` 开始，所有文件和目录都是这棵树的一部分。没有盘符的概念，物理磁盘和分区通过“挂载”的方式接入到目录树的某个位置。例如，一个 USB 驱动器可能被挂载到 `/media/usb`，之后你就可以通过这个路径访问它的内容。文件路径看起来像 `/home/username/documents/file.txt`。

这种设计的主要价值是提供统一路径。无论是本地硬盘、网络文件系统、USB 存储还是虚拟文件系统，它们都挂载到同一棵目录树中。程序通常只按路径访问文件，不必把盘符写进自身逻辑；不过更换底层存储仍可能带来权限、性能和文件系统语义上的差异。

Linux 发行版通常大体遵循文件系统层次标准（FHS），主要目录各有用途：`/home` 存放用户文件，`/etc` 存放系统级配置，`/usr` 存放大量程序和库，`/var` 存放日志等可变数据，`/tmp` 存放临时文件。实际位置仍会受发行版、安装方式和程序约定影响；熟悉这些常见目录后，可以先判断应该从哪一类位置查找。

==== 路径分隔符与大小写敏感

一个看似微小但经常引起问题的差异是路径分隔符。Windows 使用反斜杠 `\`，Linux 使用正斜杠 `/`。这个差异源于历史原因——早期 DOS 使用 `/` 作为命令行选项的前缀（类似 Linux 的 `-`），所以选择了 `\` 作为路径分隔符。

对于开发者来说，这个差异意味着你需要注意代码中的路径处理。硬编码 `\` 的路径在 Linux 上无法工作，反之亦然。好的做法是使用编程语言提供的路径处理函数（如 Python 的 `os.path.join` 或 C++ 的 `std::filesystem::path`），让程序自动处理路径分隔符的差异。在 CMake 中，无论在什么平台上，都应该使用正斜杠 `/`，CMake 会自动转换。

另一个重要差异是大小写敏感性。在 Windows 的常见文件系统配置下，`File.txt`、`file.txt` 和 `FILE.TXT` 通常指向同一个文件；在 Linux 常用的文件系统上，它们是三个不同的文件。这个差异经常造成跨平台构建错误：代码中 `#include "MyHeader.h"` 在 Windows 上可能正常，但如果实际文件名是 `myheader.h`，在 Linux 上就会编译失败。

大小写敏感性也影响命令和程序名。在 Linux 上，`ls` 和 `LS` 是不同的命令（后者通常不存在）。`make` 和 `Make` 是不同的程序。这种设计更加精确——你想要什么就得明确说什么——但也需要更加仔细。养成统一使用小写文件名的习惯可以避免很多问题。

==== 可执行文件与权限

在 Windows 上，文件关联、扩展名和可执行文件格式共同决定文件如何打开：`.exe`、`.bat` 和 `.cmd` 可以直接启动相应程序或解释器，`.msi` 则交给安装服务处理。如果文件管理器隐藏已知扩展名，`photo.jpg.exe` 一类双重扩展名可能误导用户；显示完整扩展名并核对文件来源，能够减少这种风险。

在 Linux 的程序执行接口中，扩展名不直接决定文件能否启动。普通文件通常需要相应用户拥有执行权限，所在文件系统还不能以 `noexec` 挂载，内容也必须是内核支持的可执行格式或带有有效解释器行的脚本。你可以用 `chmod u+x script.sh` 给所有者添加执行权限，用 `chmod u-x program` 移除该权限。扩展名仍会被编辑器、构建工具和桌面文件管理器用来推测文件类型，但没有扩展名的文件也可以是可执行程序；仅把任意文本文件设为可执行并不会让它变成有效程序。

这种设计把“文件名是什么”和“是否允许执行”分开：扩展名不会自动授予执行权限，可执行程序也不必遵循特定的命名约定。执行权限只是安全模型的一部分，用户仍需判断文件来源和程序行为。

Linux 常见的传统权限模型把访问者分为所有者（owner）、所属组（group）和其他人（others），每组分别设置读（r）、写（w）、执行（x）权限。你会看到类似 `-rwxr-xr-x` 的字符串，表示所有者可以读写执行，组成员和其他人可以读和执行但不能写。Linux 还支持 ACL、capability 等更细的机制；本书先从这组基础权限学起。

==== 软件安装：集中式包管理

Windows 软件可以通过安装程序、Microsoft Store、`winget` 等渠道安装，不同软件可能采用各自的更新与卸载方式。Linux 发行版则通常把大量软件集中到统一仓库中，由同一个包管理器负责安装、升级和卸载。

Ubuntu 使用 `apt`，Fedora 使用 `dnf`，Arch 使用 `pacman`。命令虽然不同，基本思路相近：从已配置的软件仓库获取软件包，依据元数据解析依赖，并集中处理更新。

```bash
# 安装软件（Ubuntu/Debian）
sudo apt install vlc

# 安装多个软件
sudo apt install git cmake build-essential

# 升级 apt 当前能够更新的已安装软件包
sudo apt update && sudo apt upgrade

# 卸载软件
sudo apt remove vlc

# 搜索软件
apt search video player
```

包管理器为仓库内的软件提供一致的操作方式。安装软件时，它会按软件包声明安装依赖；更新时，可以统一刷新索引并升级已安装的软件包；卸载时，它知道哪些文件由该软件包提供。用户配置、运行时数据和手工安装的文件不一定随卸载自动删除，因此排查残留时仍要区分文件来源。

官方仓库通常会对软件包元数据和发布文件进行签名，使包管理器能够验证下载内容是否来自已信任的软件源、传输后是否被篡改。这降低了误用非官方安装包的风险，但签名不代表软件没有漏洞；还需要及时安装安全更新，并谨慎添加第三方仓库。

当然，包管理器也有局限性。仓库中的软件可能不是最新版本（为了稳定性），某些专有软件可能不在仓库中。这时你可能需要添加第三方仓库、使用 Flatpak/Snap 等通用包格式，或者从源码编译。但对于大多数日常使用和开发需求，官方仓库中的软件已经足够。

==== 命令行文化

Windows 桌面长期以图形界面为主要入口，同时提供 cmd、PowerShell 和 Windows Terminal 等命令行工具。Linux 桌面也有完整的图形界面，但服务器管理和开发工具更常把命令行作为标准接口。

许多 Linux 服务器不安装图形界面，管理员通过 SSH 登录并使用命令行完成操作。在桌面 Linux 上，同一任务往往既能通过图形界面完成，也能通过命令行完成；后者尤其适合需要记录、重复或批量执行的操作。

命令行适合几类开发任务。首先是自动化：可以把一系列命令写成脚本，重复执行同一操作序列。其次是记录：命令能够分享和纳入版本控制；其他人还需要匹配软件版本、输入文件、权限与环境，才能复现相同结果。第三是远程操作：通过 SSH，可以用终端管理没有本地显示器的计算机。第四是参数控制：命令行工具常提供图形界面未暴露的选项，也便于保存本次调用的完整参数。

对于 RoboMaster 开发来说，命令行尤其常用。ROS 2 的工作空间管理、软件包构建和节点运行都有标准命令；Git 的命令行接口也能直接对应多数文档。GDB 可以单独使用，也可以作为 IDE 的调试后端。机器人没有连接显示器时，SSH 终端通常还是主要的远程排查入口。

命令行刚开始需要记忆一些名称和参数，但它的操作可以保存为脚本，也便于在不同机器上复现。多数基础命令在各 Linux 发行版之间相同，包管理和系统配置命令则可能随发行版而变化。

==== 系统配置：文本文件与注册表

Windows 使用注册表（Registry）保存许多系统和应用配置，也会使用普通文件、策略数据库和应用自己的数据目录。注册表是层次化数据库，可以通过系统设置、程序接口、命令或注册表编辑器管理；直接修改关键项前需要备份并确认影响范围。

Linux 中许多服务使用普通文本配置文件。系统级配置通常位于 `/etc`，用户配置常见于主目录中的隐藏文件或 `~/.config`。例如，Ubuntu 的网络配置可能位于 `/etc/netplan/`，SSH 服务器配置位于 `/etc/ssh/sshd_config`，Bash 的用户配置可写在 `~/.bashrc`。桌面环境和部分应用也会使用 dconf 或其他数据库，因此“配置都是文本文件”同样是一种概括。

文本配置可以使用普通编辑器查看和修改，也便于备份、比较差异和纳入版本控制。部分配置文件带有注释或示例，但并非所有选项都能脱离文档理解；把配置复制到另一台机器前，也要核对软件版本、文件权限和设备相关字段。

对于开发者来说，文本配置可以直接配合编辑器、差异比较、版本控制和模板工具；批量部署时，也可以由配置管理工具生成文件并设置权限。Windows 的注册表同样能够通过 PowerShell、组策略和程序接口自动化，两者的差异主要在数据表示、工具链和部署约定，而不是一方能否自动化。

文本配置也有相应代价：格式可能是 INI、YAML 或项目自定义语法，错误可能导致服务无法启动，配置还可能分散在多个位置。修改前需要查阅对应版本的文档，保留备份或版本记录，并使用软件提供的检查命令验证语法。

==== “一切皆文件”与统一接口

Unix/Linux 常用“一切皆文件”（Everything is a file）概括一种接口取向：许多资源可以通过路径或文件描述符访问，并使用打开、读取、写入和关闭等相近操作。它不是字面意义上的“所有对象都是普通文件”；例如套接字和管道可以使用文件描述符，却不一定对应普通文件路径，设备的控制功能也常需要专用接口。

许多硬件由 `/dev` 下的设备节点表示，例如磁盘可能对应 `/dev/sda`，USB 串口可能对应 `/dev/ttyUSB0`，摄像头可能对应 `/dev/video0`。程序可以对设备节点执行读写，但数据含义取决于设备协议、驱动状态、访问权限和参数配置。直接对串口运行 `cat` 只会读取当前配置下的原始数据，向设备重定向任意文本也不一定是有效命令，因此不能把它们当作所有设备通用的操作方法。

`/proc` 是内核提供的虚拟文件系统，其中按进程 ID 暴露进程状态，并提供一部分系统信息。例如，`/proc/1234/status` 显示进程 1234 的状态，`/proc/1234/cmdline` 保存其命令行，`/proc/1234/fd/` 列出它打开的文件描述符；`/proc/cpuinfo` 和 `/proc/meminfo` 则提供 CPU 与内存信息。`top`、`htop` 等监控工具会读取其中的数据，但也可能结合其他内核接口。

`/sys` 是另一套虚拟文件系统，用来描述设备、驱动和内核对象，并为部分属性提供读写入口。例如，某些电脑会在 `/sys/class/backlight/` 下提供亮度属性；具体目录名、可写范围和权限取决于硬件与驱动，写入前应先读取相邻属性并查阅对应文档。

这种设计的主要价值在于统一基本读写接口：数据通常通过打开、读写和关闭来传递，所以 `cat`、`echo`、重定向等通用工具能处理一部分设备与系统参数，Shell 脚本也能组合这些操作。不过，统一的主要是数据通道，设备配置和控制仍常需要专门接口：设置串口波特率要用 `stty` 或相应系统调用，相机取流则要使用 V4L2 接口。“一切皆文件”概括了常见的访问模型，并不表示设备的全部功能都等同于普通文件。

对 RoboMaster 开发来说，这套模型有直接用途：串口程序会打开对应设备节点，再通过 termios 等接口设置波特率和数据格式；相机程序可以打开 `/dev/video*` 并使用 V4L2 的系统调用读取帧和配置设备；监控工具则从 `/proc`、`/sys` 及其他接口收集状态。共同的文件描述符模型便于把这些数据源接入事件循环，但每类设备仍有自己的协议和控制方式。

==== 适应期的常见困惑

从 Windows 转向 Linux 时，有一些常见的困惑和问题。了解这些可以帮助你更快地适应。

“软件去哪里安装了？” 这是新手常问的问题。在 Windows 上，程序常见于下面两个目录：

- `C:\Program Files`
- `C:\Program Files (x86)`

Linux 通常不把一个软件的所有文件集中到单个目录中。通过包管理器安装后，文件会按用途分布：可执行文件可能在 `/usr/bin`，库在 `/usr/lib`，系统级配置在 `/etc`，文档在 `/usr/share/doc`。

日常运行时通常只需知道命令名。需要排查路径、配置或卸载问题时，可以分别使用下面两条命令：

```bash
command -v 命令名
dpkg -L 软件包名
```

前一条查找命令的位置，后一条列出某个 deb 包安装的文件。

“怎么运行程序？” 图形应用可以从桌面菜单启动；命令位于 `PATH` 中时，直接在终端输入名称即可。当前目录通常不在 `PATH` 中，所以运行其中的程序要写出路径，如 `./my_program`，这也避免 Shell 默认优先执行当前目录下的同名文件。程序还需要适合当前系统的格式和执行权限；对于可信的自有脚本，可以用 `chmod u+x script.sh` 给所有者添加权限，并确认解释器行正确。

“驱动程序在哪里下载？” Ubuntu 已随内核和软件仓库提供许多常见硬件的驱动，设备接入后可能会自动识别；是否可用仍取决于具体芯片、内核版本和所需功能。NVIDIA 显卡、部分 Wi-Fi 芯片和工业设备可能需要额外的软件包或厂商 SDK。遇到问题时，先用 `lspci`、`lsusb` 等命令确认准确型号，再核对 Ubuntu 与厂商的支持说明，不要只凭设备的商品名称下载脚本。

“为什么没有 C 盘？” 如前所述，Linux 不使用盘符。所有存储都挂载到目录树的某个位置。你的主目录 `/home/username` 就相当于 Windows 的 `C:\Users\Username`。外置驱动器通常挂载到 `/media` 或 `/mnt` 下。

“权限被拒绝是怎么回事？” Linux 和 Windows 都有访问控制机制，只是默认账户、权限表示和提权方式不同。Linux 报告 `Permission denied`，表示当前进程身份没有完成该操作所需的权限，也可能与目录权限、ACL、设备组或只读文件系统有关。安装系统软件等明确需要管理员权限的操作可以使用 `sudo`，如 `sudo apt install package`；不要在没有查明原因时给所有命令都加 `sudo`，否则可能扩大误操作范围并产生属于 root 的用户文件。

“命令记不住怎么办？” 许多标准命令和系统接口提供手册页，可以用 `man command` 查看，例如 `man ls`。不少命令支持 `command --help`，Bash 内置命令则可用 `help command`；具体程序也可能采用其他帮助选项。`tldr` 等工具提供面向常见用法的速查示例，但不能替代当前版本的正式文档。先熟悉高频命令，其余内容按需要查阅即可。

这些问题通常会随着实际使用逐步减少，但不必靠死记硬背解决。遇到不熟悉的命令时，先确认当前目录、用户身份和参数含义，再查看帮助与对应版本的文档；这种检查习惯比记住大量命令更重要。

=== Linux 发行版选择

前面几节我们一直在笼统地说“Linux”，但当你真的准备安装它时，第一个问题就来了：在搜索引擎里输入“Linux 下载”，你不会得到一个下载按钮，而是看到几百个陌生的名字——Ubuntu、Debian、Fedora、Arch、Manjaro……它们都自称是 Linux，却又各不相同。这一节就来解决这个选择困难：理解“发行版”是什么，认识几个主流家族，然后给出一个明确的结论——对于 RoboMaster 开发，你应该选什么。

==== 什么是发行版

严格来说，Linus Torvalds 和全球开发者维护的“Linux”是内核。内核负责管理内存、调度进程和驱动硬件，但它本身不是供普通用户直接使用的完整操作系统：Shell、系统服务、安装程序和应用软件需要由用户空间组件提供。发行版选择这些组件并完成集成、打包、安装程序和软件仓库，形成可以安装和维护的系统（distribution，常简称 distro）；图形界面则可以按用途选装。

一个发行版大致由四层构成：

- 发行版维护的 Linux 内核，负责调度、内存管理和硬件接口；不同发行版会选择不同版本、配置和补丁。
- GNU 工具及其他系统软件，构成用户空间的基础；Ubuntu 中常见的 Bash、GCC 和 GNU coreutils 属于这一层，也可能同时使用来自其他项目的实现。
- 桌面环境（desktop environment），提供窗口、任务栏、文件管理器等图形界面，这一层甚至是可选的——服务器上通常根本不装。
- 包管理器和软件仓库，负责软件的安装与更新，前面介绍过的 `apt` 就是 Ubuntu 的包管理器。

不同发行版的差异，主要体现在这几层的选择和默认配置上：用哪个包管理器、预装哪个桌面、软件版本是追新还是求稳、由商业公司维护还是社区维护。

多数发行版共享 Linux 内核接口和大量基础软件，因此命令行、路径、进程和权限等概念能够迁移；内核版本、包管理器、默认配置、发布周期和支持范围则可能明显不同。第一次选择发行版不需要覆盖所有未来需求，但应优先匹配课程、ROS 版本和硬件厂商明确支持的环境。

==== 主流发行版家族

许多发行版是在其他发行版基础上发展而来的，因此可以按上游来源分成几个主要家族。同一系列通常共享包管理器、软件包格式和大量基础设施；这会直接影响安装命令和软件包名称。

- Debian 系广泛用于服务器和桌面。Debian 重视稳定性与自由软件政策；Ubuntu 基于 Debian，由 Canonical 公司维护，更强调固定发布周期、桌面易用性和商业支持；Linux Mint 又基于 Ubuntu，默认界面更接近传统桌面布局。这一系使用 deb 软件包，常用 `apt` 管理。
- Red Hat 系以企业和社区发行版的协作为特点。Fedora 较早引入新技术，部分成果随后进入 RHEL（Red Hat Enterprise Linux）；CentOS Linux 曾提供与 RHEL 高度兼容的重构版本，后来项目重心转为位于 Fedora 与 RHEL 之间的 CentOS Stream。这一系使用 rpm 软件包，Fedora 等发行版常用 `dnf` 管理。
- Arch 系采用滚动更新（rolling release），软件持续更新而不是按固定大版本整体升级。Arch Linux 提供较少默认配置，安装和维护过程要求用户主动选择组件；Manjaro 等发行版在其基础上提供预配置环境。这一系使用 `pacman` 包管理器。

家族的划分还有一个很实际的意义：网上的教程和命令不能跨家族照抄。`apt`、`dnf`、`pacman` 的命令语法互不通用，软件包的名字在不同家族里也常常不同。以后你搜索资料时，要认准“Ubuntu”或“Debian”字样的教程；如果一篇文章通篇在用 `pacman`，那它是写给 Arch 用户的，直接照抄多半会报错。

在机器人开发这个具体场景中，ROS 官方长期把特定 Ubuntu LTS 版本列为一级支持平台，许多第三方软件包和教程也按 Ubuntu 编写。Fedora、Arch 等系统并非不能使用 ROS，但可用的二进制包、安装步骤和社区测试范围不同，使用者往往需要自行处理更多版本与依赖问题。

==== Ubuntu 与 RoboMaster 开发生态

Ubuntu 在机器人开发中的普及与 ROS 的发布策略直接相关。ROS 2 的每个发行版都会列出一级支持平台（Tier 1），其中通常包含一个对应的 Ubuntu LTS 版本；核心软件会在这些平台上持续集成、测试并发布二进制包，官方安装文档也可直接使用 `apt`。其他发行版可能通过社区软件包、容器或源码构建使用 ROS，但维护范围和步骤需要另行确认。

对新手来说，Ubuntu 的图形安装程序、硬件检测和驱动管理工具也比较完整。它的用户数量较多，常见错误通常能在官方文档、论坛或中文资料中找到讨论。搜索解决方案时仍要核对 Ubuntu 版本、硬件型号和命令作用范围，但有足够多的可参考案例会明显降低入门成本。

RoboMaster 社区中的不少开源视觉项目、教程和队内环境脚本也默认使用 Ubuntu。NVIDIA Jetson 的 JetPack 以 Ubuntu 为基础，Intel NUC 这一类小型主机（下文简称 NUC）和工控机同样可以安装 Ubuntu。开发机、仿真环境和车载平台采用相近的发行版与依赖版本，能减少配置差异；处理器架构和硬件驱动仍需分别验证。

==== 版本怎么选：22.04 还是 24.04

选定了 Ubuntu，还要选版本。Ubuntu 的版本号就是发布时间：22.04 表示 2022 年 4 月发布，24.04 表示 2024 年 4 月发布。每两年的 4 月会发布一个长期支持版本（Long Term Support，LTS），标准支持周期通常为五年；非 LTS 版本通常只有九个月。五年并不表示所有仓库组件、第三方驱动和应用都获得完全相同的安全维护，具体包仍要查看其仓库来源和支持状态。对于本书这种需要长期统一 ROS 与硬件环境的课程，优先选择仍在支持期内的 LTS。

每个版本还有一个代号，22.04 叫 Jammy Jellyfish，24.04 叫 Noble Numbat。这不只是趣闻：以后你在软件源配置和各种报错信息里会频繁看到 `jammy` 这个词，它指的就是 22.04。看到代号能对上版本号，很多资料读起来就不迷糊了。

ROS 版本是选择 Ubuntu 版本时的重要约束：ROS 2 Humble 的 Ubuntu 一级支持平台是 22.04，ROS 2 Jazzy 对应 24.04。官方提供的 deb 二进制包按这些平台构建，在其他 Ubuntu 版本上不能直接假定兼容。因此，系统版本、ROS 发行版和主要依赖应作为一组共同确定。

本书推荐并统一使用 Ubuntu 22.04 LTS，主要依据是与教学所用软件版本的对应关系：

- 本书采用 ROS 2 Humble，其官方 Ubuntu deb 软件包面向 22.04 构建；选择对应平台可以直接遵循官方安装路径。
- JetPack 6 的目标系统以 Ubuntu 22.04 用户空间为基础，开发机使用 22.04 可以减少部分包名和系统配置差异；Jetson 的 ARM64 架构、Jetson Linux、CUDA 和驱动版本仍需按 JetPack 支持矩阵处理。
- 工业相机 SDK、GPU 工具链和其他厂商软件往往只列出特定发行版与版本。选定 22.04 后仍要逐项核对准确设备型号、SDK、内核和处理器架构，不能由“同为 Ubuntu”推断兼容。

Ubuntu 公布的 22.04 标准支持周期持续到 2027 年 4 月，ROS 2 Humble 公布的维护周期也延续到 2027 年。本书因此以这组版本为统一环境；实际使用前仍应查看 Ubuntu 和 ROS 2 的当前支持页面，并检查关键软件包是否在相应维护范围内。接近或超过期限时，应规划迁移，而不是长期停留在停止维护的版本上。

如果队伍已经整体迁移到 24.04 和 ROS 2 Jazzy，应优先遵循队内经过验证的版本组合。统一的环境更便于复现问题和共用脚本，但仍要记录机器之间的硬件与驱动差异。

==== 桌面环境：用默认的就好

桌面环境是发行版之上另一个可选项，最常见的有三个：

- GNOME 是 Ubuntu 的默认桌面，界面现代简洁，交互逻辑接近 macOS，也是本书后续章节默认使用的环境。
- KDE Plasma 布局更接近 Windows，并提供丰富的外观与交互配置选项。
- XFCE 轻量省资源，界面朴素，适合老旧机器或性能紧张的场合。

Ubuntu 可以安装不同桌面，也有 Kubuntu（KDE Plasma）和 Xubuntu（XFCE）等官方变体。它们共享 Ubuntu 的基础、包管理方式和主要软件仓库，但默认桌面、预装软件、配置与部分发布镜像不同，差异并不限于图形界面本身。

本书建议初学者先使用 Ubuntu 默认的 GNOME，以便界面和后续截图保持一致。桌面环境会影响资源占用和操作入口，但终端、编译与 ROS 的核心概念基本不依赖某一种桌面。车载 NUC 为节省资源，也可能安装不带桌面的 Ubuntu Server，并通过 SSH 远程维护。因此，先熟悉命令行比反复更换桌面环境更有助于后续开发。

=== 安装 Linux 的方式

选定了 Ubuntu 22.04 LTS，下一个问题是：把它装在哪里？主流方案有三种——双系统、虚拟机、WSL。它们不是三个竞争者里挑一个最好的，而是对应不同的使用阶段和场景。这一节讲清楚每种方案的原理、优劣和适用人群，帮你做出决策；具体的安装操作留到下一章。

这个选择并非不可更改。三种方式可以共存在同一台电脑上；在虚拟机里学到的通用命令和源代码通常也能迁移到原生 Ubuntu，但涉及设备、驱动、网络和性能的配置需要重新验证。可以先按当前学习目标和硬件条件选择，再随着需求调整。

==== 双系统：适合直接访问硬件的方案

双系统（dual boot）是在同一台电脑上安装 Windows 和 Linux：为 Ubuntu 分配独立分区，开机时由引导加载程序（bootloader）提供系统选择。两个系统的主要文件位于各自分区，但可能共用 EFI 系统分区，也能在一定条件下访问对方的数据分区，因此不能理解为完全互不影响。同一时刻只运行其中一个系统，切换需要重启。

双系统中的 Ubuntu 直接运行在物理硬件上，没有常规桌面虚拟机的设备转发层，可以使用系统识别到的 CPU、内存、USB、串口和 GPU。工业相机需要持续的 USB 3.0 带宽，电控联调依赖串口，CUDA 推理则依赖 NVIDIA 驱动，这些任务通常更适合原生系统。开发机与车载 NUC 都使用 Ubuntu 时，软件环境较为接近，但硬件、架构和驱动差异仍可能影响部署结果。

代价是安装门槛和切换成本。安装时需要调整分区和启动项，部分机器还要按厂商说明处理存储控制器或磁盘加密；操作失误、断电或选错磁盘都可能造成数据损失。因此，动手前必须备份并确认恢复密钥、目标磁盘和回滚路径，下一章会逐步说明。日常使用中，重启切换系统也不如虚拟机方便。

双系统适合需要长期连接工业相机、串口或本机 GPU 的开发者。如果电脑有两个硬盘位，可以把 Ubuntu 的主要分区安装在单独硬盘上，减少调整 Windows 数据分区的操作；两个系统仍可能共享或修改 EFI 启动配置，安装前同样需要备份并确认目标磁盘。

==== 虚拟机：适合入门和隔离实验

虚拟机（virtual machine）由虚拟化软件向 Ubuntu 提供虚拟 CPU、内存、磁盘、网卡和显卡，使它能作为 Windows 中的客户机运行。常见桌面软件包括 VMware Workstation 和 VirtualBox；Ubuntu 看到的是由虚拟机软件提供的一组虚拟硬件。

虚拟机的系统磁盘通常保存在独立文件中，配置出错后可以重建；快照（snapshot）还能记录某一时刻的虚拟磁盘和可选的运行状态，便于恢复实验环境。快照依赖原虚拟磁盘，不等同于独立备份；共享目录、直通设备和剪贴板也会跨越隔离边界。它的另一项便利是两个系统可以同时运行，适合在 Windows 中阅读资料并在 Ubuntu 窗口中练习。

虚拟机需要与 Windows 分配 CPU 和内存，物理内存较少时容易同时影响宿主和客户机。常规桌面虚拟机通常只提供功能有限的虚拟显卡，不直接提供完整 CUDA 能力；USB 设备则依赖转发机制，高带宽工业相机可能出现吞吐下降、延迟波动、丢帧或兼容问题。因此，它适合命令行学习和不依赖真实设备的开发，硬件与时序结果不能直接代表车载环境。

虚拟机适合两类场景：一是暂时不调整物理磁盘，先学习 Linux 和开发工具；二是在不含重要数据的虚拟磁盘中练习分区或系统配置。熟悉终端和 Shell 后，如果项目需要工业相机、串口或 GPU，再迁移到原生 Ubuntu 即可。

==== WSL：Windows 里的 Linux 命令行

WSL（Windows Subsystem for Linux）是微软提供的 Windows 功能。WSL 2 通过轻量虚拟机运行 Linux 内核和发行版，并提供 Windows 与 Linux 文件系统互访，适合在 Windows 中直接使用 Linux 命令行工具。

WSL 2 可以编译代码、运行脚本并练习本书的大部分基础命令。配合 VS Code 的 WSL 扩展，可以从 Windows 界面编辑和调试 Linux 侧程序。文件放置位置、Windows 与 WSL 版本以及项目规模会影响构建性能，不能只凭简单示例判断大型工程的速度。

WSL 2 对硬件和图形程序的支持与原生 Ubuntu 不同。较新的版本提供 WSLg，也能借助 `usbipd-win` 等工具连接部分 USB 设备；但工业相机和 USB 转串口的兼容性、带宽、时序与长期稳定性必须按具体设备测试，不能假定与原生系统一致。

网络也需要单独确认。WSL 的 NAT、镜像网络等模式会随 Windows 与 WSL 版本而变化。ROS 2 通过 ROS 中间件抽象层（ROS Middleware，RMW）接入数据分发服务（Data Distribution Service，DDS）等具体实现；常见 DDS 发现配置会使用组播和若干通信端口。WSL 与车载 NUC 能否互相发现，取决于网络模式、防火墙、RMW/DDS 实现及其配置，应通过实际节点通信验证。

因此，WSL 适合 Linux 基础学习、C++ 构建和部分离线开发。涉及工业相机、串口、ROS 2 多机通信和整车时序时，应在经过验证的原生 Linux 或实际车载计算平台上完成最终测试。

==== 我们的推荐

综合以上对比，本书后续硬件联调默认使用原生安装的 Ubuntu 22.04 LTS，双系统是保留 Windows 时的一种常见方式。它不是所有人的唯一方案；可以根据磁盘条件、设备访问和当前学习阶段这样决策：

- 需要长期连接相机、串口和 GPU，且设备允许调整磁盘：可以使用双系统，并在操作前完成备份。
- 暂时不适合调整物理磁盘，主要目标是学习命令行和开发工具：先使用虚拟机，硬件联调时再转到原生环境。
- 主要使用 Windows，只需要 Linux 工具链完成构建或离线实验：可以使用 WSL，并单独验证设备与网络需求。

无论选哪条路径，都要先考虑备份和资源需求。涉及分区前，应把重要资料备份到独立设备或可靠的远程存储。若条件允许，可以为 Ubuntu 预留 100 GB 以上空间，因为 ROS、数据集、日志和编译产物会持续增长。16 GB 内存通常比 8 GB 更适合编译大型项目和运行仿真。带 NVIDIA 独立显卡的机器还需要配置匹配的驱动，下一章会说明具体步骤。

队伍还应记录并尽量统一系统、ROS、编译器和主要依赖版本。这样更容易复现问题、复用脚本，并在设备故障时迁移开发环境；硬件型号、驱动和本地配置等差异也要一并记录，不能只写“都是 Ubuntu”。

确定方案后，下一章“环境准备”将从备份数据和制作启动盘开始，逐步介绍双系统安装，并说明虚拟机与 WSL 的配置方法和能力边界。
