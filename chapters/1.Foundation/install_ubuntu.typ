上一章的结尾我们已经把双系统、虚拟机和 WSL2 三种方案各自的取舍讲清楚了，这里不再重复选型的论证——本章是纯粹的动手环节：把一个能用于 RoboMaster 视觉开发的 Ubuntu 环境，从下载镜像开始一步一步装到能写代码为止。

本章以 Ubuntu 22.04 LTS 为准，因为它是 ROS 2 Humble 官方绑定的系统版本，也是目前各战队车载计算平台上最常见的选择。双系统是本章的主力方案，写得最细；虚拟机和 WSL2 各有一节，讲清楚安装方法和能力边界。无论你选了哪种方案，本章最后两节“装机后的基础配置”和“开发环境搭建”都同样适用——那才是你每天真正生活在里面的环境。

=== 安装前的准备

安装操作系统不是点几下“下一步”就完事的活，它涉及磁盘分区、固件设置这些一旦出错就可能丢数据的操作。好消息是，只要准备工作做足，整个过程其实相当安全。本节把动手前需要完成的几件事讲清楚：检查硬件、下载并校验镜像、制作 U 盘启动盘、备份数据并调整 Windows 设置、检查 BIOS/UEFI 选项。这些步骤看起来琐碎，但每一步都在为后面的顺利安装排雷——安装翻车的案例里，绝大多数问题都能追溯到准备阶段的某个疏忽。

==== 检查你的硬件

先花五分钟弄清楚你要往什么机器上装系统。打开 Windows 的“设备管理器”（右键开始菜单即可找到），重点看三样东西。

第一是内存和磁盘。内存 8 GB 是底线，16 GB 才谈得上舒适——编译 C++ 工程和跑仿真都是内存大户。磁盘方面，确认剩余空间够压缩出 100 GB 给 Ubuntu；顺便在“磁盘管理”里看一眼你有几块物理硬盘，如果笔记本有两块盘（比如一块装 Windows 的固态加一块空闲的固态），把 Ubuntu 装到单独的盘上是最干净的方案，两个系统的引导互不纠缠。

第二是显卡。在设备管理器的“显示适配器”里，如果能看到 NVIDIA 字样的独立显卡，你的机器就具备跑 CUDA 推理的条件，同时也意味着安装时大概率要过一遍本章后文会讲的 nomodeset 关卡。如果只有 Intel 或 AMD 的核芯显卡，系统照装不误、桌面反而更顺滑，只是神经网络推理与你无缘，跑传统视觉算法没有问题。

第三是无线网卡，这是最容易被忽略的一项。很新的笔记本（尤其 2023 年之后的型号）搭载的部分新款 Wi-Fi 网卡，在 Ubuntu 22.04 自带的内核里可能还没有驱动，装完系统连不上网、而装驱动又需要联网，陷入死循环。破局的办法有两个：一是后文 Live 环境试驾时专门测一下 Wi-Fi，提前发现问题；二是记住*手机 USB 网络共享*这个万能应急招——用数据线连接手机，在手机上打开“USB 网络共享”，Ubuntu 会把它识别成一块即插即用的有线网卡，任何驱动都不需要。这一招在赛场上没有 Wi-Fi 时也同样救命。

==== 下载镜像与校验

操作系统的安装文件以镜像（image）的形式发布，通常是一个 `.iso` 文件。你可以把它理解为一张“光盘的完整快照”——虽然现在已经没人用光盘了，但这个格式保留了下来，写入 U 盘后就能引导计算机进入安装程序。

版本选择上，认准 *Ubuntu 22.04 LTS 桌面版（Desktop，amd64 架构）*。LTS 是 Long Term Support（长期支持）的缩写，这类版本每两年发布一次，官方提供五年的更新维护。不要贪新去装最新的非 LTS 版本——ROS 2 的每个发行版都严格绑定一个 Ubuntu 版本，Humble 对应 22.04，装错了系统版本，后面装 ROS 时会寸步难行。整个战队统一系统版本还有一个现实好处：你在自己笔记本上编译通过的代码，拿到车载 NUC 上就是同一套依赖环境，不会出现“我这能跑你那不能跑”的扯皮。

下载渠道建议用国内镜像站，比如清华大学 TUNA 镜像站或中科大镜像站，速度比官网快一个数量级。在浏览器中打开 TUNA 的 Ubuntu 发布目录：

```text
https://mirrors.tuna.tsinghua.edu.cn/ubuntu-releases/22.04/
```

下载其中的 `ubuntu-22.04.5-desktop-amd64.iso`（小版本号可能随时间更新，选页面上最新的即可），文件大小约 4.7 GB。

下载完成后，花一分钟做校验（checksum verification）。镜像在下载过程中可能因为网络波动产生损坏，损坏的镜像会导致安装进行到一半时莫名报错——那时候你很难想到问题出在镜像本身，可能会浪费一下午反复重装。校验的原理是计算文件的 SHA256 哈希值，与官方公布的值比对，只要有一个比特不同，哈希值就会完全不一样。镜像站的同一目录下有一个 `SHA256SUMS` 文件，里面记录了官方哈希值。

如果你在 Linux 或 macOS 上下载的镜像，校验命令如下：

```bash
# 在镜像所在目录执行，计算文件的 SHA256 哈希值
sha256sum ubuntu-22.04.5-desktop-amd64.iso
# 输出形如：
# 一长串十六进制字符  ubuntu-22.04.5-desktop-amd64.iso
# 与 SHA256SUMS 文件中对应行比对，完全一致即为完好
```

如果你在 Windows 上下载（大多数新队员的情况），用系统自带的 `certutil` 工具，在 PowerShell 或命令提示符中执行：

```text
certutil -hashfile ubuntu-22.04.5-desktop-amd64.iso SHA256
```

把输出的哈希值与官方值逐段比对（看开头和结尾各几位一般就够了，哈希值不存在“大部分相同”的情况）。校验通过，镜像这一环就万无一失了。

==== 制作 U 盘启动盘

计算机默认从硬盘启动，要运行安装程序，需要一个可引导的安装介质——也就是启动盘（bootable USB）。你需要一个容量 8 GB 以上的 U 盘，速度越快安装越流畅。

*警告：制作启动盘会清空 U 盘上的全部数据。* 动手前把 U 盘里的文件拷走，确认无误再继续。

推荐使用 Ventoy 制作启动盘，它的思路和传统工具完全不同：传统工具（如 Rufus）把镜像“解开”写入 U 盘，一个 U 盘只能装一个系统的安装器，换镜像要重新制作；Ventoy 则是先在 U 盘上安装一个小巧的引导环境，之后你只需要*把 ISO 文件当普通文件拷进 U 盘*，开机时 Ventoy 会扫描盘里所有的镜像文件并弹出菜单让你选。这意味着一个 U 盘可以同时放 Ubuntu 22.04、Ubuntu 20.04、Windows 安装镜像和急救工具，剩余空间还能正常当 U 盘用。对于经常要给队里新机器、新 NUC 装系统的视觉组来说，人手一个 Ventoy 盘非常省事。

Ventoy 的使用步骤：在官网 `https://www.ventoy.net` 下载 Windows 版压缩包，解压后运行 `Ventoy2Disk.exe`，在设备列表里*仔细确认选中的是你的 U 盘*，点击“安装”。完成后 U 盘会出现一个大的空白分区，把下载好的 ISO 文件直接拖进去即可。

如果你更喜欢传统方式，Rufus 也完全可用：在 `https://rufus.ie` 下载，选中 U 盘和 ISO 文件，分区类型选 *GPT*、目标系统类型选 *UEFI（非 CSM）*——现在的电脑几乎都是 UEFI 固件，这样设置兼容性最好——然后开始写入。

如果你手头已经有一台 Linux 机器（比如用队里的旧设备制作启动盘），可以用 `dd` 命令直接写入：

```bash
# 先列出所有磁盘设备，确认 U 盘的设备名
lsblk
# 假设 U 盘是 /dev/sdb（注意是整块盘 sdb，不是分区 sdb1）
sudo dd if=ubuntu-22.04.5-desktop-amd64.iso of=/dev/sdb bs=4M status=progress conv=fsync
```

*警告：`dd` 是极其危险的命令，`of=` 参数写错一个字母，就会把你的硬盘直接覆盖掉，且无法恢复。* 执行前务必用 `lsblk` 根据容量确认设备名，看清楚哪个是 8 GB 的 U 盘、哪个是 512 GB 的系统盘。不确定就不要执行，改用 Ventoy。

==== 备份数据与 Windows 侧准备

接下来要在 Windows 这一侧做三件事：备份、解除磁盘保护、腾出磁盘空间。

*备份是不可跳过的一步。* 双系统安装涉及压缩现有分区、写入新的引导程序，正常操作下 Windows 的数据不会受影响，但分区表操作一旦出错（断电、误删分区、写错设备），数据恢复的代价极高。把课程资料、代码、照片这些不可再生的文件拷贝到移动硬盘或网盘上。代码类的东西，最好的备份方式是推送到远程 Git 仓库——这也是我们在 Git 版本控制一章会反复强调的习惯。

第二件事是处理 BitLocker。较新的 Windows 笔记本往往默认开启了 BitLocker 磁盘加密，而修改分区或 BIOS 设置可能触发它的恢复模式——届时不输入恢复密钥就进不了 Windows。在“设置”中搜索“BitLocker”，要么直接关闭它（解密需要一些时间），要么至少登录微软账户把恢复密钥抄下来存到手机里。很多双系统翻车的求助帖，最后卡住的都是这一步。

第三件事是关闭 Windows 的“快速启动”（Fast Startup）。这个功能的原理是关机时不真正关机，而是把系统状态写入休眠文件，下次开机直接恢复。问题在于：休眠状态下 NTFS 分区处于“被锁定”的中间态，Ubuntu 这边挂载它时只能以只读方式打开，强行写入甚至可能损坏文件系统。在“控制面板 → 电源选项 → 选择电源按钮的功能”中，取消勾选“启用快速启动”。

最后腾出磁盘空间。在 Windows 中右键“此电脑”，选择“管理 → 磁盘管理”，右键一个剩余空间充足的分区（通常是 D 盘或 C 盘），选择“压缩卷”，压缩出至少 100 GB 的未分配空间。这一步在 Windows 里做而不是留到 Ubuntu 安装器里做，是因为 NTFS 是微软自家的文件系统，让 Windows 自己缩小自己的分区最稳妥。压缩完成后，磁盘管理里会出现一段黑色的“未分配”空间——记住它的大小，稍后在 Ubuntu 安装器里就靠这个数字认出它。

==== BIOS/UEFI 设置要点

BIOS/UEFI 是主板上的固件（firmware），负责在操作系统接管之前初始化硬件、决定从哪个设备启动。严格来说现代电脑用的都是 UEFI，BIOS 是它的上一代技术，但日常口语里大家仍混着叫。进入固件设置界面的方法是开机瞬间反复按特定按键——不同品牌不一样，常见的有 `Del`（台式机、组装机）、`F2`（联想、戴尔、华硕笔记本）、`F10`（惠普）。如果抓不住时机，可以在 Windows 中按住 Shift 点“重启”，进入高级启动菜单后选择“疑难解答 → 高级选项 → UEFI 固件设置”。

进入设置界面后，重点检查两项。第一是*关闭 Secure Boot（安全启动）*。Secure Boot 要求启动链上的每个组件都有可信签名，Ubuntu 本身是支持它的；用 `ubuntu-drivers` 从官方源装的 NVIDIA 驱动带有 Canonical 预签名，开着 Secure Boot 通常也能正常加载。但只要走出这条最优路径——手动 `.run` 安装、DKMS 编译、自定义内核——就会撞上晦涩的 MOK 签名注册关卡。视觉组的机器几乎必然要折腾 NVIDIA 驱动，新手提前关闭 Secure Boot，等于把这一整类坑提前排除。它一般在“Security”或“Boot”选项卡下。

第二是*关闭 Fast Boot*（注意这是 BIOS 里的选项，与上一小节 Windows 系统里的“快速启动”是两回事）。Fast Boot 会跳过开机时的部分硬件自检和 USB 设备枚举，可能导致按不出启动菜单、认不到 U 盘。

顺便记下你机器的*启动菜单（Boot Menu）快捷键*，常见的有 `F12`（联想、戴尔）、`F11`（微星）、`Esc` 或 `F9`（惠普）。它的作用是开机时临时选择从哪个设备启动，比进 BIOS 改启动顺序方便得多，下一节马上要用。

还有一个较少遇到但值得知道的坑：部分笔记本的硬盘控制器默认工作在 Intel RST/VMD 模式下，Ubuntu 安装器会看不到硬盘。解决办法是在 BIOS 里把硬盘模式改为 AHCI。*警告：直接修改这个选项会导致已装好的 Windows 蓝屏无法启动*，需要先在 Windows 里设置下次以安全模式启动，改完 BIOS 后进一次安全模式让 Windows 重建驱动配置，再恢复正常启动。如果安装器能正常看到硬盘，就完全不用理会这一段。

=== 双系统安装实操

双系统（dual boot）是把 Ubuntu 和 Windows 安装在同一台电脑上，开机时通过引导菜单选择进入哪个系统。它是视觉组的主力方案，原因很直接：Ubuntu 独占全部硬件性能，USB 3.0 工业相机能跑满带宽，NVIDIA 显卡能原生跑 CUDA 推理，串口设备即插即用——这些恰好都是虚拟机和 WSL2 做不到或做不好的。代价是需要动真实磁盘的分区，所以上一节的准备工作请务必完成。本节从插上 U 盘开始，走完整个安装流程。

==== 从 U 盘启动进入 Live 环境

插上启动盘，开机时按住上一节记下的启动菜单快捷键，在弹出的设备列表里选择你的 U 盘——通常显示为“UEFI: 某某品牌 USB”字样，*认准带 UEFI 前缀的那一项*。如果用的是 Ventoy，接下来会出现 Ventoy 的蓝色菜单，选中 `ubuntu-22.04.5-desktop-amd64.iso`，以普通模式（Boot in normal mode）启动。随后出现 GRUB 菜单，选择第一项“Try or Install Ubuntu”。

顺利的话，一两分钟后你会进入一个完整的 Ubuntu 桌面——但先别急着找安装图标。此刻你处在所谓的 Live 环境中：整个系统从 U 盘加载、运行在内存里，*没有碰你的硬盘分毫*。这是一个绝佳的“试驾”机会：连一下 Wi-Fi 看网卡是否被识别，动动触摸板，看看屏幕分辨率是否正常。如果某个关键硬件在 Live 环境里就不工作，装完系统它多半也不会工作，此时退出重启不会留下任何痕迹，你可以先去搜索对应型号的解决方案。

建议在 Live 环境里先把 Wi-Fi 连上再开始安装。联网状态下，安装器可以顺带下载更新和第三方驱动，能省去装完之后的不少配置。确认无误后，双击桌面上的“Install Ubuntu 22.04.5 LTS”图标，正式开始安装。

==== NVIDIA 显卡的黑屏坑：nomodeset

不少同学在上一步就翻车了：GRUB 菜单选完“Try or Install Ubuntu”，屏幕一黑再也没亮过，或者花屏、卡死在紫色画面。如果你的电脑带 NVIDIA 独立显卡（视觉组的开发机基本都带），大概率就是撞上了这个经典坑。

原因在于驱动。Live 环境里没有 NVIDIA 官方驱动，系统使用开源社区逆向开发的 nouveau 驱动来点亮显卡，而 nouveau 对较新的 NVIDIA 显卡支持很不完善。系统启动早期有一个叫内核模式设置（Kernel Mode Setting，KMS）的机制，由驱动直接接管显示模式，nouveau 在这一步出错，屏幕就黑了。解决思路是加一个内核启动参数 `nomodeset`，告诉内核跳过这一机制，先用最保守的基础显示模式把桌面点亮——分辨率可能不对、界面可能卡顿，但足够完成安装。

具体操作：重启回到 GRUB 菜单，*高亮“Try or Install Ubuntu”但不要按回车，而是按 `e` 键*进入编辑模式，找到以 `linux` 开头的那一行，在 `quiet splash` 之后加上一个词：

```text
# 编辑前（行内其余内容保持原样）：
linux /casper/vmlinuz ... quiet splash ---
# 编辑后：
linux /casper/vmlinuz ... quiet splash nomodeset ---
```

然后按 `F10`（或 `Ctrl+X`）启动。这个修改是一次性的，只对本次启动生效。

记住这个技巧，它马上还会用到一次：装完系统后的第一次开机，系统里同样还没有 NVIDIA 驱动，同样可能黑屏，同样在 GRUB 里按 `e` 加 `nomodeset` 解决。等到后面“装机后的基础配置”一节装好官方驱动，这个参数就永远不需要了。

==== 分区方案

安装器一路走到“安装类型”这一步，才是双系统安装真正的关键决策点。如果安装器检测到了 Windows，会提供“与 Windows Boot Manager 共存安装”的自动选项——它能用，但它会自动决定分区大小和位置，出了问题你都不知道它做了什么。我们建议选择最下面的*“其他选项”（Something else）*，手动规划分区。这多花五分钟，换来的是你清楚地知道自己的磁盘上每一块空间是干什么的——对一个未来要管理车载 NUC 的视觉队员来说，这五分钟本身就是必修课。

先补两个概念。分区（partition）是把物理磁盘划分成的逻辑区域，每个分区可以有自己的文件系统。挂载点（mount point）是这个分区在 Linux 目录树中出现的位置——上一章讲过，Linux 没有盘符，一切都挂在以 `/` 为根的目录树上。我们要做的就是在那段“未分配空间”（安装器里显示为“空闲”，free space）上新建分区，并指定各自的挂载点。

推荐的分区方案如下：

- *EFI 系统分区*（EFI System Partition）：如果 Ubuntu 与 Windows 装在*同一块硬盘*上，不需要新建——Windows 已经建好了一个 EFI 分区（通常 100 到 500 MB，FAT32 格式），安装器会自动识别并复用它，你只需要确认后面“安装启动引导器的设备”选的是这块盘。如果 Ubuntu 装在*单独的一块新硬盘*上，则新建一个 512 MB 的分区，用途选“EFI 系统分区”。
- *根分区* `/`：主力分区，文件系统选 ext4，挂载点填 `/`，大小建议 100 GB 起步，空间充足就再给多些。系统本身只占十几 GB，但 ROS 2、CUDA、各类开发库会吃掉几十 GB，训练数据集和调车录制的 rosbag 更是空间大户——一场校内赛录下来的包就可能有几十 GB。根分区装满的系统会出各种诡异的毛病，宁大勿小。
- *交换空间*（swap）：内存不够用时系统把暂时不活跃的内存页挪到磁盘上的区域。如果你的内存在 16 GB 及以下，建一个 16 GB 的交换分区（用途选“交换空间”）；要注意，走“其他选项”手动分区时安装器*不会*自动创建任何交换空间；这里没建的话，装完系统后可以随时用交换文件（swapfile）补上——`sudo fallocate -l 8G /swapfile && sudo chmod 600 /swapfile && sudo mkswap /swapfile && sudo swapon /swapfile`，再写进 `/etc/fstab` 永久生效，效果与交换分区相同且大小可调。编译大型工程（比如从源码编译 OpenCV）时内存峰值很高，有交换空间兜底可以避免编译进程被系统杀掉。

*警告：全程绝对不要格式化已有的 EFI 分区。* 它里面存着 Windows 的引导文件，勾选了“格式化”，装完 Ubuntu 你会发现 Windows 从启动菜单里消失了——引导文件被清空，修复起来相当麻烦。复用 EFI 分区时，挂载点是 `/boot/efi`，格式化选项保持不勾选。

*警告：新建分区前，反复确认你操作的是那段“空闲”空间*，认准大小与你在 Windows 磁盘管理里压缩出来的数字一致。Linux 下磁盘设备名和 Windows 完全不同：NVMe 固态盘叫 `nvme0n1`，它的分区叫 `nvme0n1p1`、`nvme0n1p2`；SATA 盘叫 `sda`，分区叫 `sda1`、`sda2`。删除或格式化错分区没有后悔药，这也正是前面强调备份的原因。

你可能注意到我们没有单独划分 `/home` 分区（存放用户个人文件的目录）。单独分 `/home` 的好处是重装系统时个人文件可以原地保留，代价是要提前预估两个分区各自的大小，估错了调整起来很麻烦。对新手，我们建议从简：只分根分区，个人文件的安全交给 Git 远程仓库和日常备份。等你对自己的空间使用模式有了概念，下次装机再做精细规划不迟。

分区表设置完毕后，检查页面底部“安装启动引导器的设备”：选择 EFI 分区所在的那块磁盘（整块盘，如 `nvme0n1`，而不是某个分区）。确认无误，点击“现在安装”，安装器会弹出分区变更摘要让你最后确认一遍——这是撤销的最后机会，仔细读一遍再点继续。

==== 收尾与重启

剩下的步骤就轻松了。时区地图上点选上海；键盘布局选 English (US)——写代码用的都是英文键盘布局，中文输入的问题交给后面装的输入法解决。安装选项里选“正常安装”，并勾选“为图形或无线硬件，以及其它媒体格式安装第三方软件”——这一项会顺带安装一些闭源驱动和多媒体解码器，联网状态下勾上能省不少事。

接着设置用户名、密码和主机名（hostname）。用户名全小写、不含空格和中文，简短好输入——它会出现在你之后敲的每一条命令提示符里。主机名是这台机器在网络上的名字，起个有意义的：自己的笔记本可以叫 `名字-laptop`，车载机器建议统一命名，比如 `nuc-hero`、`nuc-sentry`——之后你会经常在终端里同时 SSH 连接好几台车，提示符里清晰的主机名能救你于“把命令敲错机器”的事故。密码不要设得太复杂，Linux 下输入 sudo 密码的频率远比你想象的高。

然后就是等待。安装器复制文件加下载更新大约需要十到三十分钟。完成后点击“现在重启”，屏幕提示“Please remove the installation medium”时拔掉 U 盘，按回车。

==== 首次启动检查

重启后你看到的第一个界面应该是 GRUB 引导菜单——黑底白字，列着“Ubuntu”和“Windows Boot Manager”等条目。这就是双系统的日常入口：默认高亮 Ubuntu，十秒倒计时后自动进入；用方向键选 Windows Boot Manager 则回到 Windows。第一次启动，建议把两个系统各进一遍，确认谁都没被弄坏。

如果 GRUB 菜单里没有 Windows 条目，不要慌，Windows 没有丢，只是 GRUB 没把它扫描出来。进入 Ubuntu 打开终端（`Ctrl+Alt+T`），执行：

```bash
# 扫描硬盘上的其它操作系统
sudo os-prober
# 如果输出里出现了 Windows Boot Manager，重新生成 GRUB 菜单
sudo update-grub
```

如果 `os-prober` 没有任何输出，多半是新版 GRUB 默认关闭了系统探测功能。编辑配置文件 `/etc/default/grub`，加入一行 `GRUB_DISABLE_OS_PROBER=false`，保存后再执行一次 `sudo update-grub`，重启即可看到 Windows 条目。

如果选择 Ubuntu 后黑屏——还记得上面的 nomodeset 吗？在 GRUB 菜单按 `e`，同样在 `quiet splash` 后加 `nomodeset` 再按 `F10` 启动。这个状况会一直持续到装好 NVIDIA 驱动为止，所以进入系统后优先去完成“装机后的基础配置”一节的驱动安装。

进入桌面后做两个快速检查。第一，确认网络正常，打开终端执行 `sudo apt update`，能刷出一串软件源信息就说明网络和包管理系统都工作正常。第二，处理双系统的经典小毛病——*两边时间差八小时*。原因是硬件时钟（RTC）的解读方式不同：Linux 把主板上的时钟当作 UTC 时间，Windows 把它当作本地时间，两边各说各话，时差正好是时区偏移。最省事的修法是让 Ubuntu 迁就 Windows：

```bash
# 让 Ubuntu 也把硬件时钟当作本地时间，并同步一次
timedatectl set-local-rtc 1 --adjust-system-clock
```

执行一次即可永久生效。至此，双系统安装宣告完成。

==== 双系统的日常使用

再交代几个双系统用户每天都会碰到的问题，帮你把日子过顺。

第一个是默认启动项。GRUB 默认高亮第一项（Ubuntu），十秒后自动进入。如果你有一段时间主要用 Windows，每次开机都得守着键盘选一下，很烦。一个优雅的解法是让 GRUB *记住上次的选择*——上次进的哪个系统，下次默认还是它。编辑 GRUB 配置文件 `sudo nano /etc/default/grub`，修改和添加以下几项：

```text
# 默认启动上一次选择的条目
GRUB_DEFAULT=saved
GRUB_SAVEDEFAULT=true
# 等待时间从 10 秒缩短到 3 秒
GRUB_TIMEOUT=3
```

保存后执行 `sudo update-grub` 使配置生效。以后重启只会在你切换系统的那一次需要动键盘。

第二个是跨系统访问文件。方向很重要：*Ubuntu 读 Windows 很容易，反过来不行*。在 Ubuntu 的文件管理器侧栏里可以看到 Windows 的 NTFS 分区，点击即可挂载浏览，你的 Windows 桌面、下载目录都在里面（前提是之前关闭了快速启动，且 Windows 是正常关机而非休眠——否则只能只读打开）。而 Windows 默认完全不认识 ext4 文件系统，在 Windows 里你根本看不到 Ubuntu 的分区。所以需要在两个系统间流转的文件，要么放在 Windows 分区里由 Ubuntu 这边来读写，要么走 Git 仓库或网盘同步——对代码来说，Git 永远是正确答案。

第三个是心理预期：双系统不能同时运行，切换要重启。写代码写到一半想开只有 Windows 有的某个软件，是双系统生活最大的摩擦点。很多队员的最终形态是“Ubuntu 为主、微信网页版凑合、实在不行重启”——你会找到自己的平衡。

=== 虚拟机安装

如果你暂时不想动真实磁盘——比如笔记本是家里唯一的电脑不敢折腾，或者只是想先体验一下 Linux——虚拟机（virtual machine）是最安全的入门方式。虚拟机软件在 Windows 里模拟出一台完整的“电脑中的电脑”：虚拟的 CPU、内存、磁盘、显卡，Ubuntu 就安装在这台虚拟电脑里，和你的 Windows 井水不犯河水。玩坏了删掉重来，真实系统毫发无损。

==== 选择：VMware Workstation Pro 还是 VirtualBox

桌面虚拟机软件主要就这两个选择。VMware Workstation Pro 是老牌商业软件，性能和稳定性口碑最好，过去要几百美元的授权，2023 年底 VMware 被 Broadcom 收购后，自 2024 年起对*个人使用完全免费*，从 Broadcom 官网注册账号即可下载。VirtualBox 则是 Oracle 维护的开源软件，完全免费，功能略朴素但完全够用。

二选一即可，没有必要纠结：两者的概念体系（虚拟磁盘、快照、网络模式）完全一致，操作界面大同小异。下文以 VMware 为例，用 VirtualBox 的同学对照着做即可。

==== 创建虚拟机与安装系统

安装好 VMware 后，选择“创建新的虚拟机”，客户机操作系统选 Linux / Ubuntu 64 位。资源配置的原则是“给 Ubuntu 够用，也别把 Windows 榨干”：处理器分 4 个核心，内存分 8 GB（宿主机至少留 8 GB 给 Windows），虚拟磁盘给 60 GB 以上。虚拟磁盘默认是“按需增长”的——创建时并不立即占用 60 GB 真实空间，用多少长多少，所以这个数字可以放心给大。

创建完成后，在虚拟机设置的 CD/DVD 选项里挂载之前下载的 Ubuntu ISO 镜像，开机。之后的安装流程与双系统一节完全相同，唯一的区别是分区环节毫无心理负担——虚拟磁盘是宿主机上的一个普通文件，里面没有任何需要保护的数据，你可以放心大胆地练习“其他选项”手动分区。事实上我们*推荐*选双系统方案的同学也先在虚拟机里预演一遍分区操作，动真实磁盘之前先拿虚拟磁盘练手，是成本最低的容错。

装完系统后，在虚拟机里打开终端，安装增强工具：

```bash
# VMware 客户机增强工具：自适应分辨率、与宿主机共享剪贴板
sudo apt update
sudo apt install open-vm-tools open-vm-tools-desktop
```

重启虚拟机后，窗口可以随意拉伸、分辨率自动跟随，宿主机和虚拟机之间可以互相复制粘贴文本，体验会顺滑很多。VirtualBox 的对应物叫“增强功能”（Guest Additions），在菜单“设备”里选择“安装增强功能”即可。

==== 快照：虚拟机最大的价值

快照（snapshot）是虚拟机相对物理机的杀手级功能：把虚拟机当前的完整状态——磁盘内容、内存、运行中的程序——整个冻结保存下来，之后任何时候都能一键恢复到这个瞬间。它不是备份某些文件，而是备份*整台机器*。

养成打快照的习惯：装完系统打一个，命名“干净系统”；配完开发环境打一个，命名“环境就绪”；做任何危险实验之前打一个。危险实验包括但不限于：练习 `rm` 命令、乱改环境变量、手动折腾显卡驱动、修改系统配置文件。改坏了？恢复快照，三十秒回到案发之前，比排查修复快一百倍。

这也是虚拟机在队内培训中的独特价值：后面的“Linux & Ubuntu 操作”一章里，你会学到不少有破坏力的命令，在虚拟机里你可以放开手脚去试探它们的边界——把系统玩坏本身就是最深刻的学习方式之一，而快照让这种学习变得零成本。物理机上的双系统可没有这种后悔药。

==== 虚拟机的局限

上一章选型时已经给过结论：虚拟机当不了视觉组的长期主力。体验一段时间后，你会在三个地方实际撞上它的天花板——把成因看清楚，才好判断哪些实验适合放进虚拟机、哪些必须回到真机。

第一是图形性能。虚拟机的“显卡”是软件模拟的，3D 加速能力有限且不支持 CUDA。日常桌面操作没问题，但跑 RViz 三维可视化、Gazebo 仿真会明显掉帧，神经网络推理更是无从谈起——虚拟机里根本看不到你的 NVIDIA 显卡。

第二是 USB 设备直通（passthrough）的不可靠。虚拟机可以把宿主机的 USB 设备“转接”进来，鼠标键盘 U 盘都没问题，但工业相机是另一回事：大恒、海康的工业相机以 USB 3.0 满带宽狂灌图像数据，经过虚拟化层转接后带宽打折、延迟抖动，轻则丢帧重则掉线。串口设备（比如连接下位机的 USB 转 TTL）勉强可用，但多一层转发就多一分不确定。

第三是实时性。虚拟机的 CPU 调度受制于宿主机，Windows 后台一忙，虚拟机整体卡一下，几十毫秒的延迟尖峰对桌面使用无感，对每帧只有几毫秒预算的自瞄流水线是致命的。

结论一句话：虚拟机是优秀的学习环境和分区练习场，但整车视觉系统的开发调试，终究要回到双系统或真实的车载计算平台上。

=== WSL2

WSL2（Windows Subsystem for Linux 2）是微软官方提供的“在 Windows 里运行 Linux”方案。它的本质是一个深度集成进 Windows 的轻量级虚拟机，里面跑着真正的 Linux 内核——你的 Ubuntu 命令行和 Windows 桌面无缝共存，互相访问对方的文件，启动只要几秒钟。对于暂时离不开 Windows、又想马上开始练命令行的同学，这是门槛最低的选择。

==== 一条命令装好

WSL2 要求 Windows 10 21H2 以上或任意版本的 Windows 11。安装只需一条命令：在开始菜单搜索 PowerShell，*右键以管理员身份运行*，执行：

```text
wsl --install -d Ubuntu-22.04
```

这条命令会自动完成启用虚拟化组件、下载 Linux 内核、安装 Ubuntu 22.04 发行版的全部工作。完成后按提示重启电脑，首次启动 Ubuntu 时会让你创建一个用户名和密码（与 Windows 账户无关，规则参考双系统一节的建议）。之后在开始菜单点击“Ubuntu 22.04”，或在任意终端输入 `wsl`，就能进入 Linux 命令行。

验证一下安装是否成功：

```text
wsl --list --verbose
```

输出中应能看到 `Ubuntu-22.04`，并且 VERSION 列显示 2——这表示运行在 WSL2 模式而不是老的 WSL1 模式下，后者是系统调用翻译层，兼容性差得多。进入 Ubuntu 之后，本章后面“装机后的基础配置”里除显卡驱动外的内容（换源、常用工具）都同样适用；显卡驱动则*不需要也不能*在 WSL2 里安装——它直接复用 Windows 侧的 NVIDIA 驱动。

==== 与 Windows 的文件互通

WSL2 与 Windows 的文件系统是双向打通的，这是它相对虚拟机最舒服的地方。在 Ubuntu 这一侧，整个 Windows 的 C 盘挂载在 `/mnt/c` 下，比如你的 Windows 下载目录就是 `/mnt/c/Users/你的用户名/Downloads`，可以直接 `cd` 进去操作。在 Windows 这一侧，资源管理器的侧栏里有一个 Linux 图标（或在地址栏输入 `\\wsl$`），点进去就能浏览 Ubuntu 的整个文件系统。

但有一个重要的性能常识：*跨系统边界的文件读写很慢*。WSL2 里访问 `/mnt/c` 下的文件要经过一层网络文件协议转换，小文件密集的操作（比如编译一个 C++ 工程、`git status` 一个大仓库）会慢好几倍。所以规矩很简单：代码仓库永远克隆到 Linux 侧的家目录（`~`）下，在 Linux 侧编辑和编译；`/mnt/c` 只用来偶尔取一下 Windows 下载的文件。反过来同理，Windows 的程序也不要频繁读写 Linux 侧的文件。

==== 能干什么，不能干什么

上一章给 WSL 的定位是辅助工具，这里把能力边界划得更具体。先说能干什么。学习命令行和 Shell，完全胜任——它就是真实的 Ubuntu 用户空间，本书“Linux & Ubuntu 操作”一章的所有命令都能照跑。C++ 开发、CMake 构建、Git 版本控制，完全胜任。配合 VSCode 的 WSL 远程插件，你可以在 Windows 界面里无缝编辑和编译 Linux 侧的代码，体验相当好。WSL2 甚至支持图形程序（WSLg）和 CUDA 计算，跑一些视觉算法的离线实验也没问题。

再说不能干什么，而这几条对视觉组恰恰是硬需求。USB 工业相机基本没戏：WSL2 默认不能访问 USB 设备，社区的 usbipd 工具可以勉强转接，但对高带宽的 USB 3.0 相机极不稳定。串口设备同样受限，连接下位机调试无从谈起。实时性受宿主 Windows 调度的制约，与虚拟机同病。局域网调试时 WSL2 的网络是一层 NAT 转换，和车载设备、ROS 多机通信打交道时会平添一堆网络配置问题。

所以给 WSL2 的定位一句话：*能学命令行，不能跑整车视觉*。它是 Windows 主力机同学的最佳过渡方案和日常辅助工具——用它把 Linux 基础和 C++ 练扎实，等要真正接相机、连串口、上车调试时，再换到双系统或者直接用队里的车载 NUC。

=== 装机后的基础配置

不管通过哪种方式，你现在有了一个全新的 Ubuntu。全新意味着空白：软件源指向国外服务器慢如蜗牛、打不了中文、独立显卡没有驱动、连 `g++` 都没有装。本节把这些一次性配置逐个解决，做完之后这台机器才算真正“可以干活”。以下操作都在终端中进行——按 `Ctrl+Alt+T` 打开它，具体的命令行原理后面章节会系统讲，现在照着做并留意注释即可。

==== 更换软件源

上一章讲过，Linux 安装软件靠包管理器从软件源（repository）下载。Ubuntu 默认的源服务器在国外，国内访问的速度常常只有几十 KB/s，装一套开发环境能等到天荒地老。第一件事就是把源换成国内镜像，清华 TUNA 和中科大源都是好选择，速度能提升几十倍。

软件源配置存放在 `/etc/apt/sources.list` 文件里。动手前先备份——这是修改任何系统配置文件前的标准动作：

```bash
# 备份原始配置，出问题可以随时恢复
sudo cp /etc/apt/sources.list /etc/apt/sources.list.bak

# 用 sed 把默认源批量替换为清华源
sudo sed -i 's@//.*archive.ubuntu.com@//mirrors.tuna.tsinghua.edu.cn@g' /etc/apt/sources.list
```

如果你想看得更明白，也可以不用 `sed`，直接用文本编辑器打开配置文件（`sudo nano /etc/apt/sources.list`），把内容整个替换为：

```text
# Ubuntu 22.04 (jammy) 清华大学 TUNA 镜像源
deb https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ jammy main restricted universe multiverse
deb https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ jammy-updates main restricted universe multiverse
deb https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ jammy-backports main restricted universe multiverse
# 安全更新建议保留官方源，第一时间获取安全补丁
deb http://security.ubuntu.com/ubuntu/ jammy-security main restricted universe multiverse
```

换完源，刷新软件包索引并把系统升级到最新：

```bash
# update 是刷新“软件目录”，upgrade 才是真正升级软件
sudo apt update
sudo apt upgrade
```

`apt update` 的输出应该是一串带 `mirrors.tuna.tsinghua.edu.cn` 字样的“命中/获取”记录，几秒钟跑完——对比换源前的龟速，你会立刻体会到这一步的价值。`apt upgrade` 第一次执行可能要下载几百 MB 更新，耐心等它完成。

==== 中文输入法 fcitx5

装系统时我们选了英文环境，现在把中文输入补上。Linux 的输入法由输入法框架（input method framework）统一管理，主流的有 iBus 和 Fcitx 两家：Ubuntu 默认用 iBus，但它的中文体验平平；社区普遍推荐 Fcitx 5——词库更好、候选界面更现代、对各类程序的兼容性也更佳，是目前 Linux 中文输入的事实标准。

安装 Fcitx 5 及中文引擎：

```bash
# fcitx5 主程序 + 中文输入引擎（拼音等）+ 各图形框架的支持组件
sudo apt install fcitx5 fcitx5-chinese-addons fcitx5-frontend-gtk3 fcitx5-frontend-qt5

# 把系统默认输入法框架切换为 fcitx5
im-config -n fcitx5
```

然后设置几个环境变量，让各类应用程序知道去找 Fcitx 通信。用文本编辑器打开系统环境变量文件：

```bash
sudo nano /etc/environment
```

在文件末尾追加以下几行，然后保存退出（nano 中按 `Ctrl+O` 回车写入，再按 `Ctrl+X` 退出）：

```text
GTK_IM_MODULE=fcitx
QT_IM_MODULE=fcitx
XMODIFIERS=@im=fcitx
SDL_IM_MODULE=fcitx
```

重启电脑使配置生效。重新登录后运行 `fcitx5-configtool` 打开配置界面，把“Pinyin（拼音）”添加到输入法列表。之后按 `Ctrl+Space` 即可在中英文之间切换。打开一个文本编辑器敲几个汉字验证一下——能上屏就大功告成。

==== NVIDIA 显卡驱动

这是视觉组机器最重要的一项配置。没有官方驱动，你的 NVIDIA 显卡在 Ubuntu 下只是个亮机卡：CUDA 用不了，神经网络推理跑不动，自瞄的装甲板检测模型只能在 CPU 上以个位数帧率挣扎。前文安装阶段黑屏的 nomodeset 坑，根源也在于此——开源的 nouveau 驱动只能保证“亮”，性能和功能都指望不上。

Ubuntu 提供了官方的驱动管理工具，先看看系统检测到了什么：

```bash
# 列出检测到的硬件和可用的驱动版本
ubuntu-drivers devices
```

输出会列出你的显卡型号和一串候选驱动，其中一行末尾带有 `recommended` 标记——那就是系统推荐的版本。直接自动安装推荐版本：

```bash
# 自动安装推荐的 NVIDIA 驱动
sudo ubuntu-drivers autoinstall
# 装完必须重启，新驱动才会接管显卡
sudo reboot
```

重启后验证驱动是否正常工作：

```bash
nvidia-smi
```

如果输出一张表格，列着驱动版本、CUDA 版本、显卡型号、显存占用和当前温度，恭喜你，驱动工作正常。这条 `nvidia-smi` 命令以后会是你的老朋友——训练模型时看显存、部署推理时看利用率，都靠它。如果命令报错找不到设备，常见原因之一是 Secure Boot 拦下了未签名的驱动模块（尤其 `.run` 或 DKMS 方式装的驱动）——回看“BIOS/UEFI 设置要点”一节把它关掉，重启后一般即可恢复；同时也检查一下驱动安装过程有没有报错。

一个重要的劝退提醒：*不要去 NVIDIA 官网下载 `.run` 安装包手动装驱动*。网上不少老教程这么教，但 `.run` 方式绕过了包管理器，内核一升级驱动就失效，卸载和升级都是坑，是新手黑屏进不了桌面的头号来源。`ubuntu-drivers` 装的驱动由 apt 统一管理，内核升级时会自动重新编译适配，省心得多。

==== 常用工具一次装齐

最后把日常开发的基础工具一次性装上：

```bash
sudo apt install git curl wget vim htop tree net-tools build-essential cmake gdb openssh-server
```

简单介绍一下这一串都是什么，混个脸熟即可，后面的章节会陆续和它们正式打交道：

- `git` 是版本控制工具，整个团队协作的基石，详见 Git 版本控制一章。
- `curl` 和 `wget` 用于在命令行下载文件、测试网络接口，无数安装脚本依赖它们。
- `vim` 是终端文本编辑器，SSH 到车上没有图形界面时，它是你修改配置文件的主力工具。
- `htop` 是交互式的进程监视器，调车时看 CPU 占用、揪出吃资源的进程全靠它。
- `tree` 以树状图打印目录结构，看工程布局一目了然。
- `net-tools` 提供 `ifconfig` 等传统网络命令，排查赛场网络时经常用到。
- `build-essential` 是一个元包，一次装齐 `gcc`、`g++`、`make` 等 C/C++ 编译工具链——没有它连 Hello World 都编译不了。
- `cmake` 是 C++ 世界的主流构建系统，OpenCV、ROS 的工程都靠它组织，详见 CMake 与构建系统一章。
- `gdb` 是调试器，程序段错误崩溃时用它定位现场。
- `openssh-server` 让这台机器可以*被*远程登录。自己的笔记本可装可不装，但车载 NUC 上它是必装项——调车时你就是通过它连上车的。

==== 面向调车的电源与锁屏设置

Ubuntu 桌面版默认的电源策略是为普通笔记本用户设计的：几分钟不动就息屏、再过一会儿挂起休眠、唤醒还要输密码解锁。这套策略放在调车场景下是灾难：你在场边盯着机器人跑，车载 NUC 的系统觉得“没人在用电脑”，自顾自挂起了——视觉节点、串口通信全部停摆，机器人当场变盲。所以车载机器（以及经常拿来跑长时间任务的开发机）必须改掉这些默认行为。

图形界面里可以在“设置 → 电源”中逐项修改，但我们直接给命令行版本——以后你给一台新 NUC 装环境时，这几条命令可以直接进部署脚本：

```bash
# 永不自动息屏
gsettings set org.gnome.desktop.session idle-delay 0
# 接通电源时永不自动挂起
gsettings set org.gnome.settings-daemon.plugins.power sleep-inactive-ac-type 'nothing'
# 关闭屏幕自动锁定
gsettings set org.gnome.desktop.screensaver lock-enabled false
```

车载 NUC 上通常还会在“设置 → 用户”中开启*自动登录*——上电即进桌面，无人值守时配合开机自启脚本，机器人通电就能把视觉系统拉起来，这在赛场上是标配。当然，个人笔记本请自己权衡：关锁屏和开自动登录都是用安全性换便利，宿舍和实验室里谁都能动你的电脑。

至此，一台可以正经干活的 Ubuntu 就绪了。

=== 开发环境搭建

工具链有了，还差一个趁手的写代码环境。本节安装并配置 Visual Studio Code（VSCode），它是目前 RoboMaster 各战队使用最广的编辑器：免费、插件生态庞大，尤其是它的远程开发能力——在自己笔记本上直接编辑和调试车载 NUC 里的代码——几乎是为机器人开发量身定做的。

==== 安装 VSCode

VSCode 官方提供 Ubuntu 的 `.deb` 安装包。用浏览器打开 `https://code.visualstudio.com`，下载 `.deb` 版本（64 bit），然后在终端安装：

```bash
# 进入下载目录安装（文件名以实际下载的版本为准）
cd ~/Downloads
sudo apt install ./code_*.deb
```

有同学会问：Ubuntu 软件商店里一搜就有 VSCode，为什么要绕这个弯？商店里的是 snap 打包版本，运行在沙箱里，长期存在中文输入法失灵、访问系统文件受限等小毛病。`.deb` 版本没有这些问题，而且安装时会自动注册微软的软件源，之后 `apt upgrade` 会顺带升级 VSCode，一劳永逸。

装好后在终端输入 `code` 即可启动。更常用的姿势是在工程目录下执行 `code .`——把当前目录作为工作区打开，这会是你以后打开工程的标准动作。

==== 必装插件

点开左侧扩展图标（或按 `Ctrl+Shift+X`）搜索安装。插件不在多，先装这几个核心的：

- *C/C++*（微软官方）：提供 C++ 的代码补全、跳转定义、语法检查和调试支持，写视觉代码的基本盘。
- *CMake Tools*（微软官方）：在 VSCode 里直接配置、构建、调试 CMake 工程，状态栏一键编译，和 C/C++ 插件配合使用。
- *Remote - SSH*（微软官方）：远程开发的核心，下一小节专门讲。
- *Chinese (Simplified) 中文语言包*：把界面换成中文，可选——但建议至少适应一段英文界面，报错信息和文档终究是英文世界的。

进阶一点的选择这里点一句：C++ 语言服务还有 clangd 这个更快更准的替代品，Python 开发装官方 Python 插件即可，Git 增强有 GitLens。这些等你写代码遇到具体需求时再装不迟——插件装太多反而拖慢编辑器。

==== Remote-SSH：把代码写到机器人上

现在讲讲为什么 Remote-SSH 值得单独一节。想一个真实场景：车载 NUC 装在机器人底盘里，没有显示器没有键盘，视觉代码跑在它上面。你要改一个参数怎么办？拆车接显示器？把代码拷来拷去？都不用——NUC 连着调试 Wi-Fi，你的笔记本通过 SSH 远程登录它，而 Remote-SSH 插件让 VSCode 整个“搬”到 NUC 上：文件浏览、代码编辑、终端、调试器，操作的全是 NUC 上的真实文件，体验却和本地开发一模一样。这就是视觉组的日常调车姿势。

前提是目标机器装了 `openssh-server`（上一节已装）并且网络可达。假设 NUC 的 IP 是 `192.168.1.100`、用户名是 `rm`，先在终端验证 SSH 能通：

```bash
# 首次连接会询问是否信任对方指纹，输入 yes，然后输入对方的登录密码
ssh rm@192.168.1.100
```

能登进去就说明链路没问题（`exit` 退出）。然后在 VSCode 中按 `F1`，输入“Remote-SSH: Connect to Host”，新建 SSH 主机并填入 `rm@192.168.1.100`，VSCode 会自动在远端部署服务组件，几秒后左下角出现绿色的远程标识——此后打开的文件夹、终端都在 NUC 上。为常用的机器起个别名会更方便，编辑 `~/.ssh/config` 文件：

```text
# 之后连接时直接选 "hero" 即可，不用记 IP
Host hero
    HostName 192.168.1.100
    User rm
```

每次连接都输密码很烦，正确解法是配置 SSH 密钥实现免密登录——`ssh-keygen` 生成密钥对、公钥分发到目标机器，下一章讲 SSH 远程登录时就会把它配上；把公钥配到 GitHub 用的也是同一套流程，见 Git 版本控制一章。

==== 用一个 Hello World 验收

环境搭没搭好，不能靠感觉，要靠一次端到端的验证：从新建工程、编写代码到编译运行，完整走一遍。打开终端：

```bash
# 建一个练手目录并用 VSCode 打开
mkdir -p ~/code/hello
cd ~/code/hello
code .
```

在 VSCode 中新建文件 `hello.cpp`，输入以下内容（第一次打开 C++ 文件时，右下角会提示安装相关组件，同意即可）：

```cpp
#include <iostream>

int main() {
    // 向 RoboMaster 视觉世界问好
    std::cout << "Hello, RoboMaster CV!" << std::endl;
    return 0;
}
```

回到终端（VSCode 里按 `Ctrl+反引号` 可以直接打开内置终端，不用切窗口），编译并运行：

```bash
# 用 g++ 编译，-o 指定输出的可执行文件名
g++ hello.cpp -o hello
# 运行它（./ 表示当前目录，原因见上一章）
./hello
```

```text
Hello, RoboMaster CV!
```

看到这行输出，说明编辑器、编译器、终端这条最小工具链已经完整打通。别小看这一步——以后所有的视觉代码，无论多复杂，走的都是“编辑、编译、运行”这同一个循环，只是编译一步会逐渐交给 CMake 去指挥（详见 CMake 与构建系统一章）。如果哪一步报错了，回到对应小节检查：`code` 不存在说明 VSCode 没装好，`g++` 不存在说明 `build-essential` 没装上。

==== 终端与下一步

关于终端本身还有很大的提升空间——更好用的 Shell（zsh）、自动补全建议、语法高亮、更漂亮的提示符，这些美化和效率配置我们按下不表，等 Linux & Ubuntu 操作一章把 Shell 的原理讲清楚之后再动手，你会知其然也知其所以然。

盘点一下你现在拥有的：一个跑在合适载体上的 Ubuntu 22.04，国内高速软件源，中文输入法，正常工作的 NVIDIA 驱动，完整的 C++ 工具链，以及一个能本地能远程的现代编辑器。环境已经就绪，从下一章开始，我们正式搬进这个新家——学会用命令行在 Linux 世界里自如地生活。
