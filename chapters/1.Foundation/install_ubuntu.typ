上一章已经比较了双系统、虚拟机和 WSL2 三种方案，本章直接介绍安装操作：从下载镜像开始，逐步配置出可用于 RoboMaster 视觉开发的 Ubuntu 环境。

本章以 Ubuntu 22.04 LTS 为准，因为它是 ROS 2 Humble 的一级支持平台，也常用于 RoboMaster 的车载计算机。双系统是本章的主要方案，步骤最为详细；虚拟机和 WSL2 各有一节，分别说明安装方法和能力边界。无论选择哪种方案，最后两节“装机后的基础配置”和“开发环境搭建”都可作为后续日常开发环境的配置参考。

=== 安装前的准备

安装操作系统涉及磁盘分区和固件设置，操作错误可能造成数据丢失或系统无法启动。开始安装前，需要检查硬件、下载并校验镜像、制作 U 盘启动盘、备份数据、调整 Windows 设置，并检查 BIOS/UEFI 选项。准备阶段逐项确认，可以提前发现许多会中断安装的问题。

==== 检查你的硬件

先花几分钟记录这台机器的硬件。内存容量可以在“任务管理器 → 性能”或“设置 → 系统 → 系统信息”中查看；物理磁盘和分区在“磁盘管理”中确认；显卡与无线网卡的准确型号则可在“设备管理器”中查看。重点记录下面三类信息。

第一是内存和磁盘。本书把 8 GB 内存作为最低配置，建议使用 16 GB 或更多；编译大型 C++ 工程和运行仿真都会占用较多内存。磁盘方面，确认可以为 Ubuntu 留出至少 100 GB。还要在“磁盘管理”中确认物理硬盘数量：如果笔记本有两块硬盘，可以把 Ubuntu 安装到独立硬盘，减少对 Windows 数据分区的操作。两个系统仍可能共用或相互影响启动配置，因此安装后还要检查引导项。

第二是显卡。在设备管理器的“显示适配器”中记录准确型号；NVIDIA GPU 还需要与型号、系统和 CUDA 工具链相容的驱动，不能只根据品牌判断是否可用。安装阶段若出现黑屏，可参考后文的 `nomodeset` 处理方法。只有 Intel 或 AMD 核芯显卡也不影响 Ubuntu 和传统视觉算法的使用，但无法使用 NVIDIA CUDA，神经网络推理需要改用 CPU 或其他受支持的计算后端。

第三是无线网卡。部分较新的 Wi-Fi 网卡可能不受 Ubuntu 22.04 默认内核支持，安装后会暂时无法联网，驱动安装也会因此受阻。应在后文的 Live 环境中先测试 Wi-Fi。还可以提前准备手机 USB 网络共享：用数据线连接手机并启用“USB 网络共享”后，Ubuntu 通常会将其识别为有线网络接口。能否使用仍取决于手机、数据线和系统驱动，安装前可以实际测试一次；比赛现场没有可用 Wi-Fi 时，这种方式也可作为临时网络连接。

==== 下载镜像与校验

操作系统的安装文件通常以 `.iso` 镜像发布。ISO 镜像保存了可启动介质的文件系统和相关元数据，既可以刻录到光盘，也可以通过支持的工具写入 U 盘或由 Ventoy 加载，从而引导计算机进入安装程序。

本书使用 *Ubuntu 22.04 LTS 桌面版（Desktop，amd64 架构）*。LTS 是 Long Term Support（长期支持）的缩写，这类版本每两年发布一次，标准维护期为五年。ROS 2 各发行版有对应的一级支持系统，Humble 对应 Ubuntu 22.04；使用其他 Ubuntu 版本时，官方二进制包和安装步骤可能不同。队内统一系统与主要依赖版本，也有助于复现构建和运行问题。处理器架构、驱动和本机配置仍可能造成差异，不能只凭系统版本判断环境完全相同。

在中国大陆访问时，可以使用清华大学 TUNA 或中科大等镜像站，下载速度通常比直接访问官方服务器稳定。在浏览器中打开 TUNA 的 Ubuntu 发布目录：

```text
https://mirrors.tuna.tsinghua.edu.cn/ubuntu-releases/22.04/
```

下载其中最新的 Ubuntu 22.04 Desktop amd64 镜像，例如 `ubuntu-22.04.5-desktop-amd64.iso`。小版本号和文件大小会随发布更新而变化，通常为数 GB；下载前应同时确认版本、`desktop`、`amd64` 和 `.iso` 后缀。

下载完成后应进行校验（checksum verification）。镜像在下载或存储过程中损坏，可能在启动或安装阶段表现为读取错误。校验时计算文件的 SHA-256 哈希值，再与可信渠道发布的 `SHA256SUMS` 中对应条目比较；意外改动几乎必然会得到不同结果。哈希一致只说明本地文件与参考值对应，参考文件本身也要从 Ubuntu 官方站点或可信镜像通过 HTTPS 取得；需要进一步核对发布来源时，可以按 Ubuntu 的说明验证 `SHA256SUMS.gpg` 签名。

如果你在 Linux 或 macOS 上下载的镜像，校验命令如下：

```bash
# 在镜像所在目录执行，计算文件的 SHA256 哈希值
sha256sum ubuntu-22.04.5-desktop-amd64.iso
# 输出形如：
# 一长串十六进制字符  ubuntu-22.04.5-desktop-amd64.iso
# 与可信 SHA256SUMS 中的对应行比对；一致表示文件与该参考值相符
```

如果你在 Windows 上下载（大多数新队员的情况），用系统自带的 `certutil` 工具，在 PowerShell 或命令提示符中执行：

```text
certutil -hashfile ubuntu-22.04.5-desktop-amd64.iso SHA256
```

把输出的完整哈希值与可信 `SHA256SUMS` 中同一文件名对应的值比较，不要只看开头和结尾几位。两者一致，说明本地文件与该参考值相符，可以排除常见的下载损坏；它本身不能证明参考值的发布者身份，也不能证明镜像中的软件没有安全问题。

==== 制作 U 盘启动盘

计算机默认从硬盘启动，要运行安装程序，需要一个可引导的安装介质——也就是启动盘（bootable USB）。你需要一个容量 8 GB 以上的 U 盘，速度越快安装越流畅。

*警告：制作启动盘会清空 U 盘上的全部数据。* 动手前把 U 盘里的文件拷走，确认无误再继续。

本书使用 Ventoy 制作启动盘。Rufus 等传统工具把选定镜像写入 U 盘，更换镜像时需要重新制作；Ventoy 先在 U 盘上安装引导环境，之后可以把 ISO 当作普通文件复制到数据分区，启动时再从菜单中选择。一个 U 盘可以同时保存 Ubuntu、Windows 安装镜像和维护工具，剩余空间也可存放普通文件，适合重复为开发机或车载 NUC 安装系统的场景。

Ventoy 的使用步骤：在官网 `https://www.ventoy.net` 下载 Windows 版压缩包，解压后运行 `Ventoy2Disk.exe`，在设备列表里*仔细确认选中的是你的 U 盘*，点击“安装”。完成后 U 盘会出现一个大的空白分区，把下载好的 ISO 文件直接拖进去即可。

也可以使用 Rufus：从 `https://rufus.ie` 下载后，选中 U 盘和 ISO 文件。对于采用 UEFI 固件并以 GPT 分区的计算机，可选择 *GPT* 和 *UEFI（非 CSM）*；较旧设备或采用其他启动方式时，应先核对当前磁盘分区表和固件模式，不能统一套用这一设置。

如果你手头已经有一台 Linux 机器（比如用队里的旧设备制作启动盘），可以用 `dd` 命令直接写入：

```bash
# 先列出所有磁盘设备，确认 U 盘的设备名
lsblk
# 假设 U 盘是 /dev/sdb（注意是整块盘 sdb，不是分区 sdb1）
sudo dd if=ubuntu-22.04.5-desktop-amd64.iso of=/dev/sdb bs=4M status=progress conv=fsync
```

*警告：`dd` 会直接覆盖 `of=` 指定的设备，不会询问它是不是系统盘。* 写错目标可能破坏分区表和文件数据，恢复通常困难且可能不完整。执行前应拔掉无关移动存储，再用 `lsblk -o NAME,SIZE,MODEL,TRAN,MOUNTPOINTS` 结合容量与型号确认整块 U 盘；任何一项不确定时都应停止，改用能够明确显示设备信息的图形工具并再次核对。

==== 备份数据与 Windows 侧准备

接下来在 Windows 一侧完成四项准备：备份数据、处理 BitLocker、关闭快速启动，并腾出未分配磁盘空间。

*备份是不可跳过的一步。* 双系统安装涉及压缩现有分区、写入新的引导程序，正常操作下 Windows 的数据不会受影响，但断电、误删分区或选错设备都可能造成损失。把课程资料、代码、照片等重要文件复制到与待操作磁盘独立的设备或可靠远程存储，并抽查文件能否恢复。远程 Git 仓库适合保存已经提交和推送的代码历史，但通常不包含未跟踪文件、未推送提交、数据集、密钥和某些 Git LFS 对象，因此它只是代码备份的一部分，不能代替完整备份。

第二件事是处理 BitLocker。较新的 Windows 笔记本可能默认开启设备加密或 BitLocker，调整启动配置和固件设置后可能要求恢复密钥；没有密钥就可能无法继续启动 Windows。先在 Windows 中确认每个卷的保护状态，把恢复密钥保存到待操作电脑之外并实际核对。通常可以按当前 Windows 与设备厂商的说明暂时挂起保护，完成安装并验证 Windows 启动后再恢复；是否需要完整解密取决于组织策略和具体操作，不应只凭通用教程决定。

第三件事是关闭 Windows 的“快速启动”（Fast Startup）。启用后，Windows 关机时会结束用户会话，但把内核会话和驱动状态写入休眠文件，以便下次更快启动；这属于混合关机，不等同于完整休眠，也不是简单地“没有关机”。如果 Ubuntu 写入仍带有休眠状态的 NTFS 卷，可能造成文件系统不一致，因此 Linux 通常会拒绝读写挂载或只读处理。在“控制面板 → 电源选项 → 选择电源按钮的功能”中取消勾选“启用快速启动”，并在分区操作前执行一次完整关机。

最后腾出磁盘空间。在 Windows 中右键“此电脑”，选择“管理 → 磁盘管理”，右键一个剩余空间充足的分区（通常是 D 盘或 C 盘），选择“压缩卷”，压缩出至少 100 GB 的未分配空间。建议使用 Windows 自带工具调整 NTFS 分区。压缩完成后，磁盘管理中会出现一段黑色的“未分配”空间；记下它的容量，稍后可在 Ubuntu 安装器中据此确认目标空间。

==== BIOS/UEFI 设置要点

BIOS/UEFI 是主板上的固件（firmware），负责在操作系统接管之前初始化硬件、决定从哪个设备启动。当前多数电脑使用 UEFI，BIOS 是较早的一代技术，但设置界面仍常被口头称为“BIOS”。先在 Windows 的“系统信息”（`msinfo32`）中记录“BIOS 模式”，并在磁盘属性中确认 GPT/MBR 分区样式；本节的 EFI 分区步骤以 Windows 已按 UEFI/GPT 启动为前提。若现有 Windows 使用 Legacy/CSM 和 MBR，不要把两种启动模式混装，应先制定经过验证的迁移或同模式安装方案。进入固件设置界面通常是在开机时按设备指定按键，常见的有 `Del`、`F2`、`F10` 或 `Esc`，但仍应以机型说明书为准。如果抓不住时机，可以在 Windows 中按住 Shift 点“重启”，进入高级启动菜单后选择“疑难解答 → 高级选项 → UEFI 固件设置”。

进入设置界面后，重点检查两项。第一是 *Secure Boot（安全启动）*。Ubuntu 本身支持 Secure Boot，通过 `ubuntu-drivers` 从官方源安装且带有可信签名的 NVIDIA 驱动时，通常可以保持开启；手动安装 `.run` 驱动、使用 DKMS 模块或自定义内核时，则可能需要完成 MOK 密钥注册。关闭 Secure Boot 可以避开模块签名配置，但也会取消这项启动链校验。建议先保持默认设置，只有在确认驱动模块因签名无法加载、且理解安全影响后再关闭。该选项一般位于“Security”或“Boot”页面。

第二是*关闭 Fast Boot*（注意这是 BIOS 里的选项，与上一小节 Windows 系统里的“快速启动”是两回事）。Fast Boot 会跳过开机时的部分硬件自检和 USB 设备枚举，可能导致按不出启动菜单、认不到 U 盘。

顺便记下你机器的*启动菜单（Boot Menu）快捷键*，常见的有 `F12`（联想、戴尔）、`F11`（微星）、`Esc` 或 `F9`（惠普）。它的作用是开机时临时选择从哪个设备启动，比进 BIOS 改启动顺序方便得多，下一节马上要用。

还有一种较少遇到的情况：部分笔记本的存储控制器默认工作在 Intel RST/VMD 模式下，Ubuntu 安装器可能看不到硬盘。不要直接根据通用教程把它改成 AHCI：结果取决于机型、Windows 驱动、RAID 配置和 BitLocker 状态，错误操作可能让 Windows 无法启动或使阵列不可访问。先确认厂商是否支持切换，并按该机型或 Microsoft 的迁移流程准备 Windows 存储驱动、安全模式、BitLocker 恢复密钥和回滚方法；无法确认时，应由熟悉该设备的人员处理。如果安装器能正常看到目标磁盘，则无需改动存储模式。

=== 双系统安装实操

双系统（dual boot）是把 Ubuntu 和 Windows 安装在同一台电脑上，开机时通过引导菜单选择进入哪个系统。Ubuntu 原生运行时不经过常规虚拟机的 USB 转发和虚拟显卡层，更便于直接使用系统已支持的工业相机、串口与 NVIDIA 驱动；实际带宽、时延和稳定性仍取决于控制器、驱动、线缆、设备与负载，必须在目标机器上测量。虚拟机和 WSL2 也能在特定条件下访问部分硬件，只是配置路径和适用范围不同。双系统需要修改真实磁盘分区，因此开始前必须完成上一节的备份与检查。本节从插入 U 盘开始，走完整个安装流程。

==== 从 U 盘启动进入 Live 环境

插上启动盘，开机时按上一节记下的启动菜单快捷键，在设备列表中选择 U 盘。同一设备可能同时出现 UEFI 与 Legacy 条目；在本节已经确认 Windows 使用 UEFI/GPT 的前提下，选择带 UEFI 标识的条目，并在 Live 环境中再次确认启动模式。不要只凭品牌名称选择另一个同容量磁盘。如果使用 Ventoy，接下来会出现 Ventoy 菜单，选中实际下载的 Ubuntu 22.04 amd64 ISO，以普通模式（Boot in normal mode）启动；随后在 GRUB 菜单选择“Try or Install Ubuntu”。

正常启动后会进入 Ubuntu Live 环境。系统主要从 U 盘加载，此时尚未开始安装；但 Live 系统仍能识别和挂载磁盘，因此不要主动执行分区或写盘操作。打开终端运行 `test -d /sys/firmware/efi && echo UEFI || echo Legacy`，在本节方案中应输出 `UEFI`；若不是，应返回启动菜单重新核对，而不是继续创建 EFI 分区。随后连接 Wi-Fi，检查网卡、触摸板和屏幕分辨率是否正常。关键硬件在 Live 环境中无法使用时，安装后的默认驱动也可能存在相同问题，可以先记录硬件型号并查询支持情况。

建议在 Live 环境里先把 Wi-Fi 连上再开始安装。联网状态下，安装器可以顺带下载更新和第三方驱动，能省去装完之后的不少配置。确认无误后，双击桌面上的“Install Ubuntu 22.04.5 LTS”图标，正式开始安装。

==== NVIDIA 显卡黑屏与 `nomodeset`

选择“Try or Install Ubuntu”后，如果屏幕持续黑屏、花屏或停在紫色画面，可能与 NVIDIA 显卡的临时驱动和显示模式设置有关。这个现象并非所有 NVIDIA 设备都会出现，也可能由镜像、固件或其他硬件问题造成。

Live 环境通常没有已安装系统中的 NVIDIA 专有驱动，可能使用 nouveau 或基础显示驱动。系统启动早期会进行内核模式设置（Kernel Mode Setting，KMS）；驱动与显卡不兼容时，可能无法正确设置显示模式。可以临时添加内核参数 `nomodeset`，让系统跳过早期模式设置并使用基础显示模式。此时分辨率或界面性能可能较差，但通常足以完成安装。

具体操作：重启回到 GRUB 菜单，*高亮“Try or Install Ubuntu”但不要按回车，而是按 `e` 键*进入编辑模式，找到以 `linux` 开头的那一行，在 `quiet splash` 之后加上一个词：

```text
# 编辑前（行内其余内容保持原样）：
linux /casper/vmlinuz ... quiet splash ---
# 编辑后：
linux /casper/vmlinuz ... quiet splash nomodeset ---
```

然后按 `F10`（或 `Ctrl+X`）启动。这个修改是一次性的，只对本次启动生效。

安装后第一次启动仍可能因为驱动尚未配置而黑屏，可以用同样方法临时加入 `nomodeset`。安装并验证 NVIDIA 驱动后，应先尝试移除该参数；如果仍需依赖它启动，应继续检查驱动加载和显示配置，而不是把它长期视为默认设置。

==== 分区方案

“安装类型”决定 Ubuntu 如何使用磁盘，是双系统安装中需要重点确认的一步。检测到 Windows 后，安装器可能提供“与 Windows Boot Manager 共存安装”的自动选项，由安装器决定分区大小和位置。本书选择最下面的 *“其他选项”（Something else）* 手动规划分区，这样可以逐项确认使用的磁盘、空间和挂载点；如果无法确认这些信息，应停止操作并请有经验的成员协助。

先补两个概念。分区（partition）是把物理磁盘划分成的逻辑区域，每个分区可以有自己的文件系统。挂载点（mount point）是这个分区在 Linux 目录树中出现的位置——上一章讲过，Linux 没有盘符，一切都挂在以 `/` 为根的目录树上。我们要做的就是在那段“未分配空间”（安装器里显示为“空闲”，free space）上新建分区，并指定各自的挂载点。

推荐的分区方案如下：

- *EFI 系统分区*（EFI System Partition）：如果 Ubuntu 与 Windows 装在*同一块硬盘*上，不需要新建——Windows 已经建好了一个 EFI 分区（通常 100 到 500 MB，FAT32 格式），安装器会自动识别并复用它，你只需要确认后面“安装启动引导器的设备”选的是这块盘。如果 Ubuntu 装在*单独的一块新硬盘*上，则新建一个 512 MB 的分区，用途选“EFI 系统分区”。
- *根分区* `/`：存放系统和默认用户数据，文件系统选 ext4，挂载点填 `/`，建议至少 100 GB。系统本身只占一部分空间，ROS 2、CUDA、开发库、训练数据集和实车联调时录制的 rosbag 还会继续增长，一次活动录制的数据就可能达到几十 GB。根分区空间耗尽会导致软件更新、日志写入和程序运行失败，因此应根据数据规模预留余量并定期检查使用率。
- *交换空间*（swap）：内存紧张时，内核可以把一部分暂时不活跃的内存页移到交换空间。Ubuntu 可以使用交换分区或根文件系统中的交换文件；是否由安装器创建交换文件会随安装模式和版本变化，因此手动分区时不必为避免“没有 swap”而固定划出 16 GB。除非队伍已经明确需要单独交换分区，可以先只建立根分区，安装完成后用 `swapon --show` 和 `free -h` 检查实际配置，再根据物理内存、编译或仿真负载、磁盘空间以及是否需要休眠决定交换文件大小。交换空间能降低短时内存不足时进程被终止的概率，但速度远低于内存，也不能替代合理的并行度和内存容量；创建、持久化与回滚交换文件的安全步骤见下一章。

*警告：在这套保留 Windows 的方案中，不要格式化已有的 EFI 分区。* 其中可能同时存放 Windows、Ubuntu 和厂商工具的引导文件；格式化会删除这些文件，使一个或多个系统无法启动，之后需要使用恢复介质修复。复用 EFI 分区时，挂载点是 `/boot/efi`，格式化选项保持不勾选。

*警告：新建分区前，反复确认操作对象是预留的“空闲”空间*，容量应与之前在 Windows 磁盘管理中压缩出的数值一致。Linux 的磁盘设备名与 Windows 不同：NVMe 固态盘可能显示为 `nvme0n1`，其分区为 `nvme0n1p1`、`nvme0n1p2`；SATA 盘可能显示为 `sda`，分区为 `sda1`、`sda2`。误删或格式化分区可能造成不可恢复的数据损失，因此必须提前备份。

这里没有单独划分 `/home` 分区（存放用户个人文件的目录）。独立 `/home` 可以在重装系统时保留个人文件，但需要提前分配根分区与用户分区的容量，后续调整也更复杂。初次安装可以只划分根分区，并通过远程 Git 仓库和日常备份保护重要文件。熟悉自己的空间使用情况后，再决定以后是否拆分 `/home`。

分区表设置完毕后，检查页面底部“安装启动引导器的设备”：选择 EFI 分区所在的整块磁盘，例如 `nvme0n1`，而不是其中某个分区。点击“现在安装”后，安装器会显示分区变更摘要；实际写入前应逐项核对将要创建、格式化和保留的分区。

==== 收尾与重启

后续需要设置时区、键盘布局和安装选项。本书在时区地图中选择上海，键盘布局选择 English (US)，中文输入法稍后单独安装。选择“正常安装”后，可以勾选“为图形或无线硬件，以及其它媒体格式安装第三方软件”；联网时，安装器会同时获取部分专有驱动和多媒体解码器。是否勾选还应结合软件许可和网络条件决定。

接着设置用户名、密码和主机名（hostname）。用户名建议使用简短的小写字母组合，不含空格和中文，因为它会出现在主目录路径和命令提示符中。主机名用于在网络中区分机器，笔记本可以使用 `name-laptop`，车载计算机则可按机器人类型统一命名为 `nuc-hero`、`nuc-sentry` 等。通过 SSH 同时连接多台设备时，清楚的主机名有助于避免在错误主机上执行命令。密码应兼顾强度和可输入性，可以使用较长且容易记忆的口令，不应因为经常输入 `sudo` 密码而使用弱密码。

然后等待安装器复制文件并按所选选项下载更新；所需时间取决于 U 盘与磁盘速度、网络和更新量，不能据固定分钟数判断是否卡住。只要界面仍有进度或磁盘、网络活动，就先等待；若长时间完全无变化，再记录当前步骤和错误信息进行排查。完成后点击“现在重启”，屏幕提示“Please remove the installation medium”时拔掉 U 盘，按回车。

==== 首次启动检查

重启后通常会出现 GRUB 引导菜单，其中列出“Ubuntu”和“Windows Boot Manager”等条目。默认条目会在倒计时结束后启动，也可以用方向键选择另一个系统。第一次启动时，建议分别进入 Ubuntu 和 Windows，检查两个系统能否正常启动以及重要文件是否可读。

如果 GRUB 菜单中没有 Windows 条目，先不要据此判断 Windows 分区已经丢失；GRUB 菜单、UEFI 启动项和磁盘上的 Windows 引导文件是三层不同状态。进入 Ubuntu 打开终端（`Ctrl+Alt+T`），先执行：

```bash
# 扫描硬盘上的其它操作系统
sudo os-prober
```

如果命令不存在，应在确认软件源后安装 Ubuntu 提供的 `os-prober` 包。若输出中已经出现 Windows Boot Manager，再运行 `sudo update-grub`；Ubuntu 22.04 的 GRUB 可能默认不在生成菜单时调用 `os-prober`，此时 `update-grub` 会给出相应提示。先备份 `/etc/default/grub`，再按该版本文档设置 `GRUB_DISABLE_OS_PROBER=false`，重新运行 `sudo update-grub` 并核对它实际生成的条目。

如果直接运行 `os-prober` 就没有发现 Windows，仅修改上述 GRUB 开关不会让它凭空发现系统。应继续核对两边是否都按 UEFI 启动、`efibootmgr -v` 是否仍有 Windows Boot Manager、挂载的 EFI 分区中是否保留 Microsoft 引导文件，以及 Windows 分区是否完整；不确定时从 Windows 恢复介质或固件的一次性启动菜单验证，不要在尚未定位层级时重建或格式化 EFI 分区。

如果选择 Ubuntu 后黑屏，可以再次临时使用前文的 `nomodeset`：在 GRUB 菜单按 `e`，在 `quiet splash` 后加入 `nomodeset`，再按 `F10` 启动。进入系统后应优先完成“装机后的基础配置”一节的驱动安装，并在安装后移除该参数验证能否正常启动。

进入桌面后做两个快速检查。第一，打开终端执行 `sudo apt update`，确认网络连接和包管理器能够访问软件源。第二，如果 Windows 与 Ubuntu 显示的时间相差一个时区偏移，通常是两边对硬件时钟（RTC）的解释不同：Linux 默认按 UTC 处理，Windows 常按本地时间处理。两边必须采用一致策略。一种兼容做法是让 Ubuntu 改用本地时间：

```bash
# 兼容使用本地时间 RTC 的 Windows 配置
timedatectl set-local-rtc 1 --adjust-system-clock
```

该设置会持续保留，但 `timedatectl` 会提示本地时间 RTC 不能可靠处理部分时区和夏令时变更。另一种方案是保留 Ubuntu 的 UTC 默认值，并按适用于当前 Windows 版本的官方说明让 Windows 使用 UTC；不要在两边反复切换不同做法。完成后确认两边的时区与自动校时设置，再分别重启并检查时间。若机器启用了磁盘加密或加入组织管理，应先遵循管理员策略。

==== 双系统的日常使用

下面补充默认启动项、跨系统文件访问和系统切换三个日常问题。

第一个是默认启动项。GRUB 默认高亮第一项（Ubuntu），倒计时结束后自动进入。如果一段时间内主要使用 Windows，可以让 GRUB 记住上一次选择的系统。编辑配置文件 `sudo nano /etc/default/grub`，修改和添加以下几项：

```text
# 默认启动上一次选择的条目
GRUB_DEFAULT=saved
GRUB_SAVEDEFAULT=true
# 等待时间从 10 秒缩短到 3 秒
GRUB_TIMEOUT=3
```

保存后执行 `sudo update-grub` 使配置生效。以后重启只会在你切换系统的那一次需要动键盘。

第二个是跨系统访问文件。Ubuntu 通常可以在文件管理器中挂载 Windows 的 NTFS 分区；如果 Windows 处于休眠或快速启动状态，分区可能只能只读挂载。Windows 默认不直接挂载 Ubuntu 常用的 ext4 分区，因此双向访问并不对称。需要在两个系统之间共享的普通文件，可以存放在 NTFS 分区或通过同步工具传递；源代码适合使用 Git 远程仓库同步，但生成文件、大型数据集和未跟踪文件还需要单独的存储方案。

第三个是系统切换。双系统在同一时刻只运行其中一个系统，切换需要重启。依赖 Windows 专用软件的任务应提前安排，或评估是否可以使用替代软件、远程主机或虚拟机，减少开发过程中的频繁重启。

=== 虚拟机安装

如果暂时不适合调整物理磁盘，或者只想先练习 Linux，可以使用虚拟机（virtual machine）。虚拟机软件在 Windows 中提供虚拟 CPU、内存、磁盘和显卡，Ubuntu 安装在独立的虚拟磁盘文件中。它与宿主系统有一定隔离，系统损坏后可以重建，但共享目录、剪贴板和虚拟化软件漏洞仍可能影响宿主机，重要数据同样需要备份。

==== 选择：VMware Workstation Pro 还是 VirtualBox

常见的桌面虚拟机软件包括 VMware Workstation Pro 和 VirtualBox。VMware Workstation Pro 由 Broadcom 旗下 VMware 提供，其授权政策在 2024 年后发生过调整，下载前应查看官网的当前许可条款。VirtualBox 由 Oracle 维护，核心软件采用开源许可；扩展包可能适用不同许可证，也需要按用途确认。

两者都提供虚拟磁盘、快照和多种网络模式，但界面、硬件支持和具体行为并不完全相同。下文以 VMware 为例；使用 VirtualBox 时，应在其文档中找到对应设置。

==== 创建虚拟机与安装系统

安装 VMware 后，选择“创建新的虚拟机”，客户机操作系统选 Linux / Ubuntu 64 位。可以先分配 4 个处理器核心、8 GB 内存和 60 GB 以上虚拟磁盘，同时为 Windows 宿主系统保留足够的处理器和至少 8 GB 内存。具体数值应根据物理配置和任务调整。动态增长的虚拟磁盘不会在创建时立即占满设定容量，但最终仍会占用相应的宿主磁盘空间。

创建完成后，在虚拟机设置的 CD/DVD 选项中挂载 Ubuntu ISO 并开机。后续流程与双系统安装相近，但分区操作只作用于新建的虚拟磁盘，不会直接修改物理磁盘的分区表。可以先在这里熟悉“其他选项”中的分区界面，再操作双系统；仍要确认虚拟机选择的是虚拟磁盘，避免误把物理磁盘直通给客户机。

装完系统后，在虚拟机里打开终端，安装增强工具：

```bash
# VMware 客户机增强工具：自适应分辨率、与宿主机共享剪贴板
sudo apt update
sudo apt install open-vm-tools open-vm-tools-desktop
```

重启虚拟机后，窗口缩放、分辨率跟随和宿主机/虚拟机之间的剪贴板共享等功能通常会生效。VirtualBox 的对应组件称为“增强功能”（Guest Additions），可从“设备”菜单挂载安装介质；剪贴板共享等功能还需在设置中启用。

==== 快照：保存虚拟机状态

快照（snapshot）记录虚拟磁盘状态，并可选择包含内存与运行状态，之后可以把虚拟机恢复到该时间点。快照通常依赖原虚拟磁盘和宿主存储，不等同于独立备份；原文件损坏或宿主磁盘故障时，快照也可能一并丢失。

可以在安装系统后创建“干净系统”快照，在开发环境配置完成后创建“环境就绪”快照。练习删除命令、修改环境变量、安装显卡驱动或调整系统配置前，也可以先创建快照。出现问题后恢复快照通常比重新安装更快，但恢复会丢弃快照之后的状态变化，重要文件仍应单独保存。

虚拟机因此适合队内培训。后面的“Linux & Ubuntu 操作”一章会涉及删除文件、权限和系统配置等可能造成破坏的命令，可以先在不含重要数据的虚拟机中观察其作用范围，再在物理机上使用。快照降低了恢复成本，但不能消除误删共享目录或外部文件的风险。

==== 虚拟机的局限

虚拟机适合学习和部分开发任务，但作为整车视觉开发环境时有三类主要限制。了解原因后，可以判断哪些实验能在虚拟机中完成，哪些需要物理机或车载计算平台。

第一是图形性能。常规桌面虚拟机提供虚拟显卡，3D 加速能力有限，也不会直接向客户机提供宿主 NVIDIA 显卡的完整 CUDA 能力。日常桌面操作通常没有问题，但 RViz、Gazebo 和神经网络推理的性能与兼容性需要实际测试。支持 GPU 直通的其他虚拟化方案不在本章范围内。

第二是 USB 设备直通（passthrough）。鼠标、键盘和 U 盘等设备通常可以转交给客户机，但 USB 3.0 工业相机需要持续传输大量图像数据，经过虚拟化层后可能出现带宽下降、延迟波动、丢帧或断开。USB 转串口设备也需要经过转发；能被识别不代表时序和长期稳定性满足联调要求。

第三是时序稳定性。虚拟机线程还要经过宿主系统调度，Windows 后台任务可能造成额外的延迟波动。桌面操作可能察觉不到几十毫秒的延迟尖峰，但它会占用视觉处理链路的帧间预算，因此不能用虚拟机中的时序测量直接代表车载环境。

因此，虚拟机可用于 Linux 基础学习、分区练习和不依赖硬件的代码开发；工业相机、串口、GPU 和整链时序应在原生 Linux 或实际车载计算平台上验证。

=== WSL2

WSL2（Windows Subsystem for Linux 2）是 Microsoft 提供的 Windows 功能，通过轻量虚拟机运行 Linux 内核和发行版。它可以在 Windows 中直接打开 Ubuntu 命令行，并提供两边文件系统的互访能力，适合暂时不安装双系统、但需要练习命令行或使用 Linux 工具链的读者。

==== 使用 `wsl --install` 安装

本书要求 Windows 10 21H2 或更高版本，或 Windows 11。在开始菜单搜索 PowerShell，*右键以管理员身份运行*，执行：

```text
wsl --install -d Ubuntu-22.04
```

该命令会尝试启用所需的 Windows 组件并安装 Ubuntu 22.04。根据当前系统状态，执行后可能需要重启；若虚拟化未启用，还需检查 Windows 功能和固件设置。首次启动 Ubuntu 时会要求创建独立于 Windows 账户的用户名和密码。之后可以从开始菜单打开“Ubuntu 22.04”，或在终端输入 `wsl` 进入 Linux 命令行。

验证一下安装是否成功：

```text
wsl --list --verbose
```

输出中应能看到 `Ubuntu-22.04`，并且 VERSION 列显示 2，这表示发行版运行在 WSL2 模式。进入 Ubuntu 后，可以参考本章后面的换源和常用工具配置。WSL2 的 GPU 支持依赖 Windows 主机上的 NVIDIA 驱动，不应在 WSL 中安装普通 Linux 显示驱动；需要 CUDA 时，应按照 NVIDIA 的 WSL 专用说明安装工具包。

==== 与 Windows 的文件互通

WSL2 与 Windows 可以相互访问文件。在 Ubuntu 中，Windows 的 C 盘通常挂载到 `/mnt/c`，例如下载目录为 `/mnt/c/Users/你的用户名/Downloads`。在 Windows 资源管理器中，可以从侧栏的 Linux 项进入，也可以在地址栏输入 `\\wsl$` 查看 WSL 发行版中的文件。

跨 Windows 与 Linux 文件系统访问通常比在同一文件系统内读写慢，尤其会影响包含大量小文件的 C++ 编译和大型仓库的 `git status`。主要在 Linux 中构建的代码建议放在 Linux 家目录（`~`）下，并使用支持 WSL 的编辑器访问；`/mnt/c` 更适合交换文件。性能会随 WSL 与 Windows 版本变化，重要任务可用实际工程测量后再决定存放位置。

==== 能干什么，不能干什么

WSL2 提供 Ubuntu 用户空间，适合学习命令行与 Shell，也可用于 C++ 开发、CMake 构建和 Git 操作。配合 VS Code 的 WSL 扩展，可以从 Windows 界面编辑并编译 Linux 侧代码。较新的 WSL 还提供 WSLg 图形程序支持和特定条件下的 CUDA 支持，因此部分离线视觉实验也可以运行。涉及 systemd、内核模块、设备或网络的命令可能与原生 Ubuntu 不同，不能假定本书所有操作都原样适用。

硬件联调仍有明显限制。USB 设备通常需要借助 `usbipd-win` 等工具连接到 WSL，高带宽 USB 3.0 工业相机的兼容性、吞吐量和长期稳定性必须单独测试；USB 转串口同样如此。WSL 进程还受 Windows 宿主调度，不能用其时序结果代表车载系统。默认 NAT 或其他网络模式也可能影响 ROS 2 的组播发现以及与车载设备的通信，具体行为取决于 WSL 版本和网络配置。

因此，WSL2 可以作为 Linux 基础、C++ 开发和部分离线实验环境。工业相机、串口、ROS 2 多机通信和整车时序则应在验证过的原生 Linux 或车载 NUC 上完成。

=== 装机后的基础配置

完成 Ubuntu 安装后，还需要配置软件源、中文输入法、显卡驱动和 C++ 工具链。本节依次完成这些初始设置。以下操作均在终端中进行，可以按 `Ctrl+Alt+T` 打开；后续章节会解释相关命令的工作原理，这里先按步骤执行，并在修改系统配置前确认命令和文件路径。

==== 更换软件源

Linux 包管理器从配置的软件源（repository）下载软件包。在中国大陆访问 Ubuntu 默认服务器时，速度和稳定性可能不理想，可以改用清华 TUNA、中科大等镜像。实际速度取决于网络、镜像同步状态和地理位置，若默认源访问正常，也不必更换。

软件源配置存放在 `/etc/apt/sources.list` 文件里。动手前先备份——这是修改任何系统配置文件前的标准动作：

```bash
# 备份原始配置，出问题可以随时恢复
sudo cp /etc/apt/sources.list /etc/apt/sources.list.bak

# 用 sed 把默认源批量替换为清华源
sudo sed -i 's@//.*archive.ubuntu.com@//mirrors.tuna.tsinghua.edu.cn@g' /etc/apt/sources.list
```

如果你想看得更明白，也可以不用 `sed`，直接用文本编辑器打开配置文件（`sudo nano /etc/apt/sources.list`），把内容整个替换为：

```text
# Ubuntu 22.04 (jammy) 清华大学 TUNA 镜像源
deb https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ jammy main restricted universe multiverse
deb https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ jammy-updates main restricted universe multiverse
deb https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ jammy-backports main restricted universe multiverse
# 安全更新建议保留官方源，第一时间获取安全补丁
deb https://security.ubuntu.com/ubuntu/ jammy-security main restricted universe multiverse
```

换完源，刷新软件包索引并把系统升级到最新：

```bash
# update 是刷新“软件目录”，upgrade 才是真正升级软件
sudo apt update
sudo apt upgrade
```

`apt update` 的输出中应出现 `mirrors.tuna.tsinghua.edu.cn`，表示新的镜像配置已被读取。运行时间取决于网络；`apt upgrade` 第一次执行可能下载数百 MB 更新，完成前不要中断包管理进程。

==== 中文输入法 fcitx5

安装时选择了英文环境，现在补充中文输入。Linux 桌面通常通过输入法框架（input method framework）管理输入法，常见选择包括 Ubuntu 默认的 iBus 和 Fcitx 5。本书使用 Fcitx 5 及其中文附加组件；如果队伍已有可用的 iBus 配置，也无需为了统一教程而更换。

安装 Fcitx 5 及中文引擎：

```bash
# fcitx5 主程序 + 中文输入引擎（拼音等）+ 各图形框架的支持组件
sudo apt install fcitx5 fcitx5-chinese-addons fcitx5-frontend-gtk3 fcitx5-frontend-qt5

# 把系统默认输入法框架切换为 fcitx5
im-config -n fcitx5
```

先注销当前桌面会话并重新登录，让 `im-config` 的选择进入新会话；通常不需要手工修改系统级环境变量。重新登录后运行 `fcitx5-configtool`，把“Pinyin（拼音）”添加到输入法列表，再按 `Ctrl+Space` 测试切换。GTK、Qt、浏览器以及 Wayland/X11 会话的行为可能不同，应分别测试实际使用的应用。

如果只有某一类应用无法使用输入法，先运行 `im-config -m` 查看当前选择，并检查对应的 GTK/Qt 前端包和 Fcitx 诊断输出。部分软件或队伍统一镜像可能确实需要显式设置 `GTK_IM_MODULE`、`QT_IM_MODULE`、`XMODIFIERS` 或 `SDL_IM_MODULE`；这应作为针对具体问题的排障步骤。`/etc/environment` 会影响机器上的所有用户，修改前应备份并确认没有重复或冲突项，重新登录后还要验证各类应用，而不要把这些变量无条件追加到每台机器。

==== NVIDIA 显卡驱动

需要使用 NVIDIA GPU 和 CUDA 的机器必须安装兼容的驱动。未加载合适驱动时，系统可能只能使用基础显示或 nouveau，CUDA 不可用，神经网络推理会回退到 CPU，性能取决于具体模型和处理器。安装阶段使用 `nomodeset` 只解决临时显示问题，不能替代正式驱动。

Ubuntu 提供了官方的驱动管理工具，先看看系统检测到了什么：

```bash
# 列出检测到的硬件和可用的驱动版本
ubuntu-drivers devices
```

输出会列出你的显卡型号和一串候选驱动，其中一行末尾带有 `recommended` 标记——那就是系统推荐的版本。直接自动安装推荐版本：

```bash
# 自动安装推荐的 NVIDIA 驱动
sudo ubuntu-drivers autoinstall
# 装完必须重启，新驱动才会接管显卡
sudo reboot
```

重启后验证驱动是否正常工作：

```bash
nvidia-smi
```

正常情况下，`nvidia-smi` 会显示驱动版本、驱动所支持的 CUDA 版本、显卡型号、显存占用和温度。训练或部署推理时也可用它观察显存与利用率。命令无法访问设备时，应先检查驱动安装日志、`lsmod` 中是否加载 NVIDIA 模块，以及 Secure Boot 是否拒绝了未签名模块；不要直接假定关闭 Secure Boot 就能解决所有驱动问题。

本书不采用 NVIDIA 官网的 `.run` 安装包。它绕过 Ubuntu 包管理器，与发行版驱动包混用时容易产生文件和版本冲突，内核升级后也可能需要重新安装或编译模块。`ubuntu-drivers` 安装的驱动由 apt 和 DKMS 管理，更适合本书的 Ubuntu 环境。只有在明确需要某个官方仓库未提供的版本，并准备好处理升级和卸载时，才考虑其他安装方式。

==== 安装常用开发工具

先安装后文会使用的一组基础工具：

```bash
sudo apt install git curl wget vim htop tree build-essential cmake gdb
```

下面简要说明各软件包的用途，后续章节会继续使用：

- `git` 是版本控制工具，详见 Git 版本控制一章。
- `curl` 和 `wget` 用于在命令行下载文件和测试网络接口，许多安装脚本会调用它们。
- `vim` 是终端文本编辑器，可在通过 SSH 登录且没有图形界面时修改配置文件。
- `htop` 是交互式进程监视器，可在现场联调时查看 CPU、内存和进程状态。
- `tree` 以树状形式打印目录结构，便于检查工程布局。
- 现代 Ubuntu 网络排障优先使用 `ip`、`ss` 等 `iproute2` 工具；只有在阅读仍使用 `ifconfig`、`netstat` 的旧资料时，才需要另行安装兼容包 `net-tools`，并注意新旧命令的输出格式不同。
- `build-essential` 是一个元包，用来安装 GCC、G++、Make、标准库开发文件等常见构建基础。单独安装合适的编译器也能编译简单程序；使用这个元包是为了准备一套较完整且一致的基础工具。
- `cmake` 是常用的 C++ 构建系统，OpenCV 和许多 ROS 工程都使用它，详见 CMake 与构建系统一章。
- `gdb` 是调试器，程序段错误崩溃时用它定位现场。

只有需要被其他机器远程登录时，才安装 SSH 服务器：

```bash
sudo apt install openssh-server
```

`openssh-server` 会让这台机器能够接受 SSH 连接。车载 NUC 通常需要它；个人笔记本若不提供远程登录，可以不装。启用后还应设置可靠的账户认证，确认防火墙与调试网络的访问范围，并用 `systemctl status ssh` 检查服务状态。

==== 面向现场联调的电源与锁屏设置

Ubuntu 桌面版可能在长时间无键盘鼠标输入后息屏、锁定或挂起。车载 NUC 在无人直接操作时如果自动挂起，视觉节点和串口通信会一起停止；运行长时间任务的开发机也可能因此中断实验。因此，应根据设备用途关闭自动挂起，并分别决定是否保留息屏和锁屏。

可以在“设置 → 电源”中逐项修改，也可以使用下面的命令；批量配置新 NUC 时，可将经过验证的命令纳入部署脚本：

```bash
# 永不自动息屏
gsettings set org.gnome.desktop.session idle-delay 0
# 接通电源时永不自动挂起
gsettings set org.gnome.settings-daemon.plugins.power sleep-inactive-ac-type 'nothing'
# 关闭屏幕自动锁定
gsettings set org.gnome.desktop.screensaver lock-enabled false
```

部分车载 NUC 会在“设置 → 用户”中开启*自动登录*，并配合系统服务在上电后启动视觉程序。自动登录会让能接触设备的人直接进入该账户，关闭锁屏也有相同风险；个人笔记本不应直接套用车载配置，车载设备也应结合物理访问控制和账户权限评估。

完成这些设置后，继续配置代码编辑与远程开发环境。

=== 开发环境搭建

工具链安装完成后，还需要选择代码编辑环境。本节以 Visual Studio Code（VS Code）为例：它免费使用、插件较多，并支持从笔记本远程编辑和调试车载 NUC 上的代码。队伍已有统一编辑器时，继续沿用即可。

==== 安装 VS Code

VS Code 官方提供 Ubuntu 的 `.deb` 安装包。用浏览器打开 `https://code.visualstudio.com`，下载 `.deb` 版本（64 bit）。先在终端列出下载目录中的候选文件：

```bash
cd ~/Downloads
ls -lh -- code_*.deb
```

确认列表中目标包的时间、架构和完整文件名后，输入 `sudo apt install ./code_`，再按 `Tab` 补全为那个文件名并执行。不要直接把 `./code_*.deb` 交给 `apt`：下载目录中保留多个版本时，通配符会展开成多个参数，可能同时尝试安装多个包。

Ubuntu 软件商店提供 snap 版 VS Code，但它的沙箱机制可能影响中文输入法或部分文件访问场景。本书使用官方 `.deb` 包；安装后会注册 Microsoft 软件源，后续可随 `apt upgrade` 更新。不同发行渠道的行为会随版本变化，遇到问题时应先确认实际安装来源。

装好后在终端输入 `code` 即可启动。开发工程时，通常会先进入工程目录，再执行 `code .`，把当前目录作为工作区打开。

==== 常用插件

点开左侧扩展图标（或按 `Ctrl+Shift+X`）搜索安装。初次配置时可以先安装以下几项，之后再按实际需要补充：

- *C/C++*（微软官方）：提供 C++ 的代码补全、跳转定义、语法检查和调试支持。它改善编辑与调试体验，但命令行编译示例只依赖编译器，不要求安装该扩展。
- *CMake Tools*（微软官方）：在 VS Code 中配置、构建和调试 CMake 工程，并可与 C/C++ 插件配合使用；只在终端运行 CMake 时不需要它。
- *Remote - SSH*（微软官方）：用于连接远程开发机，下一小节会介绍具体用法。
- *Chinese (Simplified) 中文语言包*：可选，用于将界面切换为中文。错误信息和许多技术文档仍以英文为主，因此也需要熟悉常见的英文提示。

其他常用选择包括 C++ 语言服务 clangd、微软官方的 Python 插件和 Git 增强插件 GitLens。clangd 与微软 C/C++ 插件的功能有所重叠，性能和诊断效果也会随工程与配置而变化，选择一种适合队内工程的语言服务即可。插件会占用一定资源，遇到具体需求时再安装更便于排查配置问题。

==== Remote-SSH：在车载计算机上远程开发

车载 NUC 通常装在机器人底盘内，不会长期连接显示器和键盘，但修改参数、查看日志和调试程序都要直接操作 NUC 上的文件与进程。将 NUC 接入调试网络后，笔记本可以通过 SSH 登录；VS Code 的 Remote-SSH 插件则在这一连接上提供文件浏览、代码编辑、终端和调试功能。界面仍显示在笔记本上，实际打开的文件和执行的命令都位于 NUC，因此不必为每次修改拆装显示器或来回复制代码。

前提是目标机器已经按需安装 `openssh-server`，并且网络可达。第一次连接前，先在 NUC 本机或由可信管理员取得 SSH 主机密钥指纹；例如，NUC 上可以执行 `sudo ssh-keygen -l -f /etc/ssh/ssh_host_ed25519_key.pub` 查看 Ed25519 主机密钥指纹。假设 NUC 的 IP 是 `192.168.1.100`、用户名是 `rm`，再从笔记本验证 SSH 连接：

```bash
ssh rm@192.168.1.100
```

客户端首次看到该地址时会显示主机密钥指纹。只有它与刚才通过独立渠道取得的值完全一致时，才输入 `yes` 保存；不一致或连接过但指纹突然变化时应停止并排查，不能只为继续连接而删除记录。随后输入远端账户的登录凭据。能够登录说明当前网络、SSH 服务、主机身份核对和账户认证在本次测试中正常；输入 `exit` 可以退出。

然后在 VS Code 中按 `F1`，输入“Remote-SSH: Connect to Host”，新建 SSH 主机并填入 `rm@192.168.1.100`。VS Code 会在远端部署服务组件，连接完成后左下角会出现远程标识；此后打开的文件夹和终端都在 NUC 上。经常连接同一台机器时，可以在 `~/.ssh/config` 中设置别名：

```text
# 之后连接时直接选 "hero" 即可，不用记 IP
Host hero
    HostName 192.168.1.100
    User rm
```

经常连接时，可以配置 SSH 密钥认证，避免每次输入账户密码。基本过程是用 `ssh-keygen` 生成密钥对，再把公钥添加到目标机器；下一章介绍 SSH 远程登录时会给出完整步骤。GitHub 的 SSH 认证也使用密钥对，具体配置见 Git 版本控制一章。

==== 用一个 Hello World 验收

最后用一个最小程序检查开发环境：依次新建目录、编写代码、编译并运行。打开终端：

```bash
# 建一个示例目录并用 VS Code 打开
mkdir -p ~/code/hello
cd ~/code/hello
code .
```

在 VS Code 中新建文件 `hello.cpp`，输入以下内容。编辑器可能提示安装 C++ 扩展，可以按前面的功能说明决定；是否安装不影响接下来在终端调用 `g++`：

```cpp
#include <iostream>

int main() {
    // 输出一行测试信息
    std::cout << "Hello, RoboMaster CV!" << std::endl;
    return 0;
}
```

回到终端（VS Code 里按 `Ctrl+反引号` 可以直接打开内置终端），编译并运行：

```bash
# 用 g++ 编译，-o 指定输出的可执行文件名
g++ hello.cpp -o hello
# 运行它（./ 表示当前目录，原因见上一章）
./hello
```

```text
Hello, RoboMaster CV!
```

看到这行输出，说明编辑器、编译器和终端可以共同完成最基本的开发流程。后续视觉工程仍会经历“编辑、编译、运行”这一循环，只是 CMake 将负责组织多个源文件和依赖的构建过程，详见 CMake 与构建系统一章。如果某一步报错，可以先检查对应工具：找不到 `code` 通常说明 VS Code 尚未正确安装或未加入 `PATH`，找不到 `g++` 则应检查 G++ 或 `build-essential` 是否安装成功。

==== 终端与下一步

终端还可以配置其他 Shell（如 zsh）、自动补全建议、语法高亮和提示符主题。这些功能既涉及外观，也会影响日常操作方式；下一章先解释终端与 Shell 的关系，再介绍相应配置，便于理解每项设置的作用。

至此，Ubuntu 22.04 的安装方案、软件源、中文输入、NVIDIA 驱动、C++ 工具链以及本地和远程编辑环境都已介绍完毕。下一章将从终端和 Shell 开始，继续学习 Linux 中常用的文件、进程、权限和网络操作。
