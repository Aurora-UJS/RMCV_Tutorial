#import "/template/template.typ": *

「卡尔曼滤波」一章说明了如何把目标状态外推到弹丸到达的时刻，却没有给出飞行时间；「坐标变换」一章把计算放到重力方向固定的 odom 惯性系，也没有展开弹道方程。本章把这两部分接起来：输入是 odom 系中的目标位置与速度、枪口位姿和弹速估计 $v_0$，输出是云台 yaw、pitch 与用于预测的时间。几个量彼此耦合：pitch 改变水平初速，水平初速改变飞行时间，飞行时间又决定应预测到哪个目标位置。后文先建立可核对的无阻力基线，再加入阻力近似、数值求根和最外层预测迭代。

先固定几何定义：下文的水平距离 $d$ 与高度差 $h$ 都从*枪口*量到目标，而不是从相机量起。应把相机测得的目标点、手眼外参和枪口相对云台的机械位姿放入同一条坐标变换链，得到枪口系或以枪口为原点的惯性系坐标；不能只在某个轴上随手减去几厘米。本文取 $z$ 轴向上，$h>0$ 表示目标高于枪口，重力加速度写作大小 $g>0$、方向 $-z$。

=== 无阻力基线：封闭解与双解取舍

先忽略空气阻力。设 $d>0$，弹丸以速度 $v_0$、仰角 $theta$ 出膛，只受重力：

$
d = v_0 cos theta dot T, quad h = v_0 sin theta dot T - 1/2 g T^2
$

从第一式解出 $T = d \/ (v_0 cos theta)$ 代入第二式，用 $1 \/ cos^2 theta = 1 + tan^2 theta$ 整理，得到一个关于 $tan theta$ 的一元二次方程，直接给出封闭解：

$
tan theta = frac(v_0^2 plus.minus sqrt(v_0^4 - g (g d^2 + 2 h v_0^2)), g d)
$

二次方程在判别式为正时给出低、高两条数学弹道。取一组示例参数（$v_0 = 25 "m/s"$、$d = 8 "m"$、$h = 0.2 "m"$）：负号根为 $theta approx 5.0 degree$、飞行时间约 $0.32 "s"$；正号根约为 $86.4 degree$，理想模型中的最高点约 $31.8 "m"$、飞行时间约 $5.1 "s"$。RoboMaster 直射自瞄通常选择满足云台俯仰限位、场地净空和最大飞行时间约束的最短时间根，因此多数情况下是低弹道；“总取负号”不应脱离这些约束写成通用规则。若判别式小于零，则该目标在*无阻力、给定弹速*的模型下不可达；若 $d$ 接近零，公式中的除法也需要单独处理。解算器应显式返回无解状态，不能让 NaN 进入后续控制。

基础篇「软件工程基础」中的 `BallisticSolver` 单元测试假设无阻力低弹道；这里的封闭解可以为 `Solve` 提供独立期望值，并补上判别式、近零距离和机械限位等边界用例。加入速度相关阻力后，一般不再有同样简单的初等函数解，工程上通常采用数值积分、数值求根或经过验证的近似模型。

=== 阻力是否需要建模：先与误差预算比较

无阻力模型是否够用，取决于距离、弹速和系统允许的竖直误差。先用球体平方阻力模型估算量级：$F = 1/2 rho c_d A v^2$。除以质量并合并常数，得到

$
a_"阻" = underbrace(frac(rho c_d A, 2 m), k) dot v^2
$

$k$ 的单位是 $"m"^(-1)$，是这个简化模型的阻力参数。下表取干燥空气在约 25 ℃、101.3 kPa 下的密度 $rho = 1.184 thin "kg/m"^3$，并用光滑球在相应雷诺数范围内常见的 $c_d approx 0.47$ 作初算。直径和质量采用 16.8 mm/3.2 g 与 42.5 mm/44.5 g 两组示例值；实际弹丸批次、温湿度、表面接缝和旋转都会改变结果，因此这些数字用于量级判断，不是器件标称的替代品。

#figure(
table(
  columns: (auto, auto, auto, auto, auto),
  align: (left, center, center, center, center),
  stroke: (x, y) => (
    top: if y == 1 or y == 0 { 1pt } else { 0pt },
    bottom: if y == 2 { 1pt } else { 0pt },
  ),
  inset: 8pt,
  [*弹丸*], [*$k$ ($"m"^(-1)$)*], [*示例弹速*], [*出膛减速度*], [*飞 8 m 后存速*],
  [17 mm / 3.2 g], [0.0193], [25 m/s], [$12.0 thin "m/s"^2 approx 1.23 g$], [85.7%（约 21.4 m/s）],
  [42 mm / 44.5 g], [0.0089], [12 m/s], [$1.28 thin "m/s"^2 approx 0.13 g$], [93.1%],
),
caption: [平方阻力模型的量级估算（$c_d = 0.47$，$rho = 1.184 thin "kg/m"^3$）。第四列是出膛瞬间的 $k v_0^2$；17 mm 示例中它与重力同量级，说明阻力未必只是小修正。42 mm 示例的 $k$ 较小，来自此处采用的面积质量比，而不是“大弹丸必然更抗风”的普遍结论。末列按忽略重力和竖直速度的一维关系 $v(x) = v_0 e^(-k x)$ 计算，只用于比较。],
)

更直接的判断方法是比较落点。下面的数值实验先用无阻力低弹道瞄准，再用完整二维平方阻力常微分方程（ordinary differential equation，ODE）积分到同一水平距离。为单独观察距离和弹速的影响，右图把目标设为与枪口等高：对 25 m/s 的 17 mm 示例，两个模型在 3 m、5 m、8 m 处的竖直差约为 0.3 cm、1.3 cm、5.6 cm；速度改为 15 m/s 时，8 m 处约为 16 cm。这些是指定 $rho$、$c_d$、尺寸与数值模型下的结果，不是实弹命中率。若某系统分配给弹道模型的竖直误差预算为 6 cm，则图中曲线与 6 cm 线的交点给出“该近似开始超预算”的示例距离；换用 3 cm 预算或另一批弹丸，结论会随之改变。

#figure(
  image("images/practice-ballistics-drag.png", width: 100%),
  caption: [无阻力瞄准与二维平方阻力仿真的差异。左图取 17 mm、25 m/s、目标位于 8 m 与 0.2 m 高处；灰虚线是无阻力轨迹，蓝实线是 ODE 仿真，目标距离处相差约 5.6 cm。右图把目标设为与枪口等高，再扫描距离和几组示例弹速；纵轴仍是两种模型的竖直差，不是实测脱靶量。灰线是人为设定的 6 cm 竖直误差预算，不代表装甲板半高或统一命中边界。在这项预算下，17 mm 的 25、20、15 m/s 曲线分别约在 8.2、7.1、5.8 m 处越线。生成脚本 `examples/practice/ballistics_drag.py`。],
)

阻力还会改变飞行时间。同一仿真在 8 m 处给出无阻力约 321 ms、带阻力约 347 ms，相差约 26 ms；若目标以恒定 3 m/s 横移，而预测少算了这 26 ms，对应约 7.8 cm 的横向位置差。这个乘法只描述匀速目标模型下的量级，实际误差还受滤波状态、发射前延迟和目标机动影响。也就是说，阻力模型同时进入竖直补偿与目标提前量，不能只看落点高度。

=== 带阻力模型：从完整 ODE 到平直弹道近似

在二维竖直平面中，平方阻力模型可以写成

$
dot(bold(p)) = bold(v), quad
dot(bold(v)) = -k norm(bold(v)) bold(v) + (0, -g)
$

阻力方向和大小同时依赖水平、竖直速度，这组非线性 ODE 一般用数值积分求解；本章 `ballistics_drag.py` 采用 0.1 ms 固定步长作示例。生产代码还应比较缩小步长后的结果，或使用带误差控制的积分器。若每帧都用完整积分做打靶搜索，计算量会高于解析近似，因此社区实现常再引入*平直弹道*假设：对小 pitch 直射，把 $norm(bold(v)) approx v_x$，忽略竖直阻力，只保留重力。注意竖直阻力实际约为 $-k norm(bold(v)) v_z$，并不是“两个小量相乘后必然可忽略”；近似是否合格要由目标距离、pitch 和弹速范围上的对照仿真或实测决定。

水平方向于是只剩一项：

$
frac(d v_x, d t) = -k v_x^2
$

分离变量积分一次得速度，再积分一次得位移：

$
v_x (t) = frac(v_(x 0), 1 + k v_(x 0) t), quad
x(t) = 1/k ln(1 + k v_(x 0) t)
$

其中 $v_(x 0) = v_0 cos theta$ 是水平初速。这条对数位移式不是经验拟合，而是*降阶后的方程* $dot(v_x)=-k v_x^2$ 的严格积分；它并不是完整二维平方阻力 ODE 的解析解。pitch 增大、飞行时间变长或竖直速度受重力积累后，$norm(bold(v)) approx v_x$ 与“忽略竖直阻力”都会变差，高抛或大高度差场景应回到数值积分打靶。用一个拟合后的有效 $k$ 有时能在标定范围内减小这类模型差异，但该参数会同时吸收阻力、旋转、零位与模型缺项，不能再被解释成物理 $rho c_d A/(2m)$ 的测量值，也不能保证外推到其他弹速和 pitch。参数辨识一节会给出相应的留出验证方法。

=== 飞行时间：先判断有解，再安全求根

先看一个便于推导的一维目标模型：目标始终沿当前射线运动，当前水平距离为 $d$，恒定径向速度为 $dot(d)$（远离为正）。在平直弹道近似下，最早相遇时间 $T$ 满足

$
1/k ln(1 + k v_(x 0) T) = d + dot(d) T
$

左边是弹丸位移，右边是目标距离。静止目标（$dot(d) = 0$）时可直接写成 $T = (e^(k d) - 1) \/ (k v_(x 0))$；代码中宜用 `expm1(k*d) / (k*vx0)`，避免小量相减损失精度。$k$ 接近零时，位移式应切换到极限 $x(T)=v_(x 0)T$：静止目标有 $T=d/v_(x 0)$，径向匀速目标在 $v_(x 0)>dot(d)$ 时有 $T=d/(v_(x 0)-dot(d))$，否则追不上。$k>0$ 且目标径向运动时也可写成 Lambert W 形式，但数值求根更直接。定义

$
f(T) = 1/k ln(1 + k v_(x 0) T) - d - dot(d) T, quad
f'(T) = frac(v_(x 0), 1 + k v_(x 0) T) - dot(d)
$

计算 $f(T)$ 时相应使用 `log1p(k*vx0*T)`。牛顿法从初值 $T_0$ 出发，用当前点切线的零点更新：

$
T_(n+1) = T_n - frac(f(T_n), f'(T_n))
$

这一步是求 $f(T)=0$ 的牛顿法，不是对某个损失函数做梯度下降。牛顿法不需要梯度下降的学习率，但也并非在任意初值下都稳定；只有根是单根、导数不为零且初值足够接近时，才具有局部二次收敛。图中的特定算例取 $d=8 "m"$、$dot(d)=1.5 "m/s"$、$v_0=25 "m/s"$ 与 $theta=5 degree$，从 $T_0=d/v_(x 0)$ 出发，时间误差约按 $52 "ms" arrow.r 0.61 "ms" arrow.r 0.081 "μs"$ 下降，随后到达浮点精度。这个结果说明良好初值下可以很快，不是“三步适用于所有目标”的证明。

$f(0)=-d<0$，而 $f$ 的二阶导为负，所以它是凹函数，但“凹”本身不保证存在唯一正根。若目标静止或接近，即 $dot(d)<=0$，则 $f'(T)>0$，在模型允许的正时间内至多有一个根；接近目标的根还应位于目标越过枪口的时刻 $T_"cross"=d/(-dot(d))$ 之前。对退离目标，若 $dot(d) >= v_(x 0)$，弹丸初始闭合速度就不为正，且之后只会继续减速，因此没有首次追上目标的根。若 $0 < dot(d) < v_(x 0)$，$f$ 在

$
T_"peak" = frac(v_(x 0) / dot(d) - 1, k v_(x 0))
$

处达到最大值。若 $f(T_"peak")<0$，则无解；若等于零，只有一个导数也为零的相切根；若大于零，会出现两个数学交点，物理上应取区间 $[0,T_"peak"]$ 内的第一次相遇。这些结论都建立在目标保持一维匀速、弹丸与目标沿同一射线运动的假设上。

稳健实现应先构造一个 $f$ 单调上升、只可能包含*第一次相遇*的区间。静止或接近时，右端取 $T_(max)$；接近目标还应取它与 $T_"cross"$ 的较小值。退离目标则取 $T_(max)$ 与 $T_"peak"$ 的较小值。右端的 $f$ 为正时，便与 $f(0)<0$ 组成变号区间；在容差内等于零时，右端本身就是根。若右端仍为负，要继续区分“允许时域内没有根”和“退离目标在峰值处仍小于零，因而模型内确实无解”。$T_(max)$ 由场地、枪口限位、目标模型有效期和允许提前量共同设定，而不是固定照抄 1 s。建立区间后再使用带保护的牛顿法：牛顿步留在区间且 $abs(f')$ 足够大时接受，否则退回二分；每步更新区间，并同时设置时间容差、残差容差和迭代上限。这样，“无解”“超出模型时域”和“数值未收敛”会成为三个明确返回状态，而不是同一个异常数值。

=== 竖直补偿与交替迭代

在平直弹道近似中，竖直方向只保留重力。若 $h$ 已经是目标在相遇时刻相对枪口的预测高度，则

$
h = v_0 sin theta dot T - 1/2 g T^2
quad arrow.r.double quad
sin theta = frac(h + 1/2 g T^2, v_0 T)
$

程序必须先检查右侧是否落在 $[-1,1]$，再选择满足机械 pitch 范围的低角分支；越界意味着当前 $T$、弹速和高度组合在该简化模型下无解。按落点误差比例修正 pitch 的社区写法也是求根迭代，但其收敛速度取决于比例系数和局部斜率，不能仅凭“小角度近似线性”承诺固定轮数。

pitch 改变后，水平初速 $v_(x 0) = v_0 cos theta$ 也会改变，因此可交替更新：用新 $theta$ 重解 $T$，再用新 $T$ 重解 $theta$。在 $theta approx 5 degree$ 附近，pitch 改变 $1 degree$ 使 $v_(x 0)$ 的相对变化约为 $tan(theta) times 1 degree approx 0.15%$，所以常见直射工况下耦合较弱。仍应以 $abs(Delta T)$ 与 $abs(Delta theta)$ 的容差停止，并设置轮数上限；高 pitch、接近可达边界或 `asin` 参数接近 1 时，局部敏感度会明显增大，两轮收敛不再是保证。

=== 闭环：飞行时间与预测的不动点

最外层耦合来自目标预测。设从图像参考时刻到弹丸实际出膛的有效时间为 $t_d$，则滤波器要预测到 $t_d+T$；而飞行时间 $T$ 又由这个未来位置决定。可以把它写成不动点：用当前 $T$ 预测目标，再由预测位置计算新的飞行时间。一个直接实现是

+ 由当前距离得到初值 $T$，例如无阻力低弹道的飞行时间；
+ 调用滤波器的预测函数，计算 $bold(p)(t_d+T)$；匀速模型是 $bold(p)+bold(v)(t_d+T)$，旋转整车模型则由自身状态方程给出；
+ 从预测位置得到水平距离与高度，重解 $T$ 和 $theta$；
+ 重复第 2、3 步，直到增量低于容差，或达到轮数上限。

在目标速度远小于弹速、预测函数平滑且远离可达边界时，这个映射通常是局部压缩的；其导数量级与目标沿视线速度和弹丸有效水平速度之比有关，但横向运动、阻力、pitch 与非线性旋转也会进入雅可比。图中算例取目标初始距离 8 m、退离 1.5 m/s、横移 2.5 m/s，观察到局部收缩因子约 0.089：第一轮后的时间误差约 6.4 ms，第二轮约 0.56 ms，第三轮约 0.05 ms。这只支持该参数组合。实现仍应记录最终残差；若残差不降、目标预测时域过长或出现无解，应拒绝发射而不是返回最后一轮结果。

#figure(
  image("images/practice-ballistics-iteration.png", width: 100%),
  caption: [两层迭代在指定算例中的数值收敛。左图统一画时间量：灰线是二分区间半宽，因此每步严格减半；蓝线是牛顿迭代相对高精度参考根的实际时间误差，在根附近呈二次收敛。右图取 $t_d=0$，时间误差与 pitch 误差分别以秒和度表示；两条曲线的数值大小不宜横向比较，本例各自的局部收缩因子约为 0.089。对数图只能描述这次初值、参数与模型下的行为；上线前还要测试可达边界、快速接近/退离、旋转目标和坏初值，并保留区间保护与不收敛分支。生成脚本 `examples/practice/ballistics_iteration.py`。],
)

yaw 可以在每轮预测位置上由 $psi=op("atan2")(p_y(t_d+T), p_x(t_d+T))$ 直接得到；若目标模型与云台可达性相关，也可以把 yaw 限位检查放进迭代。$t_d$ 汇总曝光参考时刻到实际出膛之间的有效延迟，但识别调度、通信、云台控制和供弹过程都可能抖动或随状态变化。把它近似为常数之前，应先测量分布并确认残余量满足提前量预算，验证一节会继续展开。

=== 弹速的现实：设定值不是出膛速度

公式需要的是当前这发弹丸的实际出膛速度，而摩擦轮转速设定值只是间接输入。弹丸尺寸、轮面状态、温度和连发恢复过程都会造成逐发变化，具体波动必须从本车日志统计。无阻力、小角度近似的下坠为 $g d^2 \/ (2 v_0^2)$；在 $d=8 "m"$、$v_0=25 "m/s"$ 附近，对 $v_0$ 的局部导数约为每 1 m/s 对应 4.0 cm 竖直差。这个数字没有计入阻力和 pitch，只用于说明弹速估计值得进入误差预算。

裁判系统测速模块可提供已发弹丸的测量值，经电控转发给视觉；这正是「通信协议设定」一章“当前弹速”字段的用途。它是*上一发*的事后观测，并且自身也有缺测、延迟和测量误差，不能当作下一发真值。一个可用起点是按发射模式分别维护最近若干发的稳健均值或指数滑动估计，同时记录样本数、标准差和最后更新时间；开机、切换档位或长时间未发射时退回经过标定的模式先验。滑动窗口在摩擦轮升温或连发掉速时会滞后，因此还应把轮速、供弹节奏等状态纳入检查。

弹速上限和处罚以当季规则手册及机器人类型为准，不应把某一赛季的 17 mm、42 mm 数字写死在通用求解器中。机械与电控需要在上限下留出波动裕量；视觉侧则应保存逐发速度、模式和时间戳，比较冷机、热机、单发与连发分布。观察到分布变化只能说明出膛速度随工况变化，具体由轮面、控制还是弹丸批次造成，还需相应的单变量检查。

弹速的新鲜度也会影响部署位置：电控侧更早获得轮速、姿态和发射状态，视觉侧更容易访问目标模型、日志与数值工具。「通信协议设定」一章已经比较过上下位机解算的取舍。本章按视觉侧实现展开，但方程与边界检查不依赖运行在哪块处理器；改变的是输入时效、协议载荷、算力预算和故障处理，而不是物理模型。

=== 参数从哪里来：辨识有效 $k$

理论式 $k=rho c_d A/(2m)$ 适合作为初值。真实弹丸的接缝、旋转与速度相关 $c_d$，以及平直弹道近似遗漏的竖直阻力，都会让仅含一个 $k$ 的模型出现结构误差。可以用实弹数据拟合一个*有效* $k$，但要明确它是“在指定弹速、距离、pitch 与发射机构下使该模型误差较小的校准参数”。它与物理阻力系数形状相似，却不能单独诊断阻力、旋转升力或某个机械机制。

在符合场地规范、具有可靠防护和挡弹设施的测试环境中，可以选择 2、4、6、8 m 等多个距离，并覆盖至少两组常用弹速；每组固定枪口位姿打若干发，逐发记录 pitch、实测弹速、枪口到靶面的几何关系、落点高度、时间和发射模式。组均值便于画图，但拟合时保留单发数据与测量不确定度，才能看见异方差和离群点。基础篇 Ceres 的弹道辨识小节说明了如何把数值积分放入残差模型，也提醒了参数相关性、积分不连续和独立验证问题；这里可令残差为预测落点与实测落点之差。

辨识前应单独测量或同时建模 pitch 零位、枪口高度和弹速尺度；否则这些量与 $k$ 可能互相补偿，优化器收敛也不能证明参数可辨识。拟合值与理论量级相差很大时，先查单位、坐标和共线参数。数据要按距离和弹速留出未参与拟合的组合，并报告训练与留出残差；只有留出范围内的误差满足预算，才能在该范围部署。若不同弹速需要明显不同的有效 $k$，应增加速度相关模型或缩小适用范围，而不是把一个平均值推广到全部工况。

=== 验证：用固定靶与运动靶分层检查

固定靶可以直接观察整条发射链的结果，但落点标注、靶面姿态和距离本身也有测量误差。固定弹速模式，在 2、4、6、8 m 等距离各打一组，逐发记录弹着点相对瞄准点的竖直偏差、实测弹速和工况，再统计均值、标准差、较高分位数与样本数。均值描述当前条件下的平均偏差，标准差描述散布；二者可以帮助安排下一项检查，却不能仅凭曲线形状确认根因。

#figure(
table(
  columns: (auto, auto),
  align: (left, left),
  stroke: (x, y) => (
    top: if y == 1 or y == 0 { 1pt } else { 0pt },
    bottom: if y == 4 { 1pt } else { 0pt },
  ),
  inset: 8pt,
  [*观察到的形状*], [*优先检查与区分方法*],
  [均值随距离近似*线性*增长], [检查 pitch 零位与枪管安装角；再改变已知零位，观察斜率是否按几何预测移动],
  [均值随距离*加速*增长], [检查弹速尺度、$k$ 与阻力近似；用实测弹速分组，并与完整 ODE 及留出距离比较],
  [均值较小，但标准差较大], [分别统计逐发弹速、供弹状态、风、机构散布和落点标注误差，不能直接归给某一模块],
  [各距离存在近似固定偏差], [检查枪口/相机原点、目标高度基准和固定平移；改变靶距与瞄准点定义做交叉验证],
),
caption: [固定靶偏差表给出的排查线索。线性和加速增长分别与角度零位、弹速/下坠误差的简单模型一致，但距离偏差、外参、风和模型耦合也可能产生相似曲线。表中第二列给出能改变结论的后续检查；四个距离的相关形状只能缩小范围，不能自动划定责任或确认根因。],
)

静态表主要检查几何与弹道；运动目标还需要曝光参考时刻到实际出膛的有效延迟 $t_d$。先说明下表该怎么读：第二列不是某台车的实测，也不是来自某篇文献的“常见值”，只是用一组假设范围演示预算怎样拆。真正上车时，要把它们换成本车数据，并写清每段的起止事件和时钟域。例如，曝光参考点究竟取曝光开始、中点还是结束，“图像可用”是指驱动交付还是算法线程取到，都要先约定。各段来自不同设备与状态，不能在未测量时直接相加成一个“固定真值”。

#figure(
table(
  columns: (auto, auto, auto),
  align: (left, center, left),
  stroke: (x, y) => (
    top: if y == 1 or y == 0 { 1pt } else { 0pt },
    bottom: if y == 6 { 1pt } else { 0pt },
  ),
  inset: 7pt,
  table.header([*环节*], [示例预算范围], [处理方式]),
  [曝光参考到图像可用], [约 1～20 ms], [由曝光时序、读出和传输共同决定；先统一时钟与事件定义],
  [识别、PnP 与预测], [约 2～25 ms], [记录各阶段分布与排队时间，不能只看均值],
  [上下位机通信], [约 2～15 ms], [按帧长、链路与调度实测，记录抖动和丢包],
  [云台跟随], [数毫秒～数十 ms], [通常是动态响应而非纯延迟，应结合控制器和当前误差建模],
  [供弹到出膛], [数十～数百 ms], [随机构相位、连发状态和卡滞变化，需要发射事件测量],
  [弹丸飞行 $T$], [约 100～600 ms], [随距离、弹速、pitch 与阻力变化，每次由模型计算],
),
caption: [曝光参考时刻到命中的时间组成。第二列只演示预算拆分，不是规则值、实测结果或通用经验区间；测量本车时还要记录每段的起止事件、中位数、较高分位数、样本数与工况。飞行时间通常较长且随距离变化，但较短环节若抖动数毫秒，在高速目标上仍可能主导横向误差。只有近似常量且已映射到共同时间轴的部分才适合并入单个 $t_d$；云台跟随和供弹若明显依赖状态，应保留条件模型或不确定度。],
)

验证顺序仍然是先静后动。静态偏差达到预算后，再用已知速度、双向运动的目标检查 $t_d$ 与预测闭环。对近似恒速横移，水平残差除以目标速度可以估计一个*有效的剩余提前时间*；这个估计同时受目标速度误差、yaw 零位、相机时间戳、云台响应和供弹状态影响，不能单次相除就归因为某个延迟。应覆盖至少两档速度和两个方向，在一部分记录上拟合、另一部分上验证，并检查残差是否随方向、速度或机构相位变化。「RoboMaster应用篇」的自瞄算法设计会继续讨论整链联调。

==== 本章小结

本章从枪口坐标和无阻力封闭解出发，给出了低、高弹道与无解分支；实际选择还要满足 pitch、净空和飞行时间约束。平方阻力的影响应与本车误差预算比较，平直弹道公式 $x(t)=ln(1+k v_(x 0)t)/k$ 是降阶模型的解析解，不是完整 ODE。飞行时间求解要先建立最早根的区间，再使用带保护的牛顿法；pitch 交替更新和目标预测不动点都需要收敛判据与失败分支，图中的三轮结果只对应所列算例。$v_0$ 来自带时效与不确定度的逐发观测，有效 $k$ 必须用实弹数据辨识并在留出距离、弹速上验证。固定靶检查几何和弹道，运动靶再检查有效出膛前延迟与预测闭环。至此，协议、标定、时间对齐和弹道四章提供了组装完整自瞄系统所需的接口与验证方法。
