#import "/template/template.typ": *

理论篇的结尾留了两笔账等着本章来清。「卡尔曼滤波」一章的落地一节说：拿当前状态沿模型向前推一个弹丸飞行时间，就得到瞄准点——但飞行时间从哪来，它没讲。「坐标变换」一章证明了弹道解算必须在惯性系做——重力方向在 odom 系里是恒定的 $-z$，这是整套计算能写成干净公式的前提——但公式本身，它也没讲。现在把这两笔账一起算清：输入是滤波器给出的目标状态（位置与速度，都在 odom 系），弹速 $v_0$ 来自电控转发的实测值；输出是云台的 yaw、pitch 和一个提前时间。麻烦在于这三样互相咬着：pitch 决定水平初速，水平初速决定飞行时间，飞行时间决定目标届时在哪，目标在哪又反过来决定 pitch。全章要做的事，就是把这个耦合一层层解开，最后用迭代收拢成一个不动点。

还有一处容易搞错的起点：下文的高度差 $h$ 是目标相对*枪口*的高度差，不是相对相机——相机与枪口差着几厘米的安装偏移，这笔偏移由「相机标定与手眼标定」一章的外参、加上枪口相对云台的机械安装位置合成，进弹道计算前就该扣掉。

=== 无阻力基线：封闭解与双解取舍

先把空气抽走。设目标在水平距离 $d$、高度差 $h$ 处，弹丸以速度 $v_0$、仰角 $theta$ 出膛，只受重力：

$
d = v_0 cos theta dot T, quad h = v_0 sin theta dot T - 1/2 g T^2
$

从第一式解出 $T = d \/ (v_0 cos theta)$ 代入第二式，用 $1 \/ cos^2 theta = 1 + tan^2 theta$ 整理，得到一个关于 $tan theta$ 的一元二次方程，直接给出封闭解：

$
tan theta = frac(v_0^2 plus.minus sqrt(v_0^4 - g (g d^2 + 2 h v_0^2)), g d)
$

二次方程给两个根，都是物理上真实存在的弹道。代一组真实数字看看它们各自长什么样（$v_0 = 25 "m/s"$、$d = 8 "m"$、$h = 0.2 "m"$）：取负号的低弹道 $theta approx 5.0 degree$，飞行 $0.32 "s"$；取正号的高弹道 $theta approx 86.4 degree$——弹丸近乎垂直上天，爬到 $31.8 "m"$ 的最高点再砸下来，全程 $5.1 "s"$。迫击炮用后者，自瞄毫不犹豫选前者：飞行时间短一个数量级，预测误差随时间累积得就少；弹道平直，对角度误差不敏感；场地也根本没有三十米的净空。所以*工程上一律取负号根*。根号里的判别式还免费送一个信息：它小于零意味着这个弹速够不着这个目标——低弹速吊射远处高地时真的会遇到，解算器必须处理这种"无解"分支，而不是带着 NaN 往下算。

基础篇 Eigen 一节的 `BallisticSolver::solve` 用小步迭代逼近的就是这个问题——现在你知道它其实有封闭解。那段迭代写法的真正价值在下一节显现：一旦把空气阻力加回来，封闭解就没有了，迭代是唯一的路。

=== 阻力值不值得管：先算一笔量级账

很多队伍的第一版弹道就是上面的封闭解，也确实能打中近处的靶子。空气阻力到底要不要管？别争论，算。球形弹丸的阻力是 $F = 1/2 rho c_d A v^2$，除以质量得减速度，把常数并成一个系数：

$
a_"阻" = underbrace(frac(rho c_d A, 2 m), k) dot v^2
$

$k$ 的单位是 $"m"^(-1)$，它就是后文弹道模型里唯一的空气参数。代入真实器件：空气密度 $rho = 1.169 thin "kg/m"^3$（约 25 ℃、标准气压）；弹丸在这个速度与尺寸下的雷诺数约 $3 times 10^4$，正落在光滑球体阻力系数 $c_d approx 0.47$ 的平台区。17 mm 弹丸直径约 $16.8 "mm"$、质量按 $3.2 "g"$ 计，42 mm 弹丸直径约 $42.5 "mm"$、质量约 $44.5 "g"$，算出来是这样一张表：

#figure(
table(
  columns: (auto, auto, auto, auto, auto),
  align: (left, center, center, center, center),
  stroke: (x, y) => (
    top: if y == 1 or y == 0 { 1pt } else { 0pt },
    bottom: if y == 2 { 1pt } else { 0pt },
  ),
  inset: 8pt,
  [*弹丸*], [*$k$ ($"m"^(-1)$)*], [*典型弹速*], [*出膛减速度*], [*飞 8 m 后存速*],
  [17 mm / 3.2 g], [0.019], [25 m/s], [$11.9 thin "m/s"^2 approx 1.2 g$], [86%（约 21.5 m/s）],
  [42 mm / 44.5 g], [0.0088], [12 m/s], [$1.3 thin "m/s"^2 approx 0.13 g$], [93%],
),
caption: [平方阻力的量级账（$c_d = 0.47$，$rho = 1.169 thin "kg/m"^3$ 的 25 ℃ 标压空气）。读表规则：看第四列——17 mm 弹丸满弹速出膛时，阻力减速度是重力的 1.2 倍，"阻力是小修正"的直觉在这里是错的；再看 42 mm 一行，弹丸大一圈反而 $k$ 减半——质量随尺寸立方增长、截面积只随平方，大弹丸天生更"抗风"。存速按 $v(x) = v_0 e^(-k x)$ 计算。],
)

减速度超过重力，听起来吓人，但工程问题从来不问"力大不大"，只问"差多少厘米"。做个干净的数值实验：用无阻力封闭解瞄准，把弹丸放进带阻力的世界里飞，看它在目标处低了多少。25 m/s 的 17 mm 弹丸：3 m 处低 0.3 cm，5 m 处低 1.3 cm，8 m 处低 5.5 cm；弹速掉到 15 m/s 时（裁判系统等级低或留大余量的场景），8 m 处低到 15.9 cm——比一整块装甲板还高。对照小装甲板约 6 cm 的半高，结论清楚：*3 m 内无阻力模型随便用，5 m 开始踩线，8 m 以上不管阻力必丢*。

#figure(
  image("images/practice-ballistics-drag.png", width: 100%),
  caption: [阻力量级的两张脸。左：用无阻力模型瞄准 8 m 外的目标（品红星），灰虚线是瞄准模型以为的弹道，蓝实线是带阻力的真实飞行——在目标处低了近 6 cm，将将脱靶。右：把同一实验扫过所有距离与弹速，纵轴是脱靶量；虚线品红为 42 mm。读图规则：每条曲线与灰色"半块装甲板高"参考线的交点，就是这档弹速下无阻力模型的失效距离——25 m/s 约 8 m，20 m/s 约 7 m，15 m/s 不到 6 m；弹速越低、曲线抬头越早，所以弹速余量留得越狠的车，越需要阻力模型。生成脚本 `examples/practice/ballistics_drag.py`。],
)

这笔账其实要收两次。阻力不只压低落点，还拉长飞行时间：8 m 处 25 m/s 的弹丸，飞行时间比无阻力模型多 25 ms——目标以 3 m/s 横移时，这 25 ms 就是 7.5 cm 的预测超前量误差，和落点误差同一个量级。竖直方向瞄低了、水平方向提前量又给少了，两个误差叠在一起，就是"近处指哪打哪、远处莫名其妙打不中"的典型病历。

=== 带阻力的模型：一个能用的简化

加上阻力后，弹道方程变成水平竖直互相耦合的非线性微分方程组（阻力沿速度反方向，大小依赖合速度），没有封闭解。硬解的办法是数值积分——基础篇 `BallisticSolver::solveWithDrag` 里那个 0.1 ms 步长的仿真循环就是——但每帧解算都要跑打靶搜索，值得先问问有没有更便宜的路。有，代价是一个假设：*弹道足够平直*。自瞄的直射弹道 pitch 通常在 $10 degree$ 以内，竖直速度不到水平速度的两成，于是可以拆开处理：水平方向承担几乎全部速度，阻力全算它的；竖直方向速度小，阻力项是小量乘小量，扔掉，只留重力。

水平方向于是只剩一项：

$
frac(d v_x, d t) = -k v_x^2
$

分离变量积分一次得速度，再积分一次得位移：

$
v_x (t) = frac(v_(x 0), 1 + k v_(x 0) t), quad
x(t) = 1/k ln(1 + k v_(x 0) t)
$

其中 $v_(x 0) = v_0 cos theta$ 是水平初速。这就是社区弹道实现里那条著名的对数位移公式的全部来历——不是拟合，不是近似查表，是平方阻力模型在"平直弹道"假设下的严格积分。假设的适用边界也要说清楚：pitch 大了它就虚。对高抛弹道（吊射前哨站、打高地）竖直速度不再是小量，这套拆分失效，老老实实回到数值积分打靶。还有一处诚实账：即便在平直弹道内，这个简化模型也不是零误差——它把竖直方向的阻力整个扔了，而直射弹道的大部分行程其实在上升段（这组参数下弹道顶点在 $x approx 5.4 "m"$），被扔掉的竖直阻力在上升段向下拽着弹丸，只有过顶下坠后才向上托。用物理参数 $k = 0.019$ 对比完整二维仿真，8 m 处这份模型误差约 3 cm（简化模型偏乐观，真实弹道在目标处还要再低约 3 cm）。别急着修它：这笔误差连同所有没建模的效应，最后会被参数辨识一并吸收——表现为辨识出的有效 $k$ 略大于理论值——本章末尾讲。

=== 飞行时间：牛顿法求根，不是梯度下降

模型有了，第一个要解的量是飞行时间 $T$。目标不等人：当前水平距离 $d$，径向速度 $dot(d)$（远离为正），弹丸飞 $T$ 秒后要赶到目标届时的位置，所以 $T$ 满足

$
1/k ln(1 + k v_(x 0) T) = d + dot(d) T
$

左边是弹丸的对数位移曲线，右边是目标的匀速直线。静止目标（$dot(d) = 0$）时它可以直接反解出 $T = (e^(k d) - 1) \/ (k v_(x 0))$；带上目标运动就没有封闭解了，定义

$
f(T) = 1/k ln(1 + k v_(x 0) T) - d - dot(d) T, quad
f'(T) = frac(v_(x 0), 1 + k v_(x 0) T) - dot(d)
$

用*牛顿法*求根：从初值 $T_0$ 出发，每一步用当前点的切线代替曲线，跳到切线与横轴的交点：

$
T_(n+1) = T_n - frac(f(T_n), f'(T_n))
$

先把一个流传很广的叫法错误摁住：有的资料把这步迭代称作"梯度下降"，不对，而且不是咬文嚼字——两者是不同的算法，收敛速度差一个档次。梯度下降解的是*最小化*问题，要选学习率，误差每步乘一个常数因子，慢慢磨；牛顿法解的是*求根*问题，没有步长要调，收敛是二次的——误差的指数每步翻倍。实测：以无阻力飞行时间 $T_0 = d \/ v_(x 0)$ 作初值，$|f|$ 依次是 $1 "m" arrow.r 1.2 "cm" arrow.r 1.5 "μm" arrow.r 10^(-14) "m"$，三步到机器精度。每帧都要解一次的计算，三步和三十步是实打实的算力差别。

收敛的把握也有几何来源：$f$ 是凹函数（对数的二阶导为负），凹函数的切线永远横在曲线上方，从根的左侧出发，每一步切线的零点都不会越过真根——单调逼近，不会发散。目标静止或退离时，无阻力初值必在根的左侧（阻力只会让弹丸更慢）；目标快速逼近时初值可能落到根的右侧，但凹函数的牛顿步会在一步之内跳回左侧，之后照样单调。两个工程护栏还是要加：其一，对数无限增长，数学上连"目标比弹丸跑得快"都有根，但那个根对应弹丸已经慢成抛物线的尽头——给 $T$ 设上限（比如 1 s），超限按够不着处理；其二，迭代次数封顶，防御性编程的常识。

=== 竖直补偿与交替迭代

$T$ 在手，竖直方向按"只受重力"的假设列方程，$theta$ 直接反解：

$
h = v_0 sin theta dot T - 1/2 g T^2
quad arrow.r.double quad
sin theta = frac(h + 1/2 g T^2, v_0 T)
$

这就是重力补偿：$arcsin$ 里的分子把"目标高度"和"下坠量"加在一起，一步到位。社区常用实现里还有一种按落点误差比例修正、迭代若干轮的写法（源自官方开发平台的弹道文档谱系），殊途同归——落点对 pitch 在小角度下近乎线性，比例修正本质上是又一个收敛很快的求根迭代。

但别急着收工，耦合还没解完：pitch 变了，水平初速 $v_(x 0) = v_0 cos theta$ 跟着变，上一节解出的 $T$ 就不再准确。解法朴素——*交替迭代*：用新 $theta$ 更新 $v_(x 0)$，重解 $T$；用新 $T$ 重解 $theta$；如此往复。它收敛得飞快，直觉在余弦的平坦上：直射 pitch 在 $5 degree$ 附近，$cos theta$ 对 $theta$ 的敏感度是 $sin theta approx 0.1$——pitch 修正一度，水平初速只动千分之二，$T$ 跟着只动千分之二，再传回 $theta$ 时已经是三阶小量。耦合弱到这个程度，两轮就稳。

=== 闭环：飞行时间与预测的不动点

最外面还套着一层耦合，这次跨越了章的边界。「卡尔曼滤波」一章落地一节说过，异步预测的第三件事是*向未来外推*——拿滤波器状态推 $T$ 秒后的目标位置。可是推多远由 $T$ 说了算，$T$ 又由推出来的位置说了算。这不是死循环，是*不动点方程*：正确的 $T$ 满足"按 $T$ 外推出的位置，其对应的飞行时间恰好是 $T$"。解法还是迭代：

+ 用当前距离粗算一个 $T$（无阻力值就行）；
+ 请滤波器外推 $T$ 秒后的目标位置（匀速模型就是 $bold(p) + bold(v) T$；整车旋转模型给出的位置是 $T$ 的非线性函数，但对本环节只是个黑盒函数）；
+ 对外推位置重解 $T$ 与 $theta$（上两节的完整流程）；
+ 回到第 2 步，直到 $T$ 不再变。

收敛比价还是同一套：每轮 $T$ 的修正引起目标位置修正，再引起 $T$ 的二次修正，缩小的倍率约等于*目标径向速度与弹速之比*——步兵 2 m/s 对弹丸 25 m/s，比率不到 0.1。实测一个 8 m 外、以 1.5 m/s 退离加 2.5 m/s 横移的目标：每轮误差缩为上一轮的 0.09，两轮后 $T$ 的残差 0.5 ms、三轮后 0.05 ms——折合目标位移都不到 2 mm，早已淹没在测量噪声之下。工程上迭代三轮封顶，绰绰有余。

#figure(
  image("images/practice-ballistics-iteration.png", width: 100%),
  caption: [两层迭代的收敛实录（纵轴对数）。左：牛顿法解飞行时间，蓝线三步跌到机器精度，灰虚线是二分法参考——每步稳定砍半的"几何收敛"在对数轴上是一条缓坡（砍半的是区间宽度，$|f|$ 只按趋势下降，所以灰线有抖动），牛顿的二次收敛则是悬崖，这就是求根算法和"慢慢磨"类算法的差距。右：最外层不动点迭代，蓝线是 $T$ 的误差、品红线是 pitch 的误差，两条平行直线说明误差每轮乘同一个因子 0.09（斜率即收缩率）——第 3 轮后已在毫秒、百分之一度以下。读图规则：对数轴上的直线=几何收敛，下凹=更快；只要曲线是直的或下凹的，迭代次数就可以放心封顶。生成脚本 `examples/practice/ballistics_iteration.py`。],
)

收尾两件事。其一，yaw 不参与迭代：水平距离在迭代中已按二维矢量处理，等 $T$ 收敛后一次算出 $psi = op("atan2")(y + dot(y) T, thin x + dot(x) T)$ 即可——它就是「坐标变换」一章末尾那句"回程很短"的落实。其二，弹丸飞行不是唯一的延迟：从图像曝光到弹丸出膛还隔着识别、通信、云台响应、拨弹，这些环节近似与目标距离无关，工程上并成一个常数 $t_d$，外推时间用 $t_d + T$ 而不是裸的 $T$。$t_d$ 的构成与标定放在本章验证一节。

=== 弹速的现实：设定值不是出膛速度

以上所有公式里的 $v_0$，用什么值代？摩擦轮的转速设定值*不是*答案。弹丸尺寸有公差、摩擦轮会磨损发热、连发时转速被连续掉弹拖低，出膛速度逐发都在波动，上下一两米每秒稀松平常。这个波动值多少厘米？落点下坠 $approx g d^2 \/ (2 v_0^2)$，对 $v_0$ 求导，8 m 处每 1 m/s 的弹速误差折合 4 cm 落点——波动 $plus.minus 1.5 "m/s"$ 就把半块装甲板的容差吃光了。

好在弹速不用猜：裁判系统的测速模块给每发弹丸实测出膛速度，经电控转发给视觉——本篇「通信协议设定」一章需求盘点里"当前弹速"字段的用途，就兑现在这里。用法有讲究：单发测速含噪声，且它是*上一发*的事后数据，正确姿势是取最近若干发的滑动平均作为下一发的 $v_0$，开机后没有历史数据时退回弹速档位的典型值。还有一层系统账：裁判系统对弹速设上限（近年规则 17 mm 为 25 m/s、42 mm 为 12 m/s（英雄远程部署模式升 16.5 m/s）的量级，以当季手册为准），超速有处罚，所以设定值必须压在上限之下留出安全余量——波动越大，被迫留的余量越大，平均弹速越低，弹道越弯，视觉的日子越难过。把摩擦轮波动做小是机械和电控的功课，但视觉应该是第一个拿数据说话的人：把裁判测速逐发记下来画个直方图，波动多大、有没有掉速趋势，一目了然。

顺着弹速的新鲜度，把「通信协议设定」一章按下的架构分岔收个尾：弹道解算放上位机还是下位机，那边已经算过一整张取舍表，判据一句话——弹速与姿态在电控侧最新鲜，调试工具在视觉侧最全。本章按视觉侧解算展开是那一章定下的教学路线；走到这里你可以自己验证它的另一半承诺：公式两端通用，换端不必重学，改的只是协议载荷和代码落脚的芯片。

=== 参数从哪来：把 k 标出来

模型里那个 $k$，理论值 $rho c_d A \/ (2 m) approx 0.019 thin "m"^(-1)$ 只能当初值。真实弹丸不是理想光滑球：表面有分模线、摩擦轮赋予的旋转带来升力效应（这正是基础篇辨识代码里 lift 参数的来历）、$c_d$ 随速度小幅变化，再加上上一节说的模型简化误差——这些统统没法从手册里查出来，只能*辨识*：拿真实弹道数据反推一个*有效* $k$，让模型输出对齐现实。有效参数吸收模型误差，这是工程建模的常规操作，不丢人，丢人的是拿理论值硬扛。

数据怎么采：固定一档弹速，选几个距离（比如 2、4、6、8 m）架固定靶，每个距离固定 pitch 打十发，记录四元组（pitch、实测弹速、距离、落点相对枪口的高度差），落点取十发均值以压散布。求解器不用重写——基础篇 Ceres 一节的弹道模型参数辨识示例（`BallisticResidual` 数值积分模拟落点、残差取落点差）就是为这批数据准备的，样本喂进去解一次非线性最小二乘即可。两条验收判据：其一，拟合出的 $k$ 应与理论值同量级——差出一个数量级，先查单位和数据，不要庆祝"发现了新物理"；其二，留一个没参与拟合的距离做检验，模型在它上面的预测误差应与拟合残差相当，否则就是过拟合了（「深度学习和神经网络」一章的老朋友，穿着弹道的衣服又来了）。

=== 验证：打得准不准，数字说话

模型、参数、闭环都齐了，怎么确认整条链做对了？弹道的验收有个天然优势：落点是物理世界直接给的裁决，不需要任何软件自证。标准动作是*固定靶偏差表*——固定弹速档，2、4、6、8 m 各打一组，记录每发弹着点相对瞄准点的竖直偏差，统计均值与标准差。这张表的信息密度很高：*均值是系统误差，指向模型；标准差是随机误差，指向散布*，两者的病根完全不同，读法如下：

#figure(
table(
  columns: (auto, auto),
  align: (left, left),
  stroke: (x, y) => (
    top: if y == 1 or y == 0 { 1pt } else { 0pt },
    bottom: if y == 4 { 1pt } else { 0pt },
  ),
  inset: 8pt,
  [*偏差表的症状*], [*指向的病根*],
  [均值偏差随距离近似*线性*增长], [pitch 零位偏差或安装角——角度误差折合的位移正比于 $d$],
  [均值偏差随距离*加速*增长（近似平方）], [$v_0$ 虚高或 $k$ 偏小——下坠误差正比于 $d^2$ 起步],
  [均值都小，但标准差大], [弹速波动、机械散布——不是解算的锅，把测速直方图甩给机械],
  [近距离就有固定偏差，且与距离无关], [瞄准点定义不一致（枪口与相机的偏移没扣净）],
),
caption: [固定靶偏差表的诊断读法。读表规则：先看均值曲线随距离的*形状*再下结论——线性与平方的区别，就是角度零位与弹道模型的区别，一张四个距离的表足以把两者分开；标准差一列则划清解算与机械的责任边界。],
)

打表之前还有一笔延迟账要盘。前面把"曝光到出膛"的固定延迟并成了常数 $t_d$，它的构成值得列开——哪些能测、哪些能并、哪一项是大头，决定了优化力气该花在哪：

#figure(
table(
  columns: (auto, auto, auto),
  align: (left, center, left),
  stroke: (x, y) => (
    top: if y == 1 or y == 0 { 1pt } else { 0pt },
    bottom: if y == 7 { 1pt } else { 0pt },
  ),
  inset: 7pt,
  table.header([*环节*], [*量级*], [*处理方式*]),
  [相机曝光], [0.5～10 ms], [取曝光中点打时间戳（「时间戳对齐」一章）],
  [传输与识别], [5～25 ms], [可用时间戳直接测量，并入 $t_d$],
  [PnP 与解算], [约 1 ms], [并入 $t_d$],
  [上下位机通信], [3～15 ms], [有波动，取均值并入 $t_d$],
  [云台响应], [数十 ms], [电控的响应时间常数，不归视觉但必须计入],
  [拨弹空程], [约百 ms], [指令到出膛，随拨弹机构状态波动],
  [弹丸飞行 $T$], [100～600 ms], [*唯一随距离变化的大头*，每帧用本章模型解],
),
caption: [从曝光到命中的延迟预算（量级，具体以实测为准）。读表规则：前四行加起来不过几十毫秒且基本恒定，逐项精确测量的收益有限——粗测后并成常数即可；真正的大头是最后一行，300 ms 的飞行时间里目标能横移半米以上，这就是弹道模型精度与测距精度在全部延迟环节里权重最高的原因。云台响应与拨弹两行波动大，最实用的标法是黑箱化：固定靶已打准之后，打横向匀速移动的目标，命中点的水平残差除以目标速度，就是 $t_d$ 还没算够的部分。],
)

验证的顺序要讲究：先静后动。静态偏差表把模型与参数钉死，再引入运动目标检验 $t_d$ 与预测闭环——动态环节一旦出问题，你才能笃定病根不在弹道模型里。至于运动目标怎么打、打不中时如何区分预测的锅和弹道的锅，那是整条自瞄链路的联调问题，「RoboMaster应用篇」自瞄算法设计一章再展开。

==== 本章小结

弹道解算是自瞄链路在物理世界的最后一跳：滤波器交出的未来位置，经本章变成云台角度与提前时间。无阻力抛体有封闭解，双解取低弹道；阻力要不要管用数字回答——17 mm 弹丸出膛减速度是重力的 1.2 倍，5 m 起脱靶量踩线，且落点与提前量两头挨刀；平直弹道假设下水平方向严格可积，$x(t) = ln(1 + k v_(x 0) t) \/ k$；飞行时间用牛顿法求根，三步到机器精度，别叫它梯度下降；pitch 与 $T$ 交替迭代、再与预测外推套成不动点，收缩率约为目标弹速比，三轮封顶；$v_0$ 用裁判测速的滑动平均，$k$ 用实弹数据辨识成有效参数；验收靠固定靶偏差表——均值的形状指认系统误差，标准差划清机械责任。到这里，实战技术篇的四块地基——协议、标定、时间、弹道——都已打完，下一篇把它们组装成完整的自瞄系统。
