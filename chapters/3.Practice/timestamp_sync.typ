#import "/template/template.typ": *

「坐标变换」一章的结尾留了一笔没有还清的账：自瞄链 $bold(p)_O = bold(T)_(O G) (t) thin bold(T)_(G C) thin bold(p)_C$ 里那个不起眼的 $(t)$，必须取图像*曝光那一瞬间*的云台姿态——取错 10 ms，3 rad/s 甩动的云台就会让 4 m 外的目标偏出约 12 cm；解法当时只交代了思想（拿曝光时刻去 IMU 姿态队列里插值），工程实现留给了本章。「卡尔曼滤波」一章的异步更新一节也把"多路数据的时间戳如何对齐"指到了这里。现在把账还上。

本章回答三个问题：一帧图像的"时刻"到底是什么、为什么它天生就和姿态数据对不齐；对不齐要付出多大代价、用什么方案对齐；以及做完之后怎么确认你真的对齐了。最后一个问题的答案会浓缩成一张图——错配时，静止目标在惯性系里拖出一条尾巴；对齐后，它缩成一个点。

=== 一帧图像拍摄于何时

问"这帧图像该配哪个姿态"之前，先问一个更基本的问题：一帧图像有没有一个确定的"拍摄时刻"？答案比想象中含糊，而且含糊了三层。

第一层：*曝光不是一瞬间*。快门打开、感光芯片积累光子、快门关闭，这段时间叫曝光时间——自瞄为了压制运动模糊会把它压得很短，典型配置在几百微秒到几毫秒。曝光期间景物在动，成的像是这段时间内景物的平均，所以一帧图像的"有效采样时刻"约定取*曝光中点*。这个约定不是吹毛求疵：相机 SDK 打时间戳的位置五花八门——有的打在触发瞬间（曝光起点），有的打在读出完成之后。拿 2.5 ms 的曝光来说，起点和中点差 1.25 ms，在 3 rad/s 的甩动下就是 4 m 处 1.5 cm；弱光下把曝光拉到 10 ms，中点修正就有 5 ms——已经和整个对齐问题同一个量级。

第二层：卷帘快门的一帧里根本不存在单一时刻。「相机模型」一章讲的针孔几何对一帧图像默认"所有像素同时采样"，这其实是快门类型给的承诺。*全局快门*（global shutter）的所有像元同时开始、同时结束曝光，承诺成立；卷帘快门（rolling shutter）为了省电路，逐行曝光、逐行读出，首行与末行的曝光时刻能差出几毫秒到几十毫秒（上限就是一个帧周期）。对高速运动的目标，这意味着图像上半部和下半部拍的是不同时刻的世界：云台以 3 rad/s 扫过时，帧内 10 ms 的行间跨度就是 1.7° 的姿态差，竖直的灯条被剪切成斜的——这就是*果冻效应*。RM 用的工业相机基本都是全局快门，这层含糊通常不用操心；但如果你在用树莓派摄像头、手机模组这类卷帘器件做实验，要清楚：单帧内不存在"一个正确的时间戳"，常规做法是取中间行的曝光时刻，并接受帧内几何已经畸变的事实。

第三层，也是工程上最要命的一层：你拿到数据的时刻不是数据发生的时刻。看图像这条链：曝光结束后，数据要经过芯片读出、USB 传输、驱动回调，才轮到你的代码打时间戳——如果戳打在回调里（`now()` 一把梭），它记录的是"数据到达 CPU"的时刻，比曝光晚了几毫秒到几十毫秒，而且这段延迟随 USB 负载和 CPU 调度*抖动*。再看姿态这条链：电控内部以 1 kHz 量级解算姿态，但*下发给视觉端的频率是另一本账*，上限由链路说了算——以社区常用实现的 28 字节下行帧为例，真 115200 UART 上一帧要走约 2.4 ms，顶多装下约 400 Hz（「通信协议设定」一章的预算取 200 Hz），USB 虚拟串口（`ttyACM`，波特率只是名义参数）才装得下 1 kHz。帧走完线路，才轮到接收线程打戳——同样晚于物理时刻，只是通常更短、更稳。两条链各晚一截，配对要求的却是同一物理时刻，于是净错位等于*两条链打戳延迟之差*。这句话是全章的伏笔：后面软件方案里那个唯一的标量 offset，吸收的正是这个差值。

把三层含糊倒过来，就是时间戳质量的分级：最好的戳是硬件在采集瞬间打的（外触发信号的时刻，或相机芯片自带的硬件时间戳）；其次是 SDK 从设备侧取回的采集时刻；最差是主机收包时刻。排查时间问题的第一步，永远是搞清楚你手里的戳属于哪一级。

=== 错配的代价：一笔算得出来的账

代价的公式一行就推完。设云台角速度 $omega$，时间错位 $t_d$，错位期间云台转过的角度就是配对进坐标变换的姿态误差：

$
Delta theta = omega dot t_d
$

这个角度误差经 $bold(T)_(O G)$ 上链，把距离 $r$ 处的目标在惯性系里搬动约一段弧长：

$
e approx r dot Delta theta = r dot omega dot t_d
$

三个量全是线性的：云台转得越快、目标越远、错位越久，偏得越多。按 RM 的典型数字（目标距离 4 m）算一张表：

#figure(
table(
  columns: (auto, auto, auto, auto),
  align: (left, center, center, center),
  stroke: (x, y) => (
    top: if y == 1 or y == 0 { 1pt } else { 0pt },
    bottom: if y == 3 { 1pt } else { 0pt },
  ),
  inset: 8pt,
  [*云台角速度*], [*$t_d = 5 "ms"$*], [*$t_d = 10 "ms"$*], [*$t_d = 30 "ms"$*],
  [1 rad/s（57°/s，缓慢跟随）], [2 cm], [4 cm], [12 cm],
  [3 rad/s（172°/s，正常甩枪）], [6 cm], [12 cm], [36 cm],
  [6 rad/s（344°/s，快速甩头）], [12 cm], [24 cm], [72 cm],
),
caption: [时间错位在 4 m 处造成的位置偏差 $e = r omega t_d$。读表规则：小装甲板半宽约 6.75 cm，超过它就是整板脱靶——正常甩枪配上 10 ms 错位已经出界；表中三个变量全是线性关系，等积线上的组合等价（3 rad/s × 10 ms 与 6 rad/s × 5 ms 同罪）。"用收包时刻当图像戳"的裸奔做法，错位就落在 10～30 ms 这两列里。],
)

比数字更险的是这笔误差的*性质*。「卡尔曼滤波」一章里 $bold(R)$ 描述的测量噪声是零均值的随机抖动，多帧平均能压下去；时间错位不是——固定的 $t_d$ 产生的偏差随角速度同步起伏，每一帧都朝运动方向的同一侧偏，是系统误差。滤波器没有能力识别它，只会把它当成一致的证据采信，自信地收敛到错误的位置上。而延迟中随负载抖动的那部分，则表现为额外的测量噪声，把 $bold(R)$ 撑大。一句话：错位的固定部分让估计*偏*，抖动部分让估计*抖*，两头都不讨好。

还要把一对容易混淆的时间概念拆开。*配对错位*（本章的 $t_d$）是两条数据链打戳时刻之差，它破坏几何正确性，靠对齐消除；*处理延迟*是从曝光到指令发出的全程耗时（几十毫秒量级），它不破坏几何，但意味着指令天生迟到，靠预测提前量补偿——预测机制在「卡尔曼滤波」一章，提前量里的弹丸飞行时间在下一章「弹道解算」。处理延迟很好测：在指令发出处用当前时刻减去该帧图像的时间戳就是全程延迟，社区常用实现里有现成的延迟话题可看；更彻底的黑盒办法是拍一只由电控翻转的 LED，从"发翻转指令"到"图像里看到变化"就是全环路延迟。但*不要拿延迟数字当 offset 用*：延迟是"一条链走了多久"，offset 是"两条链的戳差多少"，量的根本不是一个东西。

最后回味「坐标变换」一章点过的那句话：静止调试时任何时刻的姿态都一样，时间戳 bug 完全隐身，只在运动中现形。所以本章从标定到验证，全部围着一个"甩"字做文章。

=== 三代对齐方案：把责任从硬件搬进软件

对齐方案在机器人领域演进了三代，本质是把"对齐责任"从硬件逐步搬进软件，精度逐级下降、门槛也逐级下降。

*第一代：硬件触发。*工业相机的 6-pin 数字 IO 里有一路光耦隔离的触发输入，电控（或任何一个共享时钟的信号源）按固定周期发脉冲，相机收到脉冲才开始曝光。快门何时打开由触发方说了算，触发方在自己的时钟里给这一帧打戳——图像和姿态从此共享同一个时钟源，错位天然趋近于零，剩下的只有曝光中点这类微秒到亚毫秒级的固定小项。代价是一根实实在在的线要接、相机要有触发引脚、电控固件要配合，且帧率由触发源控制。

*第二代：时钟同步。*设备各自采集、各自打戳，但先用 PTP（IEEE 1588）或 PPS 秒脉冲把各设备的时钟对齐到微秒级，前提是每个驱动都老老实实打"采集时刻"而非"收包时刻"的戳。这是网口相机、激光雷达等设备的标准玩法；RM 的典型链路是 USB 相机加串口 IMU，两头都不讲 PTP，这条路通常走不通。

*第三代：软件 offset 补偿。*承认两条链的戳都打在接收侧，测出两链延迟之差，用一个固定标量 offset 把它修正掉，再按修正后的时刻去姿态缓冲里插值。零硬件成本、零固件改动，精度到毫秒级——延迟的固定部分被 offset 吃掉，抖动部分留下来成为残余误差。这是社区实现的主流起点。

#figure(
table(
  columns: (auto, auto, auto, auto),
  align: (left, left, center, left),
  stroke: (x, y) => (
    top: if y == 1 or y == 0 { 1pt } else { 0pt },
    bottom: if y == 3 { 1pt } else { 0pt },
  ),
  inset: 8pt,
  [*方案*], [*机制*], [*残余错位*], [*前提与代价*],
  [硬件触发], [触发脉冲决定曝光时刻，共享时钟打戳], [µs 级], [相机有触发引脚、接线、电控配合],
  [时钟同步], [PTP/PPS 对齐各设备时钟，各自打采集戳], [µs～ms], [设备与驱动支持；USB＋串口链路难覆盖],
  [软件 offset], [收包打戳＋固定偏移修正＋插值], [ms 级（抖动不可消）], [零硬件成本；offset 需标定，链路变了要重标],
),
caption: [三代对齐方案的决策表。读表规则：从上往下精度降、门槛也降；残余错位乘上 $r omega$ 就是位置误差——按 3 rad/s、4 m 折算，1 ms 残余对应 1.2 cm，软件方案的毫秒级残余对正常甩枪够用，快速甩头想留裕量就该考虑硬件触发。三代不互斥，硬件触发之上仍可叠加软件微调。],
)

还有第四代的影子值得知道：把 $t_d$ 本身放进状态向量在线估计（视觉惯性 SLAM 里的成熟做法）。它的代价是可观测性要求——匀速运动下 $t_d$ 和位姿耦合在一起分不开，必须有变速激励才估得出来。这个"匀速不可观"的性质马上会在 offset 标定一节以更朴素的面目重现。

=== 软件对齐落地：姿态缓冲与插值

选定软件方案后，数据流长这样：串口接收线程每收到一帧姿态，打上（修正后的）时间戳，塞进一个按时间排序的缓冲；图像处理流水线拿到一帧图像，用图像戳去缓冲里查询，找到夹住该时刻的前后两条姿态，插值出曝光瞬间的姿态再上坐标变换链。缓冲只需保留最近几百毫秒——够覆盖图像链路的最大延迟即可，旧数据随时丢弃，这正是*环形缓冲*的语义。一个可以直接抄走的实现（Eigen 四元数，几十行）：

```cpp
struct StampedQuat { double t; Eigen::Quaterniond q; };

class PoseBuffer {
public:
  void push(double t, const Eigen::Quaterniond& q) {
    std::lock_guard<std::mutex> lk(mtx_);
    if (!buf_.empty() && t <= buf_.back().t) return;  // 时间必须单调
    buf_.push_back({t, q});
    while (buf_.size() > 1 && buf_.front().t < t - 0.5)
      buf_.pop_front();  // 只留最近 0.5 s
  }
  // 查询 t 时刻的姿态；t 未被数据夹住时返回空，调用方稍后重试
  std::optional<Eigen::Quaterniond> query(double t) const {
    std::lock_guard<std::mutex> lk(mtx_);
    if (buf_.size() < 2 || t < buf_.front().t || t > buf_.back().t)
      return std::nullopt;  // 太老或太新都不插，绝不外推
    auto hi = std::lower_bound(buf_.begin(), buf_.end(), t,
        [](const StampedQuat& s, double v) { return s.t < v; });
    if (hi == buf_.begin()) ++hi;
    auto lo = hi - 1;  // 夹住区间 lo->t <= t <= hi->t，不用 ==
    double a = (t - lo->t) / (hi->t - lo->t);
    return lo->q.slerp(a, hi->q);  // slerp：Eigen 内部已做半球对齐（见坑一）
  }
private:
  mutable std::mutex mtx_;
  std::deque<StampedQuat> buf_;
};
```

短短几十行埋着三个坑，每一个都有人踩过。

坑一：插值前不做半球对齐。「坐标变换」一章讲过单位四元数是旋转的双覆盖：$bold(q)$ 与 $-bold(q)$ 表示同一个姿态，比较与插值前要先对符号。这在姿态缓冲里不是理论洁癖：上游随时可能换一种符号表示，缓冲里前后两条姿态的点积一旦为负，直接插值走的就是单位球上的*大弧*——数值算一遍，相差 90° 的两个姿态若一侧带了反号，插值中点不在 45°，而在反方向 135° 处（路径长 270° 而非 90°），离正确中点整整 180°。修法一行：点积为负就给一侧取反再插。代码里那行 Eigen `slerp` 内部已做了这一步，自己手写插值时它才是真会咬人的错。顺带把一个流传甚广的说法摆正："线性插值加归一化走的是弦不是弧、中间姿态会偏"——数值上不成立：半球对齐之后，逐元素线性插值再归一化（nlerp）的路径*同样落在测地线上*，与 `slerp` 的差别只剩沿弧的参数化速度——nlerp 中段偏快、两端偏慢，夹角 90° 时插出的姿态与 `slerp` 最多也只差约 0.9°，而相邻两条下发姿态只差零点几度到一度出头（本章开头的下发频率账），差异直接淹没在数值精度里。所以工程上 nlerp 加半球对齐完全够用，`slerp` 的增量价值在缓冲出现大空洞、夹角变大时仍保持均匀角速度；真正低级但常见的错，反而是插完*忘了归一化*。

坑二：用 `==` 判等 double 时间戳。`if (imu.t == cam.t)` 这种代码，两个各自经过一串浮点运算的时间戳几乎永远不会精确相等，分支永远进不去，程序"看起来在跑、实际在漏"。时间匹配一律用区间语义（上面的 `lower_bound` 加夹住判断）或容差比较。同一个思想的延伸：整条系统的时间戳要出自同一个时钟，并且这个时钟要单调——墙上时钟被 NTP 校时往回拨一下，"时间必须单调"的前提就塌了，缓冲直接拒收数据。

*坑三：外推。*图像戳比缓冲里最新的姿态还新（串口线程恰好慢了半拍）时，最诱人的写法是拿最后两条姿态外推。别。插值有"被两端夹住"的保底，外推没有——高动态下多推几毫秒就能引入可观的姿态误差。正确姿势是等：姿态流按下发周期到来，最多等约一个周期——USB 链路 1 ms、真 UART 5 ms 量级——就能把图像时刻夹住，用几毫秒以内的等待换确定的正确性，稳赚。

offset 修正加在哪一侧是个实现细节：把每帧图像的戳往回拨 $t_d$，或把每条姿态的戳整体前移 $t_d$，效果完全相同——有意义的只有差值。社区常用实现选了后者：姿态发布时把时间戳写成"当前时刻加 offset"，意思是"这份姿态别和现在到达的图像配，和 $t_d$ 之后打戳的那帧配"。另外，如果你在 ROS 里，tf2 的时间缓冲和 message_filters 的按戳配对做的正是本章这套事（识别节点按图像戳等待坐标变换就绪再处理），换了框架，机制不变。对齐好的图像时间戳还有一个下游用途：它就是滤波器计算 $Delta t$ 的依据——「卡尔曼滤波」一章警告过 $Delta t$ 绝不能写死，帧率波动、丢帧全靠时间戳如实上报。

=== offset 从哪里来：把甩云台变成测量仪

软件方案的一切都悬在那个标量 offset 上，它从哪来？逐段测量此路不通：曝光到打戳的延迟、串口传输的延迟，分散在相机、电控、主机三个设备的三个时钟里，没有公共基准，谁也没法单独量。但好消息是根本不需要——上链的只有差值，而差值可以端到端地标出来，靠的就是"错配让静止目标动起来"这个症状本身。

思路是把症状变成损失函数。静止目标在惯性系的重建位置本该是一个点，错配让它散开；散得多开是 offset 的函数，那就扫描：

+ 把一块装甲板固定在 3～4 m 外，机器人上电、识别正常；
+ 录一段数据：来回甩云台（幅度几十度、来回一两秒一个周期），同时记录每帧的识别结果（相机系位姿加图像戳）和完整的姿态流——存成 rosbag 或自定日志都行，关键是两路原始数据和原始时间戳都在；
+ 离线重放：取候选 offset，从 $-5 "ms"$ 到 $20 "ms"$ 按 0.5 ms 步进，对每个候选值把全部帧重新上链、重建目标在惯性系的位置序列，计算散布（对均值的 RMS 距离——不需要知道目标的真实位置）；
+ 散布对候选 offset 的曲线呈 V 形，谷底对应的就是你的 offset。

社区常用实现里这个参数的真实取值是 6 ms——就是这么个量级：图像链比姿态链多走了大约半帧多一点的路。你的链路数字不同，但方法相同。

两个理论注脚让这个土办法立得住。其一，甩必须是真的甩——变速、来回。假如云台匀速转圈，$psi(t) = omega t$，错配 $Delta$ 造成的姿态误差 $psi(t + Delta) - psi(t) = omega Delta$ 是常数，重建点被整体搬到一个固定的偏移位置，散布对 offset 完全不敏感——扫描曲线是平的，offset 不可观。正弦式的来回甩让 $omega$ 在数据里跑遍正负与零，错位才会现形为散布。这正是上一节"在线 $t_d$ 估计要求变速激励"的朴素版本。其二，*谷底不是零*。扫描曲线的最低点是 PnP 测量噪声自己的散布（「相机模型」一章的深度病态贡献大头），它同时限定了扫描的分辨率——谷底附近半毫秒的差别淹在噪声起伏里就不必强分了。反过来，如果谷底高得反常（比如三四厘米下不去），说明散布里还有时间以外的账：外参标歪了，或识别本身在抖，该回「相机标定与手眼标定」一章对账。

offset 不是一劳永逸的常数。换相机或驱动、改曝光时间（曝光中点跟着移半个曝光时长）、电控改下发频率——任何一头的链路变了，差值就变了，重扫一遍。扫描是纯离线计算，录一段一分钟的数据反复用，成本几乎为零。

=== 验证：甩云台，看静止目标缩不缩成一点

标定完成不等于做对了，实战篇的规矩是每章交付一个验收动作。时间戳对齐的验收动作就一句话：甩云台打静止目标，看它在惯性系里缩不缩成一点。装甲板固定不动，云台来回甩，把每帧重建出的惯性系位置画在一张散点图上——对齐前后各画一次，同一坐标轴。合格判据同样一句话：散布回到静止云台时的水平（那是纯识别噪声的底），并且不再随甩动快慢变化。

这个验收实验完整地仿真一遍，就是本章的核心证据图。仿真里云台按 $plus.minus 40 degree$、0.8 Hz 正弦甩动（峰值角速度 3.5 rad/s，正常偏快的甩枪），相机 100 fps，图像戳比曝光晚 6 ms，PnP 噪声按「相机模型」一章的量级注入（深度 1.5 cm、横向 2 mm）：

#figure(
  image("images/practice-timestamp-alignment.png", width: 100%),
  caption: [同一段甩云台数据，对齐前后静止目标在惯性系的重建位置。左：以真实位置为原点的散布，品红为不补偿（offset 取 0），蓝为补偿 6 ms 之后，灰色虚线圆是小装甲板半宽 6.75 cm。读图抓两件事：错配的散布沿切向（横轴）拖出 8 cm 开外的尾巴且两端最密——尾巴长度正比于瞬时角速度，甩得最快的时刻错得最远，越过半宽线的那些帧照此开火就是整板脱靶；对齐后云缩回中心，RMS 从 6.1 cm 落到 1.4 cm，剩下沿深度方向（纵轴）的轻微拉长不是时间的错，是「相机模型」一章的单目深度病态。右：offset 扫描曲线，散布在真值 6 ms 处触底，谷底高度就是 PnP 噪声自己——时间的账还清之后，剩下的是几何的账。仿真脚本 `examples/practice/timestamp_alignment_demo.py`。],
)

上车复现时，如果散布没有如愿缩回去，散布的*形状*就是诊断书。散布沿运动切向拉长、大小跟角速度走、云台反转的瞬间（$omega = 0$）收回中心——时间还没对齐，回本章重扫 offset。散布跟*姿态角*走——甩到最边上偏得最多、哪怕匀速慢转也画出弧线——那是 $bold(T)_(G C)$ 外参的错（「坐标变换」一章预告过的"标歪画圈"症状），回「相机标定与手眼标定」一章。两样都有就先修时间再修外参：外参标定用的数据本身也要先对齐时间，顺序反了会把时间的错标进外参里。

==== 本章小结

- 一帧图像的时刻约定取曝光中点；时间戳分三级：硬件采集戳 > SDK 采集戳 > 主机收包戳，排查从分清级别开始。卷帘快门的帧内没有单一时刻，果冻效应是它的运动画像。
- 错配的代价 $e approx r omega t_d$，三个量全线性；它是随角速度起伏的系统误差，滤波平均不掉，静止调试看不见——一切标定与验证都在"甩"字上。
- 配对错位与处理延迟是两本账：前者破坏几何，靠对齐消除；后者只是迟到，靠预测提前量补偿。别拿延迟数字当 offset。
- 三代方案：硬件触发 µs 级、时钟同步 µs～ms、软件 offset 补偿 ms 级；RM 的 USB 相机加串口链路，现实起点是软件补偿，追求裕量再上硬件触发。
- 落地三戒：四元数插值先半球对齐、插完记得归一化（`slerp` 一行两者兼得）；double 时间戳禁用 `==`，一律区间夹住；缓冲没夹住宁可多等一个下发周期，绝不外推。
- offset 靠甩云台加扫描标出：匀速不可观，必须变速；V 形曲线的谷底是 PnP 噪声，不是零。验收看散布形状——切向拖尾跟 $omega$ 走是时间的错，弧线跟姿态角走是外参的错。

到这里，识别交出的位姿有了可信的坐标系（标定）、可信的时刻（本章）。下一章把可信的位姿变成扣扳机前的最后两个数：云台该抬多高、提前量该给多少——「弹道解算」。
