#import "/template/template.typ": *

「坐标变换」一章留下了一个具体的工程问题：自瞄链 $bold(p)_O = bold(T)_(O G) (t) thin bold(T)_(G C) thin bold(p)_C$ 中的 $(t)$，应当对应图像的曝光参考时刻。若相差 10 ms，云台以 3 rad/s 转动时，仅小角度近似就会给 4 m 外的目标带来约 12 cm 的横向位置误差。前文只说明了基本思路——用曝光时刻查询 IMU 姿态队列并插值——本章把时间戳的含义、实现方法和验证过程补完整。「卡尔曼滤波」一章异步更新所需的多路数据对时，也依赖同一套基础。

本章依次回答三个问题：一帧图像的"时刻"指什么，为什么它常与姿态数据错开；这种错位会造成多大误差，可以采用哪些对齐方案；完成补偿后，怎样用可重复的实验检查效果。一个直观现象贯穿后两问：时间错配时，静止目标在惯性系中的重建点会随云台运动拉开；正确补偿后，散布应回到静止测量的基线附近。

=== 一帧图像拍摄于何时

问"这帧图像该配哪个姿态"之前，先要确定"拍摄时刻"的定义。这里有三个容易混在一起的问题。

第一，*曝光持续一段时间*。快门打开后，感光芯片在曝光时间内积累光信号；自瞄为了限制运动模糊，常把曝光设在几百微秒到几毫秒。对亮度变化不剧烈、运动近似匀速的情况，一帧图像的"有效采样时刻"通常约定为*曝光中点*。相机或 SDK 给出的时间戳却可能对应触发沿、曝光开始、曝光结束或读出完成，使用前必须查手册或实测。以 2.5 ms 曝光为例，起点与中点相差 1.25 ms；在 3 rad/s、距离 4 m 的简化条件下，这一差值约对应 1.5 cm。若弱光下曝光增至 10 ms，起点到中点的修正就是 5 ms，已经不能忽略。

第二，*卷帘快门没有覆盖整帧的单一采样时刻*。「相机模型」一章的针孔几何默认一帧中的像素同时采样。全局快门（global shutter）的像元同时曝光，较接近这个假设；卷帘快门（rolling shutter）则让各行在不同时间开始曝光，首末行的曝光中点可能相差数毫秒到数十毫秒，具体由传感器的行时序决定。若帧内行间跨度为 10 ms，云台以 3 rad/s 转动，首末行就对应约 1.7° 的姿态差，原本竖直的灯条可能被剪切成斜线。这类随运动产生的帧内形变常被称为"果冻效应"。RM 常见的工业相机多采用全局快门；使用树莓派摄像头或手机模组等卷帘器件时，可以把中间行的曝光中点作为整帧参考时刻，但一个标量时间戳无法消除已经产生的逐行几何畸变。

第三，*数据到达主机的时刻不等于采样事件发生的时刻*。图像在曝光后还要经过传感器读出、USB 或网口传输和驱动排队；如果只在回调中调用 `now()`，记录的是主机收到数据的时刻，其中包含数毫秒到数十毫秒的传输与调度延迟，负载变化还会带来抖动。姿态链路也一样：电控可以在内部以 1 kHz 解算姿态，但发往视觉端的频率受链路吞吐限制。以 28 字节、8N1 格式的 115200 bit/s UART 帧为例，线路传输时间约为 $28 times 10 / 115200 approx 2.43 "ms"$，理论上每秒只能容纳约 411 帧，实际还要给协议处理和其他消息留出余量（「通信协议设定」一章按 200 Hz 预算）。USB CDC 虚拟串口的波特率有时只是配置参数，能否稳定传输 1 kHz 姿态仍取决于设备端实现、USB 调度和主机负载，不能由 `ttyACM` 设备名直接推出。

若图像和姿态都在同一主机的单调时钟上按收包时刻打戳，二者相对错位的固定部分近似等于*两条链稳定延迟之差*，后文的标量 offset 正是用来估计这个差值。若时间戳来自相机、控制器等不同设备，则还多了时钟起点差与计时速率差；这时仅靠一个常数只能在有限时间内近似补偿，长期运行需要把设备时钟映射到共同时间轴。

因此，检查时间戳要同时问两件事：*它标记的是哪个事件*，以及*它属于哪个时钟域*。硬件记录的曝光或触发时刻通常最接近采样事件，设备侧的 SDK 采集戳次之，主机收包戳包含的链路延迟最多；但设备硬件戳若没有可靠的跨时钟映射，也不能直接与主机或 IMU 的时间相减。先从驱动文档、日志和触发实验确认这两项，再讨论补偿精度。

=== 错配的代价：从时间误差到位置误差

先看只绕单轴转动的简化模型。设云台角速度在短时间内近似为常数 $omega$，时间错位为 $t_d$，对应的姿态误差为：

$
Delta theta = omega dot t_d
$

若目标到旋转中心的距离为 $r$，且转动方向与视线垂直，小角度下的位置误差近似为：

$
e approx r dot Delta theta = r dot omega dot t_d
$

更精确的弦长是 $2 r sin(abs(Delta theta) / 2)$；多轴运动时还要取角速度中会改变视线方向的分量。因此，$r omega t_d$ 是便于做预算的小角度模型，不是任意坐标系下逐分量都严格相等的公式。在这个模型内，距离、有效角速度和错位时长近似线性。取目标距离 4 m，可以得到下表：

#figure(
table(
  columns: (auto, auto, auto, auto),
  align: (left, center, center, center),
  stroke: (x, y) => (
    top: if y == 1 or y == 0 { 1pt } else { 0pt },
    bottom: if y == 3 { 1pt } else { 0pt },
  ),
  inset: 8pt,
  [*云台角速度*], [*$t_d = 5 "ms"$*], [*$t_d = 10 "ms"$*], [*$t_d = 30 "ms"$*],
  [1 rad/s（57°/s，缓慢跟随）], [2 cm], [4 cm], [12 cm],
  [3 rad/s（172°/s，较快转动）], [6 cm], [12 cm], [36 cm],
  [6 rad/s（344°/s，高速转动）], [12 cm], [24 cm], [72 cm],
),
caption: [简化模型 $e approx r omega t_d$ 在 4 m 处给出的位置误差量级。小装甲板的名义横向半宽约为 6.75 cm；若其他误差暂不计，横向估计误差超过这个值，瞄准中心线就会越过板面边界。表中 3 rad/s 配 10 ms 与 6 rad/s 配 5 ms 得到相同量级。主机收包戳究竟落在哪一列，必须用本机链路测量，不能仅按接口类型推定。],
)

还要注意误差的统计性质。「卡尔曼滤波」一章中的 $bold(R)$ 通常用来描述给定模型下的测量不确定性；固定 $t_d$ 引起的误差却与角速度相关，是未建模的确定性效应。同向转动时它会持续偏向运动方向，反向时符号随之改变；在完全对称的往返数据上，长期均值甚至可能接近零，但这并不使它变成逐帧独立的零均值噪声。没有显式延迟模型的滤波器可能把这种误差错误地归入目标运动或测量噪声，造成状态偏差或一致性下降，具体表现取决于运动和滤波模型。链路延迟中随负载变化的部分则增加残差散布，还可能带有时间相关性。固定偏移和随机抖动需要分别测量。

还要区分两个时间量。*配对错位*（本章的 $t_d$）描述图像与姿态对同一物理时刻的标记差，它会破坏坐标变换的几何一致性；*端到端处理延迟*描述从曝光到指令发出的耗时，它决定指令使用的信息有多旧，通常要靠状态预测和飞行时间补偿。只有当图像戳确实表示曝光事件、并已映射到指令端的同一时钟时，`发送时刻 - 图像戳` 才近似等于这段端到端延迟；若图像戳是在回调中生成，得到的只包括回调之后的处理时间。受控 LED 实验也可以测某两个明确端点之间的延迟，但结果会包含指令传输、LED 响应、相机采样相位和图像链路，实验报告必须写清端点。处理延迟是"一条链走了多久"，offset 是"两条链相对错开多少"，二者不能混用。

这也解释了为什么静止调试很难发现时间错位：姿态不变时，查询相邻时刻得到的旋转几乎相同。标定与验证必须包含方向和速度都在变化的运动，同时保证目标仍在视野内且识别稳定。

=== 三类对齐方案：触发、时钟映射与软件补偿

工程中常见的方案可以分为三类。它们不是互相替代的历史代际，也没有脱离实现条件的固定精度排序；实际系统经常把触发、时钟映射和软件微调组合起来。

*硬件触发。*许多工业相机提供隔离的数字触发输入。若电控发出触发脉冲，并在与姿态采样相同的控制器时钟中记录触发时刻，就能直接建立图像帧与控制器时间的对应关系。仍需核对触发沿到曝光开始的固定延迟、曝光中点修正、丢触发和帧编号；只有相机被触发而姿态仍只有主机收包戳时，二者并不会自动进入同一时钟域。该方案需要相机支持触发、额外接线和固件配合，帧率也受触发源控制。完成这些校准后，残余误差有机会达到微秒至亚毫秒量级，但精度应由示波器或硬件时间戳实测确认。

*时钟同步或映射。*设备各自采集并记录设备侧时间，再把这些时间映射到共同时间轴。PTP（IEEE 1588）可以在支持它的网络设备间同步时钟；PPS 只提供周期性的精确边沿，通常还要结合计数器或时间信息才能确定完整时间。硬件时间戳、驱动实现、链路不对称和时钟漂移都会影响结果，不能仅凭"支持 PTP/PPS"就假定达到微秒级。网口相机和激光雷达较常提供这类能力；RM 常见的 USB 相机加串口 IMU 未必共享同步协议，此时可以由主机估计设备时间到主机单调时钟的偏移与速率，但前提是设备确实给出采样时间。

*固定 offset 补偿。*当两条链都在同一主机单调时钟上按接收时刻打戳，可以离线估计稳定延迟之差，用一个标量修正时间，再到姿态缓冲中插值。它不需要新增同步硬件，适合作为现有 USB 相机与串口链路的起点；代价是只能补偿固定部分，传输与调度抖动仍会留下残余误差，链路或负载改变后要重新检查。若两个戳来自独立且会漂移的设备时钟，应估计形如 $t_"common" = a t_"device" + b$ 的映射，而不是长期沿用一个 offset。

#figure(
table(
  columns: (auto, auto, auto, auto),
  align: (left, left, center, left),
  stroke: (x, y) => (
    top: if y == 1 or y == 0 { 1pt } else { 0pt },
    bottom: if y == 3 { 1pt } else { 0pt },
  ),
  inset: 8pt,
  [*方案*], [*机制*], [*残余错位*], [*前提与代价*],
  [硬件触发], [同一控制器记录触发与姿态采样时间], [可达 µs～亚 ms], [需触发接口、接线、帧对应和延迟校准],
  [时钟同步/映射], [PTP 或设备戳映射到共同时间轴], [取决于硬件戳与映射], [设备、驱动和漂移估计均需支持],
  [固定 offset], [同一主机收包戳＋相对偏移＋插值], [常见为 ms 量级], [只能消固定项；需标定并监测抖动],
),
caption: [三类对齐方案及其前提。表中的精度是实现得当时的常见量级，不是接口名称带来的保证。按 3 rad/s、4 m 的小角度模型，1 ms 残余约对应 1.2 cm；应先由允许的位置误差反算时间预算 $t_(max) approx e_(max) / (r omega)$，再选择方案并用实测验收。三类方法可以组合，例如硬件触发后仍用软件估计固定小项。],
)

更复杂的系统还会把 $t_d$ 放入状态向量在线估计。这样做并不自动解决问题：时间偏移是否可观测取决于运动、传感器和状态模型。在本章后文的纯偏航、静止目标简化模型中，恒定角速度会让时间偏移与一个固定旋转混在一起，必须加入角速度变化才能用散布把它们区分开。

=== 软件对齐落地：姿态缓冲与插值

采用固定 offset 后，数据流可以这样组织：串口接收线程为每帧姿态记录修正后的时间戳，放入按时间排序的缓冲；图像处理线程用图像的曝光参考时刻查询缓冲，找到前后两条姿态并插值，再执行坐标变换。缓冲保留时长应覆盖两条链的最大相对延迟、调度抖动和允许等待时间，通常可从几百毫秒起步，再用日志调整。下面的 `std::deque` 实现的是有界时间窗口，结构简短，便于改进为生产代码：

```cpp
struct StampedQuat { double t; Eigen::Quaterniond q; };

class PoseBuffer {
public:
  void push(double t, const Eigen::Quaterniond& q) {
    std::lock_guard<std::mutex> lk(mtx_);
    // 示例丢弃乱序或重复数据；生产代码还应累计计数并告警。
    if (!buf_.empty() && t <= buf_.back().t) return;
    buf_.push_back({t, q.normalized()});
    while (buf_.size() > 1 && buf_.front().t < t - 0.5)
      buf_.pop_front();  // 只留最近 0.5 s
  }
  // 查询 t 时刻的姿态；t 未被数据夹住时返回空，调用方稍后重试
  std::optional<Eigen::Quaterniond> query(double t) const {
    std::lock_guard<std::mutex> lk(mtx_);
    if (buf_.size() < 2 || t < buf_.front().t || t > buf_.back().t)
      return std::nullopt;  // 太老或太新都不插，绝不外推
    auto hi = std::lower_bound(buf_.begin(), buf_.end(), t,
        [](const StampedQuat& s, double v) { return s.t < v; });
    if (hi == buf_.begin()) ++hi;
    auto lo = hi - 1;  // 夹住区间 lo->t <= t <= hi->t，不用 ==
    double a = (t - lo->t) / (hi->t - lo->t);
    return lo->q.slerp(a, hi->q);  // Eigen slerp 内部会选择较短的半球路径
  }
private:
  mutable std::mutex mtx_;
  std::deque<StampedQuat> buf_;
};
```

这段代码依赖三个容易被忽略的条件。

*条件一：四元数先做半球对齐。*「坐标变换」一章讲过，单位四元数 $bold(q)$ 与 $-bold(q)$ 表示同一个姿态。若相邻四元数点积为负，应先将其中一个取反，使插值选择单位球上的短弧。以相差 90° 的两个姿态为例，如果一侧换成等价的反号表示，未经处理的插值可能沿 270° 的长路径移动，中点会比预期姿态相差 180°。Eigen 的 `Quaternion::slerp` 已在内部处理符号；自行实现时则要显式检查。

逐元素线性插值后归一化（nlerp）在完成半球对齐后也位于同一条短弧上，但沿弧速度不均匀；`slerp` 则按均匀角速度参数化。两姿态夹角为 90° 时，二者在相同插值参数下的最大姿态差约为 0.9°；相邻姿态只有一两度时，这项差异通常远小于传感器与时间戳误差，但仍应根据姿态间隔和精度预算判断。无论采用哪种方法，都不能漏掉归一化和半球处理。

*条件二：按区间匹配时间，不用浮点数精确相等。*`if (imu.t == cam.t)` 要求两条经过独立计算的浮点时间戳逐位相同，实际很少成立。时间查询应使用上例的 `lower_bound` 和区间夹住语义；判断是否足够接近时再引入有物理含义的容差。系统内部还应使用同一个单调时钟，避免 NTP 校正墙上时间造成回拨。长时间运行或采用很大的纪元秒数时，可以使用整数纳秒，或把 `double` 表示为相对某个近起点的秒数，以免有效精度下降。

*条件三：明确等待、丢帧和外推策略。*当图像时刻晚于缓冲中的最新姿态时，直接用最后两点作恒角速度外推会把模型误差带入高动态区间。保守做法是等待下一帧姿态，并设置最长等待时间；超时后丢弃该图像，同时记录计数。等待时长通常接近一个姿态发布周期再加调度抖动，但不能仅按标称频率保证。若实时预算不允许等待，也可以采用带上限的模型外推，不过必须分别在加减速和丢包场景验证误差，并把最大外推时长写入配置。上例选择不外推，是安全的默认策略，而不是所有系统唯一可行的选择。

offset 可以加在任一侧，但符号要由明确定义推出。设同一物理时刻为 $t$，图像与姿态的主机收包戳分别为 $s_C = t + L_C$、$s_I = t + L_I$。用图像戳查询姿态时，应查询 $s_C - (L_C - L_I)$；等价地，也可以把所有姿态戳平移为 $s_I + (L_C - L_I)$，再直接用 $s_C$ 查询。参考实现采用后一种约定，在姿态发布时写入“当前时刻加 `timestamp_offset`”。不同项目可能使用相反符号，因此配置旁应保留公式和单元测试，不能只记一个正负号。

在 ROS 中，tf2 的时间缓冲和 `message_filters` 的按戳配对可以承载这套机制：识别节点按图像时刻等待相应坐标变换，再继续处理。换用其他框架时，事件时间、共同时间轴和区间插值这三个条件不变。对齐后的图像时间戳也用于滤波器计算 $Delta t$；「卡尔曼滤波」一章已经说明，帧率波动和丢帧存在时不能把 $Delta t$ 写死。

=== offset 从哪里来：用受控运动做端到端标定

固定 offset 需要来自测量，而不是接口经验值。曝光、读出、传输和姿态接收分布在多个设备与时钟中，若没有硬件探针或公共时基，逐段测量往往困难；但坐标变换实际需要的是两条链的有效相对偏移，可以通过端到端实验估计。核心观察仍是：静止目标在惯性系中的重建位置本应稳定，时间错配会让重建结果随云台运动变化。

可以把重建点的散布定义为候选 offset 的损失函数，并按以下步骤扫描：

+ 将装甲板或其他刚性标志固定在 3～4 m 外，调整运动幅度，使目标始终位于相机视野内并保持稳定识别；
+ 让云台做方向和速度都变化的往返运动，同时保存每帧相机系位姿、图像原始时间戳和完整姿态流。rosbag 或自定义日志都可以，重点是保留原始数据，不要只存补偿后的结果；
+ 离线重放时扫描一段覆盖合理先验的候选范围。例如可先取 $-5 "ms"$ 到 $20 "ms"$、步长 0.5 ms；对每个候选值重新插值与坐标变换，计算重建点相对自身均值的 RMS 距离。范围和步长应由链路测量与时间误差预算决定，而不是固定照抄；
+ 在这组简化条件下，曲线在正确 offset 附近通常呈平滑的碗形。取训练记录上的最小值作为候选，再用另一段不同速度的记录验证；若曲线多峰或谷底很平，当前数据不足以唯一确定 offset。

本书后文分析的一份参考实现把 `timestamp_offset` 配为 6 ms，这只说明该相机、驱动、串口与主机组合采用了这个配置值，不能证明另一台车也有相同延迟。图中的仿真同样注入 6 ms，目的是演示扫描能否找回已知参数。

这里有两个重要边界。第一，在纯偏航、静止目标且只用点云散布作损失的模型中，若云台始终匀速转动，$psi(t) = omega t$，错配 $Delta$ 带来的角度差 $psi(t + Delta) - psi(t) = omega Delta$ 是常数。重建点会整体移到另一个固定位置，围绕自身均值的散布几乎不变，因此这种损失无法识别 offset。往返变速运动让 $omega$ 覆盖正、负和接近零的区间，才为该参数提供可观测信息。

第二，*谷底不应被解释成纯 PnP 噪声的证明*。在仿真中，除注入的 PnP 噪声外其他模型都精确，所以最低散布接近设定的噪声底；实车数据还会包含外参误差、目标轻微运动、识别偏差、机械振动和剩余时钟抖动。它们既会抬高谷底，也可能改变最优 offset。若最低散布仍较大，只能说明时间补偿不足以解释全部误差，不能仅凭点云形状判定某个根因。可以分别固定云台、改变运动速度、复查外参，并在独立记录上比较，逐项缩小范围。

offset 也不一定长期不变。更换相机或驱动、调整缓冲策略、改变电控下发频率和主机负载，都可能改变相对延迟；若原始时间戳对应曝光起点，修改曝光时间还会改变到曝光中点的修正量。每次链路变更后都应重做扫描与独立验证，长期运行还要监测残差是否随温度、负载或运行时长变化，以判断常数模型是否仍适用。

=== 验证：运动时的静止目标能否保持稳定

标定值还需要用未参与扫描的数据验收。固定目标，先在云台静止时记录一段基线，再让云台以多组幅度和速度往返运动；把补偿前后的惯性系重建位置画在同一坐标轴，并分别统计 RMS、较高分位误差和样本数。合格阈值应来自整车允许的位置误差：散布需要接近同工况下的静止基线，且随角速度增加的趋势应降到预算内。静止基线本身仍包含识别、PnP、外参与机械误差，不能直接称为纯识别噪声。

下图用二维仿真演示这一检查方法。云台按 $plus.minus 20 degree$、1.6 Hz 做正弦运动，峰值角速度约 3.5 rad/s；相机为 100 fps，姿态采样时刻视为已知，图像收包戳比曝光时刻固定晚 6 ms。模型另加入标准差 1.5 cm 的深度噪声和 2.3 mm 的横向噪声，并假定全部帧都能稳定观测目标。它验证的是这段扫描与补偿代码能找回*已注入*的 offset，不代表实车上的 6 ms 或误差来源已经得到验证：

#figure(
  image("images/practice-timestamp-alignment.png", width: 100%),
  caption: [同一段仿真数据在补偿前后的静止目标重建结果。左图以设定的真实位置为原点：红色为 offset 取 0，蓝色为补偿 6 ms；灰色竖虚线标出小装甲板名义横向半宽 $plus.minus 6.75$ cm，仅用于比较横向量级，纵轴深度误差不应与板宽组成圆形命中边界。未补偿时，横向误差随角速度变化并超过 8 cm；补偿后 RMS 由 6.1 cm 降至约 1.4 cm，剩余散布来自本仿真注入的各向异性 PnP 噪声。右图的扫描最小值位于注入的 6 ms；这证明脚本在设定模型内能够恢复参数，不替代实车独立验证。脚本见 `examples/practice/timestamp_alignment_demo.py`。],
)

实车散布的形状可以提供排查线索，但不能单独确认根因。若误差主要沿运动切向、幅度随角速度增大，并在反转附近减小，现象与时间错位一致；可以改变一个已知的软件 offset，观察误差趋势是否按预测移动。若误差更随姿态角变化，匀速慢转时也重复出现弧形轨迹，则应复查 $bold(T)_(G C)$ 外参与机械挠曲。识别偏差、滚动快门和目标运动也可能产生相似图形，因此还需通过静止基线、不同速度记录和独立外参检查来区分。用于外参标定的动态数据本身也应先完成时间对齐，避免把时序误差吸收到外参估计中。

==== 本章小结

- 一帧全局快门图像通常以曝光中点作为参考时刻；卷帘快门各行时刻不同，一个帧级时间戳不能消除帧内形变。时间戳既要说明标记的事件，也要说明所属时钟域。
- 在短时匀速、单轴小角度模型下，错位误差约为 $e approx r omega t_d$。固定错位产生与运动相关的未建模误差，延迟抖动增加残差散布；静止测试难以暴露前者。
- 配对错位影响几何一致性，端到端延迟影响信息新鲜度。只有端点和时钟定义清楚时，两者才可测量，而且不能互相替代。
- 硬件触发、时钟同步/映射和固定 offset 可以组合使用。接口名称不保证精度，应从位置误差预算反算允许的时间误差，再实测验收。
- 姿态缓冲要处理四元数半球、归一化、单调时间、区间查询、乱序数据和超时策略。外推不是绝对禁止，但必须有模型、上限和高动态验证。
- offset 扫描需要变速激励和独立记录验证。曲线最小值只是在当前数据与损失函数下的候选值；较高谷底或特定散布形状只能提示还存在其他误差，不能单独确认根因。

完成标定和时间对齐后，识别位姿才同时具备明确的坐标系与采样时刻。下一章「弹道解算」继续讨论如何由目标状态、发射速度和模型假设计算云台俯仰角与飞行时间。
