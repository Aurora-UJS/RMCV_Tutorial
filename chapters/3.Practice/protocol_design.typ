#import "/template/template.typ": *

到理论篇收官为止，自瞄的数学链条已经完整：识别交出像素，PnP 交出位姿，坐标变换搬进惯性系，卡尔曼滤波给出未来位置。但这条链要在一台真车上跑起来，还缺四块地基，块块都是纯粹的工程：电控的姿态与弹速要*可靠地送过来*（「通信协议设定」）；相机的几何要*量得准*（「相机标定与手眼标定」）；图像与姿态要*对上同一时刻*（「时间戳对齐」）；预测出的位置要*变成开火的角度与提前量*（「弹道解算」）。这就是实战技术篇的四章。它们共用一条写法上的规矩：只讲怎么做、怎么验、怎么翻车——每个"为什么"都回指前两篇，每章收尾都交付一个能直接执行的验收动作。

基础篇串口一节收尾时，整条链路已经在 pty 上测通了：termios 配置、`pack(1)` 结构体、CRC 校验、逐字节状态机，"把字节可靠送到对岸"的机制全部就位。但那一节的 11 字节示例帧是个教学道具——字段是为了演示机制拍脑袋定的。真实赛季里，你要和电控组的队友坐在同一张桌前，把每个字节的含义写成一份两边都认账的文件，然后各自回去实现、再回来联调。这时要回答的问题全变了：帧里到底该装哪些字段？帧头凭什么选 `0xA5`？校验用 8 位还是 16 位？弹道在谁那边算？这些问题没有一个能靠写代码回答——它们是设计决策，每一个背后都有一笔可以算清的账。本章就把这几笔账当着你的面算完。机制层的东西（termios、状态机、CRC 原理）本章一概不重讲，全部回指基础篇；"帧头加 CRC、坏帧直接丢"的协议哲学也在「计算机系统基础」一章讲过了——本章的主场是它们之上的那一层：*协议契约的设计*。

=== 需求盘点：链路两端各自缺什么

先别急着定义 struct。协议设计的第一步是盘家底：链路两端各自*知道*什么、各自*缺*什么——通信的本质就是把"你有我无"的信息搬过去，一个方向一张清单。

电控这边有什么是视觉必需的？第一是*云台姿态*。IMU 焊在电控那一侧，以 1 kHz 量级刷新，而视觉要把 PnP 解出的相机系位姿搬进惯性系（「坐标变换」一章证明过为什么必须搬），没有姿态寸步难行。第二是*敌方颜色*。裁判系统把本方颜色告诉电控，电控翻转一下发过来——视觉程序里硬编码"打蓝色"的队伍，总有一场比赛会站错半场。第三是各类*控制信号*：操作手切换目标或陀螺仪漂移后要求视觉重置跟踪器之类。如果弹道在视觉端解算（后文专门讨论这个"如果"），还得加上*当前弹速*——摩擦轮转速与裁判系统测速都在电控手里。

反方向，视觉这边的独有资产是*对目标的认知*：目标在哪、多快、是不是还锁着，以及基于此的开火建议。至于这份认知以什么形态过链路——是解算好的云台角度指令，还是原始的目标运动状态——恰恰是本章最大的一个架构决策，先按下不表。

==== 一对真实的包

清单落成字节长什么样？下面这对收发包出自社区使用最广的开源视觉框架的串口模块（rm_serial_driver，Apache-2.0，本章仅此一处交代出处），可以当作教科书级的参考答案来逐字段解剖：

```cpp
struct ReceivePacket {            // 电控 → 视觉，28 字节
  uint8_t header = 0x5A;
  uint8_t detect_color : 1;       // 0-红 1-蓝：该打什么颜色
  bool reset_tracker : 1;         // 要求视觉重置跟踪器
  uint8_t reserved : 6;           // 保留位，留给未来
  float roll;                     // 云台姿态，单位 rad
  float pitch;
  float yaw;
  float aim_x;                    // 电控当前瞄准点（回显，供可视化）
  float aim_y;
  float aim_z;
  uint16_t checksum = 0;          // CRC16，覆盖前面全部字节
} __attribute__((packed));

struct SendPacket {               // 视觉 → 电控，48 字节
  uint8_t header = 0xA5;
  bool tracking : 1;              // 跟踪器是否锁定目标
  uint8_t id : 3;                 // 目标编号：0-前哨站 6-哨兵 7-基地
  uint8_t armors_num : 3;         // 整车装甲板数：2-平衡 3-前哨 4-普通
  uint8_t reserved : 1;
  float x, y, z;                  // 整车中心位置（惯性系）
  float yaw;                      // 当前目标装甲板朝向
  float vx, vy, vz, v_yaw;        // 平动速度与整车转速
  float r1, r2;                   // 两组装甲板的旋转半径
  float dz;                       // 相邻两层装甲板的高度差
  uint16_t checksum = 0;
} __attribute__((packed));
```

`__attribute__((packed))` 与基础篇的 `#pragma pack(1)` 是同一件事的两种写法：关掉填充，让结构体布局与线上字节流逐字节对应。先把两个包各自过一遍，再提炼里面通用的设计手法。

收包 28 字节，信息密度很高。三个欧拉角一起发而不是只发 pitch/yaw，因为把点位从相机系搬进惯性系需要*完整的*旋转——roll 平时接近零，但相机装歪或底盘倾斜时它就不是零了；欧拉角在这里够用，云台 pitch 有机械限位、远够不着 $plus.minus 90 degree$ 的万向锁死区（「坐标变换」一章的告诫在这条链路上恰好可以豁免）。`aim_x/y/z` 值得停一下：它是电控*当前实际瞄准点*的回传，视觉拿它没有算法用途，纯粹是调试用——把"视觉以为的目标"和"电控真正瞄的点"画进同一个可视化界面，两边认知一旦分叉当场现形。为调试专门花 12 个字节，这笔钱花得非常值。

发包 48 字节，内容更耐人寻味：里面*没有*云台角度指令。`x,y,z,yaw` 加 `vx,vy,vz,v_yaw` 加 `r1,r2,dz`——熟悉吗？正是「卡尔曼滤波」整车建模一节那组状态向量的工程扩展版——补上 z 向与两组半径、高度差（那一节结尾的旁注提过）。视觉发过去的不是"云台该转到哪"，而是"目标现在是什么状态"。这个选择牵出本章最重的一个架构话题，留到后面专节展开。

==== 三个通用手法：位域、保留位、没有帧尾

*位域省字节。*`detect_color` 和 `reset_tracker` 本质是两个开关，各占一个完整字节太奢侈——位域让 8 个开关挤进 1 字节。省下的绝对量不大（1 字节在 200 Hz 下是 200 B/s，约占链路 1.7%），真正的价值是把"开关类"信息集中管理、顺手留出扩展位。但位域有一条 C 标准埋的暗桩：位域在字节内的分配顺序是实现定义的，标准不保证 `detect_color` 落在 bit 0。两端恰好都用 GCC 系编译器（x86 的 gcc 与电控的 arm-none-eabi-gcc）、都在小端平台上，首个声明的位域才都落在最低位。所以协议文档里必须按 bit 编号写死（"bit 0 = detect_color"），而不是写"结构体第一个位域成员"——前者是合同，后者是对编译器的祈祷。

*reserved 是向前兼容的期权。*6 个保留 bit 加 1 个保留 bit 看似浪费，其实是整个协议里最便宜的保险：赛季中途要加一个新开关（比如"请求切换曝光档"），直接启用一个保留位——帧长不变、字段偏移不变、CRC 不变，没升级固件的那一端完全无感。这份兼容性有两条纪律要写进合同：发送端必须把保留位清零，接收端必须忽略保留位。谁违反一条，期权就作废了。

*没有帧尾，是故意的。*两个包到 `checksum` 就结束了，没有 `0x0D 0x0A` 之类的帧尾字节。敢这么设计的逻辑链是：定长协议里"帧在哪结束"由长度唯一确定，帧尾的定界功能是冗余的；帧的合法性由 CRC 判决，帧尾那点"同步检查"能力 CRC 也顺带覆盖了——CRC 失败一样会触发重新找帧头。于是帧尾能省则省，定长加 CRC 就是隐式的帧终止符。反过来说，*变长*协议里长度字段本身可能被噪声污染，那时帧尾（或者给头部单独上校验）才重新变得有价值——马上讲到。

=== 帧结构决策：每个字节都要给出理由

==== 帧头：0xA5 凭什么当选

先破除一个幻觉：帧头*不是*可靠的定界符。载荷是任意二进制（float 的四个字节什么值都可能出现），一个 48 字节的帧里，某个载荷字节恰好等于帧头的概率约 17%——基础篇状态机一节那道"假帧头锁死"思考题就是这么来的。帧头的真实身份是*同步加速器*：它让接收端在字节流里快速找到"疑似帧起点"，真正的判决交给定长加 CRC。既然只是加速器，帧头值的选择就只影响两件事：假同步的频率，和失步后重新锁定的速度。

即便如此，选值仍有讲究。`0x00` 和 `0xFF` 是最差的候选：线路空闲、断线被拉高拉低、结构体没初始化，制造出来的垃圾字节全是这两个值，拿它们当帧头等于主动往枪口上撞。`0xA5`（二进制 `10100101`）位型交替、不与常见垃圾冲突，而且它是裁判系统协议的帧起始字节——社区沿袭这个传统，队里新人看到 `0xA5` 就知道"这是帧头"，约定俗成也是工程价值。再看一个容易错过的细节：收发两个方向用了*不同*的帧头（`0x5A` 恰好是 `0xA5` 的按位取反）。这不是装饰——TX/RX 接反、半双工回声、调试时自发自收，同帧头的协议会把自己发的帧当对方的收下，错得悄无声息；异帧头让这类接线层事故直接表现为"完全收不到"，而且抓包时一眼分清字节流方向。

==== 定长还是变长

定长协议的全部优点浓缩成一个词：*简单*。解析器三步走（找头、收满、验 CRC），失步恢复的行为好推理，带宽占用是常数好预算，两端的实现加起来百来行。代价是僵硬：每帧必须传全字段，哪怕这一帧没什么可说的；想加字段就得改帧长，两端必须同步升级。

变长协议（裁判系统就是典型：帧头里带长度字段、紧随其后是命令号，不同命令不同载荷）解决的正是这份僵硬——几十种消息共用一条链路，各传各的长度。但复杂度立刻上一个台阶：长度字段本身可能被噪声毁掉，接收端按假长度吞字节，一错错一串，所以裁判系统给帧头单独配了 CRC8、整帧再配 CRC16，解析状态机也从三态涨到五六态。判据由此清晰：链路上只有一两种高频消息，用定长；消息种类多、频率杂、载荷差异大，才值得上变长。视觉与电控之间恰好是前者——每个方向各一种包、高频刷新，定长是正解，别为想象中的扩展性提前买单。

==== CRC8 还是 CRC16：检错账与算力账

#block(breakable: false)[
两笔账摆在一起看：

#figure(
table(
  columns: (auto, auto, auto, auto, auto),
  align: (left, center, center, center, center),
  stroke: (x, y) => (
    top: if y == 1 or y == 0 { 1pt } else { 0pt },
    bottom: if y == 2 { 1pt } else { 0pt },
  ),
  inset: 8pt,
  [*方案*], [*帧开销*], [*每字节计算*], [*突发错误保证*], [*随机漏检率*],
  [CRC8（位运算）], [1 字节], [8 轮移位异或], [≤ 8 bit 全检出], [$1\/256 approx 0.4%$],
  [CRC16（查表）], [2 字节], [1 次查表异或（表占 512 B）], [≤ 16 bit 全检出], [$1\/65536 approx 0.0015%$],
),
caption: [CRC 选型的两笔账。检错一列的读法：两种 CRC 都能检出所有单比特错误和不超过自身位宽的突发错误；"随机漏检率"指损坏超出保证范围时，坏帧恰好凑出合法 CRC 的概率，即 $2^(-n)$。算力一列的读法：两种实现在这条链路上都便宜到可以忽略——选型的真正筹码在检错列。],
)
]

先算算力账，因为它最常被高估。115200 波特下链路每秒最多 11520 字节，位运算 CRC8 每字节 8 轮移位异或，全速跑满也只占 72 MHz 单片机约 0.5% 的算力；查表 CRC16 每字节一次异或一次查表，代价是 512 字节常量表——对上位机不值一提，对单片机的 flash 也是零头。算力在两端都不构成选型理由，谁拿"单片机算不动 CRC16"当论据，把这笔账甩给他。

真正的筹码是检错账。假设某条线接地不良，干扰恶劣到每秒打坏一帧——7 分钟一局比赛约 420 个坏帧。CRC8 的漏网期望是 $420 \/ 256 approx 1.6$ 帧：一局比赛里可能有一两帧假数据混进控制回路，云台目标角瞬间跳向一个随机值。CRC16 的期望是 $420 \/ 65536 approx 0.006$ 帧：整个赛季都难漏一帧。一字节的带宽换四个数量级的把握，这就是双向包都选 CRC16 的理由。顺带说透实现来源：那份查表实现（多项式 `0x1021` 的反射式、初值 `0xFFFF`、无终值异或，即 CRC 目录里的 CRC-16/MCRF4XX）连函数名都和各队从裁判系统协议附录沿袭下来的实现一模一样——社区直接沿用了官方的成熟实现，你也应该沿用，*而不是*自己发明第五十一种 CRC16 变体。

变体这件事必须敲黑板。"CRC16"不是一个算法，是一族算法：多项式、初值、是否反射、结果字节序，四个参数任何一个不同，算出的值就永远对不上。经典翻车现场：两端各自从网上抄了一份"CRC16"，一份 MODBUS 一份 CCITT，联调时每一帧都校验失败，两边各自检查自己的代码半天——都"没错"。所以这四个参数是协议合同的必填条款，包括最后一条：本协议 CRC 结果*低字节在前*追加到帧尾。

==== 字节序：一条必须写下来的默契

基础篇讲过 `memcpy` 结构体收发隐含"两端字节序相同"的假设，x86 与 Cortex-M 恰好都是小端，所以整套做法成立。本章补的是契约视角：恰好成立的假设也要写进合同。字节序条款一共三句话——多字节整数小端，float 为 IEEE 754 单精度、按小端字节序传输，CRC 低字节在前。写下来花三十秒；不写，哪天有人在大端平台上跑仿真、或拿 python 脚本对拍时少写一个 `<`，就要用一下午来还。

=== 带宽预算：一根 115200 的管子有多粗

协议定型前必须过一遍算术关：这根管子装得下你想传的东西吗？115200 波特指每秒 115200 个*码元*，8N1 帧格式下每个字节要背 1 个起始位加 1 个停止位，线上共 10 bit——所以可用字节率是

$
115200 \/ 10 = 11520 "字节/秒"
$

注意串口是全双工，收发两个方向*各有*11520 B/s，互不挤占。拿真实的包算：视觉发包 48 字节，相机 100 fps 时每秒 $48 times 100 = 4800$ 字节，占发送方向 41.7%；电控 200 Hz 发 28 字节收包，每秒 5600 字节，占接收方向 48.6%。两个方向都留有余量，这套配置是健康的。

带宽账还有不那么显眼的另一半：*时延*。一帧 48 字节要在线上爬 $48 times 10 \/ 115200 approx 4.17 "ms"$——帧不是一个瞬时事件，是一列缓缓驶过的车厢。这 4.17 ms 直接计入从"视觉算完"到"电控拿到"的端到端延迟（这笔延迟去哪了、怎么补偿，是「时间戳对齐」一章的正题）。频率再往上推，问题从时延变成生存：48 字节的帧在 $11520 \/ 48 = 240 "Hz"$ 处触顶——超过这个频率，发送速度追不上生产速度，队列只进不出，每一帧的延迟都比上一帧更长，直到缓冲区炸掉。所以预算要留余量：不是怕平均值超载，是怕瞬时抖动没有消化的空间——稳态占用摸到 70% 以上，就该动手瘦身或提速了。

#figure(
  image("images/practice-protocol-bandwidth.png", width: 100%),
  caption: [115200 波特链路的带宽预算。左图：占用率随帧率线性增长，蓝线为 48 字节发包、红线为 28 字节收包；圆点标出典型工作点（100 Hz 占 41.7%、200 Hz 占 48.6%），方块是 48 字节帧的饱和点 240 Hz——线一旦进入灰色区，队列开始无界增长。右图：同一帧在时间轴上的真身——48 字节要在线上爬 4.17 ms；100 Hz 时线路 42% 忙碌，尚有喘息，200 Hz 时 83% 忙碌，帧与帧之间几乎首尾相接，任何抖动都无处消化。读图规则：左图看"装不装得下"，右图看"为什么不能只看装不装得下"——占用率没到 100% 之前，时延和抖动余量已经先没了。生成脚本 `examples/practice/protocol_bandwidth.py`。],
)

预算吃紧时有三条路，按性价比排序。第一条：*字段瘦身*。float 是最肥的类型，很多量用 16 位定点就够——yaw 用 int16 表示 $plus.minus 180 degree$，分辨率 $360 \/ 65536 approx 0.0055 degree$，远超 IMU 本身的精度，四字节瞬间变两字节。第二条：降频。收包里姿态之外的字段（颜色、复位标志）根本不需要 200 Hz，拆一个 10 Hz 的低频配置包出来，高频包只留姿态——这就自然走向了变长或多包协议，复杂度的账前面算过。第三条：提波特率。460800、921600 在常见 USB 转串芯片上都支持，但要过电控端的时钟关：接收端在每个码元中点采样，一帧 10 bit 累计漂移不能超过半个码元，两端波特率合计偏差必须小于约 5%，波特率越高、分频越粗，这个裕度越紧张——提速之后先跑长时间误码统计再上车。

最后拆穿一个容易糊弄自己的地方：如果电控是经 USB 虚拟串口直连的（设备名 `/dev/ttyACM0`，参考实现的默认配置正是它），那么链路上*根本没有真实的 UART 段*——"115200"只是个被忽略的名义参数，真实吞吐由 USB 决定（全速 USB 线速 12 Mbit/s，远超任何波特率）。而经 USB 转 TTL 芯片连接的（设备名 `/dev/ttyUSB0`），芯片到单片机那一段是真 UART，上面的预算算术句句作数。分清自己链路的物理构成，是做预算的前提；但即便走 USB 直连，这笔算术也别扔——它是你协议的"最低配置需求"，保证哪天换回真串口，系统照样能跑。

=== 弹道在哪端算：载荷是架构的投影

现在回收前面的伏笔：为什么参考实现的发包里没有云台角度？因为那支框架把运动预测、装甲板选择、弹道解算*全部放在了电控端*——视觉只当"传感器"，把目标整车状态原样交出去。而基础篇串口节的 11 字节 `GimbalCmd` 示例帧（yaw、pitch、开火标志）代表另一个极端：视觉把所有计算做完，电控只当"执行器"。两种拆分都是社区里活着的方案，你的队伍必须选一个，而且*协议载荷会随选择彻底改变*——你的帧里传什么，暴露了你把哪些计算放在哪端。

#figure(
table(
  columns: (auto, auto, auto),
  align: (left, left, left),
  stroke: (x, y) => (
    top: if y == 1 or y == 0 { 1pt } else { 0pt },
    bottom: if y == 6 { 1pt } else { 0pt },
  ),
  inset: 8pt,
  table.header([*维度*], [视觉端解算（发角度指令）], [电控端解算（发目标状态）]),
  [典型帧内容], [yaw、pitch、开火标志，约 11 字节], [整车状态向量，约 48 字节],
  [瞄准点刷新], [随指令到达，受相机帧率与链路时延限制], [电控以 1 kHz 本地外推，链路时延被吸收],
  [弹速等参数], [在电控侧，需回传给视觉], [就地取用，零延迟],
  [算法调试], [全在上位机，日志与可视化工具齐全], [分裂到单片机侧，手段贫瘠],
  [团队接口], [窄：一问一答，两组各自迭代], [宽：预测模型跨两个代码库，联调更深],
  [电控端复杂度], [执行器，最简], [要实现外推、选板与弹道，浮点与调试成本高],
),
caption: [上/下位机弹道分工的取舍表。读表规则：左列赢在"简单与可调试"，右列赢在"新鲜数据"——瞄准回路的最后一公里离 1 kHz 的 IMU 和弹速越近，高速目标下的表现越从容。没有全赢的一列，这正是它成为架构决策而非常识的原因。],
)

把表里最重的一行展开说。「卡尔曼滤波」异步更新一节讲过，预测和修正可以拆开跑：量测 100 Hz 到来，预测却能以 1 kHz 外推出"此刻"的目标状态。问题是这套外推跑在哪颗芯片上。视觉端解算的方案里，它跑在上位机——每条角度指令算完还要在串口上爬约一毫秒、进电控再排一次调度，而指令一旦发出就冻结了，两帧之间目标继续动，电控只能拿旧角度硬扛。电控端解算的方案里，外推跑在单片机上，收到的目标状态最多旧几十毫秒，但瞄准角是用*本地最新*的状态实时刷新的——相机帧率和串口时延被本地预测整段吸收。目标越快（小陀螺，或腾空越过场地坡道的"飞坡"），这个差距越值钱。

代价同样实打实。把预测和弹道搬进单片机，意味着一套浮点数值算法要在没有日志文件、没有绘图工具、printf 都嫌奢侈的环境里调试；意味着视觉组改一版整车模型，电控组的外推代码要同步改，算法的因果链横跨两个仓库、两次烧录。一句话概括判据：把计算放在离它所需最新数据最近的那端，把调试留在工具最好的那端——这两条准则经常打架，打架时看你们队电控组的工程实力站在哪边。

本书的路线要在这里交代清楚：「弹道解算」一章按视觉端解算的拆分展开。不是因为它更好，而是它的联调界面最窄、全部调试都发生在工具链齐全的上位机，适合把弹道这门课本身学明白。学完那一章，上面这张表的每一行你都有能力自己重新打分；真到了要换拆分的那天，弹道数学一个字都不用重学——变的只是代码跑在哪颗芯片上。

=== 协议表：一式两份的团队合同

协议一旦定型，它的身份就变了：不再是一段代码，而是视觉组与电控组之间的*合同*。「软件工程基础」一章给过硬件接口文档的模板（帧格式表加命令定义），这里补上合同的条款清单——每一条都对应一种真实发生过的翻车方式：

- *物理参数*：115200、8 数据位、无校验位、1 停止位、无流控。最后一项不是废话：TX/RX/GND 三线接法根本没有 RTS/CTS 线，谁的驱动开了硬件流控，发送就会永远等一个不存在的信号。
- *帧定义*：帧头值、帧总长、逐字节偏移表。
- *每个字段的语义*：类型、单位、坐标系、正方向。这是 CRC 的盲区——单位错误的帧校验完全合法、数据全错。yaw 是 rad 还是 deg（差 57.3 倍）、逆时针还是顺时针为正、pitch 抬头还是低头为正、位置在惯性系还是云台系，四个问题当面问清、白纸黑字。参考实现遵循 ROS 的 REP-103 约定（rad、右手系），而多数电控代码的习惯单位是 deg——这条鸿沟每年淹死一批新队伍。
- *位域*：钉到 bit 编号，不引用结构体声明顺序（前文讲过那是实现定义的）。
- *字节序与浮点格式*：三句话条款，前文已给。
- *CRC 参数*：多项式、初值、反射、覆盖范围、追加字节序，五项全写。
- *保留位纪律*：发送清零，接收忽略。
- *版本*：以上任何一条变动即为新版本。一个实用技巧是拿帧头值当版本号——协议大改时换一个帧头，新旧固件之间不是"解析出错的数据"而是"完全收不到帧"，大声失败永远好过安静出错。

合同还要有单一事实来源。最好的形态是两端共用同一份头文件（子仓库引用，或复制时在注释里带上来源提交号）；退一步是一份协议表文档进 git 仓库，改协议必须走双方都能看到的评审。基础篇教过的 `static_assert` 钉帧长，在这里升格为合同的执行机构——字段加错了编译期就炸，轮不到联调时抓瞎。别嫌这套流程重：协议是全队修改成本最高的组件，改一次牵动两个代码库、两次烧录、一轮联调，设计时在保留位和版本条款上多花的一小时，赛季里会以十次省下的联调还给你。

=== 验证：怎么确认两端真的一致

合同签了，两边各自回去实现——到这里为止，"一致"还只是愿望。上车前要过三关，一关比一关接近真实。

==== 第一关：金样帧，让 python 当裁判

打包再解析自己的输出，永远能通过——这种自证清白毫无价值。要验证的是*实现与合同一致*，办法是请一个中立第三方按合同独立造一帧标准答案。python 的 `struct` 模块正合适，格式串里的 `<` 把字节序条款变成了可执行的断言：

```python
import struct

def crc16(data: bytes, crc: int = 0xFFFF) -> int:
    """合同条款：多项式 0x1021 反射式（0x8408）、初值 0xFFFF、无终值异或"""
    for b in data:
        crc ^= b
        for _ in range(8):
            crc = (crc >> 1) ^ 0x8408 if crc & 1 else crc >> 1
    return crc

# 金样：detect_color=1(蓝) reset=0，rpy=(0.1,0.2,0.3)，aim=(1,2,3)
body = bytes([0x5A, 0x01]) + struct.pack('<6f', 0.1, 0.2, 0.3, 1.0, 2.0, 3.0)
frame = body + struct.pack('<H', crc16(body))   # '<H'：CRC 低字节在前
print(frame.hex(' '))
```

实际运行输出（这里的位运算实现与参考代码的查表版逐字节对拍过，二者等价）：

```text
5a 01 cd cc cc 3d cd cc 4c 3e 9a 99 99 3e 00 00
80 3f 00 00 00 40 00 00 40 40 45 f3
```

这 28 个字节就是*金样帧*（golden bytes）：把它写死进视觉端的单元测试（打包结果必须逐字节等于它，解析它必须还原出那组字段值），电控端用同一帧做同样的事。两份金样一致，两端的一致性就在各自的测试里被钉住了——今后任何人改动结构体、CRC 或字节序，自己队的 CI 会先于对面的队友发现。整条收发链路的回归测试则回指基础篇的 pty 回环：不插一根线，把打包、传输、解析、验错全部跑成自动化测试。

==== 第二关：裸字节联调，先看见再解析

硬件到齐后，别急着跑完整程序——程序一复杂，问题就没法归因。联调第一步是让电控循环发固定测试帧，上位机*只看裸字节*：

```bash
stty -F /dev/ttyACM0 115200 raw    # 配成 raw 模式（机制见基础篇串口节）
xxd -g1 < /dev/ttyACM0 | head      # 十六进制逐字节打印
```

肉眼检查三件事：帧头 `0x5A` 在不在；相邻两个帧头间距是不是恰好 28 字节；挑一个已知字段手工解码核对——比如 roll 发的是 0.1，字节流里对应偏移处就该是 `cd cc cc 3d`（拿 `struct.unpack('<f', ...)` 反解）。字节全对，再上完整解析程序；字节就不对——满屏乱码、间距漂移——问题在物理层，软件怎么改都没用，该请逻辑分析仪出场了：卡在 TX 线上直接量位宽，115200 波特下一个码元应为 $1 \/ 115200 approx 8.68 "µs"$，位宽不对是波特率配置或时钟误差，电平毛刺则查接线与共地。

==== 第三关：故障演习，好协议要会体面地失败

正常路径通了只是及格，协议设计里真正花心思的部分——CRC、帧头、重同步——全在异常路径上，而异常路径不主动演习就永远是未测试代码。三个必做的破坏性实验：

- *注入坏帧。*改一个字节但不重算 CRC，发过去，确认对端真的丢弃并计数。这一关专治"校验代码存在但从未生效"——比如算了 CRC 却没检查返回值、if 分支写反——这类 bug 在正常联调里永远不会暴露，因为正常帧永远校验通过。
- *拔线重插。*模拟比赛中最常见的物理故障，确认两端都能重连恢复：上位机要能重开设备（参考实现里有现成的重开逻辑），电控端的接收中断或 DMA 错位后要能靠帧头重新同步。
- *中途复位。*任何一端重启，都会从字节流中间开始收——先吃进半帧垃圾，然后必须在一两帧内重新锁定。锁不上的，回去重读基础篇那道假帧头思考题。

最后长跑收尾：整套系统连续跑半小时，两端各自统计发帧数、收帧数、CRC 失败数。有线串口的物理误码本底极低，CRC 失败应当是罕见事件——失败率要是到了百分之几的量级，别归咎运气，那是接地、波特率偏差或 DMA 配置在报警，逐项排查物理层。数字干净，这份协议就可以冻结进合同、贴上版本号了。还剩一件部署琐事：比赛现场插拔 USB 后设备名会在 `ttyUSB0`、`ttyUSB1` 之间漂移，用 udev 规则把设备钉成固定名字的完整做法，在「RoboMaster应用篇」串口模块一章。
