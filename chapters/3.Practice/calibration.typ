#import "/template/template.typ": *

上一章定义了视觉端与电控端的数据契约及其验证方法。要把图像量测转换到惯性系，还需要两组几何参数：描述成像与畸变的相机内参，以及相机系到云台系的固定安装变换。理论篇「相机模型」介绍了平面标定的约束、参数优化和重投影误差；本章把这些原理落实为采集、求解和验收流程，并说明每项检查能够发现什么、不能证明什么。

前半章先处理相机内参与畸变：标定板怎样制作，图像怎样覆盖姿态和视场，`calibrateCamera` 的输出怎样检查。后半章再求「坐标变换」链中的 $bold(T)_(G C)$。PnP 给出的位姿位于相机坐标系，只有结合这项相机到云台的固定变换，才能继续转换到云台系和惯性系。对应的手眼标定部分会说明坐标链、采集姿态、退化条件和独立验收动作；需要推导时回指理论篇，但不会把理论结论当成实机数据已经合格的证据。

=== 标定板：先把尺子本身做准

平面标定同时使用标定板上已知的几何坐标和图像中检测到的对应点。板面尺寸或平整度有系统误差时，优化器会把错误的几何当作真值，因此先要选择并检查标定板：

#figure(
table(
  columns: (auto, auto, auto, auto),
  align: (left, left, left, left),
  stroke: (x, y) => (
    top: if y == 1 or y == 0 { 1pt } else { 0pt },
    bottom: if y == 3 { 1pt } else { 0pt },
  ),
  inset: 8pt,
  [*板型*], [*图像点来源*], [*要求全部指定点可见*], [*适用特点*],
  [棋盘格], [黑白格内角点 + 亚像素精化], [是], [制作简单、工具成熟，适合作为入门方案],
  [圆点阵], [圆斑投影的质心], [是], [斑点中心易检测，但斜视时要考虑投影圆心与椭圆中心的偏差],
  [ChArUco], [棋盘角点 + ArUco 编号], [否（角点带编号）], [可使用部分可见的板面；API 是否可用取决于 OpenCV 版本与构建，使用前检查 `cv2.aruco`],
),
caption: [三种常见平面标定板的取舍。经典棋盘格检测通常要求指定的全部内角点可见，因此把角点覆盖到图像边缘时需要谨慎构图；ChArUco 用编号建立对应关系，可以保留部分可见视图。选择还要结合打印质量、检测工具和目标视场，表中没有对所有相机都最优的一项。],
)

板型确定后，先检查实际尺寸。打印时关闭“适应页面”等自动缩放，并用卡尺在多个位置测量格边长度；程序应填写测量值，而不是只相信打印设置。若把所有板面坐标统一放大 $alpha$ 倍，理想投影可以通过把每幅图的平移同时放大 $alpha$ 倍而保持不变，因此均匀尺度错误通常不改变内参，却会使 PnP 得到的平移按比例偏置。手眼标定把这份相机侧平移与机器人侧的真实长度共同求解，最终安装平移也会受尺度错误影响，但不应假定它在所有姿态下恰好按同一个 $alpha$ 缩放。

板面还应固定在经过检查的刚性平面上。纸张起皱、基底翘曲或粘贴不平都会破坏“所有角点共面”的模型，而且增加同一块板的照片数量不会消除这项固定偏差。较大的重投影残差可能提示板面、角点检测或模型有问题；反过来，较小 RMS 也不能单独证明板面几何准确，因为部分误差可能被内参、畸变和逐帧位姿吸收。

最后确认 pattern size 的含义。OpenCV 要求的是*内角点数，而不是格子数*：10×7 个格子的棋盘对应 9×6 个内角点。

参数元组按 `(columns, rows)` 排列，所以程序写 `(BW, BH) = (9, 6)`，不能按图像数组常见的行、列顺序反填。尺寸填错时，检测器可能失败，也可能在更大的图案中找到非预期子区域；应同时检查返回的角点数量和叠加图，而不是只看布尔返回值。行列一奇一偶（如 9×6）会让 180° 旋转后的黑白排列不同，可降低方向歧义，但不能替代明确的板坐标系、角点顺序检查；需要稳定编号时可使用 ChArUco。

=== 采集：拍多少张、怎么举板子

可以把 *20 到 40 张有效图像*作为普通镜头的起始采集量，但张数本身不是验收条件。平面标定中每个独立单应只提供有限的内参约束；姿态、距离和画面位置相近的图像会提供高度相关的信息。应先保证视场与姿态覆盖，再通过后文的逐图残差和重采样稳定性判断是否继续采集。

*覆盖整个工作视场。*标准径向/切向畸变模型用一组共享系数拟合不同半径和方向上的观测；没有角点覆盖的区域缺少直接验证，只能依赖这组全局模型的假设。若目标区域的半径或方向还超出已采样范围，才属于外推。许多镜头的径向畸变随离主点距离增大而更明显，因此四角尤其值得覆盖，但这不是所有镜头和畸变项的定律。经典棋盘格要求全部内角点可见，可通过改变板在画面中的位置和大小多拍几组；需要部分板面出画时可考虑 ChArUco。

*沿多个轴改变板面姿态。*在理想针孔模型中，若平面始终正对相机，把焦距和板面距离同时乘以 $alpha$ 可以保持投影比例不变，因而焦距与距离难以分离。倾斜板面会让角点具有不同深度；绕不同轴倾斜还能避免所有视图落在相近的退化方向。30° 到 45° 可作为一次采集的起始范围，但可用上限取决于棋盘尺寸、分辨率、景深和检测器：倾角过大导致角点过密、失焦或定位不稳时，应减小角度或靠近拍摄，而不是机械追求某个角度。

*固定成像配置并保证角点清晰。*先在工作距离上完成对焦，再关闭自动对焦并固定对焦环；变焦、光圈、图像分辨率、ROI（裁剪区域）、binning（像素合并）以及相机图像处理器（ISP）的去畸变设置也要与部署配置一致。若采集中途改变了会影响投影模型的设置，应分开标定并重新采集对应数据，不能继续混用。拍摄时让板保持静止或缩短曝光，避免运动模糊、过曝反光和景深不足破坏亚像素定位。清晰角点有助于降低定位噪声，但不能消除板面几何和模型选择造成的系统误差。

=== 跑通 calibrateCamera

核心步骤是检测角点、做亚像素精化，再把每幅图的三维—二维对应传给标定接口。下面的骨架调用 `calibrateCameraExtended`，同时保留图像路径并检查输入尺寸，便于后续定位逐图误差：

```python
import cv2, glob
import numpy as np

BW, BH, SQ = 9, 6, 0.025          # 内角点 9x6；格边长 25 mm，以卡尺实测为准
objp = np.zeros((BW * BH, 3), np.float32)
objp[:, :2] = np.mgrid[0:BW, 0:BH].T.reshape(-1, 2) * SQ

obj_pts, img_pts, used_paths = [], [], []
image_size = None
for path in sorted(glob.glob("calib/*.png")):
    gray = cv2.imread(path, cv2.IMREAD_GRAYSCALE)
    if gray is None:
        print(f"[skip unreadable] {path}")
        continue
    size = gray.shape[::-1]
    if image_size is None:
        image_size = size
    elif size != image_size:
        raise ValueError(f"mixed image sizes: {size} vs {image_size}")
    ok, corners = cv2.findChessboardCorners(gray, (BW, BH))
    if not ok:
        print(f"[skip no board] {path}")
        continue
    corners = cv2.cornerSubPix(
        gray, corners, (11, 11), (-1, -1),
        (cv2.TERM_CRITERIA_EPS + cv2.TERM_CRITERIA_MAX_ITER, 30, 0.01))
    obj_pts.append(objp.copy())
    img_pts.append(corners)
    used_paths.append(path)

if len(obj_pts) < 3:
    raise RuntimeError("need at least 3 detected views; also verify pose diversity")

rms, K, D, rvecs, tvecs, _, _, per_view = cv2.calibrateCameraExtended(
    obj_pts, img_pts, image_size, None, None,
    flags=cv2.CALIB_FIX_K3)        # 本例先只估计 k1、k2、p1、p2
print(f"RMS = {rms:.3f} px")
for path, e in zip(used_paths, per_view.ravel()):
    print(f"  {path}: {e:.3f} px")
```

代码中的“至少三张”检查只拦住数量不足，不判断姿态是否退化；三张相同或近似正视的图像仍会通过这项检查，因此还要结合姿态覆盖、当前模型假设下的参数标准差和子集稳定性判断数据是否可辨识。

这里的 `CALIB_FIX_K3` 把 $k_3$ 固定为零，只把 $k_1,k_2,p_1,p_2$ 作为畸变参数估计。对本章后面那类窄视场数据，它可以作为较低自由度的起始模型，但不能仅凭“窄视场”认定 $k_3$ 必然无用。应检查残差在图像半径上的结构，并用留出图像或重复采样比较增加参数后是否改善未参与拟合的数据；广角或鱼眼镜头还可能需要 OpenCV 的 rational 或 fisheye 模型。`calibrateCameraExtended` 比 `calibrateCamera` 多返回模型内参数标准差和 `per_view` 等诊断量，本章下一节使用每幅图的重投影 RMS；这些标准差不包含板面错误或模型失配等系统项。

ROS 相机标定工具中的 X、Y、Size、Skew 进度条提供了图形化采集提示：它们分别反映板面在画面中的位置、大小和倾斜覆盖。进度条适合发现明显缺口，但不替代角点叠加图、逐图误差和最终的独立验证。

=== 验收内参：RMS 之外还要看什么

真实采集通常没有已知内参可直接对照，因此不能靠一个数宣布“标对了”。下面分别检查拟合残差、逐图异常、物理量级和重复采样稳定性；这些证据互补，但仍只覆盖实际采集的成像配置和姿态范围。

*先看总 RMS，但不给它脱离场景的统一及格线。*使用清晰棋盘和亚像素角点时，0.1 到 0.5 像素常可作为排查的参考量级，超过 1 像素则值得检查板面、角点、成像模型和输入尺寸；具体阈值还受分辨率、角点大小、镜头视场和检测器影响。很小的训练 RMS 也不等于参数能推广到未覆盖姿态：图像少、姿态相关或模型自由度过高时，拟合数据上的残差仍可能很低。

*再看 per-view 误差和角点叠加图。*总 RMS 会把所有视图汇总，少数异常图可能被其余数据稀释。`examples/practice/calibrate_selfcheck.py` 提供了一个可重复的合成自检：用固定内参与畸变投影角点，为每个图像坐标加入标准差 0.15 像素的独立高斯噪声，并把编号 17 的噪声提高到 1.5 像素，再交给与正文相同的 OpenCV 求解流程。在当前环境中运行得到：

```text
[all 25 views] RMS = 0.472 px | fx = 1806.9 (truth 1807.1)
    worst view: #17 at 2.14 px
[bad view removed] RMS = 0.204 px | fx = 1806.6 (truth 1807.1)
    worst view: #11 at 0.25 px
```

这个合成案例说明，在所设噪声模型下，总 RMS 为 0.472 像素时仍包含一幅 2.14 像素的异常视图，而 `per_view` 能把它定位出来。实际数据应结合误差分布和角点重投影叠加图检查高误差图；不要只按最大值自动删除，因为那一张也可能是唯一覆盖某个画面角落或倾斜方向的数据，较高残差还可能暴露模型不合适。更稳妥的处理是先判断错检、模糊、反光或覆盖缺口，再修正或重拍，并记录删除理由。

删除合成异常视图后，RMS 为 0.204 像素，接近每坐标噪声标准差 $sigma = 0.15$ 时的二维误差量级 $sigma sqrt(2) approx 0.21$ 像素；恢复的 $f_x$ 与设定值相差约 0.5 像素。它验证的是：在投影模型正确、噪声独立高斯且姿态由脚本生成的这一组条件下，当前评估代码能定位人为异常并近似恢复 $f_x$；没有逐项报告和容差的其他参数不在这项结论内。拟合还会消耗部分噪声自由度，真实残差又会混入板面误差、角点偏差和模型失配，因此不能把 RMS 等同于真实角点噪声，也不能由合成自检推断真实照片已经标定正确。

*再做物理量级核对。*$f_x$ 以图像像素为单位；若图像使用传感器原生像元且没有缩放或 binning，乘以像元尺寸可得到近似物理焦距。例如，假设设备资料给出的原生像元尺寸为 3.45 µm，后文示例的 $f_x approx 1807$ 对应约 $6.23 "mm"$；若所装镜头标称 6 mm，两者处在相近量级。标称焦距、对焦位置和主面定义都会带来差异，所以这只能发现倍率级错误，不能作为精密验收。若差异很大，应检查图像缩放、ROI/binning 后的有效像元尺寸、标定文件分辨率、单位和成像模型。$f_x$ 与 $f_y$ 接近、主点靠近几何中心也只是常见现象；偏离是否可接受，要结合传感器结构、重复标定和残差分布判断。

*最后检查参数稳定性和未参与拟合的数据。*用不同图像子集或不同采集批次重复求解，比较 $f_x,f_y,c_x,c_y$ 和畸变系数的变化；若 RMS 很低但参数随删掉几张图就大幅漂移，说明当前结果不稳定，姿态覆盖、可辨识性、混入的异常图、板面偏差和模型失配都需要检查，不能只凭这一现象确定原因。还可固定求得的内参，在留出的棋盘图上重新估计板位姿并画重投影残差，或在工作距离用独立量具检查 PnP 尺度。容许变化应由下游角度与位置误差预算决定，而不是从本节的像素经验值直接推出。

=== 标定文件长这样

下面是一份开源自瞄配置中的 1440×1080 单目标定结果，可用来练习读取 ROS `CameraInfo` 字段。它只是某台相机在某次成像配置下的结果，不是同型号设备的通用参数：

```yaml
image_width: 1440
image_height: 1080
camera_matrix:
  data: [1807.12121,    0.     ,  711.11997,
            0.     , 1806.46896,  562.49495,
            0.     ,    0.     ,    1.     ]
distortion_model: plumb_bob
distortion_coefficients:
  data: [-0.078049, 0.158627, 0.000304, -0.000566, 0.000000]
```

`camera_matrix` 是理论篇的 $bold(K)$：$f_x approx 1807.1$、$f_y approx 1806.5$，主点约为 $(711.1, thin 562.5)$；若以 $(720, 540)$ 近似图像几何中心，这份主点分别偏约 8.9 和 22.5 像素。`plumb_bob` 是 ROS 对 Brown–Conrady 模型的名称，五个系数依次是 $k_1,k_2,p_1,p_2,k_3$。这里 $k_3=0$，与前面固定 $k_3$ 的示例模型一致；$k_1<0$ 表示靠近中心的主导径向项趋向桶形，但完整位移仍由全部系数共同决定。

完整 `CameraInfo` 还包含 rectification 矩阵 `R` 和 projection 矩阵 `P`。按 ROS 的字段语义，原始单目像素通常配合 `K`（这里的 `camera_matrix`）和 `D`（`distortion_coefficients`）使用。

校正后的像素要配合实际重采样所用的新矩阵。标准单目 `CameraInfo` 通常取 `P` 左侧的 3×3 部分，并不再施加原始畸变；具体图像话题是否已经校正，仍要核对发布节点。

图像尺寸同样不能忽略。`image_width`、`image_height`、ROI 和 binning 共同描述发布图像与传感器全分辨率坐标之间的映射：ROI 裁剪会改变坐标原点和有效区域，binning 或后续缩放会改变像素尺度。内参必须与实际发布的图像流对应；这些字段或处理链与标定时不一致时，不能直接套用。工程配置还应把相机序列号、对焦/变焦位置和标定日期与这组数值关联起来。

#figure(
  image("images/practice-calibration-distortion.png", width: 100%),
  caption: [按上面这组参数计算理想像素到畸变像素的位移幅度。左图给出全画幅分布，右图沿离主点最远的图像角方向取样：半径 300 像素处约 0.70 像素，约 341 像素处超过 1 像素，有效画面最远角约 9.70 像素。径向 $k_1$ 项的位移从三次量级开始，切向项还会贡献二次量级，因此完整曲线不应简称为纯 $r^3$。这些数值只对应本例参数；生成脚本 `examples/practice/calibration_distortion_map.py`。],
)

这张图展示的是模型校正量，不是重投影误差，也不能单独推出目标位置或命中偏差。PnP 有三种彼此一致的坐标路径：原始角点直接配合 $bold(K),bold(D)$；`undistortPoints(..., P=K_new)` 输出新像素坐标后配合 `K_new` 且不再传畸变；或者省略 `P` 得到归一化坐标后配合 $bold(I)$ 且不再传畸变。OpenCV 在省略 `P` 时不会返回原内参下的像素，不能把这组归一化坐标再与 $bold(K)$ 混用。选定一条路径后应用测试向量核对，既不能漏掉畸变，也不能重复校正。

=== 什么时候要重标

重标触发条件要按参数来源区分。更换镜头、改变对焦/变焦或镜头相对传感器发生机械位移，会改变原内参的适用前提，应重新验证并在超出验收线时重标。切换分辨率、binning、ROI 或 ISP 几何处理后，原文件中的数值不能不经处理直接沿用；对于有明确坐标映射的纯裁剪或缩放，可以按映射更新 $bold(K)$ 并验证，映射不明、包含非线性重采样或验证不合格时则重新标定。相机从云台上拆装、支架受撞或螺丝位置改变，首先影响的是相机到云台的手眼外参；若镜头与传感器没有相对变化，不能据此断定内参也变了。温度可能通过镜筒和焦点改变有效参数，但幅度取决于具体镜头与温区，要求高时应做冷热重复标定或留出图验证，而不是用季节或场地位置替代测量。

PnP 出现系统性距离偏差时，可先核对装甲板类型和物理尺寸：把大、小装甲板尺寸用反会使三维点模型不符并产生系统性位姿偏差。只有所有物体点坐标统一缩放时，PnP 平移才在理想条件下按同一比例缩放；若宽、高或点布局的比例改变，偏差还取决于姿态和求解残差，不能直接套一个尺寸比。随后检查当前图像尺寸/裁剪是否与内参匹配、角点与畸变处理是否一致，再分别检查内参、手眼外参和时间对齐。配置管理上应做到一台相机、一个成像模式对应一份可追踪文件，至少记录序列号、镜头、焦点/变焦、分辨率和标定版本。同型号设备仍可能因装配与对焦不同而需要各自参数；若焦距误差为 2%，在其他条件相同的近似下，5 m 深度也会产生约 2%（10 cm）的尺度偏差，这个算例说明了错用文件的风险，并不声称所有同型号相机都有 2% 差异。

=== 手眼标定：从坐标链得到 AX = XB

「坐标变换」一章中的 $bold(T)_(G C)$ 把相机系 C 中的坐标变换到云台系 G；相机与云台刚性连接时，它在一次安装中固定不变。它与相机内参标定输出的逐图“外参”不是同一个量：后者是标定板 B 到相机 C 的 $bold(T)_(C B, i)$，每幅图都有一组。

把标定板固定在参考系 O 中，并记录第 $i$ 个姿态下云台到参考系的 $bold(T)_(O G, i)$。同一个板到参考系的变换可写成

$
bold(T)_(O B) = bold(T)_(O G, i) bold(T)_(G C) bold(T)_(C B, i).
$

对姿态 $i,j$ 各写一次并消去固定的 $bold(T)_(O B)$，可得到

$
bold(A)_(i j) bold(X) = bold(X) bold(B)_(i j),
$

其中

$
bold(A)_(i j) = bold(T)_(O G, i)^(-1) bold(T)_(O G, j), quad
bold(B)_(i j) = bold(T)_(C B, i) bold(T)_(C B, j)^(-1), quad
bold(X) = bold(T)_(G C).
$

这组定义把乘法方向固定下来：PnP 通常给出板到相机的 $bold(T)_(C B)$，不能未经求逆就当成相机到板。映射到 `cv2.calibrateHandEye` 时，G 对应 gripper，O 对应 base，B 对应 target；应传入每个姿态的 `gripper2base = T_OG` 和 `target2cam = T_CB`，函数返回 `cam2gripper = T_GC`。调用后要用上面的闭环重新计算 $bold(T)_(O B)$，检查各姿态是否一致，而不能只凭函数正常返回判断方向正确。

基础篇 Ceres 示例按 $bold(A) bold(X)=bold(X) bold(B)$ 构造残差，OpenCV 则提供 Tsai、Park、Daniilidis 等求解方法。不同方法对噪声、运动幅度和退化姿态的敏感性不同，可把多方法结果差异作为诊断量，不能预设方法差异总比数据误差小。旋转方程 $bold(R)_A bold(R)_X=bold(R)_X bold(R)_B$ 不含平移，可先独立求旋转；平移方程仍依赖已求旋转、两端的长度单位和坐标原点。若旋转用手眼、平移用结构测量，必须保证两者描述的是同一 G/C 原点，并用闭环数据验收组合结果。

因此也可以把结构测量作为初值或部分约束。下面是一个只表示字段含义的配置示例，数值不代表某台实车：

```yaml
T_gimbal_camera:
  xyz: "0.10 0.0 0.05"     # 相机原点在 G 系中的位置，单位 m
  rpy: "0.0 0.0 0.0"       # 示例安装旋转；顺序和正方向必须另行定义
```

结构测量平移的优点是直观，但相机光心、yaw/pitch 轴线及其交点未必能直接用卡尺触及，毫米级读数不等于毫米级坐标原点误差。若 $bold(T)_(G C)$ 的平移误差范数为 1 cm，它对变换后点的位置影响也保持 1 cm 量级，不随目标距离线性放大，但误差方向会随云台姿态旋转。小旋转误差对点的位置影响一阶近似为 $d_perp abs(delta theta)$，其中 $d_perp$ 是点到误差旋转轴的垂直距离，因此不超过 $d abs(delta theta)$。在垂直方向取 $d=5 "m"$ 时，0.5° 对应约 4.4 cm；这个算例给出远距离安装角误差的上界量级，不证明某种支架必然产生 0.5° 误差。

因此，平移是否可以手量，要看坐标原点能否可靠定位以及 1 cm 或更小误差是否满足下游预算；旋转是否采用手眼结果，则要看数据是否具备足够激励和闭环残差是否稳定。混合路线是一个可验证的选择，不是默认最优方案。光学系到机体系的名义轴重排由坐标约定给出，真实安装相对名义方向的偏差仍属于待标定旋转。

=== 手眼数据怎么采

对于相机随云台一起运动的 eye-in-hand（眼在手上）配置，可以把棋盘固定在参考系 O 中，保持机器人底盘不动，再改变云台姿态。每次等待角速度与结构振动降到设定阈值后，记录图像及其曝光时刻对应的云台姿态，并用已验收的内参求出 $bold(T)_(C B)$。15 到 25 个有效姿态可作为起始数量，但独立运动方向和角度跨度比总数更重要：

#figure(
table(
  columns: (auto, auto, auto),
  align: (left, left, left),
  stroke: (x, y) => (
    top: if y == 1 or y == 0 { 1pt } else { 0pt },
    bottom: if y == 5 { 1pt } else { 0pt },
  ),
  inset: 8pt,
  [*检查项*], [*目的*], [*应观察的证据*],
  [15～25 个姿态作为起点], [形成多组有差异的相对运动], [子集求解是否稳定，而不只看样本数],
  [yaw、pitch 都转，并包含足够大的相对角], [避免所有旋转轴平行或运动接近单位阵], [运动轴分布、堆叠方程的奇异值/条件数],
  [板在画面中有足够角点间距], [降低 PnP 对角点噪声的敏感性], [逐图 PnP 重投影误差；深度应为正，姿态不应在平面歧义解之间跳变],
  [板面倾角保持在检测可靠范围], [避免透视压缩、失焦和错检], [角点叠加图，而不是固定 45° 阈值],
  [板固定；曝光时云台已稳定], [维持同一 $bold(T)_(O B)$ 并降低时刻错配], [角速度阈值、时间戳配对和重复采样差异],
),
caption: [手眼标定采集清单。求解器返回数值只说明算法完成，不说明运动充分可观；单轴或小角度数据可能得到有限但不稳定的结果。残差、运动矩阵条件数、不同子集/方法的一致性和物理闭环需要一起检查。],
)

退化可以直接从平移方程看出：

$
(bold(R)_A - bold(I)) bold(t)_X = bold(R)_X bold(t)_B - bold(t)_A.
$

对一次绕轴 $bold(n)$ 的旋转，$(bold(R)_A-bold(I)) bold(n)=0$，因此该运动不能约束 $bold(t)_X$ 沿这根轴的分量；旋转方程也保留绕共同轴的歧义。如果全部相对运动都只绕 yaw 轴，堆叠系统仍会秩亏或病态，数值求解器可能返回一个受噪声和初值影响很大的结果。加入 pitch 提供非平行旋转轴是解除这类退化的关键，但还要有足够角度并检查条件数、子集结果和闭环残差，不能把“两个轴都动过”当成充分验收。

可用转角还受视场限制。以 $f_x approx 1807$、宽 1440 像素为例，理想针孔水平视场约为 $2 arctan(1440/(2 times 1807)) approx 43.5 degree$；若板本身占约三分之一画面，保持整板可见时可用 yaw 跨度只有约 30°。这是本例的几何估算，不是通用最低转角。可以缩小板在画面中的占比，或者移动板后另采一组；板一旦移动，$bold(T)_(O B)$ 就改变，只能在组内构造运动对。显式接收 $(bold(A),bold(B))$ 对的求解器可以合并各组的组内约束；`cv2.calibrateHandEye` 接收的绝对姿态列表默认共享同一个固定 target，不应把不同板位姿直接拼成一列。

静止后采集会降低时间偏移的影响，因为稳定区间内姿态近似不变，但它不能取消时间戳要求。应保存图像曝光时间、姿态样本时间和稳定判据，并检查同一姿态重复采样的离散程度。缩短采集时间只能减少部分 IMU 漂移风险；是否可忽略 yaw 漂移要通过起止姿态复测或独立编码器/参考方向判断。

=== 验收手眼：静止目标在参考系中应保持稳定

验收分为代数自洽性和留出数据的物理闭环，两者覆盖的失败模式不同。

*第一层：数值残差。*对每个运动对计算误差变换

$
bold(E)_(i j) = (bold(A)_(i j) bold(X))^(-1)
                 (bold(X) bold(B)_(i j)),
$

再用 $bold(E)$ 的旋转对数范数表示角度误差、平移向量范数表示长度误差，报告中位数、尾部和最大值。还应把每个绝对姿态代回 $bold(T)_(O B,i)=bold(T)_(O G,i) bold(X) bold(T)_(C B,i)$，检查固定标定板的位姿散布，并留出部分姿态不参与求解。残差量级受 PnP 噪声、姿态测量、板尺度和运动幅度影响，不能用“零点几度/毫米级”作为所有系统的统一合格线。低训练残差只表示这些方程彼此接近；秩亏数据也可能拟合得很好，因此还必须结合上一节的可观测性和子集稳定性。

*第二层：物理闭环。*使用未参与求解的图像，固定棋盘或装甲板并保持机器人参考系不动，让云台在目标仍可见的范围内同时改变 yaw 与 pitch，再把每帧估计的目标点或板位姿转换到 O 系。理想刚体、无噪声且时间完全对齐时，同一目标应得到同一坐标；实测散布还包含角点/PnP、姿态噪声、结构弹性和时间误差。下面的图用一组指定参数展示几种误差的敏感性，它是仿真案例，不是根据轨迹形状唯一诊断根因的查表：

#figure(
  image("images/practice-calibration-handeye.png", width: 100%),
  caption: [指定条件下的手眼误差仿真：云台扫 yaw ±20°、pitch ±10°，目标位于 4 m 处。左图用完整旋转矩阵计算 O 系横截面轨迹；1 cm 平移误差形成厘米量级的小轨迹，不同轴向的 1° 旋转误差分别表现为明显散布或近似固定偏移。右图按误差轴与目标方向垂直的情形，用小角度近似 $d abs(delta theta)$ 比较距离敏感性，并以 13.5 cm 宽装甲板的 6.75 cm 半宽作尺度参考；超过参考线不等同于已验证的脱靶判据。轨迹还会受目标方向、扫描范围和其他误差影响，本图只说明可能的形态与量级。生成脚本 `examples/practice/calibration_handeye_wander.py`。],
)

在姿态轨迹和其他采集条件可比时，改变扫描速度有助于区分误差来源：在刚性、静态模型中，固定外参误差主要随姿态而不是速度变化；小时间偏移 $Delta t$ 引入的角度误差幅度一阶近似为 $norm(omega) abs(Delta t)$，对应位置误差还会随距离放大。若散布随速度明显增加，时间错配是一个需要检查的解释，但滚动快门、结构动态变形、回差和检测模糊也可能产生相似关联，仍需下一章的独立时间测试。两轴扫描能暴露更多方向，但不能保证识别所有分量。

仅观察“点云是否集中”只能检验姿态相关的漂移；若没有独立测量目标在 O 系中的位置，近似恒定的整体偏差可能看不出来。需要绝对准确度时，应测量板在 O 系的位姿，或使用另一条经过校核的几何闭环。合格线应从下游预算倒推：例如 4 m 处允许的横向误差若为 3 cm，对应的小角度量级约为 $3 "cm" / 4 "m" approx 0.43 degree$，但这 3 cm 还要在角点、PnP、姿态、时间与外参之间分配，不能全部留给手眼旋转。支架拆装、相机更换或碰撞后，应按预算重新验证；是否需要重采由验证结果决定。

==== 本章小结

内参标定要同时控制板面几何、角点清晰度、视场/姿态覆盖和成像配置。验收不能只看总 RMS，还要检查逐图与空间残差、参数在子集和批次间的稳定性、留出图表现及物理量级。每份参数都应关联相机、镜头、焦点、分辨率和处理模式；配置改变后先按既定标准复验，再决定是否重标。

手眼标定从固定板闭环导出 $bold(A) bold(X)=bold(X) bold(B)$，实现时必须明确 $bold(T)_(O G)$、$bold(T)_(C B)$ 和返回的 $bold(T)_(G C)$ 方向。采集需要非平行且幅度足够的相对旋转，并保存时间配对与稳定状态；验收要结合可观测性、刚体变换残差、留出姿态和静止目标的物理闭环。静止目标的散布验证的是静态一致性，绝对偏差还需要已知参考，而这些检查都不能替代高速运动时的时序验证。下一章「时间戳对齐」将继续处理图像曝光时刻与云台姿态的对应关系。
