#import "/template/template.typ": *

上一章结尾，协议冻结进了合同，电控的姿态与弹速开始可靠地流进视觉端。本章轮到视觉交出自己的那半边可靠：一帧图像要变成惯性系里一个可信的点，中间隔着两组必须标得准的参数——相机内参，和相机到云台的外参。理论篇「相机模型」一章把标定的数学讲完了：棋盘格提供已知三维点，单应给内参约束，最后用像素域 RMS 重投影误差做非线性精化——但收尾时留了三个字面问题：*拍多少张、怎么举板子、怎么验收*。本章开场就还这笔债。还完之后再接着往下走一步：内参只把"相机自己"量准了，PnP 交出的位姿还锁在相机系里，而「坐标变换」一章的链式公式需要 $bold(T)_(G C)$——相机到云台的固定变换——才能把目标搬进惯性系。这个变换从哪来、怎么标、标歪了怎么看出来，就是本章后半场的手眼标定。全章只关心三件事：怎么做、怎么验、怎么翻车；每个"为什么"都已经在理论篇讲过，这里只回指不重讲。

=== 标定板：先把尺子本身做准

标定的全部信息来源是"三维坐标已知的点"，而这些坐标是*印*出来的——板子本身有多准，标定结果的上限就有多高。先选板型：

#figure(
table(
  columns: (auto, auto, auto, auto),
  align: (left, left, left, left),
  stroke: (x, y) => (
    top: if y == 1 or y == 0 { 1pt } else { 0pt },
    bottom: if y == 3 { 1pt } else { 0pt },
  ),
  inset: 8pt,
  [*板型*], [*角点从哪来*], [*要求整板可见*], [*一句话取舍*],
  [棋盘格], [Harris 族响应 + 亚像素精化], [是], [精度高、打印容易，默认选它],
  [圆点阵], [椭圆质心], [是], [质心抗噪，但透视下投影圆心与椭圆中心有微小系统偏差],
  [ChArUco], [棋盘角点 + ArUco 码定位], [否（角点带编号）], [残缺视图也能用，最容易把画面角落喂满，旧版 OpenCV 需装 contrib，4.7 起已并入主库],
),
caption: [三种标定板的取舍。读表规则：主要看第三列——棋盘格任何一张图都必须整板入画，这让"把板举到画面角落"变得费劲（角落容易切出画面），ChArUco 的带编号角点正是冲这一点来的；入门用棋盘格完全够，追求角落覆盖时再换。],
)

板型定了，*制版*的三条纪律比选型更重要。第一，打印时选 100% 缩放——"适应页面"会把格子悄悄缩放百分之几；打完用卡尺量实际格边长，以量出来的值为准填进标定程序。顺带算清这个参数错了会坑谁：把所有三维点整体放大 $alpha$ 倍，深度也放大 $alpha$ 倍，投影一个像素都不变（理论篇讲过的尺度歧义），所以格边长填错*不影响内参*——但每张图的外参平移全错，后面手眼标定的平移也跟着错。第二，纸必须贴在硬而平的基底上（玻璃、亚克力、KT 板），因为板面不平等于"已知三维点"的真值本身有系统误差，这种误差你用多少张图都平均不掉——理论篇说 RMS 明显超 1 像素要"怀疑棋盘不平"，指的就是它。第三，角点数怎么数：OpenCV 的 pattern size 填的是*内角点数、不是格子数*——10×7 个格子的棋盘是 9×6 个内角点（四格相交处才是角点）。填错的症状很有欺骗性：不是标出离谱的数，而是 `findChessboardCorners` *永远返回失败*，新手会误以为光线不好，反复调曝光调了一下午。我们用一张合成棋盘实测：按 (9, 6) 找，54 个角点全数命中；按 (10, 7) 找，直接返回 false。另外行列取一奇一偶（如 9×6）不是审美问题：它让板子旋转 180° 后图案不自洽，角点排序才有唯一解。

=== 采集：拍多少张、怎么举板子

先回答张数：*20 到 40 张*，再多收益递减。理论篇算过每张图只贡献两条内参约束、姿态雷同的图约束近乎重复——所以真正的关键不是张数，是*姿态铺开*。铺开有三个具体动作：

*角落必须拍到。*畸变模型是多项式，多项式只在有数据的地方被约束，没有角点覆盖的画面角落全靠外推——恰好畸变最大的就是角落。棋盘要求整板入画，把板举到边角时很容易切出画面，多试几张，或者干脆用 ChArUco。

*板子要斜着举。*这是最容易被忽略、也最有数学分量的一条。板子正对相机时，所有角点深度相同，把焦距和距离同时放大 $alpha$ 倍，每个角点的投影*严格不变*——正对姿态给焦距的约束是病态的，拍一百张正对图也钉不住 $f_x$。倾斜让角点深度各不相同，这对互补立刻被打破。所以举板子要往各个方向斜出 30° 到 45°，像给相机展示一本翻开的书的各种角度；超过 45° 角点检测精度反而掉，不必更极端。

*锁死对焦和光圈。*理论篇讲过对焦位置会改变有效焦距——所以先在工作距离上对好焦，然后拧紧、贴胶带，标定和比赛用同一个对焦位置。标定过程中碰了对焦环，前面拍的全部作废。此外每张图拍摄时板保持静止（或曝光足够短），运动模糊会直接吃掉亚像素精度——角点定位精度是标定精度的上限，「传统视觉算法」一章讲 `cornerSubPix` 时埋过这句话，现在兑现。

=== 跑通 calibrateCamera

流程本身没有悬念：找角点、精化到亚像素、喂给 `calibrateCamera`。完整脚本骨架如下：

```python
import cv2, glob
import numpy as np

BW, BH, SQ = 9, 6, 0.025          # 内角点 9x6；格边长 25 mm，以卡尺实测为准
objp = np.zeros((BW * BH, 3), np.float32)
objp[:, :2] = np.mgrid[0:BW, 0:BH].T.reshape(-1, 2) * SQ

obj_pts, img_pts = [], []
for path in sorted(glob.glob("calib/*.png")):
    gray = cv2.imread(path, cv2.IMREAD_GRAYSCALE)
    ok, corners = cv2.findChessboardCorners(gray, (BW, BH))
    if not ok:
        print(f"[skip] {path}")     # 这张没找到板：切边了/太斜了/反光了
        continue
    corners = cv2.cornerSubPix(
        gray, corners, (11, 11), (-1, -1),
        (cv2.TERM_CRITERIA_EPS + cv2.TERM_CRITERIA_MAX_ITER, 30, 0.01))
    obj_pts.append(objp)
    img_pts.append(corners)

rms, K, D, rvecs, tvecs, _, _, per_view = cv2.calibrateCameraExtended(
    obj_pts, img_pts, gray.shape[::-1], None, None,
    flags=cv2.CALIB_FIX_K3)        # 窄视场：只标 k1 k2 p1 p2
print(f"RMS = {rms:.3f} px")
for i, e in enumerate(per_view.ravel()):
    print(f"  view {i:2d}: {e:.3f} px")
```

两处选择交代一下。`CALIB_FIX_K3` 把 $k_3$ 钉死为零，理由理论篇算过：窄视场下 $r$ 不超过 0.5，$r^6$ 项微乎其微，多标一个参数只会把噪声拟合成规律。`calibrateCameraExtended` 与 `calibrateCamera` 同源，多返回几样诊断信息——我们要的是 `per_view`（每张图各自的重投影 RMS），下一节靠它验收。图形化的路线也有：ROS 生态的标定工具会实时显示 X、Y、Size、Skew 四条覆盖进度条，本质上就是在催你完成上一节的"铺开"，社区常用，机制相同。

=== 验收内参：RMS 之外还要看什么

标定输出没有"真值"可对照，怎么知道标对了？三道检查，一道比一道具体。

*第一道：总 RMS。*口径与理论篇完全一致：合格的标定 RMS 在 *0.1 到 0.5 像素量级，明显超过 1 像素就该排查*——棋盘不平、角点错检、姿态覆盖不足。低到 0.05 以下也别高兴太早：图太少或姿态太单一时，模型把噪声也拟合进去了，数字好看恰恰是过拟合的味道。

*第二道：per-view 误差。*总 RMS 是平均数，平均数会撒谎。为了看清这套验收流程本身靠不靠谱，我们做一个没有相机也能跑的自检实验（完整脚本 `calibrate_selfcheck.py`）：用已知参数把角点投影出来、加上每坐标 0.15 像素的高斯噪声当"检测误差"，其中故意把第 17 张的噪声放大十倍模拟手抖，然后把这批合成数据喂给 `calibrateCamera`。实测输出：

```text
[all 25 views] RMS = 0.472 px | fx = 1806.9 (truth 1807.1)
    worst view: #17 at 2.14 px
[bad view removed] RMS = 0.204 px | fx = 1806.6 (truth 1807.1)
    worst view: #11 at 0.25 px
```

三个读法。其一，坏图混进去之后总 RMS 是 0.472——*仍在"合格带"里*，只看总数你根本不会起疑；per-view 一列印出来，第 17 张的 2.14 像素立刻现形。工程规则由此而来：验收看的不是平均值，是*最大值*——单张明显超群，回去看那张图（糊了、反光、角点顺序错位），剔除后重标。其二，剔除后 RMS 落在 0.204，和理论预期严丝合缝：每坐标 $sigma = 0.15$ 像素的噪声，代入理论篇的 RMS 定义（每个角点两个平方分量相加），期望值是 $sigma sqrt(2) approx 0.21$ 像素——RMS 的物理身份就是*角点检测噪声的水平*，模型对的时候它不多不少。其三，恢复出的 $f_x$ 与真值差在半个像素上下（0.03%），说明流程闭环。你的评估脚本先过这个自检，再去评真实照片，它说的话才可信。

*第三道：$f_x$ 量纲核对。*$f_x$ 是"以像元为单位的焦距"，乘回像元尺寸就该回到物理焦距：拿本章后面那份真实标定文件算，$1807 times 3.45 "µm" approx 6.23 "mm"$，配的是标称 6 mm 的定焦镜头——偏大 4%，正常（标称值本就圆整，对焦位置又会改变有效焦距）。这道换算防的是*成倍的错*：算出来 3 mm 或 12 mm，多半是角点数或分辨率设置填错了一族参数。反过来，有几样"看着不对"的数不是毛病：$f_y$ 与 $f_x$ 差万分之几（现代像元近乎正方）、主点离几何中心偏几个到几十个像素（装配公差，理论篇点过名）——这些是器件的本来面目，别当成标坏了反复重标。

=== 标定文件长这样

社区开源自瞄框架里有一份真实的标定输出，正好当读件（1440×1080 工业相机，即理论篇算例的同款规格）：

```yaml
image_width: 1440
image_height: 1080
camera_matrix:
  data: [1807.12121,    0.     ,  711.11997,
            0.     , 1806.46896,  562.49495,
            0.     ,    0.     ,    1.     ]
distortion_model: plumb_bob
distortion_coefficients:
  data: [-0.078049, 0.158627, 0.000304, -0.000566, 0.000000]
```

逐项过。`camera_matrix` 就是理论篇的 $bold(K)$：$f_x approx 1807.1$、$f_y approx 1806.5$（差万分之四）、主点 $(711.1, thin 562.5)$（离几何中心 $(720, 540)$ 偏了 9 和 22 个像素）。`plumb_bob` 是 ROS 对 Brown 五参数模型的叫法，五个数依次是 $k_1, k_2, p_1, p_2, k_3$：$k_1 = -0.078$ 是轻度桶形，$k_3$ 固定为零——正是前面 `CALIB_FIX_K3` 的选择。完整文件里还有 rectification 与 projection 两个矩阵，那是双目/去畸变重采样管线用的；单目自瞄的 PnP 只吃 `camera_matrix` 加 `distortion_coefficients`，其余可以不管。这组畸变系数落到画面上到底是多大，算出来看：

#figure(
  image("images/practice-calibration-distortion.png", width: 100%),
  caption: [把上面那份真实标定文件的畸变系数逐像素算成位移。左：全画幅的位移幅度等值图，主点（品红十字）附近几乎为零，往外按 $r^3$ 起步增长；右：沿最远角方向的位移曲线，离主点 300 像素内不足 0.7 像素，342 像素处越过 1 像素，画面角落达 9.7 像素。读图规则：跟踪状态下目标被云台拉回画面中部，畸变常在 1 像素以内——但目标刚出现在画面边缘时可达近 10 像素，按理论篇的深度放大账（5 米处每像素约 10 厘米）足以打飞，所以对 4 个角点做去畸变这一步永远不能省。生成脚本 `examples/practice/calibration_distortion_map.py`。],
)

=== 什么时候要重标

内参长在相机和镜头的组合上，动了哪样都要重标：*换镜头、拧过对焦环或光圈环、相机拆装或磕碰之后*——立刻重标，没有侥幸。温度是慢变量：镜筒热胀冷缩会让有效焦距微漂，跨季节、跨南北场地时值得例行重标一次。日常排障则倒着走：PnP 测距出现*系统性*偏差时，先查最便宜的嫌疑人——大小装甲板判型和填进去的物理尺寸（理论篇算过判错板型深度整体差 1.7 倍），再怀疑内参。最后一条是管理纪律：内参是相机的身份证，*一机一档*，文件名带上相机序列号——拿 A 相机的参数喂 B 相机，两台"同型号"相机之间百分之几的焦距个体差异，按理论篇的深度放大账在 5 米外就是十厘米量级的系统误差。

=== 手眼标定：AX = XB 以及它在 RM 的样子

现在处理后半个问题。「坐标变换」一章的链式公式里，$bold(T)_(G C)$（相机系到云台系）被标注为"固定不变、靠标定测出"——注意这里的"外参"和理论篇标定推导里"每张图的外参"是重名的两回事：那边指相机相对棋盘的位姿（每张图各一个），这边指相机相对云台的*安装*位姿（只有一个，焊死在结构上）。求它的经典做法叫手眼标定，原理一句话就能推完：把棋盘固定在墙上不动，云台摆出姿态 $i$ 和 $j$，两个姿态下"板在 odom 系里的位姿"这同一个量可以沿链条写两遍并相等，消去它，得

$
bold(A) bold(X) = bold(X) bold(B)
$

其中 $bold(A)$ 是云台在两姿态间的相对运动（IMU 或编码器给出），$bold(B)$ 是相机眼中棋盘的相对运动（对板做 PnP 给出——棋盘在这里客串了一回装甲板），$bold(X) = bold(T)_(G C)$ 是要求的固定安装。求解器不用自己写：基础篇 Ceres 一节的手眼标定示例就是按这个方程构的残差，社区也常用 `cv2.calibrateHandEye`（Tsai、Park、Daniilidis 等一列方法常量，工程上方法间的差异远小于数据质量的影响）。还有一条值得知道的结构性质：$bold(A) bold(X) = bold(X) bold(B)$ 的旋转部分 $bold(R)_A bold(R)_X = bold(R)_X bold(R)_B$ 自成方程、可以单独解——这意味着*平移和旋转允许走不同的路线*。

这就引出两条路线的取舍。社区开源配置里能看到另一条路的真实样子：

```yaml
odom2camera:
  xyz: "0.10 0.0  0.05"    # 相机在云台原点前方 10 cm、上方 5 cm——尺量的
  rpy: "0.0  0.0  0.0"     # 安装角按理想取零——赌装配完美
```

手工静态外参：平移用卡尺和结构图量，旋转按图纸理想值取零（光学系到机体系的轴重排矩阵不用标，「坐标变换」一章直接写出过）。这条路线该不该走，算一笔账就清楚。平移量错 1 厘米，目标位置就错这固定的 1 厘米，不随距离放大——卡尺量到毫米级毫无压力，*平移用手工完全够*。旋转不一样：装角差 $delta theta$ 造成的目标位置误差是 $delta theta dot d$，随距离线性放大——0.5° 在 5 米处就是 4.4 厘米，而 0.5° 恰恰是 3D 打印件受力变形、垫片不平、螺丝拧紧顺序这些日常因素给得出的量；它还随云台姿态改变方向，没法用一个常数补偿量修掉。所以结论是混合路线：*平移信卡尺，旋转信手眼*——自动标定真正值钱的就是旋转那三个自由度。

=== 手眼数据怎么采

采集现场很朴素：棋盘贴墙，机器人静止上电（odom 清零），云台摆一个姿态、*静止半秒*、同时记录一帧图像和当时的 IMU 姿态，PnP 解出板的位姿，存一组；换姿态重复，采 15 到 25 组。要求列成表：

#figure(
table(
  columns: (auto, auto, auto),
  align: (left, left, left),
  stroke: (x, y) => (
    top: if y == 1 or y == 0 { 1pt } else { 0pt },
    bottom: if y == 5 { 1pt } else { 0pt },
  ),
  inset: 8pt,
  [*要求*], [*为什么*], [*不满足的样子*],
  [15～25 组姿态], [每对姿态才贡献一组 $(bold(A), bold(B))$ 约束], [组数少解不稳，残差忽大忽小],
  [yaw、pitch 两根轴都要转，各自用足可用范围], [旋转轴全平行时方程欠约束（下文详述）], [求解器*照样给你一个数*，上车才翻车],
  [板占画面约三分之一], [太小则角点噪声直接进 $bold(B)$；太大则云台没了转动余地], [残差压不下去，或旋转覆盖不够],
  [板面与光轴夹角小于 45°], [极端角度下角点检测精度骤降], [个别组 PnP 明显离群],
  [每个姿态静止约半秒再采], [运动模糊 + 图像与姿态的时刻错配], [残差怎么调都下不来],
),
caption: [手眼标定采集清单。读表规则：第二行是唯一会*静默失败*的一条——其余几条不满足都会在残差里露馅，唯独旋转轴覆盖不足时解算照常收敛、数字看着体面，坑要到上车才炸，采集时优先盯它。],
)

那条"静默失败"值得展开讲透。方程的平移部分整理后含有因子 $(bold(R)_A - bold(I))$，它在 $bold(A)$ 的旋转轴方向上恒为零——一次绕单轴的运动，对"绕这根轴的旋转分量"和"沿这根轴的平移分量"*给不出任何信息*。所有姿态都只转 yaw，$bold(X)$ 就有一整族解满足方程，最小二乘会从中随便挑一个交差，不报错、不警告。好在云台天生就有 yaw、pitch 两根不平行的轴，两根都用上就够——这也是采集清单第二行的全部含义。还有一条云台特有的现实约束要摊开说：机械臂领域"旋转覆盖 60° 以上"的常见建议在云台上做不到，因为板必须始终在画面里，旋转范围的物理上限是视场角减去板的张角——这台 1440×1080 相机水平视场约 43°，板占三分之一画面时 yaw 可用范围只剩 30° 上下。对策有二：板别贪大，换转动余地；或者把板挪个位置再采一组，*组内配对、组间不配对*——基础篇 Ceres 版求解器吃的本来就是运动对 $(bold(A), bold(B))$，天然支持分组，`cv2.calibrateHandEye` 则默认整个列表对着同一块不动的板，分组时要每组各算或换求解器。最后，静止采集顺带绕开了图像与 IMU 姿态的时间同步问题（那是下一章的正题）；整场采集控制在几分钟内完成，IMU 的 yaw 漂移才不会混进 $bold(A)$。

=== 验收手眼：静止目标必须纹丝不动

验收分两层，先便宜后昂贵。

*第一层：数值残差。*解出 $bold(X)$ 后，把每对 $(bold(A), bold(B))$ 的 $bold(A) bold(X)$ 与 $bold(X) bold(B)$ 相减：旋转差换算成角度、平移差取毫米，报均值和最大值。这查的是*数据自洽性*——旋转残差压在零点几度、平移残差毫米级，说明采集质量过关；旋转残差上了 1° 或平移上了厘米级，先回头查采集（运动模糊、姿态时刻错配、轴覆盖不足），不要怪求解器。

*第二层：上车物理验收。*「坐标变换」一章预告过外参标歪的症状：静止目标在惯性系的坐标随云台转动漂移——固定环节的误差被时变的 $bold(T)_(O G) (t)$ 转着圈搬运。把它做成一个验收动作：静止装甲板（或棋盘）放在 4 米外，云台画八字扫（保持目标在画面里），把每帧解算出的 odom 坐标画成点云。好外参的点云缩成亚厘米的一团；坏外参的签名用仿真精确算出来是三种，各有各的长相：

#figure(
  image("images/practice-calibration-handeye.png", width: 100%),
  caption: [手眼验收的误差签名（精确仿真，云台扫 yaw ±20°、pitch ±10°，目标 4 米）。左：静止目标在 odom 横截面（$y$-$z$）里的轨迹——完美外参是一个点（星号）；1 cm 平移误差（蓝）缩成 1 cm 外的小团，几乎不随扫动移动；旋转误差 1° 若绕视轴（品红点划线）扫出高约 5 cm 的"画圈"轨迹，若垂直视轴（品红实线）则整体偏出约 6.7 cm 而几乎不动——*最会伪装的正是它*，看起来只像"准星不正"。右：误差随距离的账——旋转误差按 $delta theta dot d$ 线性放大，1° 在 3.9 米外就超过装甲板半宽（脱靶），平移误差恒定 1 cm。读图规则：点云会动 → 有绕视轴的旋转分量或时间戳问题；点云不动但整体偏 → 有垂直视轴的旋转分量；把偏移或摆幅除以距离，就是旋转误差的弧度数——摆幅这条是量级估计，严格值还要除以 2 sin(扫幅)，扫得越窄越看不出绕视轴的误差。近处验平移、远处验旋转。生成脚本 `examples/practice/calibration_handeye_wander.py`。],
)

三条读图规则之外，还有两条验收纪律。其一，*鉴别时间戳*：外参误差与云台转速无关——慢扫快扫，偏移和摆幅一个样；时间戳错配的误差是 $omega dot t_d$，随角速度线性放大、慢扫时消失。慢扫仍在的归本章，快扫才冒头的归下一章。其二，*验收和采集吃同一碗数学*：只扫单轴同样验不出绕该轴的误差分量，八字扫（两根轴都动）不是仪式感，是共轭结构决定的必需动作。定一条实用的合格线：4 米处点云的整体偏移与摆幅都压进 3 厘米以内（折合约 0.4°），拿去喂滤波器才不至于让整车建模从一开始就背着系统偏差。最后，手眼外参与内参一样有保质期：相机支架拆装过、换过相机、撞过车，重采一轮数据只要十几分钟，比带着 1° 的隐形装角打完一整场比赛便宜得多。

==== 本章小结

内参标定的全部工程含量浓缩成三句：采集时板要平、姿态要野、角落要到、对焦要锁；评估时总 RMS 落在 0.1～0.5 像素量级只是入场券，per-view 揪离群、$f_x$ 乘像元尺寸核物理焦距才算验完；换镜头、动对焦、磕碰、大温差之后重标，一机一档。手眼标定把 PnP 的输出接进坐标变换链：$bold(A) bold(X) = bold(X) bold(B)$，平移信卡尺、旋转信手眼；采集要两根轴都转、板别贪大、静止再拍，欠约束是唯一不报错的坑；验收先看 AX 与 XB 的残差，再上车让静止目标接受"纹丝不动"的审判——偏移除以距离就是你的装角误差。到这里，一帧图像已经能变成惯性系里一个可信的点。但"静止验收通过"只保证了静态一致性，云台一旦高速转动，图像和姿态的*时刻*对不对得上是另一道关口——下一章「时间戳对齐」专门守它。
