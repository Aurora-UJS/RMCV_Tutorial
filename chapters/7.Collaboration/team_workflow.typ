#import "/template/template.typ": *

=== 战队和公司不一样，但不一样在哪里

网上关于团队协作的资料，绝大多数是为企业写的：Scrum、看板、站会、工时估算、Sprint 评审。直接照搬到学生战队通常会失败——推行两周，没人再更新看板，流程自然死亡。

失败的原因不是学生不自律，而是这些方法解决的问题和战队面对的问题不同。要设计出能真正运转的协作方式，先要把战队自己的约束说清楚。

学生战队有三条硬约束：

*约束一：人员按年更替，知识持续蒸发。* 队员大二进队、大三主力、大四退役，任何一个人掌握的东西，两年后一定会从队里消失。企业里有五年工龄的老员工，战队里没有——最资深的人也只做过两个赛季。这意味着「靠人记住」的方案在战队里注定失效，而这恰恰是企业里最常用的方案。

*约束二：截止时间不可谈判。* 分区赛的日期由组委会定，不会因为「代码没写完」推迟一周。企业里的排期可以和产品经理协商，战队不能和组委会协商。所有工作必须从比赛日往回倒排。

*约束三：没有强制力。* 队员不拿工资，没有绩效考核，没有上级。任何增加负担的流程，只能靠自愿执行。一个需要每天花十分钟维护的流程，在两周内就会被放弃。这条约束决定了：#strong[能落地的流程必须足够轻]，轻到即使只有一半人执行，剩下的部分仍然有价值。

这三条约束推出的结论，和企业的常规做法有系统性差异。

// ══════════════════════════════════════════════════════════
// TODO（待补充）：企业 vs 战队的协作方式对比
//
// 这一段留给有企业实习或工作经验的队员补充。建议对比维度：
//   - 代码生命周期：企业维护数年 vs 战队赛季末重写
//   - 需求来源：客户/产品经理 vs 规则手册 + 场上表现
//   - 质量门禁：CI 强制 + 必须审批 vs 无法强制
//   - 人员流动：低，有资深工程师 vs 每年换一批
//   - 排期方式：Sprint 迭代可协商 vs 比赛日倒排不可改
//   - 文档动机：合规/交接/审计 vs 唯一动机是防止知识蒸发
//
// 补充时注意：目的不是说明「企业更规范」或「战队更灵活」，
// 而是说明两种约束下的最优解不同。避免写成对企业流程的介绍。
// ══════════════════════════════════════════════════════════

#note[
本节留有一处待补充内容（企业与战队协作方式的系统对比），将在后续版本中由有企业开发经验的队员补齐。
]

=== 分支约定

Aurora-UJS 各仓库目前实际在用的分支命名，是一套标准的按类型前缀方案：

#figure(
  table(
    columns: (auto, auto, auto),
    align: (left, left, left),
    stroke: (x, y) => (
      top: if y == 0 or y == 1 { 1pt } else { 0pt },
      bottom: if y == 5 { 1pt } else { 0pt },
    ),
    inset: 5pt,
    table.header([*前缀*], [*用途*], [*实例*]),
    [`feat/`], [新功能], [`feat/serial-bridge`],
    [`fix/`], [修 bug], [`fix/aimer-spin-judge`],
    [`docs/`], [文档、调研], [`docs/localization-survey`],
    [`wip/`], [探索性工作，可能被丢弃], [`wip/planner-stuck-recovery`],
  ),
  caption: [分支命名约定（取自战队仓库现状）],
)

`wip/` 这个前缀值得单独说明。它明确标记「这条分支可能不会被合并」，用于试一个想法但不确定能否走通的情况。给探索留一个正式位置，比让人把半成品堆在本地不敢推送要好——推上去之后，即使最终放弃，下一届也能看到这条路试过并且没走通。

三条纪律：

+ *不直接向 `master` / `main` 推送。* 即使是一行改动，也走分支加 PR。理由在下一小节。
+ *分支从最新的主分支切出。* 开始工作前先 `git pull`，避免合并时产生本可避免的冲突。
+ *分支名要能看出在做什么。* `feat/xxx`、`test`、`new` 这类名字在一个月后毫无信息量。

```bash
git checkout master
git pull
git checkout -b feat/rune-predictor
```

=== 提交信息约定

用 Conventional Commits：`类型(范围): 描述`。战队仓库里的真实例子：

```
feat(sim): 同步 rm_sim_26 至 fe5979a——MuJoCo 哨兵仿真与弹仓建模
fix(planner): 安全监控违例持续性——连续 2 拍才转移状态
fix(aiming,tracking): judge spin by w, not radius (upstream regression)
docs: sync README——多线程隔离/热重载/双侧窄道/软净空等本轮改动
```

常用类型：`feat`（新功能）、`fix`（修 bug）、`docs`（文档）、`refactor`（重构，不改行为）、`test`（测试）、`chore`（杂项）、`build`（构建配置）、`perf`（性能）。

中文描述是可以的，也更适合战队——把「为什么这么改」说清楚比用英文重要。但类型前缀保持英文，方便工具解析和统一检索。

提交信息真正的价值不在标题，在正文。标题回答「改了什么」，正文应该回答「为什么」：

```
fix(tracking): yaw 解缠绕改用增量累加

原实现用 atan2 直接取值，目标从 +179° 转到 -179° 时
被当成转了 358°，滤波器瞬间发散、丢目标。

改为累加角度增量并在跨越 ±π 时补偿 2π。
用 2026-03-15 的小陀螺 rosbag 回放验证：
原实现在 6 rad/s 下 12 秒内丢目标 3 次，改后 0 次。
```

这段信息在两年后仍然有用，而 `fix: 修复 bug` 在两周后就没用了。这是对抗知识蒸发最便宜的手段——写提交信息的时间成本几乎为零，但它是唯一一种#strong[必定]会被后人看到的文档。

#note[
一个实用的判断标准：如果这个提交在半年后引起了 bug，接手的人执行 `git blame` 找到这一行、看到这条提交信息时，他能不能明白当初为什么这么写？如果不能，正文需要补充。
]

=== PR 在战队里的真实作用

这是本章最重要的观点，也是与企业做法差异最大的地方。

在企业里，代码评审的首要目的是#strong[质量门禁]：拦住有缺陷的代码，防止它进入将维护数年的代码库。因此评审是强制的，不通过就不能合并。

在战队里，代码在赛季结束后大概率会被重写，「防止缺陷进入长期代码库」的价值就没那么高。但另一个价值高得多：

#definition[
在学生战队里，代码评审的首要作用不是找 bug，而是#strong[让第二个人知道这段代码存在]。

它是对抗知识蒸发的主要机制。一段只有作者一个人看过的代码，在作者退役的那一刻就变成了黑盒；有第二个人读过并提过问题的代码，至少还剩下一份活的记忆。
]

这个转变会改变几乎所有具体做法：

*不要卡门禁。* 如果规定「必须有人 approve 才能合并」，实际结果通常是 PR 堆积无人处理，最后大家绕过流程直接推主分支。战队里更可行的做法是：PR 必须开，但不阻塞合并；给 24 小时窗口，有人提意见就改，没人提就自己合。目标是让改动被看见，不是让改动被批准。

*评审要留下痕迹。* 「看了，没问题」这样的评论没有信息量。有价值的评论是问题：「这个 0.023 是怎么来的？」「相机断开时这个分支会走到哪？」这些问题及其回答会留在 PR 里，成为后人能检索到的资料。

*PR 描述比代码更重要。* 一份好的战队 PR 描述应该回答：

+ 解决什么问题（现象是什么，怎么复现）
+ 为什么这么改（考虑过哪些方案，为什么选这个）
+ 怎么验证的（跑了什么、结果如何）
+ 还有什么没做（已知的局限）

第 4 条经常被跳过，但它对后人价值极高——它标记出了下一个人可以接着做的地方。

#attention[
战队仓库当前的实际状况是：绝大多数仓库的 Issue 数为 0，PR 也很少。这说明重流程在这里确实推不动，而不是说明协作机制没有必要。

正确的应对不是加强制度，而是把流程削到足够轻：只保留「开分支、写清楚提交信息、开 PR 让人看见」这三件事。这三件加起来每次不超过十分钟，而它们贡献了协作机制大部分的价值。
]

=== 评审时看什么

新队员做评审时常常不知道该说什么。下面五条按优先级排列，看不完就从上往下看：

+ *这段代码在做什么，我能不能复述出来？* 如果读完说不出来，就是最有价值的反馈——不是代码错了，而是它不够清楚，而不清楚的代码会在赛场上出问题。
+ *异常路径怎么走？* 相机断开、串口超时、检测不到目标、参数文件缺失——正常路径大家都会写，问题几乎都出在这些分支。
+ *魔数从哪来？* 每个硬编码的数值都应该有来源：实测、推导、还是抄的。没有来源说明的数值是未来的地雷。
+ *有没有隐藏的耦合？* 新加的全局变量、静态状态、对某个节点启动顺序的隐含依赖，这些会在多线程或不同启动顺序下暴露。
+ *风格问题放最后，能自动化就别人工提。* 缩进和命名交给 `clang-format` 和 CI，人的注意力留给上面四条。

=== 持续集成：从最小开始

CI 在战队里的合理起点非常低：只要能自动回答「这次改动还能不能编译」。

```yaml
# .github/workflows/build.yml
name: build
on: [push, pull_request]

jobs:
  colcon-build:
    runs-on: ubuntu-24.04
    container: ros:jazzy-ros-base
    steps:
      - uses: actions/checkout@v4
      - name: 安装依赖
        run: |
          apt-get update
          rosdep update
          rosdep install --from-paths src --ignore-src -y
      - name: 编译
        run: |
          . /opt/ros/jazzy/setup.sh
          colcon build --event-handlers console_direct+
```

仅此一项就能拦下「忘了提交新文件」「改了头文件没更新 CMakeLists」这类最常见的低级错误，而它们在赛前会浪费掉最不该浪费的时间。

有了编译检查之后，再逐步加：`colcon test` 跑单元测试、`clang-format --dry-run` 检查格式、对纯算法模块跑离线数据回归。不要一开始就搭一套完整的 CI——配置复杂、经常失败的 CI 会被所有人无视，反而不如一个只检查编译但从不误报的简单流水线。

#note[
本书自身的仓库就是一个例子：`RMCV_Tutorial` 的 CI 只做一件事——用固定版本的 Typst 编译 PDF 并部署到 GitHub Pages。它保证了「推上去的源码一定能编译成书」，不多不少。
]

=== 落地清单

一份可以直接贴进战队 README 的最小约定：

+ 不向主分支直接推送，任何改动走 `feat/` `fix/` `docs/` `wip/` 分支。
+ 提交信息用 Conventional Commits，正文写清楚为什么这么改、怎么验证的。
+ 开 PR，描述里写：解决什么问题、为什么这么改、怎么验证的、还有什么没做。
+ PR 不卡门禁：24 小时内有人提意见就改，无人提意见自行合并。
+ 评审优先看「能否读懂」和「异常路径」，风格问题交给工具。

只有五条，每条都能在十分钟内完成。流程的价值来自被执行，不来自完备。
