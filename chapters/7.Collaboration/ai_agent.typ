#import "/template/template.typ": *

=== 先摆正位置

命令行编码代理（Claude Code、Codex 一类）已经成为日常工具。本节不讨论「AI 会不会取代程序员」，只讨论一个具体问题：在 RoboMaster 视觉开发里，这类工具在哪些环节确实省时间，在哪些环节会制造麻烦。

先给出结论，后面再展开论证：

#definition[
AI 代理大幅降低了#strong[写出代码]的成本，但没有降低#strong[确认代码正确]的成本。而在 RoboMaster 里，后者才是瓶颈——调车、标定、复现一次故障所花的时间，远超敲键盘的时间。因此代理的收益集中在「读懂已有代码」和「产出可被自动验证的东西」这两类工作上，而不是「替你写上车的算法」。
]

理解了这条，就能解释后面所有的使用建议和禁区。

=== 工具与安装

本节涉及的两个工具：

#figure(
  table(
    columns: (auto, auto, auto),
    align: (left, left, left),
    stroke: (x, y) => (
      top: if y == 0 or y == 1 { 1pt } else { 0pt },
      bottom: if y == 3 { 1pt } else { 0pt },
    ),
    inset: 5pt,
    table.header([*工具*], [*版本*], [*约定文件*]),
    [Claude Code], [2.1.251], [`CLAUDE.md`],
    [Codex CLI], [0.149.1], [`AGENTS.md`],
  ),
  caption: [命令行编码代理],
)

两者的工作方式类似：在项目目录里启动，它能读写文件、运行命令、查看输出，然后根据结果继续。与网页版聊天的关键差别是它能真正执行命令并看到结果——这既是它的能力来源，也是需要设边界的原因。

VSCode 里有对应的官方扩展（`anthropic.claude-code`、`openai.chatgpt`），把代理面板嵌进编辑器，与终端里使用是同一套后端。

=== 四类真正有用的场景

*一、读懂祖传代码。* 这是收益最高的用途，也最符合战队的实际痛点。

学生战队的代码库有一个特点：写代码的人已经毕业了。接手一个上届的自瞄工程，最难的不是改功能，而是搞清楚「这个 `0.023` 是什么」「为什么这里要反转 yaw 符号」。传统做法是逐行读加 `git log` 考古，一个下午可能只理清一个模块。

代理在这件事上有结构性优势：它可以同时读几十个文件、追踪调用链、交叉比对 `git log` 和注释。提问方式决定效果：

```
装甲板追踪模块的状态机有几个状态？
每个状态的进入和退出条件分别在哪个文件的哪一行？
把跳转条件里出现的所有魔数列出来，标注它们在哪定义。
```

这类问题有客观答案，可以逐条核对，代理答错时你能立刻发现。

*二、写测试。* 测试是代理产出质量最高的一类代码，原因是测试的正确性可以由「它是否真的跑过并通过」来验证，不依赖你相信代理的判断。

给定一个纯函数（例如坐标变换、弹道解算、yaw 角解缠绕），让代理补一组边界用例，通常比人工想得更全——负角度、跨越 $plus.minus pi$、零除、空输入这些情况人容易漏。

*三、查 API 和写样板。* ROS 2 的 API 面很宽，`rclcpp` 的参数回调、生命周期节点、QoS 配置这些写法查文档要翻很久。代理直接给出可编译的骨架，省下的是查文档时间，而正确与否由编译器和运行结果判定。

*四、写文档和提交信息。* 从一段 diff 生成符合规范的提交信息、把一次排查过程整理成 Issue、给模块补 README，这类工作的成本本来就低，但常常因为「懒得写」而被跳过。降低成本会实际提高文档覆盖率。

=== 三条禁区

*禁区一：代理给出的数值参数一律不可信。*

这是最需要强调的一条。问代理「二值化阈值设多少合适」，它会给出一个看起来很合理的数字，并配上一段有说服力的解释。但这个数字不来自你的相机、你的曝光设置、你的赛场灯光——它来自训练数据里的统计规律。

这不是代理「说谎」，而是这类问题本来就没有脱离具体设备的答案。判断规则很简单：

#attention[
任何需要用你的硬件、你的场地、你的数据才能确定的数值，代理的输出只能当作待验证的起点，不能当作结论。包括但不限于：二值化阈值、灯条长宽比范围、卡尔曼滤波的过程噪声和观测噪声、PnP 的重投影误差门限、弹道补偿系数、各类超时时间。

这些必须由你在实际条件下测出来。
]

*禁区二：上车运行的代码，必须有人完整读过。*

代理可以写出编译通过、看起来合理的代码，但「编译通过」和「在赛场上不会炸」之间距离很远。多线程的竞态、异常路径的资源释放、边界条件下的数组越界——这些问题不会在编译期暴露，也未必在你的几次测试里出现。

规则：任何要跑在车上的代码改动，必须有一个人能逐行解释它在做什么。做不到就不要合并。这条规则同样适用于人写的代码，只是代理让「快速产出大量看不懂的代码」变得容易了很多。

*禁区三：代理说「已修复」不等于已修复。*

代理经常会在改完代码后声称问题解决了，而实际上它只是读了代码觉得逻辑对了，并没有运行验证。这属于把「合理的解释」当成了「验证过的结论」。

对待代理的结论，用与对待人相同的标准：

#figure(
  table(
    columns: (auto, auto),
    align: (left, left),
    stroke: (x, y) => (
      top: if y == 0 or y == 1 { 1pt } else { 0pt },
      bottom: if y == 5 { 1pt } else { 0pt },
    ),
    inset: 5pt,
    table.header([*代理说*], [*你要问*]),
    [「已修复」], [跑了什么命令？输出贴出来。],
    [「测试通过」], [哪个测试？在哪台机器上、什么编译选项？],
    [「根因是 X」], [有没有别的解释也能产生同样现象？怎么排除的？],
    [「性能提升了」], [提升前后各测了几次？波动范围多大？],
  ),
  caption: [对代理结论的追问],
)

这套追问不是针对 AI 的特殊要求，它就是第一篇《软件工程基础》里讲的工程标准。代理只是让人更容易跳过这一步，因为它的表达比人更流畅、更自信。

=== 用约定文件固化项目规则

每次都重复交代「这个项目用 ROS 2 Jazzy」「提交信息要用 Conventional Commits」是浪费。两个工具都支持在项目根目录放一份约定文件，启动时自动读取：Claude Code 读 `CLAUDE.md`，Codex 读 `AGENTS.md`。

这份文件应该写什么，取决于哪些信息是「代理猜不到但必须知道」的。一份适合 RoboMaster 视觉工程的骨架：

```markdown
# 项目约定

## 环境
- Ubuntu 24.04 + ROS 2 Jazzy，不要给 Humble 或 Foxy 的写法
- 构建：`colcon build --symlink-install`，不要用裸 cmake
- Python 环境用 uv 管理，不要建议 conda

## 代码
- C++17，遵循 Google C++ Style
- 新增节点必须能通过 `colcon test`
- 不要修改 `config/` 下的标定参数文件——那是实测值

## 硬件相关（重要）
- 涉及阈值、噪声协方差、弹道系数的数值，不要凭经验给。
  给出如何测量的方法，数值留 TODO 由人填。
- 相机：大恒 MER 系列，SDK 在 /opt/Galaxy_camera
- 电控通信：自定义串口协议，见 docs/protocol.md

## 提交
- Conventional Commits，中文正文可以，标题用英文类型前缀
- 不要自动提交或推送，改完等人确认
```

其中「硬件相关」那一节是 RoboMaster 项目特有的，也最值得写。它把禁区一变成了对代理的常驻指令，减少了每次都要自己警惕的负担。

#note[
`CLAUDE.md` 和 `AGENTS.md` 应该提交进仓库。它们和 `.vscode/settings.json`、`.clang-format` 属于同一类东西：把团队约定固化成机器可读的形式，让每个克隆仓库的人（和每个人的代理）得到一致的行为。
]

=== 一个实际的工作流

以「上届的自瞄在快速小陀螺下丢目标」这类任务为例，说明代理应该出现在哪些环节：

+ *定位代码*（用代理）。让它找出追踪模块的状态机、丢失判定条件、以及所有相关阈值的定义位置。产出是一张文件与行号的清单，你逐条核对。
+ *形成假设*（人）。看完代码后判断可能原因：是丢失判定太激进，还是预测模型在高转速下不适用。这一步依赖对物理过程的理解，代理帮不上。
+ *构造可复现的验证*（用代理）。让它写一个离线回放脚本，从 rosbag 里读一段小陀螺数据，跑追踪模块并打印状态跳转。有了这个脚本，后续每次改动都能在几秒内验证，而不必去场地跑车。
+ *改代码*（人主导，代理辅助）。改动本身可以让代理写，但你要能解释每一行。
+ *验证*（人）。跑回放脚本看状态跳转是否符合预期，再上车实测。代理的「应该修好了」不作数。
+ *记录*（用代理）。把整个排查过程整理成 Issue 或提交信息，包括试过但无效的方向。

注意第 2 步和第 5 步没有代理。这两步恰好是整个流程里最需要判断力的地方，也是学习真正发生的地方。

=== 给新队员的一条建议

代理会让你在不理解的情况下也能产出能跑的代码。短期内这提高了产出，长期看它会让你停止成长——因为理解是在「卡住、查资料、想明白」的过程里建立的，而代理正好消除了卡住。

一个可操作的原则：#strong[允许代理帮你做你已经会做的事，尽量自己做你还不会的事。]

第一次写卡尔曼滤波，自己推一遍、自己调通，即使花三天。第十次写节点样板，让代理生成。前者是在建立能力，后者是在节省时间——把这两件事搞混，是新队员使用代理时最大的风险。

这不是保守。代理写不出你不会验证的东西：当代理给的滤波器发散时，能看出是过程噪声设置不当而不是重启一遍再问一次，这个能力只能自己练出来。
