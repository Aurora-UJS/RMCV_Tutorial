#import "/template/template.typ": *

=== 从比赛日往回排

企业里的排期是向前推的：这个功能大概要两周，那个要一周，加起来一个月，所以下个月发布。战队不能这样排——比赛日期由组委会确定，不会因为进度延后而推迟。所有计划必须从比赛日往回倒排。

倒排的第一步是承认一件事：#strong[赛前最后两周不应该有新功能。]

这两周要用来做完全不同的事——反复跑车、暴露偶发故障、修已知问题、准备备件和回滚方案。一个在赛前三天合并的新特性，即使功能正确，也可能引入没人见过的边界情况，而此时已经没有时间发现它。

由此得到赛季的基本节奏（以往届安排为参考，具体日期以当年组委会通知为准）：

#figure(
  table(
    columns: (auto, auto, auto),
    align: (left, left, left),
    stroke: (x, y) => (
      top: if y == 0 or y == 1 { 1pt } else { 0pt },
      bottom: if y == 7 { 1pt } else { 0pt },
    ),
    inset: 5pt,
    table.header([*阶段*], [*相对时间*], [*视觉组的主要工作*]),
    [招新与培训], [新赛季开始后 1--2 个月], [基础培训、跑通环境、读上届代码],
    [方案确定], [赛前约 5 个月], [看新赛季规则改动、定技术路线、拆分模块],
    [主力开发], [赛前 3--5 个月], [写代码、单模块验证、仿真],
    [整车联调], [赛前 1--3 个月], [与电控、导航联调，实车测试],
    [冻结与打磨], [赛前 2 周], [不合新功能，只修 bug 和调参],
    [比赛期], [—], [现场问题处置，改动必须可回滚],
    [复盘与交接], [赛季结束后], [写复盘、整理文档、带新人],
  ),
  caption: [赛季节奏（相对时间，具体以当年赛程为准）],
)

用 GitHub Milestone 把这些节点建出来，把 Issue 挂上去。这是唯一值得用的「项目管理工具」——它不需要每天维护，只在建立时投入一次，之后自动提供「距离下一个节点还剩多少事没做」的视图。

=== 冻结纪律

「冻结」指的是从某个时间点起，主分支只接受修 bug 的改动，不接受新功能。

这条纪律在战队里特别容易被破坏，因为总有人在赛前想到一个「肯定能提升命中率」的改进。判断标准应该明确：

#attention[
冻结期内，一个改动能否合并，取决于三个问题：

+ 它修的是一个#strong[已经在实车上观察到]的问题吗？（不是「理论上可能有问题」）
+ 它的影响范围能不能说清楚？（改了哪些文件、影响哪些模块）
+ 出问题时能不能在两分钟内回滚？（有没有对应的 tag 或已知可用的提交）

三个问题任何一个答不上来，就等到赛后。
]

回滚能力靠 tag 来保证。每次实车验证通过后打一个 tag：

```bash
git tag -a v2026.3.20-field-ok -m "3月20日实车验证通过：小陀螺 6rad/s 稳定命中"
git push origin v2026.3.20-field-ok
```

比赛现场出问题时，回到最近一个已验证的 tag 是最可靠的手段：

```bash
git checkout v2026.3.20-field-ok
colcon build
```

tag 的信息里要写清楚「验证到什么程度」，而不只是「能跑」。「小陀螺 6 rad/s 稳定命中」和「能跑」在赛场上是完全不同的信息。

=== Issue 当队史备忘录用

企业里 Issue 是任务追踪工具：建任务、分配、跟进、关闭。这套在战队里推不动——没有强制力，没人会主动更新状态。

但 Issue 还有另一种用法，成本低得多而价值高得多：#strong[当作可检索的队史备忘录]。

具体写什么：

*一、踩过的坑。* 花了半天才搞清楚的问题，写一条 Issue 记下来，然后关闭。标题写现象，正文写排查过程和结论。

```
标题：大恒相机在 Jazzy 下偶发掉帧，根因是 USB 自动挂起

现象：连续运行约 20 分钟后帧率从 200 掉到 60，重插 USB 恢复。

排查：先怀疑是曝光自适应（排除，固定曝光仍复现），
再怀疑是 SDK 缓冲区（排除，缓冲区未满）。
最后 dmesg 里看到 usb autosuspend 相关记录。

结论：内核 USB 自动挂起导致。禁用方法：
  echo -1 > /sys/module/usbcore/parameters/autosuspend
已写进 setup 脚本。

验证：禁用后连续跑 2 小时无掉帧。
```

这条 Issue 的价值在两年后——下一届遇到同样现象时，在仓库里搜「掉帧」就能找到。这是所有文档形式里检索性最好的一种，因为它天然带关键词，且和代码在同一个地方。

*二、为什么这么设计。* 做了一个非显然的技术决策时，记录下来：考虑过哪些方案、为什么选这个、被否决的方案问题在哪。后人想改这里时，能先看到当初的理由，而不是重新踩一遍同样的坑。

*三、明确的待办。* 「这里先用了简化模型，高速时会有偏差，以后要换」——写成 Issue 挂在下一个 Milestone 上，比写 `// TODO` 更容易被看到。

三类都不要求跟进和状态维护，写完可以直接关闭。它们的价值在于可检索，不在于流程。

#note[
判断一条信息该放哪里：

- 只与这一次改动有关 → 提交信息
- 与这段代码的当前行为有关 → 代码注释
- 与「为什么是这样」「试过什么没走通」有关 → Issue 或 PR 描述
- 与整个模块怎么用有关 → README

四者不重复，但都指向同一件事：让信息在原地可被找到，而不依赖某个人还记得。
]

=== 交接

交接是学生战队特有的、且没有任何企业经验可以照搬的环节。它决定了下一届是从上届的终点开始，还是从零开始。

失败的交接有一个共同特征：交接内容是「口头讲一遍 + 一个塞满文件的网盘」。三个月后新队员遇到问题时，既想不起来讲过什么，也不知道网盘里哪个文件是当前有效的。

有效的交接清单，按重要性排列：

+ *能跑起来。* 新队员按仓库 README 从零配置环境、编译、跑通仿真或回放。这一条不通过，其余都是空的。交接前让一个没接触过项目的人实测一遍，卡住的地方就是文档要补的地方。
+ *参数从哪来。* 每个标定文件、每个阈值的来源和测量方法。这是最容易丢失的知识——参数文件本身在仓库里，但「这个值是怎么测的、什么条件下需要重测」通常只在人脑子里。
+ *已知问题清单。* 哪些功能有已知缺陷、哪些场景下会失效、哪些代码是临时方案。诚实地写下来，比交接一个看起来完美的系统有用得多。
+ *试过但没走通的方向。* 这一条几乎总被遗漏，但它防止下一届重复消耗。写清楚试了什么、为什么失败、是路线问题还是实现问题。
+ *硬件与环境的隐含依赖。* 相机型号和 SDK 版本、串口设备名和 udev 规则、网络配置、内核参数改动——这些不在代码里，但少一条系统就跑不起来。

这份清单应该以文档形式提交进仓库，而不是发在群里。群消息会沉底，仓库里的文档会跟着代码一起被克隆。

#note[
一个检验交接质量的方法：让下一届的队员在没有上届在场的情况下，独立完成一次「从空白机器到实车跑通」的全流程。中途每卡住一次，就是一条交接遗漏。这个测试最好在上届退役前做，而不是之后。
]

=== 复盘

赛季结束后的复盘，写给下一届看，不是写给自己。

值得写的三类内容：

*技术决策的回顾。* 赛季初定的技术路线，事后看是否正确。选了传统视觉还是深度学习、用了哪种预测模型、通信协议怎么设计——哪些决策事后证明是对的，哪些应该换一种做法。

*故障记录。* 比赛中出现的每一次异常，包括没能当场定位的。「决赛第二局第三分钟突然丢目标，赛后未能复现」这样的记录也有价值——下一届出现类似现象时，至少知道这不是新问题。

*本可以更早做的事。* 事后看，哪些工作如果提前两个月做会显著改善结果。这一条对下一届排期的指导价值最大。

复盘文档放进仓库的 `docs/` 目录，按赛季命名。几届积累下来，它会成为战队最有价值的资产——比任何一版代码都值钱，因为代码会被重写，而这些判断不会过期。

=== 小结

这一篇讨论的所有机制——分支约定、提交信息、PR、Issue、Milestone、交接清单、复盘——都指向同一个目标：

#definition[
让知识独立于人存在。

学生战队最稀缺的资源不是时间，也不是经费，而是#strong[延续性]。每一届都从头开始的队伍，无论单届成员多优秀，都很难走远；而能把上届的判断和踩过的坑继承下来的队伍，会一届比一届走得更快。

工程协作的所有规矩，本质上都在服务这一件事。
]
